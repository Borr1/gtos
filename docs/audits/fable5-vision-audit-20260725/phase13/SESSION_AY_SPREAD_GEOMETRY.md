# Session AY — the spread-geometry repair (wave 13, B1800–B1849)

Read first: AW's result (`phase12/SESSION_AW_SEPARABILITY_MINE_RESULT.md`, §0.7–0.8 and §4.3
are your charter), `WAVE_11_WORKING_AGREEMENT.md` §2/§4 (all standing deltas bind), CLAUDE.md
§3–§4. Owner authority: Borhen's standing ratifications ("proceed as proposed and recommended";
"no conservative passiveness").

**What AW measured and this session ships:** `spread_r = spread_price / sl_distance_price`, so
a cost above 1 R means the proposed stop is NARROWER than the round-trip spread — a trade that
is structurally paying more to enter than it can lose by being wrong. The B7.5 pool carried
16.5 % such rows holding **49.6 % of its entire loss** (max 18.9 R). **The estate shares the
disease**: at the mid band, 33.7 % of 21,769 priced archive trades exceed the sealed
`spread_r ≤ 0.10` limit and 52.9 % exceed `total ≤ 0.15`. Applying the sealed limit
out-of-window: **all four armed sleeves improve** — `sub_mid_dn_revert` +0.121 → +0.547 R/day
(p 0.163 → **0.023**), `asia_pdl_fade` −0.902 → +0.095 with clean controls (cheapest-drop
inverts it, random moves nothing) — but it beats random on only 10 of 23 sleeves, so it is
**a per-sleeve repair, not an estate-wide rule**.

## Work orders

**AY-1 — The per-sleeve repair, measured sleeve by sleeve.**
For every estate sleeve (armed four first): gate the sleeve WITH and WITHOUT the
spread-geometry floor at the ratified rule (RECORDED, band alongside, declared cuts, decay
folds, maxbars share). Use AW's controls on every sleeve where the filter helps: the
cheapest-drop inversion control and the random-drop null — a filter that only "helps" the way
random helps is noise. Declare the looks first (family ratchet). Deliverable: the per-sleeve
verdict table — REPAIR (helps with clean controls) / NEUTRAL / HARMFUL — with the four armed
sleeves' rows written as activation-grade evidence.

**AY-2 — The repair MECHANISM, built where it belongs: generation.**
A stop narrower than the spread should never be proposed. Build the generation-side floor
(`sl_distance_price ≥ k × spread` or the per-sleeve threshold AY-1 supports), default-off,
gated the `--tags`/`--frontier-exits` way (no config byte, no token disturbance), with AR's
`DEFAULT_CONFIG`/AST-walk hardening on any new key. H1/R2 membership check on every file.
Where the right fix is a generator's stop-sizing defect rather than a filter (AW §4.3 calls
the cost tail a GENERATOR defect), fix the generator and say which sleeves that touched.

**AY-3 — The armed-four activation package.**
If AY-1 confirms AW's out-of-window numbers with clean controls at the ratified rule, write
the activation dossier for turning the floor ON for the armed four (the FN-ceremony shape:
exact flag, per-sleeve thresholds, expected before/after economics on recent folds, stop
condition, rollback). The orchestrator executes the ceremony; you arm nothing.

**AY-4 — Close AW's residuals.**
The `sub_xvol_pullback` near-admission under the filter (raw p 0.0020 vs the family bill) —
state honestly what family it would need and file it; the estate-wide 33.7 %/52.9 % exceedance
map as a receipt other sessions can cite; and AW's dirty row-cache
(`.aw_diagnostic_pool_v1.jsonl.gz`) — decide committed-or-regenerable and leave the tree clean.

## Done means

Result doc + receipts under `phase13/receipts/`, blocks B1800–B1849, scoped A/B green vs the
zero baseline, ledger/repair-queue appends, honest §what-I-got-wrong, handoff list. Never touch
the VPS; never run broker-capable scripts; never edit `config/agent_config.yaml`,
`config/profiles/redacted_account.yaml`, or any R2-bound path. Both live books trade FOUR sleeves;
everything you build stays inert until the orchestrator's ceremony.
