# Session BC — the command center (wave 13, B2000–B2049)

Owner authority: Borhen 2026-07-30, "run all." Read WAVE_11_WORKING_AGREEMENT.md,
phase11/FIVE_SLEEVE_OPERATOR_PAGE.md, phase4/CANARY_OPERATOR_PAGE.md,
scripts/book_sleeve_telemetry.py, the monitor daemon's output format.

**The objective:** the charter's command-center link — the owner sees the whole system on one
page without a terminal crawl. Build: (1) a single generated OWNER dashboard (markdown/HTML,
regenerated by one read-only script the orchestrator can run against exported host state):
both accounts' equity/headroom/positions, per-sleeve fills + R + stop-condition status,
distance-to-target, token expiries, armed set vs config truth, last heartbeat ages, and the
"abnormally quiet" alarm once BB defines it. (2) The MT5-MCP integration DESIGN (build 6060+
has native MCP): what the terminal's MCP exposes, what oversight it replaces, security posture
(read-only first), and the wiring plan — design + prototype against exported data, nothing
touching the VPS. Deliverable: the tool + the page + the MCP design doc. Never run
broker-capable scripts; never edit config or R2 paths.
