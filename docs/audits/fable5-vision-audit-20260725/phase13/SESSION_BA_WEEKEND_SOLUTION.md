# Session BA — the redacted_account weekend-holding solution (wave 13, B1900–B1949)

Owner authority: Borhen 2026-07-30, "run all... i do give explicit approval." Read
WAVE_11_WORKING_AGREEMENT.md (all deltas bind), FIRM_RULES_V1.json (the measured prohibition:
redacted_account FUNDED accounts may not hold over weekends; challenge phases are legal), the
carry-tier artifacts (AD/N — swap and hold-time evidence per sleeve).

**The objective:** the priced, buildable policy that makes the book legal on a redacted_account
FUNDED account — the known Stage-2 blocker, built before it blocks. Design and price at least:
(a) Friday-flatten (close all before the weekend, measured cost = foregone weekend-crossing R
per sleeve from the archive walks), (b) Friday entry-embargo (stop new entries N hours before
close so horizons complete), (c) per-sleeve hybrid (flatten only sleeves whose measured holds
cross weekends). Price each against the armed-four book at the ratified rule, chronological
folds. Build the winning mechanism config-gated the --tags way (no config byte, default off),
with AR's DEFAULT_CONFIG hardening. Deliverable: the owner package with the number per option
and the switch. Never touch the VPS; never edit agent_config/redacted_account profiles or R2 paths;
arm nothing.
