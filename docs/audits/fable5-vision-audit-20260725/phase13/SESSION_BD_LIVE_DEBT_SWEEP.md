# Session BD — the live-path debt sweep (wave 13, B2050–B2099)

Owner authority: Borhen 2026-07-30, "run all." Read WAVE_11_WORKING_AGREEMENT.md, then the
debt list: Session P's three packet decisions (phase3/PACKET_EMITTER_CARRY.md §7 — adopted by
AP, implementations handed off because each touches book_owner.py on the armed path), AS's
remaining handoffs, AU §1.4's hardening builds, AR's cap-absorption hardening note, AQ §6a's
want=budget+64 inertness mitigation (design the measured fix, not the unmeasured one), and the
energy_agri scale-out question (twice-measured: AD -0.308, AU +0.2302 error — build the
plain-exit alternative as a priced, inert option).

**The objective:** every filed-but-unbuilt improvement that touches the live path, built INERT
and tested, so the next carry window ships them all at once (the orchestrator composes carries;
AZ's manifest shape is the standard). H1/R2 membership check on every file; AR's
DEFAULT_CONFIG rule on every new key; behavioural tests not line-pins (the wave-12b lesson).
Deliverable: the debt ledger (built/deferred-with-reason per item) + the payload set staged for
carry. Never touch the VPS; arm nothing; never edit agent_config/profiles or R2 paths.
