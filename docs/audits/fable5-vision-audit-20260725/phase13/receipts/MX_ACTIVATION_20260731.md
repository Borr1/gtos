# mx_btcusd @ target_5R ACTIVATED on FTMO — 2026-07-31 ~01:26 UTC

**The estate's first sealed-standard admission is trading.** Host commit `d6c9c4b19`
(after `7017c6745`); backups + released flags at
`C:\Users\MSI\Documents\gtos-mx-activation-backup-20260731T011111Z\` with manifest.

## Executed exactly per Session AZ's ceremony page

- **Preflight PASS**: three files at expected pre-carry bytes; the host's unpinned
  `run_book.py` RECOGNISED as variant `96ac1b566dc8` (Session I safety spine) and the
  verifier named its matching payload — the variant-table design worked first try.
- **Carry**: four files in the mandated order (execution_packets → order_router →
  book_owner → run_book), each sha256-verified on the host before the next; both books flat
  and both kill flags held throughout.
- **`--check all` PASS, then `--check behaviour` PASS again post-restart**: the adopt path
  rehydrates the sleeve at `final_target_r=5.0`; the banner renders all eight wired cells
  (B1852's landmine dead); no frontier key exists in any config (the token binds config
  bytes — **`config_digest=ffe16657feaf` unchanged**, both tokens undisturbed).
- **Activation**: FTMO tags = the armed four + `mx_btcusd_d1_donchian_20_breakout`;
  supervisor gains a per-book `frontier` field (FTMO only); FTMO firing ledger cleared at
  arming (B365). redacted_account **untouched** — four sleeves, no frontier, `tfs=[16388]`.
- **The two proving lines, §8's whole point, both present**:
  `FRONTIER EXIT CONTRACT IS ON for mx_btcusd_d1_donchian_20_breakout: target_5R (broker
  TP 5.0R)` and `BookLauncher starting: tfs=[16388, 16408]` — the contract landed AND the
  tag landed, independently evidenced. Heartbeats healthy on the new pids after release.
- **`CARRIED_STATE.json` now lives on the host** (path → sha256 for every ceremony-touched
  file) — AZ's handoff item 6, so the next ceremony reads state instead of reconstructing.

## What it is worth — the honest numbers, from the page

Registry confidence **0.025** (0.32 % of book weight) — economically small **by design**;
the sleeve's job is the forward record (OD-AI-5: admission = eligibility). Planning number:
**+0.198 R/day** (recent folds, per the ratified population rule) — never the +0.982
full-window mean. Admits at **two of three cost bands**. The admission is contingent on two
repairs both now on this host (AQ's time-stop unit, AU's wiring) — any citation names both.
Expect long silences: a D1 breakout sleeve at 0.32 % weight.
