# Session CA — the revival gates + the fill-truth restatement (wave 14, B2100–B2149)

Read first: `phase13/receipts/CARRYCOND_FETCH_20260730.md` (the data you run on, its clock
declaration, and its honest depth limits), `phase13/SESSION_BB_SLEEVE_SUPPLY_RESULT.md`
(DECISIONS≠FILLS — the suppression measurement you restate the book with),
`phase14/TRAINING_LANE_RATIFICATION.md` (your revival candidates are the incubation lane's
first intake), `WAVE_11_WORKING_AGREEMENT.md` (all standing deltas bind), CLAUDE.md §3–§4.
Owner authority: Borhen's 2026-07-31 training-lane ratification and the standing directives
(your own judgment; build/improve/fix, never refute-and-stop; Workflow opt-in standing).

**The objective in one sentence:** the three data-blocked sleeves get their first honest
gates on the fetched bars, and the armed book's published economics get restated at the
cadence it actually fills — both as owner-readable pages.

## Work orders

**CA-1 — Revival gates for the three sleeves.** The fetch broke the blockade
(`missing: []` on all three): `metals_softband` (XAUEUR/XAGEUR/XAUAUD/XAGAUD H4),
`sub_mid_dn_revert`'s commodity members (CORN_c 2023+, COTTON_c 2025+ — shorter histories,
say so in any pooled figure), `vp_euidx_pocgrav` (GER40/UK100 M1, 2026-04-27+ depth). Data:
`/Users/borr/GTOSActive/repo/data/mt5_research_exports/bridge_ftmo_carrycond_h4_m1_20260730/`
(sidecars sanctioned; the loader accepts). Re-derive members on these bars, walk at the
**ratified rule** (RECORDED population, band alongside, cut rules declared BEFORE gating,
chronological fold table, maxbars share, broker-true costs with the era/spread models), and
price the carry at each firm's measured swap. Verdict per sleeve: `REVIVAL_CANDIDATE` /
`CONDITIONAL` / stays dead — with controls (cheapest-drop, random-drop) on any filter you
apply. Declare looks to the family ratchet before gating.

**CA-2 — File, don't arm.** Any `REVIVAL_CANDIDATE` gets an incubation dossier in the AS
shape: proposed weight (≤0.05-class), pre-registered stop rule, pre-registered promotion
rule, expected economics on RECENT folds. The incubation registry is Session CC's build —
if it lands before you finish, register; if not, the dossier is the deliverable.

**CA-3 — BB's fill-truth restatement.** BB measured that one-position-per-broker-symbol plus
per-(sleeve,symbol,day) dedup suppresses ~32 % of armed-four decisions (crypto 48.1 %,
sub_xvol 60.2 %). Restate the armed book's published per-day economics and cadence at
fill-truth: expected %/month, expected book-days/month, and the per-sleeve deltas, as ONE
page Borhen can read against the monitor. If the suppression changes any published stop
condition's arithmetic (AS's five-sleeve monitoring page), say which and by how much.

**CA-4 — The armed set's first live week, read honestly.** The books have been armed since
2026-07-29/30 with real fills accumulating. Read the live records (packets/deal exports
under `research/operations/` and the receipts — never touch the VPS) and write the
first-week reconciliation: fills vs expectation at fill-truth cadence, any sleeve tracking
its pre-registered stop condition. Small n — report it as tracking, not verdicts.

## Done means

Result doc findings-first + receipts under `phase14/receipts/`, blocks B2100–B2149, looks
declared before gating, scoped A/B green vs the ZERO baseline with the tool-emitted
`gtos-ab-receipt-v1` fence (prose receipts are a filed defect — three occurred), honest
"what I got wrong", handoff list. Never touch the VPS; never run broker-capable scripts;
never edit `config/agent_config.yaml`, `config/profiles/redacted_account.yaml`, or any R2-bound
path. You file candidates; arming is the owner's ceremony.
