# Session AU — wire the runnable exits, restamp the estate at live truth (wave 12, B1550–B1599)

Read first: `WAVE_11_WORKING_AGREEMENT.md` (wave-12 deltas at §4 apply), AQ's result
(`phase11/SESSION_AQ_CONTRACT_TRUTH_RESULT.md` — especially §6a's live hazards and the handoff),
AR's result §5 (the live-contract lesson), AP's §8 (what was deliberately not done). Owner
authority: Borhen's standing 2026-07-30 ratifications. Standing directives apply (judgment;
build-don't-refute; Workflow opt-in standing).

**The wave-11 convergence this session executes:** three sessions independently found that the
estate's published economics describe contracts the live book does not run — the one admission
was contingent on a contract repair, the nearest near-miss evaporates at the live exit, and the
largest labelling error (0.88 R/day, `asian_fade`) is a contract interaction nobody walked. The
next distance toward activation is closed by making **published == runnable**.

## Work orders

**AU-1 — Wire the frontier exits as runnable spec exits.**
`target_5R` (for `mx_btcusd`, the admission's own contract) and `target_4R` (for
`sub_xvol_pullback`, AK's frontier) as first-class, testable exit contracts the live engine can
run. AK's safe order binds: **confidence weight first, spec second**; H1/R2 membership check on
EVERY file before edit (AQ's repair path `execution_packets.py` is unbound; `exit_policy_v4.py`
and `config/agent_config.yaml` are bound — if the wiring cannot avoid a bound file, STOP and
report the seal cost as one line rather than paying it silently). Default-off / not armed:
`sub_xvol_pullback` is trading real money on its current exit — the wiring must be inert until
the orchestrator's ceremony flips it. Include AR's hardening lesson: the flag reads through
`DEFAULT_CONFIG` (the `bridge._bool` KeyError class is a silent live outage — AR filed the
AST-walk test; extend it to your new keys).

**AU-2 — Restamp the estate's published economics at the live contract.**
AQ built the three-contract machinery (`PUBLISHED` / `LIVE_TRUE` / `REPAIRED`) and ran it on the
D1 cohort. Extend it across the estate: the 10 `RESTAMP_PUBLISHED_ECONOMICS_AT_THE_LIVE_CONTRACT`
repair rows, `asian_fade`'s trailing-runner interaction (the 0.88 R/day gap at ZERO truncation —
walk the interaction, name the mechanism), and **every exit sweep now reports its `maxbars`
share** (AR's transferable finding: AL's published winner carries 20.6 % unreported). Where the
restamped number changes a tier or verdict, say so per sleeve.

**AU-3 — The five deliberate-horizon decisions, at the ratified rule.**
`mx_us100`, `mx_us500`, `mx_ger40`, `mx_us30`, `mx_nzdjpy` were BETTER under the accidental
one-bar stop (both ATR-MR sleeves flip positive). Re-gate the D1 time-stop grid at the ratified
rule (RECORDED, band alongside, declared cuts — AD's existing grid is flat/ALL_ERAS and AQ
measured that shortcut misleads 1.9×). Deliverable: a declared short-horizon contract per sleeve
that wants one, gated, with its chronological folds.

**AU-4 — Close the small debts wave 11 left.**
The `sub_mid_dn_revert` downstream rebuild (~30 machine-minutes, AQ handoff 4);
`SLEEVE_DOSSIER_V1.json`'s stale `time_stop_bars_m15: 96` on 12 sleeves (AQ's completeness-sweep
row); the learning-lane raise-guard fail-open AP measured at the parent (fix with a test, in the
default-off lane); and AP's one-line queue correction is already the orchestrator's.

## Done means

Result doc + receipts under `phase12/receipts/`, blocks B1550–B1599, scoped A/B green vs the
zero baseline, ledger/repair-queue appends, honest §what-I-got-wrong, handoff list. Never touch
the VPS; never run broker-capable scripts; never edit `config/agent_config.yaml` or
`config/profiles/redacted_account.yaml`. Both live books trade FIVE sleeves — anything you wire stays
inert until the orchestrator's ceremony.
