# Session AR — conditioning to sizing, and the member-repair program (wave 11, B1450–B1499)

Read first: `WAVE_11_WORKING_AGREEMENT.md`, AO's result doc §0/§2/§5 (the vr finding and why
the pair admission died), the repair queue (`phase6/receipts/REPAIR_QUEUE_APPEND.jsonl`, 127
rows). Owner authority: Borhen's 2026-07-30 blanket ratification; your judgment governs the how.

**The objective in one sentence:** convert AO's measured monotone `vr` structure into a
default-off sizing tilt through the consumer that already exists, and spend the repair queue's
best rows toward a second admission.

## Work orders

**AR-1 — The `vr` sizing tilt, built and priced, never armed.**
AO measured `vr` monotone in net R inside `sub_xvol_pullback`'s own firing range (Spearman
+0.4073, perm p 0.00025, tertile net R/trade 0.512 → 1.111 → 2.124 — a 4.1× spread). Build the
tilt through `admission.py`'s existing Kelly-lite conviction consumer, **default-off**, gated
the way `--tags`/`--recover-pre-gap-bar` are (no `agent_config.yaml` byte, no profile byte).
Then price it: cost-true archive A/B of the armed three-sleeve book with and without the tilt,
chronological folds published, sized on recent folds. Deliverable: an owner package with the
number and the switch — arming is Borhen's, not yours. Check the identity-filter rule first
(agreement §2) and the resolution floor on any p you quote at small n.

**AR-2 — The member-repair program: spend the queue toward a second admission.**
127 rows stand. Pick by expected value, not order: AF's two coherent families first (the only
2 of 30 that passed the two-clause coherence test), then AD's exit-geometry repairs on the
members nearest admission on RECORDED. Declare looks before gating (family V3 + ratchet, cut
rules declared). The honest target: **either a second admission at the sealed rule, or a
short-list of the three nearest misses with exactly what each is missing** — n, p at rank,
band stability, or a named repair. Do not manufacture admissions; the estate's credibility is
worth more than a row.

**AR-3 — `asia_pdl_fade`, reconciled.**
AK measured +0.085 R/day with 5/5 folds on one stop cell; AO refuted its regime
pre-declaration in the OPPOSITE direction with confidence. Reconcile the two (they are about
different levers), walk its best cell on RECORDED with the band table, and if it gates clean,
file it as a challenge-book candidate row with its decay table. If it does not, route the
repair and say why.

## Done means

Result doc + receipts under `phase11/receipts/`, blocks B1450–B1499, scoped A/B green vs the
zero baseline, ledger + repair-queue appends, handoff list. Never touch the VPS; never run
broker-capable scripts; never edit `config/agent_config.yaml` or
`config/profiles/redacted_account.yaml`.
