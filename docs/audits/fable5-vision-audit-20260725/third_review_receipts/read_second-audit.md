# read_b75-campaign (af47320b756d2911e)

## HEADLINE FACTS
- The '15.3 GB is a GiB/GB echo of 8.61 GB' claim (SECOND_AUDIT.md:619-622) is refuted twice — by arithmetic (B81: ratio 1.779 matches no unit conversion; they are two different /usr/bin/time -l fields) and by measurement (B83: one run emitted maxrss 7.716 GB AND footprint 14.838 GB) — and worktree CLAUDE.md:143-144 still carries the refuted claim plus the corrected-scope error (8.61 GB is a 2-day sub-window, not a month arm; no full-arm peak-RSS measurement exists).
- B84 [MEASURED]: killing every source-layer copy (8.4x less memory at the partition boundary, output-identical on 1.78M rows) moved arm peak RSS +3.5% (7.716 -> 7.986 GB, wall -9.0%) — the audit-derived <=3 GB/arm gate has no valid derivation, ~5.5 GB of the peak is unattributed, and E10's composed-levers path to 10-16x fails at its first lever.
- The W7 live window does not measure the W7 book (B63/B99b): only 11.5% of core-8 confidence weight ever traded, zero of 145 placements came from train-validated sleeves, 43.4% came from the two registry-marked breadth_falsified sleeves — so OD-1's premise ('W7 underperformed live') is not supported by that window, in either direction.
- B62: SECOND_AUDIT:159-160's FTMO -5.3% was half pre-W7 fleet (-2.74%) and half book (-2.55%); redacted_account's fleet era was net positive, collapsing the audit's -3.0/-3.9% range to -3.7359% (99.1% W7's).
- B66: the W7 validation charged zero commission at five independent sites while realized commission+swap was 31.6% of the loss (-12.69 R, ~half the R lost); the authorizing MC applied tick erosion with the wrong sign (+0.018 R credit vs ~-0.195 R true charge on USDJPY); stops fill clean — the friction gap is commission, not fills (F38/F39/F40).
- B67: the V4 contract-size mechanism for the FTMO-vs-FN sizing asymmetry is refuted (equal-size pairs ratio 1.411 vs unequal 1.355); the real mechanism is the de-risk governor (size_cap_multiplier 0.5405 vs 0.8537, 'derisking_into_maxdd_wall' 145/145) — the asymmetry is the safety machinery working.
- B27: both audits named EET/EEST; the measured truth is broker wall clock = America/New_York + 7h (US DST calendar; 9/9 transitions on US dates over 4.25 years) — an EU-rule repair passes the plan's own Friday-22:05Z test yet is wrong 1h for ~4 weeks/year, and the sealed-March seam is 2026-03-08, not 03-29. B56 adds a third clock neither audit asked about: FTMO's daily-loss reset is 00:00 CE(S)T while its server runs US DST.
- B8: F13's mechanism was wrong — the engine is right (provenance-refusing atom at selector_v4.py:453-537) and the fixtures are stale; the audit's implied remedy would have introduced cross-stitching into a contract-bound file; fixed via fixtures, engine untouched (44f9a02c6).
- B26 [MEASURED on VPS export]: F2's hedge ('the halt is real on the VPS, unverifiable from here') was wrong — no halt flag exists on the VPS either; the entire brake was one YAML boolean; the audit's presence-of-authorization token prescription was implemented (7c85c3105) and is now the primary brake.
- F1's architecture prescription was executed and validated: SleeveBookPolicy delegates to the exact live functions and reproduces 4,908/4,923 comparable live cycles with zero disagreements and 130/132 transmitted risk percents (B92/B99c) — the declared-live surface has replay representation for the first time; OD-1 chose the broad stack, the reverse of the audit's default recommendation (B98).
- E3 was vindicated operationally: the audit-prioritized shadow reducer found F31 on its first run (replay grants exactly zero gap-through; -8.095 R over 212 level rows) — invisible to ~54,000 lines of byte-proof code; arm ordering unchanged, but the reference arm loses ~2x per risk dollar what the seal states (B33/B34).
- E9's deletion caution was vindicated beyond its own preconditions: B43's producer-side sweep ('what does X write, and who binds that?') collapsed the 219,469-line LOW tier to 1 file / 8 lines; the right deletion unit is the whole route, by owner decision.
- The inherited test-suite frame was wrong by ~60x: the real baseline is ~692-695 bad, closed as 77.4% environment-bound / 9.9% genuine with zero genuine defects on the live decision path (B1, Q4), the committed baseline is not portable across worktrees (B38/B50), and running the suite emits trade-shaped operator notifications (F30).
- B25: the OD-2 additive split frees 2 of 44 bound paths, not 9 — seven verifiers are doubly bound from inside an executing file; B5: the audit's vendor source (origin/deploy-live) holds 638 files, not 661, missing the live dial's validation artifact (CYCLE62).
- B99e's transferable lesson: four of five errors found in Session H's own measuring harness inflated favourable numbers and none was caught by 52 mutation-proven policy tests — 'a validated port measured by an unvalidated comparator is not a validated result' (the audit shared this blind spot: its own profile harness no longer runs, and Session E's forensics generator had ten bugs).

## RECOMMENDATIONS
- Strike the refuted GiB/GB echo claim and fix the 8.61 GB scope in CLAUDE.md H3 (worktree CLAUDE.md:143-144), and add an amendment banner to SECOND_AUDIT.md Section 5.3/E8 row mirroring the F7 banner — cost: one doc edit, prevents a fourth generation inheriting a refuted number.
- The third review should treat the <=3 GB/arm target and any parallel-replay ambition as underived until a tracemalloc attribution run at the RSS high-water mark exists (B85 rec 1: ~20 min, no re-seal) — every Phase 2-5 memory argument currently rests on an unattributed ~5.5 GB.
- Re-scope any OD-1 discussion in the third review around B63/B99b: the W7 live window is evidence about the candidate book + breadth residue, not the core book, and there is no live evidence for OR against the validated core-8; do not let '-5.3%' stand as a book-era number (B62 partition: fleet -2.74% / book -2.55%).
- Carry B66's cost findings into Phase 6 planning as the priority calibration input: a commission model (per-instrument R table from broker truth: USDJPY 0.1948R ... index CFDs 0.0000R), not a fill model — and note the JPY-cross trial returned p=0.00016 negative before cost, an owner decision queued in the G1b receipt Section 10.
- Adopt B99e's comparator discipline as a standing rule for the review: any headline rate must state its denominator class (empty vs unit-bearing cycles), and measuring harnesses need their own adversarial pass — mutation-proven product tests do not cover the meter.
- List the still-open second-audit items explicitly in the third review so they are not presumed done: F14 (event ledger never persisted on sealed path), F15 (cost-threshold keys still zero consumers [MEASURED-now]), F20 (iCloud eviction shape re-flagged by B44), mt5_preflight.py still default-live (CLAUDE.md H6), F6 companion port (waits on OD-2 re-seal batch), Q7/Q8 (suite notifications; tests that encode an open live gate as expected state).
- When citing the clock, cite the rule not a constant (new_york_plus_7 via src/utils/broker_clock.py), and keep the firm-reset registry separate from server-clock rules (B56) — the program has conflated them once; any session-conditioned conclusion drawn from pre-B27 evidence inside DST-disagreement windows needs re-checking.
- For any future deletion or cleanup content in the review, require the producer-side question (what does X write, who binds that) alongside the import graph, and use whole-route retirement by owner decision as the unit (B43).
- Verify F16's residual intent with the owner: LIVE_HANDOFF.md at HEAD retains account login numbers labeled 'operational, owner-shared' (passwords absent) — confirm that labeling reflects a decision rather than an incomplete redaction.

## REPORT
# SECOND_AUDIT.md vs. eight sessions of measurement — claim-by-claim reconciliation

All SECOND_AUDIT citations are `docs/audits/fable5-vision-audit-20260725/SECOND_AUDIT.md` (718 lines, read in full). All B-block citations are `docs/audits/fable5-vision-audit-20260725/IMPLEMENTATION_STATE.md` (3,232 lines, blocks read: B1-B29 headers, B5, B8, B22-B27, B29, B33-B36, B43, B54-B56, B60-B69, B70-B72, B81-B85, B90-B99e, Phase-0 table, C1-C13, Q1-Q8). Tags: session claims are reported with their own tags; things I checked directly today are marked [MEASURED-now].

## 1. Core theses and numbered findings, one line each

**Core theses (§1 verdict table :22-33, §4 :575-584, §6 :687-715):**
- T1: The first audit's ten claims mostly HOLD; E10's "~30×" had no derivation (band ~10-16×), E9's deletion plan was not executable as specified.
- T2 (F1): GTOS contains two competing decision stacks with disjoint evidence chains; the sealed campaign measures the one the go-live dossier ordered off; the declared-live W7 book is unmeasured by any replay (:89-148); resolved by owner decision OD-1 in-document (:150-187).
- T3: The evidence machinery attests byte custody, not economic correctness; nothing recomputes any trade's R from prices (:26, :505-510).
- T4: The rebuild's decision core must be policy-plural — one core, N policy modules, so "replay measures the thing that trades" is true by construction (:699-701).
- T5 (F5): The learning loop is repair-then-connect, not rebuild — dead at exactly its input contract (:261-270, :361-406).
- T6 (F7): The research data layer labels broker time as UTC; replay and live disagree about what hour it is (:284-324).
- T7: The single highest-value decision is not code — it is the owner choosing the activation policy surface, "with the book as the default recommendation because the evidence says so" (:692-697).

**Tier-0 findings:**
- **F1** two stacks, disjoint evidence, campaign measures the ordered-off one; no document decides the activation candidate (:89-148).
- **F2** the documented hard halt has no filesystem existence on this machine; brakes are the platform boundary + committed config gates; invert to presence-of-authorization (:189-211).
- **F3** at HEAD config nothing can trade even unhalted: fleet dies at the packet wall, book at `live_activation_allowed: false` (:212-227).
- **F4** the live surface is a two-way fork; a month of live hardening + `run_book.py` + the book's entire 661-file evidence route exist only off-mainline (:229-259).
- **F5** learning loop one adapter away (model side) / one emission away (data side); blocker = the same schema problem as the perf finding (:261-270).
- **F6** a protective AI-companion safety layer exists, enabled, only on the VPS branch (:272-282).
- **F7** MT5 export path stamps broker wall clock as UTC; replay parses it as UTC; live corrects; session windows shift 2-3 h (:284-324; in-file amendment banner :286-295 already corrects the calendar).

**Lesser findings F8-F28 (table :333-355):** F8 same flag name, opposite semantics on V4 vs book paths; F9 VPS live brake key (`ultimate_book_live_broker_authority`) unknown to HEAD; F10 HEAD book composition contradicts `live_system_of_record.md`; F11 package session tokens are the only sleeve-data path into sealed admission; F12 SHORT-side live R has zero test coverage; F13 the 10 red selector tests are a real package-admission defect misdiagnosed as LFS damage; F14 sealed arms never persist `ledgers["event"]`; F15 `budget.cost_*_threshold_usd` have zero consumers; F16 `LIVE_HANDOFF.md` carries plaintext account logins; F17 replay `normalize_row` silently coerces bad OHLCV to 0.0; F18 `BookLauncher` per-tick brake watches the wrong flag; F19 `fn_smoke_trade.py` bypasses the guard stack via raw `MetaTrader5`; F20 canonical export archive is iCloud-evicted; F21 `broker_order_lifecycle_capture_v4` never captured a row; F22 typed cache duplicates ×2 by bundle-identity keying; F23 the LOW deletion tier would delete the live surface + ML pipeline; F24 the prior audit's Python universe excluded 43 % of tracked lines; F25 12 UTF-8 BOMs break ast tooling; F26 sealed engine `sys.path.insert`s a research route; F27 the contract binds 44 paths, not 42; F28 HEAD is a history graft — 1,006 formative June commits live only in the iCloud legacy repo.

## 2. Status of every headline claim against the newer evidence

### 2a. The four named refutations, verified in detail

**"15.3 GB is a GiB/GB unit echo of 8.61 GB" — REFUTED twice, and the refuted claim still stands in CLAUDE.md.**
- The claim: SECOND_AUDIT.md:619-622 (§5.3, inside the E8 CONFIRMED verdict): *"the oft-quoted '15.32 GB footprint' is a unit-conversion echo of the same measurement (14.27 GiB), and the raw `/usr/bin/time -l` output behind it was never committed."* Echoed at :31 (E8 row).
- Refuted by arithmetic (B81, :2397-2408): 15.32/8.61 = 1.779; no power of 1.024 or 1.0737 reaches it. They are the two different `/usr/bin/time -l` fields (`maximum resident set size` vs `peak memory footprint`); `CONTINUATION_BRIEF.md:164` records them side by side; four committed `PROCESS_TIME.txt` receipts show the fields genuinely differing (1.42×, 1.10×).
- Refuted by measurement (B83, :2496-2517): one reproduction run emitted **both** numbers — maxrss 7.716 GB and footprint 14.838 GB, ratio 1.923, fixture counts identical to the audit's (8,812/10/5/96/8,807). "The claim at SECOND_AUDIT.md:619-622 … is wrong and should be struck."
- Compounding correction (B81 :2416-2424): **8.61 GB is a two-day sub-window (2026-01-01..02), not a month arm** — no full-arm peak-RSS measurement exists anywhere; month figures are wall-clock extrapolations self-labeled as not-a-bound.
- **Stale doc [MEASURED-now]:** worktree `CLAUDE.md:143-144` still states both the "one replay arm peaks at 8.61 GB maxrss" scope error and the refuted "GiB/GB unit echo" parenthetical. SECOND_AUDIT §5.3 carries no amendment banner (unlike F7, which was amended in place :286-295).

**The V4 contract-size causal claim — REFUTED (B67).**
- The claim family: `VPS_EXPORT_FINDINGS.md` V4 said the 18-of-19 `trade_contract_size` divergence *"gives … a concrete mechanism rather than a hypothesis"* for the FTMO-vs-FN sizing asymmetry (echoed in B26 :544-545: "a mechanism to test"); SECOND_AUDIT.md:168-169 had claimed the FTMO-vs-FN damage asymmetry "additionally implicates execution/universe differences."
- B67 (:1948-1962) [MEASURED]: 64 same-signal pairs; equal-contract-size pairs median risk ratio **1.411** vs unequal **1.355** — indistinguishable. "V4's measurement stands; its attribution does not, and the corollary is that the sizer handles the divergence correctly." The real mechanism is the de-risk governor: `size_cap_multiplier` median 0.5405 (FTMO) vs 0.8537 (FN), `governor.reason == "derisking_into_maxdd_wall"` on 145/145 placements. FTMO entered the window already −2.74 % down from the fleet era, so its ladder was further along. **The asymmetry is the safety machinery working**, not execution/universe divergence.

**F13's mechanism — AMENDED (B8), and the audit's implied remedy would have been harmful.**
- The audit (:67-77, F13 :344): real package-admission defect — `package_execution_fill_probability` resolves None because `selector_v4.py:4004-4016` "never consults `entry_quality_fill_probability`."
- B8 (:177-196) [VERIFIED]: the line description is accurate but the inference is not. `:3995-4001` **does** consult `entry_quality_fill_probability` — for a different quantity. The real gate is upstream: `_complete_execution_fillability_atom` (`selector_v4.py:453-537`) refuses incomplete value+provenance tuples (`:508`); the ten fixtures pass bare `{"fill_probability": X}` with no provenance. **The engine is right and the fixtures are stale.** The audit's implied remedy (consult `entry_quality_fill_probability`) would have introduced exactly the cross-stitching the atom exists to prevent, in a **contract-bound** file. Fixed by fixing fixtures (Phase 0 item 0.3, `44f9a02c6`, 103/103 green, engine untouched, :1506). Q1 closed (:1516): production always supplies provenance — no production candidates were being silently denied. Note the meta-pattern: F13 was itself the audit's correction of H2's LFS story; the correction then needed correcting.

**The clock calendar — the defect CONFIRMED, the calendar REFUTED (B27); both audits named EET/EEST, truth is US DST.**
- F7's original text (:299-303, and `FULL_VISION_PLAN.md` item 5) named FTMO server time "EET/EEST (UTC+2 winter / UTC+3 summer)".
- B27 (:555-610) [MEASURED]: broker wall clock = `America/New_York` + 7 h — UTC+2/+3, but switching on **US** DST dates. Five methods, three independent exchange calendars, two non-broker references; 9 transitions over 4.25 years, every one on the US date, none on an EU date. Consequences: an EU-rule repair (what a faithful reading of the plan produces) is wrong by exactly 1 h ~4 weeks/year yet **passes the plan's own Friday-22:05Z test**; the seam inside the sealed March challenge is **2026-03-08, not 2026-03-29**, splitting the month 1 week / 3 weeks; a VPS file (`25_data/historical/US30_cash_M15.csv`) already carries the wrong EU-calendar correction. Every EU/EET statement in the tree descends from one non-discriminating median-Friday-close statistic; the repo had even flagged the open question at `sleeves/fx_jpy.py:40-45` and then guessed EU. SECOND_AUDIT F7 was amended in place with a banner (:286-295). Downstream: B29 (live half — 8 deployed sleeves fire 2-3 h late plus a day-key shift, owner decision, not repaired); B54 (split: sleeve hour gates = bug fix; `decision_day_of` = certified risk envelope = owner call); B55 (the new VPS tick export carries F7 again — broker wall clock in `time` and even in `first_tick_utc`); B56 (a **third clock** neither audit asked about: FTMO's daily-loss reset is 00:00 CE(S)T — EU calendar — while its server runs US; redacted_account resets 00:00 server; the governor reset FTMO's window 1-2 h early in the dangerous direction; fixed `f3bc6dbc2` with no config edit, FN an exact no-op).

### 2b. Verdicts on the first audit's ten claims (E1-E10) — how they fared

- **E1 (replay≠live transfer) — CONFIRMED and mechanized.** B70-B75: `divergence_matrix.py` generates the matrix from a resolved-config diff (228 replay-only allocator keys, 703/272 per-symbol spec rows vs `redacted_account`); `verify_b7_5_post_acceleration_arm.py` refuses an arm without one; `transfer_verdict` lands inside `verification_root_sha256`. Two of E1's sub-claims corrected en route: live does not re-anchor SL (B72/`E1.ENTRY_TIMING`), and "unwired at HEAD" stale since `31cf05634`.
- **E2 (run_book not in repo) — CONFIRMED, executed.** ImportError blocker (`config_bool_value`) was real; porting it also closed new finding F29 (gate resolver `_bool` treating `"false"` as True on the three authority gates — B7, fixed `1a1eeba50`). `run_book.py` vendored; 18,032 bytes at HEAD (CLAUDE.md §4).
- **E3 (bytes-not-economics) — CONFIRMED, vindicated.** The audit's shadow-reducer recommendation, run first (Session A), immediately found **F31**: the replay exit model grants exactly zero gap-through — level exits book the threshold, discarding the crossing quote's R; measured −8.095 R over 212 covered level rows, stops 107/107 adverse (B33 :818-847). B34: arm ordering and all four factorial classifications unchanged (largest shift 0.0135 vs 0.1 band) — no re-run indicated — but the reference arm loses ~2× per risk dollar what the seal states, which bites at canary-readiness. Exactly the class of defect E3 predicted byte machinery would certify.
- **E4 (no semantic parity proof) — CONFIRMED then partially remediated.** Session D revived one of three harness modules (B46-B50, two false greens found in its first version); B86: columnar arm-output identity EQUIVALENT on all eight roles.
- **E5 (proof-CPU 60.6 %/72.4 %) — CONFIRMED, with a framing caveat.** B83: phase timers on the 2-day fixture show economic hot path ~80 % of wall / proof finalisation ~3 % — not a contradiction (function-level vs phase-counter attribution), but the two numbers are easy to read as contradictory.
- **E6 (15.75 GB evidence/arm) — CONFIRMED; D1 demotion executed** (B53: 69.204 GiB → 2.5 GB, zero irreversible loss).
- **E7 (contract binds proof, repairs cost a campaign) — CONFIRMED, narrowed.** B25: the OD-2 additive split frees **2 of 44, not 9** — seven verifiers are doubly bound via `code_authority_paths` inside an executing file; three things had to stay byte-identical that the plan never named (`schema`, `status`, `predecessor_contract_binding`). R1=44 paths, R2=43 bound (CLAUDE.md:102-106).
- **E8 (memory binds; 2-way parallelism worthless) — core CONFIRMED; two sub-claims REFUTED.** The echo sub-claim and the 8.61 GB arm scope (above). The ~1.1-1.3× net 2-way measurement stands.
- **E9 (reachability; deletion plan not executable) — CONFIRMED and strengthened.** B43's own census: 13.0 % reachable (vs the audit's ~14.8 % — same conclusion, 14 BOMs vs 12). The deletion verdict is vindicated beyond the audit's own preconditions: F23 reproduced FOUR times; after a producer-side sweep the deletable set collapsed **607 files/219,469 lines → 1 file/8 lines**. The decisive question the audit also never asked: *"what does X write, and who binds that?"* — sole writers of runtime-required inputs and live-config-bound artifacts sat in the tier. Right unit = whole route, retired by owner decision.
- **E10 (band ~10-16×, "two structural fixes vs micro-optimisation") — AMENDED; the memory lever is refuted as scoped.** B84 [MEASURED]: with the source-layer copies killed (8.4× less at the partition boundary, exhaustively output-identical), **arm peak RSS did not move** (7.716 → 7.986 GB; wall −9.0 %). B85: fixing every known source-layer copy lands ~6.4 GB — misses the ≤3 GB gate by >2×; **~5.5 GB of the peak is unattributed**; recommendation NOT to pay the re-seal; "either the target needs a new derivation from an attributed profile, or the parallel-replay ambition behind it needs rescoping." R20's materialisation finding is real as description, wrong as explanation of the peak. E10's composed-levers path is therefore unproven at its first lever.

### 2c. Tier-0 findings F1-F7

- **F1 — CONFIRMED structurally; the OD-1 evidence base then inverted under it.** The two-stacks fact, the bridge replacement invariant, the zero-signal-overlap claim, and the four negative January cells all held (B32 reproduces the sealed January economics to the cent-equivalent from ledgers). OD-1 was decided the opposite of the audit's §6.2 default recommendation (owner chose the broad stack; B98). Then Session E showed the live evidence the decision rested on was mis-scoped in both directions: **B62** — SECOND_AUDIT:159-160's FTMO ≈ −5.3 % is "right about the account and wrong about the book" (W7's own contribution −2.55 %; the pre-W7 fleet −2.74 %; FN's fleet era was **net positive**, collapsing the audit's −3.0/−3.9 % range to −3.7359 %, 99.1 % of it W7's); **B63** — the headline: **the W7 live window does not measure the W7 book** — only 11.5 % of core-8's confidence weight ever placed a trade; the five validated sleeves (incl. `metals_core` conf 1.00) placed nothing; **B99b** — 145 placements over 38 days, **zero from a train-validated sleeve**, 43.4 % from the two registry-marked `breadth_falsified` sleeves; weighted mean confidence of placed units 0.2312 vs registry mean 0.4875. So "W7 underperformed live, therefore W7's edge is doubtful" is not supported; OD-1 stands for other reasons but its premise is hollowed (filed D0). F1's architectural prescription was executed: Session H built `SleeveBookPolicy` behind the new `Policy` protocol (B90), delegating to the exact live functions (B91), validated 4,908/4,923 comparable live cycles with zero disagreements (B92) and 130/132 transmitted risk percents (B99c) — the declared-live surface has replay representation for the first time.
- **F2 — AMENDED, worse.** The audit said the flags are absent locally and the halt "is real … on the VPS filesystem — which is unverifiable from here" (:202-203). B26 [MEASURED via read-only VPS export]: **no `*.flag` file exists on the VPS either** — "F2 said the VPS-side halt was 'real but unverifiable from here' — it is verifiable, and it is not real." The entire brake was one YAML boolean on a running, funded, connected host. The audit's inversion prescription (presence-of-authorization token, :208-211) was implemented verbatim (Phase 0 item 0.4, `7c85c3105`; `$GTOS_ACTIVATION_TOKEN_DIR`/`~/.gtos/activation`, risk-reducing requests exempt) and became the primary brake — the recommendation preceded the proof of its necessity. F30 (new): the test suite emits trade-shaped operator notifications into `pipeline_state/`.
- **F3 — CONFIRMED; one supporting claim corrected.** C1 [MEASURED]: `create_mt5("live")` **succeeds on macOS** — `import MetaTrader5` is inside `connect()`; "broker scripts die at import on this Mac" is wrong (construction safe, connection fails). The packet-wall and gate analysis stand.
- **F4 — CONFIRMED, executed, with corrections.** Reconciliation done (0.1a/0.1b/0.1c/0.2, B19). B5: the audit's "661 files … lives on `origin/deploy-live`" is wrong for that ref — deploy-live holds **638**, missing `CYCLE62` (the live dial's validation); vendored instead from the VPS ref; a fifth live-lineage ref (`origin/live-handoff-2026-06-15`) neither audit named. C5: the merge set was larger than the plan stated (`mt5_real.py`, `mt5_interface.py`, `broker_profile.py`). C6: a latent fail-open in the naive merge (host-local). B22: two live-risk defects found in the wholesale-merged files and fixed (wall-clock-dependent `decision_bar_iso` idempotency key; `config_bool_value` inverting the fail direction on kill switches — fixed with `config_safety_flag`).
- **F5 — CONFIRMED at audit level; implementation UNTESTED (Phases 3-4 not started).** Corroboration: the book-lane `runtime_learning_packet` capture the audit called "config-enabled and waiting" produced **99,112 packets over 38 unbroken days** (B26, B92) — and became the substrate for Session H's entire validation. The learned-edge input contract remains unbuilt.
- **F6 — CONFIRMED; port pending.** The `ai_companion:` config block deliberately waits on the OD-2 re-seal batch (`agent_config.yaml` is contract-bound; investigation-pass note :1478-1479).
- **F7 — see 2a.** Confirmed defect, refuted calendar, plus B56's third clock (firm reset rule ≠ server clock — "this programme conflated them once already").

### 2d. Lesser findings F8-F28

| F | Status | Evidence |
|---|---|---|
| F8 | UNTESTED (no session evidence either way) | — |
| F9 | RESOLVED — 4th gate ported in the bridge reconciliation; owner decided its semantics (revoking authority DOES revoke closes) | `1a1eeba50`; B21 :475 |
| F10 | CONFIRMED, FIXED (composition-drift note) | 0.0, `2e2febb08` :1501 |
| F11 | UNTESTED | — |
| F12 | CONFIRMED, FIXED, mutation-proven (sign flip fails 15/18 new tests) | B37 :914-919 |
| F13 | AMENDED (engine right, fixtures stale); FIXED via fixtures | B8; 0.3 `44f9a02c6`; Q1 closed :1516 |
| F14 | UNTESTED | — |
| F15 | CONFIRMED — still zero consumers of `cost_warning/critical_threshold_usd` [MEASURED-now: only hit is `config/agent_config.yaml:3892`] | — |
| F16 | PARTIALLY ADDRESSED [MEASURED-now]: `LIVE_HANDOFF.md` at HEAD has no passwords; account login numbers retained, explicitly labeled "operational, owner-shared" (`LIVE_HANDOFF.md:9`) | — |
| F17 | CONFIRMED-latent; coercion signature absent from all sealed arm evidence (zero exact-0.0 price fields) | Session A gaps :974-975 |
| F18 | SUBSUMED — the watched flags don't exist anywhere; token is now the brake | B26 |
| F19 | CONFIRMED, FIXED — `fn_smoke_trade.py` gated on engine authorization | `89a349868`; CLAUDE.md H6 |
| F20 | CONFIRMED, OPEN — same-shape 7,618-file tree flagged, not consolidated | B44 :1621 |
| F21 | REFUTED — 594 rows, not zero; all request-side, never a fill; a Phase-6 build item, not a data source | B26 :537-539 |
| F22 | AMENDED — the 192 = two normalizer-code-root **generations** of 96, not bundle-identity duplication; and `_file_cache` (one of R20's five bar stages) is dead code on the sealed path | B81 corr. 1 & 3 :2373-2395 |
| F23 | CONFIRMED ×4, superseded by the producer-side rule; final deletable set 1 file / 8 lines | B43 |
| F24 | CONFIRMED (independent census: 4,101 py / 2.42 M lines, 13.0 % reachable) | B43 :1109-1111 |
| F25 | CONFIRMED, count 14 not 12 | B43 :1109 |
| F26 | CONFIRMED (the 47,820-line route pull reproduced) | B43 / E9 work |
| F27 | CONFIRMED (R1 = 44; R2 = 43 bound post-split) | CLAUDE.md:102-106 |
| F28 | CONFIRMED; reference doc landed; graft-vs-reference open owner question; C12: graft step 1 not free | 0.6 :1509; Q3 :1518 |

### 2e. The audit's §1 corrections block — second-order fate

- The H2 correction (F13) needed its own correction (B8) — and the whole "11 red tests" frame both audits inherited was itself wrong: the real full-suite baseline is **692-695 bad** (B1); Q4 closed it as **77.4 % environment-bound / 12.7 % stale / 9.9 % genuine, zero genuine defects on the live decision path** (:1519), with git-LFS as the mechanism (B39) and the committed baseline non-portable across worktrees (B38/B45/B50). The A/B tool itself had a parser hole collapsing parametrized ids (B24, fixed).
- H6 amendments: follower halt check landed, `fn_smoke_trade` gated (`89a349868`); `mt5_preflight.py` **remains default-live with no halt check** (CLAUDE.md H6) — the one hazard in that family still open.
- R8/U3 and U2 zero-realised-effect closures: stand; B32 reproduces the sealed January economics exactly from ledgers.
- The owner-premise correction (zero signal overlap, :176-187): the overlap claim stands, but B97 shows the registry it used (`ULTIMATE_CANDIDATE_PACKAGE_SLEEVE_REGISTRY_LEDGER.jsonl`) is the **broad** system's shadow registry — a brief that named it "your source of truth for the static registry" would have ported an 82-row registry of the wrong strategy family (D12); the live registry is Python literals, 29 sleeves.
- C13 nuances H1/H4: the contract resolver falls through to `/Users/borr/GTOSActive/repo` on hash mismatch, so a replay from another worktree would NOT fail closed on the two JSONL ledgers.

## 3. What the audit got right that mattered

1. **The two-stacks finding (F1)** — the single most consequential architecture fact of the program. It forced OD-1 within a day of being reported, defined the policy-plural core that Sessions D/G/H then built, and its evidentiary warning ("the current system must earn activation; its own evidence is no better than what W7 died of") is precisely the posture the plan's gates encode. Its concrete artifacts held: the bridge invariant, the dossier's −0.25R/fill, the four negative January cells (reproduced exactly, B32), the zero signal-level overlap.
2. **The live-lineage fork (F4)** — confirmed in every load-bearing particular (book_owner 4,269 vs 1,852 lines; run_book only off-mainline; ImportError on vendoring; the fourth gate; the off-mainline evidence route). Phase 0 executed along exactly this map; the corrections (B5, C5, C6) were refinements, not reversals. The audit's insistence that "vendor run_book.py" was really "reconcile a three-copy fork of code, safety layer, and evidence" prevented a naive vendor that C6 shows would have shipped a latent fail-open into the governor's daily-loss baseline.
3. **The learning-stack assessment (F5/§3.1: "repair-then-connect, not rebuild")** — corroborated where it has been touched: the packet capture it flagged as armed produced 99,112 rows that powered Session H's validation; its inventory corrections of the first audit (actuator fully implemented, tests exist, model_pin/router are LLM serving) stand unchallenged; the candidate-level "thousands of rows per arm-month, not 72" reframe remains the basis of Phase 3-4 planning.
4. **The halt inversion (F2's prescription)** — the audit designed the activation token before B26 proved the flags never existed anywhere. The recommendation was implemented essentially verbatim and is now the primary brake. Getting the *mechanism class* right (absence-fails-open vs presence-fails-closed) mattered more than its wrong hedge about VPS state.
5. **The shadow-reducer priority (E3/§4)** — scheduled first, it found a real economic defect (F31) on its first run, invisible to ~54,000 lines of proof code, and B34's materiality analysis kept the campaign unblocked. This is the audit's thesis T3 demonstrated end-to-end.
6. **E9's "deletion plan not executable as specified"** — vindicated and then some (219,469 lines → 8 lines surviving).
7. **The clock defect's existence (F7)** — the calendar was wrong but the *defect class* (mislabeled timebase, replay/live disagreement, session-rule shift, mid-sealed-window seam) was right, and finding it at all is what triggered Session B's five-method measurement. The corollary warning about naive forward/replay joins stands.
8. **OD-2's shape** — the additive verification split was adopted and worked (P1 landed free), even though narrower than implied (2 of 44, B25).

## 4. Questions the audit never asked that the sessions showed mattered

1. **"Did the validated book actually trade in its live window?"** The audit treated the −5.3 %/−3.x % as the book's result, confounded only by dial/composition/hot-fixes (:161-169). It never partitioned the account P&L by era and sleeve. B62/B63/B64/B99b: half the FTMO loss was the pre-W7 fleet; 88.5 % of validated confidence weight never traded; zero placements from train-validated sleeves; the loss is evidence about the candidate book + breadth residue, not the core book.
2. **"Does the validation charge commission?"** Neither audit examined the cost model term-by-term. B66: **zero commission at five sites** including the live pre-trade engine; commission+swap = 31.6 % of the W7 loss (−12.69 R, ~half the R lost); the authorizing MC applied tick erosion with the **wrong sign** (+0.018 R credit vs a true ~−0.195 R charge on USDJPY); the gate that should catch it is circular. F38/F39/F40 filed.
3. **"Do the halt flags exist on the VPS?"** The audit declared it unverifiable and stopped; a read-only export session (B26) answered it in one day, and the answer ("not real anywhere") changed the deployment priority order.
4. **"What is actually holding the 8 GB peak?"** The audit propagated R20's materialisation finding into a ≤3 GB/arm target without attribution. B84/B85: killing all source-layer copies moves peak RSS ~0; ~5.5 GB is unattributed; the target has no valid derivation. Also never asked: "is the 8.61 GB number a month arm?" (it is a 2-day sub-window) and "are footprint and maxrss the same measurement?" (they are not).
5. **"What persistent state does the live book's sizing depend on?"** Session H: the running-conviction store (`firing_sleeves.json`) changes sizing on **78.1 % of sized cycles** (B93/B99e); nine independent day keys with intra-cycle Kelly collisions (B94, mechanism corrected by B99e to the UTC-midnight/bar-recency interaction); the gross-cap shed is first-fit, not lowest-conviction-first (B95 as corrected). The audit analyzed the book's code and evidence but never its runtime state-dependence.
6. **"Which clock does each firm's daily-loss reset use?"** B56: FTMO resets on CE(S)T (EU calendar) while its server runs US DST — a third clock; the governor was resetting FTMO's window early in the dangerous direction. The audit's clock finding stopped at the data layer.
7. **"What does a candidate-deletion file *write*, and who binds that?"** E9's added preconditions still only asked "who imports X"; B43's producer-side sweep is what actually killed the tier.
8. **"Is the inherited test-failure frame (11 red) the real baseline?"** B1: 692. The audit re-ran the 11 and the 97 hardening tests but never captured the whole-suite truth its successor sessions needed, nor asked whether the baseline was portable across worktrees (it is not — B38/B50) or whether running the suite has side effects (it pages the operator with fabricated trades — F30).
9. **"Does the packet/ledger stream emit one record per intent, and did the restore restore data or pointers?"** B92's intent-recovery accounting and B99 (109 of 113 `shadow_logs/*.jsonl` are pointer stubs; "the restore restored pointers"; the gzip second silent-empty trap) — the audit's own LFS hydration experience did not generalize into a doctrine its successors could reuse.
10. **"Is the comparator measuring the measurement?"** B99e's lesson — four of five errors in Session H's own harness inflated favourable numbers and none was caught by 52 mutation-proven tests, "a validated port measured by an unvalidated comparator is not a validated result" — names a blind spot the audit shared: it verified claims against code and sealed artifacts but rarely mutation-tested its own measuring scripts (the W7 forensics generator had ten bugs, B69; the audit's own profile harness now cannot run, B83).

## 5. Stale-document notes for the review author

- `CLAUDE.md:143-144` (H3) still carries the refuted GiB/GB echo claim and the "one replay arm peaks at 8.61 GB" scope error [MEASURED-now]. B83 says the claim "should be struck"; it has not been struck at this worktree's HEAD.
- SECOND_AUDIT §5.3/E8 row (:31, :619-622) carries no in-place amendment banner, unlike F7 — a reader of the audit alone still inherits the echo claim.
- `mt5_preflight.py` remains the one un-gated broker-capable script (CLAUDE.md H6).
- F14, F15, F20, and Q7/Q8 (suite notifications; three tests that pass only with a live gate open) remain open items no session has taken.