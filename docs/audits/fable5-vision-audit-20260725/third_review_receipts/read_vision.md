# read_vision (af41b56e55020343c)

## HEADLINE FACTS
- The vision file (2026-06-07, 707 lines) predates the ultimate_book live surface, both audits, OD-1/OD-2 and B7.5; its Current Anchor still names the KIAP-era V4 stack as the production replacement, and its implicit premise that the vNext selector path is the live path is falsified (live = ultimate_book W7, config/agent_config.yaml:1246-1394; V4 gates never fire, permissions.py:930). [VERIFIED]
- Register roll-up across ~90 limitation bullets in 13 sections: ~25% CONVERTED-TO-MEASUREMENT, ~35-40% STILL-OPEN, ~20% RETIRED, ~15% NEVER-REAL/dropped; the fully-retired section is Research Process (adversarial passes, failure-set A/B now institutional), the fully-untouched section is Market Awareness. [VERIFIED]
- The most vision-fulfilling artifact of the eight sessions is SleeveBookPolicy: the digital-twin property achieved by delegation for the declared-live book — 4,908/4,923 live cycles reproduced with zero disagreements, 130/132 transmitted risk percents exact, H7 reduced to six enumerated adapter divergences (IMPLEMENTATION_STATE.md B91/B92/B99c). [VERIFIED]
- The vision's execution-friction worry was inverted by measurement: stops fill clean (median -1.0036R gross, zero stop slippage), entry slippage half of modeled; the real friction is a missing commission term — validation charges 0.0 at five sites, live pre-trade engine omits the term (broker_net_cost_engine.py:577-583), commission+swap = 31.6% of the W7 loss = -12.69R of -25.32R (B66, F38). [VERIFIED]
- B99b outranks everything for composition risk: over 38 live days the book placed 145 trades and not one came from a train-validated sleeve; 43.4% came from registry-falsified sleeves; 4 of metals_core's 6 symbols are absent from both live profiles — 'the live portfolio is not the validated portfolio' is a failure mode the vision never names. [VERIFIED]
- The divergence matrix v2 turned CLAUDE.md H7 from prose into machinery: 54 rows (38 derived, 16 declared with self-decaying justifications), 41 blocking, verdict computed not written — all four January arms yield REPLAY_R_IS_NOT_A_LIVE_CLAIM, and arm acceptance refuses an arm without a matrix (B70-B74). [VERIFIED]
- The vision's ML section ('no feature store, no label store, no trained model') is technically still true but its premise is obsolete: a complete fail-closed learned-edge lane (builder 770L one-row-per-candidate, trainer 719L with leakage canary, walk-forward gate 570L that is a beats-production claim by construction, frozen scorer, continuous learned sizing, bounded rerate actuator) exists dormant at HEAD, dead only at its input contract (F5); no training run has ever happened. [VERIFIED/MEASURED files present]
- The envisioned autonomous self-repairing companion is NEVER-REAL and an explicit anti-goal: the historical instance was operator-driven hot-patching of live trading code between ticks (fleet hot-reloaded with 22 open positions); the deterministic bounded protective companion is real and now vendored at HEAD (src/components/ai_companion/). [VERIFIED/MEASURED]
- Orderflow/depth, news/macro regime state, and the event-driven all-symbol tick loop have zero plan lines and zero session activity — silently dropped from the vision; the M15-cadence decision loop stands. [MEASURED grep of FULL_VISION_PLAN]
- The command center never existed as a product (dashboard.py 503L has only a test consumer; live ops were CLI + write-only Telegram) while ~70% of its data list already has ledger feeds; the re-scoped 'gtos status' CLI (Phase 4 item 4) is still unbuilt after eight sessions. [VERIFIED/MEASURED]
- Replay capacity is a vision blind spot with a fresh negative result: 8GB/arm peak reproduced, the columnar source-layer fix landed output-identical with -8.9% wall but ZERO peak-RSS reduction, ~5.5GB of the peak is unattributed, and the plan's own <=3GB gate was derived from a cause measured as non-causal (B83-B85, B87). [VERIFIED]
- Clock truth is the cleanest RETIRED item: broker wall clock = America/New_York + 7h on both brokers (81 boundaries each), both audits had named the wrong calendar, broker_clock.py fails closed, per-firm daily-loss reset clocks measured to differ (B27, B56); residuals are the redacted_account DST-window data gap and the owner-decision on eight deployed sleeves (B29/B54). [VERIFIED]
- The W7 live window does not measure the W7 book: 88.5% of core-8 confidence weight never traded; the three core sleeves that traded are the registry's three weakest; 14 days cannot falsify the account-level validation (p 0.052/0.028) but one sleeve dies (JPY cluster p=0.00016, negative before costs) — validation-frequency economics is missing from the vision (B63, B68). [VERIFIED]
- Campaign state against the vision's 'sealed historical validation' ambition: January sealed and accepted (all four arms negative), April S1R1 stopped at 16/30 with no receipt and no comparand, May and sealed March unrun; F31 (zero gap-through, -8.095R uncompensated) does not disturb arm ordering but roughly doubles the reference arm's absolute loss per risk dollar (B33/B34/B36). [VERIFIED]
- Deployment-state verification is a missing vision concept proven necessary: the documented hard-halt flags do not exist on the VPS (renamed 2026-05-18, unnoticed two months); the entire live brake is one YAML boolean; activation tokens exist at HEAD but are not yet ported to the VPS — named the highest-value deployment item (B26, CLAUDE.md §4). [VERIFIED]

## RECOMMENDATIONS
- Retire the vision file from preflight authority: mark it historical, extract the still-live hypotheses (market perception, orderflow, meta-selector) into a short capability register bound to FULL_VISION_PLAN phases, and delete the KIAP-era anchor/program sections that misdirect new sessions. Cost: half a session, no code.
- Promote cost to spine: add the commission term at the five zero-commission validation sites and broker_net_cost_engine.py:577-583, de-circularize the cost gate (F38), and calibrate per-symbol/session from the 175-trade broker-truth set; run the H1/R2 membership check first since some sites may be contract-bound. Cost: one session + possible re-seal decision for bound files (owner-priced).
- Add a deployment-composition invariant: a test/monitor asserting each armed sleeve's validated symbol universe is present in the live profile, plus a silent-sleeve/weight-coverage alarm (the B99b class). Cost: one short session, tests only, no re-seal.
- Build command center v1 as the planned 'gtos status' report-pack CLI over existing JSONL feeds (~70% coverage already exists); explicitly include the deployment-state verification the halt fiasco proved necessary (token presence, flag audit, ledger liveness — the three dead execution ledgers went unnoticed 23 days). Cost: one session.
- Feed the dormant learning lane exactly as F5 prescribes (typed-event capture emitting the builder's whitelist; author the partition registry; fix the two statistical flaws) — do not redesign the lane. Cost: Phase 3/4 as planned, several sessions.
- Before any further memory work, attribute the ~5.5GB unowned share of arm peak RSS with one tracemalloc-at-high-water run (~20 min, no re-seal), then re-derive or drop the <=3GB/arm gate per B85; do not launch parallel arms until attributed.
- Adopt validation-frequency economics into every gate: per-sleeve minimum-sample arithmetic (B68-style z/p with multiplicity control) as a standing gate input, so 'insufficient live window' is computed rather than argued. Cost: small, one utility + gate wiring.
- Port the activation token and the protective companion to the VPS (B26's highest-value deployment item) — recommend to owner; execution is owner-gated and outside review scope.
- For the unconstrained architecture: keep policy-plural core + delegation-based digital twin + broker truth + fail-closed tokens + walk-forward learning gate + enforced divergence matrix + adversarial-verification discipline as the spine; drop orderflow/news/nine-depth-taxonomy/LLM-runtime/web-dashboard/self-repair-companion; add the six missing domains (cost model, composition invariant, evidence-integrity engineering, capacity model, clock domain, owner-decision register).
- Institutionalize B99e's lesson as a rule in the working agreement: any new measuring harness gets its own adversarial/mutation pass before its numbers are published — four of five errors in Session H inflated a favourable number and none was catchable by the policy's own 52 mutation-proven tests. Cost: process text only.

## REPORT
# Vision file read: `vnext_absolute_moonshot_vision_and_limitations.md` (707 lines, 2026-06-07) vs. eight implementation sessions

Source read in full: `/Users/borr/GTOSActive/worktrees/review-fable-third-20260727/.context/00_core/vnext_absolute_moonshot_vision_and_limitations.md` [MEASURED]. Evidence base: `docs/audits/fable5-vision-audit-20260725/IMPLEMENTATION_STATE.md` (3,232 lines, blocks B1–B99e), `SECOND_AUDIT.md`, `FULL_VISION_PLAN.md`, `GATE_G1B_RECEIPT.md`, CLAUDE.md, and direct filesystem checks in this worktree. The vision file predates the `ultimate_book` live surface, both audits, OD-1/OD-2, and the B7.5 campaign; its "Current Anchor" (lines 14–22) describes the KIAP-era V4 stack as the production replacement — a premise both audits have since restructured.

## 1. The capability map, compressed (vision lines 57–385, program lines 533–672)

Eleven envisioned layers plus a seven-item build program:

1. **Market Perception Layer** (L59–93): live "market whiteboard" per instrument — ticks→D1 bars, spread-to-risk, liquidity sweeps, displacement/absorption, vol regimes, session/kill-zone timing, news/calendar, cross-market correlation, risk-on/off, broker market-hours/stop/freeze/friction. Decisions read as full state (candidate + proof + source completeness + cost + policy + portfolio absorption + broker feasibility + invalidation + lifecycle owner), not "XAUUSD long signal."
2. **Candidate Generation Layer** (L95–116): broad mechanism families — OB retest, FVG, breaker, sweep reclaim, displacement continuation/failure, gaps, no-fill near/far-miss, session flow, exhaustion, stop-cascade, trapped traders, orderflow-confirmed variants, ML-discovered families, inverse/avoid candidates. Anti-narrowing mandate (one symbol/session/mechanism/Friday).
3. **Selector & Meta-Selector** (L118–142): admission from ~19 evidence dimensions; meta-selector learns which mechanisms survive broad denominators, sealed partitions, symbol/session holds, stress, broker-real costs.
4. **Portfolio Scheduler** (L144–167): admission by actual dollars of exposure — balance/equity, day baseline, realized PnL, open/pending/new worst-case SL risk, partial/BE releases, cost buffers, same-symbol + correlated-cluster exposure, internal daily overlay + prop limits, cushion expansion, DD compression, priority; never a stale count cap.
5. **Execution Policy Engine** (L169–200): evidence-selected exits (partial-BE, exhaustion, trailing, time-stop, fixed, BE-after-trigger, no-trade, per-symbol/session policies); everything tick-realistic and broker-feasible (tick order, bid/ask side, spread at trigger, same-bar order, stop/freeze levels, retcodes, latency, partial-close ticket identity, residuals, commission/swap/slippage, min stop distance). "Proxy MFE is not enough."
6. **Broker Truth Layer** (L202–228): capture + reconcile orders/deals/positions/tickets/fills/partials/residuals/modifies/rejections/close reasons/costs/manual interventions/Telegram parity/equity; broker history is final truth.
7. **Replay & Digital Twin** (L230–257): answer "what the actual current runtime would have done" at nine depths (Friday microscope → every-day → week → month → year → broad denominator → sealed validation → forward shadow → broker-real).
8. **ML Learning System** (L259–318): feature store, label store, candidate identity, path-anatomy labels, broker-real + proxy labels, no-leak rules, purged/embargoed/walk-forward/leave-symbol-out/leave-session-out/regime partitions, cost-stressed labels, uncertainty/OOD/drift/attribution/adversarial baselines; model families GBT → calibrated logistic/meta-labeling → survival/hazard → sequence/transformer → representation → ensembles → OOD → cost-aware ranking → policy selection → RL only after strict simulator realism. Must be source-bound, interpretable, baseline-compared.
9. **AI Companion Layer** (L320–338): LLM as augmentation (log anomaly detection, stale-behavior detection, post-trade autopsy, study orchestration, prompt validation, hypothesis generation, context compression, code-repair planning, operator explanation); paid calls targeted and economical.
10. **Autonomous Repair Companion** (L340–361): continuous checks (data freshness, source completeness, broker truth, stale gates/processes, symbol starvation, refusal reasons, false closes, cost/spread drift, exposure correctness, ML/selector/policy drift, production-vs-replay mismatch); proves defects from source, self-creates local repair + tests + production-change package.
11. **Command Center** (L363–385): one current-truth view — market state, candidate funnel, exact rejection reasons, exposure, risk remaining, lifecycle, broker truth, freshness, source gaps, stale warnings, active selector/policy, ML confidence, drift, learning queue, route/verification status.

**Active Next Program** (L533–672): (1) historical microscopic replay at scale (every weekday/week/month/year, row-level anatomy); (2) versioned feature store; (3) label store (17 label kinds incl. SL-before-1R, partial-then-BE, MFE/MAE, broker-real net-R, cost-adjusted R, accepted/rejected correctness); (4) train models with strong baselines; (5) ML → default-off runtime intelligence (shadow predictions → packet fields → owner-armed activation dossier); (6) daily learning loop (daily microscope/loss anatomy/missed-opportunity/broker reconciliation/scorecard/feature-label update/repair queue); (7) command center. **Execution standard** (L674–689): no arbitrary cutoffs, no Friday/one-symbol narrowing, no summary-only completion, no infinite ledgers, no unsupported production claims, no live broker action or paid API without owner command, every artifact feeds code/data/model/verifier/kill-decision, every ML result no-leak + partitioned + baselined + stress + interpretable.

## 2. The known-limitation register, classified (13 sections, vision L387–531)

Legend: **RETIRED** = fixed or measured away by the eight sessions; **CONVERTED** = converted to a measurement/mechanism (the gap is now quantified or machine-checked rather than narrative); **STILL-OPEN**; **NEVER-REAL** = the premise or capability never existed / is now an explicit non-goal. Evidence blocks cite `IMPLEMENTATION_STATE.md` line anchors (Bnn at the lines listed in §index) and audit findings.

### 2.1 Integration (L391–396)
- "Limitation is behavior quality under replay: scheduler regret, weak accepted trades, risk-headroom lockout, stale partial-BE residue, cost flips, holdout degradation" — **RETIRED as vocabulary, CONVERTED as substance**. The KIAP repair-loop program this section defines was superseded by B7.5 (CLAUDE.md §4 "Program"). "Weak accepted trades / cost flips" became measured defects: F31 zero gap-through (−8.095 R uncompensated across 212 level rows, arm ordering unchanged — B33/B34, IMPLEMENTATION_STATE.md:818–871) and F38 zero-commission validation (B66:1873). All four January arms measured negative (B34 table) — the "behavior quality" question now has sealed numbers. [VERIFIED]
- "Shared-code repair needs one owner route at a time" — **STILL-OPEN, institutionalized as process** (WAVE_2_WORKING_AGREEMENT.md; per-session branch ownership). [VERIFIED]
- "Route artifacts become truth only when merged/verified" — **CONVERTED**: failure-set A/B discipline (`scripts/pytest_failset.py`, B9/B24/B50/B88/B99a) makes "verified" mechanical; baselines measured non-portable across worktrees (B38:935, B50:1315). [VERIFIED]

### 2.2 Selector and edge (L398–409)
- "Final meta-selector does not exist" — **STILL-OPEN**. Selector V4 `enabled: true` but `live_activation_allowed: false`; `permissions.py:930` returns `None` for every candidate (CLAUDE.md §4) [VERIFIED]. The planned meta-selector is the learned-edge walk-forward gate (exists, dormant — F5, SECOND_AUDIT.md:261) plus the Phase-4 lane, not started.
- "Broad selected denominator stress pending" — **STILL-OPEN, half-executed**: January sealed and accepted (4 arms); April S1R1 stopped at 16/30 with no receipt; May and sealed March unrun (CLAUDE.md §4 campaign state). B36:904 adds April corrections (15 sealed days not 16, no comparand exists). [VERIFIED]
- "Selected-cell risk proof remains a critical live-decision field" — **CONVERTED, adversely**: `selected_cell_risk_decision_basis` was **null on all 149 live intents** and the cost gate consuming it is circular (F38, B66:1873). Worse than the vision assumed. [VERIFIED]
- "Friday evidence must not become the boundary" — **RETIRED**: month-window sealed arms are the unit; Friday vocabulary is gone from the plan.
- "Avoid/inverse intelligence not converted to live filters" — **STILL-OPEN** (no session touched it).
- "Failure anatomy not mined across all historical days" — **STILL-OPEN**; the microscope method migrated to live-evidence forensics instead (§3 below).

### 2.3 Portfolio scheduler (L411–421)
- "Ideal real-money exposure scheduler not fully live-authoritative" — **CONVERTED + STILL-OPEN**. The live incarnation is not the vNext scheduler but the `ultimate_book` governor/admission chain, now exhaustively measured by Session H: gross-cap first-fit shed with a found 28.6 % deployment-shortfall case (D1, B95:2916 as corrected by B99e:3150), nine independent day keys with 4 consequential Kelly-multiplier collisions (D2, B94:2887 + B99e), running-conviction store determining sizing on **78.1 % of sized cycles** (B93:2871 + B99e correction), A8-gate omission in the pre-count breaking the bound size-upward (D3, B96:2944). The vision's "dollars as sole admission authority" is still unproven anywhere; the instrument to test it (SleeveBookPolicy) now exists. [VERIFIED]
- "Correlated cluster exposure not full authority" — **PARTIAL**: the book collapses to one unit per cluster (B90–B95); the envisioned cross-portfolio correlation authority does not exist.
- "Same-symbol multi-ticket lifecycle immature" — **STILL-OPEN**: all three Gate3 lifecycle tests short-circuit before reaching duplicate/hedge/alias logic — live-path unexercised (B41 L2:1060). [VERIFIED]
- "Drawdown compression needs broader stress" — **CONVERTED (live)**: the derisk governor was measured operating — `derisking_into_maxdd_wall` on 145/145 placements; the FTMO/FN sizing asymmetry (ratio 1.579) **is** the safety machinery working (B67:1948). Cushion-expansion side untested.
- "Pending risk / partial release broker-real truth" — **STILL-OPEN** (877-row `pending_limit_lifecycle.jsonl` earmarked for Phase-6 calibration, FULL_VISION_PLAN.md:340–345; placement gates deliberately out of replay scope, B91).

### 2.4 Execution (L423–435)
- "Execution policy optimality not proven" — **STILL-OPEN**; but the exit *simulator* itself acquired a measured defect class: F31 zero gap-through, uncompensated by the flat 0.02 R slippage allowance (B33). **CONVERTED** in that sense.
- "`partial_be_runner` damaged family" / momentum-exhaustion comparators — **RETIRED** (KIAP-era vocabulary; live book uses none of it).
- "Trailing unproven until tick-realistic sim" — **STILL-OPEN**.
- "Same-bar/trigger ordering strict everywhere" — **PARTIAL**: replay has `same_bar_*_ambiguous` refusal branches (B35.7); conservative refusal, not resolution.
- "Stop/freeze feasibility, latency, retcodes not modeled" — **STILL-OPEN in replay, INVERTED by live measurement**: the W7 fortnight shows **stops fill clean** (median −1.0036 R gross, zero stop slippage) and entry slippage at roughly half the modeled 0.02 R (+0.0132 R mean, n=140); the real friction is a **missing commission term** — validation charges 0.0 at five independent sites, the live pre-trade engine omits the term (`broker_net_cost_engine.py:577-583`), commission+swap = 31.6 % of the W7 loss, −12.69 R of −25.32 R total (B66:1873, F38). The vision worried about the wrong half of execution friction. [VERIFIED]
- "BE/TP modify diagnostics" — **STILL-OPEN**: the BUG#28 regression guard for live SL handling has never executed (stub gates on `action == 2` vs `TRADE_ACTION_SLTP = 6`) (B42:1086). [VERIFIED]
- "False close handling enforced everywhere" — **CONVERTED/RETIRED for the book path**: VPS hardening landed at HEAD splits projection from broker truth (`realized_usd` stays `None` unless broker-reconciled, `src/notifications.py:263,345` per B19:388). [VERIFIED]

### 2.5 Broker truth and cost (L437–446)
- "Broker net-R not universal outcome authority" — **CONVERTED/RETIRED for the measured surfaces**: W7 forensics reconciles 300 closed positions to each account's closing balance **to the cent, residual 0.000000** (B61:1765); attribution partition residual 0.000000 over all 175 W7 positions (B64:1826). [VERIFIED]
- "Commission/swap/spread/slippage not used in decisions" — **CONVERTED, and the finding is bigger than the limitation**: see F38 above; plus the authorizing MC applied tick erosion with the **wrong sign** (~0.2 R/trade optimistic on the sleeve family that lost 9/9) and `TICK_SPREAD_FLOOR_R` is inert and thin (F39, F40 — B66). [VERIFIED]
- "Cost calibration by symbol/session/regime not mature" — **STILL-OPEN** (Phase 6 item; three execution ledgers dead since 2026-07-02 — B26:526).
- "Broker history joins robust" — **CONVERTED**: per-position solved price units exposed redacted_account `trade_tick_value` 2-decimal quantisation (+5.5 % GER30) (F37, B61). [VERIFIED]
- "Telegram parity" — **NEVER-REAL as a priority**: write-only Telegram; no session touched it; dead weight.
- "Manual intervention classification explicit" — **CONVERTED in-window**: hand-placed phone trades isolated as their own era (+13,166.69 FTMO / −1,018.10 FN, B62:1778). [VERIFIED]

### 2.6 Market awareness (L448–458)
Entire section — M15-cadence loop, no event-driven tick/M1 loop, immature HTF fusion, limited cross-market/correlation awareness, no macro/calendar regime layer, no first-class news state, basic spread/liquidity regimes — **STILL-OPEN, untouched by all eight sessions and absent from FULL_VISION_PLAN Phases 0–8** (grep confirms zero occurrences of orderflow/news-as-state/event-driven-loop in the plan) [MEASURED]. This is the largest never-started vision block. Session context and kill-zone logic exist in sleeves; the "market whiteboard" does not.

### 2.7 Data and source (L460–469)
- "Timezone/session-boundary/duplicate-key issues are first-class risks" — **CONVERTED, substantially RETIRED**: F7 measured — broker wall clock = `America/New_York + 7 h` on both brokers over 81 weekly boundaries each; both audits and the plan had named the wrong (EU) calendar (B27:555); `src/utils/broker_clock.py` fails closed on unregistered servers; research exports carry `.timebase.json`; regression test exists. Remaining: redacted_account DST-disagreement-window data gap (declared UNKNOWN row in the matrix, B74) and the eight deployed sleeves' clock correction — an **owner decision, deliberately not repaired** (B29:652, B54:1623). Also new: the daily-loss reset clock is per-firm and is not the MT5 server clock (B56:1671). [VERIFIED]
- "Tick/M1 availability varies" — **CONVERTED to a coverage table**: ordered-tick truth = 4 of 24 symbols × 7 months; M1 2024-01→2026-06; 1.9 GB verified VPS ticks 06-18→07-24 outside the repo (FULL_VISION_PLAN.md:303–311; CLAUDE.md §4). Acquisition still open (~290 GB backfill estimate).
- "Historical state not reconstructible if never logged" — **NEVER-REAL to fix; now quantified**: 285 unreconstructable cycles (B92), trade side absent from 92.81 % of non-placement unit packets (D4 as corrected, B99e), packet `spread_r` NULL on all 99,112 rows (F36). [VERIFIED]
- "Stale labels / context pollution" — **RETIRED by measured deletion**: Tier-A context files deleted after a 193→193 failure-set experiment; 69.204 GiB → 2.5 GB cold demotion with zero irreversible loss (B53:1594); staleness map + deletion manifests. [VERIFIED]

### 2.8 Replay and validation (L471–480)
- "Full current-runtime digital twin not complete" — **CONVERTED, and half-ACHIEVED**: (a) for the live book, `SleeveBookPolicy` achieves the digital-twin property by **delegation** to the exact live function (`bridge.evaluate_vnext_ultimate_book_admission`, the function `book_engine.py:741-746` calls in production) — 4,908/4,923 comparable live cycles reproduced with zero disagreements (B92:2843), 130/132 transmitted risk percents exact (B99c:3097), H7 reduced "from an architecture to a list of six" enumerated adapter divergences (B91:2816); (b) for the broad stack, the divergence matrix v2 mechanizes the remaining gap — auto-derived from resolved config diffs (1,790-line overlay), 54 rows, 41 blocking, verdict **computed** (`REPLAY_R_IS_NOT_A_LIVE_CLAIM`), required by arm acceptance via argparse, laundering-proof by recomputation (B70–B74:2030–2121). [VERIFIED]
- "Friday microscope not enough / one-day lift" — **RETIRED** (sealed-month unit; though H5 — no sub-window replay — still stands, CLAUDE.md H5).
- "Broad replay must include costs/stress/splits/exposure" — **PARTIAL**: B7.5 factorial has stress bands; cost realism is undermined by the zero-commission defect (F38).
- "Sealed partitions incomplete" — **STILL-OPEN** (April/May/March).
- "Forward shadow not the final rolling engine" — **STILL-OPEN**; VPS has accumulated shadow-intelligence packets continuously since 2026-07-02 (99,112 packets over 38 days) but three execution ledgers are dead 23+ days (B26). Phase 6 not started.
- "Distinguish raw/selected/broker-ready/accepted/broker outcomes" — **CONVERTED**: the two-stacks finding (F1) plus the era-partitioned W7 row set (fleet / W7 / manual) made this distinction the program's spine.

### 2.9 ML (L482–494)
Entire section ("no feature store, no label store, no trained model, no ML authority, no OOD/drift/attribution") — **STILL-OPEN as stated, but the premise is obsolete**: the second audit found a complete, fail-closed learned-edge lane already written — 770 L dataset builder (one row per **candidate**, attacking selection bias; thousands of rows per arm-month, ~2 orders of magnitude more than every prior discussion assumed), 719 L trainer with a leakage canary that refuses to emit artifacts, 570 L walk-forward gate whose pass **is a beats-production claim by construction**, frozen-coefficient scorer, selector integration for admission *and* continuous learned sizing, and an owner-armed book-rerate actuator with bounded tilts (≤1.25, 0.0-drop) — dead at exactly one point: its input contract (F5, SECOND_AUDIT.md:261–271, §3.1:361–407). All files present at HEAD [MEASURED: `src/research_infra/learned_edge_{dataset_builder,trainer,walkforward_gate}.py`, `src/components/learned_edge_layer_v4.py`, `src/components/ultimate_book/learning_actuator.py`]. Known statistical flaws are named (full-frame numeric specs, no purge/embargo enforcement, `n_competing_in_group` train/serve skew). Vision's "existing ML files are inputs, not the final system" is **half-wrong**: they are the system, minus its food. **No training run, no partition registry, no capture layer** — Phase 3/4 work, not started in the eight sessions.

### 2.10 Orderflow and depth (L496–503)
**NEVER-REAL and silently DROPPED**: zero occurrences in FULL_VISION_PLAN [MEASURED grep]; no session touched it; legacy `sierra_6b_si_depth_policy.py` exists unintegrated. Dead weight in the vision.

### 2.11 AI companion (L505–512)
- "Not a permanently running verified repair companion" — split verdict. The **protective deterministic companion** exists, bounded ({pause_new_entries, sleeve cooldown, risk_multiplier ≤ 1.0}, TTL ≤ 120 min, malformed-state fails closed into pause), formerly VPS-only (F6), **now vendored at HEAD** [MEASURED: `src/components/ai_companion/{__init__,control_state,supervisor}.py` present in this worktree; Phase 0 item 0.1b]. Not yet redeployed as a canary guard — **CONVERTED/PARTIAL**.
- The envisioned **self-repairing autonomous companion** — **NEVER-REAL, and now an explicit anti-goal**: what actually existed was operator-launched agent sessions hot-patching live trading code between ticks (fleet hot-reloaded with 22 open positions); the post-mortem's conclusion is it polished infrastructure while economics failed; "do not revive in that form" (SECOND_AUDIT.md §3.2:408–422). [VERIFIED]
- "Paid AI governance" — **PARTIALLY NEVER-REAL**: real rails exist ($5 caps, purpose gating) but `budget.cost_*_threshold_usd` keys have zero consumers (config fiction, F15); three LLM call sites, all Model-A era, triple-neutralized; `run_book.py` has zero LLM references. LLM-in-the-runtime is a non-feature. [VERIFIED]

### 2.12 Operations and command center (L514–521)
- "No final unified command center" — **STILL-OPEN + partially NEVER-REAL**: it never existed as a product (`dashboard.py`, FastAPI 503 L, only a test consumer; what ran live was CLI + write-only Telegram); ~70 % of the vision's dashboard list already has a ledger feed; the plan re-scopes it to a `gtos status` report-pack CLI (Phase 4 item 4) — **not built** [MEASURED: no such script exists].
- "Current state distributed" — **CONVERTED**: IMPLEMENTATION_STATE blocks + gate receipts + CLAUDE.md reconciliation + the divergence matrix repairing `LIVE_STATE.md`'s base-config-only caveat (B71:2044).
- "Live activation/canary/rollback not final" — **CONVERTED in mechanism, STILL-OPEN in deployment**: activation tokens are the primary brake (presence-of-authorization; risk-reducing requests exempt; no config off-switch) — but the VPS still runs on one YAML boolean with **no flag files in existence** (the documented hard halt was falsified by direct VPS inspection; flags renamed 2026-05-18, unnoticed two months) (B26:526; CLAUDE.md §4 Runtime). Token port to VPS is the named highest-value deployment item. [VERIFIED]

### 2.13 Research process (L523–531)
**RETIRED-AS-PRACTICED** — the most completely converted section. Every hazard it names became an institutional mechanism during the eight sessions: adversarial self-refutation passes are standard and productive (B35: three claims withdrawn; B69: ten generator bugs; B99e: five errors found in Session H's own measuring code — four of five inflating a favourable number); completion-messages-are-claims is enforced by failure-set A/B against parent (B9/B24); baseline non-portability across worktrees measured (B38/B50); the proof-budget rule ("name the decision it changes") is in CLAUDE.md §7. The residual truth B99e adds: *"a validated port measured by an unvalidated comparator is not a validated result"* — measurement harnesses now need their own verification, a discipline the vision never named.

**Register roll-up** (~90 bullets): roughly 25 % CONVERTED-TO-MEASUREMENT, 35–40 % STILL-OPEN (concentrated in market awareness, ML feeding, campaign completion, forward shadow, command center), ~20 % RETIRED, ~15 % NEVER-REAL/dropped (orderflow, self-repair companion, LLM runtime, Telegram parity, web dashboard).

## 3. What quietly became real; what is dead weight

**Quietly real** (capabilities the vision asked for that now exist, mostly under different names):
1. **Broker truth layer** — the vision's most fulfilled layer: to-the-cent reconciliation, exact attribution partition, per-position solved price units, era separation incl. manual trades, comment-tag sleeve identity 175/175 (B60–B64). It exists as *forensics generators* (`scripts/w7_live_forensics.py`, `w7_packet_forensics.py`), not a runtime service.
2. **Digital twin of the live book** — `src/research_infra/replay_policy/` (core/inert/sleeve_book): the vision's central replay question answered by construction for the declared-live surface (B91/B92/B99c). The first time any replay has represented what `live_system_of_record.md` declares live.
3. **Replay differential harness** — whole sealed arm, 8 roles, 235,923 rows, EQUIVALENT in 782 s at 370 MB (B47:1235): the comparator machinery the "replay at multiple depths" list implied.
4. **Divergence matrix as enforced artifact** — the repair companion's "production-vs-replay mismatch" check, as acceptance-gating machinery rather than vigilance (B70–B74).
5. **The learning system harness** — dormant but sound, incl. continuous learned sizing (the third sizing-policy family B7.5's binary arms cannot express) and the bounded book-rerate actuator (F5/§3.1).
6. **Protective companion** — vendored, bounded, fail-closed (F6 → Phase 0).
7. **Clock-truth layer** — `broker_clock.py` fail-closed registry + timebase declarations + measured per-firm daily-reset rules (B27/B56).
8. **Fail-closed activation boundary** — tokens (`src/safety/activation_token.py`), outside the repo, no disable flag.
9. **The microscope method at scale** — not applied to years of history as envisioned, but to 99,112 live packets and 300 broker positions with closed-form cycle validation; the vision's "looking closely reveals what aggregates hide" was vindicated by B99b (below).
10. **Real-money governor** — measured live doing exactly what the scheduler vision wanted risk machinery to do (B67).

**Dead weight in the vision** (still occupying preflight-document authority):
1. The **KIAP-era anchor and program** (L14–22, L533–541): superseded program, superseded comparators, a route directory that is no longer the active one.
2. **Orderflow/depth** section — no plan line, no work, no data.
3. **Autonomous self-repairing companion** — measured anti-pattern.
4. **LLM-in-the-runtime** — neutralized; the effective companion is deterministic.
5. **Nine replay depths taxonomy** — reality needs three (sub-window — still missing per H5; sealed window; forward shadow) plus the differential harness.
6. **Command center as dashboard** — feed-rich, product-absent; re-scoped to CLI.
7. **Market whiteboard / event-driven tick loop / news-regime layer** — unbuilt and deliberately deferred; carrying them as "limitations" misprices them as near-term gaps.
8. The vision's implicit premise that the **vNext V4 selector path is the production path** — falsified twice over: the live surface is the `ultimate_book` (config/agent_config.yaml:1246-1394; V4 gates never fire, `permissions.py:930`), and OD-1 subsequently made the *broad* stack the activation candidate with W7 retained as challenger (FULL_VISION_PLAN.md:42–63). The two-stacks reality (F1) is entirely outside the vision's hypothesis space.

## 4. Unconstrained architecture: spine / drop / missing

**Keep as spine** (vision elements the eight sessions validated):
- **Broker truth as final authority**, extended into a *calibrated cost model* — the fortnight proved fills clean and commission dominant; cost belongs beside price, not in a feasibility checklist.
- **Policy-plural decision core with the digital-twin property by delegation** — B91's reduction of H7 to six enumerated adapter inputs is the strongest architectural result of the whole effort; `Policy`/`DecisionCore` + `InertPolicy` second-implementation discipline (B90) should host broad, W7-challenger, and learned variants, per OD-1's portfolio-level merge (one trade-intent contract, families earn slots individually).
- **Fail-closed presence-of-authorization** (tokens) as the only activation model; absence-based brakes are measured fiction (B26).
- **The learning loop shape exactly as built**: candidate-level rows, leakage canary, walk-forward beats-production gate, frozen artifacts, bounded owner-armed actuation, default-off. Feed it; do not redesign it.
- **Typed-event capture / evidence-as-projection** (Phase 3): one fix for the 494 KB row problem and ML starvation simultaneously (F5).
- **Exposure governor + drawdown invariants** with the specific mechanical repairs Session H priced (proportional-scale shed vs first-fit; runtime-derived unified day key — the only fix covering all four collision cases, B99e).
- **Divergence matrix required at acceptance**, with derived-or-declared rows and self-decaying declarations (B72).
- **Adversarial verification as institution**, extended one level: verify the measuring harness, not only the measured thing (B99e).
- The vision's **execution standard** (L674–689) — it survived intact and is the file's most durable content.

**Drop** (from the vision, in an unconstrained rebuild): orderflow/depth until an edge question demands it; news/macro as first-class state; the full all-symbol event-driven tick loop (M15 cadence + tick truth at the execution boundary is what the evidence supports); the nine-depth taxonomy; the self-repairing companion; LLM-in-the-runtime; the web command center; KIAP vocabulary and comparator families; RL research (its own precondition — simulator realism — moved further away with F31/F38); Telegram parity as a goal.

**Missing from the vision — what eight sessions revealed that it never imagined:**
1. **Cost as an edge determinant, not a feasibility item.** Commission omission flipped a sleeve's sign (GBPJPY +0.0124 gross → −0.0915 after commission); the authorizing MC credited erosion with the wrong sign; half the live loss was cost (B66). The vision has no concept of a cost-model *gate*.
2. **Live-vs-validated composition drift.** B99b: over 38 live days, 145 placements, **zero from train-validated sleeves**; 43.4 % from sleeves the registry itself marks falsified; 4 of `metals_core`'s 6 symbols absent from both live profiles. "The live portfolio is not the validated portfolio" is a failure mode the vision never names; an unconstrained architecture needs a deployment-composition invariant (declared universe ⊇ validated universe; weight-coverage monitors; silent-sleeve alarms).
3. **Evidence-integrity engineering as a discipline** — contracts binding paths by SHA (two generations, verification-tooling splits), absolute-path seals, LFS stub traps, non-portable baselines, measuring-harness bugs. The vision's standard demands honest claims; the sessions show the *machinery* of honesty is a first-class subsystem with its own failure modes.
4. **A capacity model for replay.** 8 GB/arm peak with ~5.5 GB unattributed; the plan's own ≤3 GB gate was derived from a cause measured as non-causal (B84 negative result, B85). The vision's replay-at-scale has no resource concept at all.
5. **Clock/calendar as a safety domain** — two brokers on a US DST calendar labeled UTC; firms' daily-reset clocks differing from their own server clocks; the sealed March window straddling the divergence (B27/B56). One vision bullet; in reality a domain with its own fail-closed registry.
6. **Validation-frequency economics** — what a live window *can* falsify: 14 days cannot overturn a 2014–2026 account-level validation (p 0.052/0.028) but can kill one sleeve (JPY cluster p=0.00016 surviving Bonferroni, negative before costs) (B68). Gates need sample-size arithmetic built in.
7. **Deployment-state verification** — the halt that did not exist (B26): the vision's command center *displays* state; nothing in it *verifies* the remote protective state against its documentation.
8. **An owner-decision register** — OD-1/OD-2, B21, B29/B54, Q8: the effort runs on explicitly routed owner decisions; the vision has only a static boundary list.
9. **Negative results as first-class deliverables** — B84 (columnar fix does not move RSS) and B63 (the live window does not measure the book) redirect more work than most positive results; the vision's program vocabulary has no slot for them.