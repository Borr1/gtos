# read_learning-stack (ab9e339deb75f7071)

## HEADLINE FACTS
- All 8 learning-stack modules import at HEAD and the core lane is 53/53 tests green [MEASURED]; only wave4b/wave4c fail (8 tests) because their Wave4A input route is absent from both HEAD and /Users/borr/GTOSActive/repo — they are permanently unrunnable one-shots hardcoded to a 15,679-row June-5 universe (wave4b:397, wave4c:33).
- The learning loop already ran end-to-end once (2026-06-10): 175,009-row frame, leakage canary 0.5034, artifact emitted, and the walkforward gate FAILED it on capture_ratio 0.0 + overlap None — the model admitted zero candidates and the gate correctly refused the degenerate pass [MEASURED — ULTIMATE_LEARNED_EDGE_WALKFORWARD_GATE_V1.json]. The artifact expired 2026-06-24 and its training frame is absent from HEAD; no valid artifact or frame exists today.
- That June gate run also recorded the production heuristic floors admitting -1,683.5 R net on the validation frame — an independent datum about broad-stack admission quality, consistent with the dossier's -0.25 R/fill [MEASURED].
- The walkforward gate is genuinely a beats-production instrument: its baseline is the exact production floors p>=0.58/EV>=0.10 (gate:78-80), seven fixed fail-closed gates verified in code (gate:446-501).
- All three trainer flaws the second audit filed are still present: numeric preprocessing specs computed on the full frame pre-fold (trainer:562), no purge/embargo for the 48h label horizon on day folds (week-fold option is a partial mitigation by its own comment, trainer:548-559), and f_n_competing_in_group trained-on but with zero occurrences in selector_v4.py [MEASURED].
- The B7.5 sealed arms cannot feed the dataset builder: the runner emits 9 ledger kinds and no CANDIDATE_MICROSCOPE_LEDGER (b7_5_post_acceleration_runner.py:59-67); the builder skips any day lacking it (builder:296-298); predecision_features is computed at broader_origin_generators.py:1929 and emitted nowhere [MEASURED].
- ULTIMATE_EDGE_PARTITION_REGISTRY.jsonl EXISTS at HEAD (contra SECOND_AUDIT item 4) — but its TRAIN range 2025-06-02..2026-04-17 spans B7.5's sealed March-2026 challenge month, so it must be re-authored, never reused [MEASURED].
- selector_v4_learned_edge_enabled appears nowhere in config/ (default-off at selector_v4.py:2388); both agent_config.yaml and selector_v4.py are R2-contract-bound, so enabling the learned arm structurally requires a new contract generation [MEASURED].
- The book lane's consumption path is fully merged and tested — bridge.py:95/197 -> admission.py:1080 (GATE drop) and :1166 (multiplier clamped [0,1.25]) -> book_owner.py:567 — but no producer of SleeveEvidence exists anywhere, and recommend() structurally ignores the live_meanR/live_n fields (learning_actuator.py:45-51).
- The plan's Phase-4 wording is a stack conflation: B7.5 per-sleeve splits do not exist for W7 book sleeves — B7.5's sleeve vocabulary is fpsc_* package families of the broad Stack A [MEASURED — sleeve registry ledger]; in-tree per-W7-sleeve split evidence is the June deep-history Stack-B chain (KB6_DEEP_STACKS_RESULT.json base_train/base_fwd), which the second audit ordered adversarially audited before reliance.
- The 99,112 packets are broker-outcome-rich but feature-poor: spread_r NULL on all rows (F36), none of the 43 whitelist features present, no counterfactual rows, ~175 closed positions total, five sleeves silent, zero placements from any train-validated sleeve — an evaluation/cost dataset for the live window, not training data [MEASURED — GATE_G1B_RECEIPT].
- The packets already paid for themselves three ways today: exact commission/swap cost inputs (0.0591 R/trade, 31.6% of the W7 loss; F38 repair input), entry-slippage distribution (mean +0.0132 R, n=140), and the bridge block that enabled SleeveBookPolicy's 4,923-cycle zero-disagreement validation.
- Forward learning substrate is degrading: the three execution-evidence ledgers have written nothing since 2026-07-02 while packets keep accumulating (V3) — volume, not execution evidence.
- SleeveBookPolicy (merged in this tree, src/research_infra/replay_policy/sleeve_book.py, 512 L) is decision-level only — 15 placement gates unapplied, no outcomes — so it cannot yet produce the per-sleeve R splits the book lane needs; it becomes the split producer only once an outcome layer exists.
- Learning's blocker is identical to the performance rebuild's deliverable (F5 verified still true after eight sessions): the typed-event capture layer IS the feature/label store; no separate store should be built, and the builder's input contract should be folded into Phase 2's capture-layer spec now.

## RECOMMENDATIONS
- Book-lane producer session (~1 day): extract SleeveEvidence from the June deep-history route + live packets, emit the owner-armed rerate recommendation report (Phase 4 item 1). Must declare: Stack-B evidence chain unaudited; live-window evidence mapped explicitly into the sealed slot (or extend recommend() ~20 lines to consume live_meanR/live_n); the report targets the deactivated challenger, not the activation candidate. Note the idxrev property (rule DOWN_WEIGHTs the best live performer) to the owner before arming.
- Fold the dataset builder's input contract into the Phase-2 capture-layer spec now (zero marginal cost if done at design time, a retrofit later): per-day `<prefix>_<YYYYMMDD>_<KIND>.jsonl` naming, a candidate-microscope equivalent, and predecision_features emission on candidate rows.
- Trainer hygiene (~0.5-1 day, before any training run): per-fold numeric specs; enforce 48h purge/embargo (make week folds the default or add explicit purge); populate-or-drop f_n_competing_in_group at runtime. Also rebuild frames only on broker_clock-corrected data (F7).
- Author the B7.5 partition registry (~hours) using the June registry as a template only — Jan/Apr/May development = TRAIN/VALIDATION, March = SEALED with explicit embargo; the existing registry marks March 2026 as TRAIN and must not be passed to the builder as-is.
- Stop the wave4b/4c test bleed (~1 hour): mark their 8 permanently-red tests skipped-with-reason (inputs deleted from history) or delete them with the modules demoted to sealed evidence; they can never pass again and dilute the A/B failure-set baseline.
- VPS-side forward-substrate repair before Phase 6 (~1 session): populate packet spread_r (F36), restart/repair the three execution-evidence ledgers dead since 07-02 (V3), and fix broker_order_lifecycle_capture_v4 to capture the result side (V2) — otherwise the charter's daily-loop rung has feature-poor substrate indefinitely.
- Keep the trainer/gate/scorer/actuator unchanged through the rebuild and exempt them (plus learned_edge_walkforward_gate.py, already F23-flagged) from any deletion-tier math; the gate's baseline is production admission and it is the strongest activation-quality instrument the system owns.
- Correct FULL_VISION_PLAN Phase 4 item 1's wording in the third review: the producer's input is deep-history Stack-B splits + live packet realized evidence (and, later, SleeveBookPolicy-with-outcomes replays) — not 'B7.5 per-sleeve splits', which are broad-stack package families.
- Run the learned challenger arm only after the new engine re-runs B7.5 development windows, under a new contract generation (machine time dominated; the June precedent bounds training cost as trivial next to the 16.5h/window replay cost).
- File the June gate-run failure anatomy as an economic datum in the review: production floors admitted -1,683.5 R net on the June validation frame — corroborating evidence on broad-stack admission quality, at zero cost.

## REPORT
# Dormant learning stack vs charter destination — reader report

Worktree: `/Users/borr/GTOSActive/worktrees/review-fable-third-20260727` (integrated tree: phase2/sleeve-book-policy IS merged here via `c829dea5f`/`1e8cce4c9`, so this tree is ahead of the CLAUDE.md plan-position note).

## Q1 — Module-by-module state

All eight modules import cleanly at HEAD [MEASURED — `importlib` run, 8/8 OK]. Core learning-lane tests: **53/53 pass** [MEASURED — `tests/research_infra/test_learned_edge_dataset_builder.py`, `test_learned_edge_trainer.py`, `test_learned_edge_walkforward_gate.py`, `tests/ultimate_book/test_learning_actuator.py`, `test_runtime_learning_packet.py`, `tests/test_selector_v4_learned_mode.py`].

| module | lines | state | inputs needed / exist? |
|---|---|---|---|
| `src/research_infra/wave4b_feature_store_v2.py` | 1,497 | importable; **its 8 tests FAIL at HEAD** (with wave4c) on `FileNotFoundError` for the Wave4A route (`wave4b_feature_store_v2.py:375`) | Requires the full Wave4A ledger set; route `final_moonshot_wave4a_digital_twin_historical_microscope_2026_06_05` is **absent from this worktree AND from `/Users/borr/GTOSActive/repo`** [MEASURED]. Hardcodes a 15,679-row universe (`wave4b:397` refuses any other count) and June-5 route paths (`wave4b:22-30`). Permanently unrunnable one-shot. |
| `src/research_infra/wave4c_label_store_v2.py` | 1,540 | same — `CANONICAL_ROW_COUNT = 15679` (`wave4c:33`) | Same absent Wave4A/4B inputs. Dead one-shot; second audit's "keep as sealed evidence, do not extend" remains correct — except its tests now fail-red at HEAD and pollute the suite. |
| `src/research_infra/learned_edge_dataset_builder.py` | 770 | importable, tested green | Needs per-day ledgers named `<prefix>_<YYYYMMDD>_<KIND>.jsonl`, KIND ∈ {CANDIDATE_MICROSCOPE_LEDGER (mandatory — days without it are skipped, `builder:296-298`), MISSED_OPPORTUNITY, ORDERED_PATH_ORACLE, SIMULATED_ORDER, SIMULATED_TRADE, WINNER/LOSER_ANATOMY, EXIT_GEOMETRY} (`builder:195-206`), with `predecision_features` on candidate rows (`builder:401`). **No producer exists**: the B7.5 sealed runner emits 9 kinds — source/decision/scorecard/order/trade/oracle/missed/bucket/comparison — and no candidate-microscope (`b7_5_post_acceleration_runner.py:59-67`); `predecision_features` is computed at `broader_origin_generators.py:1929` and appears in no emitted ledger [MEASURED — repo-wide grep hits only generator, builder, scorer]. Registry: **`ULTIMATE_EDGE_PARTITION_REGISTRY.jsonl` EXISTS at HEAD** (contra SECOND_AUDIT item 4) at `research/operations/final_moonshot_v4_ultimate_mechanical_edge_2026_06_10/` — 5,792 B, committed (`41425481e`), 10 partitions incl. purge=48h/embargo=1-day doctrine. **But its TRAIN range 2025-06-02..2026-04-17 spans B7.5's sealed March-2026 challenge window** — reusing it would admit sealed days as TRAIN; it is a template, not an authority [MEASURED — registry dump]. |
| `src/research_infra/learned_edge_trainer.py` | 719 | importable, tested green | Needs a builder-produced training frame. None exists: the June frame path recorded in the artifact (`train_ledgers/ULTIMATE_LEARNED_EDGE_TRAINING_FRAME_V1.jsonl`) is **absent from HEAD** [MEASURED]. Three audit-filed statistical flaws all still present (see Q2). |
| `src/research_infra/learned_edge_walkforward_gate.py` | 570 | importable, tested green | Needs frame + artifact. Criteria verified (Q2). |
| `src/components/learned_edge_layer_v4.py` | 489 | importable; pure-python scorer, no sklearn; fail-closed refusals verified (`:433 refused_artifact_invalid`, `:444 refused_missing_required_features`) | Needs a valid frozen artifact. The only one ever produced (`ULTIMATE_LEARNED_EDGE_LAYER_V1.json`, June 10) **expired 2026-06-24** (`valid_through_utc`) — the scorer would refuse it today. |
| `src/components/ultimate_book/learning_actuator.py` | 106 | importable, tested green; pure function | Needs `SleeveEvidence` (per-sleeve train/oos/sealed meanR + n). **Constructed nowhere outside its test** [MEASURED — `train_meanR` grep hits only actuator + test]. Note: `live_meanR`/`live_n` are declared fields but `splits()` excludes them — **live evidence cannot influence `recommend()` as written** (`learning_actuator.py:45-51`). Vestigial `self.sealed_n if False else self.oos_n` at `:46` (functionally correct, aborted edit). |
| `src/components/ultimate_book/runtime_learning_packet.py` | 790 | importable, tested green; **live-enabled**: bridge default False (`bridge.py:105`) but `agent_config.yaml:1253-1256` sets `ultimate_book_runtime_learning_packet_enabled: true`, writing `shadow_logs/ultimate_book_runtime_learning_packets.jsonl` on the VPS | Consumes book runtime events; 12 event types (`:33-46`); hashes tickets/accounts (F33: unsalted SHA-256, reversible). `spread_r` is a passthrough of an outcome field nobody populates (`:411`) — F36. |

Adjacent: `selector_v4` learned integration is real and default-off — `selector_v4_learned_edge_enabled` **appears nowhere in `config/`** [MEASURED], defaults False at `selector_v4.py:2388`; learned admission floors replace heuristics inside the calibrated-admission guard (`selector_v4.py:2661-2681`); learned continuous sizing is a threshold-gradient multiplier with segment-reliability shrinkage (`selector_v4.py:4936-4967`). Both `config/agent_config.yaml` and `src/components/selector_v4.py` are R2-contract-bound [MEASURED — R2 `common_behavior_inputs`], so enabling the learned arm is by construction a new contract generation, exactly as `FULL_VISION_PLAN.md:283-286` says. A scheduler learned-ranking lane also exists (`src/research/moonshot_scheduler_v4_best_trade_allocator.py`); **6 of its 10 tests fail at this HEAD with behavioral assertion errors** (`tests/research_infra/test_scheduler_v4_learned_ranking.py`) [MEASURED; not A/B'd — likely part of the ~684-bad sparse baseline, but unlike the wave4 failures these are assertions, not missing inputs].

## Q2 — Is "dormant but sound" actually true?

**Verdict: CONFIRMED for gate/scorer/actuator; trainer is sound-with-three-known-flaws, none fixed in the eight sessions; and there is stronger evidence of soundness than the second audit cited — the loop already ran end-to-end once and the gate correctly refused a degenerate pass.**

Trainer split hygiene, read directly:
- EB categorical encodings ARE per-fold, computed on `train_rows` only inside the fold loop (`learned_edge_trainer.py:391-403`) [VERIFIED]. Beta calibration fits on OOF predictions only (docstring + code). Leakage canary hard-raises `LeakageCanaryError` and refuses to emit if shuffled-label AUC leaves 0.5±0.07 (`:564-567`) [VERIFIED].
- **Flaw 1 still present:** `numeric_specs = _numeric_specs(rows)` on the FULL frame before folding (`:562`) — clip bounds/mean/std see the test day (OOF optimism).
- **Flaw 2 still present:** day-blocked leave-one-out with no purge/embargo for the ~48 h label horizon; a `fold_granularity="week"` option was added whose own comment concedes it keeps the boundary only "at the block edge … except inside-block adjacency, which day folds share anyway" (`:548-559`). Also note this is CV, not walk-forward: train folds include days after the test day; temporal honesty lives in the partition roles (TRAIN vs VALIDATION) consumed by the gate, not in the folds.
- **Flaw 3 still present:** `f_n_competing_in_group` is populated by the builder (`builder:397`) and has **zero occurrences in `selector_v4.py`** [MEASURED] — trained-on, never served.

Walkforward gate criteria, read directly (`learned_edge_walkforward_gate.py`): **seven mechanical gates**, fixed thresholds, `overall_pass = all(...)`, None fails closed (`:446-501`): Brier ≤0.24, ECE ≤0.10, learned-admitted net-R ≥ heuristic-admitted net-R, capture ratio ≥0.25, admitted loser-rate ≤ heuristic, overlap-of-learned <0.80 (anti-mimicry; **None when zero learned admissions → fail**, `:196-210`), refusal rate ≤0.01. The heuristic baseline is the exact production floors p≥0.58 / EV≥0.10 (`:78-80`) — a pass is literally a beats-production claim. A CSCV/PBO diagnostic is computed as telemetry, non-gating (`:441-445`).

**The decisive soundness evidence the audit did not cite:** the loop ran once for real. `ULTIMATE_LEARNED_EDGE_LAYER_V1.json` (route `…mechanical_edge_2026_06_10`): **175,009 rows used, 46 folds, canary AUC 0.5034, artifact emitted** [MEASURED]. `ULTIMATE_LEARNED_EDGE_WALKFORWARD_GATE_V1.json`: **`overall_pass: false`** on 8,199 validation rows — passed Brier (0.125), ECE (0.004), net-R (0.0 ≥ **−1,683.53**), loser-rate, refusal; **failed capture_ratio (0.0 < 0.25) and overlap (None, fail-closed)** [MEASURED]. The trained model admitted zero validation candidates at t_trade=0.15 — and the gate refused the trivial "admit nothing beats a losing baseline" exploit by exactly the two gates designed for it. Two side facts worth carrying: (a) the heuristic production floors admitted **−1,683.5 R net** on that June validation frame — an independent datum consistent with the go-live dossier's −0.25 R/fill verdict on the broad stack's admission quality; (b) the data-supply claim is proven at scale (175k rows from replay ledgers, not 72 trades).

Caveat inherited unchanged: all of this trains on labels carrying the F7 clock defect (session-conditioned features on a mislabeled clock) until frames are rebuilt on `broker_clock`-corrected data.

## Q3 — The book lane: how close to closable, really

**Consumption path: fully implemented, merged, tested, and one config map away.** `ultimate_book_learning_rerate: {}` default (`bridge.py:95`), fail-safe parser (`bridge.py:197-205`), GATE=0.0 drops intents pre-sizing (`admission.py:1079-1080`), bounded multiplier clamped [0, 1.25] enters the sizing product (`admission.py:997-1001, 1166`), activity recorded (`book_owner.py:567`), and the packet schema already carries a `learning_rerate_verdict` slot (`runtime_learning_packet.py:412`). Actuation semantics: default-off, recommendation-only; `MIN_N=30`, `MAX_UP=1.25`, gate-to-zero allowed, never size-up beyond 1.25 (`learning_actuator.py:25-28`).

**Producer: does not exist, and the plan's named input does not exist either.** `FULL_VISION_PLAN.md:274-278` says the producer "reads the B7.5 per-sleeve split evidence". Three measured problems:
1. **B7.5 has no per-W7-sleeve splits.** Its sleeve vocabulary is `fpsc_*` package families of the broad Stack A (`ULTIMATE_CANDIDATE_PACKAGE_SLEEVE_REGISTRY_LEDGER.jsonl`, 82 rows — `fpsc_avoid_failure_feature_sleeve__breaker_re_entry__…` etc.) [MEASURED]; the word "sleeve" appears zero times in the January analysis script and acceptance audits [MEASURED]. B7.5 splits would rerate the broad stack's package families — which the book does not run. The plan wording is a stack-conflation.
2. **What does exist for W7 sleeves:** (a) June deep-history results in-tree with per-cell `base_train`/`base_fwd` `{n, meanR, win}` (`KB6_DEEP_STACKS_RESULT.json`, `A8_BOOK_MC_RESULT.json` base train/oos blocks) [MEASURED] — but this is the Stack-B evidence chain SECOND_AUDIT explicitly ordered adversarially audited (geometry_lib fill semantics, split hygiene) before it becomes load-bearing; extraction to per-sleeve-name splits is real work, not a read. (b) Live packet-derived realized per-sleeve (G1B §3 table): only `fx_jpy` (n=32) and `idxrev` (n=59) clear MIN_N=30; five sleeves were entirely silent; **zero placements came from any train-validated sleeve** (`THIRD_REVIEW_UNCONSTRAINED_PROMPT.md:125-127`, `GATE_G1B_RECEIPT.md:230-236`).
3. **The rule cannot consume live evidence as written** — `splits()` excludes `live_meanR/live_n` (`learning_actuator.py:45-47`). A producer must map live→the sealed slot explicitly or the rule needs a ~20-line extension. Property worth noting: with train −0.065 (n=4858) and live +0.174 (n=59), `idxrev` — the best live performer — triggers the `neg_heavy` branch and gets DOWN_WEIGHT ×0.5; the rule is conservative by construction, and the owner should see that before arming.
4. **Operational coupling:** the arm switch lives in `agent_config.yaml`, which is R2-bound (H1) — arming the live book by editing it makes the next replay fail closed unless the contract is regenerated or a live-only overlay is used.

**Distance: one session to a reproducible owner-armed recommendation report** (as the plan says) — but only if the producer honestly declares (a) Stack-B chain unaudited, (b) live-as-sealed mapping, (c) that the report points at the *deactivated challenger* (OD-1 chose the broad stack; the receipt's §8 correction), not the activation candidate. Also relevant: `SleeveBookPolicy` is now merged (`src/research_infra/replay_policy/sleeve_book.py`, 512 L; validated 4,923 cycles / 647 units / zero decision disagreements against the packets; 15 of 29 sleeves covered; G2 overall not met) — but it is **decision-level only** (15 placement gates unapplied, no fills, no outcomes), so it cannot yet generate per-sleeve R splits; it becomes the split producer only once an outcome layer exists.

## Q4 — Are the 99,112 packets usable training data today?

**What they carry** (schema `ultimate_book_runtime_learning_packet_v1`, `runtime_learning_packet.py:250-470`): identity (sleeve, symbol, direction, cluster, decision bar/day, candidate_id), admission/skip/decision reasons, placement status, full broker outcome on terminal events (entry/exit prices, per-deal commission/swap/fee, aggregate realized P&L, SL/TP, reconciliation statuses), policy-clock state, and a `bridge` block with the **full flag set, governor decision, and would/realized units** per cycle. Redaction is hashed tickets/accounts (F33: unsalted, reversible — which is also what made cross-validation possible).

**Usable TODAY — four demonstrated uses** [all MEASURED by the Phase-1/2 sessions]:
1. **Per-sleeve live realized economics** — the G1B per-sleeve table (n, net, mean R, gross R) is built from them; this is the `live` half of any SleeveEvidence.
2. **Exact cost-model inputs** — commission+swap = −1,984.35 (31.6 % of the W7 loss), 0.0591 R/trade commission at 100 % coverage; the direct repair input for F38 (validation and live pre-trade engine charge zero commission at five sites).
3. **Entry-slippage distribution** — requested-vs-filled join gives mean +0.0132 R (n=140), corroborated at +0.0108 R (n=147) by `slippage_runtime`; stops fill clean (−1.0036 R gross median) — the excess is cost, not slippage (`GATE_G1B_RECEIPT.md:373-383, 650-661`).
4. **Decision-replay validation substrate** — the bridge block is what made the SleeveBookPolicy port provable (zero disagreements over 38 days; 130/132 transmitted risk-percents reproduced).

**NOT usable as learned-edge training input**, for reasons that are structural, not fixable by volume:
- **Feature side:** none of the 43-name whitelist features exist in packets — no `f_pd_*` geometry, no session-range/ATR features; `spread_r` is declared and **NULL on all 99,112 rows** (F36, `GATE_G1B_RECEIPT.md:353, 662-664`); what exists instead of spread is the cost-gate *rejection* strings — a censored, refused-trades-only sample.
- **Label side:** genuine broker-real labels exist only for ~175 closed positions — no counterfactuals (no missed/oracle/risk-rejected rows, which is the builder's entire anti-selection-bias design), five sleeves silent, zero train-validated-sleeve placements, and 43.4 % of placements from two `breadth_falsified` sleeves. It is an evaluation set for the live window, heavily censored, not a training population.
- **Forward substrate is degrading:** the three execution-evidence ledgers (`execution_manager_v4_decisions`, `broker_order_lifecycle_capture_v4` — request-side-only anyway, V2 — and `slippage_runtime`) have written **nothing since 2026-07-02** while packets keep accumulating (V3): "volume, not execution evidence" (`VPS_EXPORT_FINDINGS.md:60-84`).

## Q5 — Verdict for the unconstrained architecture

**Where learning genuinely sits:** it is not a workstream of its own and not the current bottleneck — it is the *first consumer* of the Phase-2 capture layer. Every dead point in the lane (builder input contract, predecision-feature emission, packet feature-poverty) is the same schema problem the performance rebuild must solve anyway (F5's core claim — verified still true after eight sessions, none of which touched the lane's flaws or its inputs). Keep trainer/gate/scorer/actuator as-is (sound, tested, and once-proven end-to-end); spend zero effort on wave4b/4c beyond silencing their permanently-red tests; and make the new engine's typed-event emission natively satisfy the builder's contract (per-day ledger naming + candidate-microscope + `predecision_features`) rather than building any separate "feature store". The walkforward gate deserves protected status in any deletion math (F23) because its baseline *is* production admission — it is the strongest activation-quality instrument the system owns once fed.

**Minimum first closed loop:** the book-lane rerate report — one session, consumption path already merged, tested, and live-schema-visible. But its honest yield is governance-demonstrative (an evidence-driven, bounded, owner-armed re-rating of a *deactivated challenger* whose worst sleeves the owner already turned off by hand), and it requires resolving the three input caveats above. The first loop that touches the **activation candidate** is the candidate lane, and it has a hard dependency chain: Phase-2 engine emission → re-authored partition registry (hours; June registry is a template whose TRAIN range currently swallows sealed March) → three trainer hygiene fixes (~half-day) → frame from re-run development arms → train (machine time; June precedent: 175k rows) → walkforward gate → challenger arm under a new contract generation. Nothing in that chain argues for reordering the plan's Phase 2→4 sequencing; what it does argue is (a) fix the plan's Phase-4 wording (B7.5 splits ≠ book-sleeve splits), (b) fold the builder contract into Phase 2's capture-layer spec now so it is not retrofitted, and (c) repair the forward substrate (spread_r F36, execution ledgers V2/V3) before Phase 6, or the charter's "daily learning loop" rung will stand on feature-poor observation data indefinitely.