# read_live-surface (a8e4c80c9bb8d1a40)

## HEADLINE FACTS
- The VPS brake is three false YAML booleans, not one: apply_to_execution/live_activation_allowed/live_broker_authority all false in the exported working-tree agent_config.yaml:1161-1163 and redacted_account.yaml:11-13; CLAUDE.md §4's 'one config line' is wrong on count, right that no second mechanism exists [MEASURED]
- ultimate_book_live_broker_authority: true has never existed in any committed config — gate 4 and the book_owner mutation suppression were introduced by the 2026-07-02 shadow-flip commit itself (441708da8: parent 0 hits, commit 9 hits), so restoring any historical config cannot re-arm the VPS; accidental single-line drift also cannot fire [MEASURED]
- The activation token exists on mainline only: VPS RealMT5.order_send calls the raw module with no guard (git show redacted_host:src/mt5/mt5_real.py), and live_system_of_record.md:164 records it as an unowned, undated carry-back [MEASURED]
- Mainline's fail-closed layer is real and well-tested: RealMT5.order_send (mt5_real.py:433-449) refuses exposure-increasing requests without a valid HMAC-signed, account/namespace/config-bound, max-720h token; 36 dedicated tests + a mutating-surface census; no fail-open path on the token side [VERIFIED]
- Genuine hole in the never-trap invariant: RealMT5.get_positions masks broker fetch errors as [] and filters by MAGIC_NUMBER (mt5_real.py:321-333), so the classifier's allow-unverified-close branch is dead code on the real adapter — a close under token-absent + transient fetch error (or a non-magic position) is refused as exposure-increasing [VERIFIED]
- Config-digest token binding does not cover .env: GTOS_UB_DERISK_MODE overrides the YAML derisk_mode (agent_config.yaml:1337-1340), so the 2.0% dial's mandatory safety pair can change without invalidating a config-bound token [VERIFIED]
- scripts/mt5_preflight.py remains a raw mt5.order_send path (lines 136-155) with no halt and no token — the one ungated broker-mutating script family member on a credentialed host [MEASURED]
- No monitoring watches the authority-gate block: .tools/monitor_books.py has zero gate/authority checks, so a deliberate or erroneous three-line flip would be detected by nothing [MEASURED]
- The halt-flag layer is inert everywhere: flags are absence-checked (missing=clear, runtime_halt.py:160-179), no flag file exists on the VPS (only one archived 2026-05-18), supervisor Running, terminals connected, trade_allowed true on both [MEASURED]
- Zero of the charter's 7 culmination-A items are fully met; items 5 and 6 are partial; the campaign (April 16/30 + 3 arms, May, March challenge), OD-1/G1b, the dead execution ledgers (23+ days), and the unassembled canary package are the binding gaps [VERIFIED against plan-position and VPS export]
- Phase 6's stated evidence premise is broken: the VPS accumulates candidate/packet volume but execution_manager_v4_decisions, broker_order_lifecycle_capture_v4 and slippage_runtime have written nothing since the shadow flip, and lifecycle capture has request-side rows only (594 rows, no fills) [MEASURED per VPS_EXPORT_FINDINGS V2/V3]
- The charter standard's 'credentials are absent' is false on the host today — authenticated terminals with trade_allowed true, in shadow, pre-canary — and this divergence is unstated in the plan [MEASURED]
- A minimal token-only VPS carry (activation_token.py + mt5_real guard + run_book context call + P4 create_mt5 fix) is deployment-safe while gates are false (order_send never fires in shadow) and converts the config-only brake to presence-based fail-closed for ~half a day of work [INFERRED from code structure]

## RECOMMENDATIONS
- Deploy the token guard to the VPS now as a standalone carry decoupled from the convergence-coupled book redeploy: activation_token.py + its two import deps, the mt5_real.py order_send guard, run_book.py's set_activation_context call, and P4 create_mt5 mode validation; verify with gtos_activation_token.py status (zero tokens) and one healthy shadow cycle. ~0.5-1 day incl. a VPS session; near-zero runtime risk while gates are false.
- Fix the masked-[] close-refusal hole: make RealMT5's positions_provider distinguish fetch-error (None) from genuinely-empty, and decide the MAGIC_NUMBER filter's role for classification (classification should see all positions, not only engine-magic ones); add the token-absent+fetch-error close test. ~1-2 h.
- Add a gate tripwire: monitor_books.py (or the daily digest) asserts the four ultimate_book gates and alerts on any drift from the expected shadow values; also alert if a token appears/disappears. ~2-4 h.
- Gate or delete scripts/mt5_preflight.py (route through authorize_raw_broker_request exactly as fn_smoke_trade.py does) and sweep backfill_2026_04_29_nas100_sl.py into an archived/never-execute state. ~1-2 h.
- Close the .env hole in config binding: either include the resolved derisk_mode (and any live-behavior env override) in config_digest_for, or forbid the env override when a token binds the config. ~2-3 h + tests.
- Amend FULL_VISION_PLAN Phases 6-7: (a) name the VPS redeploy as an explicit step with its §6 carry list and verification; (b) re-scope G6 evidence to what exists (packets) with the execution-evidence clock restarting at the lifecycle-capture repair; (c) add the token-specific chaos drills (gate-flip-without-token, expiry-mid-position+fetch-error, dir unreadable, clock skew, revoke-then-restore, wrong Windows user); (d) state the credentials-present-in-shadow divergence from charter A item 6 for the owner to accept or remediate. ~0.5 day of plan surgery.
- Repair the broker_order_lifecycle_capture_v4 result side before Phase 6 formally starts — it is the named cost-calibration source and currently records intent only. ~1-3 days.
- Document revocation semantics in the operator runbook: revoke is file-removal, restore-from-backup re-authorizes until expiry; short-lived tokens (24-72 h) during canary are the compensating control. ~1 h.
- Correct CLAUDE.md §4's brake description from 'one config line' to 'one config layer (three booleans, no independent mechanism, no monitoring)' and record that gate-4 true has no historical precedent — the current text both overstates the accident risk and understates the deliberate-edit risk. ~15 min.

## REPORT
# Live activation surface — third-review reader report

## Q1. Exposure-increasing order path, end to end

### (a) Mainline (this worktree, HEAD of review branch)

Chain: `scripts/run_book_supervisor.ps1:105-113` starts one `run_book.py` per account (`profile=operator_profile` + `profile=redacted_account`, per-account kill flags `ULTIMATE_BOOK_KILL_ftmo.flag`/`_fn.flag`, `run_book_supervisor.ps1:86-87`) → `run_book.py:main` (mutex+file-lock+pid single-instance guards `run_book.py:126-177`; account-identity fail-closed `run_book.py:221-232`; `mt5.set_activation_context(namespace, config_digest)` `run_book.py:239-240`; hedging-account fail-closed `run_book.py:266-279`) → `BookLauncher.tick()` (`src/components/ultimate_book/launcher.py:204-211`: kill/halt flag file checks → `place=not (killed or halted)`) → `UltimateBookOwner.run_cycle(place=...)` (`book_owner.py:1458-1498`) → bridge four-gate (`bridge.py:479-494`) → per-intent placement → `execution.py open_trade` (runtime-halt enforce at `execution.py:3098`) → `self.mt5.order_send` (5 sites: `execution.py:6699` via `executor.submit`, plus `9733/9774/9883/9924`) → **`RealMT5.order_send` choke point** (`src/mt5/mt5_real.py:433-449`) → `enforce_broker_mutation_authorized` (`src/safety/activation_token.py:613-619`).

What blocks an exposure-increasing order today on mainline, in encounter order [all MEASURED]:
1. **Gate 3 false**: `ultimate_book_live_activation_allowed: false` (`config/agent_config.yaml:1250`) → `runtime_effect_now=False` (`bridge.py:487-490`) → `run_cycle` returns shadow before any placement (`book_owner.py:1492-1498`).
2. **Gate 4 absent** from mainline config (rg over `config/` finds no `ultimate_book_live_broker_authority` key) → `DEFAULT_CONFIG` False (`bridge.py:70`, check at `bridge.py:491-494`); gates 1-2 are true (`agent_config.yaml:1248-1249`; note mainline `config/profiles/redacted_account.yaml:11` still carries the pre-shadow `apply_to_execution: true`).
3. **book_owner mutation suppression**: `_live_broker_authority()` False suppresses placement, SL/TP modify, close AND flatten (`book_owner.py:244-256`; suppression sites 2257, 2316, 2351, 2419, 2607; flatten suppression is owner-decided B21).
4. **Runtime halt guard**: armed (`runtime_control.enabled: true`, `agent_config.yaml:596-597`) but all three flag paths are absence-checked and **missing = clear** (`src/safety/runtime_halt.py:160-179`); repo_root is CWD-relative when unconfigured (`runtime_halt.py:110`). Not currently a brake anywhere.
5. **Activation token**: absent token → `activation_token_absent` → exposure-increasing refused at the choke point (`activation_token.py:588-594`). The only presence-based fail-closed layer, and it exists **on mainline only**.

### (b) VPS deployed branch (`redacted_host`, working tree confirmed identical via export)

- Config gates [MEASURED, `/Users/borr/GTOSActive/vps-export-20260725/extracted/03_config/agent_config.yaml:1160-1163` = branch bytes]: `enabled: true`, `apply_to_execution: false`, `live_activation_allowed: false`, `live_broker_authority: false` — **three false booleans**, mirrored in `redacted_account.yaml:11-13`; `operator_profile.yaml` has no gate overrides (base applies).
- **No token**: VPS `RealMT5.order_send` calls `self._mt5.order_send(request)` directly with no guard (`git show redacted_host:src/mt5/mt5_real.py` — zero `activation` hits) [MEASURED]. `live_system_of_record.md:153-164` records the token as mainline-only carry-back, with P4 `create_mt5` mode-validation (typo → real broker connection) and the convergence-module coupling (`TypeError` every cycle if `book_owner.py` deploys without the packet module).
- **No flag files** on the VPS: only `RESEARCH_RUNTIME_HALT.flag.archived_2026-05-28_vnext_activation` exists (`VPS_EXPORT_FINDINGS.md:20-27`; export `01_flags_and_safety/`); supervisor Running, both books healthy, terminals connected, `trade_allowed: true` on both (`VPS_EXPORT_FINDINGS.md:29-32`) [MEASURED]. `runtime_control.enabled: true` on the VPS too — the guard runs and finds nothing.
- The VPS code **does** have the gate-4 conjunction and book_owner suppression (`git show redacted_host:src/components/ultimate_book/bridge.py:451`, `book_owner.py` 9 hits) [MEASURED].

### Verdict on CLAUDE.md §4 "the entire brake is one YAML boolean"

**Wrong on count, right on kind, and understates one protection.** [MEASURED]
- Count: the VPS brake is **three** false booleans in one block (`agent_config.yaml:1161-1163`) + three in `redacted_account.yaml:11-13` — a config *layer*, not one line. Firing requires the conjunction of gates 2+3+4 all true (`bridge.py:479-494` on both lineages).
- Kind: correct — a single mechanism with nothing behind it. No flag file, no terminal block, no scheduler stop, token absent from the VPS lineage, host funded/connected/supervised.
- Understated protection: `ultimate_book_live_broker_authority: true` **has never existed in any committed config**. Gate 4 and the book_owner suppression were introduced *by* the 2026-07-02 shadow-flip commit (`441708da8`: parent has 0 `_live_broker_authority` hits, commit has 9) [MEASURED]. The pre-shadow live-era config (`441708da8^:config/agent_config.yaml:1161-1162`) has gates 2-3 true and **no gate-4 line** — so restoring any historical config onto today's VPS code yields gate 4 absent → default False → still no mutation. Accidental single-line drift also cannot fire (any one gate alone is insufficient). The realistic activation-by-accident scenarios CLAUDE.md worries about (bad merge, hand-edit of one line, stale overlay) each fail closed; what remains is a deliberate three-line edit — and nothing *monitors* the gate block for such an edit (`.tools/monitor_books.py` has zero authority/gate checks [MEASURED]).
- Residual ungated paths on a credentialed host: `scripts/mt5_preflight.py` drives raw `mt5.order_send` (pending place + cancel, lines 136-155) with **no halt and no token** [MEASURED — zero activation imports], consistent with H6; `scripts/backfill_2026_04_29_nas100_sl.py` likewise raw. `fn_smoke_trade.py` is gated (`authorize_raw_broker_request` at line 363); `dual_broker_execution_follower.py` goes through `engine.open_trade` → RealMT5 (token-covered) plus its own halt check `enabled_default=True` (`dual_broker_execution_follower.py:2266-2275`).

## Q2. Token mechanism soundness

**Design [VERIFIED by reading the full module + its 36 tests]:** HMAC-SHA256 over a canonical sorted-JSON of 10 `_SIGNED_FIELDS` (`activation_token.py:87-98, 204-216`), key `secrets.token_hex(32)` chmod-0600 beside the tokens; `hmac.compare_digest`; expiry/not-before/account/namespace/config-digest checks all fail-closed, including "binding declared but unanswerable = denial" (`activation_token.py:509-528`). Mint lifetime capped at 720 h (`:83, 239-245`). Token file keyed by account digest so cross-account consumption is impossible (`:158-162`). Fresh clone/stale dir/unreadable dir/malformed token/missing signing key all **deny** (`read_token:455-475`, `verify_token:492-494`) — no fail-open on the token side. Honest threat model stated in-module: tamper-evidence, not adversary-proof (key beside tokens, `:33-41`). Test surface: 36 tests incl. per-field signature coverage, expiry-still-allows-flatten, mutating-surface census with named exemptions (`tests/safety/test_activation_token.py:510-547`), single-definition classifier assertion, and 8 raw-script guard tests.

**Risk-reducing bypass:** closes/partial closes (five-condition test, single definition shared with `execution.py:510-524`), pending cancels, stop tightens pass tokenless (`classify_request:370-425`). Stop-widen, stop-remove (sl<=0), pending placement, order-modify, unknown actions all require a token. Partial close volume ≤ position volume with opposing type → reducing; over-volume or same-direction → increasing [VERIFIED, tests :286-351].

**Holes found (ranked):**
1. **[VERIFIED — code path] The unverified-close bypass is dead code on the RealMT5 path, and its replacement direction violates the never-trap invariant.** `RealMT5.get_positions` masks a broker fetch error (`positions_get` → None) as `[]` (`mt5_real.py:321-323`), and additionally filters to `p.magic == MAGIC_NUMBER` (`:333`). The classifier's deliberate allow-on-unreadable branch (`activation_token.py:394-404`) fires only when the provider **raises**; a masked `[]` reads as "readable, position not found" → a genuine close classifies `exposure_increasing_deal_on_position` → with an absent/expired token the **close is refused**. Window: token lapsed mid-position + transient fetch error (or a position without the engine magic closed via this adapter). Bounded by engine retry-next-tick and by the raw-module flatten paths (heartbeat_monitor, flatten_all_positions — census-exempt), but it is a real contradiction of the module's own prime invariant (`:19-25`). Fix: have the positions_provider distinguish None from [] (raise or return None → unreadable), ~1 h + test.
2. **[VERIFIED] Config-digest binding does not cover `.env`.** `config_digest_for` hashes base-config + profile file bytes only (`activation_token.py:778-805`); `GTOS_UB_DERISK_MODE` in `.env` overrides YAML `derisk_mode` (`agent_config.yaml:1337-1340`) — the mandatory safety pair of the 2.0 % dial can change without invalidating a config-bound token. Same for CLI flags (`--tags`).
3. **[VERIFIED] Revocation is file-removal only; restore-from-backup silently re-authorizes** (signature and expiry still valid). Expiry (≤720 h) is the only backstop. Acceptable if the runbook says so; currently only the mint cap comments gesture at it.
4. **[INFERRED] Local-clock dependence**: expiry compares to machine UTC (`verify_token:499-504`); a VPS clock far behind keeps a lapsed token alive. No broker-time cross-check.
5. **[VERIFIED, conservative-direction] TP-only SLTP modifies that omit/zero `sl` classify increasing** (`sltp_reduces_or_preserves_risk:351-367`) — blocks a profit-lock tighten without a token; `TRADE_ACTION_CLOSE_BY` classifies unknown → token required. Operational friction only, fails safe.
6. **[VERIFIED] Windows deployment footgun**: default dir `~/.gtos/activation` resolves per Windows user; mint and the scheduled-task user must match or every mint silently lands in the wrong profile → permanent denial (fail-closed, but confusing).

Stale token dir **cannot** fail open [VERIFIED]: every read/verify failure path denies; audit-append failures are swallowed by design and cannot block anything (`append_activation_audit:690-705`).

## Q3. Distance to CONTROLLED_CANARY_READY_PENDING_HUMAN (charter A, `GTOS_ULTRA_GOAL.md:52-62`)

1. **Causal multi-window selection/risk/interaction decision** — NOT MET. April S1R1 stopped at 16/30 sealed days, no arm receipt; April S0R0/S1R0/S0R1, May, freeze, sealed March all unrun (CLAUDE.md §4). ~3+ windows × 16.5 h serialized (H3 forbids parallel arms).
2. **Strongest policy integrated/frozen/fingerprinted; rejected behavior off order paths** — NOT MET. OD-1 is conditional on the outstanding W7-forensics gate G1b (Phase 1 item 2). Mechanical rejected-behavior guard exists (replacement invariant, `bridge.py:472-477`); no frozen fingerprinted policy package.
3. **Canonical R + cash economics from the same ledgers** — NOT MET. The three execution ledgers dead since 2026-07-02 (`VPS_EXPORT_FINDINGS.md` V3); lifecycle capture is request-side only, "a build item, not a data pull" (V2); broker ground truth now exists as reconciliation baseline (V5: FTMO +7,879.56 / FN −3,770.72, both reconcile exactly).
4. **Policy-frozen untouched/forward challenge under predeclared rules** — NOT MET (follows item 1; sealed March is the designated challenge).
5. **Decision-material verification of code/tests/replay/parity** — PARTIAL. Phase 1 = 3/5 (G1a done w/ F31; live-path tests; clock truth); W7 forensics + divergence matrix v2 outstanding; Phase 2 = 1/6, G2 not met; suite not green (10 failures, F13); H7 divergence matrix v1 says replay ≠ live system. Token layer itself is well-verified (44 tests).
6. **FORWARD_SHADOW_ACTIVE + canary package properties** — PARTIAL/NOT MET. VPS shadow runs continuously but on the pre-OD-1 lineage; execution evidence dead (V3); token not deployed (`live_system_of_record.md:164`); no immutable package, no chaos drills, kill/rollback unproven; and the standard's "credentials are absent" is **false on the host today** — terminals authenticated, `trade_allowed: true`, in shadow, pre-canary (V1). This divergence between charter text and deployment reality is currently unstated anywhere.
7. **Evidence supports a candid authorize recommendation** — NOT MET (cannot precede 1-6).

## Q4. Adequacy of FULL_VISION_PLAN Phases 6-7 and what to change

The Phase 6 → 7 shape (shadow → calibrate → immutable package → G7 = standard A) is right, but four specifics are wrong or missing given the measured VPS state:

1. **Token deployment sits in Phase 7; it belongs now.** `VPS_EXPORT_FINDINGS.md:46-49` already concludes porting it is "the highest-value deployment item in the programme — ahead of Phase 7", but the plan text is unamended and `live_system_of_record.md §6` leaves it as a passive carry-back obligation with no owner or date attached to "whoever next redeploys." A minimal token-only carry (module + `mt5_real.py` guard + `run_book.py` context call + P4 `create_mt5` fix) avoids the convergence coupling entirely and is inert in shadow (order_send never fires while gates are false), so deployment risk is near zero.
2. **Phase 6's evidence premise is broken.** "Weeks of forward-shadow data already exist" counts candidate/packet volume; the execution evidence G6's cost calibration needs stopped the day shadow began (V3) and the lifecycle channel never captured a fill (V2). G6's "count what exists" must be re-scoped: packets yes, execution evidence starts at zero after the channel repair.
3. **Phase 6 quietly assumes the redeploy.** "The chosen policy (per OD-1) runs continuously" requires re-pointing the VPS at a new build — the same deploy that must carry §6's three items. The plan never names this deploy as a step with its own verification (token status shows zero tokens; shadow cycle still healthy; conflict-free carry of the convergence pair).
4. **No tripwire on the gate block, and no drills that exercise this exact geometry.** Nothing alerts if `agent_config.yaml:1161-1163` changes (`monitor_books.py` checks liveness only). Phase 7's chaos list (kill feed, kill MT5, malform ledger) should add: gate-flip-without-token (expect refusal at the choke point), token-expiry-mid-position + positions-fetch-error (the Q2#1 hole), token-dir unreadable, clock skew, revoke-then-restore-from-backup, and a run under the wrong Windows user. It should also gate or delete `mt5_preflight.py` on the VPS (the one raw-module mutation script with neither halt nor token) and bind the `.env` derisk override into the config digest or move it to YAML.

One judgment the review should carry: the system is *not* "one config line from live" — it is one *deliberate three-line edit plus zero independent mechanisms* from live on a funded host, and the fail-closed layer that would make that edit safe exists only on the build machine. The distance is small in bytes and large in intent; closing it is a half-day of deployment, not a phase.