# read_g1b (a7ac0fc0f637afa75)

## HEADLINE FACTS
- G1b verdict: PASS on attribution (residual 0.000000 over all 175 W7 positions, cent-exact against broker balances, adversarially confirmed by two independent routes) — but the substantive finding is a reframe: the W7 live window does not measure the W7 book. 88.5% of core-8's confidence weight never traded; the five high-confidence sleeves (incl. metals_core conf 1.00) placed nothing in 14 days; the three that traded are the registry's three weakest (conf 0.15, two breadth_falsified, one forward_only). [MEASURED]
- The 2.00% dial was structurally unreachable: unit worst-case stop = base x sleeve_confidence (admission.py:1038-1040) and the highest confidence that traded was 0.40, capping any unit at 0.80%. The dial choice explains 37.5% of the loss's magnitude and none of its sign (at 1.25% the book still loses). [MEASURED]
- W7's own loss is -2.5516% (FTMO) / -3.7359% (FN) = -6,287.49 = -3.1437% of the 200k base; SECOND_AUDIT's FTMO -5.3% was the account, half of it the pre-W7 fleet; 99.1% of FN's account loss is W7's; FTMO's +7.88% 'net since inception' is two hand-placed phone trades (+13,166.69). [MEASURED]
- F38 (HIGH): validation AND the live pre-trade engine charge zero commission at five independent sites (broker_net_cost_engine.py:577-583 verified at HEAD: total_cost_r = spread + slippage + swap), behind a circular gate that accepts a status string emitted by the code it gates, with selected_cell_risk_decision_basis null on all 149 live intents. Realized commission: 0.0591 R/trade, 37.8% of absolute gross P&L, concentrated exactly on the sleeves that lost (USDJPY 0.1948 R) while all six index CFDs pay zero — and idxrev, the only profitable sleeve, is the only one on a commission-free instrument set. [MEASURED]
- F39 (HIGH): the MC that authorized the book applied tick erosion with the wrong sign — INTEG_W7_FINAL_RESULT.json grants USDJPY a +0.0179 R credit where reality charges ~-0.195 R (values byte-verified at HEAD), a ~0.2 R/trade optimistic error on the family that lost 9 of 9; the cost map has no generator and is not re-derivable. [MEASURED]
- Commission+swap = -1,984.35 = 31.6% of the W7 loss = -12.69 R of the -25.32 R total: roughly half the R the book lost, it paid. Stops fill clean (median -1.0036 R gross, no stop slippage) and entry slippage is small (+0.013 R vs 0.02 assumed) — the Phase 6 fix is a cost model, not a fill model. [MEASURED]
- The JPY-cross trial (owner-approved 2026-06-16, cost gate relaxed 3x, reversal procedure documented in agent_config.yaml:717-729 — verified verbatim) returned -2,349.58 over 41 trades, 7 winners, -0.591 R net / -0.453 R gross: negative before cost as well as after. [MEASURED]
- NEW (this reader, not in the receipt): the receipt's sleeve-level p-values double-count cross-account duplicates. The 41 JPY trades collapse to 29 distinct signals (12 paired, 12/0 outcome-concordant); signal-level p = 0.00588, and Bonferroni x12 = 0.0706 — the 'survives Bonferroni' claim fails at the honest denominator. fx_jpy_ny is 0-of-6 signals (p 0.028), not 0-of-9 independent. Direction and magnitude stand; 'decisive' does not. [MEASURED]
- The cross-broker asymmetry (FN sized ~1.4-1.6x FTMO) is NOT V4's contract-size mechanism (refuted: equal-size pairs 1.411 vs unequal 1.355) — it is the de-risk governor working: FTMO entered the window -2.74% down, size_cap_multiplier median 0.5405 vs 0.8537, derisking_into_maxdd_wall on 145/145 placements. First live measurement of the safety machinery. [MEASURED]
- At HEAD, ultimate_book_one_unit_per_cluster_per_day: false (agent_config.yaml:1373, present since the 2026-07-25 foundation snapshot) — the cluster cap is globally OFF, so §10 owner-decision 2 (the jpy exemption at :1374) is framed on a moot mechanism; the real decision is whether to re-impose the certified one-unit envelope at all. The receipt never mentions the adjacent false. [MEASURED/INFERRED]
- Gaps are G1-G8 (not G1-G4): G4 (why five high-conf sleeves generated nothing — undecidable from this window, highest-value gap), G3 (21 positions lack tick data; three-day pull closes it), G2 (packet spread_r NULL on all 99,112 rows), G6 (era split rests on the comment string alone), plus corrected G1 (slippage IS measurable), G5/G7/G8. [VERIFIED against receipt text]
- F41 is in the receipt but absent from IMPLEMENTATION_STATE B60-B69: packet pretrade_cost_model.profile.dual_broker_role reads follower_projector_only for FTMO / primary_full_runtime for FN, contradicting live_system_of_record.md's 'not primary/follower' — an unexplained live-topology contradiction in the controlling authority document. [MEASURED per receipt]
- The adversarial pass covered only the broker-truth lane (its confirmed list is all broker-lane); the packet-telemetry lane — silent sleeves, governor asymmetry, rejection-string costs — rests on one generator that already produced one self-caught ~10x method error. [VERIFIED]
- The §7 model comparison (z -1.63/-1.92 rescaled; 14 days cannot falsify the validation) reads CYCLE62's distribution from origin/live-handoff-2026-06-15@98da29d2a because the artifact agent_config.yaml:1299 cites exists nowhere on disk — the baseline itself has a provenance gap the receipt discloses. [VERIFIED per receipt]
- F38 nuance the receipt omits: KB7_tick_truth.py:62-63, directly above COMMISSION_R = 0.0, says 'If a venue charges commission, this is optimistic by that amount' — the missing term was self-declared in code comments and never propagated to the gate, the MC, or the live engine. [MEASURED]

## RECOMMENDATIONS
- Run the G4 shadow generation replay (receipt §9.1): live config over 2026-06-15..07-02, count per-sleeve generation. It is the single unknown that decides whether OD-1's activation candidate has a generation defect or merely a short window. Rough cost: one bounded shadow run + a counting script, ~1 session; no broker contact.
- Restate the JPY/sleeve statistics at signal level (dedupe by sleeve+symbol+side+UTC-day; pairing already in the manifest's cross_broker_pairing.pairs) before presenting §10 decision 1 to the owner — signal-level p is 0.0059 (Bonferroni 0.071), not 0.00016. ~30 lines against LIVE_TRADE_ROWS.jsonl, <1 hour; the trial's negative verdict survives, the 'decisive' framing does not.
- Reframe §10 owner-decision 2 around ultimate_book_one_unit_per_cluster_per_day: false at agent_config.yaml:1373 (globally OFF since at least the 2026-07-25 snapshot) — the jpy exemption at :1374 is moot while the cap is off; the actual decision is re-imposing the certified one-unit envelope. Verify the VPS config matched during the live window before asserting it retroactively. ~1 hour incl. a VPS-export config diff.
- Build Phase 6's cost model directly from §12: per-class commission term (F38), erosion-sign fix in KB7_tick_mc.py (F39), regenerate ULTIMATE_REAL_COST_MAP with a checked-in generator, enforce spread floors incl. GBPJPY + the promised transfer table (F40), replace the circular commission gate with an external-truth check. Then re-run the MC for the composition that will actually be on (the 29-sleeve blend has no joint validation). ~1-2 sessions of implementation + one MC re-run.
- Commission an adversarial pass over the packet-telemetry lane (w7_packet_forensics.py) mirroring §14 — the silent-sleeve, governor-asymmetry, and rejection-string findings are currently single-generator, and that generator already had one self-caught ~10x method error. ~half a session for an independent verifier agent.
- Close G3 with the three-day tick pull (2026-06-15..17, both brokers) before quoting any spread number as window-complete; 21 of 175 positions are currently tick-blind. Cost: one VPS export session.
- Harden the forensics harness for reuse on every future export: add stop-out tail statistics (median-only today) and order-modification history to detect hand SL moves (only hand CLOSES are detectable now); keep the 12 behavioural tests as the regression floor. ~half a session.
- Resolve F41 (dual_broker_role follower_projector_only vs primary_full_runtime) against run_book.py and the pretrade engine — it contradicts live_system_of_record.md, which claims controlling authority over the live model; one of them must be corrected. ~2-3 hours of code reading.
- Wire Session F's divergence matrix live column to LIVE_TRADE_ROWS.jsonl fields (schema now LIVE_TRADE_ROW_SCHEMA.md — fix the receipt's stale LIVE_DIVERGENCE_ROW_SCHEMA.md pointer at :15 while touching it). Minimal cost, already planned.
- Treat the governor-asymmetry telemetry (size_cap_multiplier 0.5405/0.8537, derisking_into_maxdd_wall 145/145) as a live-measured behavioral fixture for Phase 2 SleeveBookPolicy tests — first field data on stress_derisk. Fold into Phase 2 test design at ~zero marginal cost.

## REPORT
# GATE_G1B_RECEIPT.md — full read report (854 lines, Session E, branch `phase1/w7-forensics`, 2026-07-26)

All file references relative to `/Users/borr/GTOSActive/worktrees/review-fable-third-20260727/`. Receipt path: `docs/audits/fable5-vision-audit-20260725/GATE_G1B_RECEIPT.md`. Artifacts verified on disk [MEASURED]: `scripts/w7_live_forensics.py` (60,325 B), `scripts/w7_packet_forensics.py` (14,369 B), `tests/test_w7_live_forensics.py` (12,240 B), `phase1/w7_forensics/LIVE_TRADE_ROWS.jsonl` (300 rows, 561 KB — not an LFS stub), `LIVE_TRADE_ROWS_MANIFEST.json` (138 KB), `W7_PACKET_FORENSICS.json` (44 KB).

## 1. Full structure and per-section key results

- **Header (:1-18)** — artifact table (6 entries) and the warning: *"This receipt was revised after an adversarial pass that refuted one of its headline claims and found ten bugs in the generator. §14 records what was withdrawn and why. Read it before quoting anything."* Note: the artifact table's row-schema pointer `LIVE_DIVERGENCE_ROW_SCHEMA.md` (:15) is **stale at HEAD** — the file was renamed `LIVE_TRADE_ROW_SCHEMA.md` at integration to resolve a same-path collision with Session F (`IMPLEMENTATION_STATE.md:2252-2258`) [MEASURED].
- **§1 Verdict (:22-73)** — **G1b PASS on its own bar**: exact attribution partition, residual 0.000000 over all 175 W7 positions. Headline reframe: **the W7 live window does not measure the W7 book** — 0.45 of core-8's 3.90 confidence weight (11.5 %) placed a single trade; the five high-confidence sleeves (metals_core 1.00, crypto 0.85, energy_agri 0.80, metals_softband 0.50, metals_ob_micro 0.30) placed nothing; the three that traded are the three weakest (all conf 0.15, two `breadth_falsified`, one `forward_only` pre-deployment). The 2.00 % dial was **structurally unreachable** (worst-case unit stop = base × conf, `admission.py:1038-1040`; max conf that traded = 0.40 → ceiling 0.80 %). G1b-failure does NOT reopen OD-1 on "book lost live" grounds — but there is **no live evidence for the book either**. Two more headline findings: JPY-cross trial returned −2,349.58/41 trades/7 winners/p=0.00016; and F38/F39 (commission absent from validation + live engine; MC erosion sign wrong).
- **§2 Denominator corrected (:75-158)** — era partitions per account. FTMO: pre-W7 fleet −2,735.56 / **W7 −2,551.57 (−2.5516 %)** / manual_mobile +13,166.69; partition 131/131, residual 0.00. redacted_account: smoke −4.98, manual −0.19, fleet **+988.47**, one hand XAGUSD −1,018.10, **W7 −3,735.92 (−3.7359 %)**; 169/169, residual 0.00. Both accounts: **175 positions, −6,287.49, −3.1437 % of the 200 k base**. SECOND_AUDIT's FTMO −5.3 % ≈ fleet+W7 (−5.2872 %) — *the audit measured the account and attributed all of it to W7; slightly over half is the fleet's*. **99.1 % of FN's account loss is W7's** (fleet era ≈ flat, −34.80).
  - **§2.1 (a)** — FTMO's "+7,879.56 net since inception" is **two hand-placed phone trades** (tickets 164264833 / 164288287, 07-02→07-03, 1.00 lot each ≈ $412 k notional each, +6,085.34 / +7,081.35), established by **five discriminators** (magic==0 vs 20260401; DEAL_REASON_MOBILE vs EXPERT; empty comment which `execution.py:3430-3444` cannot emit; sl==0 and tp==0 at entry; round 1.00 lots). Empty-comment⟺mobile is a perfect 1:1 over all 3 such positions on both accounts. Without them FTMO closes **94,712.87 (−5.29 %)**.
  - **§2.1 (b)** — true deal-ordered drawdowns: FTMO **−5.3803 %** (trough 94,619.74 @ 06-30T20:10:30Z); FN **−5.8302 % from peak** (−$5,937.60 off 101,842.99 @ 07-01T15:28:34Z). No prop rule breached (limits −5 %/day, −10 % max-DD; worst days −1.05 % / −1.41 %). Caveat: the manifest's `drawdown.series` FTMO −5.5923 % is a **W7-only synthetic curve** (excludes a fleet position closing in-window for +212.06).
  - **§2.1 (c)** — the window starts **2026-06-15**, not 06-18 (21 of 175 positions precede the candidate/mx book switch-on); bounds now derived at runtime (`w7_window_derived: 2026-06-15 → 2026-07-02` in the manifest [MEASURED]).
- **§3 Axis (a) — sleeve partition (:161-256)** — join key is broker truth (`f"W7:{sleeve}"[:16]` comment, `execution.py:3430-3444`); resolution 137 exact + 38 truncation-recovered, 0 ambiguous/0 unresolved. **magic does NOT separate stacks** (both eras carry 20260401); comment is the only discriminator, safe by emitter directionality + zero `W7:UNTAGGED` + the 11-day zero-deal gap (06-04→06-14); fleet was **hand-flattened** (7 FTMO positions closed at one instant 2026-06-03, reason MOBILE). Partition: candidate book 70 / **−4,801.44** / 76.4 % net / 67.4 % gross-loss; JPY cluster 41 / **−2,349.58** / 37.4 %; idxrev 59 / **+875.51**; mx book 5 / −11.98; other five core sleeves 0. **Two bases mandated** (FN candidate = 90.4 % net but 73.4 % gross). Cluster table (receipt-only): fx_reversion −2,526.85, liquidity_sweep −1,370.42, metal_reversion −1,087.94, crypto +183.77, indices_context +2.11, crypto_alt_or_major −10.96, jpy_fx (mx) −3.13. Per-sleeve table with binomial p (receipt-only in full): asia_pdl_fade 20 tr −1,370.42 p=0.055; ny_crypto_momentum 18 tr, 4 wins, p=0.041 **yet positive mean +0.023 R**; metal_session_reversion 7/11 wins p=0.939 yet −1,087.94. `kz_london_crypto_low`'s +258.79 is **one phone-closed BTCUSD position (+304.37)**; sleeve = −45.58 without it; flagged `hand_closed` per-row and in `attribution.hand_intervention` (manifest confirms: position 160425965 [MEASURED]).
  - **§3.1** — the five silent sleeves emitted **no unit_placed/unit_admitted/unit_shadow** across 99,112 packets; only skips are `future_decision_bar_time` (**7/3/4/7/7** per sleeve — receipt-only counts) and FN-only `profile_missing_instrument_config` for four cross-metals + DASHUSD FN genuinely lacks. Self-caught method trap: a line-level grep of the packet's `broker_unsupported_skips` summary list inflated counts ~10× and nearly published "metals silenced by config defect"; corrected to `event_type == "unit_skipped"` only. Gap **G4**: 14-days-too-short vs generation-defect **not decidable from this window**. Nuance: `idxrev` is also `breadth_falsified` and was the best live performer — registry `status` did not predict live sign; the **cluster** did.
- **§4 Axis (b) — dial (:260-338)** — **§4.1 withdrawal** of the "10-15× under-sized" claim (per-trade stat vs per-unit base; sizer quoted, `admission.py:1170-1171`). **§4.2 corrected**: observed/designed unit risk median **0.376 (FTMO, 61 units) / 0.577 (FN, 47 units)**, max 1.332/1.687, **13 of 108 units exceeded design** (kelly_lite); worst unit-day `asian_fade` FN 06-26: designed 0.80 %, observed 1.35 %, realized −922.55 (receipt-only detail). Absolute: per-trade median 0.109 %/0.143 %, max 0.450 %/0.710 %; per-day peak deployed stop-risk 1.685 %/2.127 % vs 4 % gross cap — **cap never bound**. Cross-validation: broker-derived risk vs the book's own `bridge.realized_units[].risk_pct_per_trade` agree **within 1 %** (0.1087/0.1432 vs 0.1097/0.1429) — "the cross-validation for every R figure in this receipt". Dial counterfactual (linear): at 1.25 % → FTMO −1.5947 %, FN −2.3350 %; **dial explains 37.5 % of magnitude, none of sign**. **§4.3 F32**: `governor_daily_reset_offset_hours: 3.0` (found at `agent_config.yaml:1346` at HEAD; receipt cites :1348 — 2-line drift [MEASURED]) hardcodes UTC+3 where FTMO resets 00:00 CE(S)T; wrong twice; did not bite (worst day −1.05 % vs −5 % wall).
- **§5 Axis (c) — friction (:342-560)** — **§5.1 coverage table** (receipt-only detail): packets 99,112 rows all 14 days but `spread_r` NULL on all; lifecycle capture 594 rows request-side only (`order_result` absent — V2); `execution_manager_v4_decisions` 236 rows, no broker ticket; `slippage_runtime` 165 rows, candidate_id on 33 (18-21 %); tick export 06-18→07-24 covers **88.0 % of positions** (21 uncovered). **§5.2**: explicit charges broker-exact at 100 %: commission −1,628.53 + swap −355.82 = **−1,984.35 = 31.6 % of the W7 loss**; in R **−0.0725/trade, −12.69 R total vs −25.32 R realized — "roughly half the R the book lost, it paid"**. 102 stop-outs: median gross **−1.0036 R** (clean fill, **no stop slippage**), cost −0.0720, net −1.0691. **§5.2a F38**: zero commission at **five independent sites** (`KB7_tick_truth.py:64`, `KB7_tick_crypto.py:46`, `KB7_tick_jpy.py:108-109`, `v4_timewarp…py:58965-58966`, live `broker_net_cost_engine.py:577-583` — the 3-term sum verified at HEAD [MEASURED]); the `commission_model_required` gate (`:729-736`) is **circular** (accepts a status string emitted by the code it gates, `execution_packets.py:340`; `selected_cell_risk_decision_basis` **null on all 149 live intents**). Commission by symbol: USDJPY **0.1948 R**, AUDUSD 0.1509, BTCUSD 0.1102, GBPUSD 0.1065, EURUSD 0.1063, GBPJPY 0.0927, ETHUSD 0.0589, XAUUSD 0.0054, **all six index CFDs 0.0000** (70/175 trades); swap-by-symbol receipt-only (BTCUSD 0.0373, ETHUSD 0.0919). Splits `fx_jpy` into two failures: GBPJPY gross **+0.0124** made a loser by commission alone (−0.0915 net); USDJPY gross **−0.6320**, genuine price failure plus highest commission. **§5.2b F39**: `INTEG_W7_FINAL_RESULT.json.tick_erosion_applied` grants **credits** USDJPY +0.0179 / XAUUSD +0.019 / USOIL_cash +0.037 (values byte-verified at HEAD [MEASURED]); computed `tick_real − modeled` (`KB7_tick_book_restate.py:41`), applied `R + h` (`KB7_tick_mc.py:62`); USDJPY reality = 0.0841 spread + 0.1948 commission = **0.2789 R** vs ~0.084 effectively charged → **~0.2 R/trade optimistic on the family that lost 9 of 9**. The *mechanism* is downgraded to an inference: `wave7_pairs_statarb.py:206-210` (verified [MEASURED]) charges the class map on top of a real spread crossing as a *"slippage/commission proxy"*, so the map may have been commission-inclusive by intent; both readings → same repair. Map class values (receipt-only): `jpy_fx 0.1148, metals 0.0459, energy 0.0372`; **no generator** (`450a275f8` JSON-only diff); `KB7_stale_audit.md:36-39` (simulated ledgers clamped 0.18, "NOT the source"); `KB7_execution_truth.md:31-47` tabulated the map against tick **spread** and called metals "~4x OVER-charge". **§5.2c F40**: `TICK_SPREAD_FLOOR_R` never enforced (`v4_timewarp…py:58695` hardcodes False), covers 4/18 live symbols = 26.8 % of entries, 5/9 floor symbols never traded, XAUUSD floor 3.47× understated, BTCUSD 77.8×, **GBPJPY (23 trades, worst measured spread 0.183 R) has no floor**; `is_tick_tradeable()` has no production caller; the promised transfer table (`admission.py:93-94`, "GBPJPY ← USDJPY") doesn't exist. Integrity gap: `admission.py` sets the **sealed replay's** floor yet is in neither R2's 43 bound paths nor `code_authority_paths`. **§5.3**: cost-gate rejection strings (censored to refusals): `fx_jpy` 32 rejections at measured 0.4569→**1.3744 R** vs its 0.45 limit; `fx_jpy_ny` 5; `orb_crypto_london` 9 (0.1624→0.6252 vs 0.15); + 4 more sleeves (receipt-only table). The **owner-approved JPY trial** config comment quoted verbatim (verified verbatim at `config/agent_config.yaml:717-729` [MEASURED], incl. reversal procedure "set the by_sleeve maps to {}"); rejection stop/spread ratios 2.52×, 2.38×, 1.09×, **0.77× — stop inside the spread** (receipt-only). Axis-(c) verdict: PARTIAL on spread, **COMPLETE on the term that matters** — commission/swap 100 % exact; slippage measurable and small (+0.012 R).
- **§6 Axis (d) — asymmetry (:564-604)** — 64 paired signals; FN sized larger, median risk ratio 1.398. **V4's contract-size mechanism REFUTED**: equal-size pairs ratio 1.411 vs unequal 1.355 — indistinguishable; corollary: **the sizer handles per-broker contract-size divergence correctly**. Real mechanism [MEASURED, packet telemetry]: `governor.size_cap_multiplier` median 0.5405 (FTMO) vs 0.8537 (FN), ratio 1.579; `derisking_into_maxdd_wall` on 145/145 placements; overlay counts receipt-only (`ladder_step2` ×51/×39, `coloss_breaker` ×44/×55). FTMO entered the window −2.74 % down → ladder further along → **the asymmetry is the safety machinery working**, measured live for the first time.
- **§7 Sample size (:607-644)** — vs CYCLE62 `series.core8` (n=1679, +0.08151 R, σ 0.65754), **read from `origin/live-handoff-2026-06-15@98da29d2a` because the artifact `agent_config.yaml:1299` cites exists nowhere on disk** (receipt-only provenance caveat). At certified risk: z −1.06/−1.34 (p 0.144/0.090); rescaled to deployed risk: **z −1.63/−1.92 (p 0.052/0.028)**. Accounts not poolable (64 shared signals). 14 days = 13-22 % of CYCLE62's own median days-to-target (64/110). Sleeve level: JPY pooled 41/7 wins p=0.00016, survives Bonferroni ×12 (0.0019) and survives on gross (−0.453 R). Both statements held simultaneously: the window cannot falsify the validation; the JPY trial returned a clear negative on its own terms.
- **§8 Gaps G1–G8 (:648-686)** — there are EIGHT, not four: **G1** (CORRECTED — entry slippage IS measurable: mean +0.0132 R n=140 via `order_send_observation.request.price` × `deal_cost_reconciliation.broker_entry_price`, corroborated +0.0108 R n=147 by `slippage_runtime.jsonl`; 78.6 % of days; worst legs AUDUSD +0.041/GBPUSD +0.040/USDJPY +0.039); **G2** packet `spread_r` NULL on all 99,112 rows → admitted-trade spread unrecorded, rejection strings are a censored sample; **G3** tick coverage 88.0 %, 21 positions uncovered, closable with a **three-day tick pull on both brokers**; **G4** why five high-conf sleeves generated nothing — undetermined, "highest-value single gap"; **G5** cost attribution exact for commission/swap, bounded for spread (separable only where G3∧G1 hold); **G6** the era split rests on the comment string alone — no second per-trade source; corroborations: emitter directionality, zero `W7:UNTAGGED`, 11-day gap, and CYCLE62's account label `FTMO_97p2k_tgt110` (receipt-only); **G7** sleeve-name uniqueness after MT5 16-char truncation unenforced — `mx_ger40_cash_*` and `mx_aus200_cash_*` pairs collide at 13 chars, only one of each live (F34); **G8** FN quantises `trade_tick_value` to 2 dp, wrong by +5.5 %/+5.1 %/−1.7 % (GER30/UK100/JP225); receipt's solved per-position factor immune (F37).
- **§9 What would settle OD-1 (:689-704)** — (1) **shadow-replay the 14 live days with the live config and count generation per sleeve** (closes G4; distinguishes "window too short" from "generation defect" — the latter would make OD-1's premise materially wrong); (2) **do not spend Phase 2 on a broad-stack pivot on this window's strength** — it measured neither stack; the broad V4's own **−0.25 R/fill over 454 fills** (`ULTIMATE_GO_LIVE_DOSSIER.md:276-280`) remains the better-evidenced broad-lane number, and it is negative; (3) the 29-sleeve composition that ran has **no joint validation** (`live_system_of_record.md:107-112`: CYCLE62 contains zero occurrences of "candidate"/"expansion"/"weighted12"); either give the two extra books their own MC at the live dial or switch them off (one config line each).
- **§10 Owner decisions (:708-722)** — see verbatim list below.
- **§11 Defects filed (:726-751)** — F32-F41 table (details below) + four corrections-to-record (V4 attribution refuted; SECOND_AUDIT −5.3 % misattributed; FN start pinned/fleet positive; window 06-15).
- **§12 Measured cost inputs for Phase 6 (:755-793)** — 18-row parameter table with coverage per row (receipt-only in this density): commission mean 0.0591 R (ratio-of-means 0.0601) vs modelled 0.0000; per-trade USD FTMO $8.69/FN $10.08; swap −$1.20/−$3.07; stop-out gross −1.0036; slippage +0.0132/+0.0108 vs assumed 0.02; XAUUSD 06-22 spread median FTMO $0.44/FN $0.50, FN p95 $1.31 vs FTMO $0.49 (875 k ticks); GBPJPY 0.183 R no floor; JPY live-model cost 0.457–1.374 R; median holding 2.46 h (mean 8.90, max 90); governor caps 0.5405/0.8537; per-trade spread on admitted trades **0 %**. Five-point Phase-6 priority order: (1) add a commission term; (2) charge per instrument class, not per portfolio ("the distribution is the finding, not the mean"); (3) fix the erosion sign before any MC re-run; (4) do NOT build a fill model (stops clean, slippage half of what's assumed); (5) spread floors need coverage + enforcement + the promised transfer table.
- **§13 Reproduce (:796-806)** — two deterministic read-only commands; no `MetaTrader5` import, no socket; the printed residual 0.00 against 107,879.56 / 96,229.28 is "the thing to re-check before trusting any number above" (manifest `_partition_check` verified: residual 0.0, sums match broker balances exactly [MEASURED]).
- **§14 Adversarial pass (:810-854)** — see verbatim list below.

## 2. §10 verbatim — owner decisions queued, with evidence

> **"1. The JPY-cross trial has returned its result. End it?** `agent_config.yaml:717-729` says how: set `selected_cell_pretrade_max_spread_r_by_sleeve` and `selected_cell_pretrade_max_total_cost_r_by_sleeve` to `{}`, reverting `fx_jpy`/`fx_jpy_ny` to the strict global 0.10 / 0.15 gate. Basis: −2,349.58 over 41 trades, 7 winners, −0.591 R net and **−0.453 R gross**, p = 0.00016; the live cost model itself measured up to 1.37 R round-trip cost on the sleeve."
>
> **"2. The JPY cluster is the only cluster exempt from the one-unit-per-cluster-per-day cap** (`ultimate_book_cluster_cap_exempt_clusters: ["jpy"]`, `agent_config.yaml:1370`), which let `fx_jpy` (London) and `fx_jpy_ny` (NY) both take correlated units on GBPJPY and USDJPY the same day. The exemption's stated purpose was the trial in (1). If the trial ends, does the exemption?"
>
> **"3. The candidate book carries 76.4 % of the net loss (67.4 % of gross losses) and has no cited validation.** Keeping it on at the 2.00 % base is a risk-composition decision, not an engineering one."
>
> "I have changed no config. Everything in this receipt is measurement."

**Verification note on decision 2 [MEASURED]:** at this worktree's HEAD the exemption sits at `config/agent_config.yaml:1374` (4-line drift from the cited :1370), and **the line above it, `:1373`, is `ultimate_book_one_unit_per_cluster_per_day: false`** — the cluster cap is globally OFF, with a comment block (:1363-1372) documenting the deliberate trade-off ("a cluster can take >1 correlated unit on a multi-fire day, slightly above the certified one-unit-per-cluster-per-day envelope — accepted"). `git log -L` shows both lines present in the 2026-07-25 foundation snapshot, i.e. before Session E. [INFERRED] If the vendored config mirrors the live-window VPS config, the exemption was **moot during the window** (everything was exempt), and decision 2 as framed answers a narrower question than the live envelope actually poses — the real decision is whether to re-impose the cap at all. The receipt never mentions the `false` on the adjacent line.

## 3. §14 verbatim-level — what was withdrawn and why

**Withdrawn outright:**
1. **The "10–15× below the 2.00 % dial" under-sizing claim** — it compared a per-trade statistic to a per-unit base; corrected to observed/designed **0.38–0.58× median with 13 of 108 units over design** — the de-risk overlays doing their job, not a sizing defect. Generator now emits `designed_unit_risk_pct = 0.020 × conf` and no longer prints the base beside a per-trade figure.
2. **`VPS_EXPORT_FINDINGS.md` V4's causal claim** (contract-size divergence → cross-broker asymmetry) — refuted by direct measurement (§6), "not by the verifier, but it belongs on the same list".

**Materially corrected:**
3. The two untagged FTMO positions are **hand-placed phone trades** (five discriminators), not "the post-switch broad system's first trades" — now classified `manual_mobile`.
4. **A hand exit inside the W7 window was hidden by a bug** — `kz_london_crypto_low`'s entire positive contribution is one phone close (+304.37); sleeve −45.58 without it.
5. True drawdowns **−5.3803 % (FTMO) / −5.8302 % from peak (FN)** — deeper than any era sum; what prop rules measure.
6. FN's pre-W7 **era** is flat (−34.80) — so **99.1 %** of its account loss is W7's.
7. `fx_jpy_ny`'s "worse than −1 R" is **−1.036 R gross plus commission, not stop slippage** — all groups now report gross/cost/net side by side.
8. Family shares now given on **two bases** (net vs gross-losses).

Also corrected mid-receipt (not §14 but the same class): **G1's own first publication was wrong** — "slippage 0 % coverage on the strength of V2" was wrong twice over (§8 G1), and the §3.1 packet-grep trap ("I nearly published the opposite") inflated skip counts ~10×.

**Ten generator bugs, all fixed at HEAD** (six described): wrong `ENUM_DEAL_REASON` map (`EXPERT` is 3, not 1 — labelled all EA closes as browser closes and hid the hand exit through two drafts); `_attach_sleeve_meta` resolving `sleeve_cluster` for core-8 only (**43 % of the book in cluster `unknown`** — the one axis that feeds sizing); balance/trade split keyed on deal **type** not symbol truthiness (a commission/dividend row with a symbol would have corrupted the reconciliation that IS the correctness proof); window bounds hardcoded under a comment claiming they were derived; falsy-zero guards on risk/R (`is not None` now); `daily_curve` DST offset resolved at exit instant not entry's.

**Confirmed independently by the verifier:** exact reconciliation both accounts (two separate routes); era split + disjointness; FTMO −2.55 %/−2.74 % decomposition; FN −3.7359 % point + positive fleet; the two untagged positions (+13,166.69); `fx_jpy_ny` 0-of-9; family shares.

## 4. Receipt content NOT in IMPLEMENTATION_STATE.md B60-B69

The blocks compress well; these receipt-only items matter for the review:
1. **FN smoke-test and manual eras** (7 positions −4.98; 1 position −0.19) and the FTMO manual-trade forensic detail (tickets, prices, times, ≈$825 k combined notional, the "without them −5.29 %" counterfactual, the 1:1 empty-comment⟺mobile equivalence at n=3).
2. **The manifest's −5.5923 % FTMO figure is a W7-only synthetic curve** — labelled caveat against misquoting it as the account trough (−5.3803 %).
3. **Both-account W7 total: −3.1437 % of the 200 k base** (the single number for "what did W7 cost").
4. **Full cluster table and full 10-row per-sleeve table with p-values** — incl. ny_crypto_momentum's odd cell (p=0.041 on wins yet positive mean R) and metal_session_reversion (7/11 wins, still −1,087.94) — the blocks carry only family-level and JPY/idxrev rows.
5. **Fleet hand-flattening** (7 FTMO positions closed at one instant 06-03, MOBILE) as an era-boundary corroboration.
6. **Silent-sleeve skip counts** (`future_decision_bar_time` 7/3/4/7/7) — the only evidence the five sleeves' loops ran at all.
7. **Worst unit-day**: `asian_fade` FN 06-26, designed 0.80 % → observed 1.35 % (1.69×), realized −922.55; and the 61/47 measured-unit split.
8. **§5.1 coverage table row detail**: lifecycle 594 rows request-only; `execution_manager_v4_decisions` 236 rows no broker ticket; `slippage_runtime` 165 rows candidate_id on 33.
9. **Swap-by-symbol** (BTCUSD 0.0373 R, ETHUSD 0.0919 R) and the full 14-symbol commission table with n per symbol.
10. **F39 supporting record**: full `tick_erosion_applied` JSON (incl. HEATOIL_c −1.506 dropped legs); `ULTIMATE_REAL_COST_MAP.json` class values (jpy_fx 0.1148 / metals 0.0459 / energy 0.0372); `KB7_stale_audit.md:36-39` (clamp 0.18, "NOT the source of the deployed map"); `KB7_execution_truth.md:31-47` ("~4x OVER-charge (too harsh)").
11. **§5.3 full rejection table** (7 sleeves, ranges) and the stop/spread ratios **2.52× / 2.38× / 1.09× / 0.77× (stop inside the spread)**.
12. **§7's certified-risk z-row** (−1.06/−1.34, p 0.144/0.090) beside the rescaled row, the model expectations (+1.973 % ± 4.254 % certified; +1.017/+1.191 % rescaled), and the **CYCLE62 provenance caveat**: the distribution is read from `origin/live-handoff-2026-06-15@98da29d2a` because the artifact `agent_config.yaml:1299` cites **exists nowhere on disk**.
13. **Gaps G5, G6 (incl. the `FTMO_97p2k_tgt110` corroboration), G7's collision specifics, G3's closure cost** (three-day tick pull) — blocks carry only G4 and fold the rest into defect ids.
14. **§9's three OD-1-settling items** incl. the broad-lane anchor (**−0.25 R/fill over 454 fills**, `ULTIMATE_GO_LIVE_DOSSIER.md:276-280`) and the CYCLE62-zero-occurrences citation (`live_system_of_record.md:107-112`).
15. **F41 — entirely absent from B60-B69**: `pretrade_cost_model.profile.dual_broker_role` reads `follower_projector_only` for FTMO (80 intents) and `primary_full_runtime` for FN (69), contradicting `live_system_of_record.md`'s "two fully independent per-account workers, **not** primary/follower". An unexplained structural asymmetry, explicitly not the axis-(d) mechanism.
16. **§12's Phase-6 parameter table** (per-trade USD commission, swap USD, XAUUSD one-day spread distribution incl. FN p95 $1.31 vs FTMO $0.49, holding-time stats, governor cap medians, slippage worst-legs) and the five-point priority ordering.
17. **§13's reproduce contract** (deterministic, no broker imports, residual as the correctness check).
18. Reverse-diff (blocks-only, absent from the receipt): B69's **12 behavioural tests** (`tests/test_w7_live_forensics.py`) — the receipt's artifact table does not list the test file; and B69's explicit count that the ENUM bug relabelled **27** EA closes.

## 5. Robustness assessment — what could still be wrong; what the adversarial pass did NOT cover

**(a) [MEASURED — new, this reader's own check] The sleeve-level statistics double-count cross-account duplicates.** From `LIVE_TRADE_ROWS.jsonl`: the JPY cluster's 41 trades collapse to **29 distinct (sleeve, symbol, side, UTC-day) signals**; 12 signals fired on both accounts and were **12/0 outcome-concordant** — i.e. paired trades are effectively perfectly dependent. At signal level: 6 winners of 29, **p = 0.00588** (vs the receipt's pooled 0.00016); Bonferroni ×12 → **0.0706, which does NOT survive at 0.05**. `fx_jpy_ny` is 0-of-6 signals, p = 0.0277 (vs the receipt's 0.0046). `idxrev`'s 59 trades are 34 signals. The receipt itself states accounts "cannot be pooled into a stronger claim" at account level (§7) yet pools trades across accounts at sleeve level — an internal inconsistency the verifier did not flag (it confirmed computations, not independence assumptions). What survives untouched: the sign, the mean −0.591 R net / −0.453 R gross, the 1.37 R measured round-trip cost, and 0-of-6 independent NY signals — the trial's negative verdict stands, but "decisive, survives Bonferroni" should be restated at the signal level before OD-1/§10.1 is taken on it.
**(b) The packet-telemetry lane was not adversarially verified.** §14's confirmed-items list is entirely broker-lane. The silent-sleeves finding (§3.1), the governor-asymmetry mechanism (§6), and the rejection-string cost recovery (§5.3) rest on `w7_packet_forensics.py` alone — the lane that already produced one self-caught ~10× method error. An adversarial pass over this lane is the missing mirror.
**(c) G4 has a third branch the receipt does not name: capture loss.** "No packets" is read as "no generation"; packet-emission completeness per sleeve per day is not demonstrated. The sparse `future_decision_bar_time` skips (7 events/14 days for metals_core) prove occasional loop liveness, not continuous evaluation coverage.
**(d) The Binomial(n, 0.45) null is asserted "generous" without citing per-sleeve validated win rates.** If a sleeve's design win rate is materially below 0.45, its p weakens; if above, strengthens. Not audited.
**(e) The dial counterfactual assumes strict linearity.** At a 1.25 % base the drawdown path is shallower → the `derisking_into_maxdd_wall` ladder (engaged on 145/145 placements) would cap less → realized loss would not scale exactly 0.625×. Direction: the −956.84/−1,400.97 "attributable to the dial" figures are upper bounds. Unstated in the receipt.
**(f) Stop-fill quality is reported as a median only.** −1.0036 R median hides tails; no p95/worst stop-out is given; gap-through risk (JP225/GER40 overnight, weekend crypto) unexamined. "No stop slippage" is a median claim.
**(g) Hand interference is only detectable at closes.** Deal `reason` catches hand closes; a hand-moved SL (phone stop-tighten/widen) would silently distort `risk_at_entry_usd`, R normalization, and designed-vs-observed — no order-modification history was examined.
**(h) The §7 model comparison inherits an unverifiable baseline** — CYCLE62's series read from a git branch because the cited artifact doesn't exist on disk (the receipt discloses this; the review should carry it forward).
**(i) The window end is endogenous.** Last entry 07-02 — the same day the three execution ledgers died — and the halt followed; the receipt never says why trading stopped. Any expectancy statistic over a drawdown-terminated window carries stopping-rule bias.
**(j) F41 is flagged but not chased.** If FTMO's pretrade model genuinely ran `follower_projector_only`, per-account gate evaluation could differ in ways §6's sizing measurement would not capture.
**(k) Minor citation drift at HEAD** [MEASURED]: exemption at :1374 not :1370; governor offset at :1346 not :1348; the artifact-table schema pointer renamed at integration. Cosmetic, but quoting the receipt's line numbers against HEAD will miss.
**(l) F38 nuance the receipt omits** [MEASURED]: `KB7_tick_truth.py:62-63`, directly above the zero-commission dict, reads *"If a venue charges commission, this is optimistic by that amount; reported in the note."* — the omission was **self-declared** in at least one site; the failure mode is that a declared optimism never propagated into the gate, the MC, or the live engine. This slightly reframes F38 from "silent missing term" to "acknowledged approximation that was never closed", which matters for the repair narrative (and for whether other self-declared approximations are lurking in KB7 notes).
**(m) What is rock-solid:** the reconciliation (residual 0.0 to the cent against broker balances, two independent verifier routes, manifest `_partition_check` re-verified here), the era partition, the commission/swap totals (broker truth, 100 % coverage), the five-discriminator manual-trade classification, the within-1 % risk cross-validation from disjoint inputs, and the F38/F39 evidence sites (all code citations spot-verified at HEAD).

## 6. What should be built ON this receipt

1. **The G4 shadow generation replay** (§9.1) — run the live config over 06-15→07-02 counting per-sleeve generation. This is the single decision-relevant unknown: it decides whether OD-1's activation candidate has a generation defect or just a short window. The row set gives the exact dates/symbols to condition on.
2. **Signal-level restatement of the sleeve statistics** before the §10 owner decisions are presented — dedupe by (sleeve, symbol, side, day) as measured above (~30 lines against `LIVE_TRADE_ROWS.jsonl`; the manifest's `cross_broker_pairing.pairs` already carries the join).
3. **Phase 6 cost model directly from §12**: per-class commission term (F38), erosion-sign fix in `KB7_tick_mc.py` (F39), a regenerated `ULTIMATE_REAL_COST_MAP` **with a generator this time**, spread-floor coverage/enforcement incl. GBPJPY + the promised transfer table (F40), and a non-circular commission gate (external truth, not a self-emitted status string). Then re-run the MC for whatever composition will actually be on (§9.3: the 29-sleeve blend has no joint validation).
4. **Session F divergence matrix live side** — already the receipt's stated consumer (`LIVE_TRADE_ROW_SCHEMA.md`); the matrix's live column should cite row fields, not receipt prose.
5. **A permanent live-forensics harness**: `w7_live_forensics.py` re-run on every future export tree becomes the standing live-truth reducer; extend it with stop-out tail stats (fix (f)) and order-modification history for hand SL moves (fix (g)); keep `tests/test_w7_live_forensics.py`'s 12 invariants as the regression floor.
6. **Adversarial pass over the packet lane** (fix (b)) before the governor-asymmetry and silent-sleeve findings are treated as settled inputs to Phase 2 `SleeveBookPolicy`.
7. **Close G3** (three-day tick pull, 06-15..17, both brokers) before quoting any spread figure as window-complete.
8. **Resolve F41** against `run_book.py` + the pretrade engine — either `live_system_of_record.md` or the packet is wrong about live topology, and that document claims controlling authority.
9. **Owner decision package**: §10's three decisions with ready one-line diffs — and reframe decision 2 around `ultimate_book_one_unit_per_cluster_per_day: false` at HEAD (the cap itself, not just the exemption).
10. **Feed the governor telemetry (§6) into Phase 2** as a live-measured fixture: the first-ever field measurement of `stress_derisk` (0.54 vs 0.85 median caps, 145/145 `derisking_into_maxdd_wall`) is a behavioral spec for any sleeve-book policy replay.