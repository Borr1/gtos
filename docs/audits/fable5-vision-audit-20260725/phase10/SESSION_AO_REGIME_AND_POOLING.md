# Session AO — regime conditioning for the two blocked candidates, and the power-pool mx_btcusd was never given

**Wave 10.** Worktree `worktrees/wave10-regime-pooling-20260730`, branch
`phase10/regime-pooling`, from `main`. **Blocks B1300–B1349.**

**Read `../WAVE_10_WORKING_AGREEMENT.md` first**, then AL's result §2/§4/§5 + §10 items 1–3,
AB's regime-spine result (the dials), and AH's result §4.3 (the pre-declared-bucket discipline
that is now the standard: state the bucket from what the mechanism IS, before any number).

## The mission

Three sleeves converged on the same next lever and nobody has pulled it. All three are
population-sensitive — publish everything on both RECORDED and DECIDABLE per agreement §2–4.

**1. `sub_xvol_pullback` — armed, best cell banked (`target_4R`@RECORDED +1.365 R/day, p 0.0080,
2.8× from rank-1).** Pre-declare its bucket from the mechanism (it is a pullback-in-expansion —
say what regime it SHOULD want before looking), gate at the declared family on AB's dials at the
banked exit. Also close its coverage question: WHY do the decidability populations drop it to
n=56 (metals/index wide-band eras) — is that recoverable data or structural? This sleeve carries
real money; its dossier row must end this session with either a conditioned cell or the exact
reason none exists.

**2. `asia_pdl_fade` — exit exhausted (120-cell cross), 5/5 folds at the winner, 23× from the
bar.** Same discipline. 2,827 trades give the dials real resolution; if no pre-declared bucket
coheres, the enumerated map with its own bill is the deliverable (AH's §5.3 shape).

**3. `mx_btcusd`'s power-pool — AL §10 item 1, never run.** AF refuted the 9-symbol crypto
cluster as DIVERSIFICATION (dispersion 4.74); pooling for POWER on a single significance test is
a different question. Pool `mx_btcusd` with its donchian D1 siblings (the coherent-positive
members per AK §4's net-basis table) as ONE pooled hypothesis at `target_5R`-equivalent exits,
declare it via the ratchet (it is a new hypothesis; the bill rises and that is correct), gate on
both populations. If the pooled cell admits where the single-symbol cell is band-fragile
(band_high p 0.0051), that is the robustness the admission currently lacks.

**4. `sub_mid_dn_revert` follow-through (AM's §1.3):** at p 0.0198 with robustness now +0.361,
its named blocker is regime (`REGIME_GATE_OR_PARK` on both arms). One pre-declared dial pass on
the RE-CLOCKED population (AM's driver, imported). It would admit at ≤5 looks; the family is 35+
— so the honest target is the robustness gate, not significance alone.

## Deliverables
`phase10/receipts/REGIME_CONDITIONING_V1.json` + `phase10/receipts/BTC_POWER_POOL_V1.json`;
repair-queue rows (session `AO`); result doc with §2 receipt; every look in the ledger; blocks
B1300–B1349.

## Not yours
The VPS. Arming, α, population-rule ratification (AN's package; Borhen's call), sleeve
composition. Merging to `main`. `config/agent_config.yaml`.

Use your own judgment on scope and on whether anything above is wrong — and say so in your report.
