# Session HN context anchor

`execution_authority: false`

`activation_authority: false`

`result_bearing_science_executed: false`

`result_use_status: SOURCE_CONTROL_ONLY_NO_OUTCOME_READ`

| Material rule | Enforcement point | Required terminal evidence |
|---|---|---|
| Exact source ancestry, evidence-only commits excluded from implementation ancestry | `git rev-parse`, `git rev-list --parents -n 1`, exact `git show <commit>:<path>` bytes, SHA-256 ledger | source and evidence commits retain one parent each; completion names the tested source commit |
| Fixed sparse and resource envelope | sparse-rule count/hash, `df -Pk`, process inventory before estimation/materialization | 388 rules at `8b1978...f7b5b`, at least 8 GiB free throughout, no competing heavy worker |
| HG candidate/family/windows/thresholds and 73,999-row denominator are immutable | commissioned-payload hash table plus four-part identity parser | January/April/May only; 73,999 unique `(candidate_id, symbol, side, decision_time_utc)` rows; zero duplicates or unmatched rows |
| Source/control only; no result-bearing or economic read | parser allowlist exposes identity and predecision-control fields only; AST/import audit; command log | no return, fill, cost, PnL, exact-R, proxy-R, expectancy, promotion, gate, replay, P1, paper, calibration, optimization, or canary execution |
| Offline-only and broker-inert | forbidden-import AST/transitive audit; no network/broker/VPS/runtime command; source paths must be preserved local files | zero `MetaTrader5`, real MT5, execution/orchestrator, `order_send`, token, credential, live-config, account, order, deal, position, or history access |
| True UTC outranks names and broker-wall labels | source-manifest hashes, explicit `time_column_basis`, `new_york_plus_7` conversion authority, aware-timestamp parser | naïve/mixed timebases and DST errors fail closed; every admitted source has a verified as-of boundary |
| No postdecision leakage | closed-bar index uses bar-open plus timeframe duration at or before decision time; independent slice verifier | zero postdecision M15/H1 reads and every identity has at least 51 fully closed M15 bars |
| Exact reconstruction semantics | hash checks for `broader_origin_generators.py`, `market_state.py`, H1-from-M15 implementation and transitive dependencies | contemporaneous `M15.atr_14` and `H1.breaker_blocks` reconstructed with formation/mitigation/retest fields and exact generator identity match |
| Composite identity and multiplicity | tuple key everywhere; candidate ID is never a key alone | 73,999/73,999 one-to-one coverage, zero duplicate tuple keys, exact reused-candidate multiplicity |
| Compact immutable packet | deduplicated per-symbol time series, identity-to-slice index, content-addressed external hold, manifest verifier | source path/bytes/SHA/schema/timebase/as-of/generator/dependency/route/slice bindings; external-byte tampering fails closed |
| U4 and U11 only; U5 owner-reserved | sanitized committed/offline profile-symbol projection and inert route bundle; no account secret or selected volume | U4/U11 closed; volume remains literal `NOT_EVALUABLE_OWNER_INPUT_REQUIRED` |
| March/February/live-forward boundary | window allowlist and path-token refusal; April may use raw predecision March warmup only under the bound source manifest | no March outcome/path payload, no February selection/calibration/veto/promotion, no live-forward outcome/path payload |
| Commit and closeout discipline | source/test commit first; packet generated only from immutable source; focused tests, independent verifier, A/B tool, JSON parse, compile, receipt validator, `git diff --check` | exactly one self-contained `gtos-ab-receipt-v1` fence, evidence-only closeout commit, clean branch |

Closeout will include an instruction-coverage audit against every row above and end in exactly one commissioned terminal state. This anchor is retrieval guidance only; immutable HG/HK/HL/HM bytes and current disk evidence remain authority.
