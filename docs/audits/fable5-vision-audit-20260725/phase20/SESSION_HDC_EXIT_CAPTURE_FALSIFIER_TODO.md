# Session HDC — exit/capture semantics falsification TODO

Scope is offline research infrastructure on builder head `82c41b122`. No result below is authority
until the attacks, independent reproductions, failure-set A/B, and closeout receipt complete.

## Reproduced before editing

- Branch, builder HEAD, and clean starting status matched the commission; 31 GiB was free.
- The pre-outcome FC protocol declares stop/floor/time-box/target/terminal priority, says an earlier
  target wins, retains a +2R hard target, and charges full source cost once after partial accounting.
- The CS declarations bind January, April, and amended May captures; March is blacked out; the gate
  is RECORDED/FTMO-mid/B-balanced with 59 all-declared members, 10,000 resamples, and seed 20260729.
- HC changes capture authority, exit collision handling, and segmented nulls; its source delta is
  substantially larger than its result summary alone.

## Builder claims, treated as untrusted

- Same-timestamp target/deadline belongs to `time_box`; a favorable deadline gap is target-capped.
- All 214 historical FC time-box projections and 3,200 fast/reference comparisons are unchanged.
- Capture authority is immutable and null blocks never bridge captures.
- A uniform common circular phase is prospective; CS changes from p/q 0.0025997400/0.1533846615 to
  0.0021158854/0.1248372396 and remains rejected.

## Reviewer hypotheses and attacks

1. Derive one row/path temporal state machine and exhaust strict, tied, delayed, gap, stop/floor,
   partial, cost, reason, and timestamp cases, including both synthetic M1 point orders.
2. Recompute the 214-outcome projection and all 3,200 equivalence cases from committed inputs rather
   than accepting HC's pinned test.
3. Attack partial/invalid/duplicate capture declarations; empty, reversed, overlapping, reordered,
   touching and blackout windows; runtime replacement; serialization; and legacy absent-field seals.
4. Independently reconstruct pooled ordering and capture lengths; exercise empty/unequal/short
   segments, fixed/auto blocks, and invalid length sums.
5. Test whether common-phase sign flips are invariant to independent capture rotations and labels.
   Compare only ex-ante candidate rules, including independent phase marginalization and the sealed
   Monte Carlo transition, before reading which p-value is disposition-favorable.
6. Remove only duplication or generality that does not serve deterministic exits, sealed captures,
   or boundary-safe nulls. Preserve the walk-forward architecture.

If implementation changes, the first fix may begin only after the exact builder-head focused failure
set is captured. `activation_authority: false`.
