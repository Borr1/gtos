# Wave 20 science preregistration

## Conditional execution decision

**No result-bearing science may start until G0 passes.** G0 must integrate only the reviewed HDA
fidelity repair, the reviewed HDE cost repair, and HDF's final reviewed exit/capture repair at
exact committed hashes; preserve the hydrated baseline node set; introduce no new bad test ID,
default-on research surface, activation authority, or opportunity suppression; and leave
execution_authority and activation_authority false.

HDF closed while HG was being authored. It found HDC unsafe as-is, repaired mutable capture
authority, terminal-observation state and exact-tail ties in implementation commit
14c0e2ad1c4390ba78412832179d9b1e1dbf16f2, and independently upheld the fixed breaker's ADMIT at
q=0.0576171875 in closeout da2514dc6c9ceee16ed85f01b314ace973d50980. The preregistered branch
condition is therefore resolved:

- **Selected:** HDF validates the fixed-family ADMIT with its repair, so carry that unchanged breaker
  directly to complete-path proof and skip breadth.
- **Frozen counterfactual:** had HDF rejected the claim or left it NOT_EVALUABLE, the single guarded
  book-diversifier test below would have run with the unchanged 59-member bill.
- HDC bytes alone remain unsafe and cannot enter G0.

OB persistence remains the cheapest independent fallback, but it does not open while the
HDF-admitted V27 breaker advances: declaring OB's V28 bill in parallel would mutate the family
surface without helping the selected candidate. If the breaker complete path rejects or stops,
O1 runs before N1 or FC2. One immutable source run may fan out into the two required observability
captures and supply NY-metals/FC2 inputs, but their hypotheses, denominators, multiplicity and
receipts remain separate. Four heavy executions, including outcome-decoded aborts and reruns, are
the hard maximum before review.

This document authorizes **no execution**. It preregisters future commissioned lanes only.

## Compact DAG

    G0 repair integration ─────┬──── O1 fixed OB persistence (fallback) ────┐
                              │                                             │
    S0 metadata inventory ────┼──── HG_B0 HDF router ─ ADMIT SELECTED ──────┤
        (may overlap G0)       │             └─ REJECT / N-E ─ HG_B1 SKIPPED ┤
                              │                                             ├─ K1 calibration
                              └──── C0 one immutable capture ─┬─ N1 NY ────┤   only if needed
                                                             └─ F1 FC2 ───┘
                                                                               │
                                                                               v
                                     P1 candidate → order → fill → exit → owner package

G0 blocks every result-bearing child. S0 is metadata-only and cannot clear G0. C0 is one physical
heavy run with a pre-patch witness and two logical, separately sealed outputs, but it is not on the
fixed breaker's critical path because those fields do not decide its frozen geometry. All
replay-scale work is serial.

## Authority and unknown map

| Class | Current bound truth | Consequence |
|---|---|---|
| Available published/sealed data | Wave 20 control/preservation/baseline; Wave 19 FA/CR/CS/FC/FG results and receipts; preserved FA plus CS raw ledgers; CS January/April/May fold/path declarations; FC v1 protocol; reviewed HDA, HDE and HDF repairs | Enough to fix gates, select the direct breaker branch and design thin continuation runners without opening new outcomes |
| Absent data | Complete broad-V4 RECORDED NY population; authenticated 2025-06-02..07-11 lane packs; exact hard/rank and 15-field capture; aligned base-book trace; disjoint calibration manifests | Each absence produces the named source capture and a NOT_EVALUABLE stop, never a negative economic claim |
| Executable code | Default-off OB transform, exit evaluator, train runner/cuts, guarded diversifier/book replay, CS/CR/FC analysis tools | Reuse these components; add only thin manifest/fill/fan-out adapters |
| Unverified claims | Any live/canary fidelity; CR independence; FC2 persistence; calibration benefit; no-opportunity-loss through the complete route | None may enter a promotion claim before its node closes |
| Owner decisions | Risk dial, allocation, canary/incubation/arming, activation token, live broker action, March release, sealed-contract change, paid compute | The final terminal state is an owner package, never activation |

The exact machine authority, hashes, inspected paths and node contracts are in
phase20/receipts/WAVE20_SCIENCE_PREREGISTRATION.json.

## Frozen global boundaries

- March 2026 and live-forward outcomes remain unread by HG. February is attribution-only and may
  never select, calibrate, optimize, veto or promote.
- W7/ultimate-book rows never stand in for broad-V4 candidates. In HG_B1 they may serve only as the
  explicitly labelled base-book comparator.
- Missing cost, fill, path, terminal or condition data never removes an opportunity. It makes the
  affected gate NOT_EVALUABLE.
- No threshold relaxation, family shrinkage, post-hoc veto, alpha retuning, hidden top-k, or
  unbilled look is allowed.
- Every candidate remains research-only and default-off. execution_authority: false.
  activation_authority: false.
- At least 8 GiB must remain free before, during and after every run. A cap breach preserves
  technical logs, avoids partial-economic interpretation, and stops.
- Machine fields ending TO_RESOLVE or RESOLVE_AFTER name future artifacts that do not yet exist;
  they are not authority. A node must replace every token it consumes with an exact commit/SHA-256
  in a preflight receipt before outcome decode, or stop NOT_EVALUABLE.

## Schedule and reuse

| Stage | Work | Parallel rule | Heavy count |
|---|---|---|---:|
| 0 | G0 integration and S0 metadata inventory | May overlap; S0 cannot clear G0 | 0 |
| 1 | Selected HG_B0 direct branch; O1/C0 thin-runner and source preflight only | Light preflight may overlap | 0 |
| 2 | C0 immutable capture, only if N1/F1 or a consuming route opens | Alone | 0–1 |
| 3 | N1/F1 light projections if still needed; K1 only if applicable | No heavy overlap | 0–1 |
| 4 | P1 offline complete-path replay, then prospective paper shadow | Serialized | 2 |

C0, K1, P1 offline and P1 paper shadow are at most four heavy executions total. Mandatory review
occurs as soon as the fourth completes or aborts after outcome decode. Any rerun counts.

Before each heavy execution: focused known-answer tests, source hash/schema check, synthetic
fill/collision table, a one-day no-outcome dry run, disk/process preflight, and confirmation that no
other heavy process owns the machine.

The candidate waterfall is fixed: HDF breaker → P1; only a candidate-specific P1 failure opens O1;
only O1 REJECT opens C0/N1; only N1 REJECT or candidate-specific NOT_EVALUABLE with authoritative
C0 inputs opens FC2. Any shared
route/source/infrastructure absence is an honest stop, not permission to cycle candidates.

## G0 — repair-integration entry gate

**Hypothesis.** HDA, HDE and HDF coexist without new failures, authority drift, default-on behavior
or opportunity loss.

**Exact authority.**

- Control: 43b900b5d3ef4c6748c109400757c72b1dd0a245.
- HDA: implementation 4f5c5d4286764b622cc0f539ac40887a2bbbd8b4; close
  9894fa0c72f68af587d4a04004de44f7076b9416; completion SHA-256
  1dde05da1c9e0746bb1f0dffa646103d11e7fc33d6ae1de3a5506953c7ab8ec6; A/B SHA-256
  4913147cb9a5e3d8cc27a99349cedbb3c2194b2b224fc2ae6c1e4ae49325f866.
- HDE: implementation cf70fd7e36eaaab8d5d04e04db8c85a7685b0a83; close
  52ede5eabcb5d09d8ceb6e12d878f7bd0442e5ad; completion SHA-256
  6e74d8ba7f83704d6262c39777be2786ea54d9d5cf36b8b1fb64e03aee6dd993. Require the
  124/355/46/166 same-scope failure sets, zero regressions, and FD byte/numeric identity.
- HDF: implementation 14c0e2ad1c4390ba78412832179d9b1e1dbf16f2; closeout
  da2514dc6c9ceee16ed85f01b314ace973d50980; completion SHA-256
  e10b972bb0763bc4a01e6f7cb5fc1e187368037f4dfed2ad6eb782e16d77be78; A/B SHA-256
  f0bb2f339332ec0d77a7b894bb2998d7c3b33d3b4af62c84ef91302852c1c4de; findings-first result
  SHA-256 ae0ec19f891196aaf3e1a710ff981954b81a33ed4d03c0ea0999f7988c8b3f8e. Require exactly its
  SAFE_ONLY_WITH_HDF_COMMITS disposition, 17-node HC 13 bad / HDC 7 bad / HDF 0 bad same-test
  proof, 59-member bill, three capture boundaries, FC 3,200-row oracle and 214 timebox identity.
  Its fixed breaker result is ADMIT; HDC without HDF is forbidden.

**Suite command.**

    /usr/bin/time -l env PYTHONDONTWRITEBYTECODE=1 pytest -q tests/ \
      --junitxml=/private/tmp/wave20_post_integration.xml

The 12,974 baseline node IDs must all still collect. New reviewed tests must pass. The post bad-ID
set must be a subset of the two official baseline failures; no new ID is allowed. The preserved
388-rule sparse profile SHA-256 is
8b19788683e6ed07682b5562260bd41478f50775ebf173e50038d8b5531f7b5b.

**Default-off checks.** OB transform default false; condition and hard-observability patches absent
from default and safe patch sets; exit evaluator has no runtime/config/broker surface; HDF candidate
not queued or armed; all activation/execution authority false; no fixture, test, candidate or
opportunity removal.

**Terminals.** PASS opens downstream commissioning. Any unsafe/missing reviewer evidence, new bad
ID, missing baseline node, default-on path or opportunity suppression is REJECT. Incomplete HDF or a
resource-bound suite is a higher-information stop. Cap: 10,800 s, 6 GiB RSS, 2 GiB incremental disk.
Outputs: source ledger, JUnit, A/B receipt and integration gate receipt.

## S0 — metadata-only source inventory

**Hypothesis and denominator.** Every source entry required by the remaining DAG can be classified
present, absent or conflicting without decoding economics. The denominator is every path named by
O1/HG_B1/C0/N1/F1/K1/P1; none may silently disappear.

The smallest missing tool is a metadata-only manifest reader that permits file presence, size,
SHA-256, schema, declared spans and lineage; refuses outcome keys, March/live paths and February
economics; and emits exact missing spans. It must create no pool.

**Gate.** Zero hash conflicts, forbidden touches or silent omissions. Presence is not economics;
absence returns SOURCE_ABSENT_WITH_EXACT_CAPTURE_PREREQUISITE. Cap: 900 s, 0.75 GiB RSS, 0.1 GiB
disk. S0 may overlap G0 but carries no science authority.

## O1 — fixed OB-retest persistence

**Branch status.** Frozen first fallback. It executes only if the HDF-admitted breaker reaches P1
REJECT or NOT_EVALUABLE; it does not append V28 while the V27 breaker is on the critical path.

**Hypothesis.** The already selected current_ob_retest, as-declared 1.5D target / 0.25D stop
geometry remains positive on both independent path-complete RECORDED folds:
2026-04-16..04-30 and 2026-05-16..05-30. The unchanged gate still carries the published January
2026-01-16..01-30 construction fold as its first of three folds; January is not described as
independent confirmation. February, March and live-forward are excluded.

**Decision identity.**

- Member: fb_current_ob_retest_as_declared_target_1p5d_stop_0p25d_v1.
- Canonical identity SHA-256:
  3e8e9508ce309a84a28a3b6156425bd3ab563b9990f72e13d39bc777b1252a7d.
- Orientation as declared; recorded order fill semantics; 120-minute horizon; FTMO-Server3,
  mid-band HDE-complete cost; population RECORDED; B_balanced alpha 0.10.
- Before outcome decode, atomically append this one member to V27: 59/57 becomes V28 60
  all-declared / 58 looks-taken. Pad the other 59 p-values at 1.0. This is one paid graduation bill.

**Inputs.** Every source current_ob_retest row in all three full captures; CQ January path receipt
87e8a086… and ordered-sidecar ffa2a215…; April pool/path 8665b88e… / 5fc38c24…; May
d126bbab… / 803cf08c…. Reconstruct exact fill or no-fill from recorded order semantics and ordered
M1/ticks; start the outcome path strictly after fill; parse tick time through
exit_overlay.true_utc_tick_time_us; charge all four HDE-authoritative cost components.

The smallest runner work is a thin composition of the existing OB transform, CS composite/path
loader, true-UTC exit parser and HDF-final segmented gate. It must not equate decision time with
fill.

**Gate.** Preserve the unchanged three-fold B_balanced thresholds, including three evaluable folds,
positive-fold fraction ≥0.60, positive pooled and drop-best-fold net, at least 30 trades total and
five per fold, and exact segmented both_conservative BH q ≤0.10. Add the preregistered persistence
conjunction that both new April and May folds have net mean and paired delta >0. Require 100%
opportunity, fill/no-fill, terminal and cost coverage before invoking the gate. M1 ambiguity uses
both extrema orders and the lower net.

ADMIT advances default-off. Any evaluable failure is REJECT with no geometry/orientation reopening.
Any missing row authority is NOT_EVALUABLE with the exact source span. Cap: 1,800 s, 2 GiB RSS,
1 GiB disk. Outputs bind V28, source/path identity, per-fold results, gate and A/B.

## HG_B0/HG_B1 — exactly one breaker branch

### HG_B0: HDF router/direct branch

HG_B0 reads only the final HDF receipt. It recomputes no economics. HDF's observed ADMIT advances the
unchanged cq_current_breaker_re_entry_inverted_5d_stop_0p25d member and skips HG_B1. The family remains
V27: 59 all-declared, 57 looks, 58 p=1 pads, B_balanced alpha 0.10, RECORDED. Any integration-time
identity mismatch or use of HDC without HDF blocks.

### HG_B1: guarded book-diversifier branch

**Status: SKIPPED_BY_PREREGISTERED_BRANCH.** The following is retained as the exact counterfactual
plan, not execution authority.

**Hypothesis.** The unchanged breaker daily series adds positive, low-correlation,
non-risk-regressing value to the actual base book even though standalone admission did not clear.
This is one test, not a breadth grid.

**Inputs and denominator.** HDF-final January/April/May daily series; the union of all declared
capture days with candidate and base-book zero days retained; a hash-bound actual base-book
candidate-to-exit trace on those dates. W7/ultimate rows may be the base comparator only. They never
replace the broad breaker population.

Use guarded diversifier cdc42e33… and book replay 3198060a…. Fixed request weight 0.5,
correlation ceiling 0.35, 3,000 bootstrap draws, minimum 30 overlap days, candidate/book SD ratio
≥0.05, applied scale ≤3, lifetime mean R >0, blackout gross R ≥0, 100% cost coverage, positive
book return and Sharpe deltas, no risk regression, informative book, and predicted/measured
Sharpe-delta sign agreement. The single incremental-book p-value pays the same fixed 59-member BH
bill at alpha 0.10; q must be ≤0.10.

Any guard or q failure is REJECT with no alpha/weight/correlation retune. Missing or uninformative
base-book evidence is NOT_EVALUABLE. Cap: 1,800 s, 2 GiB RSS, 1 GiB disk.

Exactly one branch executes because direct persistence and breadth answer the same breaker
continuation. Running breadth after a validated direct ADMIT would spend a second look without
changing the decision.

## C0 — one shared source capture, two independent observability receipts

**Hypotheses.**

1. safe+hard-eligibility-observability makes the exact hard-eligible versus neutral-rank partition
   identifiable without changing an opportunity or economic field.
2. safe+conditions preserves all 15 as-of-safe entry fields on that same immutable population
   without changing an opportunity or economic field.

C0 executes only if N1 or F1 opens, or if a survivor route actually consumes these fields. It is
deferred on the HDF-fixed breaker critical path; forcing an unrelated absent source to block that
candidate would add work without changing its decision.

Use 2025-06-02..2025-07-11, split 06-02..06-13, 06-16..06-27 and 06-30..07-11. The partition is
train_backfill_2025H2, but the training-lane surface is VAL and every output carries the used-once
disclosure. This is a new telemetry capture, not a virgin headline-economics surface.

The currently absent prerequisite is an authenticated lane registry with complete D1/H4/H1/M15
dependencies, monthly M1, ordered ticks where available, FTMO-Server3 true-UTC conversion and
per-file hashes. Until it exists, C0 stops before outcome decode.

The smallest runner change is one fan-out sink: snapshot a canonical pre-patch identity/economics
witness once; apply the explicit combined patch list once; emit a hard-eligibility projection and a
conditions projection with separate manifests, SHA-256 values and receipts. No second safe baseline
replay is needed.

The hard partition is exact: the producer's hard-eligible instance-key set; its neutral order sorted
by neutral-rank SHA-256 then canonical key; and selected=true versus selected=false over those same
keys, retaining scheduler/finalizer ranks, status and block reason. Publish counts and digests for
all four sets and prove their subset/equality reconciliation—do not infer a partition from scalar
score rows.

The 15 fields are: atr14_over_atr50, stop_distance_atr, target_distance_atr,
close_position_in_lookback_range, trend_state_m15, trend_transition_flag,
dist_to_prior_high20_atr, dist_to_prior_low20_atr, trigger_bar_range_atr,
trigger_bar_body_atr, compression_ratio_prior_bar, bars_since_session_open,
close_to_close_vol_8_over_48, sweep_depth_atr and session_open_range_width_atr.

**Capture gate.** Zero source/pre/post identity, economics, opportunity-count, hard-count/digest,
condition-key, malformed-value or unexplained-drop mismatch. Null is explicit and retained; it
cannot suppress a row. C0 does no ranking, veto or economic selection. Cap: 10,800 s, 6 GiB RSS,
4 GiB disk; heavy count 1.

N1 and F1 may consume C0's immutable bytes. Sharing the source does not share a statistical family:
each downstream lane pays and reports its own denominator and look.

## N1 — executable NY-metals population

**Hypothesis.** The already-billed cp_true_utc_ny_metals_long_v1 member survives its frozen gate on
an independently recorded, complete broad-V4 executable population.

The exact member remains candidate_id 1493e333da89dbd6, spec digest
0f4382260c4b5ba8b1acb03d624c5ecf51d8decb46f448fa1287327263631ff7, V27 59/57,
RECORDED, B_balanced alpha 0.10. It adds no bill and permits no predicate/band change.

**Source contract.** Every XAGUSD/XAUUSD NY-session LONG broad-V4 M15 decision before fill/scoring;
true-UTC H4/D1 features and lookbacks; ordered M1/ticks through terminal; explicit fill/no-fill;
fill UTC/price, exit UTC, pre-cost gross R, terminal reason, path source and SHA-256; HDE complete
cost; HDA v2 hash-bound lineage/recording authority. CJ, W7 and ultimate-book rows are forbidden
substitutes.

January 2026-01-21..01-30 is mechanical same-lineage reproduction only. Gate evidence comes from
C0's source-bound population. Extend the existing CR runner only to accept that manifest and HDA v2
fidelity. Require recall ≥0.50, any HDA-prescribed precision floor, and 100% candidate,
fill/no-fill, terminal and cost coverage before the unchanged 59-member gate.

ADMIT advances default-off; evaluable gate failure is REJECT with no repair family; any missing
source is NOT_EVALUABLE. Cap: 1,800 s, 2 GiB RSS, 1 GiB disk.

## F1 — FC2 fixed 24-cell family

**Hypothesis.** A mechanically representative exit action improves the full-denominator identity
policy only within one entry-state slice and persists across all three C0 blocks.

The family declaration SHA-256 is
1152cf8b552e790646ee37ce6c003a7399e3e7e751bd7f7864ced8f7a91e3cf0. It is exactly:

- Actions: break-even at +0.75R; harvest 50% at +0.75R; timebox at 60 minutes. These are the central
  pre-existing grid values selected mechanically, not FC's known top ranks.
- Predicate slices: authoritative cost ≤0.15R and >0.15R; NY and non-NY session; ATR14/ATR50 ≤1.0
  and >1.0; ORDERED_TICK and M1_CONSERVATIVE.
- Cartesian product: 3 × 8 = 24 one-factor cells. No interactions, top-k or expansion.

Each cell is a policy over the **full C0 opportunity denominator**: predicate-true filled rows use
the action; the complement and explicit-null rows use identity; no-fill remains no-fill. Condition
completeness below 100% makes that cell NOT_EVALUABLE rather than shrinking it. No old FC cell is
reopened as a full-population policy.

Use HDF-final priority: hard stop; previously active floor; deadline; trigger action on the current
non-exiting observation; target unless the same deadline timestamp; floor update for the next
observation; terminal only after the final observation completes its state transition. For M1 run
both extrema orders and take lower net. Partial gross uses original-size fractions and deducts full
HDE-authoritative cost once.

The three fresh blocks are 2025-06-02..06-13, 06-16..06-27 and 06-30..07-11. Reproduce the old
January first-13/last-8 boundary as historical compatibility only. February is a frozen-family
attribution appendix only and never gates.

**Gate.** BH across all 24 at alpha 0.10; q ≤0.10; positive net and paired delta in every block;
positive drop-best-block delta; at least 30 active rows total and five per block; 100% condition,
cost, fill/path/terminal coverage; actual improvement strictly exceeds the side flip and shuffle
seeds 1901/1902/1903. All 24 are reported; no top-k.

Survivors advance default-off. An evaluable failure rejects the cell; zero survivors rejects FC2.
Missing coverage is NOT_EVALUABLE. Cap: 1,800 s, 2 GiB RSS, 1 GiB disk.

## K1 — calibration only after survival and only when consumed

K1 is skipped if no geometry survives. It is also SKIPPED_NOT_APPLICABLE when the complete route is
deterministic and does not consume learned probability/value; calibration is not ceremonial work.

For a route that does consume it, fit one joint p_fill / p_positive_net_R / expected_net_R
calibrator on 2024-10-01..11-29 TRAIN, serialize it before opening the disjoint
2024-12-04..12-31 TRAIN holdout, and embargo 2024-12-02..12-03 plus every overlapping label span.
Reserve P1's paper shadow as TEST. C0's geometry-selection blocks, February and March are excluded.

The full survivor opportunity denominator, including no-fill and negative net outcomes, is
retained. HDE cost and HDF fill/terminal semantics are fixed. No hyperparameter sweep, refit or
opportunity veto.

On the single holdout, both Brier and log loss must strictly beat the uncalibrated comparator;
calibration intercept absolute value ≤0.05, slope 0.8–1.2, ECE ≤0.05; net-R MAE strictly better,
absolute mean bias ≤0.05R; at least 500 fit and 200 holdout opportunities; opportunity-count delta zero. Failure rejects the calibrator, not the
surviving deterministic geometry unless that route requires it. Missing disjoint manifests is
NOT_EVALUABLE. Cap: one serialized heavy execution, 10,800 s / 6 GiB RSS / 4 GiB disk.

## P1 — complete-path shadow and canary-readiness package

**Hypothesis.** One admitted default-off candidate traverses the actual candidate, permission,
scheduler, order-preimage, exact fill/no-fill, HDE cost and HDF exit path with no unexplained
opportunity loss and retains its unchanged frozen-gate disposition.

The smallest missing runner is a broker-inert adapter over actual route modules. It must refuse
MetaTrader5 import and order_send; emit a stage ledger carrying composite identity, disposition,
reason, timestamp and source hash; emit a missed-opportunity row for every source identity absent
later; and invoke the survivor's unchanged gate on the complete-path outputs.

Run the offline proof on the survivor's exact admitted non-February/non-March captures. Then collect
a nonexecuting paper shadow for at least 20 trading days and 50 opportunities; extend only to 40
days if fewer than 50. Insufficient opportunities after 40 days is a higher-information stop, not a
negative result.

**Mechanical gate.** 100% source disposition, route decision, order-preimage, fill/no-fill,
exit/terminal, cost and FTMO profile-symbol coverage; zero unexplained misses or duplicates; net-R
arithmetic within 5e-9; frozen economic gate still ADMIT; zero broker orders; execution/activation
authority false. The selected breaker's economic authority is FTMO operator_profile only.
redacted_account is reported as mechanical route compatibility and cannot enter an economic-readiness
claim without its own cost-true preregistered gate.

Success is FTMO_SCOPED_CANARY_READINESS_PACKAGE_READY_FOR_OWNER_DECISION_ACTIVATION_FALSE. Any mechanical
mismatch or frozen-gate failure rejects the default-off route adapter. Rollback removes/disables
only that new adapter or candidate mapping, preserves evidence, and touches no runtime/config/broker
or token surface. Missing route/source/profile evidence or insufficient paper opportunities is a
named higher-information stop.

Offline cap: 10,800 s, 6 GiB RSS, 4 GiB disk, one heavy execution. Paper shadow cap: 2 GiB RSS,
2 GiB disk, one heavy execution. The owner package records the source/gate chain, missed
opportunities, resource receipt, rollback, residual risk and the owner-only risk/allocation/canary
questions.

## Terminal interpretation

- **Canary readiness:** one candidate has a complete, default-off, broker-inert route proof and an
  owner decision package. It is not armed, queued or activated.
- **Honest higher-information stop:** the exact source, fidelity, route, sample or resource
  prerequisite is named and no reduced-population economic result is reported.
- **Rejection:** the evidence was complete and a fixed preregistered threshold failed. No threshold
  or family is revised inside the same lane.

No March outcome, live-forward outcome, result-bearing replay, source/test/config/runtime edit,
broker/VPS/token access, merge or push occurred in HG.
