# CL pass-surface no-op ceremony — EXECUTED 2026-07-31, ZERO MUTATION PROVED

Executed by the orchestrator (Fable 5) per `../../activation_pass_surface/PASS_SURFACE_CEREMONY.md`,
under OD-ALL-IN. Session CL never touched the VPS; this receipt is the orchestrator's.

## Result

| step | result | when (UTC) |
|---|---|---|
| package build + `--check package` | `PASS — PACKAGE IS SEALED NO-OP` | 2026-07-31 ~19:25 |
| host preflight capture (read-only) | `HOST_PREFLIGHT.json`, sha256 `d165e77b88c8…`, 24,796 B | 2026-07-31 20:41:42 |
| `--check preflight` | `PASS — HOST MATCHES APPROVED NO-OP BASELINE` | 2026-07-31 ~20:45 |
| mutation step | **none — the approved set is empty by measurement** | — |
| host postflight capture | `HOST_POSTFLIGHT.json`, sha256 `239c33c5d98a…`, 24,796 B | 2026-07-31 ~20:50 |
| `--check postflight` | `PASS — ZERO MUTATION PROVED` | 2026-07-31 ~20:50 |

Host truth proven: branch `vps/ultimate-conditioned-expansion-minimal-2026-06-18`, HEAD
`267cccc94`, supervisor SHA-256 `63079cec1cb9…` (19,575 B — full digest in the captures).
FTMO worker pair (PIDs 2380/1172, created 2026-07-31 11:00:57Z, 12 ms apart) carries exactly
the five tags + `--frontier-exits mx_btcusd_d1_donchian_20_breakout` +
`--spread-geometry-floor sub_mid_dn_revert,sub_xvol_pullback`; redacted_account pair (4708/7080,
11:01:01Z) carries exactly the four tags, no frontier, same floor. Both firing ledgers were
absent at both captures (quiet book, `_KEEP_DAYS=2`), consistent pre/post. 26 FTMO + 24 FN
startup proving lines captured, including both spread-floor banners per book, the FTMO MX
frontier banner, `tfs=[16388, 16408]` (FTMO) and `tfs=[16388]` (FN).

## Two integration repairs, disclosed

1. **`verify_pass_surface.py::one_process` modelled a book as one OS process; a live book is
   a parent/child pair** (byte-identical command lines, ~12 ms apart — CM's ceremony §5–6 has
   always said "two parent/child pairs"). Repaired to accept 1–2 rows with identical command
   lines created ≤60 s apart; differing command lines or a wide creation gap still refuse
   (that is the stale-survivor signature the check exists to catch). `MANIFEST.json`
   regenerated by `build_package.py`; the 6 ceremony tests pass.
2. **The committed capture script wedges PowerShell 5.1's `ConvertTo-Json`**: `Get-Content`
   lines carry ETS note properties (PSDrive→PSProvider object graphs) and `-Depth 12`
   serialization never returns (>35 min observed; 23 ms after the fix). The executed capture
   tool is `capture_marked.ps1` (committed alongside this receipt): the committed script plus
   `"$_"` string-coercion in `Log-Proofs`/key extraction and per-section progress marks.
   Output schema is identical; the JSONs here were produced by it.

## Consequence

The live books are PROVEN to run the last promoted set as of these captures. This is the
baseline the CM + CN + CO composed carry (2026-08-02 00:00 UTC boundary) starts from. Per the
ceremony's own §1.3, those later ceremonies move the host and CL's baseline claim ends at
their preflight.
