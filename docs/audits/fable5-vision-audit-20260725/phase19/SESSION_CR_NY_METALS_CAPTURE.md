# Session CR — the NY-metals-LONG capture contract, then the frozen gate for real

**Wave 19. Blocks B2950–B2999. Branch `phase19/ny-metals-capture` from current `main`.**
Result doc: `phase19/SESSION_CR_NY_METALS_CAPTURE_RESULT.md`. Receipts: `phase19/receipts/`.
You are Codex, working alone in this worktree. The orchestrator (Fable 5) reviews and merges;
you never touch `main`, the VPS, or any broker-capable script.

## Owner authority

OD-ALL-IN (`phase17/OD_ALL_IN_20260801.md`) governs. The commission is a brief, not a script;
build/improve/fix, never refute-and-stop. If the gate ADMITS, the candidate goes to the
orchestrator's ceremony queue with a full dossier — you arm nothing yourself.

## Standing safety boundary

March 2026 outcomes NEVER read; live-forward veto-only; February 2026 is CP's used-once VAL —
**you may not re-read February economics**; no VPS, no broker-capable scripts, no token-bound
config bytes; every look declared in the iteration ledger; `.hermes/` estates machine-local.

## Read first

1. `phase18/SESSION_CP_TRUE_UTC_FACTORY_RESULT.md` §5 — your entire mission is its four-part
   capture contract, filed at `phase18/receipts/CP_TRUE_UTC_NY_METALS_LONG_RECORDED_GATE_V1.json`
   lines 5–19. Read the receipt itself.
2. `phase18/receipts/CANDIDATE_FAMILY_V27.json` — the multiplicity authority (59 declared /
   57 looks). **CP's candidate is already billed. Re-running the gate with new evidence takes NO
   new graduation bill** (CP handoff §9.3). If you need any NEW look, declare it.
3. `phase16/SESSION_CJ_REMATERIALIZATION_RESULT.md` — the true-UTC estate. RECORDED eras for the
   gate come from the estate the ratified gate already uses (AA's walked RECORDED population and
   its era coverage), not from CJ's lane months alone.

## The mission

CP's factory survivor `cp_true_utc_ny_metals_long_v1` (candidate `1493e333da89dbd6`, spec digest
`0f4382260c4b…`) is NOT_EVALUABLE at the frozen gate for two named reasons. Close both:

1. **Generate the declared NY-metals-LONG policy as an executable candidate population over
   untouched OOS RECORDED eras** — exact fill/no-fill classification per candidate; for each
   fill: fill UTC/price, exit UTC, pre-cost gross R, terminal reason, content-bound path source.
2. **Measure and register generator fidelity at the frozen 0.50 recall floor** — a real
   measurement, not a structural transfer, unless the register's rules genuinely permit the
   transfer class (CQ's fidelity note shows how to do this honestly).
3. **Re-run the same all-declared RECORDED `B_balanced` α = 0.10 spec at the V27 tip** without
   another graduation bill. Report the verdict with per-band detail; never print a bare ADMIT.
4. **If ADMIT**: write the activation dossier (exact contract, exit frontier if measured, CL-class
   firm-true MC pricing at both accounts' measured rules, per-account verdicts) and hand it to the
   ceremony queue. If REJECT or NOT_EVALUABLE: name the exact missing capture, as CP did.

## Verification discipline

Scoped A/B: tool-emitted `gtos-ab-receipt-v1` fence vs the committed ZERO baseline (12,671+ —
read `receipts/FAILSET_BASELINE_MAIN.json` for the current figure), embedded in
`phase19/receipts/SESSION_CR_AB_RECEIPT.md`. Behavioural tests for anything wired into `src/`.
IMPLEMENTATION_STATE blocks B2950–B2999; retire your in-flight row in your closing commit.
Result doc ends with "What I got wrong". pytest has no `--timeout`; `gtos_hydrate_test_data.py`
hydrates even on `--help`; the R2 drift check reports CN's authorized engine break — do not
"fix" it back.
