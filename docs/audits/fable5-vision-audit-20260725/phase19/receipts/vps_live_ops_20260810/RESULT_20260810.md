# Session LR — VPS live ops, 2026-08-10

Executed natively on the live host, both accounts armed, owner-approved.
Companion file: `PRESTATE_20260810.txt` (full before-state).

## Headline

Both activation tokens were 4 days from expiry (2026-08-14T17:23Z). Both are
re-minted and valid to **2026-09-09T13:02Z**. Nothing else on the host was
changed: no config byte, no gate, no tag, no argv, no restart, no order.

Two of the four ceremonies in the brief (CN, CM) turned out to be **already
executed** by Session LN on 2026-08-06. They were verified, not re-applied.

## T0 — State verify

Recorded in `PRESTATE_20260810.txt`. Summary:

| check | result |
|---|---|
| both run_book workers | running, healthy heartbeats 12:58Z/12:59Z |
| monitor daemon | cycle 427, down=[] hung=[] blind=[] alerts=0 |
| three authority gates | all `true` (agent_config.yaml L1161-1163) |
| host HEAD | `b4567fbd39e0ecfc2964e96c7e7bba465a72c158` |
| MT5 FTMO | connected, trade_allowed=true, hedging, eq 108,342.47 |
| MT5 redacted_account | connected, trade_allowed=true, hedging, eq 96,229.28 |
| open positions | **0 on both accounts** |
| token-bound config bytes | byte-identical to LN's 2026-08-06 prestate |

**Deviation from the brief:** the brief expected NO `--frontier-exits` on either
worker. FTMO carries `--frontier-exits crypto`. This is Session LN's CM ceremony
(commit `f43e5cd2b`), not drift. redacted_account has no frontier, as intended.

## T1 — FTMO token re-mint

| | before | after |
|---|---|---|
| namespace | `operator_profile` | `operator_profile` (unchanged) |
| config digest | `ffe16657feaf…ece8` | `ffe16657feaf…ece8` (unchanged) |
| issued | 2026-07-31T17:23:49Z | 2026-08-10T13:02:25Z |
| expires | 2026-08-14T17:23:49Z | **2026-09-09T13:02:25Z** |
| verify | valid | `activation_token_valid` |

## T2 — redacted_account token re-mint + F1

| | before | after |
|---|---|---|
| namespace | `redacted_account_live_bee34003` | `redacted_account_live_bee34003` (unchanged) |
| config digest | `e184a81d3b1b…4f3b` | `e184a81d3b1b…4f3b` (unchanged) |
| issued | 2026-08-06T01:28:37Z | 2026-08-10T13:02:33Z |
| expires | 2026-08-14T17:23:51Z | **2026-09-09T13:02:33Z** |
| verify | valid | `activation_token_valid` |

### F1 was already repaired before this session

The brief states the FN token still carries namespace `redacted_account`. It does not.
**Session LO repaired F1 on 2026-08-06T01:28Z** (host commit `c989b1198`). The
superseded token's own `note` field records it verbatim:

> "Session LO F1 repair: bind to the namespace run_book.py declares
> (redacted_account_live_bee34003); prior token bound the profile name and refused
> every entry"

Evidence from `activation_audit.jsonl` (the token layer's own decision log, 270 rows):

- **231** `activation_token_namespace_mismatch` rows, all on **2026-08-04**,
  13:00:42Z → 14:59:08Z, detail `redacted_account_live_bee34003 != redacted_account`,
  `allowed=false`. One carries `{symbol: UKOUSD, type: SELL, volume: 0.52}` —
  the 08-04 energy trade the brief refers to. Confirmed.
- **Zero** mismatch rows for the live namespace after 2026-08-06T01:28Z.
- **5** `activation_token_valid` rows for `redacted_account_live_bee34003`,
  `allowed=true`, on 2026-08-06 — positive acceptance, not merely silence.
- Session LO/LQ's negative controls in the same log (`redacted_account`,
  `not_a_real_namespace`, …) all correctly REFUSED.

Correction to the brief's timeline: the bad token was minted 07-31T17:23Z, but
refusals only materialised on 08-04, because that is when the FN book first
tried to open exposure. It was not refusing continuously from 07-31.

### Re-verified today, after the new mint

```
FN  --namespace redacted_account_live_bee34003  -> valid=true  activation_token_valid
FN  --namespace redacted_account (the F1 bug)   -> valid=false activation_token_namespace_mismatch
                                             detail "redacted_account != redacted_account_live_bee34003"
```

The negative control's detail is the exact mirror of the 08-04 refusals, which
proves the binding orientation is now correct.

### End-to-end link to the running processes

Both live workers logged, at their 2026-08-06 08:39 start:

```
FTMO: activation context declared (namespace=operator_profile,     config_digest=ffe16657feaf)
FN:   activation context declared (namespace=redacted_account_live_bee34003,  config_digest=e184a81d3b1b)
```

These are exactly the namespace and digest the new tokens bind. No restart was
needed: the token is read at the `order_send` choke point, and `write_token`
is write-then-rename (`os.replace`), which is why minting mid-session is safe.

## T3 — CN: ALREADY EXECUTED, verified, not re-applied

Session LN executed CN on 2026-08-06 (commit `f43e5cd2b`). All seven
destinations verified present on disk today:

```
95dd8eb5b2b195a7    8801  src/costs/coverage.py                            (new)
b630b52f1a1f1e5a   39029  src/costs/model.py                               (new)
b7ee8c0b4daeb306    1702  src/costs/__init__.py                            (new)
edda5f1902136ae0   30983  src/components/ultimate_book/packet_economics.py
69efc33e27e23eee  232936  src/components/ultimate_book/book_owner.py
82f61b57a41310ef   37830  src/components/broker_net_cost_engine.py
927bed1f4ee2775a   39934  src/components/ultimate_book/execution_packets.py
research/operations/broker_truth_layer_2026_07_27/BROKER_TRUE_COSTS_V1.json  (new)
```

`book_owner.py` hashes to `69efc33e27e2`, which is precisely the after-hash LN's
commit declares, built on the **host** base `b7a82dc3d241` — not the shipped
CO-based payload that would have crashed both books at construction.

**LN's rebuild script was deliberately NOT re-run.** It verifies before-bytes by
design; run against already-rebuilt bytes it would fail its own check. Re-applying
an anchored edit set to already-edited files is exactly the failure mode the
ceremony discipline exists to prevent.

## T4 — CM: already executed, cannot be deferred

The brief asked for a deferral record on the grounds that the wave-20
corrected-quote-convention re-walk (`origin/main 9392e9bbd`) moved crypto's best
exit cell in 2 of 3 bands, superseding CM's planning basis.

**CM is already live.** `scripts/run_book_supervisor.ps1:140` carries
`frontier="crypto"`, the FTMO worker runs with `--frontier-exits crypto`, and the
book banners `FRONTIER EXIT CONTRACT IS ON for crypto: stop_1p5x_target_scale`.

Deferring it now would mean *removing* a live exit contract — a live-behaviour
change on an armed account, which is beyond both the brief's own safety lines and
its stated intent ("DO NOT EXECUTE" assumed CM was unapplied). **No change made.**

→ **This needs an owner decision.** The wave-20 concern is substantive and CM is
running on the superseded basis. Flagged, not acted on. If Borhen wants CM rolled
back, that is a scoped supervisor edit plus a restart, and both accounts are
currently flat, which is the cheapest possible moment to do it.

## T5 — CO: confirmed lapsed

Owner-directed lapse, declarations expired 2026-08-08. Nothing executed. CO's
destination files remain at before-state; proof: `book_owner.py` contains **0**
`lane_weight` references, so CO's lane-weight payload is absent, exactly as LN
recorded when rebuilding CN on the host base instead of CO's.

## T6 — not applicable

T3 required no restart (nothing to apply), so no restart occurred and I2
(unbuffered book logging) was correctly skipped. Host not rebooted; the pagefile
remains an owner item.

## Book health after the mint

The 13:00Z H4 cycle ran on both books (FTMO 13:00:12Z, FN 13:00:39Z), before the
mint at 13:02Z, and both report:

```
halted=False  killed=False  runtime_effect_now=True  place=True
reason=no_candidates_this_bar   tags=[crypto, energy_agri, sub_xvol_pullback, sub_mid_dn_revert]
```

Neither kill flag exists on disk. (`killed=True` in the 08-06 startup banner is a
transient artifact of the restart choreography, not a live state.)

## Unexpected findings

1. **CN and CM were already done.** The brief's T3/T4 premises were stale by four
   days. Verified rather than re-executed.
2. **F1 was already repaired** on 08-06 by Session LO; today's FN work was an
   expiry extension, not a namespace fix.
3. **`MAX_TOKEN_LIFETIME_HOURS = 720` (30 days)** caps the mint. The brief asked
   for expiry 2026-09-14 (~840 h away); that is refused by `build_token`. Minted
   at the 720 h maximum → 2026-09-09. **I did not edit the safety cap to reach the
   requested date.** The 08-14 cliff is fully removed; a further re-mint is due
   before 2026-09-09.
4. **redacted_account skips 13 symbol/sleeve pairs every cycle** with
   `profile_missing_instrument_config`: DASHUSD (crypto), and XAUEUR, XAGEUR,
   XAUAUD, XAGAUD, CORN_c, COTTON_c (both substrate sleeves). FTMO skips none.
   FN is silently running a narrower universe than FTMO. Not fixed here: it needs
   a `config/profiles/redacted_account.yaml` edit, which would change the token-bound
   digest and require another re-mint. Recommend scheduling with the next mint.
5. The live-tree `CLAUDE.md` contains no "hazards H1–H8" or "section 4" as the
   brief describes; it is the 109-line GTOS agent-instructions file. The brief's
   safety lines were followed directly instead.

## Receipt handling note

The four `*_token.json` copies beside this file have their `signature` (HMAC)
field **redacted**, because this repo has a GitHub push remote and a live token's
signature has no evidentiary value here. Each redacted copy records the sha256 of
the unredacted file as `write_token` wrote it, so byte-integrity remains provable:

```
BEFORE_ftmo        1972f42bacda53b0590a6bd0d2dd050ac7e9a4e7467cec9f5a8c2d0ef6e84452
BEFORE_redacted_account  346a3168682190176e2f9cc2b53c521e968eaca69046f0cfe8df1f56b7ce71de
AFTER_ftmo         1c9300d73c456bbd8fee1974f277027a698284292909e36fa79d3a6860376d6d
AFTER_redacted_account   c09eba11f3dd7f516227d8827fc1b8d4809eac680f46e2a37660c2bf1f90667e
```

The two AFTER hashes were re-read from the live token directory after redaction
and match exactly, confirming the live tokens themselves were never modified by
the receipt-writing step.

## Push status

`origin` is configured for push, but the remote branch head is `redacted_host`, many
commits behind the host. Sessions LN, LO and LQ all committed host-only. I
followed that precedent and **did not push**. Publishing these receipts to GitHub
is an outward-facing action and is left to the owner.
