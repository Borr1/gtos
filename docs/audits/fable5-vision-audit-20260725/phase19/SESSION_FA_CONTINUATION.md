# Session FA — continuation commission (OD-BROAD-FORENSIC-2): verify the Sol estate, land it gated, repair the system, measure it, stage the virgin confirm

**Wave 19+. Blocks B3050–B3099 (continue) + B3100–B3199 (take — verify the range is
unclaimed with one rg sweep over docs/audits before recording the take in
`WAVE_11_WORKING_AGREEMENT.md` §4 together with the missing wave-19/20 records).**
Branch `phase19/broad-forensic`, worktree
`/Users/borr/GTOSActive/worktrees/wave19-broad-forensic-20260801`.
**You are Claude Fable 5 at maximum effort, ultracode standing** — the owner's Workflow
multi-agent opt-in from the original commission carries forward ("a fable session with
workflow and subagents"). **Every agent in your tree of work — walkers, explorers,
reviewers, verifiers, judges, synthesizers — is Fable (`claude-fable-5`): never pass a
model override that downgrades a lane, never delegate to Codex or Opus. Owner's word,
given twice.** Result doc: `phase19/SESSION_FA_BROAD_FORENSIC_RESULT.md`
(re-authored in YOUR voice — see §3). Receipts: `phase19/receipts/forensic/`.

**Your first three actions, in order, before any other work:**
1. Read this file end to end.
2. Read `phase19/FA_CONTINUATION_VERIFIED_STATE.md` — the frozen state map. Every claim
   there is tagged [VR]/[AP]/[CT]; those tags are load-bearing. Nothing tagged [AP] or
   [CT] may be quoted as fact until your own phases upgrade it.
3. Run the CLAUDE.md preflight (`python3 scripts/generate_live_state.py`, then the
   numbered reads). CLAUDE.md hazards H1–H8 bind absolutely.

## §1 The owner's mandate

The 2026-08-01 mandate is quoted verbatim in `phase19/SESSION_FA_BROAD_FORENSIC.md` §"The
owner's mandate" — it remains your charter: follow the decision cycle in the replays from
the top level until literally the last point of either trade or reject; find what is
causing each specific thing; the solution is never "no more hours on this."

**The owner's continuation words, verbatim (2026-08-03):**

> "i wasn't bullish of the 'kill the broad selector idea and find new edges from scratch'
> idea and that's why i insisted on a full and thorough audit on the different components
> of the broad system from the market state to selection to literally every single gate
> in the replay system and what happens exactly and what makes the system negative, and
> fixing the system comes from fixing the small issues we find in all its components.
> and to make sure that every single part is literally doing its part and its job and not
> a conservative blocker, because if the system is losing it must be losing for a reason,
> maybe the sleeves arent accurate, maybe the configuration isn't accurate, maybe the
> implementation is overkill, maybe the gates are too many, that's why i wanted the full
> fable audit and review so that the whole puzzle is solved first in there and then we
> see the results and we fix all the issues we find […] i want […] not only the audit but
> also the fixing and the work needed to fix the whole thing and seeing the results and
> we move forward from there […] i dont like passive conservatism and there is no reason
> to have something that is killing the edge for no reason and there is no reason to wait
> for days for live data when we have infinite data to dig from already, also some codex
> sessions did some work if you can review it too but im not sure if it's organized or
> good reviewed work and not repetitive sessions."

**The owner's second directive, verbatim (2026-08-03, after reviewing the first draft
of this commission):**

> "can you please remove any safe conservatism that is in the prompt and any language
> that might pull it back psychologically and please all reviewers and explorers and
> workers should be fable so that the puzzle is solved and we we work on fixing it
> completely and not just go the other way completely and also make it see the edge too
> and understand the system and how it's suspposed to be from start to finish in a a
> single full flow and make sure that flow is flawless and then all the issues will be
> clear and easily fixable, also please make it merge everything to main with my
> eplxicit approval and delete and cleanup all the other worktrees when done"

That directive is folded into every section below: the Fable-only tree of work (header),
the FULL FLOW centerpiece (Phase B item 1), the edge-bearing arm (Phase D arm v), the
fix-and-capture doctrine (§3), the owner-approved merges and the estate cleanup
(LAND gate + Closing ceremony).

**Owner decisions of record (2026-08-03):**

- **OD-FA2-1 (March):** Pre-register the March one-shot NOW; March outcomes are read
  ONLY on Borhen's explicit later word, after he has seen the frozen repair set's
  Jan/Feb results. You freeze the protocol; he pulls the trigger.
- **OD-FA2-2 (merge, revised same day by the directive above):** You prepare both
  landings and **execute each merge to `main` on Borhen's explicit approval**. Gate for
  the integration landing: Phase-A disposition receipts complete; the breaker flip
  independently re-derived (A1); full-suite A/B vs the sealed wave-20 baseline
  (12,809/2/0) with zero regressions by failure set. When the gate is green you bring
  him the landing summary and merge on his word — same for the closing merge of this
  branch's results. `main` is touched at those two approved moments and nowhere else.
- **OD-FA2-3 (June gap):** 2026-06-01..07-28 is opened as TRAIN-labeled data (never
  confirmation-grade); pack materialization authorized when needed.
- **OD-FA2-4 (cleanup):** When everything is merged, the estate's other worktrees are
  deleted and cleaned up — on his approval, per the Closing ceremony section.

## §2 What is true when you start (one paragraph; numbers in the appendix)

FA Phase 1 completed and reconciles digit-for-digit: selection is provably NEUTRAL (the
picks were not the defect), the gates refused negative-mean sets on average yet 61–84 %
of pool winners die at the cost stage because tight stop geometry inflates
`spread_r = spread/|entry−stop|` until 74–82 % of candidates are priced untradeable, the
belief/EV layer is structural fiction (Brier skill −1.15/−0.96, a hardcoded 0.55
confidence constant, EV priced at a 1.5R contract that is never walked), parts of the
cost model are placeholders (BTCUSD spread 0.0001; February's constant 0.12 defaults),
and the executed February book was **gross-positive** — its entire loss was the 4.165 R
cost bill. FA Phase 2 died 100 % on the weekly rate limit; Codex sessions FB–FF executed
those five lanes instead (grid → 2 survivors incl. the inverted breaker; exit overlays →
0/40 persist; conditions → 0 cells; defects → 9 classes, Feb re-decode −178.6 R; 
composition → FVG negative, no veto licensed), FG integrated everything by cherry-pick
and ghost-wrote your result doc, and wave 20 (HA..HP) hardened it falsifier-paired —
leaving 28 unmerged branches, one CONTESTED verdict (the breaker REJECT→ADMIT flip), and
zero governance record. Every prior conclusion — FA's included — is a hypothesis with a
tag, not an inherited fact.

And hold the other half of the picture the whole way through: **the surface provably
contains edge.** 92.5 % / 90.0 % of decision points carried an ex-post-positive
candidate (oracle +1.164 / +1.179 R per decision point); the inverted breaker measures
+7.52 R/trade pooled OOS across three folds with all three positive; FB's grid found two
survivors and seven more gross-positive families under the cost bill; and the executed
February book already trades gross-positive. Your product is the flow that CAPTURES that
edge — not a tidier explanation of losing it, and not a replacement system: this system,
fixed completely.

## §3 Doctrine (binding)

1. **Passive conservatism is a defect class.** Every stage must prove with numbers that
   it earns its keep — **including the gates' own caps and thresholds**; "correct on
   average" is a measurement, not a defense, when the same gate eats 61–84 % of the
   winners. The measurement is symmetric because that is how a loosening gets PROVEN
   safe and LANDED — measure what it frees and what it admits, train then held out —
   never because loosening is suspect by default. Nothing in the flow is exempt from
   repair.
2. **Evidence classes on every claim**: `DIAGNOSTIC` (attribution, billed:false) /
   `DEVELOPMENT-FITTED` (anything shaped on burned windows — which is ALL of Jan, Feb,
   Apr, May, and the June gap) / `PREREGISTERED-CONFIRM` (March one-shot only). Nothing
   upstream of the virgin one-shot is ever quoted as confirmatory. The RESULT doc labels
   every number with its class.
3. **Prior sessions' conclusions are hypotheses.** FG's first-person
   `SESSION_FA_BROAD_FORENSIC_RESULT.md` (on `phase19/sol-integration`) is INPUT TO
   VERIFY — you re-author the result in your own voice from your own receipts, labeling
   each finding FA-verified vs Sol-inherited-verified. Any prior claim your measurement
   contradicts is refuted loudly, whoever wrote it.
4. **Kill/park/arm stays Borhen's.** Your deliverable is understanding + landed repairs +
   measured results + a decision sheet, priced. Nothing arms itself out of this
   commission, and no verdict to abandon the family exists in your vocabulary — the
   owner never allowed one.
5. **The posture is fix-and-capture, not retreat.** When a defect is found, the next
   action is its repair spec — not a mitigation note. When an analysis kills a cell, the
   AXIS stays open unless the axis itself was measured dead. Write findings, numbers,
   repairs, deltas; no hedge language, no pre-emptive deprioritizing of a lane because
   it "probably" won't pay — measure it or schedule the measurement that decides it.

## §4 The phases, with gates

One session, chained Workflows. **A phase is complete only when its receipts are
COMMITTED and `phase19/receipts/forensic/RESUME_STATE.json` is updated** (phase, what's
done, what's next, artifact paths). That file is what lets a successor session continue
without re-derivation if you die mid-run — FA's outputs sat untracked for two days; that
never happens again. Persist every intermediate to disk as you go; commit at every phase
boundary, not at the end.

### A0 — Build + fence the integration candidate (first; everything else runs on it)

1. From `main` (f8c05d0ac), build the integration branch: merge tip
   `phase20/p1-m1-verifier-repair` (`502d90616`); cherry-pick the four doc-only leaves
   `11594419a` (HK close), `5c94d2caa` (HL close), `ec4555696` (HM close), `97d4c7da1`
   (HN docs); **discard** `phase20/p1-m1-sparse-repair` (repudiated by HP; record the
   discard). Never separately merge FG's seven cherry-pick sources (double-apply).
2. **HV adopt-or-kill**: the untracked 3,092-line
   `src/research_infra/p1_offline_complete_path_runner.py` + test + fill-authority
   prereg in the `wave20-p1-m1-verifier-repair-20260802` worktree. Adopt = commit with
   its test passing and its prereg frozen (execution stays PARKED per §7 of the
   appendix); kill = record why. Its referenced output hold does not exist — do not
   invent one.
3. **Relocate docs/-resident production code** (`wave20_complete_path_shadow.py` 47 KB,
   `wave20_source_inventory.py`, `verify_wave20_breaker_branch.py`,
   `cs_breaker_folds.py`, `session_fc_exit_overlay.py`) into `src/research_infra/` with
   tests; leave import shims or update callers; re-resolve forward references (old
   SHA-bound receipts stay valid as history — landing is merge, not rebase).
4. FA route is already committed on this branch, byte-verified 76/76 vs FG's manifest
   (`phase19/receipts/forensic/FA_ROUTE_BYTEVERIFY_20260803.json`).
5. **The identity fence**: (a) full suite on the integration branch, diffed with
   `scripts/pytest_failset.py` against the sealed wave-20 baseline (12,809/2/0) — zero
   regressions by failure set required; (b) one 2-day `engineering_stop_after_day` lane
   arm on January, diffed against the sealed CJ baseline's first two day-ledgers —
   **integration must not move a single outcome**. If either fence fails, fix the
   integration before ANY measurement; every later number depends on this one engine
   identity.

### A1 — Clean-room re-derivation of the breaker flip (parallel to A2; gates the LAND, WAVE20_B0, and every breaker-dependent number)

Spawn an ISOLATED subagent that receives ONLY: CS's frozen fold data, the ratified gate
spec, and the null-construction question ("is the common-circular-phase permutation rule
rotation-invariant? build the correct null and compute p"). It must NOT see HDC/HDF's
code, their p-values, or this file. Requirements: **≥10,000 permutations** (the claimed
p=0.0009765625 is exactly 1/1024 — the old test's resolution floor; an ADMIT may not
rest on a floor); then, in the open, re-issue the verdict table for **every** family
member whose verdict passed through the affected code path, flips reported in BOTH
directions (an ADMIT→REJECT elsewhere binds immediately; a REJECT→ADMIT elsewhere
graduates nobody without its own declared step). Consequence tree, fixed now: confirmed
⇒ the breaker's ADMIT stands at the corrected null, file the method-defect ledger row +
the re-issued table (A4), and the challenge-dossier option goes on the owner sheet;
refuted ⇒ breaker reverts to REJECT, O1 (OB folds) becomes Phase B's lead item, and
WAVE20_B1 stays skipped either way (no auto-authorization). (WAVE20 node ids are
underscore-prefixed in these docs to keep the block-citation scanner from reading them
as evidence blocks; the prereg spells them bare.)

### A2 — Verify FB–FF + reconcile (the owner's "review the codex work")

Spot-recompute from raw artifacts (not from Sol's receipts): FB's two grid survivors
(+11.901 / +1.647 R at their cells), FC's 0/40 with the V17 sign flip, FE's zero cells,
FD's −178.589 R Feb re-decode with 67 sign flips, FF's FVG split. Reconcile the
contradiction register A–Q (appendix §4) — pick ONE loss-class rule and restate both
months under it. Then re-author `SESSION_FA_BROAD_FORENSIC_RESULT.md` in your voice:
six axes closed with numbers, every finding labeled FA-verified / Sol-inherited-verified
/ refuted, evidence class on every number.

### A3 — Disposition receipts

`phase19/receipts/forensic/SOL_DISPOSITION.md`: per-branch adopt/refute/discard with the
one-line reason and receipt pointer. Preserve HDF's three volatile `/private/tmp`
comparator worktrees (archive the comparison inputs/outputs into receipts, or re-derive
the three-way A/B on durable storage) BEFORE any reboot risk.

### A4 — Multiplicity + governance

1. **Method-defect ledger row** for the HDC/HDF null correction + the A1-re-issued
   verdict table committed as the statistical record (this is a test-engine change, not
   a membership change — no version bump for it alone).
2. **Retroactive screening-family declarations**: FB's 198×10 grid, FC's 40 overlays,
   FE's 15 fields are declared SCREENING FAMILIES (internally max-T/BH controlled;
   non-graduating cells are DEAD absent a new declaration). OB-retest's graduation is
   already priced: **adopt O1 AS-WRITTEN** (identity SHA 3e8e9508…, V28 single-append
   with 59 pads at 1.0) — only its schedule position is superseded; redesigning it would
   be a second look.
3. **WAVE20 supersession register**: finalize appendix §7's table (G0
   superseded-with-reason; S0 absorbed into the March preflight; WAVE20_B0
   consumed-conditional-on-A1; WAVE20_B1 stays skipped; O1 adopted as-written; C0/N1
   REPRICED on the February evidence — the NY-metals class went gross-negative in
   February's own funnel, so its follow-ups re-enter the queue the moment the full-flow
   walk or the cost-repaired reruns revive the class, and the capture INFRASTRUCTURE
   stays live regardless; F1/FC2 recorded BLOCKED on C0's absent authenticated registry
   — unblock by building the registry if the exit axis demands it, never by improvising
   inputs; K1 parked, the Phase-B pool screen is named BELIEF-RECAL; P1 execution
   SCHEDULED-ON-NEED — the moment any breaker decision moves toward money, fill truth
   becomes REQUIRED and P1 runs with HP's M1-acceptance rule landed, turning the
   492/10,810 full-tick/M1-only split into a measurement instead of an assumption).
   One ledger row cites the prereg doc + JSON SHAs.
4. **WAVE_11_WORKING_AGREEMENT.md §4**: write the missing wave-19 landing record and the
   wave-20 commission/disposition record, plus your block take.

### LAND gate (OD-FA2-2 revised)

When A0's two fences pass AND A1 is done AND A2/A3 receipts are committed: bring Borhen
the landing summary (what merges, the A/B receipt, the A1 verdict) and **merge the
integration branch to `main` on his explicit word**, A/B receipt embedded in the merge
commit message. Development (B/C/D) runs on the integration branch meanwhile; nothing
waits on the merge except the merge.

### B — Close the forensic (what nobody has run)

1. **THE FULL FLOW — the centerpiece.** Build `phase19/FULL_FLOW_SPEC.md`: the system
   start to finish in ONE document — data → market state/context → candidate generation
   → beliefs (probability/EV/confidence) → costs → gates → selection → sizing →
   execution/fills → exits → attribution/learning. For every link, four columns:
   (i) what it is SUPPOSED to do (design intent, from code and charter); (ii) what it
   measurably DOES (receipts); (iii) verdict — **FLAWLESS**, or **DEFECTIVE/
   MISCALIBRATED with its repair id**; (iv) where edge ENTERS and where it LEAKS at that
   link, quantified in R. Feed it with **the instrumented short-window replay walk** —
   the owner's literal ask, one 2-day January lane window with instrumentation: walk
   EVERY candidate's fate on those days with full context — what the market was doing,
   what each gate saw and decided and why, what ranked, what sized, what the exit did.
   **The repair program's completion criterion IS this document: every link ends
   FLAWLESS or carries a landed repair with its measured delta.** This is the puzzle
   solved in one picture — the owner's words: understand the system from start to
   finish in a single full flow, make that flow flawless, and the issues become clear
   and easily fixable. It is also the narrative spine of the RESULT doc.
2. **S0R1 + S0R2 neutral-seed arms on January** (~3 h total): decides whether the
   executed book's family mix and the ~4.2 R/month cost bill are seed artifacts, and
   supplies the seed-noise band the March MDE needs. Two extra seeds, not one — two
   points give no dispersion estimate.
3. **BELIEF-RECAL** (pool screen, free): recalibrate probability/EV to base-rate honesty
   on January, ask whether ANY subpopulation clears its own breakeven under honest
   beliefs, check on February (labeled). This answers whether a selectable positive
   sub-book exists at true costs — the owner's axis 6 — independent of new candidates.
4. The remaining Phase-1 open questions that gate repairs (appendix §3.9): where
   `candidate_probability` is computed and clamped, the 0.92 fill template, the
   binary_population vs close-reason mismatch (L).
5. **Adversarial verify pass** over the combined FA+Sol top causal claims — independent
   refuter lenses, before synthesis (the original commission required it; still unrun).

### C — Author the repair set

Every repair in the `repairs.py` pattern: registry entry + paired INERT CONTROL +
`INCOMPATIBLE_REPAIRS` rows (horizon × `swap_horizon_true` explicitly) + ex-ante spec
with expected effect and defect classification
{bug | miscalibration | wrong-geometry | overkill | unjustified-gate | missing-condition}
committed BEFORE its first measured run. The set, from the verified evidence:

- **R-COST-TRUTH**: replace placeholder spreads (BTCUSD 0.0001, UKOIL/USOIL constants,
  February's cost_missing 0.12 defaults) with measured session-conditional spread models
  from the tick corpus (+7h broker wall-clock with the measured US-DST calendar via
  `src/utils/broker_clock.py`; era label "Jun–Jul 2026 spreads applied to Jan/Feb"
  disclosed). **Both directions**: replay stamps `commission_r: 0.0` — honest costs may
  be HIGHER; FD's re-decode already moved February −178.6 R. Either direction is
  decision-grade; say which way it lands.
- **R-BELIEF**: the honesty bundle — remove the sha256(family) probability term, the
  hardcoded 0.55 confidence, the +EV floor; price EV at the contract actually walked
  (2.0R-capped giveback, not 1.5R); wire `context["cost_r"]` so thesis EV is net-of-cost.
- **R-SCHEMA**: February emitter drift (`pretrade_cost_packet_status` dropped;
  `cost_missing` class) + the `(candidate_id, decision_time_utc, symbol, side)` join key
  + the 3,536 softened-reject non-monotonicity (fix or document as intended semantics).
- **R-GEOMETRY**: family-fit stop/target geometry, DEVELOPMENT-labeled, FB's grid as the
  prior (its screening-family declaration covers the looks; any graduation is a new
  declared step). This is where the cost gate's winner-kill gets addressed from one
  side — geometry that survives its own spread honestly.
- **R-CAPS**: the other side. The cost gate's own caps
  (`selected_cell_pretrade_max_spread_r = 0.10`, `total_cost_r ≤ 0.15`,
  `agent_config.yaml:715-716`) are measurement subjects like every other link: at
  honest spreads and repaired geometry, re-derive what the caps SHOULD be from the
  frontier of freed winners vs admitted losers, and repair them with the same receipts
  if they are mispriced. "The gate was right on average" is where the analysis starts,
  not where it stops.
- **Adopt Sol's four implemented default-off repairs** (FD semantics, CR capture infra,
  FF observability, FE telemetry) after A2 verification.

### D — Results, three tiers (the caps are numbers, not judgment calls)

- **T1 (free, first)**: pool-level screens for every repair on the January CQ sidecar
  (3.23 M ordered observations). Label "no selection feedback" wherever the repair moves
  admission (cost/belief repairs change the executed set; the sidecar cannot see that).
- **Feb sidecar decision**: build a February ordered sidecar (heavy CP decode) ONLY if
  exit/geometry repairs survive Jan T1; otherwise February gets lane arms only. Record
  the decision either way.
- **T2 (FOUR baseline-comparable lane arms per window, ~1.5 h each, strictly serial —
  plus the edge-bearing fifth when an admission stands)**:
  | arm | contents | question |
  |---|---|---|
  | (i) | **cost-truth-only** (R-COST-TRUTH alone) | the cheaper-path hypothesis: Feb executed book was gross-positive +0.204 R — is the book positive at honest costs alone? |
  | (ii) | composed minus geometry (R-COST-TRUTH + R-BELIEF + R-SCHEMA) | do the defect fixes move the book without any contract change? |
  | (iii) | full composed (+ R-GEOMETRY / R-CAPS) | the repair set's headline |
  | (iv) | composed INERT-CONTROL (paired neutral patches) | plumbing is inert; any (iv)−baseline delta is a bug |
  | (v) | **composed + admitted candidates** — runs when A1 confirms the breaker ADMIT and/or another candidate passes the gate; a DECLARED, billed read of its own | **the system with its edge in**: what the repaired flow does when it also captures the admitted edge |
  Baselines are the sealed 57/58-trade receipts — never re-run. 2-day smoke before every
  new arm config. **COMPOSITION RULE: arms (i)–(iv) contain REPAIRS only; candidates
  ride arm (v)** — the inverted breaker and OB-retest graduate at the ratified rule
  against the A4-adjudicated family table and then get MEASURED IN, openly declared,
  rather than smuggled into a repair delta. The edge is part of the flow; it enters
  through its own door.
- **T3 (leave-one-out, cap ≤6 arms)**: only where |ΔT1| > 0.5 R/window, or admission-set
  change > 5 %, or T1/T2 signs disagree. Per-repair deltas do not sum (path dependence —
  memory guard); say so wherever you print them.
- **June-gap TRAIN packs** (OD-FA2-3): materialize only if the repair set needs a third
  window for robustness; the tick-corpus overlap (2026-06-18..07-24) doubles as
  spread-truth validation.
- Every Jan/Feb/June number: `DEVELOPMENT-FITTED`, labeled.

### E — Freeze, pre-register March, synthesize

1. **Freeze the composed repair set** (exact registry entries + config + SHAs).
2. **March registered report** (`phase19/receipts/forensic/MARCH_PREREG_V1.json` +
   companion md), frozen BEFORE any March byte is read, containing at minimum:
   - the arms fixed at freeze time, ALL in ONE decode event (still one look at March
     outcomes): S0R0 baseline, the frozen composed arm, and — if an admission stands at
     freeze — the composed+admitted arm, so the confirm measures the repaired system AND
     the repaired system with its edge in;
   - primary endpoint: **paired daily net-R delta** (pairing removes market-level
     variance; ~20 trading days is underpowered unpaired);
   - one-sided α and the pass threshold, fixed ex ante; MDE computed from day-ledger
     variance + the Phase-B seed band (item 2 of Phase B) — the threshold must clear
     the seed band;
   - NOT_EVALUABLE terminals (pack absent, decode fault, arm identity mismatch);
   - the CR March-decode asterisk adjudicated IN the prereg (the virginity argument
     written down before the read, or the read refused);
   - S0-style metadata-only March-pack preflight (presence/hash, zero outcome decode);
   - executor: a FRESH micro-session that mechanically runs the frozen protocol —
     **Borhen triggers it (OD-FA2-1)**; your job is to hand him a protocol so tight the
     micro-session cannot wobble.
3. **Synthesis**: the re-authored RESULT doc (findings-first; six axes closed with
   numbers and evidence classes; the FULL FLOW as narrative spine); "What I got wrong"
   (yours AND inherited errors you propagated before catching); the **owner decision
   sheet**: landing state, A1 outcome and its consequences (challenge-dossier option if
   ADMIT), the T2 arm table with the cost-truth-only headline and arm (v) if it ran,
   what March would decide and the exact trigger phrase, arm/park/iterate options
   priced.

### Closing ceremony — merge everything, then clean the estate (OD-FA2-2/OD-FA2-4)

When the results are in and the RESULT doc is written:

1. **Present Borhen the closing summary**: what merges (the integration branch if not
   yet landed, plus this branch's full results), the A/B receipts, and the worktree
   cleanup inventory below. **On his explicit word, execute the merges to `main`.**
2. **Then clean the estate.** For EVERY worktree other than the main repo
   (`/Users/borr/GTOSActive/repo`), the main-branch checkout
   (`claude-opus5-architecture-audit-20260725`), and this one: the wave14–wave20
   session worktrees, `orchestrator-fable-20260730`, the `/private/tmp/gtos-hdf-*`
   scratch trio, and the four `replay-accel-*` trees — build one inventory row each:
   branch fully merged into `main`? untracked machine-local state present? load-bearing?
3. **Relocate load-bearing machine-local state FIRST.** The named case:
   `wave16-rematerialization`'s `.hermes/evidence/.../LANE_INPUTS_TRUE_UTC_V1/` — the
   lane-pack registry your own T2 arms read. Move it to a durable hold, update its
   registered paths, and **prove a 2-day arm runs from the new location BEFORE the old
   one is deleted**. Same for every untracked arm route named in the appendix. (This is
   also why cleanup is strictly LAST — the lane dies if wave16 goes early.)
4. `git worktree remove` + branch delete everything whose row is clean. Anything
   unmerged or unrelocatable goes on the exception list in the same summary — including
   the `replay-accel-*` trees, whose row carries a note on H4's sealed-path bindings so
   the approval is informed. **Borhen approves the sweep as one decision; deletions
   happen after his word, never before.**

## §5 Measurement standard

Ratified admission rule: `CANDIDATE_BOOK_V1`, all-declared basis, sealed `B_balanced`
α = 0.10 (`phase8/receipts/CANDIDATE_FAMILY_V1.json` → `ratified_rule`); family ratchet
from the A4-adjudicated table (V27 59/57 as inherited; O1's V28 single-append as
declared; A1's re-issue if it changes the table). Ledger discipline: every look declared
via `iteration_ledger.record()` (never hand-appended); attribution reads class
`FORENSIC_DIAGNOSTIC` `billed:false` with `owner_mandate_20260801` provenance on
February; candidate reads declared AND billed; screening families per A4. The garden of
forking paths closes ONLY at the March one-shot — no burned-window result is
confirmatory, however good it looks.

## §6 Boundaries (absolute)

March 2026 outcomes: NEVER in this session — prereg only, Borhen triggers (OD-FA2-1).
Live-forward (2026-07-29+) outcomes: NEVER. No VPS, no broker-capable scripts, no
token-bound config bytes, no activation tokens, no sealed-path replay launches (H1/H5 —
lane path only). R2 drift check reports CN's authorized engine break + two ledger
materialization rows — known, leave them. `main` is touched at exactly the two
owner-approved moments of OD-FA2-2 (the LAND gate and the closing merge) and nowhere
else. Arming and any verdict over the family are Borhen's — delivered priced, never
enacted. H3: never two replay arms concurrently — all heavy runs strictly serial,
2-day smoke first, 8 GiB headroom floor.

## §7 Mechanics

Workflow tool standing opt-in; run verify/judge/synthesis agents at max effort. Every
`agent()` call inherits the session model — Fable — and no lane ever sets a per-agent
model override below it. Persist
every intermediate to disk; commit receipts at EVERY phase boundary (per-phase gate);
maintain `RESUME_STATE.json`; workflow scripts are re-runnable via `scriptPath` — design
them idempotent so a successor session resumes from disk, not from memory. A/B fence:
`gtos-ab-receipt-v1` embedded in `phase19/receipts/SESSION_FA_AB_RECEIPT.md` — suite
diffs via `scripts/pytest_failset.py` against the sealed wave-20 baseline; never
re-capture a "before". Retire the in-flight block rows in your closing commit.

## §8 Deliverables

1. `FULL_FLOW_SPEC.md` — the system start to finish in one flawless-or-repaired flow;
   the program's completion criterion and the owner's centerpiece.
2. `SESSION_FA_BROAD_FORENSIC_RESULT.md` — re-authored, findings-first, six axes closed,
   evidence classes throughout, the full flow as spine.
3. `SOL_DISPOSITION.md` + per-branch receipts; the LAND merge executed on Borhen's word
   (or the blocking reason in front of him).
4. A1 statistical record: corrected-null derivation + full re-issued verdict table.
5. The repair set: registry code + tests + ex-ante specs + receipts; Sol adoptions;
   R-CAPS included.
6. T1/T2/T3 result tables with the cost-truth-only headline explicitly called out, and
   arm (v) — the system with its edge in — whenever an admission stands.
7. `MARCH_PREREG_V1.json` + md — frozen, unexecuted, with the trigger phrase for Borhen.
8. WAVE20 supersession register + method-defect ledger row + all iteration-ledger rows +
   WA §4 records + block-take.
9. The closing ceremony executed: everything merged to `main` on Borhen's word, the
   estate worktrees cleaned per the inventory, load-bearing machine-local state
   relocated and re-proven first.
10. "What I got wrong" — house style, including inherited errors.
11. Arming, challenge dossiers, and any verdict over the family: delivered PRICED on the
    owner sheet; the decisions stay Borhen's.
