# Session CE — the sleeping-improvements activation package (wave 15, B2300–B2349)

Read first: `phase15/OD_HISTORICAL_FIRST_SCALING.md` (your charter — a measured,
controls-clean improvement to an armed sleeve must not sleep behind a default-off flag),
`phase13/SESSION_AY_SPREAD_GEOMETRY_RESULT.md` (the floor: REPAIR on `sub_mid_dn_revert`
+0.426 R/day p→0.023 and `sub_xvol_pullback` +0.264 p→0.0020, NEUTRAL on crypto/energy_agri,
clean controls), `phase13/activation_carry_mx/` (Session AZ's carry-package shape — MANIFEST
with per-file before/after sha256, verify_carry.py, ceremony page; **this is the shape you
ship**) plus the host's `CARRIED_STATE.json` discipline, `phase8/SESSION_AH_CONDITIONING_AND_ENTRY_RESULT.md`
+ `phase9/OWNER_DECISION_ENTRY_HOUR.md` (the RATIFIED entry lever: ONE hour off rollover),
`phase13/SESSION_BA_WEEKEND_SOLUTION_RESULT.md`, `phase14/receipts/CA_MX_INCUBATION_V1.json`
(the promotion rule you restate), `WAVE_11_WORKING_AGREEMENT.md` §6, CLAUDE.md §3–§4.
Owner authority: OD-HISTORICAL-FIRST (2026-07-31) and the standing directives.

**The objective in one sentence:** every measured-and-clean improvement that is currently
built-but-asleep gets carried to the ceremony step — the orchestrator arms them; you arm
nothing.

## Work orders

**CE-1 — The spread-floor activation carry (the clearest case in the estate).** AY's floor
is REPAIR-grade on two ARMED sleeves and it is default-off. Build the AZ-style carry
package at merged `main` for `run_book.py` / `book_owner.py` (+ whatever the floor's import
closure needs on the host lineage): MANIFEST.json with copy order and before/after sha256
composed against the host's `CARRIED_STATE.json`, payload files, `verify_carry.py` whose
behaviour gates prove (a) the floor resolves for `sub_mid_dn_revert` and `sub_xvol_pullback`
at AY's per-sleeve thresholds, (b) NEUTRAL sleeves and `mx_btcusd`'s frontier contract are
untouched, (c) floor + weekend + frontier kwargs compose (the triple-kwarg signatures were
hand-merged at the wave-13 train — verify at the call sites), and a one-page ceremony:
exact `--spread-geometry-floor sub_mid_dn_revert,sub_xvol_pullback` supervisor-line edit,
flags-held restart order, the log line that PROVES the floor armed, expected economics on
recent folds, stop condition, rollback.

**CE-2 — The entry-hour lever, ratified ten days of sessions ago and still unimplemented.**
The decision is recorded (`phase9/OWNER_DECISION_ENTRY_HOUR.md`: enter ONE hour off
rollover; 94–98 % of the saving, net peaks 1–2 h). Implement it on the live generation path
the `--tags`-gated way (default-off, no config byte, AR's `DEFAULT_CONFIG`/AST hardening on
any new key), with an identity proof that OFF is byte-identical to today. Then the
activation dossier: WHICH armed members it touches (measure, don't assume — the finding was
the FX D1 cohort at broker hour 00; say exactly which members of the armed five it moves
and by how much on recent folds), expected per-member deltas, ceremony page. H1/R2
membership check on every file you touch.

**CE-3 — The weekend policy, stated honestly.** BA built `--weekend-flat` default-off,
binding FN funded-only. Write its one-page activation recommendation (arm / don't-arm, at
which account, with the measured cost/benefit) so the owner decides once instead of it
sleeping undecided.

**CE-4 — Promotion rules restated under OD-HISTORICAL-FIRST.** `mx_btcusd`'s P1 (+34 R over
60 fills ≈ 51 months) uses the live stream as the clock, which the directive forbids.
Restate promotion for `mx_btcusd` — and as the TEMPLATE for every future incubant —
historical-primary: weight steps up on historical-milestone evidence (e.g. the admission
surviving new RECORDED folds as data accrues, band robustness, the repair-interaction
holding) plus NO live veto (stop rules unchanged — they are risk bounds and stay fast).
Deliver as an amendment artifact beside `CA_MX_INCUBATION_V1.json` (do not edit CA's sealed
dossier), a re-registration path in the incubation registry (CC's shapes), and the
agreement language. State the false-promotion arithmetic of the restated rule as honestly
as CA stated the old one's.

**CE-5 — (if the backfill lands before you finish) vp decidability.** The orchestrator is
fetching GER40/UK100 M1 back toward 2025-12-29 in parallel with your session. If
`data/mt5_research_exports/bridge_ftmo_vp_m1_backfill_20260731/` exists when you reach this
order, re-run CA's gate (`phase14/receipts/ca_revival_gate.py` pattern) for
`vp_euidx_pocgrav` at the ratified rule and report the verdict; a REVIVAL_CANDIDATE gets a
CC-shape incubation dossier. If the data is not there, skip with one line — the orchestrator
owns the fetch.

## Done means

Result doc findings-first + dossiers + carry package committed, blocks B2300–B2349, scoped
A/B green vs the ZERO baseline (now 12,403) with the tool-emitted `gtos-ab-receipt-v1`
fence, honest "what I got wrong", handoff = the ceremony pages. Never touch the VPS (the
orchestrator executes every ceremony); never run broker-capable scripts; never edit
`config/agent_config.yaml`, `config/profiles/redacted_account.yaml`, or any R2-bound path. Session
CD is running in parallel on the same machine — keep your compute light (gate walks and
builds, no replay arms).
