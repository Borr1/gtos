# Session CH — the two measurements that arm levers (wave 15, B2450–B2499)

**Codex session #2.** The conventions in `SESSION_CG_LANE_FOOTPRINT.md` §"Conventions that
bind you" apply verbatim (blocks in IMPLEMENTATION_STATE.md, tool-emitted `gtos-ab-receipt-v1`
A/B fence, findings-first result doc with an honest what-I-got-wrong, commit-as-you-go, the
never-touch list). Read first: `phase15/CE_PROMOTION_HISTORICAL_FIRST.md` (work order 1 —
the milestones and their declared looks), `phase15/CE_ENTRY_HOUR_ACTIVATION_DOSSIER.md` +
`phase15/OD_HISTORICAL_FIRST_SCALING.md` (work order 2 — why the fetch decides the lever),
`phase13/SESSION_BB_SLEEVE_SUPPLY_RESULT.md` §quiet-book (work order 3),
`WAVE_11_WORKING_AGREEMENT.md` §1/§6 (the ratified rule and the training lane bind every
admission-grade sentence). Owner authority: OD-HISTORICAL-FIRST.

**The objective in one sentence:** run the two measurements CE priced — the historical
promotion milestones for `mx_btcusd` and the JPY hour-00 gate on the fresh M15 — and deliver
arming-grade answers instead of scheduled ones.

## Work orders

**CH-1 — M1 and M2 of P1-HIST, exactly as declared.** CE declared the looks
(`CE_MX_PROMOTION_AMENDMENT_V1.json`); do not re-declare, do not widen. M1: cross-broker
replication of the `mx_btcusd@target_5R` admission basis on the redacted_account BTCUSD archive.
M2: cross-instrument mechanism replication on `mx_ethusd` / `mx_avausd` / `mx_nzdjpy` at the
ratified rule (RECORDED, band alongside, chronological folds, maxbars share). **CE's own
caveat binds you: if `mx_avausd` is NOT_EVALUABLE, record the milestone UNREACHABLE and use
the stricter form — never quietly the looser one.** Deliverable: the milestone table with
pass/fail per declared threshold, and the honest false-promotion arithmetic of whatever
P1-HIST state results. If milestones pass, the weight-step request is written as an OWNER
page — you arm and re-weight nothing.

**CH-2 — The JPY hour-00 gate, on data that now exists.** The orchestrator fetched the five
JPY crosses at M15 back to 2022
(`/Users/borr/GTOSActive/repo/data/mt5_research_exports/bridge_ftmo_jpy_m15_20260731/`,
sanctioned sidecars, hash-verified; CE had SEVEN measurable trades, you have four years).
Gate `sub_mid_dn_revert`'s five JPY-cross members WITH and WITHOUT the ratified ONE-hour-off-
rollover entry (the implemented `--entry-hour` semantics — read `entry_hour.py`, don't
re-derive the convention), at the ratified rule, with AW-class controls (random-shift null on
the entry hour; the cheapest-drop analogue). The question is CE's: does the gross half
survive the cost saving at the members' real geometry? Deliverable: the per-member table and
an ARM / DO-NOT-ARM recommendation for `--entry-hour` on `sub_mid_dn_revert`, recent-fold
planning numbers, plus the metals-at-reopen divergence CE filed stated as in-scope-or-not.

**CH-3 — Quiet-book silence thresholds, per account (small).** BB's alarm was calibrated on
the pre-mx armed set. Re-run `bb_fill_truth.py`'s cadence derivation per CURRENT account set
(FTMO five sleeves, FN four) and emit the two thresholds as a receipt the monitor cites. CA's
handoff 3 and BB's handoff 4 both point here; close both with one artifact.

## Done means

Result doc `phase15/SESSION_CH_LEVER_MEASUREMENTS_RESULT.md` findings-first + receipts under
`phase15/receipts/`, blocks B2450–B2499, looks stay within CE's declarations (any new look =
declare BEFORE gating, family ratchet discipline), scoped A/B vs the ZERO baseline (12,476)
with the tool-emitted fence, honest what-I-got-wrong, handoff list. Never touch the VPS;
never run broker-capable scripts; never edit `config/agent_config.yaml`,
`config/profiles/redacted_account.yaml`, or any R2-bound path; March 2026 outcome-unread on every
path. Sessions CD/CG run in parallel — keep heavy runs ≤2 concurrent.
