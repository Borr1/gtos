# Wave 10 — working agreement

**Orchestrated by Fable 5, cut 2026-07-30 from the wave-9 evidence.** The standing correction and
Borhen's three instructions are unchanged and binding: sleeves are repaired, never killed; use
your own judgment — the commission is a brief; build-improve-fix, never refute-and-stop; the
Workflow opt-in is standing, and the wave-8/9 standard is an adversarial pass over your own
load-bearing claims before publishing (waves 8–9 caught 40+ real errors that way).

Wave 9's map: **the estate has its first ADMIT at the sealed α** (`mx_btcusd @ target_5R`,
RECORDED@mid, p 0.0011, q 0.0385 at m=35, also at `A_strict`) — conditional on a population rule
nobody ever chose deliberately (the model's own `decidable` field REJECTS the same cell) and on
the band (rejects at `band_high`). `sub_mid_dn_revert` re-clocked is 10× closer (p 0.0198,
retention sign flipped). The entry lever is ONE hour, not four. Regime conditioning is the named
next lever for both `sub_xvol_pullback` (target_4R banked, 2.8× short) and `asia_pdl_fade`
(exit surface exhausted, 23× short). Read `phase9/SESSION_AL_ADMISSION_CLOSERS_RESULT.md` §0/§3.4
and `phase9/SESSION_AM_CLOCK_AND_ENTRY_FRONTIER_RESULT.md` §0 before your own prompt.

## 1. Authority and the live account

Unchanged from wave 9 §1 — read it. FTMO live on three sleeves (`include_clean3: true` on the
host is now **[VERIFIED]**, `phase8/receipts/VPS_STEP_ZERO_VERIFIED.md`). Never run a
broker-capable script; never edit `config/agent_config.yaml`; the VPS is reachable directly by
the ORCHESTRATOR ONLY (owner-granted, 2026-07-30) — sessions still never touch it. H1 membership
check before editing under `src/`.

## 2–4. Verification, ledger, machine discipline

Unchanged from wave 9 §2–§4, plus wave-9 lessons now binding:
- **R0 extends to columns**: `q` is comparable only within one artifact (BH is a step-up over the
  submitted vector). Never quote a `q` across artifacts; `p_raw` travels, `q` does not.
- **Stamp the cost band on every arm** — an unstamped arm is the R0 violation AL §8.8 documents.
- A silent `null` in an adversarial artifact is worse than a failed attack — fall back loudly.
- An A/B by reverting uncommitted files: restore by copy-back, never `git checkout`.
- The population rule is undecided this wave: publish anything population-sensitive on
  **both** `era_class == RECORDED` and `decidable` until AN's package lands and Borhen ratifies.

## 5. Block allocation

Wave 9: AL `B1150–B1199` · AM `B1200–B1241` — landed.
**Wave 10: AN `B1250–B1299` · AO `B1300–B1349` · AP `B1350–B1399`** (AP commissioned
2026-07-30 under Borhen's blanket ratification of the decision queue —
`phase10/SESSION_AP_DECISION_IMPLEMENTATION.md`).
Need more? Next free 50 above `B1400`. Whoever raises the ceiling past a sibling owns
`IN_FLIGHT_WAVE_RANGES`.

## 6–7. Done means / reporting

Unchanged from wave 9 §6–§7.
