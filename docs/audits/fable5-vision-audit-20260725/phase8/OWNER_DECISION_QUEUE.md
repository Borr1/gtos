# Owner decision queue — Session AI, wave 8, 2026-07-30

> **DIRECTED 2026-08-01 (Borhen): OD-BROAD-FORENSIC — kill authority reclaimed, full forensic
> mandated.** The owner rejects the "dead for good / no more hours" framing of the broad-V4
> family: that authority was never delegated, and the orchestrator withdraws the framing. The
> measured record stands as measurement only (virgin February: 0/20 positive days under the
> committed protocol → the S1R1 continuation branch not justified). Mandate, verbatim in
> `../phase19/SESSION_FA_BROAD_FORENSIC.md`: a dedicated FABLE session with Fable subagents
> (Workflow tool; not Codex, not Opus) walks the decision cycle in the replays from the top
> level to the last trade/reject, with context, and attributes exactly — wrong selection vs
> wrong sleeve vs inaccurate sleeve vs inaccurate conditions — why the system buys losers
> while its own pool contains winners (the inverted-breaker +11.9 R/trade and the NY-metals
> cell are existence proofs). Deliverable: the complete defect map + ranked repairs to the
> sealed gate. Session FA launched 2026-08-01; February re-decode authorized for attribution
> only under this mandate.

> **EXECUTED 2026-08-01 under OD-ALL-IN (Borhen: "solve the puzzle and proceed all the way
> with my explicit approval"): the CJ + wave-17 train is merged and the three real host
> changes are scheduled for the 2026-08-02 00:00 UTC boundary ceremony** — CM's FTMO
> `crypto@stop_1p5x_target_scale`, CN's broker-true commission (both books, fail-closed),
> CO's lane vector (FTMO crypto ×1.15, mx ×1.15; FN neutral). **The puzzle's honest answer,
> priced by CL: the direct sleeve-widening set is EMPTY** — every candidate addition makes
> firm-true P2 `p_pass` worse (both JPY −0.1121 on FTMO, `metals_core` ×0.50 −0.1198),
> so OD-ALL-IN's "everything now" lands as: arm every measured improvement on already-armed
> sleeves (done at the boundary), run the two incubation candidates through exact-contract
> parity (CL's named next build), and point the factory (CP/CQ, launched) at the true-UTC
> estate where new families can actually come from. Nothing measured-negative was armed;
> that is the same arithmetic that protects the pass the directive is trying to reach.

> **DIRECTED 2026-08-01 (Borhen): OD-ALL-IN — the direct-to-live directive.** Verbatim and
> binding interpretation at `../phase17/OD_ALL_IN_20260801.md`. In one line: no new accounts;
> the pass surface widens on the two challenge accounts with more sleeves, more ideas, and
> live testing via the incubation lane; **anything the factory approves is armed on both
> accounts directly by the orchestrator's ceremony — this queue stops being a waiting room
> for factory outputs.** The sealed gate keeps defining "approved"; per-account measured
> verdicts still bind; the dial/allocation/strategic calls remain Borhen's. Same-day
> execution: token re-mint BOTH accounts (valid to 2026-08-14, config digests unchanged
> `ffe16657feaf…`/`e184a81d3b1b…`, minted flat 0/0); wave 17 commissioned — CL pass-surface,
> CM armed-money exit fidelity, CN live commission truth (H-CD-2's "priced for owner" state
> is RESOLVED by this directive + the standing seal break: wire it), CO learning-lane live
> activation. CP/CQ queued behind CJ.

> **AUTHORIZED 2026-07-31 by owner word (Borhen: "yes you can break the seal and proceed as
> proposed"): THE SEAL BREAK (H-CD-1)** — re-materialize replay sources/day packs under the
> repaired exporter at true UTC (Session CJ, wave 16). Consequence, priced and accepted: the
> parked B7.5 campaign's sealed-window resume option (~36 MH, JANUARY_BANK §7) is DEAD the
> moment re-materialized January exists — AW (no directional edge, model ceiling below
> break-even) and CD (negative on 21/21 days under repairs; deficit pre-cost) had already
> reduced its value to zero. The frozen engine's sealed artifacts remain untouched history.

> **RESOLVED 2026-07-31 by owner word (Borhen): OD-BA-0/1** — no support ticket needed; the
> redacted_account weekend rule does not reach 24/7 instruments, so the weekend policy is
> pre-selected as (a′) `--weekend-flat` with crypto exempt, to arm at the redacted_account pass
> (FTMO never). Recorded here; BA/CE's binding analysis otherwise unchanged.
> **ARMED 2026-07-31 ~11:01Z: the spread-geometry floor** on `sub_mid_dn_revert` +
> `sub_xvol_pullback`, both accounts — receipt
> `../phase15/receipts/SPREAD_FLOOR_ARMED_20260731.md`, host `267cccc94`.

> **DIRECTED 2026-07-31 (Borhen): OD-HISTORICAL-FIRST SCALING** — the live stream is a veto,
> never the clock; missing data is a fetch, never a verdict; built-but-unarmed measured
> improvements are a defect state. Record: `../phase15/OD_HISTORICAL_FIRST_SCALING.md`.
> Implementation: Session CE (B2300–B2349) + the orchestrator's vp M1 backfill fetch.

> **RATIFIED 2026-07-31 (Borhen: "i give explicit approval, please proceed as proposed with
> the training lane"): THE TRAINING LANE** — three-surface protocol (TRAIN unbilled / VAL
> used-once / TEST inviolate), one family bill per candidate at graduation, incubation ≤5
> sleeves at ≤0.05-class weights with pre-registered stop+promotion rules, sealed admission
> rule frozen, compute box approved as an owner-purchased option. Record:
> `../phase14/TRAINING_LANE_RATIFICATION.md`. Implementation: wave 14 (CA/CB/CC, CD reserved).

> **THE QUEUE IS CLEARED. Borhen ratified every item's own written recommendation on 2026-07-30
> ("yes proceed with all") and Session AP implemented them (blocks B1350–B1373,
> `../phase10/SESSION_AP_DECISION_IMPLEMENTATION_RESULT.md`). Read this block before any item
> below; the items are retained because they are the evidence the decisions rest on, not because
> anything in them is still open.**
>
> | # | decision as RECORDED | receipt |
> |---|---|---|
> | **1** | RATIFIED — `CANDIDATE_BOOK_V1` @ `all_declared`, sealed `B_balanced` α = 0.10. Residual executed by wave 9 (AL). | `../phase9/receipts/CANDIDATE_FAMILY_V2.json` → `ratified_rule` |
> | **2** | blocker RESOLVED (redacted_account max-DD is **static** 10 % of initial). Arming is the orchestrator's ceremony. | `receipts/FN_MAXDD_KIND.md` |
> | **3** | **NOT ADDED — on the GATES, not the carry.** Routed to the entry side: `fx_jpy` → §4.8 meta-label filter, `sub_mid_dn_revert` → regime conditioning on the RE-CLOCKED stream. | `../phase10/receipts/OWNER_DECISION_RECORD_AP.json`, `REPAIR_QUEUE_AP.json` |
> | **4** | **option C executed** — V2 beside V1, V1 asserted unmoved, with `book_days` and tier-identity controls. | `../phase10/receipts/ap_recost_v2.py`, `research/operations/w7_recost_2026_07_30/` |
> | **5** | **registry weight stays 0.025.** Admission is eligibility, not allocation. The 3.81 % `EXCEEDS_THE_DIAL` vol-matched branch explicitly NOT adopted. No code change. | `../phase10/receipts/OWNER_DECISION_RECORD_AP.json` |
> | **6** | **PACKAGE built, not an activation.** Blocking condition since resolved (population rule ratified as RECORDED); condition 3 binds its sizing to +0.198 R/day. Account choice open. | `../phase10/receipts/CHALLENGE_ACCOUNT_PACKAGE_V1.json`, `../phase10/CHALLENGE_ACCOUNT_CANARY_PAGE.md` |
> | **7** | **flag stays OFF — and the premise is CONFIRMED**, 20 of 45 stale candidates dropped a real closed bar (11 unconfounded). The shape is the H4 index cohort at a **daily** session close, not the weekend. Existence measured; the economics are still Borhen's. | `scripts/pre_gap_bar_premise.py`, `../phase10/receipts/AP_PRE_GAP_PREMISE_V1.json` |
> | **8** | **SIGNED and SPENT.** Coverage 85.5 % → 100 %, failing gates 2 → 1, and **nine trades cannot move a p-value** — 342 cells, zero ADMIT. Residual is sample, not cost. | `../phase10/receipts/ENERGY_CLASS_PEER_TRANSFER_V1.json`, `AP_ENERGY_FAMILY_V2.json` |
>
> **AE's five and P's three are also cleared** (`../phase10/receipts/AP_LANE_ADOPTIONS_V1.json`):
> three of AE's confirmed as shipped, two implemented in the default-off lane (the `MIN_N`
> disagreement rule and the symmetric materiality band), and all three of P's accepted with the
> implementations handed off because each touches `book_owner.py` on the armed path.
>
> **Three corrections this file's readers need.** (a) OD-AI-8's blocker was **two** refusals, not
> one — `NATGAS_cash` has no `instruments` entry in the live FTMO profile, so `cost_r` refuses on a
> missing instrument *before* it reads the commission; the profile is R2-bound, so that half is a
> live-**tradeability** handoff. (b) Any **PARTIAL UNIVERSE** stamp in a pre-2026-07-30 artifact
> names a cause it did not measure — `gate.py` asserted "no measured spread" for every refusal;
> read `unpriced_reasons` instead. (c) OD-AI-3's `+36 %/month for 2.6 points` trade-off remains
> **available** — the decision was "not yet", not "no", and it revisits when an entry repair moves
> one of those gates.

> **Decided or resolved since this file was written (updated 2026-07-30 at ceremony close):**
>
> - **OD-AI-1 is RATIFIED**: family `CANDIDATE_BOOK_V1`, **all-declared** basis, sealed
>   `B_balanced` **α = 0.10** (Borhen, 2026-07-30, recorded with provenance in
>   `receipts/CANDIDATE_FAMILY_V1.json` → `ratified_rule`, pinned by
>   `tests/research_infra/test_candidate_family.py`). α = 0.20 is research triage only.
> - **The step-zero preamble below is RESOLVED**: `include_clean3: true` read directly on the
>   host (`receipts/VPS_STEP_ZERO_VERIFIED.md`) — the armed book generates all three sleeves,
>   and the published figures price the book that is running.
> - **The VPS ceremony is EXECUTED and committed** (`receipts/VPS_CEREMONY_COMPLETED.md`,
>   host commit `118071eaa`): all 9 carry files at after-bytes, verifier PASS, monitor daemon
>   on carried code, arming state committed so no checkout can disarm. OD-AI-7's flag remains
>   OFF — that decision is still open.
> - **The FX D1 entry convention is RATIFIED as ONE hour off the rollover**
>   (`../phase9/OWNER_DECISION_ENTRY_HOUR.md`) — supersedes AH's 4 h; implementation rides the
>   next re-derivation wave. Not one of the eight below, recorded here so this file stays the
>   index.
> - **OD-AI-2's blocking unknown is RESOLVED** (`receipts/FN_MAXDD_KIND.md`): redacted_account
>   Stellar 2-Step's max loss is **10 % of INITIAL balance — static**, per the firm's own help
>   center, independently matched by the live monitor's headroom arithmetic. The trailing-
>   inversion caveat is dead; the published ranking stands (FN P2 0.9331 ≥ FTMO 0.9172). The
>   arm/don't-arm decision itself is still yours.
> - **The population rule is RATIFIED: `RECORDED`, with AN's two conditions** (Borhen 2026-07-30,
>   blanket "as proposed and recommended"; recorded in `../phase10/receipts/POPULATION_RULE_V1.json`
>   → `ratified_rule`, pinned by `tests/research_infra/test_population_rule_ratified.py`). The
>   first admission therefore STANDS as **"`mx_btcusd @ target_5R` admits at two of three cost
>   bands"** — never a bare ADMIT — and any use of it is **sized on the recent folds**
>   (+0.198 R/day, 13.2 % of the early folds), because the admission decays chronologically and
>   no gate can see decay. AM's band-widened era table is adopted (0 verdicts move).
> - **AO killed the would-be pair admission** (`sub_xvol_pullback @ 4R | ac60 ≥ median`): post-hoc
>   cut, fails Bonferroni over the cuts tried, and the permutation test's own resolution floor at
>   n = 44 sits ABOVE the BH rank-1 bar — the test cannot admit there. Its real finding moved to
>   the sizing lane: `vr` is monotone in net R inside the sleeve's firing range (tertiles
>   0.512 → 1.111 → 2.124 R/trade, perm p 0.00025).
>
> - **OD-AI-2 is EXECUTED** — redacted_account armed 2026-07-30 on the three-sleeve set
>   (`receipts/FN_ARMING_20260730.md`).
> - **OD-AI-3 is EXECUTED — on the LIVE accounts** (Borhen 2026-07-30, twice: "yes yes proceed
>   with those i give explicit approval", then the account-model clarification "i accept
>   activating the sleeves there" — there is no separate challenge track; the two live accounts
>   are the proving ground and the inactive FTMO is the reserve). Both books run FIVE sleeves
>   since ~11:52Z: receipt `receipts/FIVE_SLEEVE_EXPANSION_20260730.md`, host `f855250cd`.
>   Priced at measured p99: P2 0.9172→0.8915 for 4.501→6.120 %/mo. Neither added sleeve passes
>   an admission standard — armed on explicit owner risk acceptance, on the record.
> - **OD-AI-6 is REFRAMED by the same clarification**: the candidate book (`mx_btcusd @
>   target_5R`) activates on the LIVE accounts — but only when AQ's time-stop repair makes the
>   admitted contract runnable (arming today would trade the −0.141 R/day truncated contract
>   the evidence rejects). Session AS (`../phase11/SESSION_AS_CHALLENGE_BOOKS.md`, rewritten)
>   builds the activation dossier; the RESERVE account's gate is in AS-3's fleet map.
> - **The full edge program is ORDERED** ("for the edge puzzle to be fully solved too with as
>   many replays as possible") — AS-3 is the fleet map; the orchestrator schedules waves from it.
>
> **Still open below: OD-AI-4/5/7/8's remaining halves as recorded above** (plus AE's five in
> `phase7/receipts/AE_OWNER_DECISIONS.json` and P's three in `phase3/PACKET_EMITTER_CARRY.md` §7 —
> Session AP is adopting those per their own recommendations).

**Eight decisions, each with the evidence, the options priced, and a recommendation.** Designed
to be cleared in one sitting. Nothing here was decided by a session and nothing here has been
actuated.

AE's five open items are **not restated** — they are in
`phase7/receipts/AE_OWNER_DECISIONS.json` with their own numbers (family scope, the two
false-alarm budgets, `LIVE_UP_STEP`, the `MIN_N` rule, `idxrev`'s materiality band). Session P's
three packet decisions are in `phase3/PACKET_EMITTER_CARRY.md` §7. This file adds what wave 8
made decidable.

> **BEFORE ANY OF THIS — one read on the host, and it is not a decision.** The record disagrees
> with itself about `ultimate_book_include_clean3`: `CLAUDE.md` §4 says it is `true` on the VPS
> (set at the 14:25 arming, which is what admits `sub_xvol_pullback`), while mainline and the
> 2026-07-25 export both read `false`. **At `false`, `--tags crypto,energy_agri,sub_xvol_pullback`
> resolves to TWO sleeves** — measured — and every %/mo and `p_pass` figure below for the armed
> three describes a book that is not running. No session has touched the host, so it is
> `[UNVERIFIED]`. It is step zero of `VPS_CEREMONY_PACKAGE_2.md` and one command.

**The one thing to read first, because it reframes everything below.** The armed three earn
**4.501 %/month** on the window that selected them and **0.313 %/month** on the whole archive —
14.4× apart, same arithmetic, same sizing (B1079). `CLAUDE.md` already says to treat the small
number as the expected case; wave 8 confirms it from a second artifact by a second route. Every
"%/mo" in this document is the cache-window figure unless it says otherwise, so read each of
them as an upper bound.

---

## OD-AI-1 — What family is a candidate-book admission corrected against, and at what α?

**This is the decision the whole session was built to hand you, and it is now bounded.** It was
an open-ended honour-system number; it is now a choice between three enumerated lists and three
α values, with the answer measured at every combination.

**Evidence.** `phase8/receipts/CANDIDATE_FAMILY_V1.json` (the declaration),
`AI_FAMILY_SENSITIVITY_V1.json` (every combination), `AI_GATE_AT_DECLARED_FAMILY_V1.json` (the
real gate re-run, AA's published seal reproducing exactly).

Three things you should know before choosing:

1. **The candidate book is 32, and that is not a judgement call.** It is what the live config's
   own resolvers build (`active_specs` with the include-flags and market-expansion policy from
   `agent_config.yaml:1270-1284`). AF §11 guessed "~12–30"; measured, it is 32. It is
   set-identical to the 32 AA walked — **by construction, because AA's generator calls the same
   resolver**, so read that as "nobody had declared it", not as two derivations agreeing.

   **Two warnings that come with it.** `active_specs` takes no `include_clean3`, so the live
   `effective_registry` is a **different 29** — 32 minus `sub_mid_dn_revert`,
   `sub_xvol_pullback`, `vp_euidx_pocgrav` — and `sub_xvol_pullback`, one of the two sleeves
   admitted here, is *outside* it in this repository's config. And the three zero-trade
   exclusions are **repairable data gaps**: fetch the GER40/UK100 M1 feed or the two missing
   instruments' bars and the family returns to 32. Closing a data gap is a good thing that
   costs an admission, and the ratchet permits the rise by design.
2. **AA's and AD's 69 double-counts.** 69 = 32 judged + 12 W looks + 25 X looks, and W's and X's
   sleeves are both **subsets** of the same 32. The union of all three sessions' distinct
   hypotheses is 32. BH and Bonferroni correct for hypotheses, not for re-measurements; the
   ledger's *look events* are the right input to the DSR deflation and the wrong one here. Their
   published q-values are over-corrected, and over-correcting **rejects real edges**.
3. **Three of the 32 tested no hypothesis at all** (`mx_eu50_cash_*`, `mx_fra40_cash_*`,
   `vp_euidx_pocgrav`: zero trades, `NOT_EVALUABLE`, `GENERATION`). Excluding them is
   outcome-independent — it is a property of the data and the wiring, decided before any return
   was seen, the same class of restriction as `coverage_policy="restrict_to_priced"`. Both counts
   are published and the narrower one is never the default.

**Options, priced.** What ADMITS on significance, from the real gate on a **consistently
restricted population** (`era_class == RECORDED`, outcome-independent, mid band):

| family | m | α = 0.05 | α = 0.10 | α = 0.20 |
|---|---:|---|---|---|
| `CANDIDATE_BOOK_V1` looks-taken | 29 | — | — | **`mx_btcusd` + `sub_xvol_pullback`** |
| `CANDIDATE_BOOK_V1` all-declared | 32 | — | — | both |
| `MECHANISM_CROSS_V1` (AF's grid) | 276 | — | — | — |
| `ESTATE_UNION_V1` | 298 | — | — | — |
| AA/AD's historical 69 | 69 | — | — | — |
| trial ledger, all looks | 4,785 ¹ | — | — | — |

¹ The ledger stood at 4,785 when this was computed and is 6,692 after this session's own rows —
deliberately the *pre-session* count, because correcting a look against a bill that already
includes it is circular. Nothing admits at either number. (And 1,907 of those AI rows record 851
distinct looks; the duplication is an append-only artefact this session caused and declares in its
result doc §9, not extra work.)

**The α = 0.10 column is empty, and an earlier draft of this table said otherwise. That draft
was wrong and the correction matters.** It mixed two populations inside one Benjamini-Hochberg
vector — AF's RECORDED-era p for `mx_btcusd` beside AA's all-eras p for `sub_xvol_pullback` —
which is what this session's own dossier rule R0 forbids. Run consistently, the two p-values
**swap** (`mx_btcusd` 0.011999 → 0.006399, `sub_xvol_pullback` 0.006099 → 0.011999), the pair's
q is unchanged to five decimals at 0.174, and the verdict does not move.

**So the family declaration is NECESSARY and NOT SUFFICIENT, and that is the honest state.** It
takes q from **0.414 to 0.174** — a 2.4× improvement that is real, permanent and free — but the
flip to ADMIT also needs **α = 0.20**, and:

> **α = 0.20 is `options.C_exploratory`'s alpha, and its own sealed note says
> *"RESEARCH TRIAGE ONLY. A pass here means 'worth spending more data on', never 'worth arming'.
> Do not let a C-pass reach a book."***

So there is no version of this where a family choice alone puts a sleeve in a book. **I am not
recommending α = 0.20.**

**Three more things measured, all of which belong in this decision.**

- **The pair admits or fails together.** Benjamini-Hochberg is a step-up procedure and each
  lifts the other's threshold. Splitting them makes both strictly worse.
- **`sub_xvol_pullback` — already armed with your money — has ZERO integer slack on two
  non-significance gates.** `n_folds_evaluable` 3 against a floor of 3 (lose one fold and it is
  NOT_EVALUABLE before significance is ever read), and `3 of 4` positive folds against a
  `ceil(0.60 × 4) = 3` requirement (one fold sign-flip and it REJECTs). Its whole significance
  test rests on 31 OOS days and 71 scored trades.
- **The binding constraint is `sub_xvol_pullback`, not `mx_btcusd`.** At m = 29, BH rank 2 needs
  p ≤ 0.006897 and `mx_btcusd` is **already inside it** at 0.006399. The pair is blocked because
  `sub_xvol_pullback` at 0.011999 cannot hold rank 1 (needs ≤ 0.003448).

**Recommendation: ratify `CANDIDATE_BOOK_V1` on the `looks_taken` basis as the standing family
rule, hold α at the sealed 0.10, and spend the next session on the two banked prescriptions that
close the residual.**

Why ratify anyway, given that nothing admits today: the rule is worth having *because* it is not
outcome-dependent. Ratifying it now — before the two repairs run — is the only way it can ever be
prospective for the look that follows. Ratifying it *after* seeing which α admits is the defect
this artifact exists to close. The number is 32 (29 looks taken), it is the code's own answer to
"what could be armed", and it needs no stopping rule because the config supplies one.

**And the residual is two measurements, both already banked, neither needing new data:**

1. **Carry AD's `target_5R` onto the RECORDED-era population.** +0.309 R/day and p_raw 0.0101 on
   the as-walked eras; **never measured on RECORDED.** `mx_btcusd`'s p improved 0.011999 →
   0.006399 under the era restriction alone, so the exit repair is a second, independent
   improvement on the same trades.
2. **`sub_xvol_pullback`'s `vr ≥ 1.4` variant takes n from 88 → 420** and stays positive out of
   window. It is the sleeve actually blocking the pair, and 420 trades is a different p-value.

Either could move the pair inside α = 0.10 at the declared family. **That is the whole remaining
distance between this estate and its first new-edge admission at an arming-grade standard**, and
it is a session's work rather than a data ceremony.

---

## OD-AI-2 — Arm redacted_account on the same three sleeves?

**Evidence.** `phase8/receipts/BOOKS_MC_V1.json`, both accounts at their own measured rules,
60,000 paths, fwd 2025+, worst carry, live sizing:

| book | account | P2 `p_pass` | P2 cal-days | %/mo |
|---|---|---:|---:|---:|
| the armed three | FTMO | 0.9172 | 60 | 4.501 |
| **the same three** | **redacted_account** | **0.9331** | 51 | 4.848 |
| redacted_account's published 4-sleeve survivor set | redacted_account | 0.9097 | 53 | 4.833 |

**redacted_account scores higher than FTMO on the same book.** The 1.59 pp gap is real and ~10 standard
errors, so it is not noise. `FOURTH_REVIEW.md` §7.2 called this "the cheapest real diversification
available to the programme" and said it needed no new measurement.

**But it is NOT the 8 %-vs-10 % target, and an earlier draft of this page said it was.** Holding
the series fixed and swapping only the target moves `p_pass` +0.0012 on FTMO's series (1.0 SE) and
+0.0003 on redacted_account's (0.3 SE) — **1.8 %–7.3 % of the gap.** Holding the target at FTMO's 10/5
and swapping only the SERIES accounts for **0.0157 of the 0.0159.** The driver is redacted_account's
cheaper worst-case carry on an identical 117-day trade set: carry drag 0.1247 R/book-day against
FTMO's 0.1585, worst day −1.278 against −1.428 unit R. The target buys **time**, not pass
probability — median 17 book-days to pass against 20.

The direction check settles it: at **zero nights FTMO is ahead** (P2 0.9930 vs 0.9927) and at one
night it is a tie, despite redacted_account holding the easier target in every cell. redacted_account wins
only in the worst-carry cell.

> **THE ONE MEASUREMENT TO TAKE BEFORE ARMING, and it is not another Monte Carlo.**
> redacted_account's `max_overall_loss_pct` is `TRANSFERRED` with **no `kind` field** — inherited from
> the assertion that the two firms' 100k limits are identical (Session Q §3 flagged exactly this).
> Modelling it as FTMO's static $90,000 floor is therefore an assumption. **On the trailing basis
> redacted_account is 0.8948 — 2.24 pp BELOW FTMO's MEASURED 0.9172. The ranking inverts, and that
> sensitivity is 19× the entire target effect.** One captured page from redacted_account decides whether
> this account is better than FTMO or worse.

**One correction to that section.** Its survivor set is listed as four including
`vp_euidx_pocgrav` (UNCONDITIONAL there) — but that sleeve **generates nothing**: it fails closed
at `sleeves/vp_euidx.py:70` for want of a GER40/UK100 M1 aux feed and has 0 trades in AA's walk.
Read the 3-sleeve row. Its cache rows exist, so an MC can size it; the live book cannot produce
one of its trades.

**Two cautions carried forward, both measured by earlier sessions.**

- **Pin `derisk_mode: smooth` explicitly** in any dial discussion. `GovernorLimits` defaults to
  `"band"` and `smooth` is held on only by the ≥ 2 % interlock, which a dial move below 2 %
  releases (Session V).
- **redacted_account prohibits weekend holding on the FUNDED account** [`FIRM_RULES_V1.json`,
  MEASURED], and all these sleeves carry a 320-hour horizon that cannot avoid a weekend. **Every
  redacted_account number here describes the challenge phases only.** The config to make the funded
  account legal does not exist.

**Recommendation: yes — but establish the max-DD kind first, and it is one page rather than a
run.** It is a second funded
surface at a different broker's costs on a book you have already accepted, at better firm rules,
with no new measurement and no composition question. The package is AC-lane work: its own
`--tags`, its own token, its own kill flag, and the ceremony shape FTMO's already used. The
weekend-holding rule is a **post-pass** problem, so it does not block the challenge phases —
but it must be solved before a payout, and it is not solved today.

---

## OD-AI-3 — AD's restated sleeves: a real trade-off, and the choice is yours

**An earlier draft of this page told you to confirm NOT adding them, on a measurement that was
circular. That draft was wrong and this is the corrected version.**

AD restated `sub_mid_dn_revert` and `fx_jpy` to UNCONDITIONAL on **both** accounts once carry was
measured instead of assumed. My first composition test charged them at the **modelled max** carry —
and `recost_w7_validation.row_cost:873` charges `swap_r_per_night × min(nights, SLEEVE_MAX_NIGHTS)`
**without ever consulting `CARRY_STRUCTURAL`**, so that cell bills:

| sleeve | charged | AD's measured mean | overcharge |
|---|---:|---:|---:|
| `fx_jpy` | 3 nights | **0.0015** (`frac_zero` 0.999, n = 3,984) | **~2,000×** |
| `sub_mid_dn_revert` | 14 nights | 1.308 | 10.7× |
| the armed three | 14 each | 4.98 / 4.36 / 2.98 | 2.8×–4.7× |

**The cell overcharges exactly the two sleeves the restatement promotes, by 400× more than it
overcharges the book they are being compared against.** Testing a measured-hold restatement at the
modelled max carry tests it against the assumption it exists to replace.

Re-run at AD's own measured nights (60,000 paths, fwd 2025+, FTMO, live sizing):

| carry basis | P2 3 → 5 sleeves | %/mo 3 → 5 | P2 cal-days |
|---|---|---|---|
| AD measured **mean** | 0.9805 → 0.9671 (**−0.013**) | 5.859 → **7.897** (×1.35) | 51 → **43** (8 sooner) |
| AD measured **p99** | 0.9172 → 0.8915 (**−0.026**) | 4.501 → **6.120** (×1.36) | 60 → **48** |
| modelled max (the circular cell) | 0.9172 → 0.7548 (−0.162) | 4.501 → 4.373 (×0.97) | 60 → 53 |

redacted_account is the same shape: ×1.33 and 6 days sooner for −0.014 at measured mean; ×0.35 for
**−0.47** at modelled max.

**The `measured_p99` row is the one to read.** AD's p99 for the armed three *is* their max (14.0),
so that row holds the armed three at their own worst carry and charges only the two added sleeves
at their measured tail: **+36 % per calendar month and 12 days sooner, for 2.6 points of
`p_pass`.**

**So this is a risk-preference question, not a measurement.** `p_fail_daily` and `p_timeout` are
0.0 throughout, so the only failure mode is touching the drawdown floor — you are buying ~36 %
more per calendar month and about a fortnight of time with about 2.6 points of two-phase pass
probability.

**Two things that argue against taking it even so, and they are not small.**

- **`p_pass` falls on every basis.** There is no carry assumption under which adding them is free.
- **Neither sleeve has passed anything.** On the archive walk `fx_jpy` fails **all five** core
  gates and `sub_mid_dn_revert` fails **robustness and significance** (that walk is FTMO's —
  `GateSpec.account` — so one account's read). A sleeve whose carry no longer blocks it is a sleeve
  whose blocker has **moved**, not cleared. AD named where `fx_jpy`'s went: it needs a 1.89× gross
  multiple at a 2× stop and delivers 0.175×, so its next step is the §4.8 meta-label entry filter.

**Recommendation: do not add them yet — but for the right reason, which is the gates and not the
carry.** The tier restatement is sound and its composition value is positive at the carry it
measures; what is missing is that neither sleeve clears an admission standard on any population.
Route both to the entry side, and revisit this table when an entry repair moves one of those gates.
If you would rather have the ~36 % and the fortnight now, the number is above and the decision is
legitimately yours — I would take it on a challenge account before a funded one.

## OD-AI-4 — Re-seal `SURVIVOR_BOOK_V1.json` at the better cost coverage?

**Evidence.** `SURVIVOR_BOOK_V1.json` and `MC_FIRM_TRUE_V1.json` **no longer reproduce from their
own generators at HEAD**, and the cause is benign: `8f6da5150` ("price the six symbols that were
blocking the LIVE book's own scores") and `33d854189` ("close the metals tick gap") extended
`BROKER_TRUE_COSTS_V1.json` after Session Q sealed its figures — on **FTMO only**, which now
carries 167 priced instruments against redacted_account's 76.

| | before | after |
|---|---:|---:|
| `crypto` MEASURED rows | 35 of 104 | **72** |
| `idxrev` MEASURED rows | 4,939 | **5,876** |
| redacted_account published MC fields still exact | — | **48 of 48** |
| FTMO published MC field mismatches | — | **23** |
| `book_days` mismatches, all 36 cells | — | **0** |

`book_days` reproducing everywhere is the load-bearing control: a cost re-pricing cannot add or
remove a day a sleeve traded, so **the trade set is unchanged and only the pricing moved.** And
redacted_account reproducing exactly is what makes this a diagnosis rather than a guess.

**Options.**

| | consequence |
|---|---|
| **A. Re-seal** at the better coverage | every published `p_pass` in the OD-3 dossier and `CLAUDE.md` §4 moves, and ten documents citing them go stale in one commit. More MEASURED coverage is strictly better evidence (dossier R7). |
| **B. Leave sealed, publish the drift** (what wave 8 did) | the artifacts stay comparable to every figure you have seen; anyone re-deriving them must pass `--allow-cost-artifact-drift` and read why. |
| **C. Re-seal as a V2 beside V1** | both readable, nothing goes stale, and one more artifact to keep straight. |

**Recommendation: C, when someone next needs the recost route anyway — not as its own task.**
The drift is now diagnosed, tested and refused-by-default, which is most of the value. `crypto`
going from 34 %-measured to 69 %-measured matters, because `crypto` supplies 38.8 % of the book's
edge and dossier rule R7 says coverage is part of the number — but it changes no verdict today,
and re-sealing an owner-facing artifact to improve a footnote is how a document set loses its
audit trail.

**Already fixed, no decision needed:** `build_survivor_book.py` took no arguments and overwrote
the committed artifact unconditionally, so running it to *check* reproduction destroyed the thing
being checked. It did exactly that in this session and cost a `git checkout`. It now writes
nothing without `--out` or `--write-committed`.

---

## OD-AI-5 — What weight does an admitted candidate get?

**Evidence.** `mx_btcusd`'s `effective_registry` confidence is **0.025**
(`default_off_runtime_capable_zero_activation`) against `sub_xvol_pullback`'s 0.45 and `crypto`'s
0.85 — **34× smaller than the armed sleeve.** On the archive population, at the live 2 % dial:

| challenge book | weights | %/mo | P2 `p_pass` | P2 cal-days |
|---|---|---:|---:|---:|
| `mx_btcusd` + `sub_xvol_pullback` | registry (0.025 / 0.45) | **0.136** | 0.9971 | 2,415 |
| the same two | equal (1.0 / 1.0) | **0.848** | 0.5822 | 181 |
| the armed three, same population (control) | registry | 0.313 | 0.5660 | 495 |

**At the allocator's own convention, admitting `mx_btcusd` is economically inert** — 0.136 %/month
and 6.6 years to a two-phase pass. At equal weight it earns 6× more and the pass probability
collapses from 0.997 to 0.582, because the book is then genuinely risking 2 % per unit on two
sleeves.

**One number not to misread.** The vol-matched branch of this book reports **3.81 %** per
correlated unit — *above* the 2 % dial — because vol-matching scales UP a book quieter than the
reference. It is a correct output of the convention applied mechanically to a two-sleeve book and
it is **not** a sizing proposal. Flagged as `EXCEEDS_THE_DIAL` on the row.

**Recommendation: leave the registry weight where it is, and treat admission as eligibility
rather than allocation.** A seed weight of 0.025 is what "default-off, runtime-capable, zero
activation" is supposed to mean, and it is the right first size for a sleeve with no live record
at all. If you want it to matter, the lever is a **challenge account** (OD-AI-6), not a
re-weighting inside the funded book — a challenge account is where a candidate's forward record
accrues while capital is being won rather than while parked, and the downside is bounded by the
challenge fee.

---

## OD-AI-6 — Commission a challenge account for the candidate book?

**Evidence.** `FOURTH_REVIEW.md` §7.3 names this as where the candidate book ships, and you have
three challenge accounts ready (`OWNER_SESSION_CONTEXT.md:110`). What wave 8 adds is the
membership and its price: the book is **two sleeves**, one of which is already armed on FTMO, and
at the allocator's convention it earns 0.136 %/month.

**Honest reading of what this buys and what it does not.**

- It buys a **forward record on real broker truth** for the estate's best new-edge candidate,
  which is the one thing no amount of archive work can produce. `mx_btcusd` has **0 live fills**;
  so does every armed sleeve.
- It does **not** buy a diversified cluster. AF refuted that: the same rule across the nine-symbol
  crypto class scores **−0.1849** with a dispersion ratio of 4.74 and a 1.80 R best-to-worst
  spread. Do not price a crypto cluster cap as diversification.
- It does **not** escape the concentration doubt. On the books that could be armed,
  `crypto + sub_xvol_pullback` is **48.9 %–74.2 %** of the edge (Session Q §10.2), and
  `sub_xvol_pullback` is in this book too.
- The `mx_*` D1 family's **live time stop truncates 72–90 % of its trades** (`time_stop_bars` is
  M15 printed bars for every sleeve), so an arming package for `mx_btcusd` **must state which exit
  it is armed on**. Its published economics are at a contract the live book does not run, and what
  it wants is a wider target (`target_5R`, +0.309 R/day) rather than its live 1-bar stop
  (−0.141 R/day — a DELTA against the published contract, not a level; mislabel corrected at the
  wave-11 train per AP §8, and AQ later measured the repaired-vs-live delta at +0.272 R/day on
  the ratified rule).

**Recommendation: yes, but sequence it after OD-AI-2 and OD-AI-1.** redacted_account is a better book
on better rules for no new work; a challenge account is a *new* book on a *new* surface, and it
should not start before the family rule that admits its members is ratified. When it does start,
the package needs: the ratified family and α in writing, the exit contract named, its own token,
its own `--tags`, its own canary page, and a stated stop condition.

---

## OD-AI-7 — Turn on `--recover-pre-gap-bar`?

**Not yet, and the wiring is now inert and ready.** AB measured that the last closed bar before
every session gap is unreachable — `bar_provider.candles_to_bars` drops it as "forming" and the
recency guard then refuses it as stale. It is **1.3–6.8 %** of five H4 sleeves' trades and
**4.26 %** of 134,027 D1 trades, because every Friday is a pre-gap bar.

It is now reachable from `run_book.py --recover-pre-gap-bar`, default OFF, gated exactly the way
`--tags` is — so **no `agent_config.yaml` byte moves and the activation token's digest is
untouched.** 8 tests, including that the recovered bar survives the recency guard, which a
fetch-only fix would not have.

| | direction |
|---|---|
| D1 (134,027 trades) | the unreachable population is **worse**: −0.0771 R against +0.0117 R. The defect is currently **saving** money on this family. |
| H4 | the sleeve it costs most is `sub_xvol_pullback` at 6.4 % — **armed**, and its only failing gate is significance at n = 88, so 6 more trades is not nothing. |
| the premise | `[UNVERIFIED]` on the live MT5 feed. Whether the terminal returns a forming candle at a session close is a property of the terminal and **cannot be checked from this machine.** |

**Recommendation: land the wiring (already inert), verify the live-feed premise as a by-product
of the packet carry, then decide.** Once `spread_r` and broker deal timestamps are on every
evaluated leg (C4 in `VPS_CEREMONY_PACKAGE_2.md`), an operator can read directly whether a Friday
close produced a candle the engine dropped. **That makes this decision downstream of C4** — which
is another reason to ship C4 first.

---

## OD-AI-8 — One deal row, or a signed peer transfer, for `NATGAS.cash`

**The cheapest item on this page, and it is the only route to a question about armed money.**

`energy_agri` is armed and its live `partial_be_runner` scale-out contract measures
**−0.308 R/day** against the plain exit. AD flagged it as thin and it is: n = 67 trades, 3
evaluable folds, p_raw 0.111.

**Wave 8 measured that forward live evidence cannot settle it.** `energy_agri` produced **67
archive trades in 26 years** and **0 live fills in 38 days**. The learning lane's own floor is 30
fills across 30 distinct days; at ~7 book-days/month that is not a schedule, it is decades.

**The route that can settle it is AF's data ask.** `energy_agri`'s FVG mechanism is three symbols
wide and pools at **+0.4571 R/day** — the strongest mean in AF's entire 30-family grid, dispersion
ratio 0.117, 3 of 3 members positive, band-stable across all four cost configurations. It fails
on **sample**, and one thing unblocks it: **a `NATGAS.cash` COMMISSION.** Its spread is already
measured; `cost_r` refuses on `commission.kind = "unknown"`, and redacted_account carries no gas
instrument to transfer from.

**Options: one redacted_account gas deal row, one FTMO gas deal row, or a stated energy-class peer
transfer someone is willing to sign.** It is **not** a tick capture and **not** a re-run — both
leave the commission UNKNOWN. `HEATOIL.c`, the sleeve's other dropped energy leg, is in the same
state.

**Recommendation: sign the energy-class peer transfer.** It is a sentence, it unblocks the
strongest mechanism in the grid, and it is the only thing on this page that improves the evidence
for a sleeve that is trading your money today. If you would rather not sign a transfer, one gas
deal row on either account does the same job with less judgement — but nothing does it for free.

---

## The table, for one sitting

| # | decision | recommendation | what it costs to say yes | what it buys |
|---|---|---|---|---|
| **1** | the family and α for candidate admission | ratify `CANDIDATE_BOOK_V1` @ `looks_taken`, **hold α at the sealed 0.10** | a written rule; no arming | q 0.414 → 0.174 permanently. Nothing admits YET — the residual is two banked prescriptions, not a data ask |
| **2** | arm redacted_account on the same three | **yes, after one page** — capture its max-DD `kind` | an AC-lane ceremony, its own token | a second funded surface, P2 0.9331 vs FTMO's 0.9172 — but the ranking **inverts** if the max-DD is trailing |
| **3** | AD's restated sleeves | **not yet — on the GATES, not the carry** | forgoes +36 %/mo and 6–12 days | at measured carry it costs 2.6 pts of 2-phase pass, not the 16 an earlier draft claimed. Neither sleeve clears an admission standard on any population |
| **4** | re-seal the survivor book | **C — later, beside V1** | one artifact more to track | keeps every figure you have seen comparable |
| **5** | weight for an admitted candidate | leave at registry 0.025 | admission ≠ allocation | 0.136 %/mo instead of 0.848 %/mo, and P2 0.997 instead of 0.582 |
| **6** | commission a challenge account | yes, **after 1 and 2** | a challenge fee | the only forward record `mx_btcusd` can ever get |
| **7** | `--recover-pre-gap-bar` | **not yet** — wiring landed, inert | nothing | 4.26 % of D1 trades, in the direction the evidence does not yet support |
| **8** | `NATGAS.cash` commission | **sign the peer transfer** | a sentence | the only route to the `energy_agri` question, on armed money |

**By reference, not restated:** AE's five (`AE_OWNER_DECISIONS.json`), Session P's three
(`PACKET_EMITTER_CARRY.md` §7), and the ceremony package's own sequencing
(`VPS_CEREMONY_PACKAGE_2.md` §6).
