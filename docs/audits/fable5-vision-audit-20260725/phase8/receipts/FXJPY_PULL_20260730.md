# fx_jpy pulled from both live books — 2026-07-30 ~14:57 UTC

**The armed set is FOUR sleeves: `crypto, energy_agri, sub_xvol_pullback, sub_mid_dn_revert`.**
Host commit `7017c6745`; backup + released flags at
`C:\Users\MSI\Documents\gtos-fxjpy-pull-backup-20260730T145605Z`.

## Why — the evidence, all three legs

1. **AV (wave 12) measured the sleeve NEGATIVE on its own extended, cost-true, LIVE-contract
   stream at every cost band**: 1,326 decisions over 2024-01-02…2026-07-24 on the matched FTMO
   M15 archive, gross +0.156 R/trade against 0.251 R of mid-band cost → net **−0.095**; at the
   ratified rule **−0.166 R/trade OOS** (862 scored-fold trades), **p 0.994**, **all five core
   gates failing**, 1 of 5 chronological folds positive with the two most recent at −0.079 and
   −0.336, and `in_sample.mean_is_r = −0.156` — in- and out-of-sample agree in sign, so no
   contamination story rescues it. AV also found the estate's fx_jpy stream **triple-counts
   every decision** (the generator's `_CATCHUP_GRACE = 2`), so every previously published
   per-trade figure for this sleeve averaged three same-signal entries.
2. **Its named repair path measured EMPTY**: the §4.8 meta-label filter — fx_jpy's routed
   repair since AD — scores OOS **AUC 0.5233** on 1,206 walk-forward trades, with a working
   positive control (>0.90 on a planted feature). Absent signal, not absent implementation.
3. **Its live record is a real loss**: −13.948 R net over 32 fills (AS §0.5 — not significant
   alone, but the sign agrees with both measurements above).

## Authority and the pattern

Armed this morning under the owner's explicit risk acceptance **with the caveat on record**
(the expansion receipt: "neither added sleeve passes an admission standard"). Pulled this
afternoon by the orchestrator on the day's new decisive-grade evidence, under the owner's
standing "proceed as proposed and recommended" and "no conservative passiveness" — holding a
measured-negative sleeve that fires ~0.84×/day live is leakage, not aggression. This is the
same evidence-pull pattern as `metals_core` at the 14:25 arming adjustment (owner's own
precedent, later corroborated by the learning lane). **Instantly reversible**: re-add the tag,
one ceremony.

## Ceremony discipline

Both accounts verified FLAT first (mandatory here beyond habit: AS's adopt-path
`KeyError('trigger_r')` fix is not yet carried to the host, so a restart must only happen with
zero open positions until it is). Flags held → backup + manifest → both tag strings edited
(guard: exactly 2 occurrences or abort) → task restart, books counted to zero → 4 workers
verified on the four-sleeve tags, `tfs=[16388]` re-derived (M15 left with fx_jpy, confirming
the spec-derivation a third time) → flags released. Config untouched; both tokens undisturbed.

## What stays

`fx_jpy` remains in the estate with its repair enumeration OPEN, per doctrine: the meta-label
route is refuted, the triplication is fixed in measurement, and the remaining named avenues
(entry-hour conditioning on its M15 session surface, regime conditioning on the re-clocked
stream) stay on the repair queue. Parked from LIVE ALLOCATION, not from research.

## And the mx_btcusd precondition cleared in the same session

The F15 `copy_rates` probe (read-only, both terminals): **BTCUSD M15 returns 7,800 bars on
FTMO and on redacted_account — above the 7,680 the repaired time stop needs.** AQ's inertness hazard
(§6a) is cleared on both terminals. `mx_btcusd`'s live activation now waits only on the
wiring carry to the host (AU's `--frontier-exits` + AS's adopt-path fix + AQ's
`execution_packets.py` repair, one composed carry ceremony).
