# redacted_account armed on the three-sleeve set — 2026-07-30 ~05:18 UTC

**OD-AI-2 executed.** Owner authority: Borhen, 2026-07-30 — "yes proceed with all" after the
orchestrator's report recommending yes, which itself followed the queue's recommendation
("yes — but establish the max-DD kind first"). The max-DD kind was established the same day:
**static 10 % of initial** (`FN_MAXDD_KIND.md`), so the FN ≥ FTMO ranking stands
(P2 `p_pass` 0.9331 vs 0.9172, armed three, fwd 2025+, worst carry, `BOOKS_MC_V1.json`).

Executed by the orchestrator over host-admin. Host commit **`eb7c28516`**
(after the ceremony commit `118071eaa`). Backups + the released flag at
`C:\Users\MSI\Documents\gtos-fn-arming-backup-20260730T051338Z\` with manifest.

## Sequence (kill flag HELD throughout until the final step)

1. Preconditions: FN kill flag present, FTMO's absent, both accounts flat.
2. Backup: `redacted_account.yaml`, `run_book_supervisor.ps1`, FN `firing_sleeves.json`.
3. `config/profiles/redacted_account.yaml:11-13`: three gates → `true` (line 11 comment records
   the arming; the file's SHADOW/parity header is 2026-07-02 doctrine, superseded).
4. `scripts/run_book_supervisor.ps1:141`: FN worker gains
   `tags="crypto,energy_agri,sub_xvol_pullback"` — same armed set as FTMO.
5. **FN token minted** (post-edit, so it binds the armed bytes): namespace
   `redacted_account_live_bee34003`, digest `e184a81d3b1b…`, expires **2026-08-06 05:14 UTC**,
   issued-by borhen. **FTMO token re-minted** to the same expiry with a corrected note
   (THREE-sleeve set); its digest came back **`ffe16657feaf` unchanged**, proving the FN
   edits touch nothing FTMO's binding reads.
6. **B365 mitigation**: FN namespace `firing_sleeves.json` deleted (backed up) — a mid-day
   arm must not inherit the shadow book's wider firing union (+25 % sizing hazard).
7. Supervisor task restarted (its worker args are parsed at ITS start — a ps1 edit reaches
   respawns only through a task restart): stop task → book pythons counted to zero → start.
8. Post-start verification, all four workers back:
   - both namespaces carry `--tags crypto,energy_agri,sub_xvol_pullback` on their live
     command lines;
   - FTMO declares `config_digest=ffe16657feaf`, `authority_gates_ON=True killed=False`;
   - FN declares `config_digest=e184a81d3b1b` (= its token), `authority_gates_ON=True
     killed=True` (flag still held — fail-closed exactly as designed);
   - FN `account identity verified` on 6 fields (company/currency/login_sha256/server/paths);
   - the armed FN worker runs **`tfs=[16388]`** — H4-only, the armed shape FTMO runs; the
     shadow worker's `[16388, 15, 16408]` is gone with the shadow config.
9. **Kill flag released** (moved to the backup dir, reversible). Heartbeat healthy on the new
   pid within a minute; monitor shows both accounts flat, equity right
   (FTMO $107,872 / FN $96,229).

## What redacted_account arming means, honestly

- Same three sleeves, second broker's costs, better firm rules — "the cheapest real
  diversification available to the programme" (FOURTH_REVIEW §7.2). Expected case is the
  archive number, not the window number (CLAUDE.md §4).
- FN's profile carries `ultimate_book_profit_target_pct: 0.08` (challenge step-1) and
  `risk.max_daily_loss_pct: 4.0` — the pre-existing, more conservative daily bound.
- **The weekend-holding rule is a FUNDED-account problem** (challenge phases are legal); it
  must be solved before a redacted_account payout and is not solved today (queue item 2's caution,
  still true).
- Disarm path, either account: **flatten first**, then revoke the token
  (`gtos_activation_token.py revoke`) or re-hold the kill flag. Never gate-first (H8).
