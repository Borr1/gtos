# Wave 21 full-flow truth closure — controlling prompt

Status: `IN_PROGRESS`

Owner direction: execute the proposed work all the way, use parallel Sol/max workers as useful,
repair and verify rather than stopping at findings, and durably push every justified result.

## Objective

Establish, from current source-bound inputs, whether GTOS's broad research stack behaves coherently
from raw bars through candidate generation, Selector V4, Scheduler V4, risk sizing, order/fill
lifecycle, quote-side execution, exits, costs, and portfolio accounting. Build the missing full-flow
instrument, run equal-scope comparisons across the published baseline, foundational repairs, and the
latest forward repairs, classify every decision-bearing delta by stage and cause, repair every
same-evidence-class defect that can be repaired, and leave the strongest honest system and result
durably integrated.

This lane must keep the broad V4 stack and the live `ultimate_book` W7 stack separate. Broad-stack
results do not certify W7. W7 economics require their own exact-current recost/replay lane and their
own evidence classes.

## Mandatory preflight and anti-staleness

Before work, and again after compaction, interruption, uncertainty, or a material merge:

1. Regenerate and read `.context/LIVE_STATE.md`.
2. Read `CLAUDE.md`, `GTOS_ULTRA_GOAL.md`, `live_system_of_record.md`,
   `.context/00_core/goal_session_research_discipline.md`,
   `.context/00_core/research_operating_doctrine.md`, the orchestration/merge controls, this prompt,
   `CONTEXT_ANCHOR.md`, and the newest phase-19/20 route artifacts relevant to the lane.
3. Rebuild Context OS and generate a task-specific `ultimate --include-memory` pack.
4. Verify branch HEAD, merge base, status, sparse/LFS state, free disk, active processes, source
   hashes, and exact evidence paths from disk. Do not rely on chat memory; chat and session prose are
   navigation only.

These are active instructions, not background reading. Operationalize them in the commands, artifacts,
tests, and stop conditions of every lane. The posture is constructive and curious: there is no
conservative brake on local building, measurement, or same-class repair within the forbidden surfaces.

Literal impossibility means exactly that full same-evidence-class pursuit has exhausted every executable
read, export, search, parser, repair, proxy, ablation, metric, audit, and review action that could change
the answer. Anything less remains `IN_PROGRESS`.

## Evidence class and forbidden surfaces

The result-bearing lane is local, source-bound replay/research plus engineering proof. It may change
repo code, tests, replay tools, cost models, verifiers, docs, and default-off research configuration.

It must not mutate broker/account/order/deal/position state, activation tokens, live services,
scheduled tasks, VPS files, or live process state. Current-host recovery is read-only and must be
reported separately. No risk dial, allocation composition, activation, promotion, or sealed-economic
contract decision is made for the owner. Do not relax thresholds, suppress opportunities, trim a
population to manufacture a positive result, or convert `REJECT`/`NOT_EVALUABLE` into promotion.

Also forbidden without a new owner command: a production change or live trading/broker operation;
paid API or vendor access; credential use; or live config, risk, execution, safety, canary, selector,
account, order, history, deal, or position mutation. Local source and default-off research work remains
authorized.

## Required full flow

The instrument must use the actual production/research implementations, not handwritten substitutes,
and must preserve a stable composite identity at each boundary:

`source bars -> market/as-of state -> all broad generators -> normalized candidate identity ->`
`Selector V4 admission/ranking -> Scheduler V4 allocation/occupancy -> continuous risk sizing ->`
`order construction -> fill/no-fill lifecycle -> quote-side/tick-aware path walk -> exits ->`
`component costs -> trade/account/portfolio economics -> missed-opportunity accounting`

At every stage emit a compact decision-bearing ledger sufficient to answer:

- which identities entered and left the stage;
- the exact reason for every rejection, mutation, merge, deduplication, replacement, or size change;
- whether chronology, session, lifecycle, profile/account, symbol, side, or cost coverage caused it;
- which final trades and economics depend on each upstream difference.

No full monolith evidence payload is required. Evidence is a projection over the decision trace.
There is no arbitrary top-N or number-limited cutoff: preserve all material rows in the full ledger and
summarize only after the decision-bearing population is complete.

## Comparator contract

Run the same source bytes, window, profile, options, and command against at least:

1. published `origin/main` at Wave-21 start (`9392e9bbd` unless refreshed current disk proves a newer
   remote base);
2. the foundational-repair state (`5d3fe32c7` plus current mainline integration where required);
3. the latest committed forward-repair state (`phase19/partial-bar-generator` at `ca7dec531` unless
   current disk proves a successor);
4. the integrated candidate after justified repairs.

If exact historical execution is impossible, prove the precise incompatibility and build the closest
equal-scope adapter without changing economic semantics. Every comparator declares code hash, input
hashes, config/profile fingerprint, option flags, population, and output hashes.

## Required defect closure

Pursue these named surfaces until fixed, proven non-defective on current code, or reduced to an exact
source/capture/owner dependency:

- quote-side residuals: time-varying short-side spread, resting-limit tick validation, and gap-through;
- candidate chronology/session/DST correctness, stable POI lifecycle, stale bars, malformed past-stop
  orders, identity explosion, duplicates, and target/stop policy provenance;
- Selector V4 provenance, lifecycle, composite identity, admission, ranking, and package semantics;
- Scheduler V4 deduplication, occupancy, scarcity, rank preservation, allocation, correlation, and
  option-count inflation;
- ATR/timeframe units, risk sizing, lot/account translation, and stage-to-stage identity joins;
- commission, spread-era authority, holding-time/swap units, slippage scale, FX conversion, DST, and
  explicit coverage-class propagation;
- seal/import closure and a compact independent economic recomputation;
- exact current W7 recost/replay on its own stack, with no transfer from broad V4.

Turn negative findings into a bounded causal repair, a materially distinct same-class experiment, or
an exact higher-information stop. Do not stop at a defect list while an in-scope repair or test remains.

## Verification

Every behavior change requires adversarial behavioral tests and a failure-set A/B against its clean
parent or an identical clean comparator. The integration owner runs focused tests after each merge and
one equal-scope full-suite A/B at closure. Record pass/fail sets, not only counts. Check sparse/LFS
requirements, source hashes, deterministic reruns, import closure, secrets, oversized Git blobs, and
that default-off research behavior remains default-off unless an existing owner decision says otherwise.

Economic reruns must publish gross and component-net values from the same rows, day-block uncertainty,
concentration, coverage class, and stage attribution. A preflight, test pass, or receipt is never
reported as an economic result.

## Parallel ownership and integration

Use isolated branches/worktrees or disjoint write scopes. Each lane returns source commit(s), touched
paths, tests, A/B receipt, evidence class, unresolved dependencies, and an explicit integration order.
No worker merges or pushes `main`; the integration owner inspects artifacts and owns the merge train.
Research branches may be pushed for durability after oversized-blob and secret checks.

## Terminal closure

This route completes only when all of the following are true:

1. A runnable raw-input-to-final-economics full-flow command and independent compact verifier exist.
2. The comparator matrix ran on the largest decision-bearing open population practical from current
   source authority; any narrower scope has an exact blocker and follow-on command.
3. Every stage delta is classified and traceable to final trade/economic consequences.
4. All same-evidence-class defects are repaired or closed with exact proof/impossibility/source needs.
5. Broad V4 and W7 conclusions are separately measured and separately stated.
6. Focused tests, deterministic reruns, failure-set A/B, LFS/sparse/secret/blob checks, and integration
   review pass.
7. Scoped commits and durable remote branches/PRs preserve the work; `main` is updated only when its
   integration gate is satisfied.
8. The owner receives a candid result: what is correct, what was wrong, what economics changed, what
   is still unknown, and the exact next owner or external action, if any.
9. A machine-readable completion audit and output manifest prove the terminal conditions and name every
   unresolved item, source dependency, branch decision, and implementation decision.

Context or token exhaustion is an `IN_PROGRESS` checkpoint, never completion. Every blocker is pursued
until cleared, proven impossible from approved routes, or reduced to an exact owner/access/source/capture
requirement. No vague placeholders or lazy future-work labels are allowed.
