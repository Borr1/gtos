# Divergence Sampling — week 2026-19 (insufficient sample)

- **Generated:** 2026-05-04 07:00 UTC
- **Available shadow rows since last sample:** 0
- **Required threshold:** 20

No classification batch produced this week. The shadow log has not accumulated enough `mode=shadow` rows since the last sampling run.

Possible causes:

- Live fleet is in dead zone / weekend — divergences only emit during trading.
- Live fleet was restarted and the state file hasn't advanced because no new divergences logged.
- `detector_version` was flipped back to `v1` — dual-compute disabled.

The sampler will retry next Monday. If multiple weeks pass with zero rows, check `config/agent_config.yaml` → `market_state.detector_version` and the orchestrator log for dual-compute errors.
