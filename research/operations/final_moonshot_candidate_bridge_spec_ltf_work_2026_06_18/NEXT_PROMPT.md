# Hardened Goal-Session Prompt - Candidate Profile Patch And Broker-Specific Subset

One-line starter:

`/goal Follow this full controlling prompt as the complete objective. Run mandatory GTOS preflight, do not rely on chat memory, reread this prompt plus goal_session_research_discipline.md and research_operating_doctrine.md after any compaction/resume/interruption/uncertainty, operate as a constructive deployment-package goal session with no conservative brake, pursue full same-evidence-class work before declaring an external requirement, preserve every useful candidate idea with no arbitrary top-N, preserve all material rows in full ledgers, use the localhost Docker MT5 bridge for allowed OHLCV/volume/spec/history data whenever data is needed, avoid orderflow/depth and all broker/account/order/deal/position/credential/live VPS mutation, and mark complete only after artifacts, verifier/tests, completion audit, scoped commit, and exact owner/VPS action boundary exist.`

## Objective

Continue from:

`research/operations/final_moonshot_candidate_bridge_spec_ltf_work_2026_06_18/`

The current route proves:

- all `14/14` unfinished candidate symbols are native on the localhost FTMO Server3 bridge;
- `XPDUSD`, `XPTUSD`, and `XRPUSD` now have bridge-backed M1 coverage, so LTF gap count is `0`;
- the seven reintroduced FX symbols are dual-broker profile-patchable from FTMO bridge + existing redacted_account/VPS spec ledgers;
- `DASHUSD`, `LTCUSD`, `XPDUSD`, `XPTUSD`, `XRPUSD`, and `XTZUSD` are FTMO-patchable but need broker-specific subset semantics or a redacted_account export;
- `NATGAS_cash` remains research-only until fresh cost proof overcomes the W7 hard-drop reason.

Build the next route that materializes the profile/spec/native work into default-off candidate-book deployment support without widening live global eligibility casually.

## Mandatory Context Use

Run:

```bash
python3 scripts/generate_live_state.py
```

Then read at minimum:

- `.context/LIVE_STATE.md`
- `.context/00_core/current_vnext_system_map.md`
- `.context/00_core/current_repo_reading_order.md`
- `.context/00_core/goal_session_research_discipline.md`
- `.context/00_core/research_operating_doctrine.md`
- `.context/00_core/orchestrator_successor_operating_brief.md`
- `.context/00_core/orchestrator_methodology_hardening_controls.md`
- `research/operations/final_moonshot_candidate_enabled_unified_replay_mc_2026_06_18/`
- `research/operations/final_moonshot_candidate_bridge_spec_ltf_work_2026_06_18/`
- `research/operations/final_moonshot_candidate_activation_readiness_2026_06_18/`
- `config/profiles/operator_profile.yaml`
- `config/profiles/redacted_account.yaml`

Treat those files as active instructions, not background. The completion audit must record instruction coverage, builder posture, anti-boxing questions pursued, same-evidence-class repairs, and exact source/export requirements left.

## Evidence Class And Materialization Standard

Evidence class: read-only broker profile/spec/history/cost repair, route-local deployment-package dossier, default-off config/profile/code proposal or implementation, and focused verifier/test coverage.

Literal impossibility means exactly this: every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action inside the evidence class has been tried or proven inapplicable, and the remaining requirement names the exact forbidden surface, missing source, export, parser, field, or different evidence class needed.

The result materialization standard is not prose-only: materialize source completeness, exact-R or proxy-R/expectancy where fields permit it, implementation decision rows, branch decision artifacts, profile patch rows, source-capture manifests, and verifier-backed owner/VPS action boundaries before completion.

## Required Work

1. Generate FTMO Server3 profile contract rows for bridge-ready candidate symbols, preserving existing profile style and risk-source metadata.
2. Generate redacted_account profile contract rows for the seven reintroduced FX symbols from existing live/VPS redacted_account spec ledgers.
3. Do not add FTMO-only symbols to redacted_account by identity fallback. Either obtain redacted_account-native bridge/spec data or implement a default-off broker-specific candidate subset/skip dossier.
4. Do not widen the global live native-eligible list casually. Prefer candidate-book-specific default-off readiness/proposal support unless production code proves global widening cannot affect current live behavior.
5. Keep `NATGAS_cash` research-only until fresh tick/cost proof overcomes the W7 hard-drop.
6. Rerun candidate activation readiness after profile/spec changes and record the reduced work rows.
7. Produce verifier/tests, route audit, owner/VPS action boundary, rollback criteria, and monitoring criteria. No live flip.

## Forbidden Surfaces

- No live trading, broker account, order, deal, position, history mutation, or credential mutation/disclosure.
- No orderflow/depth data for this phase.
- No paid API/vendor calls without a separate manifest and explicit approval.
- No live VPS restart, reload, supervisor mutation, or process mutation.
- No remote push unless the owner explicitly approves in the active turn.
- No candidate-book live activation. Production code/config/test edits are allowed only if default-off, route-owned, verifier-covered, and do not mutate live broker state.

## Required Artifacts

Create route-local artifacts including:

- profile patch ledger;
- broker-specific subset or redacted_account export requirement ledger;
- updated readiness reduction ledger;
- default-off code/config proposal or implementation;
- inspire-not-kill translation ledger;
- verifier result;
- completion audit;
- decision ledger;
- saturation audit;
- output manifest;
- focused pytest coverage.

## Completion Standard

Complete only when profile/spec/native work is materially reduced and every remaining row has an exact executable next action. Same-evidence-class work must be pursued before any external source requirement is recorded.
