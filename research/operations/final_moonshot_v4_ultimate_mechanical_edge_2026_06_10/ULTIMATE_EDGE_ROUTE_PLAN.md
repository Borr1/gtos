# Ultimate Mechanical Edge Route — Plan & Action Queue

Route status: `ACTIVE_PHASE0_DATA_ENGINE`
Owner approval anchors (2026-06-10 session): unlimited bridge data export approved; learned frozen model approved as full admission + sizing authority after gates; ruin target P(daily −5% breach) ≤ 0.1%/day (~3% per 30-day phase).
Master plan: `/Users/borr/.claude/plans/curious-roaming-perlis.md` (approved 2026-06-10). This route dir is the durable on-disk authority.

## Program summary

Successor route to `final_moonshot_v4_kiap_ultimate_system_repair_2026_06_07` (closed package, item #13 superseded by this program). Diagnosis: admission/ranking runs on uncalibrated heuristic scores (Brier 0.302 / ECE 0.330); exit policy is one global parameter set; risk limits are hand constants with no measured ruin probability; data was starved (~17 replayable days). Fix: learned calibrated edge layer (full counterfactual labels from missed-opportunity/oracle ledgers), segment-aware exit engine, ruin-aware Monte Carlo risk governor, scheduler ranking retrain — all trained/validated on a ~1-year bridge-backfilled replay horizon under frozen partition law.

## Phases

0. Bridge data engine + partition law (this phase)
1. Counterfactual training frame (`learned_edge_dataset_builder`)
2. Learned edge layer (trainer + frozen artifact + selector_v4 learned mode + walk-forward gate)
3. Segment exit engine (headroom audit → tournament → frozen segment policy table)
4. Ruin Monte Carlo governor (`risk_governor_v4`, MC calibration, outer circuit breaker)
5. Scheduler ranking retrain (learned scores replace hand-set weights)
6. Joint integration → one-shot sealed gate → production-return dossier inputs

## Action queue

1. [x] Install/verify Docker MT5 bridge client (`siliconmetatrader5==1.2.3`, container `siliconmetatrader5-kasm`, localhost:8001, FTMO-Server3 login hash `310fcf06…` — matches `operator_profile` profile).
2. [x] Probe history depth: D1/H4/H1/M15/M1 available from 2025-06-02 for the 24-symbol surface; tick at least to 2026-02-03. FTMO terminal symbol names: GER40.cash, JP225.cash, US100.cash (NAS100), US500.cash (SPX500), UK100.cash, UKOIL.cash, US30.cash, USOIL.cash.
3. [x] Bridge parity certification: `scripts/export_mt5_research_ohlcv.py --bridge-host localhost` re-export of 2026-04-27/28 EURUSD+NAS100 M1 is BYTE-IDENTICAL to the certified `timewarp_ftmo_20260427_20260428` package → `ULTIMATE_EDGE_BRIDGE_PARITY_PROOF.json` (PASS_BYTE_IDENTICAL). No new exporter needed — existing exporters already support the bridge.
4. [x] Freeze partition law → `ULTIMATE_EDGE_PARTITION_REGISTRY.jsonl` (TRAIN 2025-06-02→2026-04-17 untouched backfill + burned/touched slices flagged; VALIDATION 2026-05-14→29 excl. May 18; SEALED Apr 27-28, May 05, Jun 01-09 reserved single-shot; STRESS May 01/04; FORWARD weekly roll-in).
5. [x] Complete full backfill: `bridge_ftmo_htf_20250601_20260610` (D1/H4/H1, 198,690 rows), `bridge_ftmo_m15_20250601_20260610` (612,190 rows), `bridge_ftmo_m1_YYYYMM` × 13 monthly packages (~9.07M rows). Total 9,877,480 rows, 24 symbols in every package, zero anomalies, ~6 minutes wall clock → `ULTIMATE_EDGE_SOURCE_BACKFILL_MANIFEST.json` (data packages live under gitignored `data/mt5_research_exports/`, evidence via manifests).
6. [~] Materialize replay ledgers for TRAIN backfill days via `run_ultimate_edge_train_materialization.py` (bounded day-by-day, day-relative lookback overrides recorded per day, fresh SimulatedBroker per day, SEALED/VALIDATION refusal, resumable via day-progress ledger). Probes: 2026-03-02 completed (838 candidates, 3 fills, ~100s), 2025-09-15 completed (786 candidates, 9 fills, zero missing symbols). Full run 2025-07-01→2026-04-30 + 2026-05-06→13 launched in background 2026-06-10 12:53 UTC (~215 days, ~6h). Ledgers land under `train_ledgers/` with prefix `ULTIMATE_EDGE_TRAIN_<YYYYMMDD>_`. SEALED June days exported but NEVER replayed.
7. [x] Phase 1 (builder): `src/research_infra/learned_edge_dataset_builder.py` + `tests/research_infra/test_learned_edge_dataset_builder.py` (8 passed). Smoke run over existing KIAP rolling ledgers: 7,565 candidate rows / 9 days, 7,188 outcome-labeled (95.0%), dispositions = 71 filled + 1,349 risk-rejected-counterfactual + 6,141 missed + 4 unresolved; partition roles assigned from registry; SEALED refusal enforced by construction. Full-frame build waits on item 6 ledgers.
8. [ ] Phase 1b: `predecision_features` block in `broader_origin_generators._candidate` + re-replay of TRAIN/VALIDATION (V2 features).
9. [~] Phase 2 (model core done): `src/research_infra/learned_edge_trainer.py` (L2 logistic + OOF EB target encoding + beta calibration + day folds + label-shuffle leakage canary + scorer-parity check) and `src/components/learned_edge_layer_v4.py` (pure-Python frozen-artifact scorer, fail-closed on schema/sha/staleness/missing-features). Smoke train on KIAP frame (5,804 rows, 7 folds): outcome head OOF Brier 0.122 / ECE 0.035 / AUC 0.612 vs heuristic Brier 0.302 / ECE 0.330 — calibration ~10x better; shuffle canary 0.488 clean. 15 tests pass across the stack. REMAINING: full-frame training after item 6, selector_v4 learned mode + shadow-parity ledger, walk-forward gate module.
10. [~] Phase 3 (go/no-go DONE — tournament GO): `src/research_infra/ultimate_exit_headroom_quantification.py` → `ULTIMATE_EXIT_HEADROOM_QUANTIFICATION_KIAP_BASELINE.json`. KIAP 9-day baseline: 48 winners gave back 36.18R total (mean 0.75R); best coarse abort rule (tighten −0.4R before 0.25R progress) fires 11/21 losers saving ~6.0R upper-bound, zero winners killed; global headroom ~4.69 R/day vs ~1-2.6 achieved. GO: energy/fx/index/jpy_fx; metals thin-n no-go pending full TRAIN rerun. REMAINING: PolicySpec abort extensions, tournament module, segment table + wiring.
11. [~] Phase 4 (build done): `src/components/risk_governor_v4.py` + `src/research_infra/ultimate_ruin_monte_carlo_governor.py` committed (38 tests; seeded deterministic bootstrap, CRN calibration grid, Wilson upper-CI feasibility, count-cap ablation, breakers at 4.2%/8%). REMAINING: real-ledger calibration run on the full TRAIN day records + sensitivity suite + tilt audit + timewarp/scheduler budget-path integration.
12. [~] Phase 5 (mode done): scheduler V4 learned-ranking mode committed (default-off, fail-soft missing-score penalty, vetoes dominate; 82 tests). REMAINING: end-to-end ranking with the trained artifact + regret metric on validation.

### 2026-06-11 build-wave record
All 7 subsystems implemented via parallel agent wave with adversarial review (5 approve, 2 fix_required) + full battery (144/144 new tests; 451 passed touched-module suites; remaining failures pre-existing repo-state, NOT wave regressions). Review majors fixed before commit: (a) execution.py dispatch for both abort actions + replay-parity consecutive adverse-close counter (fail-closed when missing); (b) selector scorer-exception wrapped into explicit refusal; (c) tournament resolver session-vocabulary normalization + params-fidelity opt-in gating (fix agent). Known pre-existing debt recorded: stale test_gtos_vnext_runtime config-wiring pins vs committed KIAP config (separate reconciliation task); test_vnext_broader_origin_orchestrator monkeypatch-target cluster (20 failures) pre-existing at HEAD; test_moonshot_candidate_quality_selector 3 weekend-contract failures pre-existing at HEAD.
13. [ ] Phase 6: joint rerun on VALIDATION → freeze → forward-claim pre-declaration → one-shot SEALED gate → dossier inputs + weekly runbook.

### 2026-06-11 V1 training + gate verdict (trial budget +1, recorded)
- Full universe materialized: 234 completed replay days (2025-07-01→2026-05-29; Dec/April recovered after ReplayClock partial-hydration fix). Baseline truth under incumbent mechanism: **−223.6R / 187 initial days, 30% win rate, 40/187 positive days** — the recent May positives were unrepresentative.
- Frame: `train_ledgers/ULTIMATE_LEARNED_EDGE_TRAINING_FRAME_V1.jsonl` — 185,025 candidates, 95.3% outcome-labeled (149,320 missed + 34,515 risk-rejected counterfactual + 1,147 filled).
- V1 artifact `ULTIMATE_LEARNED_EDGE_LAYER_V1.json` (175,009 rows, 46 weekly folds): outcome OOF Brier 0.1257 / **ECE 0.0014** / AUC 0.656; shuffle canary 0.503. On untouched VALIDATION: Brier 0.1251 / ECE 0.0042 vs heuristic Brier 0.537 / ECE 0.631.
- **Walk-forward gate V1: REJECTED (expected, evidence-grade)** → `ULTIMATE_LEARNED_EDGE_WALKFORWARD_GATE_V1.json`. Learned floors admit 0/8,199 validation rows; heuristic floors admit 7,798 (95%) summing **−1,683.5R at 55% loser rate**. EV distribution (identical train/validation): median −0.233R, EV>0 only 0.56%, max +0.10R.
- **Program finding:** under the incumbent exit policy (momentum 2R cap) + ~0.15-0.18R costs, the candidate pool contains essentially no admissible edge at any floor — selection cannot rescue the payoff engine. P(target|fill) mean 0.155, max 0.34. The upside is exactly where the headroom audit measured it: exits (giveback 0.75R/winner + abort savings ≈ 4.7R/day upper bound).
- **Next critical path (item 13 reshaped): joint exit×selection iteration** — (a) full-universe segment tournament over all 234 days; (b) re-label every candidate's net R under its segment's selected policy via counterfactual path replay; (c) retrain EV head on improved-exit labels; (d) re-gate on VALIDATION (trial budget +1); (e) only then governor calibration + scheduler ranking on the surviving EV distribution; (f) sealed gate.

### 2026-06-11 full-universe tournament + EXIT-ORACLE BOUND (the decisive pair)
- Tournament over 234 days (`ULTIMATE_EXIT_POLICY_SEGMENT_TABLE_V1.json`): 186 segments, 8 promoted, all with p<1e-24 at n up to 33,326 — but deltas only **+0.006..+0.047 R/trade** vs incumbent. The tested family (≥1.0R triggers, ≥1.5R fixed targets, 0.35R trail gaps) cannot move a −0.23R median EV.
- **Exit-oracle bound over the same 34,180 filled paths: mean +0.72R, median +0.49R, 76.8% positive after 0.17R costs. MFE distribution: median 0.659R, p75 1.61R, p90 2.13R.**
- **Synthesis: the generators produce live raw material (+0.72R/trade oracle ceiling), but every tested exit policy activates ABOVE the median path's 0.66R peak.** The realizable payoff region is sub-1R MFE capture + loss-tail compression — a region the current grid never sampled.
- **NEXT (first action on resume): sub-1R exit grid** in the tournament (`default_grid` extension): micro fixed targets {0.4,0.5,0.65,0.75,1.0}; partial {50%,67%} at {0.4,0.5}R with BE + runner; trail triggers {0.25,0.5} gaps {0.2,0.35}; aborts MANDATORY-crossed (adverse {0.4,0.5,0.6} × mfe-floor {0.25}); time stops {4,8,16} bars. EV algebra check per spec against the MFE distribution before replay (cheap pre-screen: T-target EV = P(MFE≥T)·(T−c) − tail math). Investigate why abort crosses showed no dominance in V1 grid results (verify they actually evaluated). Then relabel → retrain → re-gate per the standing loop.
- Boundary: tournament/oracle numbers are replay/proxy evidence over m1 paths; not broker-real claims; sealed days untouched.

## Binding rules (from approved plan + repo doctrine)

- Read-only broker surface; no order/credential/remote mutation; runtime halt flags stay authoritative.
- Whitelist-only features; forbidden-field contracts enforced; leakage canaries required per training run.
- Sealed partitions single-use; trial budget logged; no per-day tuning; incumbent-by-default in every tournament.
- All new live-path components ship default-off behind config gates until their gates pass; activation is a scoped, owner-visible config change.
- Evidence classes: replay/proxy labels are never broker-real claims; tick promotion for selected orders.

## Resume rule

After compaction/interruption: regenerate `.context/LIVE_STATE.md`, reread this file, `ULTIMATE_EDGE_PARTITION_REGISTRY.jsonl`, `ULTIMATE_EDGE_BRIDGE_PARITY_PROOF.json`, the backfill manifest rollup, and the approved master plan. Continue from the first unchecked action-queue item. Do not touch SEALED partitions outside item 13.

### 2026-06-11 geometry experiment — FIRST POSITIVE CELLS (scale-2 decision)
- Stop-width scale knob (default-off, candidate-id stable across scales) + isolated experiment ledgers. Cost-model finding: candidate `expected_cost_r` is a configured per-symbol CONSTANT (0.18 regardless of stop width) — replay economics corrected as cost_r/scale (all components price- or lot-denominated); live pretrade packets unaffected (compute spread/stop live).
- Scale curve (Sept 2025 + Feb 2026, 42 days/scale): scale-1 oracle +0.72/cost 0.17; scale-2 oracle +0.62/cost ~0.085. Sub-1R trails still lose replay at scale 2 (M1 range ≈ 0.52 R2); winner trailing t1.0/g0.1/c2 at +0.0295 vs incumbent. Scales 3/4/5 September runs for curve completion.
- **RELABELED EV-TAIL AT SCALE 2 (winner exit, corrected cost, 6,691 paths): 45.3% positive, p75 +0.87R; 5/52 coarse segments POSITIVE MEAN pre-selection** — jpy_fx/structural_distance_extreme/london +0.144 (n=48), energy/session_open_range_break/ny +0.099 (n=62), energy/displacement_continuation/ny +0.075 (n=113), jpy_fx/liquidity_sweep_reclaim/ny +0.056 (n=76), fx/liquidity_sweep_reclaim/tokyo +0.014 (n=59). Matches KIAP's metals/energy/JPY investigation leads. Multiple-comparisons risk over 52 cells acknowledged — confirmation belongs to the learned layer + validation gate, never hand-promotion.
- **DECISION: V2 candidate geometry = stop-width scale 2.0.** Full-year re-materialization at scale 2 launched into `train_ledgers_scale2/` (TRAIN + VALIDATION with --allow-validation; SEALED refused; V2 predecision features now emitted by generators). Then: full-year scale-2 tournament → relabel under per-segment winners with corrected costs → retrain EV head → re-gate (trial budget +1).
- AMENDMENT (same day): scale-3 two-month read BEATS scale-2 — mean −0.108 vs −0.147, 10/53 positive-mean segments (jpy_fx/sweep/ny +0.327 n=75, metals/sweep/london +0.288 n=72), global winner is be_only_t0p5 + abort(0.4/0.25) — abort primitives win at wide geometry. Full-year launch HELD until scale-4/5 September curve points land; pick = argmax(robust structure), then full-year materialization at the chosen scale (script: /tmp/gtos_scale2_fullyear.sh — update --stop-width-scale and subdir before relaunch).
- CURVE COMPLETE (September-only, per-trade mean / positive-segment weight): 2x −0.120/0.206; 3x −0.082/0.250; **4x −0.048/0.281 (ARGMAX)**; 5x −0.062/0.276 (turnover). energy/session_open_range_break/ny positive at every scale (+0.24..0.43). **V2 GEOMETRY LOCKED: stop-width scale 4.0.** Full-year materialization into `train_ledgers_scale4/` launched (TRAIN + VALIDATION; SEALED refused). On completion: full-year scale-4 tournament → relabel under per-segment winners (cost = config/4) → V2 frame via dataset builder over train_ledgers_scale4 → retrain → re-gate (trial budget +1).

### 2026-06-11 V3/V4 iterations + SEGMENT BOOK V1 (honest edge inventory)
- V3 (full-universe relabel, 176,140/185,043 paths incl. missed-candidate limit-touch reconstruction): calibration excellent (ECE 0.005 train AND validation, canary 0.50) but outcome AUC 0.528 — v1 features cannot discriminate trail-exit winners. Gate REJECTED (admitted 0).
- V4 (15 f_pd_* path-character features wired through builder/scorer/trainer, verified in frame and artifact): AUC unchanged 0.528 — pd features carry near-zero LINEAR marginal signal (top pd coef 0.13 vs categorical encodings 0.5-1.2). Gate REJECTED. Candidate-level discrimination ceiling at this information set is real for linear models; GBM challenger remains untried.
- **SEGMENT BOOK V1** (`build_segment_book.py` → `ULTIMATE_SEGMENT_BOOK_V1.json`): TRAIN-only one-sided tests across 291 cells, BH(0.10) → **1 surviving cell (fx/structural_distance_extreme/moonshot_h12_13, train +0.156 n=353)**; single-shot validation: **9 trades, +2.96R (+0.329/trade)**. Ad-hoc pre-scan (n≥200, 95%LB>0, no BH) had 3 cells with 2/2 out-of-time HOLDS and book +0.18/trade over 42 validation trades.
- **HONEST EDGE INVENTORY: the 7 generators contain real, out-of-time-stable positive pockets worth ~+0.5-1 R/day replay-proxy at current certification power — system-wide expectancy remains negative outside them.** Power is n-limited (1yr M1 retention bounds history; cells n~300).
- GROWTH FORKS (next decision): (a) symbol-universe expansion — bridge exposes 166 FTMO symbols vs the 24-surface; same pipeline, multiplies cell n and adds segments; (b) GBM challenger for nonlinear candidate-level discrimination; (c) new candidate origin generators (registry has 12 unexplored families); (d) deploy-track: governor calibration + sealed gate on the thin-but-certified book as-is.

### 2026-06-11 ALL-IN CYCLE (owner: full approval, no constraints, finish the ultimate system)
**Chosen path: (a) symbol-universe expansion (main) + (b) GBM challenger (parallel). In flight: bridge universe probe (/tmp/gtos_universe_probe.json) + GBM challenger on FRAME_V4_RELABELED (/tmp/gtos_gbm_challenger.py).**

UNIVERSE EXPANSION EXECUTION BRIEF (exact steps for resume):
1. Read /tmp/gtos_universe_probe.json → expansion set = symbols with full M1 probe depth (≥5000 rows in 10 days) and sane spread (exclude spread_points > ~80 on 5-digit FX, exotic CFDs with huge spreads). Canonical name = bridge name with '.'→'_' (e.g. AUS200.cash → AUS200_cash).
2. Export per earlier pattern (scripts/export_mt5_research_ohlcv.py --bridge-host localhost --yes-live-readonly --source-broker FTMO --source-role owner_authorized_research_hydration --not-redacted_account-native --source-truth-scope ordered_price_path_only_not_broker_order_lifecycle_truth): HTF D1,H4,H1 full range 2025-06-01→now (one label bridge_ftmo_ext_htf_*), M15 full range, M1 monthly packages bridge_ftmo_ext_m1_YYYYMM. --symbol CANON:BRIDGE pairs from the probe.
3. Engine surface at runtime (do NOT edit committed constants): materializer gains --surface-file (JSON list of canonical symbols + canonical→bridge map); before each day it sets tw.GTOS_PRE_HALT_LIVE_SURFACE/tw.INCLUDED_SYMBOLS and updates tw.FTMO_SYMBOL_MAP; recorded in progress rows. KIAP parity verifiers are unaffected (they pin their own routes).
4. Generator prerequisites for unknown symbols: (i) SESSION_WINDOWS fallback — check broader_origin_generators._session_at/session_windows handling for unmapped symbols; add a default window set (use the moonshot hourly windows + standard london/ny/tokyo) gated to expansion symbols; (ii) ASSET_CLASS_BY_SYMBOL extension in learned_edge_dataset_builder + learned_edge_layer_v4 (mirror! parity test pins equality) — classify by bridge path group (Forex*/Crypto/Indices/Commodities/Metals/Energies); (iii) config defaults: replay_symbol_config tolerates missing instruments.* overrides; expected_cost_r defaults ~0.17 config-constant (relabel corrects /scale); verify timewarp risk authority default for unknown symbols (risk_config_source) on a 1-day probe before batch.
5. Probe first: 1 day (2025-09-15) on ~10 new symbols; then 2 months (Sept+Feb) across the full expansion at scale 4 into experiment_ledgers/universe_ext_probe/; segment-book rebuild over scale4 24-surface + expansion probe; if cell count/book size grows materially → full-year expansion materialization → full pipeline (tournament/relabel/book/gate).
6. Deploy-track in parallel once book ≥ ~5 certified cells: ruin-governor calibration (ultimate_ruin_monte_carlo_governor CLI on the BOOK trade stream), scheduler ranking, forward-claim pre-declaration, ONE-SHOT sealed gate (Apr 27-28, May 05, Jun 01-09).
GBM: if challenger validation AUC ≥ 0.58 with sane calibration → integrate as artifact alternative (extend trainer/scorer for tree ensembles or distill into the logistic via stacked feature); else record ceiling and close fork (b).
- GBM FORK CLOSED (2026-06-11): HistGradientBoosting on FRAME_V4_RELABELED, validation AUC 0.511 (worse than logistic 0.528), top-decile mean net negative. No nonlinear candidate-level signal at this information set; per-candidate discrimination ceiling confirmed across model classes. Segment book remains the certified-edge mechanism; universe expansion is the active power lever.
- UNIVERSE PROBE RESULT: 34 M1-deep symbols; EXPANSION SET (22, sane spreads): USDSGD USDCNH DXY_cash AUS200_cash EU50_cash FRA40_cash US2000_cash N25_cash XAUEUR XAUAUD XAGAUD XAGEUR XCUUSD NATGAS_cash HEATOIL_c COTTON_c CORN_c LTCUSD DOTUSD ADAUSD DASHUSD XTZUSD (canonical = bridge name with '.'→'_'). Excluded: USDSEK/USDZAR/HK50/SPN35/XPD/XPT/SOYBEAN/WHEAT/XRP (spreads) + single equities (session regime). Exports launching to bridge_ftmo_ext_* labels. NEXT after export: materializer --surface-file runtime override + generator session-window fallback + asset-class map extension (mirror builder/scorer!) per brief step 3-4, then Sept+Feb probe at scale 4.

### 2026-06-11 OWNER DIRECTIVE — governing definition of the ULTIMATE SYSTEM
The owner (full authority granted) defines the bar: not just intelligence/universe expansion — the SYSTEM itself must be excellent. An ultimate system does NOT hold 8-hour trades hoping a distant target prints; does NOT widen stops 4x defensively and push targets further with unknown odds. It enters precisely, resolves FAST, knows its odds, and executes cleanly. Adopted as route doctrine:
1. **Trade velocity is a first-class objective**: optimize R per HOUR, not just R. Milestone clocks (time-to-0.25R..3R, time-to-MFE/MAE) exist on every replayed trade — mine them; per-segment time-stop calibration from time-to-MFE distributions; duration enters the gate metrics.
2. **Entry precision over defensive geometry**: scale-4 stops were a cost/noise patch, not the end state. Measure counterfactual M1-resolution entries (post-signal pullback/retest triggers) from existing paths — better entries shrink MAE and let stops TIGHTEN back while keeping cost_r low. Target: recover scale-1-like risk distances with scale-4-like cost ratios via entry quality.
3. **Execution truth to the tick** where it matters (unlimited data approved): tick-promote the certified-segment trades; measure slippage/spread reality per cell.
4. Expansion continues in parallel (universe probe live: 40 symbols generating, 1,273 candidates/day; 6 short-session CFDs need M1 floor 300 override — added).
NEXT QUEUE: (1) ext probe Sept+Feb materialization (running); (2) ENTRY/VELOCITY STUDY from existing scale-4 oracle+M1 paths: counterfactual entry triggers x time-stop grid, measured on net R AND R/hour; (3) segment-book rebuild over 46-symbol probe; (4) governor calibration + sealed gate when book is fat enough.
- 2026-06-11 INFRA: ETIMEDOUT root cause = disk at 98% (125GB of replay artifacts). Pruned ~75GB of regenerable bulk (scale-1/2/3/5 ledger bulk, scale-4 asof/sidecar/event/account kinds, intermediate frames) — evidence preserved (progress ledgers, committed tables/artifacts, FRAME_V4_RELABELED). Materializer hardened (source-package errors skip the day). 120Gi free; expansion probe months rerunning caffeinated.
- 2026-06-12 ENTRY/VELOCITY STUDY (ULTIMATE_ENTRY_VELOCITY_STUDY.json, 159,892 paths): pullback entries improve EV monotonically (global base −0.063 → −0.013 at 0.4R pullback incl. missed-as-zero); **106/348 segments positive EV under pullback-0.4 entries** (vs ~10 before) — entry precision ~10x'd the positive surface, confirming owner doctrine. Velocity: per-segment median time-to-MFE spans 27min (metals/sweep/h23 — ideal) to 12h (slow fx cells — exclude or time-stop). NEXT: (1) expanded book V2 (chain running); (2) integrate pullback-entry trigger + per-segment time-stops into the candidate/exit contract (generator entry_reference offset + ExitPolicy time stops from velocity table); (3) rebuild book on pullback-adjusted labels with BH; (4) governor + sealed gate on the enlarged certified book.
- 2026-06-12 BOOK V2 ATTEMPT — TWO DEFECTS FOUND (result quarantined, do not cite ULTIMATE_SEGMENT_BOOK_V2_EXPANDED.json): (1) tournament build_path_dataset `_m1_file_for` searches only bridge_ftmo_m1_* — must also glob bridge_ftmo_ext_m1_* so new-symbol paths reconstruct; (2) combined frame double-counts Sept+Feb days for the original 24 symbols (universe_ext_probe overlaps train_ledgers_scale4) — combined book must drop probe rows whose symbol is in the 24-surface. FIX BOTH then rebuild combined book. Then: pullback-entry + velocity-time-stop contract integration (generators entry_reference offset knob analogous to stop-width scale; ExitPolicy per-segment time stops from ULTIMATE_ENTRY_VELOCITY_STUDY), full-universe re-materialization with pullback entries, final book under BH, governor calibration, sealed gate.

### 2026-06-12 BRANCH VERDICT — hand-designed generators have no certifiable edge at this scale
- Pullback book (313 cells, TRAIN-only, missed-as-zero EV): BH per-cell survivors = 0; pre-registered top-15-by-t PORTFOLIO on validation = −0.025/candidate, t=−0.61, FAIL (7 cells positive / 7 negative / 1 zero — coin-flip persistence). `ULTIMATE_PULLBACK_BOOK_V1.json`.
- Accumulated honest gates across the program: selection AUC ceiling 0.53 (linear+GBM+pd features); exit realizable gains +0.03..0.07R/trade vs oracle +0.62 (M1-unreachable); geometry argmax scale-4 still mean-negative; segment persistence at noise level. **Every TRAIN edge shrinks toward zero out-of-time.** Per research doctrine this branch's claim is now: the 7 April-era hand-designed origin families on the 24/46-symbol surface contain no deployable mechanical edge provable at 1 year of M1 data.
- **PIVOT (all-in continues): mine the GENERATION layer from data** — the only layer never learned. Origin-discovery program: conditional forward-return mining over the full-year M15/M1 universe (predecision feature buckets × horizon grid, TRAIN-only, BH across all mined cells, effect-size floors, then out-of-time confirmation; sealed days untouched). Hand-designed setups are the hypothesis space that failed; the data proposes the next one.
- Full-year 46-symbol univ materialization continues in background (train_ledgers_univ/) — its frame still feeds the miner with maximal breadth.
- 2026-06-12 MINED ORIGIN LIVE IN GENERATION: range_extreme_reversion implemented default-off (1.0xATR drift-matched stops, scale-exempt), probe 1,958 cands/day. Full-year 46-symbol materialization RESTARTED with mined family included (train_ledgers_univ/, ~10-12h: ~3,400 cands/day total). ON COMPLETION: frame build → relabel (note: tournament table may need reversion-family segments — rerun tournament on univ ledgers first) → segment/portfolio book with reversion cells → walk-forward gate → if reversion book certifies: governor calibration on book stream → scheduler ranking → forward-claim pre-declaration → ONE-SHOT SEALED gate. Velocity doctrine: reversion family is H=4-32 bars (1-8h) — time-stop calibration from mined horizon structure.
- 2026-06-12 RECONCILIATION VERDICT (ULTIMATE_REVERSION_TIME_EXIT_TEST.json): mined reversion drift does NOT survive order-level path accounting even under its own time-exit/wide-stop design — 3/339 cells at t>=2 on TRAIN (below chance), validation mixed. Causes: multi-day range-state persistence inflates day-clustered t (days not independent); bid-close bounce at extremes. ORDER-LEVEL TRUTH WINS. Third hypothesis space closed honestly (hand-designed setups; per-candidate selection; M15 bar-drift cells). Consistent root across all: retail spreads vs M15-scale move sizes. NEXT LEVER: D1-horizon mining — spreads are negligible at D1 ATR scale and bridge D1 history spans years (real power). Probe D1 depth, then run the miner at D1 resolution over maximal history with episode-clustered (not day-clustered) statistics.
- 2026-06-13 DEEP-HORIZON MINE V1: H4 over 2022-2026 (46 symbols, 34,600 state-change episodes — episode semantics ENFORCED): 184/486 BH survivors, 127/183 (69%) same-sign out-of-time. D1 underpowered. Swaps captured into ULTIMATE_SYMBOL_SPREAD_SNAPSHOT.json. NEXT (resume here): H4 order-level reconciliation — entry at state change, time exit at horizon, wide stop, costs = spread + swap_per_night x nights held; if top cells survive → H4 origin family build (default-off) → univ pipeline → book → gate. If not → record fourth honest closure and the program's cost-barrier conclusion stands at all tested resolutions.
- 2026-06-13 H4 RECONCILIATION (ULTIMATE_H4_RECONCILIATION_V1.json): full 51-cell portfolio FAILS validation (−0.27/trade, t=−6). BUT coherent sub-family holds: METALS pos=high CONTINUATION at H=30 H4 bars (5d) — train +0.83..+1.16 net of spread+swap, val +0.38..+1.83 across 6 qualifiers (val n 11..195). REGIME CONFOUND SUSPECTED (gold bull in both periods). NEXT on resume: (a) baseline test — do these cells beat always-long-metals on identical bars? (b) sub-period sign stability 2022/2023/2024/2025 (gold fell in 2022H2); (c) short-side symmetry. Only if all three pass → metals-continuation H4 origin family build → univ pipeline → book → gate.
- 2026-06-13 REGIME VERDICT (ULTIMATE_METALS_REGIME_CONFOUND_TEST.json): metals continuation FAILS the pre-registered gate — always-long baseline +0.84/episode vs conditioned +1.11 (increment +0.27 overall, ~0 in 2022-2023); short side sign-flips with regime. The family is long-gold beta, not structure. FOURTH honest closure.
- **PROGRAM CONSOLIDATED FINDING (all-in cycle complete): across M15 trade-level, M15 bar-drift, H4/D1 deep-history mining — every apparent edge died under honest accounting exactly in proportion to its honesty (costs, episode independence, regime baselines). No certifiable mechanical OHLC-state edge exists on this 46-symbol CFD universe at retail costs. The only persistent profit factor found anywhere is regime exposure (beta).** Remaining honest directions (owner decision): different data classes (order flow/positioning/fundamentals), different cost venues (futures), or explicit regime-following with its known beta character under the ruin governor.

### 2026-06-13 CORRECTION + MICROSTRUCTURE PIVOT (owner challenge: "no way no edge survives")
- SELF-CORRECTION: every prior study (V1-V4 selection, exits, geometry, books, M15/H4/D1 mines) conditioned on PRICE GEOMETRY ONLY. Two layers were available the whole time and never used: (1) `volume` (tick_volume) is a column in EVERY exported bar; (2) the bridge serves full tick data (bid/ask/ms/flags/volume) free — verified 9,198 XAUUSD ticks/2h, ~2yr depth on liquid symbols. "No mechanical edge" was really "no edge in the most-arbitraged layer (price geometry)". The market-maker-wave layer was untested.
- FIRST MICROSTRUCTURE PROBE (ULTIMATE_MICROSTRUCTURE_MINE_V1.json, H4 vol+sweep, REGIME-BASELINED edge-over-beta): 356 cells, 19 BH survivors, 9/19 same-sign out-of-time. NEW persistent family price-only missed: jpy_fx downtrend + range-low + DRY volume -> long (exhaustion reversion), train edge +1.55R over beta (t=3.76), val +1.47R (n=94/15); plus higher-n jpy short-at-highs +0.20R edge (t=3.19, n=1067/215, val +0.41R). Metals "shorts" correctly exposed as negative-edge beta artifacts by the baseline. Not a system yet — first proof the untested layer is alive.
- VISION (reaffirmed with owner): the ultimate system rides participation/liquidity waves (volume surge, absorption, stop-runs) with geometry+cost matched to move scale and DYNAMIC volume-exhaustion exits — not candle prediction. "Retail cost" = spread+swap; it is a FUNCTION of stop distance/timing, not a fixed tax (0.17R at 0.25xATR stops vs ~0.04R at 1xATR vs negligible on high-participation expansion). The generators traded the worst moments (tight stops on low-participation noise) — guaranteeing the cost dominated.
- PLAN: Wave A microstructure bar features (vol delta proxy, absorption, sweep+reclaim, spread-regime) — IN PROGRESS. Wave B tick-truth promotion (aggressor flow, true per-moment spread/cost) on liquid symbols. Wave C dynamic wave-exhaustion exits (exit-oracle bound +0.72R is the ceiling; volume is the missing exit signal). Wave D forward validation on the 2 existing CFD challenge accounts (real fills = final truth; no expensive API until first payouts). MT5-only data throughout.

### 2026-06-13 MICROSTRUCTURE ENGINE — DEVELOPED BOOK (the volume layer pays)
- Real costs (ULTIMATE_REAL_COST_MAP: fx .16/jpy .11/index .064/metals .046/energy .037) + volume setups (stop-run, dry-exhaustion, participation-break, absorption, volume-delta-divergence) + dynamic exits (fixed/vol_exhaust/atr_trail) + regime baseline + episode semantics + sub-period stability.
- H4 (ULTIMATE_MICROSTRUCTURE_ENGINE_H4.json): 42,415 episodes, 564 cells, 255 BH survivors, **126-setup developed book** with edge>0 AND out-of-sample same-sign AND sub-period stability>=0.5. 150 positive-OOS overall. Strongest by robustness: index/S5_vdelta_divergence/short (n=712/193, edge +0.49R, t=3.97, stab 1.0, val +0.79R); crypto/S4_absorption_reversal/long (n=196/111, stab 1.0, val +0.58-0.99R); jpy/S5_vdelta_divergence (edge +0.90R, val +0.59R). FIRST positive developed book in the program after price-only dead ends.
- IN FLIGHT: M15 engine; bounded tick export (XAUUSD/EURUSD/USDJPY/XAGUSD, 2025-10..2026-04) for Wave B order-flow truth.
- NEXT: adversarial verification harness (bootstrap CI, per-symbol decomposition, sub-period table, cost stress 1.5x/2x, beta-robust) on top candidates -> workflow adversarial reasoning -> go-live candidate book -> default-off origin generators -> timewarp order-level -> governor -> challenge-account forward test. DEVELOP, don't nuke.

### 2026-06-13 WAVE B (gold ticks) — cost truth + feed-type honesty
- 32.2M XAUUSD ticks (2025-10..2026-04): FTMO CFD tick feed is QUOTE-ONLY (bid/ask; volume=last=0). HONESTY REFINEMENT: bar `volume` = tick/quote COUNT (participation intensity), not traded volume; absorption/vdelta are price x quote-intensity proxies — legitimate and live-computable, but NOT literal order flow. Mechanism reframed accordingly.
- REAL gold spread: median 0.37 (0.78bps), p99 1.17. On 2.5xATR microstructure stops -> round-trip cost ~0.012-0.023R, ~10x BELOW the validated metals edge (+0.19R). The flat cost map (0.046R, measured on the OLD 0.25xATR tight stops) overcharges the wide-stop setups ~3-4x further. COST-REALISM RISK RETIRED for metals/gold; FX (wider rel spread, 0.16R map) still to confirm with EURUSD ticks (export resumes post-validation).
- Confidence update: metals/gold microstructure leg cost cushion is decisive; this raises the order-level-survival prior for the metals anchor specifically.

### 2026-06-13 order-level validation infra note
- train_ledgers_micro disk bounded via upgraded janitor: per-day CANDIDATE_MICROSCOPE filtered to microstructure-only rows (grep, idempotent, .microfiltered marker) + all non-reconciliation kinds pruned (sidecar/scorecard/order/trade/winner/loser/exit/rollup/asof/event/account). Candidate 14G->3.4G; total ~3.7G; projected ~11G for 234 days.
- CONSEQUENCE: order-level reconciliation regime-baseline (non-micro paths) is empty -> harness reports RAW realized R after real per-asset-class cost (beta=0). This is the account-truth number; interpret vs the upstream trend-conditional-beta-adjusted edges (already established in ULTIMATE_MICROSTRUCTURE_VERIFICATION) to catch drift inflation. Raw order-level R > ~0.1R with sign matching the beta-adjusted edge = clean fills; raw >> beta-adjusted = drift suspicion.
