GTOS go-live Phase A patch 0001 — DISABLE the broad losing selector_v4.

WHY: ULTIMATE_GO_LIVE_DOSSIER.md §8.1 + PORTFOLIO_BUILD_W7_FINAL.md — the native broad
V4 selector loses live (-0.25R/fill, -113.4R over 454 fills, negative every month;
confirms hard-halt history). Go-live is a NARROW REPLACEMENT (the clean_3 book through
the new sizing surface), NOT an augmentation. This patch flips the broad selector's
triple-gate OFF so it cannot drive execution.

STATUS: STAGED — DO NOT auto-apply. The human reviews and applies with:
    git apply research/operations/final_moonshot_v4_ultimate_mechanical_edge_2026_06_10/GOLIVE_vps_deploy/patches/0001-disable-broad-selector-v4.patch
Then re-run the runtime/selector tests. Reversible: `git apply -R` the same file.

This flips all three gate keys to false (enabled AND apply_to_execution AND
live_activation_allowed) so the disable is unambiguous regardless of which gate the
runtime reads. The remaining selector_v4_* tuning keys are left in place (inert once
the gates are off) so the diff is minimal and easy to audit/revert.

--- a/config/agent_config.yaml
+++ b/config/agent_config.yaml
@@ -737,9 +737,12 @@
   # Selector V4 is the active broker-net admission authority after Wave3.5.
   # Runtime halt files remain the physical deployment control; these flags make
   # V4 the local final-say authority when the runtime path is exercised.
-  selector_v4_enabled: true
-  selector_v4_apply_to_execution: true
-  selector_v4_live_activation_allowed: true
+  # GO-LIVE Phase A (2026-06-15): the broad V4 selector is the proven-losing
+  # surface (-0.25R/fill). Disabled as a STANDALONE REPLACEMENT by the clean_3
+  # ultimate_book deploy. Triple-gate OFF; tuning keys left inert below.
+  selector_v4_enabled: false
+  selector_v4_apply_to_execution: false
+  selector_v4_live_activation_allowed: false
   selector_v4_min_broker_net_trade_ev_r: 0.10
   selector_v4_min_no_trade_ev_r: 0.02
   selector_v4_min_confluence_score: 0.15
