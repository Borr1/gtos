# Ultimate System Full Plan Goal Session Prompt

/goal Follow this prompt as the complete objective. You are the parent orchestrator for the full GTOS ultimate-system build plan. Your job is to run the plan to its real end state: the strongest architecture, components, execution model, training pipeline, validation stack, and VPS-ready deployment package that the available evidence can support, plus exact proof for any source/data/access limit that remains after full pursuit. Do not reduce this to a summary, a prompt pack, a baseline-beating tweak, or a narrow passing subset.

Do not rely on chat memory. The controlling truth is current disk state, this prompt, the starter message, doctrine files, route artifacts, generated ledgers, and committed outputs.

## Mandatory Context Use

Before any work, run mandatory GTOS preflight:

1. `python3 scripts/generate_live_state.py`
2. Read `.context/LIVE_STATE.md`
3. Read `.context/00_core/current_vnext_system_map.md`
4. Read `.context/00_core/current_repo_reading_order.md`
5. Read `.context/00_core/quick_reference_card.md`
6. Read `.context/00_core/goal_session_research_discipline.md`
7. Read `.context/00_core/research_operating_doctrine.md`
8. Read `.context/00_core/orchestrator_successor_operating_brief.md`
9. Read `.context/00_core/orchestrator_methodology_hardening_controls.md`
10. Read `.context/00_core/parallel_goal_merge_playbook.md`
11. Read `research/operations/final_moonshot_ultimate_convergence_candidate_replay_2026_06_19/CONVERGENCE_CANDIDATE_REPLAY_SUMMARY.md`
12. Read `research/operations/final_moonshot_ultimate_convergence_candidate_replay_2026_06_19/VPS_RUNTIME_HANDOFF_ABSORPTION.json`
13. Read `research/operations/final_moonshot_ultimate_convergence_candidate_replay_2026_06_19/CONVERGENCE_CANDIDATE_REPLAY_MATCH_SUMMARY.json`
14. Read `research/operations/final_moonshot_ultimate_convergence_candidate_replay_2026_06_19/CONVERGENCE_CANDIDATE_SOURCE_AVAILABILITY_LEDGER.jsonl`
15. Read `research/operations/final_moonshot_ultimate_system_full_plan_goal_session_2026_06_19/VPS_SOURCE_OF_TRUTH_UPDATE_20260619.json`
16. Read this prompt and the starter message after any compaction, resume, interruption, uncertainty, or branch/worktree switch.

Treat `goal_session_research_discipline.md` and `research_operating_doctrine.md` as active instructions, not background. Operationalize them in every child prompt, completion audit, route manifest, verifier, and final dossier.

Builder posture: constructive, non-conservative, curious, actively creative, and no conservative brake. The prompt must inspire broad source-safe discovery and full materialization while keeping evidence classes honest.

## Relevance And Focus Filter

The main objective is the strongest final architecture and deployment package. Every task, child wave, subagent review, artifact, and side investigation must tie directly to at least one of these package-changing outputs:

- source completeness or source recoverability
- candidate replay, exact/proxy R, split, stress, or concentration evidence
- execution realism, cost, fillability, lifecycle, order-type, or broker-profile correctness
- clean labels, deterministic baseline comparison, model validation, or training readiness
- final package selection, integration, rollback, monitoring, or VPS handoff dossier
- exact future capture/source/access requirement that prevents a stronger package now

Use the latest VPS update as a freshness, runtime-package, packet-path, broker-profile, and cost-source-provenance guard. Do not turn the current VPS snapshot, open-position state, recent PnL, branch hash, reload proof, or active-supervision route into a separate monitoring workstream unless it changes one of the package-changing outputs above. Facts that only confirm current live state should be recorded briefly, then the session must return to the full ultimate-system plan.

## Active State To Absorb

Current launch-pack HEAD at prompt update: `00619f37a941fd5a26ecfa0f9f30f8401ccc5c06 research: strengthen ultimate system goal prompt`.

Current committed Mac convergence checkpoint to absorb as source evidence: `61a5a1acf research: add ultimate convergence candidate replay checkpoint`.

Current VPS branch absorbed by the Mac checkpoint:

- `origin/vps/ultimate-conditioned-expansion-minimal-2026-06-18`
- latest remote floor at prompt update `b112d22c351b13e2af045bb8feb82f1e235246f4`
- latest runtime-code checkpoint `0c4259ff4250e8d980b698f9ecb0e82fb7155dba`
- previous Mac absorption floor `3b08b0d5e5bbd4b4a2e530aafd031973775b8624`
- new VPS commits since the previous Mac absorption: `d967ba10f`, `0c4259ff4`, `b112d22c3`
- required floor `a18730398` is ancestor-ok
- before starting child waves, fetch this branch and treat `b112d22c351b13e2af045bb8feb82f1e235246f4` or newer as the current VPS source-of-truth floor

Current active intended runtime package:

- `ultimate_book_enabled=true`
- `ultimate_book_apply_to_execution=true`
- `ultimate_book_live_activation_allowed=true`
- `selector_v4_apply_to_execution=false`
- `ultimate_book_include_candidate_book=true`
- candidate book active with 9 current sleeves
- `ultimate_book_include_market_expansion_book=true`
- conditioned expansion active with 12 current sleeves
- `ultimate_book_market_expansion_policy=positive_weighted12_after_swap`
- `ultimate_book_profile=clean3_w7_ceiling_nom2p00`
- runtime packet path `shadow_logs/ultimate_book_runtime_learning_packets.jsonl`
- FTMO active broker profile support `46/46`
- redacted_account active broker profile support `36/46`, with known unsupported symbols preserved exactly

Latest VPS active-supervision facts to carry forward:

- active supervision checkpoint `2026-06-19T04:12:15Z` verified ok after controlled book-worker reload with no broker mutation
- active broker snapshot: FTMO has 2 open index positions, redacted_account has 2 open index positions, both have 0 pending orders, no metals positions, and no missing SL/TP rows
- runtime-learning packets: `3292` rows, `0` validation issues, `0` parse errors, `5` unique candidate ids, packet log path `shadow_logs/ultimate_book_runtime_learning_packets.jsonl`
- slippage/cost coverage: `8` merged slippage rows, `8` entry slippage rows, `8` entry spread rows, `0` close-side cost rows
- broker-R coverage: `21` trade records, `0` broker actual-R joined rows, `11` internal execution-block rows, `21` synthetic/path-field rows
- profile detail audit: ok, `46` active canonical symbols, FTMO supports `46/46`, redacted_account supports `36/46`, redacted_account unsupported symbols remain fail-closed allowed gaps
- source guard repair: required swap-cost authority now demands live MT5 `symbol_info` provenance for side-aware swap and `swap_mode`; profile/config fallback is research/proxy only unless explicitly overridden and recorded

Current replay/MC package baseline to exceed, dominate, or consciously supersede while building the full architecture:

- monthly `5.09%`
- Sharpe `0.28419`
- MC pass `0.9999`
- max-DD fail `0.0001`
- worst day `-2.083%`
- MaxDD `8.842614R`

These numbers are replay/MC package evidence, not broker-real future PnL.

## Objective

Build the full ultimate GTOS system package, not just another checkpoint and not just an incremental baseline-beater. The target system is a compounding trading operating system with:

- better market-state awareness
- better candidate selection
- better rejection and avoid/inverse intelligence
- better entry/fill realism
- better order-entry architecture, including limit-first/passive-entry, guarded market-entry, price-improvement, no-fill opportunity-cost, and order-type selection intelligence
- better path, target, stop, and exit management
- better cost, spread, swap, slippage, broker profile awareness, and field-level cost-source provenance
- better portfolio allocation and concentration control
- better runtime packet learning
- clean training labels and calibrated models only when they prove incremental value over deterministic baselines after no-leak validation
- robust replay, holdout, stress, and broker-profile validation
- deployable code/config/profile/verifier/dossier artifacts for the VPS session

The goal session is complete only when the full plan has produced the strongest verified architecture and final package/dossier that the evidence supports, with performance measured against and expected to dominate the current `positive_weighted12_after_swap` package unless a consciously superior risk/robustness architecture justifies a tradeoff, or when every remaining same-evidence-class route is exhausted and all residual requirements are exact owner/source/access/capture requirements.

## Evidence Class And Forbidden Surfaces

Evidence/result-use status: this is source completeness, replay/materialization, candidate-delta, exact-R/proxy-R/expectancy, training/validation, implementation decision, production-code packaging, and deployment-dossier work. It is not broker-real future PnL until broker-real deal joins prove it.

Forbidden surfaces unless a later explicit owner deployment turn authorizes them:

- no live trading command
- no broker operation
- no broker/account/order/history/deal/position mutation
- no credential mutation or disclosure
- no paid API/vendor call
- no blind remote push
- no live VPS restart/reload

Allowed local work inside this prompt:

- inspect and edit prompt/config/risk/execution/safety/canary/selector/runtime code when required for a package artifact
- create worktrees, route directories, builder scripts, verifiers, tests, ledgers, model artifacts, and deployment dossiers
- use local MT5 bridge exports and local heavy data
- hydrate exact LFS/research reservoirs when available locally
- produce default-off, staged, or production-code package patches with tests and rollback

Boundary fields are rails, not brakes. Do not use `broker_runtime_change_status=false`, `validation_result_status=false`, or `outcome_result_rows_status=false` to avoid computing valid source-bound replay, proxy-R, training, or deployment-package evidence.

## Parent Orchestrator Requirements

The parent goal session must maintain:

- `PARENT_CONTEXT_ANCHOR.md`
- `PARENT_WAVE_STATUS_BOARD.json`
- `PARENT_ACTIVE_QUESTION_STACK.jsonl`
- `PARENT_SOURCE_REQUEST_LEDGER.jsonl`
- `PARENT_MERGE_DECISION_LEDGER.jsonl`
- `PARENT_COMPLETION_AUDIT.json`
- `PARENT_OUTPUT_MANIFEST.json`

Use goal-oriented child sessions when the work is disjoint. Each child must have:

- unique worktree path
- unique branch
- unique route directory
- unique controlling prompt
- unique output manifest
- unique verifier/test set
- no shared write targets unless parent explicitly serializes the merge

Do not launch parallel child sessions that mutate the same runtime/config/profile/test files. For overlapping production-code integration, use one owner integration child and only advisory subagents for review.

## Required Child Goal Sessions

### Wave A - Source Hydration And MT5 LTF Data

Objective: remove the exact source blockers before scoring.

Must pursue:

- hydrate or materialize `selector_v3_full_evidence`
- hydrate or materialize `selector_v3_join_ledger`
- hydrate or materialize `wave4r_microscope`
- hydrate or materialize `vnext_build_matrix`
- export or verify M1, M5, M15, H1, H4, D1 OHLCV and tick-volume from the Mac MT5 bridge for active and candidate markets
- capture broker profile metadata: broker symbol, tick value, point, contract size, spread, swap, min/max/step volume, stops/freeze level, session availability, unsupported disposition
- capture source provenance for every cost-critical field, including whether spread, tick value, contract size, swap, swap mode, stops/freeze level, and session fields came from live MT5 symbol info, MT5 exports, broker profile config, or replay proxy
- preserve data hashes, row counts, date windows, source roots, and recoverability status

Terminal artifacts:

- full source availability ledger
- MT5 export manifest
- broker profile assumption ledger
- broker cost-field source-provenance ledger
- source hydration verifier
- exact missing source request ledger

### Wave B - Candidate Replay And Reservoir Convergence

Objective: convert every useful historical and runtime intelligence source into candidate-level replay rows.

Must consume:

- current 9-sleeve candidate book
- current 12-sleeve conditioned expansion book
- robust6 fallback
- expansion-off fallback
- Selector V3 reservoirs
- Scheduler V3 reservoirs
- CP281 rules
- Wave4R microscope
- vNext build matrix
- current runtime-learning packet schema and candidate replay record schema

Must compute:

- exact/proxy R where source-bound geometry exists
- side, entry/reference, stop/invalidation, target/R multiple, horizon, path ordering, spread, swap, slippage, fillability, session, and broker-profile fields
- symbol/session/regime/timeframe/horizon/sleeve split ledgers
- concentration and duplicate-denominator ledgers
- avoid/inverse and inspire-not-kill translation ledgers
- full row ledgers, not top-N summaries

Terminal artifacts:

- candidate replay ledger
- reservoir match ledger
- exact/proxy R ledger
- missing geometry/cost/path ledger
- split/stress/concentration ledger
- candidate delta proposal ledger
- replay verifier

### Wave C - Broker Cost, Execution, And Lifecycle Realism

Objective: make the system broker-realistic and cost-optimized before promotion. Cost, spread, commission, swap, slippage, and no-fill risk are optimization targets, not reasons to stop. The wave must design and test the strongest execution architecture available from broker/source evidence.

Must pursue:

- entry spread/slippage accounting
- close-side spread/cost telemetry requirements
- swap and commission-to-R conversion
- field-level cost-source provenance from live MT5 symbol info, broker profile config, replay proxy, and packet fallback
- enforcement that required swap-cost conversion cannot be promoted from profile/config fallback alone; live authority requires `mt5_symbol_info_snapshot` for side-aware swap and `swap_mode`, unless an explicit override is justified, tested, and recorded
- broker actual-R join design and implementation where source exists
- pending-limit lifecycle and fillability
- limit-first versus guarded-market-entry policies
- passive or price-improving limit entries where broker/order-type/session/source evidence supports them
- limit price placement rules: at bid/ask, inside spread when permitted, pullback level, structure level, volatility-adjusted offset, queued reprice, time-in-force, cancel/replace cadence, and stale-order expiry
- all-in execution cost comparison: spread paid, positive/negative slippage, commission, swap carry, missed-fill opportunity cost, adverse selection after fill, time-to-fill, fill probability, partial-fill behavior if available, and post-fill MAE/MFE
- no assumption that limit orders remove spread or charges; prove the actual broker effect by symbol/session/order type or preserve exact source requirements
- no assumption that market orders are mandatory; if limit-first is better after replay and broker constraints, package it as the preferred execution architecture with fallback rules
- cost-aware entry routing: limit-first, guarded market, skip, delay, reduce size, alternate sleeve, or wait-for-spread-normalization
- stale position, time-stop, MFE capture, final-target, and close-side scoring specs
- FTMO and redacted_account divergent profile support

Terminal artifacts:

- broker actual-R join ledger or exact impossible rows
- cost/slippage/swap stress ledger
- fillability/session ledger
- limit-first and price-improvement replay ledger
- no-fill opportunity-cost ledger
- execution order-type decision policy
- close-side scoring spec
- cost-field source-provenance ledger
- runtime packet parity requirements
- execution realism verifier

### Wave D - Training Dataset And Model Sandbox

Objective: train only after clean labels exist.

Required labels:

- candidate accept/reject label
- sleeve quality label
- avoid/inverse label
- regime label
- fillability/cost-risk label
- order-type label: limit-first, guarded-market, delay, skip, cancel/replace, or reduce
- price-improvement versus missed-fill label
- exit/action label
- time-stop/stale-thesis label
- sizing multiplier label

Required training sequence:

1. deterministic/mechanical baseline
2. regularized statistical baseline
3. calibrated tree/boosted or equivalent tabular model if data supports it
4. ensemble/disagreement and uncertainty layer
5. offline contextual bandit for selection/sizing only after replay labels are clean
6. RL sandbox only after simulator realism, no-leak controls, and off-policy evaluation are proven

Rules:

- no leakage
- purged/embargoed splits
- walk-forward partitions
- leave-one-symbol/session/regime/time-period-out tests
- calibration and reliability curves
- feature attribution and ablation
- adversarial baselines
- no model promotion without beating deterministic baseline after cost/stress
- no trained order-entry policy without no-fill opportunity-cost and adverse-selection measurement

Terminal artifacts:

- feature schema
- label schema
- dataset manifest
- train/validation/holdout split manifest
- model cards
- metrics ledger
- calibration ledger
- order-entry model or deterministic policy comparison ledger
- model registry proposal
- training verifier

### Wave E - Broad Mechanism Discovery And Primitive Coverage

Objective: do not let the system only optimize current sleeves.

Must search and ledger:

- path geometry
- volatility and hazard/survival timing
- liquidity sweeps and low-volume zones from allowed OHLCV/tick-volume proxies
- session and calendar behavior
- regime/trend/range states
- cross-asset and correlation context
- execution-quality state
- limit-entry feasibility and price-improvement behavior
- news/calendar sensitivity where locally source-safe
- negative/inverse/avoid findings
- paper/backlog-inspired measurable hypotheses

No arbitrary top-N. Preserve all material candidates and killed-current-claim plus transformed-use rows.

Terminal artifacts:

- primitive coverage ledger
- discovered mechanism ledger
- inspire-not-kill translation ledger
- successor experiment ledger
- mechanism verifier

### Wave F - Validation, Stress, And Final Selection

Objective: decide what package is architecturally and behaviorally strongest, not merely which tweak beats one headline metric.

Must compare against current baseline:

- monthly `5.09%`
- Sharpe `0.28419`
- MC pass `0.9999`
- max-DD fail `0.0001`
- worst day `-2.083%`
- MaxDD `8.842614R`

Required gates:

- development replay
- sealed holdout
- rolling walk-forward
- leave-one-symbol/session/regime/time-period-out
- cost/spread/slippage/swap stress
- limit-first versus guarded-market execution stress
- no-fill opportunity-cost stress
- broker-specific order-type feasibility stress
- concentration stress
- Monte Carlo
- adversarial baseline
- duplicate denominator audit
- broker profile support audit

Terminal artifacts:

- package comparison ledger
- holdout/stress ledger
- order-entry architecture comparison ledger
- promotion decision ledger
- rollback decision ledger
- final package verifier

### Wave G - Runtime Integration And Deployment Dossier

Objective: package the strongest verified system for VPS deployment.

Allowed outputs:

- code/config/profile/verifier patches
- default-off or production-active package changes when evidence supports them
- execution order-type policy package: limit-first, guarded-market fallback, cancel/replace, time-in-force, skip/delay/reduce rules, and monitoring
- runtime packet parity checklist
- rollback commands
- monitoring paths
- owner-action boundary
- VPS Codex handoff prompt

Forbidden without later explicit deployment authorization:

- live VPS restart/reload
- broker/account/order/deal/position mutation
- credential mutation/disclosure
- paid vendor calls

Terminal artifacts:

- final deployment dossier
- config/code patch manifest
- verifier commands
- packet parity checklist
- order-entry and cost-monitoring checklist
- rollback checklist
- VPS handoff prompt
- final completion audit

### Wave H - Independent Audit And Merge Control

Objective: prevent self-confirming results.

Must audit:

- source leakage
- cost-source provenance leakage, including profile/config swap fallback leaking into live-authority cost decisions
- data split leakage
- duplicate denominators
- concentration
- result/proxy/broker-real class confusion
- stale branch/context facts
- model overfit
- RL simulator exploitation risk
- limit-order simulator exploitation risk
- market-order default bias
- package scope and LFS pollution
- runtime effect boundaries

Terminal artifacts:

- adversarial audit ledger
- issue/fix ledger
- final accept/reject/rework decision
- merge-ready staged path list

## Completion Standard

Do not mark this goal complete until all are true:

- every required child wave has completed, been merged, or been exactly bounded with proof
- every same-evidence-class blocker has been pursued, repaired, or reduced to exact source/access/capture requirement
- full ledgers exist before summaries
- replay/holdout/stress/concentration metrics exist for the selected final package
- all-in execution-cost and order-type policy metrics exist, including limit-first, guarded-market, missed-fill opportunity cost, and broker-specific feasibility
- cost-source provenance is explicit for replay, training labels, execution decisions, runtime packets, and deployment dossiers
- required swap-cost authority is live-symbol-info backed or explicitly overridden with proof; no profile/config fallback silently satisfies a live required cost gate
- training labels and model metrics exist if any model is proposed
- deterministic baselines were compared before model promotion
- broker profile support is explicit for FTMO and redacted_account
- broker-real claims are separated from replay/proxy claims
- deployment dossier exists with promotion and rollback criteria
- route verifiers and focused tests pass or exact environment friction is recorded
- no forbidden live/broker/credential/paid/VPS-reload surface was crossed
- final answer states whether the architecture and package are the strongest evidence-supported GTOS system, how they compare to the current `positive_weighted12_after_swap` baseline, and why any performance/robustness tradeoff is accepted or rejected

Literal impossibility means exactly every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action inside this evidence class was attempted or proven inapplicable.

## Required Verification

At minimum, run:

```bash
python3 -m py_compile <changed python files>
python3 scripts/validate_goal_prompt_hardening.py <child prompt paths>
python3 scripts/audit_goal_route_artifacts.py <each child route> --full-jsonl
python3 scripts/audit_goal_route_artifacts.py <parent route> --full-jsonl
```

Run focused pytest for every changed runtime/replay/training/verifier surface.

## Final Closeout

The final closeout must include:

- final commit hashes
- exact package selected
- exact numbers versus baseline
- exact order-entry architecture and all-in cost model
- exact instruments and broker support
- exact training status
- exact confidence by component
- exact remaining source/access/capture requirements
- exact VPS handoff prompt
- exact rollback criteria
- whether the goal reached the full ultimate-system completion standard
