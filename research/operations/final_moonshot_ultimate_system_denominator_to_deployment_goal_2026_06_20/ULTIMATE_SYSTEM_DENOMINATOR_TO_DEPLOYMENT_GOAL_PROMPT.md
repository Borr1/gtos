# Ultimate System Denominator-To-Deployment Goal Prompt

/goal Follow this prompt as the complete objective. You are continuing the GTOS ultimate-system build from the current disk state, not from chat memory. Your job is to drive the system from the current final-package sleeve checkpoint through exact denominator repair, clean labels, final package selection, runtime integration, independent audit, and VPS-ready deployment handoff. Do not stop at another summary, blocker packet, prompt pack, or conservative "not ready" label when same-evidence-class repair is still possible.

This is a builder/repair/validation/production-code-package lane. The posture is constructive, aggressive, source-bound, and non-conservative. Build the strongest auditable system the evidence can support. Evidence-class boundaries are rails against fake claims, not brakes against computation.

## Mandatory Preflight And Context

Before doing any route work:

1. Run `python3 scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/current_vnext_system_map.md`.
4. Read `.context/00_core/current_repo_reading_order.md`.
5. Read `.context/00_core/quick_reference_card.md`.
6. Read `.context/00_core/goal_session_research_discipline.md`.
7. Read `.context/00_core/research_operating_doctrine.md`.
8. Read `.context/00_core/orchestrator_successor_operating_brief.md`.
9. Read `.context/00_core/orchestrator_methodology_hardening_controls.md`.
10. Read `.context/00_core/parallel_goal_merge_playbook.md`.
11. Read this prompt and the starter message after every compaction, resume, interruption, uncertainty, or worktree/branch switch.

Treat the doctrine files as active instructions, not background. The route completion audit must state how this lane operationalized builder posture, anti-boxing, same-evidence-class pursuit, no arbitrary top-N, and proof-or-impossibility.

## Current Disk Anchors To Verify

Verify these from disk before relying on them. They are the expected current state, not permission to skip verification.

- Worktree: `/Users/borr/Documents/gtos/repo/ai-trading-agent-ultimate-convergence-20260619`
- Branch: `research/ultimate-convergence-candidate-replay-2026-06-19`
- Current launch-pack HEAD: verify from disk; expected latest launch-pack commit message is `research: add denominator to deployment goal prompt`
- Package base checkpoint to continue from: `a5ba3e9afe6a423c149af8eef17c955dd3fbf655`
- Package base checkpoint message: `research: bridge lifecycle fillability sleeves`
- Parent route: `research/operations/final_moonshot_ultimate_system_full_plan_goal_session_2026_06_19`
- Latest package routes:
  - `research/operations/final_moonshot_ultimate_system_final_package_synthesis_2026_06_19`
  - `research/operations/final_moonshot_ultimate_system_final_package_acceptance_compression_2026_06_19`
  - `research/operations/final_moonshot_ultimate_system_final_package_lifecycle_fillability_bridge_2026_06_19`

Read at minimum:

- `PARENT_COMPLETION_AUDIT.json`
- `PARENT_WAVE_STATUS_BOARD.json`
- `PARENT_ACTIVE_QUESTION_STACK.jsonl`
- `PARENT_VERIFICATION_RESULT.json`
- `PARENT_LIFECYCLE_FILLABILITY_BRIDGE_VERIFICATION_RESULT.json`
- `FINAL_PACKAGE_SYNTHESIS_SUMMARY.json`
- `FINAL_PACKAGE_ACCEPTANCE_COMPRESSION_SUMMARY.json`
- `FINAL_PACKAGE_LIFECYCLE_FILLABILITY_BRIDGE_SUMMARY.json`
- all residual/source-gap ledgers in the three package routes

The previous long goal session created one known route-local timestamp-only dirty file:

- `research/operations/final_moonshot_ultimate_system_full_plan_goal_session_2026_06_19/PARENT_LIFECYCLE_FILLABILITY_BRIDGE_VERIFICATION_RESULT.json`

Treat timestamp-only verifier churn as informational unless the route needs to commit a refreshed verifier result. Do not stage unrelated dirty files:

- `.context/LIVE_STATE.md`
- `research/operations/final_moonshot_v4_kiap_ultimate_system_repair_2026_06_07/ULTIMATE_UNTOUCHED_DYNAMIC_SOURCE_MANIFEST.json`
- `src/__init__.py`
- `src/components/ultimate_book/__init__.py`
- `tests/__init__.py`

If Git hits `fatal: mmap failed: Resource deadlock avoided`, do not weaken the work. Use direct file scans, scoped staged-path checks, route-local verifiers, and Git plumbing only when necessary, recording the exact fallback.

## Current Verified Package State

Current package evidence is strong, but not deployable yet.

Expected current facts to verify:

- Full shortlist: `1,101` package rows.
- Compressed package: `82` deterministic sleeve candidates.
- Sleeve members: `1,101`.
- Main engine: `20` scheduler-lifecycle merge sleeves carrying most of the preserved discovery signal.
- Direct promote sleeves: `3`, currently small compared with scheduler-lifecycle merge sleeves.
- Redesign sleeves: `24`.
- Avoid/failure-feature sleeves: `27`.
- Source-required hold sleeves: `8`.
- Preserved combined source-bound discovery signal: `1,249,248.03066685R`.
- Candidate-level source-bound R: about `285,719.394083207R`.
- Selector lift: about `725,896.086267422R`.
- Scheduler result R: about `237,632.550316221R`.
- Lifecycle bridge rows: `19,216`.
- Lifecycle labels: `877`.
- Context-bridged sleeves: `73 / 82`.
- Context-bridged members: `730 / 1,101`.
- Exact denominator joins: `0`.
- Training-ready clean labels: `0`.
- Ticket-bound broker actual-R rows: `1`.
- Close-side all-in cost rows: `1`.
- Remaining close-history rows: `8`.
- `final_package_selected=false`.
- `model_training_allowed=false`.
- `live_execution_activation_allowed=false`.
- Wave H independent audit previously required rework before final package.

Do not misread the `1.249M R` as broker-real deployable expectancy. It is a preserved source-bound discovery signal across overlapping evidence classes. The job is to convert it into a selected, executable, cost-aware, validated, deployable package without losing the edge and without faking live expectancy.

## Objective

Reach the strongest evidence-supported GTOS ultimate system package.

The final package should be a compact integrated architecture, not thousands of unmanaged bots. Current evidence points toward one GTOS kernel with a scheduler-lifecycle core, likely centered on the `20` scheduler-lifecycle merge sleeves, with any promote/redesign/avoid/source-required sleeves used only according to evidence. Do not assume this architecture is final; prove, improve, compress, expand, merge, redesign, or reject sleeve components from source-bound evidence.

The goal is complete only when one of these is true:

1. A final package is selected, verified, audited, integrated into code/config/profile/verifier artifacts, and packaged for VPS deployment with rollback and monitoring; or
2. Every same-evidence-class path to a final package has been exhausted, and the remaining requirements are exact source/access/capture requirements with row-level proof.

Completion requires work, not wording.

## Evidence Class And Forbidden Surfaces

Evidence/result-use status: source completeness, exact/proxy R, replay/materialization, denominator repair, clean-label construction, deterministic baseline comparison, model-training readiness, final package selection, production-code packaging, execution-cost architecture, deployment dossier, and VPS handoff. RESULT_MATERIALIZATION_REQUIRED.

Allowed local work:

- create route directories, builders, verifiers, tests, ledgers, manifests, prompt packs, and deployment dossiers;
- inspect and edit local code/config/profile/verifier/launcher surfaces when required for a package artifact;
- use local MT5 bridge exports and local heavy data;
- selectively hydrate exact LFS/research reservoirs when available locally;
- search sibling worktrees and route artifacts;
- create default-off, staged, or production-code package patches with tests and rollback criteria;
- use goal-oriented child sessions only for disjoint write scopes.

Forbidden surfaces unless a later explicit deployment turn authorizes them:

- no live trading command;
- no broker operation;
- no broker/account/order/history/deal/position mutation;
- no credential mutation or disclosure;
- no paid API/vendor call;
- no blind remote push;
- no live VPS restart/reload.

Read-only local evidence extraction, MT5 bridge export, route artifact hydration, code/config patching, verifier building, and deployment dossier creation are allowed.

## Non-Negotiable Research Posture

- No chat memory reliance.
- No arbitrary top-N or top 3/5/10 closure. Full ledgers preserve all material rows.
- No conservative brake. Gates prevent fake promotion; they do not prevent repair, computation, training preparation, code packaging, or final selection once evidence supports it.
- No "blocked" final answer when same-evidence-class repair remains possible.
- No "needs forward data" if sealed historical replay, MT5 exports, route artifacts, or read-only local sources can answer the question.
- No killing useful mechanisms. Unsupported current claims must be translated into feature, veto, sizing hint, risk cap, regime label, source requirement, default-off candidate, watch item, or successor experiment.
- No final live/broker-real expectancy claim from proxy/replay evidence.
- No model training before clean no-leak labels and deterministic baselines.
- No production deployment dossier without exact package selection, cost/source provenance, validation, rollback, monitoring, and Wave H audit.

Literal impossibility means exactly every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action inside this evidence class was attempted or proven inapplicable.

## Required Route Directory

Create a new route directory:

`research/operations/final_moonshot_ultimate_system_denominator_to_deployment_execution_2026_06_20`

Maintain at minimum:

- `CONTEXT_ANCHOR.md`
- `ACTIVE_QUESTION_STACK.jsonl`
- `SOURCE_SEARCH_LEDGER.jsonl`
- `DENOMINATOR_NAMESPACE_REPAIR_LEDGER.jsonl`
- `SLEEVE_MEMBER_EXACT_JOIN_LEDGER.jsonl`
- `LIFECYCLE_ORDER_TYPE_JOIN_LEDGER.jsonl`
- `CLEAN_LABEL_DATASET_LEDGER.jsonl`
- `DETERMINISTIC_BASELINE_LEDGER.jsonl`
- `FINAL_PACKAGE_SELECTION_LEDGER.jsonl`
- `EXECUTION_ORDER_TYPE_POLICY_LEDGER.jsonl`
- `COST_SOURCE_PROVENANCE_LEDGER.jsonl`
- `VALIDATION_STRESS_LEDGER.jsonl`
- `MODEL_TRAINING_OR_REJECTION_LEDGER.jsonl`
- `PRODUCTION_INTEGRATION_LEDGER.jsonl`
- `DEPLOYMENT_DOSSIER.md`
- `VPS_HANDOFF_PROMPT.md`
- `ROLLBACK_AND_MONITORING_CHECKLIST.md`
- `SATURATION_SELF_RED_TEAM.md`
- `COMPLETION_AUDIT.json`
- `OUTPUT_MANIFEST.json`
- route-local builder/verifier scripts

If a later artifact is not applicable because the route proves a prior exact requirement cannot close, emit the artifact with exact proof and status, not a missing file.

## Phase 0 - Stabilize Current State

1. Regenerate and read live state.
2. Verify HEAD, branch, worktree path, sparse checkout state, disk space, and dirty files.
3. Re-run or inspect:
   - package synthesis verifier,
   - package acceptance/compression verifier,
   - lifecycle bridge verifier,
   - parent verifier,
   - supplemental parent lifecycle verifier.
4. Record Git/LFS/mmap friction and the exact fallback policy.
5. Do not commit unrelated dirty files.

## Phase 1 - Exact Denominator Namespace Repair

This is the first hard gate. Do not skip it.

Goal: transform the current context bridge into exact selected-package row joins.

Pursue every same-evidence-class repair path:

- recover `candidate_id` from upstream shortlist, selector, scheduler, Wave B/C/F, hydrated replay, runtime-learning packet, pending lifecycle, trade record, and placement capture artifacts;
- build or recover a stable `decision_window_id` if exact candidate IDs are not present;
- join `source_axis_row_index`, symbol, side, session bucket, framework, origin family, candidate ID, decision time, decision window, order type, pending ticket, lifecycle state, cancel/replace/time-in-force state, fill/no-fill outcome, and broker/profile namespace;
- search route-local artifacts, parent ledgers, sibling worktree files, local MT5 bridge exports, `pipeline_state`, `shadow_logs` when hydrated, and latest VPS-source truth artifacts;
- if current Wave B/C universe cannot join the `877` lifecycle labels, build the selected-package replay extension for the May 3-12 runtime lifecycle window or prove exact impossibility;
- do not use weekly/context-only joins as denominator proof;
- do not infer historical order intent from price movement alone;
- use context joins as diagnostics only until exact row joins exist.

Required outputs:

- exact join ledger for every `1,101` sleeve member;
- exact no-join proof for rows that cannot be repaired;
- source search ledger with every path/key searched;
- denominator status by sleeve, member, symbol, side, session, framework, and origin family;
- updated residual gate ledger.

If exact denominator joins remain `0`, the route is not done unless it proves every repair path impossible and emits exact source/capture requirements.

## Phase 2 - Clean Labels And Deterministic Baselines

Only after Phase 1 produces exact joins or exact bounded exclusions.

Construct clean no-leak labels for:

- candidate accept/reject;
- sleeve quality;
- avoid/inverse;
- regime;
- fillability/cost risk;
- order type: limit-first, guarded market, delay, skip, cancel/replace, reduce;
- price improvement versus missed-fill;
- exit/action;
- time-stop/stale thesis;
- sizing multiplier.

Rules:

- freeze final package target before training labels are used;
- define clean as-of feature contract;
- define duplicate policy;
- define broker actual-R versus proxy target contract;
- use purged/embargoed splits;
- use walk-forward and leave-one-symbol/session/regime/time-period-out splits;
- build deterministic/mechanical baselines before any model;
- compare regularized statistical/tree models only after deterministic baseline exists;
- no RL unless simulator realism and off-policy evaluation are proven.

Required outputs:

- label schema;
- feature schema;
- dataset manifest;
- split manifest;
- deterministic baseline ledger;
- model-training or model-rejection ledger;
- calibration and reliability ledgers if models train;
- verifier proving no-leak label readiness.

## Phase 3 - Final Package Selection

Select the strongest package only after denominator and label gates are repaired or exactly bounded.

Compare all material variants:

- `20` scheduler-lifecycle merge sleeve core;
- inclusion/exclusion of `3` promote sleeves;
- redesigned candidates that become computable;
- avoid/failure-feature sleeves as veto/features/risk controls, not live signals unless proven;
- source-required sleeves after repair or exact exclusion;
- limit-first/passive-entry versus guarded-market fallback;
- cost-aware skip/delay/reduce/queue/replace rules;
- broker-specific FTMO and redacted_account support.

Required validation:

- development replay;
- sealed or rolling holdout where available;
- rolling walk-forward;
- leave-one-symbol/session/regime/time-period-out;
- duplicate denominator audit;
- concentration stress;
- cost/spread/slippage/swap stress;
- broker-profile support audit;
- no-fill opportunity cost;
- limit-order adverse selection;
- Monte Carlo;
- adversarial baselines;
- comparison against current `positive_weighted12_after_swap` baseline:
  - monthly `5.09%`;
  - Sharpe `0.28419`;
  - MC pass `0.9999`;
  - max-DD fail `0.0001`;
  - worst day `-2.083%`;
  - MaxDD `8.842614R`.

Output exact package decision:

- selected sleeves;
- excluded sleeves and transformed use;
- selected order-entry architecture;
- expected replay/proxy R and expectancy;
- broker-real calibration boundary;
- confidence by component;
- kill/reduce/pause criteria;
- rollback criteria.

## Phase 4 - Execution And Cost Architecture

Do not let cost become a brake; make it an optimization layer.

Evaluate:

- limit-first / passive entry;
- guarded market entry;
- delay / skip / reduce;
- cancel/replace;
- time-in-force;
- queue and replacement logic;
- spread normalization;
- broker stops/freeze/session constraints;
- price improvement;
- missed-fill opportunity cost;
- adverse selection after fill;
- close-side cost;
- commission/swap/slippage conversion to R.

No assumption is allowed that limit orders remove cost. No assumption is allowed that market orders are mandatory. Prove the best architecture per symbol/session/order-type/broker-profile or preserve exact source requirements.

Required outputs:

- execution order-type decision policy;
- cost-source provenance ledger;
- limit-first versus guarded-market comparison;
- close-side scoring spec;
- runtime packet parity requirements.

## Phase 5 - Production Integration Package

When the final package is selected, integrate it as code/config/profile/verifier artifacts.

Required:

- include/exclude scope ledger;
- config patch manifest;
- runtime packet parity checklist;
- package verifier;
- focused tests for changed code/config/profile/runtime surfaces;
- rollback commands;
- monitoring paths;
- owner-action boundary;
- VPS Codex handoff prompt.

The route may author code/config/profile/verifier/launcher changes locally. It must not restart VPS, mutate broker/account/order/deal/position state, reveal credentials, call paid vendors, or blind-push.

## Phase 6 - Independent Audit And Final Dossier

Run an independent Wave H style audit before final acceptance.

Audit:

- source leakage;
- label leakage;
- cost-source provenance leakage;
- profile/config fallback misuse;
- duplicate denominator errors;
- concentration;
- result/proxy/broker-real class confusion;
- model overfit;
- RL/simulator exploitation risk if applicable;
- limit-order simulator exploitation risk;
- package scope and LFS pollution;
- runtime effect boundaries.

Required final outputs:

- adversarial audit ledger;
- fix ledger;
- final accept/reject/rework decision;
- deployment dossier;
- VPS handoff prompt;
- rollback and monitoring checklist.

## Verification Commands

Run all that apply, recording exact environment friction:

```bash
python3 -m py_compile <changed python files>
python3 scripts/validate_goal_prompt_hardening.py research/operations/final_moonshot_ultimate_system_denominator_to_deployment_goal_2026_06_20/ULTIMATE_SYSTEM_DENOMINATOR_TO_DEPLOYMENT_GOAL_PROMPT.md
python3 scripts/audit_goal_route_artifacts.py research/operations/final_moonshot_ultimate_system_denominator_to_deployment_execution_2026_06_20 --full-jsonl
python3 scripts/audit_goal_route_artifacts.py research/operations/final_moonshot_ultimate_system_full_plan_goal_session_2026_06_19 --full-jsonl
python3 research/operations/final_moonshot_ultimate_system_full_plan_goal_session_2026_06_19/verify_parent_ultimate_system_full_plan.py
python3 research/operations/final_moonshot_ultimate_system_full_plan_goal_session_2026_06_19/verify_parent_lifecycle_fillability_bridge_absorption.py
```

Run focused pytest for every changed runtime, replay, training, config, verifier, or package surface. If base `pytest` is unavailable, use `uv run --with pytest pytest ...` and record the fallback.

Run `git diff --check` where Git can read. If Git mmap/deadlock blocks it, run direct scoped whitespace/JSON scans and record the exact fallback.

## Completion Audit Requirements

The final `COMPLETION_AUDIT.json` must include:

- current HEAD and branch;
- every input route read;
- instruction coverage for doctrine and this prompt;
- exact denominator join counts;
- exact clean-label counts;
- deterministic baseline metrics;
- model metrics or explicit model rejection reason;
- final package selected or exact reason no package can be selected;
- selected sleeve IDs and excluded/transformed-use sleeve IDs;
- replay/proxy performance numbers;
- broker-real calibration rows and remaining broker-real source gaps;
- execution-cost/order-type architecture;
- FTMO and redacted_account support matrix;
- deployment dossier status;
- Wave H audit decision;
- verification commands and results;
- scoped commit hashes;
- exact remaining source/access/capture requirements;
- whether the ultimate-system completion standard was reached.

Do not mark complete while `final_package_selected=false` unless the audit proves exact exhausted impossibility from available sources. Do not mark complete while exact denominator, clean labels, final selection, integration, and audit remain merely named but not pursued.
