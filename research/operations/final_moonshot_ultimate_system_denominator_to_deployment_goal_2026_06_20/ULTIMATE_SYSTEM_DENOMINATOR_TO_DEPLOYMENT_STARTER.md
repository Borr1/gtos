/goal Follow the full controlling prompt in `research/operations/final_moonshot_ultimate_system_denominator_to_deployment_goal_2026_06_20/ULTIMATE_SYSTEM_DENOMINATOR_TO_DEPLOYMENT_GOAL_PROMPT.md` as the complete objective. Start in `/Users/borr/Documents/gtos/repo/ai-trading-agent-ultimate-convergence-20260619` on branch `research/ultimate-convergence-candidate-replay-2026-06-19`; verify current HEAD and latest launch-pack commit message from disk, verify package base checkpoint `a5ba3e9afe6a423c149af8eef17c955dd3fbf655 research: bridge lifecycle fillability sleeves`, and do mandatory GTOS preflight before relying on any fact. Do not rely on chat memory. Reread this starter, the controlling prompt, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, orchestrator controls, parent route artifacts, package synthesis/compression/lifecycle-bridge artifacts, and current `LIVE_STATE` after every compaction, resume, interruption, uncertainty, or worktree switch. Treat the doctrine and this prompt as active instructions, not background, and operationalize instruction-coverage in every route manifest and completion audit. Evidence class is source completeness, exact/proxy R, denominator repair, clean-label construction, deterministic baseline comparison, model-training readiness, final package selection, production-code packaging, execution-cost architecture, deployment dossier, and VPS handoff with RESULT_MATERIALIZATION_REQUIRED. Forbidden surfaces are live trading, broker operation, broker/account/order/history/deal/position mutation, credential mutation/disclosure, paid API/vendor calls, blind remote push, and live VPS restart/reload. Builder posture is constructive, aggressive, source-bound, anti-boxing, and no conservative brake: preserve the `1,249,248.03066685R` discovery signal, treat the `20` scheduler-lifecycle merge sleeves as the likely core only after proof, repair exact denominator namespace first, do not stop at context-only joins, no arbitrary top-N or top 3/5/10, full ledgers for all rows, pursue every same-evidence-class blocker until repaired, proven impossible, or reduced to exact source/access/capture requirement. First hard lane: create `research/operations/final_moonshot_ultimate_system_denominator_to_deployment_execution_2026_06_20`, carry `candidate_id` or stable `decision_window_id` plus pending-order/order-type/lifecycle namespace onto the `1,101` sleeve members, join the `877` lifecycle labels exactly where possible, build selected-package replay extension if needed, and prove or repair every no-join row. Continue after that through clean no-leak labels, deterministic baselines, final package selection, execution/order-type/cost architecture including limit-first versus guarded-market with missed-fill opportunity cost, production integration, Wave H independent audit, deployment dossier, VPS handoff prompt, rollback and monitoring. Literal impossibility means exactly every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action inside this evidence class was attempted or proven inapplicable. Run py_compile, prompt hardening validation, route verifier, parent verifier, artifact audits, focused tests, JSON/whitespace scans, and scoped commits. Do not mark complete while `final_package_selected=false` unless the route proves exhausted impossibility from available sources; the goal ends only with the strongest evidence-supported deployable GTOS package or exact row-level proof of what source/capture/access prevents it.
