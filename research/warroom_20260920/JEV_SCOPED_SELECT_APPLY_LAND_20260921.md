# SCOPED SLEEVE-SELECT APPLY LAND — 2026-09-21

XAU conflict helper was MISSING while env=1 — implemented `should_stand_three_fresh_xau_conflict`.
GBPJPY conflict APPLY env=1 + `should_stand_gbpjpy_conflict_tag` + admission refuse reasons.
Global SLEEVE_SELECT_APPLY stays 0. Writer recycled.
