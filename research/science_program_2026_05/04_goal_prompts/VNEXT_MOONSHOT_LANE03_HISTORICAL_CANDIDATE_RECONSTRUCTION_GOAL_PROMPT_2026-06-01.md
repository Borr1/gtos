# vNext Moonshot Lane 03 - Historical Candidate Reconstruction

Own reconstruction of the candidate/event universe across all available history. The goal is not Friday-only replay. The goal is to produce the row-level universe that downstream microscope, feature, label, replay, selector, scheduler, execution, and ML lanes consume.

## Mandatory Context Use

Run mandatory preflight. Read doctrine, moonshot vision, master route, Lane01 source authority, Lane02 as-of contract when present with exact absent-state row when not, Friday microscope route, next-level selected denominator routes, live/shadow logs, local MT5 preservation manifest, and relevant candidate generator/runtime code. Do not rely on chat memory. After compaction/resume/interruption/uncertainty, reread prompt/starter/doctrine/latest artifacts from disk.

This is a reconstruction builder lane. Apply active creativity and no conservative brake: search every source-bound way to reconstruct candidates, skips, rejects, accepted trades, no-entry rows, stuck paths, missed opportunities, selected-denominator rows, and current-system projection rows. Truthfulness is mandatory: do not invent historical system intent that was never logged; label mechanical replay/projection separately.

## Objective

Write route outputs under:

`research/operations/vnext_moonshot_lane03_historical_candidate_reconstruction_2026_06_01/`

Build canonical candidate/event ledgers over all available symbols and windows supported by source authority. Include current vNext frameworks and mechanism families: OB retest, FVG fill, breaker re-entry, liquidity sweep reclaim, displacement continuation, failed displacement, no-fill/near-miss, session-flow continuation, exhaustion, stop-cascade, inverse/avoid candidates, and any additional mechanism discoverable from existing code/logs/routes/data.

## Required Work

- Reconstruct event rows from raw logs, selected-denominator artifacts, Friday microscope, live companion, route artifacts, runtime decisions, shadow logs, and replay generators.
- Where original GTOS intent is unavailable, build mechanical/as-of replay candidates and label them as projection, not original intent.
- Preserve all material rows with identity, symbol, session, side, framework/origin, source path, source row, candidate time, source-use state, and as-of status.
- Produce row classes: raw generated, selected, bridge, skipped, rejected, broker-ready, accepted, placed, no-entry, stuck, missed opportunity, stale blocker, correct reject candidate, projection-only.
- Add duplicate/denominator policy and canonical IDs.
- Build source gap ledger for rows that cannot be reconstructed because the historical system state is non-generatable.

## Completion Standard

No arbitrary top-N, no one-day or one-symbol narrowing, no representative-only mechanisms. Full ledgers must preserve all material rows. Full same-evidence-class pursuit is required; every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action must be attempted or proven inapplicable.

Forbidden surfaces: live trading broker operation, order action, credential print/change, paid API/vendor calls, remote push, hidden production activation. Read-only MT5 market/history/spec/account-history export is authorized when needed to reconstruct data-supported candidates and timing.

Finish with candidate ledgers, reconstruction code, duplicate policy, mechanism coverage ledger, source gap ledger, manifest, verifier, focused tests, and completion audit.

## Operative Hardening Anchor

Read `goal_session_research_discipline.md` and `research_operating_doctrine.md`; do not rely on chat memory. Treat those files as active instructions, not background. This is constructive research/build work with curiosity, active creativity, and no conservative brake inside the evidence class. Full same-evidence-class pursuit is mandatory. The phrase literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action has been attempted or proven inapplicable. No arbitrary top-N, top 3/5/10, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows. Produce artifact, ledger, manifest, verifier or focused test output, completion audit, source-capture or source completeness decisions, branch decision or implementation decision, and exact R, proxy R, or expectancy fields when result materialization is owned; when result fields are absent, produce row-level missing-field proof plus the strongest source-bound proxy or exact capture/export requirement. Scope boundaries are rails around execution, spend, credentials, remotes, and hidden production activation; they are not permission to shrink local/repo/MT5/read-only evidence pursuit. Forbidden surfaces remain production-change activation, live trading broker operation, paid API/vendor calls, broker account order history deal position changes, credential/remotes, and prompt/config/risk/execution/safety/canary/selector changes outside scoped default-off or dossier authority.
