/goal Follow the full controlling prompt in `research/science_program_2026_05/04_goal_prompts/VNEXT_MOONSHOT_PRODUCTION_REPLACEMENT_ACTIVATION_REPAIR_HARDENING_GOAL_PROMPT_2026-05-27.md` as the complete objective. Repair and harden the committed vNext/moonshot production replacement activation from current disk evidence. The vNext/moonshot replacement system is the target; old PrimaryAnalyzer/L2/legacy three-framework paths have no fallback authority and no ceiling authority. Run mandatory preflight, create route-local state under `research/science_program_2026_05/06_outcome_testing/vnext_moonshot_production_replacement_activation_repair_hardening_2026_05_27/`, and maintain a stage spine so context compaction resumes from disk. Close every named and newly discovered production-correctness gate with code, tests, verifiers, artifacts, and scoped commit evidence: stale Stage12 selected-count test, semantic verifier unconditional success exit, hardcoded completion-audit pass strings, stale manifest hashes, rollback proof depth, vNext-native prescreen/news parity, dynamic pending-fill policy persistence, execution policy support matrix, broker-resolved monitor/tick parity, broader-origin pending notification parity, redacted_account broker/risk geometry from current specs, cost/slippage/commission/fill capture, stale route-state HEAD metadata, non-mutating check-mode verifiers, and gates against fake completion or dead-selector collapse. Treat prior `GER40`, `UKOIL_cash`, and `USOIL_cash` broker-unavailable exclusions as stale alias evidence; run non-mutating MT5 verification for `GER30`, `UKOUSD`, and `USOUSD`; verified aliases enter aliases/config/runtime/broker onboarding/activation map/selector/risk/monitoring/tests and regenerate dependent artifacts; failed aliases get exact failed-field proof and row-level exclusion evidence. Preserve and repair current activation evidence instead of narrowing to the old system: final selector `274,146` rows, `+122,779.52522361604R`, expectancy `+0.44786181532328045R`, PF `2.4382409880020854`, WR `50.69743859111568%`, with `60,748` old-three rows and `213,398` broader-origin rows. Recompute all numbers from disk before use; any number change triggers exact-cause analysis and regeneration of dependent artifacts. Build canonical row-level frequency/R/executable-trade ledger separating selected opportunity rows, positive selector rows, risk-positive executable rows, risk-zero/repair-required rows with current `fail_closed` labels, deduped order intents, pending intents, fills/no-fills, wins/losses/breakevens, monthly/weekly/daily/hourly frequency, symbol/family/session/side/policy breakdowns, and exact R distributions. Read and consume prior question/anatomy/moonshot artifacts: `24,327` activation rows, `24,337` moonshot rows, and `73,001` replacement closure rows; grouped closure/template answers are not production consumption, so dereference evidence and map every activation-relevant finding to implemented runtime/config, verified test/artifact, exact row-level exclusion, capture contract, AI-budget packet, or repaired work item. Use subagents for fresh non-mutating audits across verifier integrity, runtime leakage, pending-fill execution, broker/risk/monitoring, alias repair, frequency ledger, and prior question-stack consumption; patch the critical path while they audit, then integrate every returned production-correctness finding before staging. Terminal blocker labels have no authority. A failure is repair work, a missing source is capture/exclusion work, a timeout is sharding work, and stale artifacts are regeneration work. Every wrapper or helper must repair, verify, preserve exact row evidence, or prove literal impossibility. Stage and commit after all required gates are closed with meaningful fail paths, non-mutating checks, current manifests, current route state, clean scoped diffs, canonical ledgers, and final committed evidence.
