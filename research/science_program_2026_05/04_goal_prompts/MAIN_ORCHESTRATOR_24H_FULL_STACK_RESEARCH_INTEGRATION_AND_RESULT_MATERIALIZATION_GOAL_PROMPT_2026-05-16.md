# MAIN_ORCHESTRATOR_24H_FULL_STACK_RESEARCH_INTEGRATION_AND_RESULT_MATERIALIZATION

Date: 2026-05-16
Mode: main-worktree successor goal session
Primary objective: `MAIN_ORCHESTRATOR_24H_FULL_STACK_RESEARCH_INTEGRATION_AND_RESULT_MATERIALIZATION`
First concrete objective: `MATERIALIZE_CURRENT_REPLAYABLE_GEOMETRY_RESULTS_FROM_MOONSHOT_AND_READY8_INPUTS_ON_MAIN`

## Non-Negotiable Mission

This is a 24-hour main-orchestrator execution session. It must produce tangible result materialization and system-improvement decisions from disk artifacts. It must not create another ceremonial ladder. It must not turn missing detail into a broad dependency bucket. It must compute every exact/proxy R row currently computable from disk and prove every non-computable row at row level.

This prompt and its starter override any older weak, soft, or conservative language in prior context. Older defensive boundary language must be interpreted as evidence-class and live-broker rails only. It is not permission to avoid result computation, skip live/shadow evidence, refuse justified production-code changes, emit another prompt, or stop at a blocker.

No conservative brake. Proof-or-impossibility, same-evidence-class pursuit, active creativity, and curiosity apply inside the evidence/live-broker boundaries. The prompt, starter, SESSION_56, `goal_session_research_discipline.md`, `research_operating_doctrine.md`, and hardening controls are active instructions, not background. The completion audit must include instruction-coverage. No arbitrary top-N, top 3/5/10, representative-only sampling, or number-limited cutoff is allowed; preserve all material rows in full ledgers before summarizing.

Retired defensive wrapper labels and keep-closed fields have zero authority to suppress implementation candidates, scoped code changes, replay escalation, forward-shadow escalation, or system-upgrade decisions. Any source-bound improvement over current GTOS, even small, must be surfaced, quantified, and converted into a concrete candidate, implementation, replay, forward-shadow, or kill decision.

`.context/00_core/research_current_state.md` contains stale `R12` / READY8 ladder language under `2026-05-16 READY8 R11 G12 Audit Acceptance And R12 Geometry Capture Route`. SESSION_56 and this prompt supersede that stale section. Do not reopen R12. Do not reopen READY8 standalone. Do not create a READY8 capture-only ladder because stale context says R12 existed.

## Mandatory Disk Preflight

1. Run `py -3 scripts\generate_live_state.py` and read `.context/LIVE_STATE.md`.
2. Read `AGENTS.md`, `CLAUDE.md`, `.context/02_session_handoffs/SESSION_56_MAIN_ORCHESTRATOR_24H_SUCCESSOR_HANDOFF_2026-05-16.md`, `.context/00_core/orchestrator_successor_operating_brief.md`, `.context/00_core/orchestrator_methodology_hardening_controls.md`, `.context/00_core/parallel_goal_merge_playbook.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_current_state.md`, `research/science_program_2026_05/05_synthesis/ORCHESTRATOR_ROUTE_STATUS_REGISTRY_2026-05-15.json`, and `research/science_program_2026_05/05_synthesis/ORCHESTRATOR_CROSS_ROUTE_QUESTION_AMBIGUITY_LEDGER_2026-05-15.json`.
3. Read the READY8 final decision artifacts under `research/science_program_2026_05/06_outcome_testing/decide_ready8_scid_and_related_geometry_research_direction/`.
4. If `` exists, read and snapshot it. If unreadable, diagnose the exact OS/Git/access error, try alternate local paths and safe access methods, request access only if needed, and make no moonshot-based claim without a fixed snapshot. Unreadable moonshot/source paths are not a stop condition; continue every non-dependent materialization task in parallel and record the exact dependency boundary.
5. Do not rely on chat memory, pasted closeouts, or assumed route state.
6. After any compaction, resume, interruption, or uncertainty, regenerate `LIVE_STATE` and reread this prompt, the starter, SESSION_56 handoff, doctrine, hardening controls, and current artifacts from disk. Re-apply the override above before continuing.

## Ownership Boundary

Moonshot owns branch-local detail builders and deep discovery:

- SOURCE acquisition/detail;
- M1 support ordering;
- M15 bounded ordering;
- positive challenger detail;
- entry/adverse redesign;
- branch-local discovery/detail expansion.

Main owns integration, result materialization, system decisions, context freshness, and turning research into GTOS improvements. Do not duplicate or compete with moonshot detail builders. Consume current moonshot artifacts as a fixed disk snapshot. At start, snapshot moonshot HEAD, branch, git status, every consumed artifact path, size, SHA256, and committed vs working-tree-only status. Do not modify, delete, stage, or commit anything in ``. If moonshot files change while main is running, keep the initial consumed snapshot fixed and record that boundary.

This long main orchestrator is not a blank primitive-science discovery sprint. Moonshot owns deep primitive-science discovery, branch-local market-behavior mining, raw hypothesis expansion, and builder-family detail. Main owns integration, result materialization, research-to-system conversion, and decision-making. Build an exhaustive hypothesis/system-improvement inventory and classify every item into exactly one bucket:

1. `COMPUTE_NOW_FROM_CURRENT_DISK_EVIDENCE`
2. `COMPUTE_NOW_FROM_CURRENT_MOONSHOT_SNAPSHOT`
3. `MERGE_READY8_OR_PRIOR_RESEARCH_AS_FEATURE_CONTROL_OR_FAILURE_INTELLIGENCE`
4. `REPLAY_OR_RETEST_NOW_USING_EXISTING_LOCAL_DATA`
5. `IMPLEMENTATION_CANDIDATE_WITH_EXISTING_NUMERIC_SUPPORT`
6. `KILL_OR_AVOID_WITH_EVIDENCE`
7. `OWNED_BY_ACTIVE_MOONSHOT_BUILDER`
8. `OUT_OF_SCOPE_FOR_MAIN_BECAUSE_NO_CURRENT_COMPUTABLE_PATH`

Buckets 7 and 8 are not deferral buckets. Every bucket-7 or bucket-8 row must include exact artifact paths checked, exact missing evidence, exact owner, and why main cannot compute it from current disk evidence. Do not enter open-ended primitive-science exploration unless it directly produces exact R rows, proxy R rows, expectancy-style summaries, replay result rows, system-upgrade evidence, an implementation candidate, or kill/avoid evidence. Do not expand the hypothesis universe faster than result materialization.

## Live/Forward Evidence And Production Improvement Authority

Live intelligence, forward data, shadow logs, live issues, broker/account truth logs, malformed AI outputs, execution logs, no-fill logs, candidate logs, path/fillability logs, strategy-follow logs, and production incidents are first-class evidence. Research artifacts are not the only source of truth. Read and use current live/shadow/forward evidence from disk wherever it drives system improvement.

When disk evidence identifies a production weakness, implement the scoped fix. Do not refuse or defer code/prompt/config/tooling changes from conservative fear. This authority covers `src/`, `prompts/`, `config/`, research tooling, validators, replay builders, logging, execution-adjacent code, risk-adjacent code, selector logic, canary/test infrastructure, and system integration code.

Required accountability for production changes: read existing code before editing; cite the exact live/research evidence driving the change; preserve or improve observability; add or update tests/verifiers; document before/after behavior; preserve rollback clarity; commit scoped changes; state exactly what changed and why.

This prompt is explicit owner approval for repo code, prompt, config, risk, execution-adjacent, safety, canary, selector, tooling, logging, replay, validator, and system-design changes when evidence exposes an improvement path. Interpret evidence broadly and actively: disk artifacts, live/shadow/forward logs, broker/account truth logs, MT5/Sierra historical data, moonshot artifacts, code inspection, recomputable analysis, verifier/test failures, and observed production friction all count. This supersedes older doctrine text that forbids those repo surfaces. The only separately gated actions are live broker operations that place, close, or modify trades; paid/API/vendor spend; credentials; and remote push.

Broker evidence distinction: live broker operations require a separate explicit owner command, but reading/parsing broker/account/order/history/deal/position logs, account truth files, exported history, MT5 historical data, shadow logs, and local broker evidence is authorized and required whenever relevant.

## Subagent Execution

Use subagents aggressively for non-blocking moonshot snapshot audit, live/shadow evidence audit, old research archaeology, result-ledger validation, and red-team/code review. Keep the critical path local: main performs the first replayable-geometry materialization and any evidence-driven implementation decisions. Do not use subagents as a reason to defer decisions.

## READY8 Boundary

READY8 SCID standalone is closed. Terminal decision: `MERGE_READY8_SCID_INTELLIGENCE_INTO_A_BROADER_SYSTEM_BUT_DO_NOT_CONTINUE_IT_AS_STANDALONE`.

Use READY8 only as merged intelligence: tags, priors, control/failure rows, HAZ001 density/waiting-time context, MAC inverse/avoid-filter candidates, UNC004 source-confidence diagnostics, HAZ005 source-control/capture requirements, and neutral movement/failure intelligence. Do not reopen READY8 as standalone. Do not create R12 READY8. Do not create another G0/G12/audit/capture ladder for the same missing-geometry problem.

## First Materialization Objective

Build current replayable-geometry result artifacts from READY8 final intelligence plus current moonshot/replayable geometry artifacts. Required artifacts:

- moonshot input snapshot;
- unified candidate ledger;
- geometry binding ledger;
- exact/proxy R result ledger;
- non-computable row proof ledger;
- expectancy-style summary;
- cost/stress sensitivity summary;
- branch/family/symbol/session/horizon split summary;
- READY8-tagged vs untagged comparison;
- survivor/failure decision ledger;
- current-snapshot dependency ledger only for rows that genuinely cannot receive exact/proxy R;
- completion audit;
- lightweight verifier/check.

## Computation Rules

Compute exact R wherever exact side, entry, stop, target, fillability, path, and cost exist. Compute proxy R wherever current M1/M15/tick/OHLC/Sierra/moonshot artifacts provide proxy side, entry, target/stop threshold, path ordering, fillability, or cost/stress data. Do not require exact broker/live execution for proxy R. Do not require completed moonshot finality for current-snapshot proxy R.

If a row has enough current evidence for exact R or proxy R, compute it now. Do not label it dependency. Current-snapshot dependency is not an escape bucket. For every row without exact/proxy R, emit row-level proof with:

- row id;
- artifact paths checked;
- input hashes;
- exact missing field(s);
- why no weaker proxy R is computable from current disk evidence;
- which active moonshot builder family owns the missing detail;
- whether the row is killed, preserved as current-snapshot dependency, or convertible to another proxy.

## Required Summaries

The result summary must include actual counts for candidate rows, exact R rows, proxy R rows, non-computable rows, target-first, stop-first, neither, ambiguous, gross expectancy, cost-adjusted expectancy, stress-adjusted expectancy, win rate, loss rate, average win R, average loss R, branch/family/symbol/session/horizon splits, duplicate/effective-N, concentration, pass/control delta, READY8-tagged vs untagged comparison, and kill/keep/redesign/current-snapshot decisions by branch.

## Older Research Integration After Replayable-Geometry Materialization

After current replayable-geometry materialization is complete, use the remaining window to integrate older strong research candidates and variants from disk: v2/v3/v4 variants, J46-J49, S79, K54/K55, no-fill/forward source capture, FVG/OB confluence, orderflow/depth/Sierra artifacts, live/shadow logs, broker actual-R audits, strategy follow candidates/evaluations, and exit/risk variants. Identify what must be killed, merged, replayed, implemented, or preserved as forward-capture material. Do not do this before the replayable-geometry result layer is materialized.

Old-research mapping must run in parallel by subagents while main performs the first replayable-geometry materialization. Main must not delay exact/proxy-R materialization, and it must not waste the 24-hour window serializing every archaeology task before computing results.

## Forbidden End States

Do not end with `needs more geometry`, `needs capture`, `needs audit`, `needs G12`, `needs G0`, `wait for future session`, `emit next prompt`, `summary only`, `blocker packet only`, `capture requirement only`, or `proof-of-impossibility only`.

Terminal decision for the first materialization objective must be exactly one of:

- `CURRENT_REPLAYABLE_GEOMETRY_RESULTS_MATERIALIZED_WITH_EXACT_AND_PROXY_R_ROWS`
- `CURRENT_REPLAYABLE_GEOMETRY_RESULTS_MATERIALIZED_WITH_EXACT_R_ROWS_ONLY`
- `CURRENT_REPLAYABLE_GEOMETRY_RESULTS_MATERIALIZED_WITH_PROXY_R_ROWS_ONLY`
- `CURRENT_REPLAYABLE_GEOMETRY_RESULTS_MATERIALIZED_NO_COMPUTABLE_R_ROWS_AFTER_CURRENT_ARTIFACT_SCAN`
- `CURRENT_REPLAYABLE_GEOMETRY_RESULTS_MATERIALIZED_AND_SUBSTRATE_KILLED_AFTER_COMPUTATION`

## Hard Live Boundary

Broker/account/order/history/deal/position operations, paid/API/vendor spend, credentials, and remote push require a separate explicit operational command. Repository code, prompt, config, tooling, logging, selector, risk, execution-adjacent, safety, canary, validator, replay, and system-design improvements are authorized inside this session when evidence drives them. Legacy defensive wrapper fields must not be preserved in new active outputs from this lane and must not suppress implementation, replay, forward-shadow, code, prompt, config, tooling, logging, selector, risk, execution-adjacent, or system-design improvements supported by evidence.

## Completion Standard

The session must run the full 24-hour window. There is no early close condition when a plate is cleared. A checkpoint, context file, summary, verifier, partial artifact, first materialization result, scoped commit, or context refresh is not final completion before 24 hours. Each completed plate requires scoped artifacts, JSON/JSONL parse checks, lightweight verifier/check, scoped commit on main, and context refresh; then immediately continue to the next highest-value result, live/shadow, replay, implementation, or system-improvement task from disk evidence.

## Launch Commands

```powershell
cd C:\Users\MSI\Documents\ai-trading-agent
codex --enable goals
```

Paste the starter from `research/science_program_2026_05/04_goal_prompts/MAIN_ORCHESTRATOR_24H_FULL_STACK_RESEARCH_INTEGRATION_AND_RESULT_MATERIALIZATION_STARTER_2026-05-16.txt`.
