# vNext Absolute Moonshot Post-Lane18 - Source Capture Repair

Own the source-capture repair lane opened by the post-Lane18 Master refresh. The objective is to turn every material source gap from Lane16, Lane17, Lane18, MT5 cache preservation, VPS export requirements, live/shadow evidence, and current repo data into filled source rows, reconstructable source rows, export requirements, forward-capture contracts, parser/code repairs, tests, verifiers, and downstream contracts for Selector V3, Scheduler V3, Execution Policy V3, ML, Repair Companion, Command Center, and production-change dossiers.

This lane is not a summary, not an audit-only pass, not a missing-data labeler, and not a passive queue. It must repair what can be repaired now from current disk, local heavy data, preserved MT5 cache, repo evidence, read-only exports that are already available, and approved local parsing. Gaps that remain must be exact, row-level, field-level, source-level requirements.

## Mandatory Context Use

Run mandatory preflight from disk before using memory. Regenerate `.context/LIVE_STATE.md`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/vnext_absolute_moonshot_vision_and_limitations.md`, `.context/00_core/local_heavy_data_inventory.md` if present, the latest handoff as historical only, and the current Master route at `research/operations/vnext_absolute_moonshot_master_orchestration_2026_06_01/`.

Read these source authorities from disk:

- Lane01 source authority.
- Lane02 no-leak/as-of contract.
- Lane03 historical candidate reconstruction.
- Lane04 historical microscope engine.
- Lane05 feature store.
- Lane06 label store.
- Lane07 broker truth/cost calibration.
- Lane08 digital twin replay.
- Lane09 Meta-Selector V2.
- Lane09B selector-scheduler reconciliation.
- Lane10 Scheduler V2.
- Lane10B scheduler conflict anatomy.
- Lane11 execution policy engine.
- Lane16 historical microscope scale.
- Lane17 market awareness whiteboard.
- Lane18 broker truth/cost capture V2.
- MT5 local cache preservation route.
- compliant VPS/data-preservation route.
- live companion route and current source/capture runtime code.

After compaction, resume, interruption, uncertainty, or long waits, regenerate `LIVE_STATE`, reread this prompt, the starter, doctrine, Master post-Lane18 decision, and latest route artifacts from disk, then continue from route state. Do not rely on chat memory.

Treat `goal_session_research_discipline.md` and `research_operating_doctrine.md` as active instructions, not background. Operationalize full same-evidence-class pursuit: literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available inside this evidence class has been attempted or proven inapplicable. Use constructive repair/build posture with curiosity, active creativity, and no conservative brake inside this evidence class.

## Objective

Write route outputs under:

`research/operations/vnext_absolute_moonshot_post_lane18_source_capture_repair_2026_06_01/`

Build the post-Lane18 source repair package that consumes every material gap class and produces repaired source coverage, exact unrepaired capture/export requirements, reusable parsers/helpers, verifiers, focused tests, and downstream contracts.

## Required Work

- Build a complete source-gap inventory across Lane16, Lane17, Lane18, Lane05/Lane06/Lane08 gap ledgers, Lane10/Lane10B scheduler gaps, Lane11 policy source gaps, MT5 preservation artifacts, VPS export requirements, live companion packets, shadow logs, runtime logs, data directories, exports, pipeline state, and route manifests.
- Classify every material gap row as `filled_now`, `reconstructed_now`, `proxy_bound_now`, `read_only_export_required`, `forward_capture_required`, `non_generatable_historical_truth`, or `blocked_with_exact_source_requirement`.
- Repair every source gap that can be repaired from current disk, preserved cache, local repo data, route artifacts, shadow logs, local MT5 cache, or already available read-only exports. Do not stop at gap taxonomy when parsing, joining, hashing, or route-local reconstruction can fill the field.
- Build or patch parsers/helpers where missing source formats are recoverable, including market bars, tick/quote files, spread/cost samples, MT5 cache inventories, symbol/spec/session metadata, broker-truth exports, runtime packet fields, and route manifests.
- Preserve exact as-of/no-leak status for each repaired field. A repaired market-data field, replay-only field, broker-real field, proxy field, decision-available field, label-only field, and non-generatable historical intent field must not be collapsed into one class.
- Produce row-level source coverage deltas for every downstream consumer: Selector V3, Scheduler V3, Execution Policy V3, Digital Twin V2, ML dataset/baselines, ML policy intelligence, Repair Companion, Command Center, and production-change dossier.
- Build source-capture contracts for every prospectively capturable missing field: field name, owning runtime component, capture time, as-of rule, schema, source path, hash/manifest rule, null reason, verifier check, and downstream consumers.
- Distinguish recoverable market data from non-generatable historical system intent. Do not infer historical broker/order/lifecycle intent from price movement. If intent was not logged and no source-bound local artifact exists, freeze the exact prospective capture requirement.
- Build implementation decisions for default-off capture/logging helpers where code can be added without changing live trading behavior. Any live-behavior activation remains outside this lane.
- Produce full machine-readable ledgers. Summaries may rank after full ledgers exist, but no arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff may replace full row preservation.
- Add verifiers and focused tests that fail on unclassified gaps, hidden nulls, missing row identity, missing source path/hash, source/as-of leakage, stale old-system fields, broken manifest entries, incomplete downstream contracts, and unverifiable parser output.

## Saturation And Self-Red-Team Pass

Before completion, run a written saturation pass and pursue every same-evidence-class gap it exposes. At minimum answer with artifacts:

- Which source gaps were silently inherited from old lanes, and what was done to repair them now?
- Which fields can be recovered from current disk, prior route artifacts, local heavy data, preserved MT5 cache, or read-only exports?
- Which gaps are true non-generatable historical truth rather than missing parser work?
- Which exact downstream decisions are impossible until a field is captured prospectively?
- Which fields leak label/outcome/broker-real truth into decision-phase features when mishandled?
- Which source roots were searched, which were not present, and which exact source/capture action remains?
- Which repaired rows changed Selector V3, Scheduler V3, Execution Policy V3, ML, repair companion, or command-center readiness?
- What verifier would reject a future stale or lossy source-capture claim?

If any answer exposes a repairable same-evidence-class gap, repair it, recompute affected artifacts, and rerun verifiers before completion.

## Completion Standard

Completion requires code, route artifacts, and verification. Required outputs:

- source-gap superledger with all material gap rows preserved;
- repaired-source ledger;
- source-coverage delta ledger;
- parser/helper implementation ledger;
- read-only export requirement ledger;
- forward-capture contract;
- non-generatable historical-truth ledger;
- source/as-of/no-leak validation ledger;
- downstream contracts for Selector V3, Scheduler V3, Execution Policy V3, ML, Repair Companion, Command Center, and production dossier;
- result-use status with exact R, proxy R, and expectancy fields when this lane owns result materialization, and row-level proof when those fields are outside this lane;
- source-capture and source completeness decisions;
- branch decision and implementation decision ledgers;
- route manifest with hashes, counts, byte sizes, and source paths;
- verifier and focused tests;
- completion audit with instruction coverage, saturation answers, row counts, gap deltas, exact blockers, and scoped commit.

No generic "missing data" closure. No unresolved row without exact field/source/window/path/capture owner. No relaunch of Lane16, Lane17, or Lane18. Consume them as terminal inputs unless a changed hash or verifier defect proves a concrete reason to inspect a row again.

## Hard Boundaries

Forbidden surfaces: production-change activation, live trading broker operation, broker account order history deal position mutation, paid API/vendor spend, credential changes or disclosure, remote push, hidden production activation, and prompt/config/risk/execution/safety/canary/selector live behavior changes outside scoped default-off capture or dossier authority.

Authorized inside this evidence class: read-only local/repo/source inspection, local heavy-data parsing, preserved MT5 cache parsing, already available read-only export parsing, code for default-off capture helpers, route-local builders/verifiers/tests, manifests, source-capture contracts, source completeness decisions, branch decisions, implementation decisions, source-bound exact/proxy fields where owned, and scoped commits.
