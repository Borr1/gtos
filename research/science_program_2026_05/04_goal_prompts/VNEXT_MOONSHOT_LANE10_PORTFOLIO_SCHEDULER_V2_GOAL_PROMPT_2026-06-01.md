# vNext Moonshot Lane 10 - Portfolio Scheduler V2

Own the money-risk portfolio scheduler. The system must admit trades by actual account exposure, not stale count caps or vague session limits. Many strong small-risk trades can pass when total exposure is inside limits; one oversized or correlated weak trade must be blocked.

## Mandatory Context Use

Run mandatory preflight. Read doctrine, moonshot vision, master route, Lane01 source authority, Lane02 as-of contract, feature/label/replay/broker truth outputs when present with exact absent-state rows when not, Lane01 fixed Friday portfolio replay, Lane02 broad selected replay, Lane05 next-level runtime scheduler package, current permissions/risk code, and config. Do not rely on chat memory. After compaction/resume/interruption/uncertainty, reread prompt/starter/doctrine/latest artifacts from disk.

This is a scheduler implementation and replay lane. It must compute behavior and patch default-off/current-package code where this lane owns it. It must not stop at describing risk philosophy.

## Objective

Write route outputs under:

`research/operations/vnext_moonshot_lane10_portfolio_scheduler_v2_2026_06_01/`

Build Scheduler V2: balance/equity, day-start baseline, realized broker PnL, open worst-case SL risk, pending worst-case SL risk, new candidate worst-case risk, partial release, BE release, spread/slippage/commission buffer, same-symbol exposure, correlated cluster exposure, daily overlay, external prop limits, total limits, drawdown compression, cushion expansion, and priority ranking between simultaneous candidates.

## Required Work

- Audit all count/session/KZ/concurrent caps and classify whether they remain authority or evidence-only under vNext.
- Implement or package money-risk authority using broker truth, selected-cell risk, actual lot/contract geometry, cost buffers, and account state.
- Build replay scheduler over Friday, broad selected denominator, daily/weekly/monthly slices, and stress scenarios.
- Produce accepted/rejected ledgers with exact risk dollars, risk pct, buffer, reason, and opportunity cost.
- Handle same-symbol and correlated exposure rules explicitly.
- Add tests for simultaneous candidate ranking, partial/BE release, open/pending/new risk, cushion expansion, drawdown compression, and stale count-cap rejection.

## Completion Standard

No arbitrary top-N and no "max two trades" style shortcut as final authority. Full same-evidence-class pursuit is required: every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action must be attempted or proven inapplicable.

Forbidden surfaces: live trading broker operation, order action, credential print/change, paid API/vendor calls, remote push, hidden production activation. Risk/config/permission code is authorized only as scoped scheduler package patches with tests/verifier and runtime-effect boundary.

Finish with scheduler code/package, replay ledgers, risk/exposure metrics, stale-cap audit, verifier, focused tests, manifest, and completion audit.

## Operative Hardening Anchor

Read `goal_session_research_discipline.md` and `research_operating_doctrine.md`; do not rely on chat memory. Treat those files as active instructions, not background. This is constructive research/build work with curiosity, active creativity, and no conservative brake inside the evidence class. Full same-evidence-class pursuit is mandatory. The phrase literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action has been attempted or proven inapplicable. No arbitrary top-N, top 3/5/10, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows. Produce artifact, ledger, manifest, verifier or focused test output, completion audit, source-capture or source completeness decisions, branch decision or implementation decision, and exact R, proxy R, or expectancy fields when result materialization is owned; when result fields are absent, produce row-level missing-field proof plus the strongest source-bound proxy or exact capture/export requirement. Scope boundaries are rails around execution, spend, credentials, remotes, and hidden production activation; they are not permission to shrink local/repo/MT5/read-only evidence pursuit. Forbidden surfaces remain production-change activation, live trading broker operation, paid API/vendor calls, broker account order history deal position changes, credential/remotes, and prompt/config/risk/execution/safety/canary/selector changes outside scoped default-off or dossier authority.
