# SCID Forward Capture Implementation Design Package From Offline Schema Synthesis

Date: 2026-05-12
Owner lane: prompt pack emitted by G0 SCID forward-capture offline-schema synthesis
Evidence class: `SCID_FORWARD_CAPTURE_IMPLEMENTATION_DESIGN_PACKAGE_FROM_OFFLINE_SCHEMA_SYNTHESIS_ONLY`
Input G0 route: `research/science_program_2026_05/06_outcome_testing/g0_scid_forward_capture_offline_schema_package_synthesis_control/`
Input G12 audit: `research/science_program_2026_05/06_outcome_testing/g12_scid_forward_capture_offline_schema_implementation_package_audit/`
Promotion posture: `NO_PROMOTION_VERDICT`
Required flags: `validation_safe=false`, `outcome_review_opened=false`, `live_effect=false`

## Objective

Design the exact no-live implementation package that would transform the accepted offline schema contract into reviewable additive capture points, redacted event rows, fail-closed parser adapters, rollback plan, and G12 audit inputs without wiring live behavior or changing production decision logic.

Examples are starting points, not limits. Current GTOS OB/retest logic, current SCID-only framing, the ten accepted schema groups, and the listed route families are not the research horizon. Stay auditable, as-of, no-leak, source/control-only, and hard-boundary-compliant while maximizing useful implementation-readiness output.

This route must produce implementation-grade design evidence, not a memo. The output should let a later owner-approved implementation lane make the smallest additive code changes with minimal ambiguity. Do not edit production code in this route; instead, emit proposed patch artifacts, function contracts, file/path ownership, tests, rollback plan, restart policy, and approval gates under the research route.

## Maximum Output Requirements

Push the design package to the practical maximum inside this no-live evidence class. Speed is not a quality constraint, and "needs later approval" is not enough. If a later approval is needed, freeze the exact approval, exact patch target, exact runtime risk, exact rollback, and exact verification that would be required.

This route is complete only if it answers:

- Which existing source/logging/code surfaces could carry each of the ten capture groups?
- Which exact files/functions/classes would be touched in a later additive implementation lane?
- Which fields are already observable, which need new additive capture, which need synthetic-only harness support, and which remain forbidden?
- What proposed patch shape would add capture without changing decisions, risk, execution, prompts, canaries, selectors, or live behavior?
- What redaction and no-leak rules prevent broker/account/order/history/deal/position or result/performance leakage?
- What tests prove parser, redaction, fail-closed, schema-version, stale/as-of, duplicate-key, unavailable-source, and forbidden-field behavior before live wiring?
- What owner approval, live restart, monitoring, kill-switch, rollback, and G12 audit gates would be required before any future code is wired?
- What would make this route a shallow design loop, and which emitted artifacts prevent that?

## Mandatory Preflight

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/quick_reference_card.md`.
4. Read `.context/00_core/research_operating_doctrine.md`.
5. Read `.context/00_core/goal_session_research_discipline.md`.
6. Read `.context/00_core/research_current_state.md`.
7. Read this prompt from disk.
8. Read the accepted G12 offline-schema audit decision, schema contract audit, fixture recomputation audit, manifest/read-only/no-leak audit, output manifest, verifier result, completion audit, and closeout verification.
9. Read the original offline schema package field-group schema ledger, parser/redaction/as-of/no-leak validator contract, fixture manifest, fixture validation result, read-only monitoring alignment ledger, manifest hash policy, output manifest, verifier result, and closeout verification.
10. Read the immediate upstream G0/G12 source-capture synthesis artifacts needed to preserve the 3,014 candidate boundary, ten capture groups, manifest-binding repair, and anti-boxing route bundle.
11. Inspect existing source/control and shadow-capture code surfaces in read-only mode, including at least `src/research_infra/forward_capture.py`, relevant shadow/logger modules under `src/research_infra/` and `src/components/`, relevant tests under `tests/`, and current shadow/runtime JSONL shape examples only where allowed by no-leak boundaries.

Do not rely on chat memory. If interrupted or compacted, regenerate `LIVE_STATE`, re-read this prompt and the current route artifacts from disk, and resume from the emitted context anchor.

## Mandatory Context Use

Treat `.context/00_core/goal_session_research_discipline.md` and `.context/00_core/research_operating_doctrine.md` as active instructions. Record in the completion audit:

- whether both were read after preflight;
- the builder/control posture applied;
- anti-boxing questions pursued;
- searched roots and artifacts inspected;
- proof-or-impossibility stop condition;
- any doctrine requirement deliberately not answered because it crosses validation, result scoring, broker evidence, AI/API, paid/vendor, raw-blob, prompt/config/risk/safety/execution/canary/selector, or live-behavior boundaries.

## Accepted Package Boundary

Carry forward the accepted G12 decision `ACCEPT_AS_G12_SCID_FORWARD_CAPTURE_OFFLINE_SCHEMA_IMPLEMENTATION_PACKAGE_CONTROL_EVIDENCE_ONLY`. Preserve exactly:

- offline schema/parser/fixture/validator/read-only alignment evidence only;
- live wiring absent and required as a future gate;
- 3,014 `candidate_input_row_id` values and 3,014 `duplicate_proxy_denominator_key` values as source/control coverage expectations only, not result denominators;
- ten capture groups: side, entry, stop, target, POI, framework, lifecycle, LTF, orderflow/proxy, baseline-control;
- manifest-binding repair: current G12 prompt hash rebound, self-referential output manifest hash drift non-blocking, all other hash mismatches strict.

## Hard Boundaries

Preserve `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, and `live_effect=false`. Do not open validation/result-scoring/strategy-edge/R/PnL/win-rate/expectancy/performance/promotion/live behavior/AI/API/paid-vendor/broker-account-order-history-deal-position/raw-market-blob/prompt-config-risk/safety/execution/canary/selector changes.

Use checkpointing, chunking, and resumable ledgers rather than accepting shallow summaries. Do not stage unrelated runtime, shadow, live-monitoring, raw market-data, or production-code dirt.

## Route-Specific Requirements

- Treat implementation as design/package only; do not edit `src/`, `prompts/`, `config/`, `scripts/watchdog.ps1`, canaries, selectors, or live process controls.
- Freeze future owner approval needed for any actual live logger wiring or restart.
- Include proposed patch artifacts that a later owner-approved implementation lane can audit before any code change. Proposed patch artifacts must live under the research route and must not modify production files.
- Map every proposed implementation point to additive/no-decision-impact behavior, source/as-of rule, fail-closed status, redaction rule, duplicate policy, parser/schema rule, tests, and G12 audit evidence.
- If a capture group cannot be mapped to an existing safe insertion point, pursue alternatives inside this evidence class: read-only existing artifact shape, synthetic harness support, proposed new additive logger, or exact future owner/live approval gate. Do not stop at "not currently implemented."
- Create a sequencing plan that separates what can be done now as design/test-harness/read-only expansion from what requires owner-approved live wiring.

## Required Outputs

- context anchor and accepted G12/G0 reconciliation
- per-capture-group implementation design ledger
- source/logger insertion-point proposal ledger with no code wiring
- proposed patch plan artifacts for later owner-approved implementation, including file paths, function/class contracts, proposed new files, proposed tests, and no-live/no-decision-impact assertions
- redaction/fail-closed/as-of/duplicate/rollback/test plan
- owner approval and live restart gate dossier
- sequencing ledger for implementation-design, synthetic-only harness, read-only alignment, G12 audit, owner approval, and eventual live wiring
- saturation/self-red-team ledger proving this is not a shallow design summary or another infrastructure loop
- G12 implementation-design audit prompt and one-line starter
- standalone verifier, focused tests, completion audit, closeout verification

## Completion Standard

1. All ten capture groups mapped to proposed additive source/logging design without live wiring.
2. Every design item includes source/as-of/no-leak/redaction/fail-closed/duplicate/rollback/test/G12 acceptance criteria.
3. Proposed patch artifacts are emitted under the research route for every later code/file change that would be needed, without editing production files.
4. Owner approval, restart, monitoring, rollback, and G12 audit gates are exact.
5. Saturation/self-red-team ledger proves the route did not stop at a memo, did not loop, did not treat examples as limits, and did not hide same-evidence-class design work behind future approval.
6. No production source, prompt, config, risk, safety, execution, canary, selector, API, broker, raw blob, validation, or result surface changed.
7. Verifier and focused tests pass or exact blockers are recorded.

## One-Line Starter

`/goal Follow the full controlling prompt in research/science_program_2026_05/04_goal_prompts/SCID_FORWARD_CAPTURE_IMPLEMENTATION_DESIGN_PACKAGE_FROM_OFFLINE_SCHEMA_SYNTHESIS_GOAL_PROMPT_2026-05-12.md as the complete objective; do mandatory preflight and context refresh first; do not rely on chat memory; stay SCID_FORWARD_CAPTURE_IMPLEMENTATION_DESIGN_PACKAGE_FROM_OFFLINE_SCHEMA_SYNTHESIS_ONLY with no validation/result-scoring/strategy-edge/R/PnL/win-rate/expectancy/performance/promotion/live behavior/AI/API/paid-vendor/broker-account-order-history-deal-position/raw-market-blob/prompt-config-risk-safety-execution-canary-selector changes; inspect accepted audit/package artifacts plus existing source/logging/test surfaces read-only; preserve accepted G12 offline schema package, 3,014 candidate coverage boundary, ten capture groups, manifest-binding repair, offline-only/live-wiring-absent boundary, NO_PROMOTION_VERDICT, validation_safe=false, outcome_review_opened=false, live_effect=false; build implementation-grade design ledgers, proposed patch artifacts under the research route only, owner approval/restart/rollback/G12 gates, sequencing and saturation proof that this is not a memo or loop; pursue proof-or-impossibility until the prompt file's completion standard is fully satisfied.`
