# vNext Moonshot Production Replacement Activation Repair Hardening Goal Prompt

Route id: `vnext_moonshot_production_replacement_activation_repair_hardening_2026_05_27`

## Objective

Repair, harden, verify, and commit the current vNext/moonshot production replacement activation package from current disk evidence.

The target system is the vNext/moonshot production replacement system. The old `PrimaryAnalyzer -> L2 -> legacy three-framework` path has no fallback authority and no ceiling authority. Preserve the production replacement direction, close every discovered repair gate, and produce a committed package whose runtime code, config, tests, route artifacts, manifests, and completion evidence all agree.

Completion requires active code, active config, runtime tests, route artifacts, manifests, and verifiers that all execute the repaired vNext/moonshot replacement path across the broker-resolved universe with verifier-enforced acceptance gates, dynamic execution persistence, monitoring parity for every verified broker symbol, correct route metadata, truthful manifests, canonical frequency/R/executable-trade ledgers, and non-self-certifying completion evidence.

## Authority And Override

This prompt controls the route. If older handoffs, stale prompt files, stale route reports, stale docs, comments, tests, or route artifacts conflict with this prompt, the current disk evidence plus this prompt wins.

These are active instructions, not background. Operationalize them in code, tests, verifiers, ledgers, manifests, route state, and instruction-coverage evidence. Do not rely on chat memory.

Retired weak artifact phrases, including default-off wording, shadow-log wording, delayed-activation wording, passive-watch wording, deferral wording, blocker wording, readiness-denial wording, and legacy-fallback wording, have zero governing authority in this route. Current code and current route evidence decide the technical state. When such language appears in active files, remove it, replace it with current production-replacement semantics, or record exact evidence that the text is historical and cannot govern runtime behavior.

There is no terminal blocker label for this route. A failure is a work item. A missing source is a source-capture or exclusion work item. A stale artifact is a regeneration work item. A failing test is a repair work item. A timeout is a sharding work item. A giant artifact is a packaging or local-evidence work item. A mismatch is a reconciliation work item. Do the extra work until the concrete technical issue is closed.

Full same-evidence-class pursuit is mandatory. Literal impossibility means exactly the session has exhausted every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available in the repo and local machine for that same evidence class, and has written exact proof of why no further action can run in this environment.

No conservative brake. The builder posture is constructive, direct, curious, and active: repair the production replacement system to its strongest evidence-backed form instead of narrowing it to a smaller old-system-compatible surface.

No top-N, no arbitrary top-N, no top 3/5/10, and no number-limited cutoff is allowed to hide material evidence. Preserve all material rows, write full ledger artifacts or exact row-bearing shards, and use histograms or manifests with exact recoverability.

Every wrapper, ledger, and helper layer must repair code/config, verify behavior, preserve exact row-level evidence, or prove literal environment impossibility after exhausting same-evidence-class actions inside this route.

## Mandatory Preflight

Start from disk, not chat memory.

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read the latest handoff in `.context/02_session_handoffs/`.
4. Read:
   - `.context/00_core/orchestrator_successor_operating_brief.md`
   - `.context/00_core/orchestrator_methodology_hardening_controls.md`
   - `.context/00_core/parallel_goal_merge_playbook.md`
   - `.context/00_core/quick_reference_card.md`
   - `.context/00_core/research_operating_doctrine.md`
   - `.context/00_core/research_current_state.md`
   - `.context/00_READING_ORDER.md`
5. Read the previous activation controlling prompt and starter:
   - `research/science_program_2026_05/04_goal_prompts/VNEXT_MOONSHOT_PRODUCTION_REPLACEMENT_ACTIVATION_GOAL_PROMPT_2026-05-26.md`
   - `research/science_program_2026_05/04_goal_prompts/VNEXT_MOONSHOT_PRODUCTION_REPLACEMENT_ACTIVATION_STARTER_2026-05-26.txt`
6. Read the current production replacement route directory:
   - `research/science_program_2026_05/06_outcome_testing/vnext_moonshot_production_replacement_activation_2026_05_26/`
7. Read the current runtime/config surfaces:
   - `config/agent_config.yaml`
   - `config/profiles/redacted_account.yaml`
   - `src/components/orchestrator.py`
   - `src/components/execution.py`
   - `src/components/gtos_vnext_runtime.py`
   - `src/components/broader_origin_generators.py`
   - `src/components/moonshot_default_off_policy_router.py`
   - `scripts/_live_monitor_iter.py`
8. Read `.context/00_core/goal_session_research_discipline.md`.
9. Inspect `git status --short`, `git log -5 --oneline`, and route-owned diffs before editing.

## Evidence Class And Required Local Work

This route is repo-local production-replacement engineering: code, config, risk, execution logic, safety gates, canary surfaces, selectors, runtime paths, tests, verifiers, manifests, ledgers, capture contracts, and route artifacts.

Boundary for side effects, not ambition: finish the production-change implementation in repo; represent runtime launch/live trading/broker operation processes with runnable config, tests, manifests, launch/capture packets, and route evidence; represent paid API/vendor calls and broker account/order/history/deal/position mutation with local mocks, dry-run contracts, exact prompt packets, budget manifests, and capture interfaces.

Every external integration surface must have a concrete repo-local contract that lets the next execution surface run from evidence instead of prose.

## Result Materialization Standard

Every result row, aggregate, exclusion, and repair decision must carry result-use-status. Distinguish exact-R, proxy-R, expectancy, branch decision, implementation decision, source-capture status, source completeness, and production-selection status.

Proxy results and exact execution truth must stay visibly distinct. Proxy results that can drive implementation decisions must be preserved and materialized in ledgers and acceptance gates.

## Current Activation Baseline To Preserve And Repair

These are the current evidence anchors. Recompute them from disk before using them.

- Activation commits:
  - `a7f1f77b50 vnext: activate moonshot production replacement`
  - `9bb758429 research: record vnext activation commit proof`
- Active mode in `config/agent_config.yaml`:
  - `gtos_vnext_runtime.enabled: true`
  - `gtos_vnext_runtime.mode: production_replacement_vnext_moonshot`
  - `gtos_vnext_runtime.apply_to_execution: true`
  - `moonshot_dynamic_execution_router_enabled: true`
  - `moonshot_dynamic_execution_router_apply_to_execution: true`
  - `moonshot_broader_origin_live_generation_enabled: true`
  - `moonshot_broader_origin_execute_pre_ai_pre_l2: true`
  - `moonshot_broader_origin_extended_session_enabled: true`
  - `moonshot_broader_origin_cross_asset_fetch_enabled: true`
- Prior activated broker-native eligible symbols before GER/Oil alias repair:
  - `AUDJPY`
  - `AUDUSD`
  - `BTCUSD`
  - `CHFJPY`
  - `ETHUSD`
  - `EURGBP`
  - `EURJPY`
  - `EURUSD`
  - `GBPJPY`
  - `GBPUSD`
  - `JP225`
  - `NAS100`
  - `NZDUSD`
  - `SPX500`
  - `UK100`
  - `US30_cash`
  - `USDCAD`
  - `USDCHF`
  - `USDJPY`
  - `XAGUSD`
  - `XAUUSD`
- Prior excluded symbols requiring alias re-verification:
  - `GER40`
  - `UKOIL_cash`
  - `USOIL_cash`
- New broker-alias evidence from user MT5 Symbols screenshots after the prior activation route:
  - `GER40` appears in MT5 as `GER30` with description `Germany 40 Cash index`.
  - `UKOIL_cash` appears in MT5 as `UKOUSD` with description `Crude Oil Brent Cash`.
  - `USOIL_cash` appears in MT5 as `USOUSD` with description `West Texas Intermediate Crude Oil cash`.
  - Therefore the prior `broker_unavailable` exclusion for these three markets is stale alias-resolution evidence, not final market impossibility. Programmatic non-mutating MT5 verification must decide the final broker-resolved status.
- Final selector evidence:
  - combined selected rows: `274,146`
  - total R: `+122,779.52522361604`
  - expectancy: `+0.44786181532328045R`
  - PF: `2.4382409880020854`
  - WR: `50.69743859111568%`
  - old-three rows: `60,748`
  - broader-origin selected rows: `213,398`
  - old-three BE-after-trigger total R: `+34,687.89081232328`
  - broader-origin total R: `+88,091.63441129276`
- Outside-session evidence:
  - outside-session rows: `169,530`
  - expanded into production execution: `125,584`
  - outside expanded total R: `+54,605.46692603049`
  - outside expanded expectancy: `+0.4348122923782527R`
  - outside expanded PF: `2.375865944805206`
- Outside-session risk reconciliation:
  - expanded rows: `125,584`
  - risk positive execution rows: `11,971`
  - current artifact `fail_closed` / repair-required rows: `113,613`
  - current artifact `fail_closed` / repair-required reasons:
    - `105,066` spread/slippage cost negative EV or spread cost exceeds threshold
    - `4,879` unresolved commission evidence
    - `3,668` selector metrics not positive
- Broker/risk geometry:
  - broker spec rows: `24`
  - effective config rows: `24`
  - selected-cell risk rows: `1,344`
  - positive selected-cell risk rows: `111`
  - zero-risk rows: `1,233`
  - positive selected-cell risk currently `0.25%`

These numbers are not decorations. Any number change triggers exact-cause analysis, regeneration of every dependent artifact, test updates, and preserved row-level evidence.

## Current System Interpretation To Make Exact

The current vNext implementation is a broad moonshot selector, not a single strategy. It combines:

- old-three repaired current-origin families: `ob_retest`, `fvg_fill`, `breaker_re_entry`
- broader moonshot origin families: `liquidity_sweep_reclaim`, `structural_distance_extreme`, `displacement_continuation`, `cross_asset_lead_lag`, `session_open_range_break`, `volatility_compression_expansion`, `regime_transition_break`

The route must explicitly report what works, what fails, what was selected, what was excluded, and why. It must not narrow the system to one family, one market, one session, or the old live surface. It must activate every family, market, session, side, and policy surface supported by row-level evidence; every exclusion requires exact negative, missing-source, broker-invalid, or repair-required proof.

The current selector is high-frequency at the opportunity-row level. That is not the same as final live broker order frequency. The route must materialize a canonical row-level membership ledger that separates:

- selected opportunity rows
- positive selector rows
- risk-positive executable rows
- risk-zero/repair-required rows, including current `fail_closed` artifact labels as non-terminal repair/exclusion evidence
- deduplicated order-intent rows
- pending-intent rows
- filled-trade rows
- no-fill rows
- rejected/broker-invalid rows

The route must compute exact frequency and distribution ledgers by month, week, day, UTC hour, symbol, origin family, framework, session, side, policy, win/loss/breakeven bucket, R bucket, cost bucket, and risk disposition. It must report wins, losses, breakevens, average winner, average loser, median R, quantile R, total R, expectancy, PF, and trade/opportunity frequency for every activated component and for the combined system.

The route must explain the apparent `~50%` win rate correctly: with average winner near `+1.5R`, average loser near `-1R`, and breakeven outcomes from BE-after-trigger, `~50%` WR with PF above `2` is strong. It must still prove this from canonical row-bearing evidence and must not rely on summary claims without row proof.

## Prior Question-Stack Status To Reopen Correctly

The prior question/anatomy work was not ignored, but it was not fully consumed into production code/config/tests either. The activation route created `24,327` question rows. The moonshot route carried `24,337` question rows. The production-replacement Stage07 closure imported the activation question stack, moonshot question stack, imported moonshot question stack, and `10` replacement-route questions into `73,001` closure rows.

Current Stage07 closure counts were:

- `answered_with_disk_evidence`: `53,109`
- `source_capture_required`: `19,268`
- `ai_budget_required`: `416`
- `killed_redesigned_with_reason`: `179`
- `implemented`: `22`
- `external_surface_package_required`: `7`

Treat that Stage07 closure ledger as input evidence, not final consumption. It classified and linked questions to evidence paths, but many rows remain source-capture, AI-budget, external-surface, killed/redesigned, or broadly answered-by-disk categories. It also has one row per question while many answers point to shared summaries or repeated closure templates. This route must reopen every activation-relevant closure row, especially all non-implemented rows, dereference the cited evidence artifact, and decide whether the specific question becomes runtime/config/test implementation, exact row-level exclusion, capture contract, budgeted AI test package, negative fixture, or repaired work item. A previous `answered_with_disk_evidence` status is not enough when the answer has not reached the current production replacement code/config/tests or canonical executable-trade ledger.

## Required Repairs

Close every repair below. This list is the minimum floor, not the ceiling. Add every additional repair exposed by current disk evidence, subagent audits, tests, verifiers, route artifacts, runtime inspection, config inspection, prior question/anatomy intelligence, or replay evidence. Pursue the full repair frontier until no same-evidence-class production-correctness issue remains.

### 1. Stale Stage12 Semantic Test

`research/.../test_vnext_replacement_stage12_semantic_verifier.py` contains a stale hardcoded `287390` selected-row assertion while current final selector evidence is `274146`.

Repair it. The test must validate current selector truth from artifacts/manifests, not freeze a stale number without proof. Add a negative drift test proving a stale selected-row count fails.

### 2. Stage12 Semantic Verifier Exit Code

`verify_vnext_replacement_stage12_semantic_red_team.py` computes activation gate status but returns success unconditionally.

Repair the verifier so failed activation gates, blocking checks, missing required proofs, stale manifests, stale route state, and count mismatches return nonzero. Add tests that force fail paths and prove nonzero exit.

### 3. Completion Audit Must Stop Self-Certifying

`build_vnext_replacement_stage13_completion_audit.py` embeds pass strings such as `294 passed`, `82 passed`, `42 passed`, and verifier pass labels.

Replace hardcoded pass claims with fresh machine-readable result ingestion. The audit must fail when required test/verifier result files are absent, stale, mismatched to current HEAD, mismatched to current route hash, or older than the files they claim to validate.

### 4. Manifest Hash Truth

The output manifest contains stale hashes for at least:

- `VNEXT_REPLACEMENT_COMPLETION_AUDIT_2026-05-26.json`
- `VNEXT_REPLACEMENT_CONFIG_OVERLAY_DIFF_2026-05-26.yaml`

Regenerate the manifest deterministically. Add a manifest verifier that hashes every route-owned artifact listed in the manifest and fails on any mismatch. The verifier must cover JSON, JSONL, YAML, Markdown, and gzipped shard artifacts listed by the manifest.

### 5. Rollback Proof Must Be Executable

The rollback proof currently validates overlay booleans, not a full executable rollback surface.

Build rollback proof using temp-copy or temp-config execution that proves:

- rollback overlay applies cleanly
- vNext replacement flags revert through the rollback artifact
- focused runtime/router tests pass under rollback config for every runtime surface touched by the rollback
- the production package restores to activation config after rollback proof
- no repo mutation remains; any intended route evidence mutation is explicit, regenerated, manifested, and committed

### 6. vNext-Native Prescreen And News Gate Parity

The broader-origin path runs before old `prescreen_mso(mso)` and old economic-calendar pre-AI blocks. The old pipeline is not the target, but its valid risk controls cannot silently disappear.

Implement vNext-native prescreen/news gating before broader-origin execution. The gate must be explicit, tested, and logged. It must not resurrect old PrimaryAnalyzer/L2 as a dependency. Obsolete legacy prescreen rules must be replaced with vNext-equivalent rules and equivalence proof in code and tests.

### 7. Pending Limit Dynamic Execution Persistence

Dynamic execution parameters must persist from candidate generation to pending intent to disk to fill to `open_trade()`.

Persist placement-time values, not recomputed defaults:

- dynamic policy id
- dynamic policy status
- BE trigger R
- final target R
- trigger price
- final target price
- time-stop bars
- selected-cell risk
- source event hash/details
- activation family/origin family
- selector row id or selector proof hash

Add E2E tests proving a broader-origin limit order fill preserves BE-after-trigger/dynamic replacement through `ExecutionEngine.set_limit_intent()`, disk persistence, `check_limit_fill()`, and `open_trade()`. Prove fixed `1.5R`, J46, and J49 do not reactivate through pending-fill recomputation.

### 8. Execution Policy Support Matrix

Execution-active policies must have complete live execution semantics.

Create a verifier-enforced support matrix for dynamic execution policies. `be_after_trigger` is currently the applied policy. Any challenger policy that lacks complete live execution semantics must fail before execution selection. If a challenger policy has evidence strong enough to become execution-active, implement the execution semantics, tests, rollback behavior, monitoring, and replay proof in this route.

### 9. Broker-Resolved Monitoring And Tick Surface

`scripts/_live_monitor_iter.py` still contains old 7-symbol constants.

Repair monitoring/tick daemon surfaces so every verified broker-native eligible symbol drives production monitoring. Use config-driven symbol discovery as the primary implementation. Add tests proving no old 5/7-symbol ceiling governs production replacement. Every symbol exclusion must match exact broker-read evidence and carry row-level proof.

### 9A. Broker Alias Repair For GER/Oil Markets

Repair the stale alias exclusion for the three previously excluded markets.

Programmatically verify these user-observed MT5 aliases with non-mutating MT5 calls:

- canonical replay symbol `GER40` -> broker symbol candidate `GER30`
- canonical replay symbol `UKOIL_cash` -> broker symbol candidate `UKOUSD`
- canonical replay symbol `USOIL_cash` -> broker symbol candidate `USOUSD`

For each candidate, verify `symbol_info`, `symbol_select`, visibility, trade mode, tick availability, M1/M5/M15/H1/H4/D1 bar availability, spread, point, tick size, tick value, contract size, volume min/step/max, filling mode, order mode, session hours, swap fields, commission fields when exposed, and alias normalization. Verified aliases must enter config/runtime aliases, broker onboarding ledgers, activation map, source map, selector ledgers, risk geometry, monitoring, and tests so these markets enter the broker-resolved universe. Failed aliases must receive exact non-mutating proof of the failed field and preserved row-level exclusion evidence.

### 10. Pending Notification Parity

Broader-origin pending order placement must have notification/logging parity with the old pending path.

Add Telegram limit notification parity or exact tested proof that an equivalent production notification already fires. This must include symbol, side, entry, SL, dynamic policy, risk, source/origin family, and selector proof reference.

### 11. redacted_account Risk And Broker Geometry From Current Specs

The funded profile and broker specs must not be stale old-system leftovers.

Re-derive effective risk, tick size, point size, volume step, min/max lots, contract size, spread/slippage proxy, commission source, symbol alias, and instrument class for every activated broker-native symbol from current config and every local broker/spec evidence source. Update `config/profiles/redacted_account.yaml` and active config from current evidence. Add verifier-enforced proof for every market.

Risk must be opportunity-aware and vNext-specific. Risk percentages in the moonshot path must come from selected-cell evidence, prop-risk geometry, and EV proof. The risk governor must respect prop constraints while preserving high-EV opportunity where the system evidence supports it.

### 12. Cost, Slippage, Commission, And Fill Lifecycle Capture

Build the capture contract required to improve live execution realism:

- decision-time spread
- order-send spread
- fill spread
- slippage in points and R
- commission
- swap
- partial fills
- reject/fill latency
- pending-age-to-fill
- actual BE movement
- actual final target or stop outcome
- dynamic exit action timeline

Use all existing local logs and broker history. For missing fields, add capture fields and verifiers so live execution produces the missing truth from this point forward. Preserve executable current evidence and separate it from forward capture evidence.

### 13. Route State And HEAD Integrity

Current route state contains stale `current_head` metadata.

Repair route state so HEAD, activation commit proof, generated artifact timestamps, manifest hashes, verifier results, and commit proof agree. Add a verifier that fails when route metadata references a stale HEAD after committed route changes.

### 14. Non-Mutating Or Explicitly-Regenerated Verifiers

Verifiers that rewrite proof artifacts during `--check` pollute state.

Split write and check modes. Check mode must not rewrite artifacts. Write mode must regenerate artifacts intentionally and update manifest/state. Add tests plus command evidence proving check mode is non-mutating for route-owned files.

### 15. Acceptance Gates Against Safe-But-Dead And Fake Completion

Production replacement completion must fail when the system collapses opportunity or hides failure behind artifact existence.

Add gates for:

- selected-row collapse versus current selector truth
- old-three fallback ceiling
- missing broader-origin execution path
- stale count assertions
- stale manifest hashes
- route state current-head drift
- hardcoded pass strings
- no-candidate fallback to old pipeline
- missing vNext-native prescreen/news gate
- missing dynamic pending-fill persistence
- unsupported dynamic execution policy
- old 5/7-symbol monitoring ceiling
- stale 21-symbol ceiling after GER/Oil broker aliases verify
- missing pending notification parity
- missing funded profile/broker geometry proof
- completion audit self-certification

### 15A. Canonical Frequency And Executable-Trade Ledger

Create a canonical final row-level membership ledger for the combined system. It must reconcile final selector summaries, old-three row reconstruction, broader-origin allowlist groups, outside-session shards, risk reconciliation, and runtime code paths into one auditable membership spine.

The ledger must expose exact counts and metrics for:

- opportunity rows
- selected rows
- executable after risk/cost rows
- order-intent rows after dedupe/concurrency/session/correlation/prop governance
- fills and no-fills from all current local evidence, plus exact capture requirements for any missing lifecycle fields
- wins, losses, breakevens
- average R, quantile R, PF, expectancy, total R
- monthly/weekly/daily/hourly frequency
- per-symbol, per-family, per-framework, per-session, per-side, per-policy breakdowns

If any reconstructed row count differs from the current `274,146` selector summary, stop the completion path, find the exact join/filter difference, regenerate the summary or ledger, and make the verifier fail on future drift.

### 16. Consume Prior Question And Anatomy Intelligence

Read and use the prior exhaustive question and anatomy artifacts as active implementation inputs. Use them to check whether any activation-relevant insight has not reached code/config/tests.

Required inputs include:

- `research/science_program_2026_05/06_outcome_testing/vnext_activation_edge_anatomy_ai_budget_ml_feasibility_2026_05_26/VNEXT_ACTIVATION_QUESTION_STACK_LEDGER_2026-05-26.jsonl`
- `research/science_program_2026_05/06_outcome_testing/vnext_activation_edge_anatomy_ai_budget_ml_feasibility_2026_05_26/VNEXT_ACTIVATION_FINAL_REPORT_2026-05-26.md`
- the exact row-level activation/anatomy shards in that route
- the moonshot dynamic execution repair route artifacts from `vnext_moonshot_substrate_dynamic_execution_repair_2026_05_26`
- the production replacement activation route artifacts from `vnext_moonshot_production_replacement_activation_2026_05_26`

Create a repair-intelligence ledger that maps each activation-relevant question/finding to one of:

- implemented in runtime/config
- verified by test/artifact
- excluded with exact row-level proof
- not activation-relevant with proof
- repaired during this route

Every non-implemented activation-relevant finding must become a work item and then be closed in this route. The repair-intelligence ledger must reference the prior `73,001` replacement question-closure rows and explicitly distinguish prior bookkeeping closure from actual production consumption. Group-level answer text is not production consumption; the route must map each activation-relevant question to the exact artifact rows, code/config/test surface, exclusion proof, capture contract, or AI-budget packet that answers that specific question.

## Stages

### Stage 00 - Disk Reanchor And Repair Inventory

Create a route directory:

`research/science_program_2026_05/06_outcome_testing/vnext_moonshot_production_replacement_activation_repair_hardening_2026_05_27/`

Write:

- current git/status snapshot
- required-file read ledger
- route-control ledger
- complete repair inventory
- current activation-number freeze
- exact dirty-file responsibility classification

### Stage 01 - Acceptance And Evidence Hardening

Repair Stage12/Stage13 tests, verifier exit codes, manifest hashing, completion audit result ingestion, route state HEAD integrity, and non-mutating check mode.

Write proof artifacts and tests. No completion audit can pass by self-declaration.

### Stage 02 - Runtime Path Hardening

Repair vNext-native prescreen/news gating, no-candidate replacement semantics, dynamic pending-fill persistence, execution policy support matrix, pending notification parity, and old-pipeline leakage tests.

Prove the broader-origin route reaches execution without PrimaryAnalyzer/L2 and that no old fallback ceiling remains.

### Stage 03 - Broker, Risk, Monitoring, Alias Repair, And Cost Capture

Repair funded profile/effective config evidence, broker-resolved monitoring/tick surfaces, GER/Oil alias resolution, broker geometry proof, risk/EV rows, and live cost/fill capture contracts.

Produce row-level or symbol-level ledgers for every activated market.

### Stage 04 - Frequency, Distribution, Intelligence Consumption, And Missed-Finding Sweep

Build the canonical frequency/R/executable-trade ledger. Read the prior question stacks, anatomy ledgers, dynamic execution artifacts, and production replacement artifacts. Build the repair-intelligence ledger and close every activation-relevant gap.

### Stage 05 - Full Verification Matrix

Run deterministic verification shards:

- focused unit tests for each repaired surface
- route verifiers in write mode
- route verifiers in non-mutating check mode
- relevant runtime/config/orchestrator/execution tests
- manifest hash verifier
- rollback proof verifier
- broker-resolved schedule/monitor parity verifier
- dynamic pending-fill E2E verifier
- canonical frequency/R/executable-trade verifier
- semantic activation verifier with negative fixtures

If a full test file times out, split by test class/function or deterministic shards and record every shard verdict. Timeout is not evidence.

### Stage 06 - Commit And Final Route State

Regenerate:

- session state
- output manifest
- control ledger
- completion audit
- final semantic verification result
- final repair report
- rollback proof
- route-state current-head proof

Stage and commit route-owned repairs; list unrelated files separately with proof. Include `Co-Authored-By: Codex GPT-5 <redacted@example.com>` in commit messages.

## Subagent Use

Use subagents for fresh non-mutating disk audits when the route touches multiple surfaces. Keep each subagent unboxed and specific:

- acceptance/verifier/audit integrity
- runtime/orchestrator old-pipeline leakage
- execution/pending-fill/dynamic policy persistence
- broker/risk/config/broker-resolved monitoring
- prior question-stack and anatomy intelligence consumption

Patch the critical path while subagents audit. Before staging, integrate every returned finding. Any subagent finding that affects production correctness becomes a repair gate.

## Context Compaction Controls

At every stage transition and before every commit, reread this prompt, route state, control ledger, and current disk evidence. Update a compact route-local stage spine with:

- current stage
- completed gates
- open gates
- active files
- latest numbers
- latest verifier/test commands and results
- next exact action

After context compaction, resume from the stage spine and this prompt, not chat memory. Classify steering text in the control ledger as active, already-applied, obsolete, or superseded based on current disk state.

## Non-Negotiable Completion Standard

The route reaches completion after all are true:

- vNext/moonshot production replacement remains the active production target.
- No old live fallback ceiling can govern the production replacement path.
- Every required repair above is implemented, tested, and recorded.
- Every verifier has meaningful fail paths and returns nonzero on failure.
- Check-mode verifiers do not mutate proof files.
- Manifest hashes match actual artifacts.
- Completion audit consumes fresh machine-readable evidence, not hardcoded pass strings.
- Dynamic execution state persists through pending fills without recomputation drift.
- monitoring/tick surfaces match the verified broker-resolved universe, including GER/Oil aliases that pass non-mutating MT5 verification.
- Broker/risk/cost capture evidence is current and symbol-complete.
- Canonical row-level frequency/R/executable-trade ledgers reconcile selector summaries, row shards, risk ledgers, and runtime paths.
- Prior activation/anatomy/question intelligence has been consumed into code/config/tests or exact row-level exclusions.
- Route state HEAD, commit proof, final semantic result, and manifest agree.
- Route-owned changes are committed; unrelated pre-existing files are listed with proof.
