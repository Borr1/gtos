# Lane 08 - Execution Policy And Microstructure Stress Goal Prompt

You own execution-policy and microstructure stress. The job is to evaluate partial-BE, momentum exhaustion, fixed comparators, time stops, trailing variants, and every source-bound execution-management mechanism on the correct selected/portfolio denominators with executable tick-path realism.

## Active Context

Run `python scripts/generate_live_state.py`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, and `.context/00_core/research_operating_doctrine.md`. Read Friday microscope artifacts, dynamic router replay artifacts, momentum promotion artifacts, live companion lifecycle logs, and broker cost/truth artifacts. Consume Lane01/Lane02/Lane06 outputs as they land on disk. Do not rely on chat memory. After any context compaction, resume, interruption, long wait, tool crash, or uncertainty, regenerate `LIVE_STATE` and reread this starter, this controlling prompt, the doctrine files, lane-specific context, and latest route artifacts from disk before continuing.

## Route Directory

Write route-owned outputs under:

`research/operations/vnext_lane08_execution_policy_microstructure_stress_2026_05_31/`

## Required Work

Build strict execution policy stress over selected and portfolio-ready denominators. This is not an MFE fantasy and not a raw candidate tournament. Simulate:

- `partial_be_runner`, `momentum_exhaustion`, fixed comparators, BE comparator, time-stop variants, and trailing variants;
- tick ordering, M1 fallback confidence, same-bar ambiguity, spread, slippage, commission, swap, stops level, freeze level, modify cadence, rejected modify handling, latency assumptions, and ticket-bound lifecycle;
- partial release, residual risk, BE/TP/SL modifications, and same-symbol conflicts;
- symbol/session/origin/family/path-shape splits;
- downside metrics, drawdown, loss streak, exposure, concentration, and cost sensitivity.

Robust execution policies produce an implementation decision and local code/test package. Failed policies produce exact failure anatomy and a segment-specific exception, avoid filter, redesign branch, or source-capture requirement.

## Completion Standard

Produce execution policy ledgers, microstructure stress ledgers, exact/proxy R and expectancy summaries, implementation decision ledger, verifier, tests, manifest, and completion audit.

No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff is allowed; preserve all material policy rows and branches. Full same-evidence-class pursuit is required. Literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available from the approved source class has been attempted or proven inapplicable with row-level source proof.

There is no conservative brake inside execution-policy replay, stress, and local implementation work. Operationalize `.context/00_core/goal_session_research_discipline.md` and `.context/00_core/research_operating_doctrine.md` as active instructions, not background. Use as many highest-reasoning subagents as needed for tick-path realism, broker-constraint review, leakage audit, and policy-segment red-team.

External action boundary: live trading broker operation, paid API/vendor spending, credential changes, and hidden production-change deployment are outside the task. Reading broker/account/order/history/deal/position evidence is required for cost and lifecycle realism. Prompt/config/risk/execution/safety/canary/selector code must be inspected, patched locally, and tested for evidence-supported execution-policy changes.
