# READY8_FAIL_CLOSED_PATH_HORIZON_SOURCE_REPAIR

Date: 2026-05-15

## Evidence Class

`READY8_FAIL_CLOSED_PATH_HORIZON_SOURCE_REPAIR_ONLY`

This route pursues the fail-closed source gaps accepted by the READY8 discriminative sealed-validation audit. It is a source-repair and sensitivity route, not a promotion or live behavior route. It must not merely classify missing rows; it must search, repair where source-safe, recompute affected source-control artifacts where appropriate, and write exact proof for anything non-generatable.

## Mandatory Preflight And Context Use

Run and read:

1. `python scripts/generate_live_state.py`
2. `.context/LIVE_STATE.md`
3. `.context/00_core/quick_reference_card.md`
4. `.context/00_core/goal_session_research_discipline.md`
5. `.context/00_core/research_operating_doctrine.md`
6. `.context/00_core/research_current_state.md`
7. `.context/00_core/local_heavy_data_inventory.md`
8. `.context/00_core/ai_in_loop_cost_control_research_plan.md`
9. latest handoff
10. accepted G12 sealed-validation audit directory
11. sealed execution fail-closed ledger
12. target-result packet and rowset design directories

Do not rely on chat memory. The accepted G12 audit identified fail-closed rows as exact source-repair routes. This prompt exists to pursue them, not to defer them.

Maximum-strength posture: do not be conservative, boxed, shallow, or shortcut-driven. The absence of promotion permission is not a reason to avoid source repair, source search, sensitivity recomputation, uncomfortable negative evidence, or creative source-safe explanations. Examples in this prompt are starting points, not limits. If the data opens more questions, branches, interactions, source roots, or repair paths, pursue all of them inside this evidence class until answered, killed with data, proven impossible, or reduced to an exact owner/access/source/capture requirement.

## Mandatory Active Methodology Application

Treat `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, and `.context/00_core/orchestrator_methodology_hardening_controls.md` as active instructions, not background. Operationalize them in the route output with an instruction-coverage ledger.

No arbitrary top-N, no top-N shortcut, no "top 3/5/10", and no number-limited cutoff are allowed for questions, ambiguities, possibilities, open doors, opportunities, blockers, source roots, branches, or routes. Preserve the full ledger of all fail-closed families, source-search attempts, repaired rows, true source gaps, sensitivity recomputations, killed cases, underpowered cases, and exact impossibility proofs until there is no more same-evidence-class intelligence to extract. Summaries may rank findings, but the machine-readable artifacts must keep all material rows.

Full artifact review is mandatory; do not substitute route summaries, closeout prose, or sampled examples for disk artifact inspection. The machine-readable ledgers must explicitly preserve questions, ambiguities, source roots, open doors, closed doors, failed branches, successful branches, and follow-up routes without arbitrary caps. No-loop termination means the route is complete only when the remaining same-evidence-class intelligence, repairable blocker set, and actionable ambiguity set are all exactly zero or bounded by a cited access/evidence-class impossibility. Any G12-style audit agent, validator prompt, or follow-up prompt emitted by this route must verify artifacts, repair same-evidence-class issues when possible, and must not invent limitations or split/break work for fake productivity.

No conservative brake is allowed. Use constructive curiosity and active creativity inside the evidence class: if fail-closed repair opens a same-evidence-class blocker, ambiguity, source root, row-family split, or sensitivity path, pursue it before completion instead of turning it into vague later work.

The completion audit must prove same-evidence-class exhaustion: all discovered fail-closed families are answered with data, killed with data, repaired, proven impossible, or converted into an exact owner/access/source/capture requirement.

## Accepted Disk Facts To Bind

- Accepted target-result rows: `192,896`.
- Computable rows: `162,336`.
- Row-level fail-closed rows: `30,560`.
- Fail-closed audit ledger rows: `2,161`, aggregating `35,811` fail-closed or excluded target rows.
- Accepted fail-closed families:
  - `FAIL_CLOSED_HORIZON_BAR_MISSING`
  - `FAIL_CLOSED_HORIZON_BAR_NOT_RECORD_PRESENT`
  - `FAIL_CLOSED_PATH_BAR_MISSING`
  - `FAIL_CLOSED_PATH_BAR_NOT_RECORD_PRESENT`
  - `ROLE_FAIL_CLOSED_COMPUTABLE_EXCLUDED`
- Affected cards include at least HAZ-001 and HAZ-005; the route must prove exact card/horizon/source distribution from disk.

## Objective

Recover or exactly close every source-safe fail-closed path/horizon gap in the accepted READY8 sealed/stress packet. If recovered rows materially affect HAZ-001, HAZ-005, UNC-004, MAC, BEH, or adversarial control interpretation, emit sensitivity ledgers and downstream G12 prompts. If rows cannot be recovered, prove why and freeze exact source/capture requirements.

## Required Search And Repair Ladder

For every fail-closed family and affected symbol/session/window:

1. Reconstruct the exact candidate/card/horizon/target-family/source-window needing repair from accepted fail-closed ledgers.
2. Search committed rowset/target-result/source-control manifests.
3. Search `C:\Users\MSI\Documents\ai-trading-agent\data`, `data\ticks`, `data\external`, `shadow_logs`, `exports`, and relevant route directories.
4. Search targeted prior worktrees/caches under `C:\tmp`.
5. Search Sierra/source-control packet artifacts where relevant.
6. If market bars/ticks are recoverable through allowed read-only local files, materialize bounded source-hash rows.
7. If read-only extraction is required and the lane allows it, request exact approval and continue after approval.
8. If the missing field is non-generatable historical GTOS source-state, prove it and emit the forward capture contract.

## Required Outputs

Create a versioned route directory and emit:

- context anchor and accepted fail-closed binding ledger;
- full fail-closed row inventory with no top-N cap;
- per-card/per-horizon/per-target-family/per-symbol/per-session/per-source-segment distribution;
- searched-root/acquisition ladder ledger;
- recovered source file/hash ledger;
- repaired row packet if source-safe repair succeeds;
- unrecoverable proof ledger with exact field/window/source/action requirements;
- sensitivity ledger showing how repaired or still-missing rows affect HAZ-001, HAZ-005, UNC-004, MAC, BEH, and ADV controls;
- no-leak/as-of/duplicate policy ledger;
- saturation/self-red-team ledger;
- concise synthesis;
- verifier/focused tests where useful;
- next G12 prompt/starter for any repaired packet or material sensitivity artifact.

## Safe Boundaries

Preserve `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, and `live_effect=false`.

Do not open live trading behavior, promotion, R/PnL, trade win-rate, expectancy, live-readiness claims, AI/API calls, paid/vendor access, broker account/order/history/deal/position evidence, raw market blob commits, registry edits, remote pushes, or prompt/config/risk/safety/execution/canary/selector changes.

These boundaries do not prevent read-only local source search, source-safe repair, no-API sensitivity recomputation, or exact capture-contract design.

## Completion Standard

Complete only when every fail-closed family is repaired, proven impossible from approved inputs, or converted into an exact source/access/capture requirement; all sensitivity views are emitted; no same-evidence-class blockers remain vague; verifier/tests pass where useful; and scoped artifacts are committed.
