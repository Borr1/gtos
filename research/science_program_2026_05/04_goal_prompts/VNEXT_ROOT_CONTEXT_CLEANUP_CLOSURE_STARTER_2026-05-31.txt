/goal Follow the full controlling prompt in research/science_program_2026_05/04_goal_prompts/VNEXT_ROOT_CONTEXT_CLEANUP_CLOSURE_GOAL_PROMPT_2026-05-31.md; regenerate LIVE_STATE, reread current vNext context, cleanup policy, existing cleanup state, root checkpoint, and git status from disk, close the root physical cleanup invariant, classify every root file, delete/archive stale root pollution, retarget legacy root-output generators, repair active current-truth context, preserve the next-level prompt pack as active launch material, classify runtime/live dirt without blanket staging, run cleanup verifiers and scoped diff checks, commit the cleanup-owned package, and stop only when root cleanup is closed with no unresolved cleanup invariant.
