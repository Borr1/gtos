/goal Follow the full controlling prompt in research/science_program_2026_05/04_goal_prompts/VNEXT_DUAL_BROKER_LIVE_MONITORING_REPAIR_SUPERVISOR_GOAL_PROMPT_2026-06-02.md as the complete objective; run mandatory preflight and current-state refresh first; reread this starter, the controlling prompt, goal_session_research_discipline.md, research_operating_doctrine.md, orchestrator_successor_operating_brief.md, orchestrator_methodology_hardening_controls.md, parallel_goal_merge_playbook.md, LIVE_STATE, current vNext map, reading order, prior VPS supervisor artifacts, and VPS/V3/FTMO integration artifacts from disk after any compaction/resume/interruption/tool crash/long wait/uncertainty; treat these as active instructions, not background, operationalize instruction-coverage in the completion audit, and do not rely on chat memory; CEO production approval is embedded: make the strongest current V3/moonshot vNext system work live as intended on both redacted_account and FTMO, change any code/config/profile/launcher/watchdog/supervisor/runtime package/verifier/test/route/process/namespace/data-capture/notification/rollback surface required for correct dual-broker production behavior, enable FTMO order-capable live run_agent execution alongside redacted_account after terminal/account/profile/namespace/risk/data/candidate/notification/rollback verification, and wire/activate the strongest verified V3 selector/scheduler/execution disposition when current evidence supports activation; operate in DUAL_BROKER_VPS_LIVE_RUNTIME_DATA_BROKER_PROCESS_REPAIR_SUPERVISION with production-change claims beyond verified code/config/profile/runtime evidence, live trading behavior changes outside the approved vNext contract, discretionary broker operation mutation, discretionary broker/account/order/history/deal/position mutation, credential print/change, and paid/API/vendor spend reserved for separate exact owner command; no conservative brake applies inside this evidence class; full same-evidence-class pursuit is mandatory and literal impossibility means exactly every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available from VPS/local/MT5/broker/process/log/code/route evidence has been performed and recorded; expect real live defects after research integration but never invent issues or inflate severity without raw evidence; keep redacted_account, FTMO, V3 selector, V3 scheduler, V3 execution, processes, data, packets, candidates, lifecycle, risk, notification, namespace, rollback, tests, and verifiers in scope; personally inspect every material candidate/trade/lifecycle class through raw evidence rather than script-only summaries; checkpoints are mandatory after preflight, every material inspection, repair, activation, restart/reload, verifier/test run, compaction/resume, and before long operations; repair-first priority is mandatory, so ledgers/manifests/parsers/summaries must drive fixes and cannot expand while a proven repair is waiting; repair every verified defect, activate/restart/reload affected GTOS components immediately when needed, rerun focused tests/verifiers, preserve all material rows/events with no arbitrary top-N/top 3/5/10 cutoff, maintain finite work queues and checkpoints with no repeated loops without changed evidence or validator gap, record result materialization with result-use-status, exact-R/proxy-R/expectancy status where relevant, source-capture/source completeness, branch decision, implementation decision, runtime-effect boundary, source-use state, commit/push scoped repairs with exact path/ref proof, and keep the goal active until the CEO stops it or a tested persistent dual-broker supervisor replaces it.
