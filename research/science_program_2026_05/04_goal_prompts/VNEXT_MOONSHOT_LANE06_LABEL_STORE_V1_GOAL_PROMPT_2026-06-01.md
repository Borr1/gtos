# vNext Moonshot Lane 06 - Label Store V1

Own the label store for the moonshot program. Build explicit no-leak outcome labels that ML, selector, scheduler, execution, and replay can use without confusing broker-real truth, source-bound proxy truth, and projected/replay labels.

## Mandatory Context Use

Run mandatory preflight. Read doctrine, moonshot vision, master route, Lane01 source authority, Lane02 as-of contract, Lane03 candidate reconstruction, Lane04 microscope outputs, Lane05 feature contracts when present with exact absent-state row when not, broker truth lanes, Friday microscope, and local MT5 preservation artifacts. Do not rely on chat memory. After compaction/resume/interruption/uncertainty, reread prompt/starter/doctrine/latest artifacts from disk.

This is a label-store builder lane. Apply truthfulness and active creativity: preserve useful negative labels, avoid labels, stale blocker labels, and path anatomy labels. Do not let broker-real labels and proxy labels collapse into one class.

## Objective

Write route outputs under:

`research/operations/vnext_moonshot_lane06_label_store_v1_2026_06_01/`

Build explicit labels: SL-before-1R, partial-then-BE, partial-then-final, no-entry-touch, stuck/no-resolution, time-to-1R, time-to-SL, time-to-final, MFE, MAE, broker-real net-R, source-bound proxy-R, cost-adjusted R, execution-policy result, stale blocker, correct rejection, missed opportunity, manual intervention, broker modify failure, false close, ambiguity state, and no-trade baseline outcome.

## Required Work

- Define label schema with evidence class, source-use state, label timing, outcome availability, and downstream eligibility.
- Build label extraction from microscope paths, broker truth, selected denominator, replay rows, and source-bound proxy paths.
- Separate broker-real labels from source-bound proxy labels and replay/projection labels.
- Add no-leak checks that prevent labels from appearing in feature rows.
- Build label coverage and missing-label reason ledgers.
- Preserve all path/failure anatomy as labels for every row where the evidence is source-bound; when not source-bound, preserve the exact missing-field/source reason.

## Completion Standard

No arbitrary top-N, no positive-only labels, no losing-path discard. Full material labels and missing-label reasons must be preserved. Full same-evidence-class pursuit is required; every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action must be attempted or proven inapplicable.

Forbidden surfaces: live trading broker operation, order action, credential print/change, paid API/vendor calls, remote push, hidden production activation. Read-only broker/account/order/history/deal/position export is authorized when required for broker-real label truth.

Finish with label schema, builders, label ledgers, no-leak integration, coverage/gap ledgers, manifest, verifier, focused tests, and completion audit.

## Operative Hardening Anchor

Read `goal_session_research_discipline.md` and `research_operating_doctrine.md`; do not rely on chat memory. Treat those files as active instructions, not background. This is constructive research/build work with curiosity, active creativity, and no conservative brake inside the evidence class. Full same-evidence-class pursuit is mandatory. The phrase literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action has been attempted or proven inapplicable. No arbitrary top-N, top 3/5/10, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows. Produce artifact, ledger, manifest, verifier or focused test output, completion audit, source-capture or source completeness decisions, branch decision or implementation decision, and exact R, proxy R, or expectancy fields when result materialization is owned; when result fields are absent, produce row-level missing-field proof plus the strongest source-bound proxy or exact capture/export requirement. Scope boundaries are rails around execution, spend, credentials, remotes, and hidden production activation; they are not permission to shrink local/repo/MT5/read-only evidence pursuit. Forbidden surfaces remain production-change activation, live trading broker operation, paid API/vendor calls, broker account order history deal position changes, credential/remotes, and prompt/config/risk/execution/safety/canary/selector changes outside scoped default-off or dossier authority.
