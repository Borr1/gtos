# vNext Absolute Moonshot Lane18 - Broker Truth Cost Capture V2

Own broker truth and cost capture V2. The objective is to turn broker/account/order/deal/position/cost/spread/retcode truth into universal source contracts and forward-capture code so selector, scheduler, execution, replay, ML, repair, and command-center layers stop relying on stale projections where broker truth or exact proxy proof is required.

## Mandatory Context Use

Run mandatory preflight from disk before using memory. Read `.context/LIVE_STATE.md`, current system map, reading order, quick reference, `goal_session_research_discipline.md`, `research_operating_doctrine.md`, `vnext_absolute_moonshot_vision_and_limitations.md`, current Master route, Lane01 source authority, Lane02 as-of contract, Lane06 labels, Lane07 broker truth/cost calibration, Lane08 digital twin, Lane10 scheduler, Lane10B conflict anatomy, Lane11 execution policy engine, live companion route, MT5 local cache preservation route, compliant VPS preservation route, current execution/broker integration code, and notification/account truth code.

After compaction, resume, interruption, uncertainty, or long waits, regenerate `LIVE_STATE`, reread this prompt, starter, doctrine, and latest route artifacts from disk, then continue from route state.

This is a broker-truth/cost source and capture implementation lane. It must produce source contracts, capture/export requirements, default-off code paths, validators, tests, and downstream contracts.

Treat `goal_session_research_discipline.md` and `research_operating_doctrine.md` as active instructions, not background. Do not rely on chat memory. This lane must operationalize full same-evidence-class pursuit: literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available inside this evidence class has been attempted or proven inapplicable. Apply constructive builder posture with curiosity, active creativity, and no conservative brake inside the research evidence class.

## Objective

Write route outputs under:

`research/operations/vnext_absolute_moonshot_lane18_broker_truth_cost_capture_v2_2026_06_01/`

Build broker truth and cost capture V2 for all 24 symbols and every lifecycle surface that affects risk, execution, accounting, replay, ML labels, repair, and command-center truth.

## Required Work

- Inventory broker truth sources: local route evidence, shadow logs, account truth logs, MT5 local cache, broker history exports when present, symbol specs, session schedules, stops/freeze, tick value, contract size, margin, spread samples, commission, swap, order/deal/position history, partial close, residual tickets, modify retcodes, close reasons, false local close events, Telegram parity.
- Build universal broker-truth row contracts for orders, deals, positions, fills, partials, residuals, modifies, rejections, closes, costs, account snapshots, and manual/client interventions.
- Build source-gap ledgers for every missing symbol/window/field/source. Distinguish recoverable market data from non-generatable historical broker intent/order/lifecycle truth.
- Add or patch default-off/prospective capture code where current runtime lacks required fields: modify request/retcode details, stop/freeze context, spread-at-action, cost fields, ticket identity, partial/residual lifecycle, false close proof, account baseline, equity/balance, and Telegram parity source.
- Build cost calibration ledgers by symbol/session/regime where source fields exist, with exact proxy/capture status when fields are incomplete.
- Feed downstream contracts for Scheduler V3, Execution V3, Digital Twin V2, ML labels, Repair Companion, and Command Center.
- Add tests/verifiers that fail on projected PnL dominating broker truth, missing ticket-bound lifecycle fields, unmanaged false-close classification, missing cost-source reason, and broker-real/proxy label confusion.

## Completion Standard

No projection masquerading as broker truth. No generic cost gap. No lifecycle row without ticket/source identity. No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows, source states, cost fields, lifecycle fields, capture decisions, branch decisions, and implementation decisions. Completion requires universal contracts and code/capture repairs for every field that can be captured prospectively, with exact historical gaps where data was never logged.

Finish with builder/capture code for every capturable missing field, broker-truth contracts, cost ledgers, source-gap ledgers, downstream contract, verifier, focused tests, completion audit, source-use state, result-use status, source-capture or source completeness decisions, branch decision or implementation decision, exact R/proxy R/expectancy fields where result materialization is owned, row-level missing-field proof where fields are absent, runtime-effect boundary, and scoped commit.

## Hard Boundaries

Forbidden surfaces: production-change activation, live trading broker operation, broker account order history deal position mutation, paid API/vendor spend, credential changes or disclosure, remote push, hidden production activation, and prompt/config/risk/execution/safety/canary/selector live behavior changes outside scoped default-off or dossier authority. Read-only MT5/account/history/spec/source inspection is authorized for every locally available source; unavailable manual/export-only fields must become exact capture/export requirements.
