# G12 SCID READY8 Discriminative Numerical Screen Audit

Evidence class: `G12_SCID_READY8_DISCRIMINATIVE_NUMERICAL_SCREEN_AUDIT_ONLY`

Objective: independently audit the G0 repaired-discriminative numerical screen at `research/science_program_2026_05/06_outcome_testing/g0_scid_ready8_discriminative_target_result_control_evidence_synthesis_after_g12_audit` as quarantined control evidence only. Verify full-population execution, counts, row hashes/source pointers, denominator-role handling, fail-closed preservation, explanation/data-backing ledgers, no-shortcut proof, and forbidden-surface closure. Accept, reject, or repair same-G12 issues without opening validation, promotion, AI/API, paid/vendor, broker account/order/history/deal/position, registry, remote, raw-market-blob, or live trading behavior.

## Mandatory Context Use

1. Run `python scripts/generate_live_state.py`, then read `.context/LIVE_STATE.md`.
2. Read `.context/00_core/goal_session_research_discipline.md` and `.context/00_core/research_operating_doctrine.md` as active instructions.
3. Read `.context/00_core/research_current_state.md`, `.context/00_core/local_heavy_data_inventory.md`, and `.context/00_core/ai_in_loop_cost_control_research_plan.md`.
4. Read the accepted upstream G12 audit at `research/science_program_2026_05/06_outcome_testing/g12_scid_ready8_disc_target_result_audit`.
5. Read the G0 screen route, including decision, fact, screen-execution, aggregate, contrast, candidate-level, partition-level, failure-anatomy, explanation, ambiguity, closeout, data-backing, repair, prompt-hardening, saturation, manifest, verifier, and focused-test artifacts.

Apply strict-but-fair G12 audit posture. The builder/G0 route was allowed to be constructive and anti-boxed; this audit must attack count drift, denominator leakage, fail-closed dropping, performance-language leakage, shortcut artifacts, row/hash mismatches, missing explanation coverage, and unsafe next-route claims. Do not invent blockers, do not reject because the route was broad or constructive, and do not let audit posture become conservative theater. Every rejection or warning must identify the exact file, row family, count, hash, field, source, or rule that fails. If the issue is repairable inside this same G12 evidence class, repair it, rerun the affected verifier/tests, refresh the manifest/audit artifacts, and only then decide.

Do not audit by filename presence only. Parse the large JSONL ledgers enough to recompute schemas, line counts, status distributions, role distributions, movement-shift distributions, failure reasons, ambiguity terminal statuses, and explanation coverage. The accepted G0 route claims full-population exhaustion; this audit must independently confirm that claim from disk or show the exact counterexample.

## Required Checks

- Recompute or verify exact accepted upstream facts: `8` cards, `3,014` candidates, `24,112` repaired rowset rows, rowset hash `fa478206605376275ae971e283f977cc6c77a2d7fd395b82354df8380662c9e3`, `192,896` target rows, `162,336` computable rows, `30,560` fail-closed rows, horizons `1/4/16/32`, and two target families.
- Verify the G0 screen processed every accepted target-result row and did not use top-N-only, representative-only, compact-only, early-stop, or arbitrary-limit substitutes.
- Verify pass/control/non-applicable/fail-closed denominator roles remain visible and no row status was silently converted into a pass claim.
- Verify candidate-level, partition-level, failure-anatomy, explanation, ambiguity, and data-backing ledgers are complete enough to cover the controlling prompt, not just present by filename.
- Verify explanation rows do not convert neutral movement into R, PnL, win-rate, expectancy, performance, validation, promotion, or live-readiness claims.
- Verify next-route ranking sends this new numerical screen to independent G12 audit first.
- Verify `goal_session_research_discipline.md` and `research_operating_doctrine.md` are embedded into this prompt and were used in completion audit, not merely read.

## Same-G12 Repair And No-Loop Standard

- Same-G12 repair is mandatory for stale manifest hashes, text EOL drift, Windows path/pycache friction, missing verification-result refreshes, prompt/starter hash drift, obvious count cache drift, or verifier fragility when the underlying target ledgers are intact.
- Do not emit a new repair prompt for an issue that can be repaired, recomputed, rebound, rehashed, or reverified in this audit without crossing evidence class.
- If a true blocker remains, prove why it cannot be repaired from approved local inputs and name the exact file/field/source/access/action required.
- Mark complete only when `same_g12_repairable_items_remaining=0`, `same_g12_unanswered_audit_questions_remaining=0`, and the terminal decision is backed by machine-readable recomputation evidence.

## Required Outputs

- G12 decision ledger with exact terminal decision, accepted/rejected/repaired state, and no invented blocker language.
- Full recomputation ledger covering target rows, rowset rows, candidate rows, hashes, denominator roles, fail-closed rows, aggregate/contrast/explanation/ambiguity/failure/data-backing line counts, and key distributions.
- Same-G12 repair ledger, even if empty, listing every issue considered and its final status.
- Completion audit proving mandatory context use, large-ledger parsing, no-shortcut audit, same-G12 repair exhaustion, safe flags, and exact next route.
- Verification result plus focused tests; use AST/no-bytecode syntax fallback only when Windows bytecode/cache friction is the proven issue.
- If accepted, emit the next G0 learning/synthesis prompt and starter. If rejected, emit the exact same-evidence-class repair prompt and starter with every blocker already pursued as far as this audit permits.

Keep `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, and `live_effect=false`.
