# vNext Absolute Moonshot Selector V3

Own Selector V3. The objective is to turn the current Meta-Selector V2, selector-scheduler reconciliation, scheduler conflict anatomy, execution-policy evidence, scaled microscope, market whiteboard, broker/cost capture contracts, and source-capture repair outputs into a stronger default-off selector engine package with row-level provenance, no-leak proof, split/stress/deconcentration proof, runtime packet contracts, tests, verifiers, and downstream contracts.

Selector V3 is not a Friday rule, not a hardcoded positive subset, not a report, not a narrow ML handoff, and not another generic selector summary. It must decide which mechanisms deserve trade, reduce, avoid, capture, or source-repair treatment under the full current vNext evidence universe, while preserving every material negative and repairable pattern as system intelligence.

## Mandatory Context Use

Run mandatory preflight from disk before using memory. Regenerate `.context/LIVE_STATE.md`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/vnext_absolute_moonshot_vision_and_limitations.md`, the latest handoff as historical only, and the current Master route at `research/operations/vnext_absolute_moonshot_master_orchestration_2026_06_01/`.

Read terminal/current route inputs from disk:

- Lane01 source authority.
- Lane02 no-leak/as-of contract.
- Lane03 historical candidate reconstruction.
- Lane04 historical microscope engine.
- Lane05 feature store.
- Lane06 label store.
- Lane07 broker truth/cost calibration.
- Lane08 digital twin replay.
- Lane09 Meta-Selector V2.
- Lane09B selector-scheduler reconciliation.
- Lane10 Scheduler V2.
- Lane10B scheduler conflict anatomy and multi-ticket design.
- Lane11 execution policy engine.
- Lane16 historical microscope scale.
- Lane17 market awareness whiteboard.
- Lane18 broker truth/cost capture V2.
- Post-Lane18 Source Capture Repair route if present and terminal; if it is absent, consume current Lane16/Lane17/Lane18 source-gap contracts and record the exact dependency while completing every independent selector task.
- Current selector/router/runtime packet code and default-off policy router code.

After compaction, resume, interruption, uncertainty, or long waits, regenerate `LIVE_STATE`, reread this prompt, starter, doctrine, Master post-Lane18 decision, latest Source Capture Repair artifacts if present, and latest Selector V3 route artifacts from disk, then continue from route state. Do not rely on chat memory.

Treat `goal_session_research_discipline.md` and `research_operating_doctrine.md` as active instructions, not background. Operationalize full same-evidence-class pursuit: literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available inside this evidence class has been attempted or proven inapplicable. Use constructive selector-builder posture with curiosity, active creativity, and no conservative brake inside this evidence class.

## Objective

Write route outputs under:

`research/operations/vnext_absolute_moonshot_selector_v3_2026_06_01/`

Build a default-off Selector V3 package that consumes the complete current evidence stack and produces selector logic, rule/score/action packages, source-gap dispositions, tests, verifiers, and downstream contracts.

## Required Work

- Join Lane09 selector evidence, Lane09B scheduler-aware refinement, Lane10B conflict anatomy, Lane11 expanded policy evidence, Lane16 path anatomy, Lane17 market whiteboard fields, Lane18 broker/cost capture contracts, and Source Capture Repair outputs where present.
- Preserve full row identity across the 289,928-row replay/microscope universe and every relevant missing-gap row. No row may disappear without a machine-readable disposition.
- Build selector actions across trade, reduce_risk, avoid, capture_repair, no_trade_by_evidence, and source_required. Do not collapse loss patterns into generic rejects; convert them into avoid/reduce/capture intelligence.
- Evaluate mechanism/session/symbol/side/regime/origin/framework/spread-to-risk/source-completeness/market-whiteboard/execution-policy/scheduler-conflict interactions without arbitrary top-N cutoffs. Full ledgers must preserve all material groups before ranked summaries.
- Identify false-positive selector families, scheduler-blocked positive edge families, source-gap families, policy-dependent families, and stable/unstable promoted families using split/stress/deconcentration evidence.
- Build a runtime-eligible selector packet contract: only as-of decision fields, no outcomes, no path labels, no broker-realized profit, no future fields, no hidden label leakage.
- Build default-off implementation code or package files that can be consumed by runtime/router tests without activating live behavior.
- Build source-gap and capture-dependency rows for every selector decision that cannot be made from current source truth.
- Produce branch decisions by mechanism family: promote, reduce, avoid, capture, merge, redesign, keep default-off, or kill only the unsupported current claim while preserving useful mechanism intelligence.
- Add verifiers and focused tests that fail on missing row joins, hidden future fields, stale Lane09-only behavior, Friday-only narrowing, source-gap loss, arbitrary top-N closure, old-system leakage, unbounded live activation, and missing downstream contracts.

## Saturation And Self-Red-Team Pass

Before completion, run a written saturation pass and pursue every same-evidence-class gap it exposes. At minimum answer with artifacts:

- Which Selector V2 families survive Scheduler V2/V3 evidence, Lane11 policy evidence, Lane16 microscope, and Lane17 whiteboard context?
- Which promoted families become reduce_risk, avoid, capture_repair, or source_required after full evidence is joined?
- Which scheduler-blocked positive families deserve selector redesign rather than deletion?
- Which loss clusters become avoid rules, reduce rules, or source-capture requirements?
- Which market-whiteboard states alter selector interpretation by symbol/session/regime/side?
- Which fields are runtime-eligible and which are forbidden label/result fields?
- Which source gaps prevent exact selector action, and what exact Source Capture Repair or forward-capture field owns them?
- Which future G12/production-change audit rejection has been preempted by tests, verifiers, manifests, and no-leak contracts?

If any answer exposes a feasible same-evidence-class join, parser, repair, split, ablation, stress, branch decision, or verifier, do it before completion.

## Completion Standard

Completion requires:

- full selector evidence ledger;
- mechanism/action decision ledger;
- selector-scheduler-execution integration ledger;
- market-whiteboard selector context ledger;
- source-gap and capture-dependency ledger;
- no-leak/runtime-eligible packet schema;
- default-off Selector V3 package or implementation code;
- source-capture and source completeness decisions;
- branch decision and implementation decision ledgers;
- result-use status with exact R, proxy R, and expectancy fields where selector result materialization is owned, and row-level proof where result fields are outside this lane;
- split/stress/deconcentration outputs;
- downstream contracts for Scheduler V3, Execution Policy V3, ML, Repair Companion, Command Center, and production dossier;
- route manifest, verifier, focused tests, completion audit, and scoped commit.

No hardcoded Friday rule. No ML-only handoff. No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff. No activation of live selector behavior. Completion means Selector V3 has a concrete default-off package and machine-checkable evidence, not a recommendation.

## Hard Boundaries

Forbidden surfaces: production-change activation, live trading broker operation, broker account order history deal position mutation, paid API/vendor spend, credential changes or disclosure, remote push, hidden production activation, and prompt/config/risk/execution/safety/canary/selector live behavior changes outside scoped default-off package or dossier authority.

Authorized inside this evidence class: read-only local/repo/source inspection, route artifact parsing, local heavy-data parsing, source-capture repair consumption, selector package generation, default-off code paths, tests, verifiers, manifests, no-leak checks, branch decisions, implementation decisions, source completeness decisions, exact/proxy R analysis where owned by selector materialization, and scoped commits.
