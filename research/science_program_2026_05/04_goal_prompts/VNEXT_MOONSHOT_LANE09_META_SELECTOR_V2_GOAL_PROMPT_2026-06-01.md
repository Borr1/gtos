# vNext Moonshot Lane 09 - Meta-Selector V2

Own the next-generation meta-selector. The objective is to decide which candidates deserve risk, which must be avoided, which must be reduced, which need capture/repair, and which need a different execution policy, using broad historical evidence instead of one Friday rule.

## Mandatory Context Use

Run mandatory preflight. Read doctrine, moonshot vision, master route, Lane01 source authority, Lane02 as-of contract, Lane03 candidate reconstruction, Lane04 microscope, Lane05 features, Lane06 labels, Lane07 broker truth, Lane08 replay when present with exact absent-state row when not, and current Lane03 next-level selector package. Do not rely on chat memory. After compaction/resume/interruption/uncertainty, reread prompt/starter/doctrine/latest artifacts from disk.

This is a selector discovery and default-off implementation lane. Treat examples as starting points. Current GTOS frameworks are not the horizon. Search every source-bound mechanism, avoid pattern, inverse pattern, regime split, and failure family available in the approved evidence.

## Objective

Write route outputs under:

`research/operations/vnext_moonshot_lane09_meta_selector_v2_2026_06_01/`

Build Meta-Selector V2 over selected/broker-ready/replay-eligible denominators. It must evaluate symbol, session, origin, framework, side, spread/R, volatility, M1/tick behavior, source completeness, selected-cell risk proof, historical split performance, cost stress, concentration, failure anatomy, broker feasibility, and every live/forward behavior slice that can be source-bound.

## Required Work

- Build selector candidate rule/score families: tradeable, avoid, reduce, capture/repair, route-to-policy, no-trade.
- Evaluate across broad selected denominators, Friday microscope, daily/weekly/monthly slices, leave-symbol-out, leave-session-out, regime holds, cost stress, and concentration.
- Preserve all material positive and negative families. Loss patterns must become avoid/intelligence candidates, not discarded rows.
- Produce exact/proxy R, expectancy, PF, WR, drawdown, and stress metrics when fields exist; when fields are absent, write row-level missing-field proof and strongest source-bound proxy.
- Implement default-off selector package and runtime packet fields for every selector clause that survives evidence gates; unsupported clauses get kill/redesign/capture decisions.
- Add tests that prevent Friday-only hardcoding and post-outcome feature use.

## Completion Standard

No arbitrary top-N, no one-Friday rule, no one-symbol/session narrowing, no positive-only summaries. Full same-evidence-class pursuit is required: every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action must be attempted or proven inapplicable.

Forbidden surfaces: live trading broker operation, order action, credential print/change, paid API/vendor calls, remote push, hidden production activation. Prompt/config/selector code is authorized for scoped default-off research/package patches with tests and verifier.

Finish with selector evidence ledgers, branch decisions, default-off package, runtime packet/capture requirements, manifest, verifier, focused tests, and completion audit.

## Operative Hardening Anchor

Read `goal_session_research_discipline.md` and `research_operating_doctrine.md`; do not rely on chat memory. Treat those files as active instructions, not background. This is constructive research/build work with curiosity, active creativity, and no conservative brake inside the evidence class. Full same-evidence-class pursuit is mandatory. The phrase literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action has been attempted or proven inapplicable. No arbitrary top-N, top 3/5/10, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows. Produce artifact, ledger, manifest, verifier or focused test output, completion audit, source-capture or source completeness decisions, branch decision or implementation decision, and exact R, proxy R, or expectancy fields when result materialization is owned; when result fields are absent, produce row-level missing-field proof plus the strongest source-bound proxy or exact capture/export requirement. Scope boundaries are rails around execution, spend, credentials, remotes, and hidden production activation; they are not permission to shrink local/repo/MT5/read-only evidence pursuit. Forbidden surfaces remain production-change activation, live trading broker operation, paid API/vendor calls, broker account order history deal position changes, credential/remotes, and prompt/config/risk/execution/safety/canary/selector changes outside scoped default-off or dossier authority.
