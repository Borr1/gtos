# vNext Absolute Moonshot Lane17 - Market Awareness Whiteboard

Own the unified market-awareness whiteboard. The objective is to build the explicit state layer that lets vNext know what every market is doing, what data is trustworthy, what regime it is in, what spread/cost/liquidity conditions apply, and how that state should feed selector, scheduler, execution, ML, repair, and command-center layers.

## Mandatory Context Use

Run mandatory preflight from disk before using memory. Read `.context/LIVE_STATE.md`, current system map, reading order, quick reference, `goal_session_research_discipline.md`, `research_operating_doctrine.md`, `vnext_absolute_moonshot_vision_and_limitations.md`, current Master route, Lane01 source authority, Lane02 as-of contract, Lane03 reconstruction, Lane04 microscope, Lane05 feature store, Lane06 label store, Lane07 broker truth/cost, Lane08 digital twin, Lane09 selector, Lane09B, Lane10 scheduler, Lane10B, Lane11 execution policy engine, and current runtime market-state/capture code.

After compaction, resume, interruption, uncertainty, or long waits, regenerate `LIVE_STATE`, reread this prompt, starter, doctrine, and latest route artifacts from disk, then continue from route state.

This is a market-state implementation lane. It must produce schemas, builders, replay/forward state rows, validators, tests, and default-off integration contracts.

Treat `goal_session_research_discipline.md` and `research_operating_doctrine.md` as active instructions, not background. Do not rely on chat memory. This lane must operationalize full same-evidence-class pursuit: literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available inside this evidence class has been attempted or proven inapplicable. Apply constructive builder posture with curiosity, active creativity, and no conservative brake inside the research evidence class.

## Objective

Write route outputs under:

`research/operations/vnext_absolute_moonshot_lane17_market_awareness_whiteboard_2026_06_01/`

Build a unified vNext market whiteboard covering all 24 symbols across available D1/H4/H1/M15/M1/tick/spread/session/correlation/regime/source-completeness data.

## Required Work

- Define the market-awareness schema: symbol, broker symbol, timestamp, timeframe coverage, HTF structure, M15 structure, M1 path state, tick state, spread-to-risk, volatility state, session state, market-hours state, source completeness, broker feasibility fields, correlation/cluster fields, regime fields, and stale/null reason fields.
- Build whiteboard builders for historical replay rows and current/forward snapshots, with exact source-gap rows for every missing field, symbol, window, or source.
- Join source authority and as-of contracts so every whiteboard field is labeled as decision-available, replay-only, label-only, broker-real, proxy, missing, or non-generatable.
- Build correlation/cluster and risk-on/risk-off proxy states using existing local/replay data only.
- Build spread/liquidity/friction state by symbol/session where source fields exist; write exact capture requirements where they do not.
- Connect the whiteboard to downstream contracts for Selector V3, Scheduler V3, Execution V3, ML, Repair Companion, and Command Center.
- Add tests/verifiers that fail on future leakage, stale old-system fields, missing source-state reason, symbol starvation, broken 24-symbol coverage, and one-timeframe-only state.
- Produce implementation decisions for runtime packet fields and default-off forward capture fields without activating hidden live behavior.

## Completion Standard

No single-timeframe whiteboard. No hidden nulls. No summary-only state. No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows, source states, symbol coverage, field coverage, and decisions. No source gap without exact field/symbol/window/source requirement. Completion requires a machine-readable whiteboard that downstream lanes can consume without reading old stale context.

Finish with schema, builder, whiteboard ledgers, source-completeness ledgers, correlation/regime ledgers, downstream contract, verifier, focused tests, completion audit, source-use state, result-use status, runtime-effect boundary, and scoped commit.

## Hard Boundaries

Forbidden surfaces: production-change activation, live trading broker operation, broker account order history deal position mutation, paid API/vendor spend, credential changes or disclosure, remote push, hidden production activation, and prompt/config/risk/execution/safety/canary/selector live behavior changes outside scoped default-off or dossier authority. Read-only local/repo/MT5/cache/source inspection and local historical parsing are authorized inside this evidence class.
