# NO_API_MECHANICAL_REPLAY_ENGINE_FROM_SOURCE_UNIVERSE Goal Prompt

Date: 2026-05-10
Owner lane: no-API mechanical replay from audited source universe
Promotion posture: `NO_PROMOTION_VERDICT`
Required flags: `validation_safe=false`, `outcome_review_opened=false`, `live_effect=false`

## Goal

Build `NO_API_MECHANICAL_REPLAY_ENGINE_FROM_SOURCE_UNIVERSE` from the source universe and contracts emitted by `NO_API_HISTORICAL_REPLAY_ENGINE_AND_MISSED_OPPORTUNITY_INVENTORY`.

Use the audited source universe to run broad deterministic pre-AI mechanical candidate replay across source-safe local historical OHLCV/tick/context data. This is discovery/control work only. It must not claim original GTOS AI intent unless source-logged prompt/input/output/gate/lifecycle truth exists. Projection rows remain projection-only.

The route should be aggressive about breadth and learning throughput, but strict about pre-registration and evidence class. It must freeze mechanical definitions before replay, include dumb/adversarial baselines where source-safe, and make every output separable from validation/promotional claims.

This route must pursue every source-safe mechanical family it opens, and every high-value family it considers but does not open, to a terminal status. A family or source slice may end as replay-inventory-built, discovery-path-label-inventory-built, exact source/hash/parser blocker, exact projection-only blocker, exact evidence-class blocker, or not-applicable with proof. It must not end as "not attempted", "too broad", "n too small", "not local", "future work", or "needs data" unless the approved source/search/acquisition ladder has been exhausted, the same-evidence-class continuation path has been pursued as far as safe, and the exact next route or owner action is written.

The goal is not to build a toy prototype. Build the strongest source-safe no-API mechanical replay substrate that the audited universe, local heavy data, and current safety boundaries allow. Bounded passes are acceptable only as explicit engineering bounds with a machine-checkable excluded-slice ledger and a concrete continuation route; they are not acceptable as a way to avoid obvious same-evidence-class work.

## Mandatory Preflight

1. Run `python scripts\generate_live_state.py`.
2. Read `.context\LIVE_STATE.md`.
3. Read the latest numbered `.context\02_session_handoffs\*`.
4. Read `.context\00_core\quick_reference_card.md`.
5. Read `.context\00_core\research_operating_doctrine.md`.
6. Read `.context\00_core\goal_session_research_discipline.md`.
7. Read `.context\00_core\local_heavy_data_inventory.md`.
8. Read `.context\00_core\ai_in_loop_cost_control_research_plan.md`.
9. Read `.context\00_core\research_current_state.md`.
10. Read `research\science_program_2026_05\06_outcome_testing\no_api_historical_replay_engine_and_missed_opportunity_inventory\NO_API_HISTORICAL_REPLAY_SOURCE_UNIVERSE_LEDGER_2026-05-10.json`.
11. Read the replay source contract, partition ledger, missed-opportunity inventory summary, and completion audit from the same route directory.

## Evidence Class

`NO_API_MECHANICAL_PRE_AI_REPLAY_DISCOVERY_ONLY`.

Allowed: deterministic mechanical candidate generation, source-hashed OHLC/tick/context replay, projection-only missed-opportunity comparators, duplicate/no-leak/partition controls, breadth/stress diagnostics, and source expansion requirements.

Forbidden: validation execution, promotion, PnL/R/win-rate/expectancy claims as validation, broker actual-R, MT5 account/order/history/deal/position values, paid/API/Databento calls, remote push, live restart, live trading behavior, prompt/config/risk/permissions/safety/selector/canary changes, credentials, and claiming projection rows are original GTOS AI intent.

## Required Work

1. Select source rows from the audited source universe with explicit hashes or large-file hash prerequisites. Re-run or cite the latest accepted local data catalog/runtime source-universe snapshot when path drift, sibling-worktree paths, ignored heavy data, or large-file deferrals could affect coverage. If a large-file hash deferral blocks a high-value source-safe family, resolve the hash/manifest requirement inside this route when safe instead of skipping the family.
2. Freeze input schemas, duplicate keys, no-lookahead/as-of policy, and projection-only label policy before running replay.
3. Build a mechanical-family registry before path inspection. At minimum consider OB/retest, FVG/fill, breaker/re-entry, opening-drive/no-fill lifecycle, session/KZ sweeps, liquidity/stop-run context, simple baseline comparators, and any source-safe adjacent family suggested by the current research state, source universe, missed-opportunity ledger, shadow logs, science doctrine, or local data catalog. If a family is excluded, record the exact source, schema, as-of, parser, compute, evidence-class, or contamination blocker; search for the missing source first; and write a repair/continuation prompt only after same-evidence-class pursuit is exhausted or a hard boundary is reached.
4. Build deterministic no-API replay tooling for broad mechanical candidate discovery across symbols, timeframes, sessions, kill zones, source families, regimes/market phases where source-safe, and available historical partitions.
5. Produce unscored candidate/source inventories first; if the route opens path labels, keep them discovery-only and separate from validation/result/promotion language.
6. Include baseline and robustness controls where possible without scoring validation: random-time/window comparators, shifted-entry/delayed-entry variants, simple momentum/mean-reversion baselines, duplicate/concentration checks, source-family splits, and top-outlier sensitivity flags. These are discovery diagnostics, not promotion metrics.
7. Audit breadth, duplicates, concentration, missing source fields, source-family coverage, timeframe coverage, session/KZ coverage, symbol coverage, regime/phase coverage, and projection-only boundaries.
8. Produce a family terminal-status ledger covering every considered family/source slice and every skipped family/source slice.
9. Produce builder, verifier, focused tests, no-leak audit, completion audit, and the next selected full prompt/starter.

## Saturation Mandate

Before completion, run a written and machine-checkable saturation pass. This pass must answer whether the route accidentally boxed itself by family, timeframe, symbol, session, source type, local worktree, parser availability, large-file hashing, compute convenience, or the first obvious hypothesis.

The saturation pass must include:

1. A family/source-slice registry with status for every considered mechanical family and source slice, including skipped items.
2. A searched-root ledger covering current worktree, absolute main repo data roots, ignored heavy data roots, prior worktrees under `C:\tmp\gtos_otb`, Sierra roots, MT5/recovered tick roots, shadow logs, source-control ledgers, and the accepted local research catalog when relevant.
3. A same-evidence-class continuation ledger for every blocker found during implementation. If a blocker can be cleared with local search, parser repair, hash repair, read-only extraction, source-safe fixture generation, or explicit approval inside the lane, pursue it before completion.
4. A proof that no opened family was left at "prototype only" when the same prompt could safely continue to candidate inventory or discovery path-label inventory.
5. A ledger of exact excluded rows/slices/families with the reason, the searched sources, whether the missing object is recoverable market data or non-generatable GTOS source-state truth, and the next executable route if any.
6. A no-placeholder scan for `TBD`, `TODO`, `unknown`, `maybe`, `later`, and generic "future work" language in generated decision/completion artifacts, allowing those words only inside quoted source text or exact negative-evidence labels with explanation.

If the route discovers a new source-safe family, source root, parser gap, or replay branch that materially changes coverage, it should pursue it in this goal unless doing so crosses into validation, result scoring, AI/API calls, live trading behavior, paid/vendor access, credentials/remotes, or another explicit evidence-class gate.

## Completion Standard

Complete only when the deterministic no-API replay inventory is built or proven impossible with exact source/hash/parser blockers, all projection-only and source-state boundaries are machine-checkable, no validation/result/live/API surfaces are opened, verifier/tests pass, scoped artifacts are committed, and the completion audit records `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, and `live_effect=false`.

## Hardening Requirements

Do not under-scope this route to a narrow sample unless the audited source universe proves only that sample is source-safe. If compute/time forces a bounded first pass, the route must make that bound explicit, maximize coverage within it, prove why the unrun work could not safely fit the current evidence class, and emit a continuation manifest that names every excluded family/source slice and exact next action. Do not treat `n too small`, `not local`, `worktree missing`, `too many rows`, `too expensive`, `too broad`, or `not in git` as terminal blockers before the approved source/search/acquisition ladder and same-evidence-class continuation ladder are exhausted. If approval is needed for a source-safe command or local access, request it and continue after approval rather than stopping. Do not run ad hoc path scoring before mechanical definitions, duplicate policies, and as-of rules are frozen. Do not use AI/API calls. Do not call a discovery label a result.

The useful output is a machine-checkable replay inventory and a ranked next route. If the next best route is a discovery path-label lane, write it as discovery-only. If the next best route is source repair or source expansion V2, write that instead. If the next best route is API/AI-delta sampling, leave it as API-gated later and do not call the API.

The completion audit must include: source universe rows consumed, rows excluded with exact reason, family terminal-status counts, source-family coverage, symbol/timeframe/session/KZ/regime coverage, large-file deferral resolutions or exact blockers, and a next-route plan that continues the highest-value unfinished source-safe work rather than drifting back to passive waiting.
