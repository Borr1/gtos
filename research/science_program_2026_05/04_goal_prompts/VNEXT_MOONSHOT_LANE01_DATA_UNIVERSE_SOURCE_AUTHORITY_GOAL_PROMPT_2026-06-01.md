# vNext Moonshot Lane 01 - Data Universe And Source Authority

Own the canonical data universe for the absolute moonshot program. Build the source authority map that tells every downstream lane what evidence exists, what it can prove, what it cannot prove, where it lives, how complete it is, and how each source is authorized for use without leakage or stale-context drift.

## Mandatory Context Use

Run mandatory preflight. Read `.context/LIVE_STATE.md`, current vNext map, reading order, quick reference, `goal_session_research_discipline.md`, `research_operating_doctrine.md`, the absolute moonshot vision file, the master orchestration route when present and exact dependency-state row when absent, the MT5 local cache preservation route, the compliant VPS/data preservation route, Friday microscope route, next-level lane package, live companion route, and selected-denominator artifacts. Do not rely on chat memory. After compaction/resume/interruption/uncertainty, reread this prompt, starter, doctrine, and latest lane artifacts from disk.

This is a data/source-authority builder lane. Treat doctrine as active instructions, not background. Apply curiosity and active creativity: search beyond obvious roots, current summaries, one timeframe, one route, one data modality, and one symbol. Result materialization means source inventories, source completeness, branch decisions, exact source-use state, and implementation decisions for downstream lanes.

## Objective

Write route outputs under:

`research/operations/vnext_moonshot_lane01_data_universe_source_authority_2026_06_01/`

Inventory and classify every material data surface available to the moonshot program: repo `data/`, `exports/`, `shadow_logs/`, `pipeline_state/`, `knowledge_base/`, next-level lanes, Friday microscope route, live companion route, activation repair routes, local MT5 preservation manifest/archive listing, broker/server read-only exports when present, and any locally available heavy-data roots referenced by current context. Read-only MT5 market/history/spec/account-history export is authorized when required to close source authority; if unavailable after all authorized local/read-only acquisition is exhausted, record exact missing symbol/window/field/source requirement.

Do not create Sierra, Databento, orderflow/depth API work. Do not use paid/vendor sources.

## Required Work

- Build a data-source inventory with path, evidence class, authority level, row count/file count/bytes, time coverage, symbol coverage, timeframe coverage, format, parser availability, freshness, and source-use rules.
- Classify source authority: broker-real truth, MT5 market history, local MT5 cache, replay projection, selected denominator, shadow/live runtime log, route summary, stale/historical/comparator, sensitive/excluded, missing but recoverable, non-generatable historical truth.
- Compare local repo evidence against preserved MT5 cache and any read-only broker/server export evidence.
- Produce source gap ledgers for tick, M1, M5, M15, H1, H4, D1, broker specs, account/deal/order/position history, spread, stops/freeze, swaps, commissions, symbol aliases, session schedules, and cost fields.
- Produce downstream source contracts for the historical microscope, feature store, label store, digital twin, ML, selector, scheduler, execution, and broker truth lanes.
- Add parsers/checkers for every downstream source class that lacks deterministic parser, schema, or source-completeness enforcement.

## Completion Standard

No arbitrary top-N, representative-only, or number-limited cutoff. Preserve all material roots, files, source classes, gaps, and authority decisions. Full same-evidence-class pursuit is required: every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action must be attempted or proven inapplicable before a source blocker is final.

Forbidden surfaces: live trading broker operation, order action, credential print/change, paid API/vendor calls, remote push, hidden production activation. Broker/account/order/history/deal/position reading/export is authorized when read-only and source-authority relevant.

Finish with inventory ledgers, source authority map, gap ledgers, downstream contracts, manifest, verifier, focused tests, and completion audit.

## Operative Hardening Anchor

Read `goal_session_research_discipline.md` and `research_operating_doctrine.md`; do not rely on chat memory. Treat those files as active instructions, not background. This is constructive research/build work with curiosity, active creativity, and no conservative brake inside the evidence class. Full same-evidence-class pursuit is mandatory. The phrase literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action has been attempted or proven inapplicable. No arbitrary top-N, top 3/5/10, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows. Produce artifact, ledger, manifest, verifier or focused test output, completion audit, source-capture or source completeness decisions, branch decision or implementation decision, and exact R, proxy R, or expectancy fields when result materialization is owned; when result fields are absent, produce row-level missing-field proof plus the strongest source-bound proxy or exact capture/export requirement. Scope boundaries are rails around execution, spend, credentials, remotes, and hidden production activation; they are not permission to shrink local/repo/MT5/read-only evidence pursuit. Forbidden surfaces remain production-change activation, live trading broker operation, paid API/vendor calls, broker account order history deal position changes, credential/remotes, and prompt/config/risk/execution/safety/canary/selector changes outside scoped default-off or dossier authority.
