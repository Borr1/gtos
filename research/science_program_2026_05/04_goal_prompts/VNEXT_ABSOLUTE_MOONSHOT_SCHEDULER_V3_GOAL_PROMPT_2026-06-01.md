# vNext Absolute Moonshot Scheduler V3

Own Scheduler V3. The objective is to turn Lane10 Scheduler V2, Lane10B conflict anatomy, Lane09B selector-scheduler reconciliation, Lane11 execution-policy evidence, Lane16 historical microscope, Lane17 market whiteboard, Lane18 broker/cost capture contracts, and Source Capture Repair outputs into a stronger default-off portfolio scheduler package that governs trade admission by real account-risk exposure, lifecycle semantics, market context, and opportunity priority.

Scheduler V3 is not a count-cap refresh, not a risk summary, not a static max-trades rule, and not a passive conflict ledger. It must build the next scheduler design/code/package that admits many small strong opportunities when total worst-case exposure is inside limits, blocks or reduces weak/oversized/correlated exposure, and converts missed edge into concrete scheduling intelligence.

## Mandatory Context Use

Run mandatory preflight from disk before using memory. Regenerate `.context/LIVE_STATE.md`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/vnext_absolute_moonshot_vision_and_limitations.md`, the latest handoff as historical only, and the current Master route at `research/operations/vnext_absolute_moonshot_master_orchestration_2026_06_01/`.

Read terminal/current route inputs from disk:

- Lane01 source authority.
- Lane02 no-leak/as-of contract.
- Lane03 historical candidate reconstruction.
- Lane04 historical microscope engine.
- Lane05 feature store.
- Lane06 label store.
- Lane07 broker truth/cost calibration.
- Lane08 digital twin replay.
- Lane09 Meta-Selector V2.
- Lane09B selector-scheduler reconciliation.
- Lane10 Scheduler V2.
- Lane10B scheduler conflict anatomy and multi-ticket lifecycle design.
- Lane11 execution policy engine.
- Lane16 historical microscope scale.
- Lane17 market awareness whiteboard.
- Lane18 broker truth/cost capture V2.
- Post-Lane18 Source Capture Repair route if present and terminal; if it is absent, consume current gap contracts and record the exact dependency while completing every independent scheduler task.
- Current risk/config/scheduler/runtime packet code and account-risk governor code.

After compaction, resume, interruption, uncertainty, or long waits, regenerate `LIVE_STATE`, reread this prompt, starter, doctrine, Master post-Lane18 decision, latest Source Capture Repair artifacts if present, and latest Scheduler V3 route artifacts from disk, then continue from route state. Do not rely on chat memory.

Treat `goal_session_research_discipline.md` and `research_operating_doctrine.md` as active instructions, not background. Operationalize full same-evidence-class pursuit: literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available inside this evidence class has been attempted or proven inapplicable. Use constructive scheduler-builder posture with curiosity, active creativity, and no conservative brake inside this evidence class.

## Objective

Write route outputs under:

`research/operations/vnext_absolute_moonshot_scheduler_v3_2026_06_01/`

Build a default-off Scheduler V3 package that consumes complete current evidence and produces account-risk exposure admission, reduced-risk admission, conflict anatomy repair, multi-ticket lifecycle design, tests, verifiers, and downstream contracts.

## Required Work

- Join Lane10 scheduler rows, Lane10B conflict anatomy, Lane09B reconciliation, Lane11 policy dependency, Lane16 path anatomy, Lane17 market whiteboard/correlation/regime state, Lane18 broker/cost capture contracts, and Source Capture Repair outputs where present.
- Preserve full 289,928-row scheduler identity and all material missing-gap/conflict rows. No accepted, reduced, rejected, blocked-positive, false-positive, or source-gap row may disappear without a machine-readable disposition.
- Replace stale count/session-cap authority with account-risk exposure authority in the default-off package: balance/equity, day-start baseline, realized broker PnL, open worst-case SL risk, pending worst-case SL risk, new trade risk, selected-cell risk, actual SL distance, lot/contract geometry, spread/slippage/commission/swap buffer, daily overlay, external prop limits, total account limits, realized cushion, and drawdown compression.
- Build multi-ticket and same-symbol lifecycle design from Lane10B: isolated ticket identity, same-symbol same-side stacking, long/short hedged conflicts, partial release, BE release, residual tickets, conflict priority, and exact conditions for reject/reduce/admit.
- Build correlation/cluster exposure rules using Lane17 correlation/regime fields and exact source gaps where correlation state is incomplete.
- Convert the `repairable scheduler block` families into concrete Scheduler V3 actions: admit, admit reduced-risk, delay, replace, queue, conflict-net, require source, or reject with exact current reason.
- Rank competing opportunities by selector quality, scheduler state, market context, policy expectation, cost stress, source completeness, path anatomy, and portfolio marginal risk without arbitrary top-N cutoff. Full ledgers must preserve all material rows before ranked summaries.
- Build default-off scheduler package/code that can be consumed by tests and future runtime integration without activating live behavior.
- Add verifiers and focused tests that fail on stale count-cap authority, missing money-risk fields, exposure math gaps, unbounded correlated risk, same-symbol lifecycle ambiguity, lost blocked-edge rows, hidden source gaps, arbitrary top-N closure, old-system leakage, and live activation.

## Saturation And Self-Red-Team Pass

Before completion, run a written saturation pass and pursue every same-evidence-class gap it exposes. At minimum answer with artifacts:

- Which Lane10 rejected/conflict rows were correct, repairable, source-limited, or scheduler-design limited?
- Which blocked-positive rows can be recovered by reduced-risk admission, partial release, BE release, same-symbol lifecycle semantics, correlation-aware netting, or priority ranking?
- Which apparent opportunities remain bad after money-risk, market-state, execution-policy, and cost context?
- Which count/session-cap remnants still exist in code/config/tests and how are they neutralized or preserved as evidence-only fields?
- Which exact account-risk fields are missing and what source/capture/export owns them?
- Which same-symbol/multi-ticket scenarios are supported, rejected, or default-off pending lifecycle proof?
- Which scheduler decisions would a skeptical production-change audit reject, and what tests/verifiers prevent that failure?

If any answer exposes a feasible same-evidence-class join, parser, repair, stress, exposure computation, lifecycle design, branch decision, or verifier, do it before completion.

## Completion Standard

Completion requires:

- full scheduler evidence ledger;
- accepted/reduced/rejected decision ledger;
- conflict anatomy disposition ledger;
- blocked-edge recovery ledger;
- money-risk exposure ledger;
- same-symbol and multi-ticket lifecycle design contract;
- correlation/cluster exposure ledger;
- source-gap and capture-dependency ledger;
- default-off Scheduler V3 package or implementation code;
- source-capture and source completeness decisions;
- branch decision and implementation decision ledgers;
- result-use status with exact R, proxy R, and expectancy fields where scheduler result materialization is owned, and row-level proof where result fields are outside this lane;
- split/stress/deconcentration outputs;
- downstream contracts for Selector V3, Execution Policy V3, ML, Repair Companion, Command Center, and production dossier;
- route manifest, verifier, focused tests, completion audit, and scoped commit.

No static max-trades authority. No count-cap closure. No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff. No live scheduler activation. Completion means Scheduler V3 has a concrete default-off package and machine-checkable exposure/lifecycle evidence, not a recommendation.

## Hard Boundaries

Forbidden surfaces: production-change activation, live trading broker operation, broker account order history deal position mutation, paid API/vendor spend, credential changes or disclosure, remote push, hidden production activation, and prompt/config/risk/execution/safety/canary/selector live behavior changes outside scoped default-off package or dossier authority.

Authorized inside this evidence class: read-only local/repo/source inspection, route artifact parsing, local heavy-data parsing, source-capture repair consumption, scheduler package generation, default-off code paths, tests, verifiers, manifests, no-leak checks, branch decisions, implementation decisions, source completeness decisions, exact/proxy R analysis where owned by scheduler materialization, and scoped commits.
