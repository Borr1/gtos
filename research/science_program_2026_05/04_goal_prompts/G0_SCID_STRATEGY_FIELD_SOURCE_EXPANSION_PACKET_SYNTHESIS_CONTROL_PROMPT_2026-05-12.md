# G0 SCID Strategy-Field Source Expansion Packet Synthesis Control Prompt

Date: 2026-05-12
Owner lane: G0 synthesis/control only after G12 strategy-field packet audit
Evidence class: `G0_SCID_STRATEGY_FIELD_SOURCE_EXPANSION_PACKET_SYNTHESIS_CONTROL_ONLY`
Input route: `research/science_program_2026_05/06_outcome_testing/g12_scid_strategy_field_source_expansion_packet_audit/`
Promotion posture: `NO_PROMOTION_VERDICT`
Required flags: `validation_safe=false`, `outcome_review_opened=false`, `live_effect=false`

## Objective

Synthesize the accepted G12 audit of the SCID strategy-field source expansion packet and choose the next constructive control route. Decide what fastest auditable path moves the program toward direction-aware edge testing while staying inside the hard forbidden-surface boundaries. Candidate paths include, but are not limited to, result-design readiness, forward capture implementation, broader source recovery, LTF/orderflow/proxy expansion, or a combined route. This is a G0 control decision only. Do not execute validation, score results, claim strategy edge, compute R/PnL/win-rate/expectancy/performance, open broker account/order/history/deal/position evidence, call AI/API, use paid/vendor access, commit raw market-data blobs, or change live behavior or trading-surface files.

This G0 must be ambitious, curious, and analytical, not timid. The accepted absence of historical strategy-intent fields is not permission to stop with a conservative "no edge can be tested" conclusion. It is a routing problem. The route must actively choose the fastest auditable path toward future edge testing: broaden source search if exact recoverable source-state may exist, build forward/source capture if historical truth is non-generatable, open LTF/orderflow/proxy source expansion if it can explain neutral behavior, and design tightly bounded result-readiness prerequisites wherever enough auditable fields are sufficient. Do not prime the session to find reasons to stop. Record limitations only as constraints on how to build the next constructive route, not as the center of the objective.

For this prompt, "source-safe" must not be interpreted as conservative, narrow, or limited to already accepted ideas. It means only this: every route must preserve an audit trail, as-of discipline, no-leak separation, and hard forbidden-surface boundaries. It does not limit creativity, data modalities, mechanism search, route count, instrument/session/timeframe perspectives, or hypothesis families.

## Maximum Curiosity And Anti-Boxing Requirement

This prompt must not box the session into the listed route examples, the existing GTOS edge, current production frameworks, SCID-only thinking, current timeframe/session views, or the first apparent missing-field explanation. The route must ask what else can be extracted, joined, derived, captured, transformed, indexed, segmented, or designed from the accepted artifacts and adjacent local evidence to move toward new edge discovery while keeping an audit trail. Search for as many useful hard-boundary-compliant route families as the evidence class allows, then rank them. Do not limit the route search to one idea just because it is the easiest to audit.

The goal is to maximize constructive research optionality: open every high-value auditable path that can later support direction-aware edge testing, regime/mechanism discovery, failure anatomy, or source capture. If the best answer is a bundle of parallel routes, emit a ranked bundle with separate full prompt files instead of forcing one narrow route. Do not write a limitation-first memo. The completion audit must prove that anti-boxing was applied directly from this prompt, not inferred from chat memory.

## Mandatory Preflight

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/quick_reference_card.md`.
4. Read `.context/00_core/research_operating_doctrine.md`.
5. Read `.context/00_core/goal_session_research_discipline.md`.
6. Read `.context/00_core/research_current_state.md`.
7. Read this prompt from disk.
8. Read the G12 strategy-field packet audit route manifest, decision ledger, completion audit, row coverage audit, field-status audit, fail-closed/prospective/forbidden audit, no-leak/raw-blob/live-surface audit, source hash/input binding audit, saturation ledger, and closeout verification.
9. Read the builder packet manifest, status summary, fail-closed ledger, prospective capture ledger, duplicate denominator ledger, and completion audit.
10. Read the accepted G0/G12 neutral-target evidence chain that led to the builder packet.

Do not rely on chat memory. If interrupted, regenerate `LIVE_STATE` and resume from disk artifacts.

## Mandatory Context Use

Treat `.context/00_core/goal_session_research_discipline.md` and `.context/00_core/research_operating_doctrine.md` as active G0 synthesis instructions. G0 posture here is constructive synthesis and route selection, not scoring and not adversarial acceptance. Separate source-control evidence, future capture requirements, result-design readiness, and validation. The completion audit must record whether both doctrine files were read after preflight, what was synthesized, what constructive paths were considered, what was deliberately not answered, and why the selected next route stays inside safe research/control boundaries.

Also apply the orchestrator artifact-inspection standard from `goal_session_research_discipline.md`: a closeout message is not evidence. This G0 route must inspect the material G12 and builder artifacts before ranking the next route, including completion audits, decision ledgers, verification results, output manifests, source inventory, fail-closed/prospective ledgers, saturation/self-red-team ledgers, and representative closure rows or status distributions from large JSONL artifacts.

Builder/discovery anti-conservatism still applies to route selection: do not frame the result as "blocked, therefore stop" when an exact auditable next route can move the research toward direction-aware edge testing. Do not foreground audit caveats, closure language, or defensive limitation language as the main work of this G0. The output should be a strong next research route, not a passive limitation memo.

## Hard Boundaries

Preserve `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, and `live_effect=false`.

Do not open validation execution, target/result/performance scoring, strategy-edge claims, R/PnL/win-rate/expectancy/performance, broker account/order/history/deal/position evidence, AI/API calls, paid/vendor access, raw `.scid`, `.parquet`, `.csv`, `.dly`, `.bin`, `.depth`, or market-data blob commits, live behavior, or prompt/config/risk/safety/execution/canary/selector changes.

## Required G0 Synthesis Questions

1. Does the accepted G12 packet make a result-design lane ready, or do the seven fail-closed strategy-intent families block direction-aware result design?
2. Should the next route prioritize forward capture implementation for strategy source fields, lower-timeframe availability, and orderflow/depth/proxy context?
3. Is a broader historical source search justified, or has the audit proven that missing historical GTOS intent/source-state is non-generatable from approved artifacts?
4. What constructive route families can turn the neutral source-control candidates and historical strategy-field absence into stronger future evidence, including route families beyond the listed examples?
5. Which future lane owns exact source/capture/logger/parser/schema/redaction/as-of/G12 acceptance requirements?
6. What must remain forbidden until a separate owner-approved evidence class exists?

## Required Constructive Route-Expansion Treatment

Do not default to a conservative route. Treat the accepted G12 packet as a launchpad for the strongest next auditable pursuit. Audit caveats are inputs for route engineering, not internal brakes. This prompt should maximize the chance of finding useful edge-relevant structure later by opening the best constructive lane or lane bundle now.

For the accepted packet, decide which constructive path or parallel path set is strongest. The list below is mandatory to consider, not exhaustive:

- broader historical source search before capture implementation if exact recoverable source-state may exist;
- forward/source capture implementation if missing truth is non-generatable historical GTOS intent/source-state;
- LTF/orderflow/depth/proxy source expansion if market context can explain neutral behavior without inventing strategy intent;
- direction-aware result-design prerequisite if enough auditable fields can be frozen before scoring;
- packet/contract repair only if a material source-control defect blocks all constructive routes.

The route-ranking ledger must explicitly score at least:

- `SCID_STRATEGY_SOURCE_FIELD_FORWARD_CAPTURE_IMPLEMENTATION_CONTRACT`
- `SCID_STRATEGY_FIELD_BROADER_HISTORICAL_SOURCE_SEARCH`
- `SCID_DIRECTION_AWARE_RESULT_DESIGN`
- `SCID_LTF_AND_ORDERFLOW_PROXY_SOURCE_EXPANSION`
- `SCID_COMBINED_SOURCE_SEARCH_AND_FORWARD_CAPTURE_ROUTE`

The route-ranking ledger must also add any newly discovered route family that is auditable, hard-boundary-compliant, and plausibly useful. Do not discard a route because it is not named above; name it, score it, and either select it, include it in a parallel bundle, or explain exactly why another constructive route is stronger.

The selected rank-1 route must explain why it beats the alternatives by speed to edge testing, expected information gain, source recoverability, implementation burden, G12 auditability, future validation readiness, and risk of boxing the research into the existing GTOS framework. If result-design is not ready, say exactly which field families block it and what route closes them fastest. If broader source search is not chosen, prove why an immediate capture/expansion route is higher leverage. If capture implementation is chosen, emit exact field schema, source logger/capture requirements, redaction/no-leak rules, G12 acceptance criteria, and no-live-effect boundaries.

If multiple constructive routes are complementary, emit ordered prompt files rather than collapsing them into one vague future-work item. A closure-only route is not acceptable as the main output while any constructive auditable route remains available.

## Required Outputs

Create a route under:

`research/science_program_2026_05/06_outcome_testing/g0_scid_strategy_field_source_expansion_packet_synthesis/`

Emit at minimum:

- context anchor;
- accepted G12 audit reconciliation;
- source-field readiness synthesis;
- route option ranking ledger;
- anti-boxing and route-discovery ledger;
- capture-only implementation route specification if ranked first;
- constructive learning and route-expansion ledger;
- forbidden-surface/no-leak continuity audit;
- decision ledger;
- output manifest;
- standalone verifier;
- focused tests;
- completion audit;
- closeout verification;
- next controlling prompt for the selected route.

The next controlling prompt must be a full hardened prompt file, not a brief prompt pack. It must include mandatory context use, artifact-inspection requirements, proof-or-impossibility, saturation/self-red-team, exact outputs, verifier/focused tests, scoped commits, and safe flags.

## Required Saturation And Self-Red-Team Pass

Before completion, explicitly answer and pursue same-synthesis-class issues exposed by these questions:

- Are we about to call result-design ready when side/entry/stop/target/POI/setup/lifecycle are all fail-closed?
- Are we underusing the accepted neutral target packet by stopping at "missing strategy fields" without designing the next capture/source route?
- Are we leaving any auditable, hard-boundary-compliant route family unsearched because it was not named in the prompt examples?
- Are we accidentally treating the current GTOS frameworks, current edge, SCID source, M15 as-of bars, or neutral target packet as the whole research horizon?
- Which source-state truth is non-generatable historically, and which source/market data remains recoverable?
- Which future capture fields are mandatory before direction-aware result scoring can be meaningful?
- Which future lower-timeframe/orderflow/proxy fields could explain neutral behavior without becoming strategy intent?
- Which route gets us fastest toward edge testing without fabricating historical intent or crossing evidence-class boundaries?
- What would make the selected next prompt more curious, complete, and useful before a later G12 reviews it?

If any answer exposes an allowed same-synthesis-class gap, pursue it before completion. If the next action crosses into validation, result scoring, broker evidence, AI/API, paid/vendor access, raw data commits, live behavior, or source-trading-surface changes, freeze it as a future route instead.

## Allowed Terminal Decisions

Use exactly one:

- `ACCEPT_AS_G0_STRATEGY_FIELD_PACKET_SYNTHESIS_WITH_RANKED_NEXT_ROUTE`
- `REPAIR_G12_STRATEGY_FIELD_PACKET_AUDIT_REQUIRED`
- `REPAIR_STRATEGY_FIELD_PACKET_REQUIRED`

## Completion Standard

Mark complete only after the G12 audit is reconciled from disk, every required synthesis question is answered, the route-ranking ledger scores every required constructive route option, saturation/self-red-team is complete, the selected next route is emitted as a full controlling prompt, verifier and focused tests pass, scoped commits are created, `.context/00_core/research_current_state.md` is refreshed if materially stale, final `python scripts/generate_live_state.py` is run, and no unrelated runtime/shadow/live dirt is staged.

## One-Line Starter

`/goal Follow the full controlling prompt in research/science_program_2026_05/04_goal_prompts/G0_SCID_STRATEGY_FIELD_SOURCE_EXPANSION_PACKET_SYNTHESIS_CONTROL_PROMPT_2026-05-12.md as the complete objective; do mandatory preflight and context refresh first; do not rely on chat memory; stay G0_SCID_STRATEGY_FIELD_SOURCE_EXPANSION_PACKET_SYNTHESIS_CONTROL_ONLY with no validation/strategy-edge/R/PnL/win-rate/expectancy/performance/promotion/live behavior/AI/API/paid-vendor/broker-account-order-history-deal-position/raw-market-blob/prompt-config-risk-safety-execution-canary-selector changes; inspect the actual G12/builder artifacts, synthesize the accepted 3,014-row strategy-field packet, and find as many constructive, curious, auditable hard-boundary-compliant route families as possible toward direction-aware edge testing beyond the listed examples/current GTOS edge/current frameworks; rank them by speed to edge testing and information gain; emit a ranked route bundle if multiple paths are valuable; do not center caveats or closure if a stronger constructive route exists; emit G0 route, anti-boxing ledger, verifier, focused tests, full hardened next prompt(s), scoped commits, closeout verification, NO_PROMOTION_VERDICT, validation_safe=false, outcome_review_opened=false, live_effect=false; mark complete only when the prompt file's completion standard is fully satisfied.`
