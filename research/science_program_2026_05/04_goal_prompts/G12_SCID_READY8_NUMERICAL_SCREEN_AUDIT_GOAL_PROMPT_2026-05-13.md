# G12 SCID READY8 Numerical Screen Audit

Date: 2026-05-13

## Evidence Class

`G12_SCID_READY8_NUMERICAL_SCREEN_AUDIT_ONLY`

This is an independent G12 audit of the completed G0 READY8 quarantined numerical-screen route. It is strict, disk-backed, and recomputation-first. It must not become conservative theater, invented blocker fishing, or a lazy rejection/acceptance from pasted closeout text.

## Mandatory Starting Context

Run the repo preflight and read from disk, not chat memory:

1. `python scripts/generate_live_state.py`
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/quick_reference_card.md`.
4. Read `.context/00_core/research_operating_doctrine.md`.
5. Read `.context/00_core/goal_session_research_discipline.md`.
6. Read `.context/00_core/research_current_state.md`.
7. Read the accepted target-result G12 audit directory:
   - `research/science_program_2026_05/06_outcome_testing/g12_scid_noapi_ready8_quarantined_target_result_packet_audit/`
8. Read the G0 numerical-screen route directory:
   - `research/science_program_2026_05/06_outcome_testing/g0_scid_noapi_ready8_target_result_synthesis_and_quarantined_numerical_screen/`

## Audit Objective

Audit whether the G0 READY8 numerical-screen route is valid as quarantined numerical-screen control evidence.

The audit must independently verify, from disk:

- exact accepted-substrate reconciliation:
  - `3,014` source candidates,
  - `8` READY8 cards,
  - `24,112` rowset rows,
  - `192,896` target-result rows,
  - horizons `1/4/16/32`,
  - `2` neutral target families;
- all required G0 artifacts exist, parse, and bind to the accepted target-result packet;
- the numerical screen was full-population or verifier-proven lossless-equivalent, not sampling, compact-only summary, top-N-only evidence, skipped partitions, or speed-driven pruning;
- all card movement fingerprints matching `ADV-001` and `ADV-003` is supported by recomputation or a lossless verifier proof;
- partition, duplicate, interaction, candidate-card, negative, recursive discovery, exhaustive coverage, and final self-interrogation ledgers are complete enough for their claimed evidence class;
- `known_same_evidence_class_intelligence_remaining=0` is supported by the coverage artifacts, not merely asserted;
- `same_evidence_class_items_remaining_after_final_loop=0` is supported by the final self-interrogation ledger, not merely asserted;
- card-specific edge/ranking claims are killed or bounded as the G0 route says;
- no validation, strategy performance, R/PnL/win-rate/expectancy, live-readiness, promotion, AI/API, paid/vendor, broker account/order/history/deal/position evidence, raw market blob, remote, registry, or live trading behavior was opened.

## Audit Posture

Be rigorous and fair.

Do not make up limitations just to break the route. A blocker must cite exact file, row, field, hash, parser, count, schema, source/as-of, no-leak, dirty-scope, or verifier evidence. Suspicion alone is not a blocker.

Do not reject broad, non-OB, negative, redundant, or anti-signal findings because they are not familiar GTOS edge claims. The point of this route is quarantined numerical intelligence and failure anatomy, not promotion.

Do not soften real issues. If the artifact does not prove what it claims, record the exact failure and repair path. If the issue can be repaired inside this G12 evidence class, repair it before final decision. Same-G12 repairs include line-ending/hash normalization, stale manifest rebinding, verifier invocation fixes, parse/shape checks, completion-audit metadata fixes, no-leak scans, output-manifest refreshes, and exact route-local verification hardening.

Do not stop with "needs another prompt" for a same-G12 audit issue. Split only if the next action crosses evidence class boundaries, requires unavailable external/source access, opens forbidden live/broker/API/paid/raw surfaces, or requires non-generatable source truth.

## Required Audit Questions

Answer all of these in artifacts:

1. Did the G0 route read and bind the accepted G12 target-result packet from disk?
2. Do the exact counts reconcile: `3,014 / 8 / 24,112 / 192,896 / 4 / 2`?
3. Are all required artifacts present and parseable?
4. Is the candidate example ledger complete, and is its apparent small Git entry a valid LFS pointer/materialization state rather than evidence loss?
5. Were the large ledgers stored safely without normal-Git oversized blobs?
6. Did the G0 route actually compute full-population numerical screens?
7. Are the matrix, partition, duplicate, interaction, negative, recursive, coverage, and self-interrogation ledgers internally consistent?
8. Is the all-card-fingerprints-identical conclusion supported?
9. Are card-specific edge claims correctly killed or bounded?
10. Are horizon/target-family/partition/fail-closed/candidate-anatomy findings preserved without being promoted?
11. Are `known_same_evidence_class_intelligence_remaining=0` and `same_evidence_class_items_remaining_after_final_loop=0` credible within this evidence class?
12. Did the route avoid arbitrary top-N truncation and speed-driven shortcut artifacts?
13. Are safe flags closed everywhere?
14. Are there exact repair requirements? If yes, can they be repaired inside this G12 route before final decision?
15. What terminal decision is justified by disk evidence?

## Required Output Directory

Create:

`research/science_program_2026_05/06_outcome_testing/g12_scid_ready8_numerical_screen_audit/`

## Required Artifacts

Emit versioned artifacts with `2026-05-13` in the file name:

1. `G12_SCID_READY8_NUMERICAL_SCREEN_AUDIT_DECISION_LEDGER_2026-05-13.json`
   - Terminal decision.
   - Accepted G0 route paths and commit.
   - Exact row/count reconciliation.
   - Audit findings.
   - Any same-G12 repairs performed.
   - Safe flags.

2. `G12_SCID_READY8_NUMERICAL_SCREEN_AUDIT_RECOMPUTATION_LEDGER_2026-05-13.json`
   - Independent recomputation or lossless verifier proof for row counts, matrix rows, baseline delta rows, candidate example rows, duplicate contributor rows, partition fields, safe flags, and exhaustion counters.
   - LFS pointer/materialization check for the candidate example ledger.

3. `G12_SCID_READY8_NUMERICAL_SCREEN_AUDIT_REPAIR_LEDGER_2026-05-13.json`
   - Every issue found.
   - Whether repaired inside this route.
   - Exact unrepaired blocker only if not same-G12-repairable.

4. `G12_SCID_READY8_NUMERICAL_SCREEN_AUDIT_COMPLETION_AUDIT_2026-05-13.json`
   - Checklist for every required audit question.
   - `can_mark_goal_complete`.
   - Proof that no invented/vague blockers remain.

5. `G12_SCID_READY8_NUMERICAL_SCREEN_AUDIT_VERIFICATION_RESULT_2026-05-13.json`
   - Result from the standalone audit verifier.

6. Standalone audit builder, verifier, and focused tests.

7. If accepted, emit the next G0 synthesis prompt for the accepted numerical-screen learning.

8. If rejected or repair-blocked, emit exact repair prompt only after same-G12 repair options are exhausted.

## Verification Requirements

Before marking complete:

1. Rerun the G0 route verifier from disk.
2. Rerun the G0 focused tests if feasible.
3. Build the G12 audit artifacts.
4. Run the G12 standalone verifier.
5. Run G12 focused pytest.
6. Syntax-check new G12 Python files. If Windows pycache friction appears, use explicit cfile or AST/no-bytecode fallback and record it precisely.
7. Parse every emitted G12 JSON/JSONL artifact.
8. Confirm no raw market-data blob commits or normal-Git oversized blobs in the G12 route.
9. Confirm safe flags remain:
   - `NO_PROMOTION_VERDICT`
   - `validation_safe=false`
   - `outcome_review_opened=false`
   - `live_effect=false`
10. Regenerate `.context/LIVE_STATE.md`.
11. Update `.context/00_core/research_current_state.md`.
12. Commit scoped audit/prompt/context changes only.

## Completion Standard

The goal may be marked complete only if:

- all audit questions are answered from disk;
- all exact counts reconcile or exact same-G12 repair/blocker evidence is written;
- no invented blockers, vague limitations, or conservative rejection claims remain;
- same-G12 repairs were pursued before terminal decision;
- verifier and focused tests pass;
- no forbidden surface was opened;
- terminal decision is one of:
  - `ACCEPT_AS_G12_SCID_READY8_NUMERICAL_SCREEN_CONTROL_EVIDENCE_ONLY`,
  - `ACCEPT_WITH_EXACT_NONBLOCKING_FOLLOWUPS`,
  - `REPAIR_BLOCKED_WITH_EXACT_G12_NUMERICAL_SCREEN_REPAIR_REQUIREMENTS`,
  - `REJECT_WITH_EXACT_SOURCE_OR_VERIFICATION_FAILURES`.
