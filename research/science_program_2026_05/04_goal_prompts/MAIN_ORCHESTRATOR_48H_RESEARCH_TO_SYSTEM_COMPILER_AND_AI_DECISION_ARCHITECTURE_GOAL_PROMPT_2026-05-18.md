# MAIN_ORCHESTRATOR_48H_RESEARCH_TO_SYSTEM_COMPILER_AND_AI_DECISION_ARCHITECTURE

Date: 2026-05-18
Mode: main-worktree long goal session
Primary objective: `MAIN_ORCHESTRATOR_48H_RESEARCH_TO_SYSTEM_COMPILER_AND_AI_DECISION_ARCHITECTURE`

## Mission

Run on `main` for a full 48-hour wall-clock window. This session replaces the prior 36h launch objective. It is the research-to-system compiler: consume every material GTOS research result, moonshot result, live/shadow friction result, old v2/v3/v4/cascade result, primitive-science hypothesis, market-expansion artifact, and code-surface finding, then convert the evidence into scored replay rows, implementation decisions, architecture decisions, and verified code/config/prompt/tooling changes.

The primary scoring truth is simulated historical replay R, exact replay R where the replay geometry is complete, source-bound proxy R where the geometry is derived from disk evidence, and replayable expectancy. Do not chase live account realized R, owner R, or broker-fill R as the objective or as a blocker. Existing broker/account/order/history/deal/position files may be read only for execution-friction calibration, join diagnostics, or incident context. They must not replace replay/backtest scoring.

The session must not be boxed into READY8, OB retest, the current seven-symbol universe, a single timeframe, a single market family, a single entry style, a single exit style, a single branch family, or a single prior hypothesis. The system is allowed to become mechanical where mechanical evidence is stronger than the AI decision loop. The AI/API layer is not sacred: remove it from branches it slows down or weakens, narrow it to defined ambiguity cases, turn it into explanation-only, harden it to strict schema where it earns value, or keep it only where replay/log/test evidence shows measurable value beyond deterministic scorers.

## North-Star System Compiler Contract

The north star is the strongest GTOS system architecture that can be built from the whole accumulated research base. This session must absorb the sea of intelligence already on disk and turn it into the exact system changes needed: mechanical scorers, selectors, avoid filters, branch routers, market/timeframe expansion, entry/exit/risk behavior, AI/API role changes, replay validators, prompt/schema changes, and observability. The output is not a synthesis of what might work. The output is a planned, verified conversion of evidence into system behavior.

Moonshot is the current execution/scoring substrate because it has replay geometry, simulated/proxy R rows, scorer surfaces, avoid surfaces, path ordering, held replay rows, and broad expansion artifacts. Prior research is the intelligence library: old winners, losers, blockers, research-paper ideas, v2/v3/v4/cascade outcomes, live friction, primitive sciences, and failure intelligence must explain, improve, filter, invert, merge, or reject moonshot/system candidates. The session must compile both layers together.

Do not wander through the research ocean. Keep a single operating line at every checkpoint:

1. identify the active evidence universe and row counts from disk;
2. map each evidence family to a system surface or to an exact replay/implementation task;
3. implement, replay, expand, or remove/narrow the affected system behavior;
4. verify with tests, parsers, replay checks, or ledgers;
5. commit scoped results and immediately continue to the next highest-value system conversion plate.

If a task does not move toward a stronger executable GTOS architecture, a stronger replay-backed mechanical system, a broader market/timeframe result base, or a clearer AI/API decision architecture, stop that task and move to the nearest task that does. The goal is the system that can actually trade better, not a catalog of intelligence.

This prompt is explicit owner approval for repository code, config, prompt, risk, execution-adjacent, safety/canary/selector, AI prompt/schema/parser, replay, logging, tooling, validator, test, and system-design changes when they are tied to disk evidence, replay/proxy-R results, test failures, production friction, or architecture conversion. Do not wait for separate approval to edit those repo surfaces inside this goal. Use tests, verifiers, before/after ledgers, and rollback clarity to keep changes accountable.

Treat this prompt, the starter, doctrine, current-state docs, latest handoff, active moonshot artifacts, and current repo artifacts as active instructions, not background. Operationalize them and record instruction-coverage evidence.

No conservative brake. No arbitrary top-N, top 3/5/10, representative-only sample, or number-limited cutoff. Preserve all material rows in full ledgers before summarizing. Proof-or-impossibility and same-evidence-class pursuit apply, but proof rows are not the product; they are only acceptable where computation, implementation, replay, extraction, or a weaker source-bound proxy is genuinely exhausted after searching the relevant disk/source class.

Retired defensive wrapper language from older artifacts has no authority in this session and must not be emitted in new active builders, summaries, starters, result ledgers, verifiers, or reasoning. Do not rewrite old historical artifacts only for wording cleanup. Patch active behavior and new outputs when old language can steer current work away from computation, implementation, replay, or market expansion.

## Mandatory Disk Preflight And Reread Discipline

1. Run `py -3 scripts\generate_live_state.py` and read `.context/LIVE_STATE.md`.
2. Read `AGENTS.md`, `CLAUDE.md`, the latest file in `.context/02_session_handoffs/`, `.context/00_core/quick_reference_card.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/orchestrator_successor_operating_brief.md`, `.context/00_core/orchestrator_methodology_hardening_controls.md`, `.context/00_core/parallel_goal_merge_playbook.md`, `.context/00_core/research_current_state.md`, `.context/00_core/local_heavy_data_inventory.md`, `.context/00_core/ai_in_loop_cost_control_research_plan.md`, the route registry, the cross-route question/ambiguity ledger, this prompt, and the starter.
3. Snapshot `` if it exists: HEAD, branch, status, consumed paths, sizes, SHA256 hashes, committed versus working-tree-only state, and latest checkpoint/result files. Consume CP215 and every later checkpoint present at launch, including uncommitted but readable working-tree artifacts with commit-status labels. At minimum, if present, consume CP216 expanded-market proxy-R performance, CP217 side-pair robustness, CP218 temporal robustness, and all market-expansion artifacts. Do not modify that worktree from this main session.
4. Read main 24h/36h artifacts, READY8 final-decision artifacts, old research outputs, live/shadow/forward logs, strategy/no-fill/pending lifecycle logs, malformed AI output logs, existing simulations, and current production code before making claims.
5. Do not rely on chat memory or pasted closeouts. After compaction, resume, interruption, uncertainty, long gap, checkpoint transition, or plate transition, regenerate `LIVE_STATE` and reread this prompt, the starter, doctrine, latest handoff, current-state doc, latest moonshot snapshot paths, and latest artifacts before continuing.
6. Every checkpoint must update a scoped active-run ledger that preserves: what was reread, what artifact set was consumed, what row universe is active, what code surfaces changed, what tests/verifiers ran, what exact next plate is active, and why the next plate follows from disk evidence.

## Evidence Universe

Build and keep current a complete evidence inventory. Include all material artifacts and code surfaces, not only the files easiest to parse:

- moonshot CP203+ through latest checkpoint, including CP213 performance matrix, CP214/CP215 surfaces and executions, CP216 expanded-market proxy-R performance, CP217 side-pair robustness, CP218 temporal robustness if present, repaired-proxy execution registry, held replay rows, scorer/avoid candidates, terminal kill rows, and all broad market/timeframe expansion artifacts;
- explicit moonshot market expansion seeds: the 301-row/44-symbol market-expansion population, the 1,145-row/47-symbol-label expansion matrices, and any later expanded-market checkpoint produced after those files;
- main 24h/36h result materialization ledgers, scorer patches, source-capture patches, implementation decisions, live/shadow gap closure, tests, and code changes;
- READY8 final decision and its neutral/failure/HAZ001/MAC/UNC004/HAZ005 intelligence as tags, priors, avoid/control labels, or context features only;
- V2/V2B/V3/V4/cascade and prompt-cascade research, including why V3 was empirically stronger and why V4/LIRA were shelved;
- J46-J49 portfolio policy, S79 risk policy, K54/K55 ML/filter work, FVG/OB, breaker, liquidity sweep, no-fill, pending lifecycle, orderflow, Sierra, MT5, spread/cost, source-repair, exit, side-aware risk, session/regime/time/calendar, and expansion blockers;
- live/shadow/forward logs as production-friction and behavior evidence, not as a reason to wait for sparse forward samples;
- code and config in `src/`, `prompts/`, `config/`, `scripts/`, `tests/`, `shadow_logs/`, `knowledge_base/`, and research tooling.

## Master Output Contract

Produce a master evidence-to-action matrix with one row for every material research family, result ledger, branch family, implementation candidate, gate/filter, AI decision component, market/timeframe expansion candidate, primitive-science hypothesis, and production friction class. Each row must include:

- source artifact path(s), source hashes, row counts, and relevant IDs;
- simulated/replay exact R or proxy R where computable, gross/cost/stress expectancy, win/loss/zero counts, average win/loss R, duplicate/effective-N, concentration, pass/control delta, symbol/session/timeframe/horizon splits;
- current production touchpoint and files affected;
- action: implement, modify, remove, merge, redesign, replay, expand, convert to avoid-filter, convert to mechanical scorer, narrow AI, remove AI from branch, keep as feature, or kill unsupported claim while preserving useful intelligence;
- exact code/config/prompt/tooling/test changes made or row-level reason none can be made;
- before/after expected behavior and verification command.

The session must emit machine-readable ledgers, concise synthesis files, a completion audit, and an instruction-coverage ledger. Summary prose alone is not progress.

The matrix is a compiler table, not a backlog. Every row must end as code changed, config/prompt/schema changed, replay/result materialized, market/timeframe expanded, branch routed, avoid/filter/scorer decision implemented, AI role changed, or exact source/access proof with the next executable owner inside the same 48h plan.

## Implementation Plates

The plates below are all in scope. Do not treat them as a top-N list. Work through them as evidence and time allow; after one plate is checkpointed, immediately continue to the next highest-value computation, implementation, replay, expansion, or system-upgrade plate.

### Plate A - Moonshot To GTOS System Compiler

Consume the latest moonshot artifacts into current GTOS integration code. Start with CP213/CP214/CP215: 1,057 replay matrix rows, 638 retained recommendation rows, 419 terminal kill rows, 794 target-first rows, 263 stop-first rows, 532 scorer surfaces, 106 avoid surfaces, and 638 passing surface executions. Then consume CP216/CP217 and later if present: 45,220 expanded-market simulated-R rows, 22,610 side-pair rows, 7,669 side-filter implementation rows, 126 avoid-intelligence rows, 14,815 redesign rows, and any temporal robustness rows. Turn them into runnable branch-local scorers, avoid filters, comparator outputs, shadow-production decision modules, replay validators, or config/prompt changes with tests.

Do not duplicate active moonshot builders. Consume their fixed snapshot and convert it into GTOS system behavior. Preserve and use the broad expansion population and matrices; the current seven-symbol universe is only the starting seed.

### Plate B - Market, Timeframe, And Primitive-Science Expansion

The current instrument list is not the research boundary. Consume the 301-row/44-symbol and 1,145-row/47-symbol-label moonshot expansion artifacts, then expand further where local, MT5, Sierra, historical, and free/public data sources allow. Do not stop because a file is not already in the repo if MT5/Sierra/local tooling can extract it.

Cover primitive sciences broadly: structure, liquidity, volatility, trend/momentum, mean reversion, session/calendar, spread/cost/fill, path ordering, intermarket context, regime, execution mechanics, exits, risk, position management, and failure/avoid behavior. Every expansion must produce numeric rows, implementation rows, replay rows, code, tests, or exact source/access proof.

### Plate C - AI/API Decision Architecture

Audit the AI/API role using prompt outputs, malformed logs, refusal logs, schema/parser behavior, canary behavior, cost/latency, deterministic precision bugs, live candidate flow, replay comparisons, and mechanical scorer coverage. Decide and implement the strongest architecture:

- deterministic scorer replaces AI for branches with complete mechanical rules and replay evidence;
- AI becomes explanation-only where deterministic decisioning is stronger;
- AI is called only for explicitly defined ambiguity classes where mechanical rules do not cover the case;
- AI prompt/schema/parser is hardened where the API remains a decision participant;
- API is removed from a branch if it adds latency, malformed output, or decision noise without measurable replay/log benefit;
- current AI path remains only where evidence shows value beyond deterministic alternatives.

Do not spend new paid API calls. Use existing artifacts, tests, local replay, prompt fixtures, malformed logs, canary fixtures, and code inspection. Produce code/config/prompt/parser/test changes from replay/log/test evidence; do not leave AI/API architecture as prose when a repo change can encode the decision.

### Plate D - Gate, Filter, And Selector Architecture

Inventory every live and shadow gate/filter/selector: touch count, L2 framework checks, POI availability, min RR, confidence handling, SL buffer/tolerance, correlation, kill-zone/session windows, SPRT/CUSUM, risk sizing, heartbeat behavior, framework dispatch, canary/test fixtures, malformed response handling, no-fill handling, pending lifecycle, symbol risk asymmetry, and selector logic. For each, quantify what it blocks, what it saves, what it costs in missed replay R, and whether it should be kept, changed, narrowed, inverted into avoid intelligence, defaulted differently, removed, or converted into a mechanical scorer/selector.

### Plate E - Legacy Research Merger

Map every prior research winner, loser, blocker, and expansion path into the current system architecture. Include V2/V2B/V3/V4/cascade, J46-J49, S79, K54/K55, FVG/OB, breaker, liquidity, no-fill, pending lifecycle, orderflow/Sierra, market expansion, exits, risk, session/regime/time, side-aware, and primitive-science hypotheses. For each, implement now, replay now, merge as feature/avoid/control, redesign, expand, or kill unsupported claim. Do not lose useful failure intelligence.

### Plate F - Production Upgrade Implementation

Change the system. Research that only sits in a ledger has not reached the goal. Implement scoped production or shadow-production changes tied to replay/log/test/code evidence: scorers, filters, selectors, prompt/schema, config, risk logic, execution-adjacent behavior, observability, replay validators, data extractors, test/canary coverage, or AI-removal/AI-narrowing paths. Each code change must have a test or verifier and a clear before/after description.

## Operational Side Effects

Production-change authority covers live trading repo code paths. Repository code, prompts, config, risk logic, execution-adjacent code, safety/canary/test infrastructure, selector logic, tooling, logging, replay builders, validators, AI prompt/schema/parser code, and system-design docs are authorized inside this goal when tied to disk evidence, replay/proxy-R, test failures, production friction, or architecture conversion. Actual broker operations that place, close, or modify trades; paid API/vendor spend; credentials; and remote push are not part of this session unless the owner gives a separate explicit operational command. Reading broker/account/order/history/deal/position files already on disk is allowed only as context. This boundary must not reduce computation, implementation, replay, market expansion, AI architecture work, or code changes inside the repo.

## Completion Standard

The session must run the full 48-hour window. No early finish after a checkpoint, verifier, context refresh, first result table, first implementation, first AI decision, or first market-expansion pass. When a plate is complete, immediately continue to the next highest-value plate from disk evidence.

Every checkpoint must include scoped artifacts, JSON/JSONL parse checks for new ledgers, focused tests or verifiers for changed code, scoped commits on main, and context refresh. The final closeout must include:

- total simulated/replay exact R rows, proxy R rows, and non-computable rows;
- gross, cost-adjusted, and stress-adjusted expectancy by branch/family/symbol/session/timeframe/horizon;
- implemented changes with file paths, tests, and before/after behavior;
- gates/filters/selectors kept, changed, removed, inverted, or converted and the evidence for each;
- AI/API decision-loop architecture after the session, including exactly where AI was removed, narrowed, retained, or hardened and why;
- old research merged, redesigned, expanded, or killed;
- market/timeframe expansion performed, rows created, symbols/timeframes covered, and remaining source/access proof;
- remaining highest-value work that could not be completed inside 48 hours, with exact source and implementation paths.

Forbidden final products: summary-only output, blocker-only output, prompt-only output, audit-only output, capture-only output, waiting for live data, chasing account-owner R, ending because one branch family was handled, or ending before every current plate has either produced computation/implementation or exact row-level exhaustion proof. The goal is tangible system improvement from all accumulated evidence.

## Launch

```powershell
cd C:\Users\MSI\Documents\ai-trading-agent
codex --enable goals -c model_reasoning_effort=xhigh
```

Paste the starter from:

`research/science_program_2026_05/04_goal_prompts/MAIN_ORCHESTRATOR_48H_RESEARCH_TO_SYSTEM_COMPILER_AND_AI_DECISION_ARCHITECTURE_STARTER_2026-05-18.txt`
