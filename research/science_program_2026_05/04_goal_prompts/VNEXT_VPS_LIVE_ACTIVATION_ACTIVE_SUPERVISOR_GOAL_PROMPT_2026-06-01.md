# vNext VPS Live Activation Active Supervisor Goal Prompt

Date: 2026-06-01

Route id: `vnext_vps_live_activation_active_supervisor_2026_06_01`

Evidence class: `VPS_LIVE_RUNTIME_DATA_BROKER_PROCESS_REPAIR_SUPERVISION`

Objective: verify the VPS is running the current vNext/moonshot production system correctly, prove every production data dependency and generated data stream is present and usable, prove MT5 is connected and producing the required broker/tick/bar evidence for all 24 symbols, prove the latest production fixes are present in code/config/process behavior, repair every verified defect immediately, restart or reload affected components after repair, record before/after proof, and remain active as the live supervisor.

This is the current live supervisor for the VPS. It is not the older May 28 monitor, not a watch-only lane, not a prose audit, not a stale checkpoint refresher, and not a narrow health check.

## Current Production Contract

Rebuild the current state from disk before taking action. Current disk evidence overrides chat memory, older handoffs, old README sections, old 5-symbol/7-symbol snippets, old fixed-target route notes, old PrimaryAnalyzer/L2 material, old static `1.5R` production language, and stale monitor prompts.

Current vNext truth to prove from code/config/artifacts/logs/processes:

- Active system: 24-symbol vNext/moonshot production replacement.
- Live symbols: `AUDJPY`, `AUDUSD`, `BTCUSD`, `CHFJPY`, `ETHUSD`, `EURGBP`, `EURJPY`, `EURUSD`, `GBPJPY`, `GBPUSD`, `GER40`, `JP225`, `NAS100`, `NZDUSD`, `SPX500`, `UK100`, `UKOIL_cash`, `US30_cash`, `USDCAD`, `USDCHF`, `USDJPY`, `USOIL_cash`, `XAGUSD`, `XAUUSD`.
- Current execution policy contract: `momentum_exhaustion` primary with `partial_be_runner` exception selection.
- Fixed `1.5R`, J46/J49, BE-only, and old `PrimaryAnalyzer -> L2` are historical, diagnostic, comparator, or fail-closed references only unless current-head code explicitly proves a current vNext role.
- Gate stack: repaired vNext live/replay gates, dynamic target awareness, Gate1/Gate3 parity, selected-cell/risk packet completeness, current execution policy id propagation, same-symbol conflict handling, ticket-bound partial/residual management, broker-close reconciliation through MT5 deal proof, M1 capture state fields, tick/M1 capture freshness classification, and old-system absence proof.
- Production behavior must be proven from the running VPS process set, not only from local files.

## Mandatory Context Use

Before work:

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/current_vnext_system_map.md`.
4. Read `.context/00_core/current_repo_reading_order.md`.
5. Read `.context/00_core/quick_reference_card.md`.
6. Read `.context/00_core/goal_session_research_discipline.md`.
7. Read `.context/00_core/research_operating_doctrine.md`.
8. Read `.context/00_core/orchestrator_successor_operating_brief.md`.
9. Read `.context/00_core/orchestrator_methodology_hardening_controls.md`.
10. Read `.context/00_core/parallel_goal_merge_playbook.md`.
11. Read current live companion, lane, route, and verifier artifacts that the regenerated `LIVE_STATE` and reading-order files point to.
12. Read this controlling prompt and its starter message from disk.

After any context compaction, resume, interruption, tool crash, long wait, restart, or uncertainty, repeat the context read from disk and continue from the route state files. Do not rely on chat memory.

Final audit must record instruction coverage:

- preflight files read;
- prompt/starter reread after every resume;
- evidence class enforced;
- live-runtime boundary enforced;
- current vNext contract verified;
- defect classes pursued;
- repairs applied;
- restarts/reloads performed;
- tests/verifiers run;
- remaining blockers with exact owner/source/action.

## Hardening Controls

These are active instructions, not background. Operationalize them in route ledgers, verifier output, repair records, restart/reload records, checkpoint state, and the final completion audit.

No conservative brake applies inside this evidence class. Use constructive, curiosity-driven, active creativity to verify and repair the live VPS system across process, data, broker, runtime, packet, lifecycle, risk, notification, and supervisor surfaces. Do not convert a repairable same-evidence-class defect into a report-only item.

Full same-evidence-class pursuit is mandatory. Literal impossibility means exactly this: every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action that can resolve the defect from available local, VPS, MT5, broker, process, log, route, and code evidence has been performed and recorded.

Result materialization standard: every material result must state result-use-status, exact-R/proxy-R/expectancy status where relevant, source-capture/source completeness status, branch decision, implementation decision, runtime effect, restart/reload effect, and broker/lifecycle effect. Defect ledgers, blocker ledgers, and checkpoint reports are intermediate artifacts unless they drive repair, verification, restart/reload, or exact impossibility proof.

No arbitrary top-N, top 3/5/10, number-limited cutoff, representative-only pass, or example-only pass is allowed for incidents, symbols, dependencies, data streams, nulls, stale fields, anomalies, repairs, blockers, source roots, process classes, candidate classes, or lifecycle states. Ranked summaries are allowed only after the full ledger preserves all material rows/events.

Production-change and live trading boundaries must be explicit without weakening the repair objective. Broker operation, paid API/vendor usage, broker account/order/history/deal/position mutation, prompt/config/risk/execution/safety/canary/selector changes outside the current vNext production contract, credential changes, and remote push require separate exact CEO approval.

## Hard Boundaries

Allowed inside this goal:

- read-only MT5 account, symbol, tick, bar, order, position, deal, history, and broker-spec inspection;
- local file repair;
- code repair;
- config repair that restores the current vNext production contract;
- launcher/watchdog/scheduler/process repair;
- data-capture repair;
- packet/logging/verifier/test repair;
- scoped commits;
- local process restart/reload needed to apply a verified repair;
- harmless notification queue/Telegram verification that does not expose secrets and does not mutate broker state.

Forbidden without separate exact CEO approval:

- manual broker order placement;
- manual broker order modification;
- manual broker order cancellation;
- manual position close;
- credential print/change;
- paid/vendor/API spend;
- remote push;
- new strategy/risk/selector/execution behavior outside the current vNext production contract.

The already-running production system can manage trades through its current approved code path. This goal verifies and repairs that code path. It does not invent discretionary broker actions.

## Required Route Directory

Create and maintain:

`research/operations/vnext_vps_live_activation_active_supervisor_2026_06_01/`

Required route files:

- `VPS_SUPERVISOR_STATE.json`
- `VPS_SUPERVISOR_ACTION_LEDGER.jsonl`
- `VPS_GIT_VERSION_AUDIT.json`
- `VPS_DATA_DEPENDENCY_LEDGER.jsonl`
- `VPS_REQUIRED_FILE_LEDGER.jsonl`
- `VPS_ENVIRONMENT_LEDGER.jsonl`
- `VPS_MT5_ACCOUNT_READINESS.json`
- `VPS_MT5_SYMBOL_SOURCE_LEDGER.jsonl`
- `VPS_BROKER_SPEC_LEDGER.jsonl`
- `VPS_PROCESS_HEALTH_LEDGER.jsonl`
- `VPS_WATCHDOG_SCHEDULER_LEDGER.jsonl`
- `VPS_DATA_CAPTURE_HEALTH_LEDGER.jsonl`
- `VPS_RUNTIME_DECISION_LEDGER.jsonl`
- `VPS_CANDIDATE_PACKET_LEDGER.jsonl`
- `VPS_OPEN_TRADE_LIFECYCLE_LEDGER.jsonl`
- `VPS_BROKER_RECONCILIATION_LEDGER.jsonl`
- `VPS_RISK_EXPOSURE_LEDGER.jsonl`
- `VPS_NOTIFICATION_LEDGER.jsonl`
- `VPS_ANOMALY_LEDGER.jsonl`
- `VPS_REPAIR_LEDGER.jsonl`
- `VPS_RESTART_RELOAD_LEDGER.jsonl`
- `VPS_VERIFICATION_RESULT.json`
- `VPS_OUTPUT_MANIFEST.json`
- `VPS_COMPLETION_OR_CONTINUATION_AUDIT.json`
- route-owned verifier script and focused tests where the route exposes code-level defects.

Artifacts are not ceremony. Every artifact must feed verification, repair, restart, live supervision, or future recovery.

## Stage 00 - Reanchor VPS Runtime

Prove the VPS code and runtime surface:

- repository path;
- current `HEAD`;
- `origin/main`;
- local dirty state;
- LFS status and broken pointer check;
- Python executable/version;
- installed packages;
- MT5 Python package import;
- operating user;
- timezone;
- disk free space;
- CPU/RAM pressure;
- process permissions;
- `.env` presence without secret values;
- `GTOS_PROFILE`;
- `GTOS_MODE`;
- `RESEARCH_RUNTIME_HALT.flag`;
- `AUTOSTART_DISABLED.flag`;
- Task Scheduler entries;
- launcher paths in `start_all.bat`;
- watchdog paths in `scripts/watchdog.ps1`, `scripts/watchdog.bat`, and `scripts/watchdog_launcher.vbs`;
- hardcoded path drift from laptop paths to VPS paths;
- every running GTOS process command line.

If the VPS is behind the current approved remote production code, update the VPS repo to the current approved remote state, rerun dependency checks, and restart/reload affected processes. Do not push.

## Stage 01 - Required Production File And Data Dependency Closure

Build the production dependency map from code/config, not guesses.

Scan these surfaces:

- `config/agent_config.yaml`
- `config/profiles/*.yaml`
- `start_all.bat`
- `run_agent.py`
- `scripts/watchdog.ps1`
- `scripts/watchdog.bat`
- `scripts/watchdog_launcher.vbs`
- `src/components/orchestrator.py`
- `src/components/execution.py`
- `src/components/gtos_vnext_runtime.py`
- `src/components/m1_capture.py`
- `src/components/tick_capture.py`
- `src/research/moonshot_default_off_policy_router.py`
- notification queue code;
- risk/permission/governor code;
- active selector package references;
- active route/verifier references used by live runtime;
- calendar/news files;
- broker alias/spec files;
- persistent state files;
- runtime output directories;
- data directories required by current runtime.

For every required dependency, record:

- path;
- role;
- owning code/config reference;
- exists status;
- file type;
- size;
- hash when useful;
- mtime;
- LFS pointer status;
- parse/load status;
- stale/cold/current classification;
- runtime impact;
- repair action.

If a required live dependency is missing, broken, stale, malformed, or a raw LFS pointer, repair it before continuing. If it is cold research evidence and not required for live execution, classify it as cold and exclude it from live readiness.

## Stage 02 - MT5 Account, Symbol, Broker Spec, And Market Data Proof

Use MT5 read-only evidence.

Prove:

- terminal initialized;
- intended account is connected;
- account login/server/company;
- account balance/equity/margin/free margin;
- trade mode and terminal trade permission;
- no unintended account mismatch;
- open positions;
- pending orders;
- recent deals/history availability;
- current server time;
- local/VPS time alignment;
- all 24 symbols resolved to broker-native names;
- all 24 symbols selected in Market Watch;
- latest tick per symbol;
- M1/M15/H1/H4/D1 bars per symbol;
- sufficient lookback for live runtime;
- symbol info fields: digits, point, trade tick size, trade tick value, contract size, volume min/max/step, stops level, freeze level, spread, swap fields, margin fields, session/trade mode fields when available.

Required alias verification:

- `AUDJPY -> AUDJPY`
- `AUDUSD -> AUDUSD`
- `BTCUSD -> BTCUSD`
- `CHFJPY -> CHFJPY`
- `ETHUSD -> ETHUSD`
- `EURGBP -> EURGBP`
- `EURJPY -> EURJPY`
- `EURUSD -> EURUSD`
- `GBPJPY -> GBPJPY`
- `GBPUSD -> GBPUSD`
- `GER40 -> GER30`
- `JP225 -> JP225`
- `NAS100 -> NDX100`
- `NZDUSD -> NZDUSD`
- `SPX500 -> SPX500`
- `UK100 -> UK100`
- `UKOIL_cash -> UKOUSD`
- `US30_cash -> US30`
- `USDCAD -> USDCAD`
- `USDCHF -> USDCHF`
- `USDJPY -> USDJPY`
- `USOIL_cash -> USOUSD`
- `XAGUSD -> XAGUSD`
- `XAUUSD -> XAUUSD`

Repair missing selection, alias drift, unavailable symbols, bad broker specs, path mistakes, and stale market-data initialization immediately. Reverify after repair.

## Stage 03 - Process, Watchdog, Scheduler, And Always-On Proof

Prove the runtime process set from PIDs, command lines, locks, heartbeats, logs, and output writes:

- 24 orchestrators;
- 24 tick-capture workers;
- M1 capture daemon;
- displacement logger;
- heartbeat monitor;
- notification queue worker;
- watchdog;
- scheduled task for launch;
- scheduled task or always-on mechanism for watchdog.

For each process, record:

- process name;
- PID;
- command line;
- repo path;
- profile;
- mode;
- symbol when applicable;
- start time;
- heartbeat path;
- latest heartbeat timestamp;
- latest log path;
- latest log timestamp;
- last action;
- last error;
- restart requirement.

If a process is missing, stale, running from the wrong repo, running the wrong profile/mode, writing to the wrong path, or alive without data movement when data is expected to move, repair and restart it.

Weekend/market-closed/broker-idle classifications must be explicit. A daemon-alive/no-new-broker-tick state is not the same as dead capture. A feed-open/no-data state is a defect.

## Stage 04 - Tick, M1, M15, Forward Data, And Intelligence Capture

Prove continuous forward data generation:

- tick capture per symbol;
- tick state age per symbol;
- tick file/date path per symbol;
- M1 capture per symbol;
- M1 capture state fields: row count, newest closed bar, bar age, no-new-row reason, read errors;
- M15 candle cadence;
- D1/H4/H1/M15 base state ingestion;
- lower-timeframe/pending-limit monitor data;
- closed-bar versus live tick semantics;
- zero-row classifications;
- malformed/corrupt data files;
- retention/storage behavior.

The system must not be blind. Every candidate and every no-candidate period must have enough data evidence to explain what the system saw.

Repair:

- missing tick paths;
- missing M1 directories;
- stale M1 state;
- stale tick state;
- zero-row-without-reason;
- corrupt parquet/CSV/json;
- symbol alias path drift;
- date rollover failure;
- capture daemon crash;
- wrong data root on VPS;
- logging that hides null fields.

## Stage 05 - vNext Runtime Decision Chain Proof

Prove every live decision row and packet carries current vNext truth:

- candidate identity;
- broker symbol;
- symbol alias;
- origin family;
- framework;
- side;
- session;
- route family;
- source mode;
- HTF/M15/M1/tick snapshots;
- broker snapshot;
- raw geometry;
- repaired geometry;
- Gate0;
- Gate1;
- Gate3;
- dynamic policy;
- `execution_policy_id`;
- selected-cell proof;
- selected-cell source identity;
- risk/lot proof;
- prop checks;
- portfolio exposure;
- same-symbol/correlation decision;
- final order readiness;
- refusal/repair reasons;
- old-system absence proof;
- null/zero reason where any field is empty or zero.

Every current live candidate, skip, rejection, no-trade, placement, fill, partial, modify, close, and notification must be reconstructable.

Repair:

- missing packet fields;
- generic refusal labels;
- `framework_not_activated` drift when current selector has exact proof;
- `selected_cell_risk_not_verified_or_zero` drift when source identity can be recovered;
- stale Gate1 terminal behavior;
- stale Gate3 spread behavior;
- stale fixed-target labels;
- policy id loss;
- old PA/L2 fallback leakage;
- candidate/no-candidate silence.

## Stage 06 - Open Trades, Orders, Deals, And Lifecycle Supervision

Continuously inspect open positions, pending orders, recent deals, local lifecycle files, Telegram messages, and runtime decisions.

For every open or pending vNext trade, prove:

- ticket;
- symbol;
- broker symbol;
- side;
- magic/comment;
- volume;
- entry price;
- SL;
- TP;
- selected execution policy;
- selected-cell risk;
- current worst-case risk dollars;
- current risk percent;
- current floating PnL;
- partial-close state;
- BE state;
- residual ticket identity;
- lifecycle state;
- local record state;
- broker record state;
- latest management action;
- next expected management action;
- modify attempts and retcodes;
- stop/freeze/spread constraints;
- Telegram parity.

Every lifecycle action must be ticket-bound. After partial close, residual management must bind by ticket/position identity, symbol, side, magic, volume, and broker response. No `our_positions[0]` authority.

Broker-close truth requires MT5 close-deal proof. Do not retire a local position as closed merely because it disappeared from a short position poll.

Repair:

- false local close;
- false Telegram close;
- missing MT5 deal join;
- wrong residual ticket;
- BE/TP modify diagnostics missing;
- failed modify retries without actionable request/retcode/comment;
- unmanaged residual exposure;
- same-symbol accidental hedge/stack;
- local PnL projection overriding broker truth.

## Stage 07 - Risk, Prop, Portfolio, And Exposure Authority

Prove risk from real money and account exposure:

- balance;
- equity;
- day-start baseline;
- realized broker PnL;
- open worst-case SL risk;
- pending worst-case SL risk;
- new-trade risk;
- selected-cell risk percent;
- lot size;
- contract geometry;
- tick value;
- spread buffer;
- slippage buffer;
- commission/swap cost;
- internal daily overlay;
- external daily limit;
- total account limit;
- same-symbol exposure;
- correlated cluster exposure;
- risk release after partial/BE.

Count/session caps cannot silently block valid vNext trades when account-risk exposure remains inside the current configured limit. Any count cap that remains active must be proven as a current vNext rule from code/config and recorded with exact reason.

Repair stale prop deferrals, stale base-risk fallback, stale R-count blocking, stale equity source, stale session caps, stale day-start baseline, and missing risk-dollar evidence.

## Stage 08 - Notifications, Operator Truth, And Command Surface

Verify Telegram/notification system:

- queue worker alive;
- queue depth;
- queue drain;
- token/chat presence without printing values;
- message formatting;
- current vNext policy labels;
- no stale fixed/J46/J49/BE-only production wording;
- candidate/rejection/order/fill/partial/BE/close/reconciliation lifecycle parity;
- failures include exact request/retcode/source.

Run only non-broker-mutating notification tests. Record synthetic versus real lifecycle status separately.

Repair notification queue dead zones, stale labels, missing lifecycle fields, false close notifications, failed send retries without diagnostics, and mismatched broker truth.

## Stage 09 - AI Supervisor Linkage

This goal must start the active AI supervisor as a repair companion for the running system.

The supervisor role:

- read live runtime evidence;
- detect stale behavior;
- detect nulls and zero-without-reason;
- detect missing fields;
- detect dead/stale processes;
- detect broker/data divergence;
- detect candidate packet defects;
- detect old-system leakage;
- detect malformed notification/lifecycle truth;
- verify defects against source evidence;
- repair code/config/process/data-capture/logging defects;
- restart/reload affected components;
- rerun focused tests and verifiers;
- record checkpoint proof.

The supervisor must not become an unbounded chat loop. It must operate from route files, delta checks, exact anomaly classes, and restartable state.

AI API usage:

- Verify offline payload construction before any paid call.
- Paid/vendor/API calls require separate exact CEO approval.
- If API calls are approved, log prompt schema, response schema, null fields, cost, latency, fallback behavior, malformed response handling, and whether the response changed any repair decision.
- The mechanical vNext production path remains authority unless current code proves an approved AI decision role.

## Stage 10 - Supervisor Cycle Contract

After initial verification, keep cycling:

1. Refresh current state.
2. Check processes.
3. Check MT5 connection and account.
4. Check 24-symbol tick/M1/M15 freshness.
5. Check candidates and no-candidate proof.
6. Check open trades/orders/deals.
7. Check risk/exposure.
8. Check packet completeness.
9. Check old-system leakage.
10. Check notification queue.
11. Check verifier status.
12. Detect anomalies.
13. Repair verified defects.
14. Restart/reload affected components.
15. Reverify.
16. Write checkpoint.

Do not rebuild giant research ledgers on every cycle. Use delta checks for live supervision. Run full verifiers when code/config/data contracts changed or when an anomaly requires full recomputation.

Checkpoint files must show:

- timestamp;
- cycle id;
- live account state;
- process state;
- data freshness state;
- candidate state;
- open-trade state;
- risk state;
- notification state;
- anomalies found;
- repairs applied;
- tests/verifiers run;
- restarts/reloads;
- remaining exact blockers;
- next cycle focus.

## Subagent Use

Use subagents for independent high-reasoning audits when the surface is broad. Their outputs are advisory until reconciled against disk evidence and route artifacts.

Required subagent roles when available:

- git/files/dependencies auditor;
- MT5/account/symbol/broker-spec auditor;
- tick/M1/M15/data-capture auditor;
- process/watchdog/scheduler auditor;
- vNext runtime/gate/packet/risk auditor;
- broker lifecycle/open-trade/notification auditor;
- stale-context/old-system-leakage auditor.

Each subagent must inspect files/logs/process evidence and return exact paths, commands, counts, issues, repairs, and confidence. No generic summaries.

## Verification

Run the strongest focused verification available from current disk, including route-owned verifier and tests created by this goal.

Baseline checks:

```powershell
python scripts/generate_live_state.py
python -m py_compile run_agent.py src/components/orchestrator.py src/components/execution.py src/components/gtos_vnext_runtime.py src/components/m1_capture.py src/components/tick_capture.py
python -m pytest tests/test_m1_capture.py tests/test_config_symbol_aliases.py tests/test_notifications.py tests/test_limit_order_flow.py tests/test_vnext_broader_origin_orchestrator.py -q
```

Run current vNext verifiers that exist on disk:

- live/replay gate-stack verifier;
- post-reload candidate proof verifier;
- starvation intelligence verifier;
- broker lifecycle truth verifier;
- Lane01/Lane04/Lane06/Lane07/Lane08/Lane09/Master verifiers when their artifacts are active inputs for the live runtime;
- any current route verifier named by `LIVE_STATE` or the reading order.

If a listed verifier path does not exist on the VPS, record the exact absence and classify whether it is required for live execution, required for research reproducibility, or cold evidence. Live-required verifiers/artifacts must be restored or regenerated.

## Completion And Continuation Standard

Initial activation verification is complete only when:

- VPS repo version is proven;
- current vNext contract is proven from code/config/process/log evidence;
- all required production files are present and loadable;
- all 24 symbols are broker-resolved and source-checked;
- MT5 account and market data are proven;
- 24 orchestrators are running or exact market/platform evidence explains a bounded exception;
- 24 tick captures are running or exact market/platform evidence explains a bounded exception;
- M1 capture is running and per-symbol state is written;
- watchdog/scheduler are configured and proven;
- candidate/no-candidate evidence is complete;
- open trades/orders/deals are reconciled;
- risk/exposure authority is current;
- notification queue is current;
- every verified defect found during this run is repaired, restarted/reloaded, retested, and recorded;
- supervisor cycle state is restartable from route files.

The goal remains active after initial verification unless the CEO stops it or a persistent supervisor service replaces the goal session. A clean snapshot is a checkpoint, not terminal completion.

Terminal completion requires one of:

- CEO explicitly stops this live supervisor route;
- a tested persistent supervisor service replaces the goal session and is proven alive;
- a hard forbidden boundary blocks continuation and the blocker is recorded with exact source/action/approval requirement.

## Prohibited Weak Closure

Do not mark complete because:

- checks ran once;
- no candidates appeared yet;
- no trade closed yet;
- broker is quiet;
- logs look normal;
- a summary exists;
- a blocker ledger exists;
- only examples in this prompt were checked;
- context compacted;
- the session is long.

Completion requires the standard above. Every other state is a checkpoint with next live-supervision cycle recorded.
