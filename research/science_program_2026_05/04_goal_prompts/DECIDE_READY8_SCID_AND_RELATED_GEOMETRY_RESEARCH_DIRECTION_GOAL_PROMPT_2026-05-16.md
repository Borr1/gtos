# DECIDE_READY8_SCID_AND_RELATED_GEOMETRY_RESEARCH_DIRECTION

Date: 2026-05-16
Objective: `DECIDE_READY8_SCID_AND_RELATED_GEOMETRY_RESEARCH_DIRECTION`
Mode: decisive research-direction execution session
Promotion posture: `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, `live_effect=false`
Execution location: main worktree only, `C:\Users\MSI\Documents\ai-trading-agent`

Do not create a separate worktree. Do not create a separate branch. This is not a parallel route. This is the orchestration stop-condition for the READY8 SCID ladder, and the scoped decision artifacts must be committed on main.

## Owner Stop Condition

This session exists to stop the READY8 SCID G0/G12/repair/capture-requirement ladder now.

The final product is a research-direction decision. The session must make the READY8 SCID research-direction decision now. It must not prepare, defer, summarize, hand off, route, gate, audit, ask for another session, or produce a vague recommendation. It is not a G12, not a G0, not an audit route, not a prompt-hardening route, not a blocker packet, not a capture-requirement packet, and not a proof-of-impossibility artifact. A next prompt is not completion. A blocker ledger is not completion. A clean "missing geometry" classification is not completion. If missing geometry is encountered, immediately choose one of the allowed decision branches below: reconstruct/proxy now, implement forward geometry capture, merge or redirect to moonshot/replayable geometry, preserve as neutral movement only, or merge into a broader system without continuing READY8 SCID as standalone.

## Hard Prohibitions

- No next prompt.
- No next starter.
- No G0.
- No G12.
- No audit route.
- No prompt-hardening route.
- No blocker packet.
- No capture-requirement-only packet.
- No proof-of-impossibility-only output.
- No "needs more geometry" terminal state.
- No "wait for future session" terminal state.
- No "continue research later" terminal state.
- No sixth decision.
- No vague recommendation.
- No summary-only answer.
- No representative-only evidence.
- No arbitrary top-N, top 3/5/10, or number-limited cutoff.

## Allowed Terminal Decisions

Exactly one terminal decision is allowed. The final ledger must set `terminal_decision` to exactly one of:

1. `TRADEABLE_NOW_WITH_RECONSTRUCTED_OR_PROXY_GEOMETRY`
2. `TRADEABLE_AFTER_FORWARD_GEOMETRY_CAPTURE_IMPLEMENTED`
3. `READY8_SCID_NOT_THE_RIGHT_PATH_REDIRECT_TO_MOONSHOT_OR_OTHER_REPLAYABLE_GEOMETRY`
4. `READY8_SCID_USEFUL_ONLY_AS_NEUTRAL_MOVEMENT_INTELLIGENCE_NOT_TRADE_STRATEGY`
5. `MERGE_READY8_SCID_INTELLIGENCE_INTO_A_BROADER_SYSTEM_BUT_DO_NOT_CONTINUE_IT_AS_STANDALONE`

No other terminal decision is allowed. The session must explicitly evaluate all five decisions against disk evidence and choose one. If evidence is mixed, choose the evidence-dominant terminal decision and explain the tradeoff; do not invent a sixth state and do not defer the decision.

## Mandatory Context Use

Before any conclusion, regenerate and read `.context/LIVE_STATE.md`, then read `AGENTS.md`, `CLAUDE.md`, the latest numbered handoff in `.context/02_session_handoffs/`, `.context/00_core/orchestrator_successor_operating_brief.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/orchestrator_methodology_hardening_controls.md`, `.context/00_core/parallel_goal_merge_playbook.md`, `.context/00_core/research_current_state.md`, `research/science_program_2026_05/05_synthesis/ORCHESTRATOR_ROUTE_STATUS_REGISTRY_2026-05-15.json`, and `research/science_program_2026_05/05_synthesis/ORCHESTRATOR_CROSS_ROUTE_QUESTION_AMBIGUITY_LEDGER_2026-05-15.json`.

Do not rely on chat memory, pasted closeouts, or previous assistant summaries. Treat this prompt, the starter message, `goal_session_research_discipline.md`, and `research_operating_doctrine.md` as active instructions, not background. After any context compaction, resume, interruption, or uncertainty, regenerate `LIVE_STATE` and reread this prompt, the starter message, the doctrine files, the latest decision artifacts, and the current route context anchor from disk before continuing. This is a proof-or-impossibility session only in the limited sense that same-evidence-class issues that affect the final decision must be pursued until answered, repaired, or proven impossible; proof-of-impossibility by itself is not an allowed terminal decision.

This is a constructive decision session. Apply curiosity and active creativity, but keep evidence truthful. Safe flags are rails, not brakes. Do not use `NO_PROMOTION_VERDICT` as a reason to avoid a research decision. Do not use missing live/broker PnL as a reason to avoid deciding whether READY8 SCID is useful, merge-worthy, redirect-worthy, or standalone-dead.

## Evidence Class And Forbidden Surfaces

Use accepted research artifacts, route-local artifacts, committed shadow logs, local historical/MT5/Sierra-derived data, source-bound repaired rows, and moonshot artifacts if present. You must compute any additional source-bound summaries, joins, consistency checks, or row-level reconciliations needed to make the terminal decision fully evidence-backed. Do not skip computation because it is inconvenient; compute it unless it is impossible from available disk/local/source-bound evidence, and record exact impossibility if so. You may create only the allowed scoped decision artifacts listed below.

Do not change live trading behavior. Do not edit `src/`, `prompts/`, `config/`, risk, safety, execution, canary, selector, broker/order/account/history/deal/position code or data, AI/API calls, paid/vendor pulls, credentials, remotes, raw market blob commits, or promotion/live controls. If the chosen decision implies an implementation action that touches these surfaces, record the exact implementation action and approval boundary in the decision memo; do not silently edit forbidden surfaces.

No arbitrary top-N, top 3/5/10, number-limited cutoff, representative-only sample, or summary-only substitute is allowed for evidence that matters to the decision. Preserve all material rows in full ledger form before summarizing.

Safe flags must remain:

- `NO_PROMOTION_VERDICT`
- `validation_safe=false`
- `outcome_review_opened=false`
- `live_effect=false`

## Required Disk Evidence To Read And Reconcile

Read the full relevant R7-R11/G12 READY8 route artifacts from disk, not summaries. At minimum inspect decision ledgers, completion audits, verification results, output manifests, source-hash ledgers, row-universe ledgers, repaired-target ledgers, failure-intelligence ledgers, weak-overlap/source-root ledgers, and synthesis notes for:

- `ready8_expanded_sealed_validation_packet_after_repairs`
- `ready8_expanded_validation_scoring_result_materialization`
- `ready8_expanded_forward_retest_source_capture_packet_after_g12_scoring_audit`
- `ready8_r9_accepted_packet_sealed_historical_validation_execution`
- `ready8_r11_trade_geometry_source_capture_repair_packet_after_r10_g12_audit`
- the accepted G12 artifacts attached to those routes
- the earlier accepted READY8 discriminative sealed validation/control artifacts when needed for denominator interpretation

Also inspect current relevant `shadow_logs/`, `knowledge_base/`, `data/historical_2026/`, local Sierra/SCID-derived artifacts referenced by manifests, and the moonshot worktree/artifacts at `` if present because moonshot evidence may dominate the decision. If that worktree is absent or Git marks it dubious, read files directly by path where possible and record the access result; do not stop.

## Known Disk-Backed Numerical Evidence To Verify

These numbers are anchor evidence already observed from disk and must be verified or corrected by reading the artifacts. If current disk evidence differs, disk wins and the discrepancy must be explained.

- R11 G12 accepted the R11 packet as audited with `row_universe=5,502`.
- R11 row universe: `182` packet rows plus `5,320` repaired target rows.
- R10/R11 repaired target neutral movement rows: `5,320` numeric rows.
- Neutral movement signs: `3,688` positive, `1,512` negative, `120` zero.
- Neutral movement mean: `0.0028383067873157895`; median `0.000763935138`; min `-0.01169556985`; max `0.075757630536`.
- Neutral movement by symbol: `EURUSD n=848 mean=0.0008678985648396242`; `GBPUSD_6B n=848 mean=0.0011977881442452852`; `NAS100_NQ n=848 mean=0.0009690268853490547`; `US30_YM n=848 mean=0.0011437718682547145`; `USDJPY_6J n=848 mean=0.00010060324120754664`; `XAGUSD_SI n=232 mean=0.03034737053841382`; `XAUUSD_GC n=848 mean=0.005224687408849056`.
- R11 source-root search: `51` searched roots.
- R11 weak overlaps: `80` weak symbol-time overlaps; `4,000` stored weak source-match rows.
- R11 exact trade geometry: `0` exact R/expectancy rows and `0` target/stop hit/miss rows.
- R10 geometry coverage: `5,502` rows; `5,320` repaired target rows; `182` packet rows.
- R10/R11 failure intelligence: `2,641` failure-intelligence rows.
- R10 packet family counts: `143` HAZ001 rows, `32` MAC rows, `5` HAZ005 rows, `1,154` UNC004 rows, `1` residual row.
- R8 scoring/materialization: `182` packet rows, `182` row results, `5,320` repaired target-consumption rows, `2,641` failure-intelligence rows.
- Earlier READY8 anchor: `3,014` source candidates, `24,112` repaired discriminative rowset rows, `192,896` target-result rows, `162,336` computable rows, `30,560` fail-closed rows.
- Fail-closed repair: `35,811` fail-closed/excluded inventory rows, `30,560` target fail-closed rows, `5,251` role-excluded denominator rows, `220` local SCID recovered M15 windows, `5,320` repair-candidate target rows, `25,240` still fail-closed rows.
- Moonshot artifacts, if present, must be considered for decision context. Known helpful numbers include: `39,003,954` tick rows over `114` tick parquet files; `8,977` primitive event rows; `22,229` target event rows; `420` triage/control rows; `72` path-geometry challenger/residual/question rows; `3,128` path-geometry rows; `18` residual rows; `250,133` Sierra input primitive event rows; `489,748` Sierra target event rows; `4,590` Sierra flag-control rows; `260,651` Sierra fail-closed rows; and any stronger replayable geometry/R-style or path-ordering numbers present in moonshot artifacts.

## Decision Standard

The standard is decision progress, not artifact completeness. You may write artifacts only to make the decision durable and machine-checkable. Do not create a downstream G12/G0/prompt ladder.

For each allowed terminal decision, write a complete disk-backed evidence case. The human memo may be readable and direct, but the decision ledger/evidence matrix must preserve all material evidence needed to defend or reject that terminal decision.

- what evidence would make this decision correct;
- what current disk evidence supports it;
- what current disk evidence argues against it;
- whether missing geometry can be reconstructed/proxied now, must be forward captured, should be merged into moonshot/replayable geometry, makes READY8 SCID neutral-only, or makes standalone READY8 SCID the wrong path;
- what exact implementation action follows if this decision is chosen.

Choose one decision. The decision must answer:

- What READY8 SCID is useful for.
- What READY8 SCID is not useful for.
- Whether READY8 SCID can become a trade strategy.
- What exact evidence supports that.
- What exact numbers matter.
- Whether GTOS should continue READY8 SCID, merge it, or stop it as standalone.
- What implementation action happens next in the system.
- What changes in research priority after this decision.

## Missing Geometry Handling

Do not stop at missing side, entry, stop, target, fillability, path-order, cost/slippage, or R geometry. When those gaps appear, immediately make one of these choices and defend it:

- reconstruct or proxy geometry now from accepted/source-bound artifacts and compute research R-style/proxy results;
- implement forward geometry capture as the next system action because the SCID path is worth continuing as a future trade-strategy candidate;
- merge with moonshot or another replayable geometry lane because that lane has stronger executable geometry;
- preserve READY8 SCID as neutral movement intelligence only and stop standalone strategy work;
- merge READY8 SCID intelligence into a broader system but stop standalone READY8 SCID route continuation.

Same-evidence-class issues should still be pursued and repaired inside this session when they materially affect the decision. But the session must not transform that repair into a new route ladder. If repair changes the decision, recompute the affected decision evidence and decide.

## Output Requirements

Allowed outputs only:

- decision memo;
- machine-readable decision ledger;
- full evidence matrix;
- completion audit section inside the decision memo or ledger;
- lightweight invariant verifier/check only if needed;
- scoped commit.

Write outputs under:

`research/science_program_2026_05/06_outcome_testing/decide_ready8_scid_and_related_geometry_research_direction/`

Required files:

1. `DECIDE_READY8_SCID_RESEARCH_DIRECTION_DECISION_MEMO_2026-05-16.md`
   - Human-readable final decision.
   - Must name exactly one allowed terminal decision.
   - Must include the useful/not-useful/trade-strategy/continue-merge-stop/implementation-action/research-priority answers.
   - Must include the verified numerical evidence.
   - Must explicitly say no G12, no G0, no audit route, no prompt-hardening route, and no next prompt is required after this decision.

2. `DECIDE_READY8_SCID_RESEARCH_DIRECTION_LEDGER_2026-05-16.json`
   - Machine-readable ledger with exact keys:
     - `route_id`
     - `terminal_decision`
     - `allowed_terminal_decisions`
     - `all_allowed_decisions_evaluated`
     - `disallowed_terminal_states_rejected`
     - `ready8_scid_useful_for`
     - `ready8_scid_not_useful_for`
     - `can_become_trade_strategy`
     - `continue_merge_or_stop`
     - `implementation_action_next`
     - `research_priority_change`
     - `key_numbers`
     - `evidence_sources_read`
     - `moonshot_evidence_considered`
     - `safe_flags`
     - `completion_audit`
   - `terminal_decision` must be exactly one of the five allowed strings.
   - `disallowed_terminal_states_rejected` must explicitly reject `needs_g12`, `needs_g0`, `needs_audit`, `needs_prompt_hardening`, `needs_capture_packet_only`, `missing_geometry_only`, `proof_of_impossibility_only`, and `emit_next_prompt`.

3. `DECIDE_READY8_SCID_RESEARCH_DIRECTION_EVIDENCE_MATRIX_2026-05-16.jsonl`
   - Full decision evidence rows. No arbitrary top-N, top 3/5/10, representative-only sample, or summary-only substitute.
   - Include all material evidence rows needed to justify the decision, including READY8/R7-R11 evidence, failure intelligence, weak overlaps, source-root coverage, neutral movement summaries, and moonshot/main artifact numbers that influence the decision.

Only if needed:

- `DECIDE_READY8_SCID_RESEARCH_DIRECTION_DECISION_CHECK_RESULT_2026-05-16.json`
  - A lightweight local verifier/check result for the one-of-five decision invariant and required count presence. This is not an audit route and not a G12/G0 gate.

Do not emit a next prompt or starter. If you create one, the goal failed.

## Verification And Completion

Run JSON parse checks for every JSON/JSONL artifact you create. Run focused tests or a lightweight verifier only if it directly checks decision invariants and required evidence counts; do not create an audit route. Run any existing route verifier/tests that are feasible and relevant only as evidence sanity checks, not as a new acceptance ladder. Record verifier/test results in the decision ledger.

The completion audit section must prove:

- preflight and mandatory context were read from disk;
- this prompt and starter were reread after any compaction/resume/interruption/uncertainty;
- all five allowed terminal decisions were evaluated;
- exactly one allowed terminal decision was selected;
- no disallowed terminal state was used;
- no G12/G0/audit/prompt-hardening/next-route artifact was emitted;
- all required numerical anchors were verified or corrected from disk;
- all material same-evidence-class issues that could alter the decision were pursued or exactly bounded;
- safe flags remain `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, `live_effect=false`;
- no forbidden live, promotion, broker, order, account, history, deal, position, prompt, config, risk, safety, execution, canary, selector, AI/API, paid/vendor, raw-blob, credential, remote-push, or live-trading surface changed.

Mark the goal complete only after the decision memo, ledger, evidence matrix, parse checks, and scoped commit are complete.
