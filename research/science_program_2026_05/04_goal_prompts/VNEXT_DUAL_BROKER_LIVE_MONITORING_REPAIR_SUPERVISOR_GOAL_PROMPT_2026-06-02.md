# vNext Dual-Broker Live Monitoring And Repair Supervisor Goal Prompt

Date: 2026-06-02

Route id: `vnext_dual_broker_live_monitoring_repair_supervisor_2026_06_02`

Evidence class: `DUAL_BROKER_VPS_LIVE_RUNTIME_DATA_BROKER_PROCESS_REPAIR_SUPERVISION`

Objective: become the active repair supervisor for the strongest current V3/moonshot vNext production runtime across redacted_account and FTMO. Rebuild current truth from disk and live VPS evidence, understand the complete production system before judging it, make the V3 selector/scheduler/execution moonshot version work as intended in both live accounts, prove both broker/account pathways, prove all runtime data and intelligence streams, prove every live-facing code/config/profile/process/packet/risk/execution/notification dependency, inspect every material candidate/trade/lifecycle event from raw evidence, repair every verified defect inside this evidence class, restart or reload affected components immediately when repair or activation requires it, rerun focused verification, record before/after proof, and continue from checkpointed route state without drifting after context compaction.

CEO production approval is embedded in this prompt: after current-disk, MT5, namespace, risk, data, candidate, notification, and rollback verification, enable FTMO order-capable live execution alongside the already-running redacted_account production runtime. This approval covers changes to any code, config, profile, launcher, watchdog, supervisor, runtime package, verifier, test, route artifact, process orchestration, namespace, data-capture, notification, and rollback surface required to make the dual-broker production system correct and live. The session must not hesitate because a file is live-facing; live-facing repairs are the purpose of this goal.

Expected posture: live production after major research will expose defects, mismatches, stale assumptions, namespace gaps, process gaps, V3 wiring gaps, packet gaps, risk gaps, and lifecycle gaps. Pursue real evidence until those defects are repaired. Do not manufacture issues, inflate severity, or force findings. Do not collapse onto one issue while leaving other production surfaces uninspected. The standard is complete dual-account live correctness, not issue volume.

This is the successor to the VPS live activation active supervisor route. It inherits that route as a minimum and expands it to the dual-broker, V3-package, FTMO activation-approved production surface. It is not a watch-only monitor, not a script-only health check, not a summary route, not a stale checkpoint refresher, not a one-account check, and not an infinite ledger loop.

## Current Production Anchor To Verify From Disk

Treat these as evidence anchors that must be verified from current disk, current Git state, current VPS state, and current MT5 state before use:

- Current remote main must be verified at runtime from Git, not from any hardcoded hash in this prompt.
- Prior live supervisor route: `research/operations/vnext_vps_live_activation_active_supervisor_2026_06_01/`.
- VPS/V3/FTMO integration route: `research/operations/vnext_vps_v3_full_promotion_and_ftmo_setup_2026_06_02/`.
- redacted_account authority expected: existing normal MT5 terminal at `C:\Program Files\MetaTrader 5\terminal64.exe`, existing active redacted_account process group, existing live order-capable runtime if current disk/process evidence still proves that state.
- FTMO authority expected: portable MT5 terminal at `C:\MT5\FTMO\terminal64.exe` using `/portable`, FTMO profile verified, terminal/account verified, and now approved for order-capable `run_agent` activation after verification proves terminal/account/profile/namespace/risk/data/candidate/notification isolation.
- V3 package state must be proven from disk and runtime. Selector V3, Scheduler V3, and Execution Policy V3 must receive an executable disposition: active, activated in this goal, staged with an exact unclosed requirement, or rejected with evidence. Default-off from an older route is historical until current disk proves it remains the correct production disposition.

Evidence anchors are not authority. Current disk and current live evidence are authority. Replace stale facts with current values in the route state.

## Mandatory Context Use

Before any repair, process restart, code claim, broker claim, or completion claim:

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/current_vnext_system_map.md`.
4. Read `.context/00_core/current_repo_reading_order.md`.
5. Read `.context/00_core/quick_reference_card.md`.
6. Read `.context/00_core/goal_session_research_discipline.md`.
7. Read `.context/00_core/research_operating_doctrine.md`.
8. Read `.context/00_core/orchestrator_successor_operating_brief.md`.
9. Read `.context/00_core/orchestrator_methodology_hardening_controls.md`.
10. Read `.context/00_core/parallel_goal_merge_playbook.md`.
11. Read the prior VPS supervisor prompt and starter:
    - `research/science_program_2026_05/04_goal_prompts/VNEXT_VPS_LIVE_ACTIVATION_ACTIVE_SUPERVISOR_GOAL_PROMPT_2026-06-01.md`
    - `research/science_program_2026_05/04_goal_prompts/VNEXT_VPS_LIVE_ACTIVATION_ACTIVE_SUPERVISOR_STARTER_2026-06-01.txt`
12. Read the prior VPS supervisor route artifacts, including `VPS_SUPERVISOR_STATE.json`, `VPS_VERIFICATION_RESULT.json`, `VPS_ANOMALY_LEDGER.jsonl`, `VPS_REPAIR_LEDGER.jsonl`, `VPS_RESTART_RELOAD_LEDGER.jsonl`, `VPS_OPEN_TRADE_LIFECYCLE_LEDGER.jsonl`, `VPS_RISK_EXPOSURE_LEDGER.jsonl`, and `VPS_COMPLETION_OR_CONTINUATION_AUDIT.json`.
13. Read the VPS/V3/FTMO integration route artifacts, including `VPS_V3_FTMO_COMPLETION_AUDIT.json`, `VPS_V3_FTMO_VERIFICATION_RESULT.json`, MT5 probe artifacts, profile verification artifacts, namespace collision verifier artifacts, V3 package provenance artifacts, and rollback/handoff artifacts.
14. Read the active broker profiles and runtime config:
    - `config/agent_config.yaml`
    - `config/profiles/redacted_account.yaml`
    - `config/profiles/ftmo.yaml`
    - `config/profiles/operator_profile.yaml`
15. Read current live-facing code before claiming behavior: `run_agent.py`, `start_all.bat`, watchdog scripts, `src/components/orchestrator.py`, `src/components/gtos_vnext_runtime.py`, `src/components/execution.py`, `src/components/m1_capture.py`, `src/components/tick_capture.py`, `src/components/broker_truth_cost_capture_v2.py`, `src/research/moonshot_selector_v3_default_off.py`, Scheduler V3 helper/package code, and Execution Policy V3 helper/package code.

After context compaction, resume, interruption, command failure, tool crash, long wait, process restart, or uncertainty, rerun preflight, reread this prompt, reread the starter, reread the doctrine files, reread the current route state, and continue from route artifacts. Do not rely on chat memory. Chat memory is not an authority source.

The completion or continuation audit must record instruction coverage: files read, route artifacts read, current HEAD, current remote, broker profiles inspected, both MT5 terminals probed, processes inspected, candidates/trades inspected, defects pursued, repairs applied, restarts/reloads performed, tests/verifiers run, remaining exact blockers, and next checkpoint focus.

## Hardening Controls

These are active instructions, not background. Operationalize them in the work queue, ledgers, route verifier, repair records, restart/reload records, checkpoints, and completion audit.

No conservative brake applies inside this evidence class. The job is live-system truth and repair. Verification without repair is incomplete when the defect is repairable from current code, config, data, process, log, profile, route, MT5, or broker-read-only evidence.

Full same-evidence-class pursuit is mandatory. Same-class defects, stale facts, missing fields, source gaps, process gaps, lifecycle gaps, account/profile gaps, namespace gaps, and verifier gaps must be pursued until repaired, proven impossible, or reduced to an exact owner/source/action requirement.

Literal impossibility means exactly this: every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available from current VPS/local/MT5/broker/process/log/code/route evidence has been performed and recorded.

Truth discipline is mandatory. A defect exists only when raw evidence, current code/config, live process state, MT5 read-only state, route artifacts, logs, or verifier/test output prove it. The session must not invent defects to satisfy the expectation that live issues may exist. It must also not soften, ignore, or defer a proven defect because it touches production code or requires restart/reload.

Coverage discipline is mandatory. The session must keep all production surfaces in view: redacted_account, FTMO, V3 selector, V3 scheduler, V3 execution policy, launcher, watchdog, supervisor, process health, tick/M1/M15 capture, packet completeness, candidate/no-candidate proof, order lifecycle, risk, notification truth, namespace isolation, rollback, tests, verifiers, and current route state. Deep pursuit of one defect is valid only while the finite work queue preserves the remaining surfaces and resumes them after the repair.

No arbitrary top-N, top 3/5/10, representative-only pass, or example-only pass is allowed for broker accounts, symbols, processes, candidates, no-candidate periods, trades, tickets, lifecycle events, nulls, zero-without-reason fields, anomalies, stale labels, source gaps, rejected rows, repairs, blockers, or source roots. Summaries may rank after the complete ledger preserves all material rows/events.

No script-only closure. Scripts, builders, parsers, and verifiers locate evidence and enforce contracts; the agent must personally inspect every material new candidate, order, fill, partial, modify, close, false-close, notification, unresolved risk state, rejected packet class, stale-data class, and account/profile/process anomaly through raw evidence pointers. The route audit must distinguish machine-parsed coverage from direct agent inspection coverage.

No infinite ledger loop. Maintain a finite work queue. Every shard/event/surface has exactly one state: `discovered`, `inventoried`, `inspected`, `preserved`, `repaired`, `restarted_or_reloaded`, `validated`, `closed`, or `blocked_with_exact_missing_input`. Reprocess a closed item only when its source hash/state/timestamp changed or a verifier identifies a concrete gap. Repeated reruns without new evidence, changed source state, or a targeted validator failure are forbidden.

Repair-first priority is mandatory. Ledgers, manifests, parsers, builders, and summaries are tools for finding and proving repairs; they are not the primary outcome. When raw evidence proves a repairable defect, fix the defect, restart/reload if required, reverify, and then update the ledger with before/after proof. Do not expand ledgers while a proven repair is waiting.

Checkpoint discipline is mandatory. The route must write a checkpoint after preflight, after each material defect class is inspected, after every repair, after every restart/reload, after every activation step, after every verifier/test run, after context compaction/resume, and before any long-running operation. Each checkpoint must state current account/process/data/candidate/trade/risk/V3/notification status, active defect, repair state, next exact action, and what must not be repeated. A session that loses direction must resume from the latest checkpoint, not restart from chat memory or rerun broad ledgers.

Every artifact must feed one of these outputs: defect repair, restart/reload decision, live supervision checkpoint, verifier/test coverage, broker/account/profile proof, candidate/trade/lifecycle proof, data-source proof, V3 disposition proof, rollback proof, or exact blocker proof. Artifacts created only to make the route look large are invalid.

Record result materialization for every material finding with result-use-status, exact-R/proxy-R/expectancy status where relevant, source-capture/source completeness, branch decision, implementation decision, runtime-effect boundary, source-use state, account namespace, repair action, validation action, and closure state.

## Authorized Work And Hard Boundaries

Authorized inside this goal:

- read-only MT5 account, terminal, symbol, tick, bar, order, position, deal, history, and broker-spec inspection for redacted_account and FTMO as the evidence channel;
- local/VPS code repair across any file required for correct dual-broker production behavior;
- config/profile repair for current vNext, redacted_account, FTMO, namespace, data-root, supervisor, watchdog, launcher, packet, risk, notification, verifier, and route integrity;
- data-capture repair;
- logging/packet/schema/verifier/test repair;
- process, watchdog, scheduler, heartbeat, notification worker, M1, tick, displacement, and orchestrator repair;
- restart or reload of affected GTOS runtime components when needed to apply a verified repair;
- FTMO order-capable live `run_agent` activation after verification proves account/profile/terminal/namespace/risk/data/candidate/notification isolation and rollback readiness;
- redacted_account and FTMO concurrent runtime activation, reload, repair, and supervision through the approved vNext system-managed code path;
- V3 selector, scheduler, execution policy, packet, risk, and rollback wiring changes required to make production runtime consume the strongest verified package disposition;
- immediate restart, reload, relaunch, or watchdog repair when code/config/process/data-capture/notification/V3 activation changes require runtime application;
- scoped commits for repaired code/config/tests/route artifacts;
- scoped remote push for repairs produced by this goal, with exact branch/ref/commit/touched-path proof. This prompt and starter provide scoped push approval for this goal's owned changes.

Forbidden surfaces core: production-change claims beyond verified code/config/profile/runtime evidence, live trading behavior changes outside the approved vNext production contract, broker operation mutation outside system-managed vNext runtime activation/execution, paid API or vendor spend, broker account/order/history/deal/position mutation outside approved system-managed vNext execution, and prompt/config/risk/execution/safety/canary/selector behavior changes beyond this goal's authorized repair and dual-broker activation surface require a separate exact owner command.

Forbidden broker/account mutation without a separate exact owner command:

- manual broker order placement;
- manual broker order modification;
- manual broker order cancellation;
- manual position close;
- credential printing or credential change;
- paid/vendor/API spend.

The already-running redacted_account production system and the newly authorized FTMO production runtime may manage trades through the approved vNext code path. This goal verifies, repairs, activates, and supervises that code path. It does not perform discretionary manual trading.

FTMO order-capable activation is approved by this prompt. If FTMO remains staged at session start, prove staged readiness, prove terminal/account/profile/namespace/risk/data/candidate/notification isolation, execute the approved activation path, then verify process/data/candidate/risk/notification/lifecycle proof for both accounts. If activation exposes a defect, repair it, restart/reload the affected component, reverify, and continue supervising both accounts.

## Required Route Directory

Create and maintain:

`research/operations/vnext_dual_broker_live_monitoring_repair_supervisor_2026_06_02/`

Required artifacts:

- `DUAL_SUPERVISOR_CONTEXT_ANCHOR.md`
- `DUAL_SUPERVISOR_STATE.json`
- `DUAL_SUPERVISOR_WORK_QUEUE.jsonl`
- `DUAL_SUPERVISOR_ACTION_LEDGER.jsonl`
- `DUAL_GIT_VERSION_AUDIT.json`
- `DUAL_SOURCE_INVENTORY_LEDGER.jsonl`
- `DUAL_REQUIRED_FILE_LEDGER.jsonl`
- `DUAL_ENVIRONMENT_LEDGER.jsonl`
- `DUAL_ACCOUNT_PROFILE_LEDGER.jsonl`
- `DUAL_MT5_TERMINAL_ACCOUNT_PROBE.json`
- `DUAL_MT5_SYMBOL_SPEC_LEDGER.jsonl`
- `DUAL_NAMESPACE_ISOLATION_LEDGER.jsonl`
- `DUAL_PROCESS_HEALTH_LEDGER.jsonl`
- `DUAL_WATCHDOG_SCHEDULER_LEDGER.jsonl`
- `DUAL_DATA_CAPTURE_HEALTH_LEDGER.jsonl`
- `DUAL_RUNTIME_DECISION_LEDGER.jsonl`
- `DUAL_CANDIDATE_PACKET_LEDGER.jsonl`
- `DUAL_CANDIDATE_AGENT_INSPECTION_LEDGER.jsonl`
- `DUAL_NO_CANDIDATE_PROOF_LEDGER.jsonl`
- `DUAL_OPEN_TRADE_LIFECYCLE_LEDGER.jsonl`
- `DUAL_TRADE_AGENT_INSPECTION_LEDGER.jsonl`
- `DUAL_BROKER_RECONCILIATION_LEDGER.jsonl`
- `DUAL_RISK_EXPOSURE_LEDGER.jsonl`
- `DUAL_V3_RUNTIME_DISPOSITION_LEDGER.jsonl`
- `DUAL_NOTIFICATION_LEDGER.jsonl`
- `DUAL_ANOMALY_LEDGER.jsonl`
- `DUAL_REPAIR_LEDGER.jsonl`
- `DUAL_RESTART_RELOAD_LEDGER.jsonl`
- `DUAL_SUPERVISOR_CHECKPOINTS.jsonl`
- `DUAL_VERIFICATION_RESULT.json`
- `DUAL_OUTPUT_MANIFEST.json`
- `DUAL_COMPLETION_OR_CONTINUATION_AUDIT.json`
- route-owned verifier script;
- focused tests for every code/config/schema repair made by this goal.

Route artifacts must preserve raw evidence pointers, hashes, timestamps, source paths, account namespace, symbol, ticket, lifecycle stage, direct-inspection status, repair action, validation action, and closure state.

## Stage 00 - Reanchor Repository, Branch, Runtime, And Prior Evidence

Prove:

- current repo path on VPS;
- current branch;
- current `HEAD`;
- current `origin/main`;
- current local dirty state classified by production relevance;
- LFS pointer status for runtime-required files;
- Python version and executable path;
- MT5 Python package import;
- active user;
- timezone;
- disk space;
- memory/CPU pressure;
- environment variables relevant to GTOS profiles/modes;
- `.env` presence without values;
- halt/autostart flags;
- Task Scheduler entries;
- watchdog/startup scripts;
- all running GTOS process command lines;
- previous VPS supervisor route status and open continuation gates;
- VPS/V3/FTMO route status and current V3 package disposition.

If local code is behind current approved remote production code, bring the VPS repo to current approved code, classify local changes before touching them, apply scoped production integration, and verify after update. Preserve live runtime changes and other-agent changes; never reset unrelated work.

## Stage 01 - Dual Broker Account, Terminal, Profile, And Namespace Proof

For redacted_account and FTMO separately prove:

- profile file path;
- profile parse result;
- account identity hash;
- broker server/company;
- terminal executable path;
- terminal data path;
- terminal mode;
- process namespace;
- runtime namespace;
- log namespace;
- data namespace;
- pipeline state namespace;
- account balance/equity/margin/free margin;
- trade permission fields;
- current open positions;
- pending orders;
- recent deals/history read-only availability;
- server time versus local/VPS time;
- account mismatch absence.

Required namespace separation:

- distinct terminal paths;
- distinct data paths;
- distinct profile names;
- distinct account identity hashes;
- distinct broker truth/log roots;
- distinct `pipeline_state` or account-scoped state authority where concurrent operation needs it;
- distinct `data/m1` and `data/ticks` paths or an explicit verified safe shared-read model;
- distinct comments/magic/account tagging for order/lifecycle records;
- distinct notification/account labels.

Repair profile, alias, namespace, path, comment/magic, state-root, and account-routing defects immediately. If a defect could mix redacted_account and FTMO state, repair before supervising order-capable FTMO execution.

## Stage 02 - Process, Watchdog, Scheduler, Always-On, And Dual Runtime Proof

Prove process state from PIDs, command lines, locks, heartbeats, logs, and output writes:

- redacted_account orchestrator fleet;
- FTMO orchestrator fleet after approved activation; if it is not running at session start, activate it after verification and then prove it;
- tick-capture workers for each active account;
- M1 capture for each active account or a verified account-isolated capture model;
- displacement logger;
- heartbeat monitor;
- notification queue worker;
- watchdog;
- scheduled task or always-on mechanism;
- live supervisor/repair companion process state.

For each process record account/profile, symbol, PID, start time, repo path, Python executable, command line, heartbeat path, latest heartbeat timestamp, latest log timestamp, last action, last error, data root, and restart requirement.

Repair missing, duplicated, stale, wrong-repo, wrong-profile, wrong-terminal, wrong-namespace, wrong-data-root, dead-heartbeat, or hidden-failure processes. Verify with process query, heartbeat movement, log movement, and affected route verifier.

Market-closed/broker-idle classification must be explicit. A daemon alive while broker quotes are naturally idle is not a defect. A feed-open process without expected data movement is a defect.

## Stage 03 - Dual Data Capture And Source Completeness

For every active broker account and every required symbol prove:

- broker-native symbol resolution;
- Market Watch selection;
- latest tick;
- tick date path;
- tick file write movement;
- M1 newest closed bar;
- M1 row count;
- M1 state file fields;
- M15/H1/H4/D1 availability required by runtime;
- no-new-row reason when applicable;
- read errors;
- malformed/corrupt files;
- zero-row-without-reason absence;
- date rollover behavior;
- account namespace in file path;
- alias namespace in file path;
- source-completeness classification.

Repair missing symbols, stale capture, missing state fields, corrupted files, path drift, alias drift, date rollover defects, M1/tick daemon crashes, and any logging that hides null or zero fields.

## Stage 04 - vNext/V3 Runtime Decision Chain Proof

Prove the live decision chain for each active account:

- candidate identity;
- account/profile namespace;
- canonical symbol and broker symbol;
- origin family;
- framework;
- side;
- session;
- source mode;
- HTF/M15/M1/tick snapshots;
- broker snapshot;
- raw geometry;
- repaired geometry;
- Gate0/Gate1/Gate3;
- dynamic policy;
- `execution_policy_id`;
- Selector V3 disposition and runtime wiring decision;
- Scheduler V3 disposition and runtime wiring decision;
- Execution Policy V3 disposition and runtime wiring decision;
- selected-cell proof;
- selected-cell source identity;
- risk/lot proof;
- prop/account checks;
- portfolio exposure;
- same-symbol and correlation decision;
- final order readiness;
- refusal/repair reasons;
- old-system absence proof;
- null/zero reason for every empty or zero material field.

For V3 specifically, prove from code/config/runtime artifacts whether each component is `active`, `activated_in_this_goal`, `staged_default_off_with_exact_unclosed_requirement`, or `rejected_with_evidence`. A component cannot remain an inert ledger with no runtime disposition. If current evidence supports activation, wire and verify activation with rollback proof. If activation is blocked, record the exact code/config/source/test/broker/process requirement and keep the component testable and verifier-covered.

Repair generic refusal labels, stale `framework_not_activated` drift, stale selected-cell risk bridge drift, old Gate1/Gate3 behavior, stale fixed-target labels, policy id loss, old PA/L2 fallback leakage, missing packet fields, cross-account packet mixing, and no-candidate silence.

## Stage 05 - Direct Candidate And No-Candidate Inspection

Build machine-parsed candidate/no-candidate ledgers, then perform direct agent inspection.

Directly inspect every material candidate and no-candidate class since the last checkpoint:

- all order-path candidates;
- all broker-ready candidates;
- all placement attempts;
- all Gate1/Gate3 rejects;
- all selected-cell/risk bridge issues;
- all prop/account deferrals;
- all same-symbol/correlation conflicts;
- all V3 `source_required`, `capture_repair`, `avoid`, `reduce_risk`, and `trade` actions when present, and exact absence proof when not present;
- all stale label classes;
- all malformed packets;
- all null/zero-without-reason classes;
- all account/profile namespace anomalies;
- every rare event class.

For each inspected item record raw file path, line/key, timestamp, account, symbol, candidate id, decision, direct inspection finding, machine parser finding, agreement status, repair requirement, and closure state.

Scripts are not enough. A parser saying `ok=true` does not close direct inspection. The final audit must show direct inspection coverage for every material candidate/trade/lifecycle class.

## Stage 06 - Open Trades, Orders, Deals, Lifecycle, And Broker Truth

For every open position, pending order, recent deal, local lifecycle row, runtime decision row, Telegram/notification row, and broker reconciliation row across both accounts prove:

- account/profile namespace;
- ticket/order/deal id;
- symbol/broker symbol;
- side;
- magic/comment;
- volume;
- entry price;
- SL/TP;
- selected execution policy;
- selected-cell risk;
- lot/risk dollars;
- worst-case risk;
- current floating PnL;
- commission/swap where available;
- partial-close state;
- BE state;
- residual ticket identity;
- modify attempts;
- retcode/comment;
- stop/freeze/spread constraints;
- local lifecycle state;
- broker lifecycle state;
- notification parity;
- next expected management action.

Every lifecycle management action must be ticket-bound by ticket/position identity, symbol, side, magic/comment, volume, account namespace, and broker response. Broker-close truth requires MT5 close-deal proof. Local projections cannot replace broker truth.

Repair false local closes, false notifications, missing MT5 joins, wrong residual ticket binding, BE/TP modify diagnostics gaps, failed retries without actionable retcode/request capture, unmanaged residual exposure, accidental same-symbol hedge/stack, cross-account lifecycle mixing, and projected PnL reported as broker truth.

## Stage 07 - Risk, Prop, Portfolio, Correlation, And Account Exposure Authority

For each account separately and for cross-account operator visibility prove:

- balance;
- equity;
- day-start baseline;
- realized broker PnL;
- open worst-case SL risk;
- pending worst-case SL risk;
- new-trade risk;
- selected-cell risk percent;
- configured profile risk percent;
- effective selected-cell risk percent;
- lot size;
- broker contract geometry;
- tick value;
- spread/slippage/commission/swap buffers;
- internal daily overlay;
- external account/challenge limits;
- total account limits;
- same-symbol exposure;
- correlated cluster exposure;
- risk release after partial/BE;
- portfolio scheduler state;
- V3 scheduler default-off/active disposition.

Count/session caps cannot silently control current vNext unless current code/config proves a current rule. Risk authority is money-risk and account exposure. Repair stale prop deferrals, stale base-risk fallback, stale labeling of configured risk versus effective selected-cell risk, stale R-count blocking, stale equity source, stale session caps, stale day-start baseline, missing risk-dollar evidence, and cross-account risk namespace collision.

## Stage 08 - Notifications, Telegram, Operator Truth, And Account Labels

Verify:

- notification queue worker alive;
- queue depth;
- queue drain;
- token/chat presence without exposing values;
- account labels in messages;
- current vNext/V3 policy labels;
- candidate/rejection/order/fill/partial/BE/close/reconciliation parity;
- failed send diagnostics;
- false-close absence;
- old fixed/J46/J49/BE-only production wording absence;
- redacted_account and FTMO separation.

Repair queue dead zones, stale labels, missing lifecycle fields, account-label loss, false close messages, failed-send diagnostics gaps, and notification/broker truth mismatch.

## Stage 09 - Live Repair Companion Cycle

Operate in checkpointed cycles:

1. Refresh current disk and live state.
2. Inspect both MT5 terminals/accounts.
3. Inspect process and watchdog state.
4. Inspect tick/M1/M15 data movement.
5. Inspect candidate/no-candidate evidence.
6. Inspect open trades/orders/deals/lifecycle evidence.
7. Inspect risk/exposure evidence.
8. Inspect V3 runtime disposition.
9. Inspect notifications.
10. Detect anomalies.
11. Repair verified defects.
12. Restart/reload affected components.
13. Rerun focused tests and verifiers.
14. Write checkpoint.
15. Advance the finite work queue.

Use delta checks for live cycles. Run full route verifiers when code/config/schema/data-contract changes or when an anomaly demands full recomputation. Do not rebuild giant historical research ledgers during routine live supervision unless a live defect requires that exact source evidence.

Repair beats ledger expansion. If the cycle finds a verified defect, stop widening the ledger, fix the defect, restart/reload the affected component, run focused verification, and record the checkpoint. Resume broader inspection only after the defect is closed or converted into an exact blocker.

## Subagent Use

Use subagents when available for independent disk-backed audits. Assign disjoint review roles so they do not duplicate work:

- Git/version/LFS/dependency auditor;
- redacted_account MT5/account/symbol/broker-spec auditor;
- FTMO MT5/account/symbol/broker-spec auditor;
- process/watchdog/scheduler auditor;
- tick/M1/M15/data-capture auditor;
- vNext/V3 runtime/packet/risk auditor;
- broker lifecycle/open-trade/notification auditor;
- stale-context/old-system-leakage auditor;
- finite-work-queue/loop-detector auditor.

Subagent outputs are advisory until reconciled against raw disk evidence and route artifacts. Each subagent must return paths, commands, counts, issue ids, repair recommendations, confidence, and any exact blocker.

## Verification

Run the strongest focused verification available from current disk after initial inspection and after every material repair. Include route-owned verifier and focused tests created by this goal.

Baseline commands where paths exist:

```powershell
python scripts/generate_live_state.py
python -m py_compile run_agent.py src/components/orchestrator.py src/components/execution.py src/components/gtos_vnext_runtime.py src/components/m1_capture.py src/components/tick_capture.py src/components/broker_truth_cost_capture_v2.py src/research/moonshot_selector_v3_default_off.py
python -m pytest tests/test_broker_profile_namespace.py tests/test_verify_broker_profile.py tests/test_m1_capture.py tests/test_tick_capture.py tests/test_notifications.py tests/test_execution.py tests/test_vnext_broader_origin_orchestrator.py tests/test_moonshot_default_off_policy_router.py tests/test_vnext_production_wiring.py tests/test_start_all_runtime_contract.py tests/test_watchdog_e2e_verify.py -q
```

Run current route verifiers on disk for:

- VPS live supervisor;
- VPS/V3/FTMO integration;
- FTMO profile verification;
- redacted_account profile verification;
- namespace collision;
- V3 package provenance;
- live activation checkpoint;
- post-reload candidate proof;
- starvation intelligence;
- broker lifecycle truth;
- notification queue;
- any route verifier named by `LIVE_STATE`, current reading order, or active route artifacts as live-required.

If a verifier is absent, record the exact absence and classify it as live-required, production-handoff-required, research-reproducibility-only, or cold evidence. Restore, regenerate, or replace live-required verification.

## Completion And Continuation Standard

Initial checkpoint closure requires:

- current repo/branch/HEAD/remote proven;
- both account profiles parsed and verified;
- both MT5 terminals/accounts probed;
- redacted_account active state proven;
- FTMO order-capable active state proven after approved activation;
- namespace isolation proven;
- all required production files present/loadable;
- all active broker symbols resolved and source-checked;
- process/watchdog/scheduler state proven;
- data capture state proven;
- candidate/no-candidate evidence complete for the current window;
- direct agent inspection coverage complete for all material candidate/trade/lifecycle classes;
- open trades/orders/deals reconciled;
- risk/exposure authority current;
- V3 disposition current;
- notification truth current;
- every verified defect repaired, restarted/reloaded where needed, retested, and recorded;
- finite work queue has no unprocessed material current-cycle item;
- continuation checkpoint records next cycle focus.

The goal remains active after initial checkpoint unless the CEO stops it or a tested persistent supervisor service replaces it. A clean snapshot is a checkpoint, not terminal completion.

Terminal completion requires one of:

- CEO explicitly stops this supervisor route;
- a tested persistent supervisor service replaces the goal session and is proven alive across both broker pathways;
- a hard forbidden boundary blocks continuation and the route records exact source/action/approval requirement.

## Prohibited Weak Closure

The route is not complete because checks ran once, the broker is quiet, no candidates appeared, no trade closed, one account works, a script passed, a parser summary exists, a route verifier passed without direct inspection, a blocker ledger exists, examples in this prompt were checked, context compacted, runtime is long, or the session produced a large ledger.

Completion requires the standard above. Every other state is a checkpoint with exact next live-supervision action.
