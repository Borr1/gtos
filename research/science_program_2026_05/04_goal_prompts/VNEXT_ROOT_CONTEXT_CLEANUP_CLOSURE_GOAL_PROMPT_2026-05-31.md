# vNext Root Context Cleanup Closure Goal Prompt

Date: 2026-05-31
Route id: `vnext_root_context_cleanup_closure_2026_05_31`
Evidence class: `ROOT_CONTEXT_CLEANUP_CLOSURE_CURRENT_VNEXT_PRE_LAUNCH`

## Objective

Close the cleanup route's current incomplete invariant before the next-level vNext goal-session program starts:

`root_physical_cleanup_pending_classify_delete_or_archive_root_files`

This goal session owns a sharp cleanup closure: root physical cleanup, stale root artifact deletion/archive, old generator output retargeting, active context/current-truth repair, runtime-dirt policy, verification, and scoped commit. The route closes the current root/context invariant and stops there with committed proof. It must reduce context pollution now, make future agents faster, and leave the next-level vNext prompt pack preserved as active launch material.

## Required Preflight

Run `python scripts/generate_live_state.py`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/repo_cleanup_and_staleness_policy.md`, `.context/00_core/goal_session_research_discipline.md`, and `.context/00_core/research_operating_doctrine.md`.

Read the existing cleanup route state and checkpoint artifacts:

- `research/operations/vnext_repo_context_cleanup_deletion_2026_05_29/REPO_CLEANUP_STATE.json`
- `research/operations/vnext_repo_context_cleanup_deletion_2026_05_29/REPO_ROOT_PHYSICAL_CLEANUP_CHECKPOINT_SUMMARY.json`
- `research/science_program_2026_05/04_goal_prompts/VNEXT_REPO_CONTEXT_CLEANUP_DELETION_GOAL_PROMPT_2026-05-29.md` as historical route context.

Read the current root file list and current `git status --short` from disk. Do not rely on chat memory.

After compaction, interruption, long wait, tool crash, or uncertainty, reread this prompt, the starter, `.context/LIVE_STATE.md`, cleanup state, cleanup checkpoint, and current git status before continuing.

Operationalize these active instructions, `.context/00_core/goal_session_research_discipline.md`, and `.context/00_core/research_operating_doctrine.md`; they are not background.

## Current Facts To Re-Verify

The previous cleanup route state says the first incomplete invariant is root physical cleanup. Current root still contains April-era and stale-context material such as old reports, zero-byte logs, root one-off scripts, old result dumps, and temporary Friday-number files. Examples include:

- `PHASE1_SYNTHESIS.md`
- `RED_TEAM_100_PERCENT.md`
- `mechanical_backtest.py`
- `monitor.sh`
- `pressure_test.py`
- old Opus/Sonnet comparison reports
- old April root analysis reports
- zero-byte root logs
- `tmp_weekend_numbers.json`

Examples are not limits. Classify every root file.

The previous cleanup session started valid output-retargeting edits in:

- `scripts/confidence_decomposition_test.py`
- `scripts/sprint_alpha_analysis.py`
- `scripts/sprint_alpha_four_analyses.py`
- `scripts/sprint_alpha_task1_rerun.py`

Inspect these edits from disk. Keep and complete valid retargeting edits that move stale root outputs into `research/archive/...` and pass syntax/reference checks. Repair incomplete archive paths, directory creation, imports, or tests.

The next-level prompt pack created on 2026-05-31 is active launch material, not stale prompt clutter. Preserve it and include it in the scoped commit or record its uncommitted status explicitly:

- `VNEXT_NEXT_LEVEL_PARALLEL_PROGRAM_LAUNCH_PACK_2026-05-31.md`
- `VNEXT_NEXT_LEVEL_MASTER_ORCHESTRATION_*_2026-05-31.*`
- `VNEXT_LANE01_*_2026-05-31.*` through `VNEXT_LANE09_*_2026-05-31.*`

## Execution Lanes

### 1. Root Physical Cleanup

Classify every root file into one of:

- `KEEP_ACTIVE_ENTRYPOINT`
- `KEEP_LOCAL_SECRET_OUT_OF_COMMIT`
- `KEEP_ACTIVE_CONFIG_OR_RUNNER`
- `DELETE_STALE_ROOT_CONTEXT`
- `ARCHIVE_THEN_DELETE_ROOT_CONTEXT`
- `RETARGET_GENERATOR_THEN_DELETE_OUTPUT`
- `REWRITE_OR_REPOINT_ROOT_DOC`

Keep root lean. Root retains operational entrypoints and current docs/configs: `AGENTS.md`, `CLAUDE.md`, `README.md`, `.gitignore`, `.gitattributes`, `pyproject.toml`, `requirements.txt`, `run_agent.py`, `start_all.bat`, `run_kap.sh`, local `.env*`, and root files that prove current authority from disk. Delete or archive stale April-era reports, obsolete one-off scripts, zero-byte logs, stale result dumps, stale generated analysis files, and old context-polluting markdown after extracting unique current intelligence into current truth or archive pointers.

Before deleting a tracked root file, search current active docs/code/tests/manifests for live references. Historical references do not preserve stale root context. Repair active references to current summaries or archive pointers before deletion.

### 2. Script Output Retargeting

Finish old generator retargeting so stale root outputs do not reappear. The four known script edits must:

- write generated legacy outputs under `research/archive/root_legacy_artifacts_2026_05_31/generated/...`;
- create output directories before writing;
- preserve script behavior for intentional legacy reruns;
- pass `py_compile`;
- be covered by a cleanup evidence row.

Find other root-writing scripts with `rg` and repair those that recreate stale root outputs. Keep edits scoped to cleanup, output retargeting, context pointers, and verification.

### 3. Current Context Repair

Repair active context so future agents read current vNext truth first:

- `.context/00_core/current_vnext_system_map.md`
- `.context/00_core/current_repo_reading_order.md`
- `.context/00_core/repo_cleanup_and_staleness_policy.md`
- `.context/00_core/research_current_state.md`
- `README.md`
- `CLAUDE.md`
- `AGENTS.md`

Remove stale current-framing: old 7-symbol production truth, old PrimaryAnalyzer/L2 current path, fixed 1.5R/J46/J49/BE-only as current production defaults, stale April-era root reports as authority, and old cleanup state that claims current files are unresolved after this route closes them.

Keep useful historical intelligence through current summaries or archive manifests. Do not keep polluted root files because one section is useful; extract the useful section first.

### 4. Runtime And Live Dirt Policy

Classify current `shadow_logs/`, `pipeline_state/`, `data/m1/`, archived gz movements, and generated program-control dirt instead of blanket-staging raw churn:

- hot evidence retained out of default reading path;
- curated summary/manifest to commit;
- stale runtime noise to delete/archive;
- generated local churn to ignore.

Commit curated evidence artifacts and policies, not raw churn. Update `.gitignore` or cleanup policy for recurring generated junk classes when the evidence supports it.

### 5. LFS And Large File Current-HEAD Check

Run a current-HEAD large-file/LFS check. This closure route fixes current root/context large-file pollution and records exact rewrite-ready proof for historical bloat that remains outside HEAD. Repository history rewrite is recorded as proof and command plan, not executed in this route. When no current-HEAD GitHub hard-limit files exist, record that and move on.

### 6. Verification

Write route outputs under:

`research/operations/vnext_root_context_cleanup_closure_2026_05_31/`

Required artifacts:

- `ROOT_CLEANUP_FILE_DECISION_LEDGER.jsonl`
- `ROOT_CLEANUP_DELETE_LEDGER.jsonl`
- `ROOT_CLEANUP_ARCHIVE_POINTERS.json`
- `ROOT_GENERATOR_RETARGET_LEDGER.jsonl`
- `ROOT_CONTEXT_REPAIR_LEDGER.jsonl`
- `RUNTIME_DIRT_POLICY_LEDGER.jsonl`
- `CURRENT_HEAD_LFS_LARGE_FILE_CHECK.json`
- `ROOT_CLEANUP_VERIFICATION_RESULT.json`
- `ROOT_CLEANUP_COMPLETION_AUDIT.json`
- `ROOT_CLEANUP_OUTPUT_MANIFEST.json`

Each artifact must include result materialization, source completeness, branch decision, and implementation decision fields where the row class supports them. The route must report before/after root file counts, deleted/archived byte counts, retargeted generator counts, repaired active-context stale-claim counts, runtime-dirt classifications, and current-HEAD large/LFS status.

Run and record:

- `py_compile` for changed scripts;
- cleanup/staleness guardrail checks present on disk;
- prompt hardening validator for this prompt;
- root cleanliness check proving root no longer has stale unclassified files;
- broken reference check for deleted/moved root files;
- `git diff --check` scoped to cleanup-owned files;
- `git status --short` classification proving the commit scope is cleanup-owned and runtime/live raw churn is not blindly staged.

## Loop Control

No infinite ledger, review, or retest loop. A ledger row is useful only when it drives deletion, archive, current-context repair, generator retargeting, runtime-dirt policy, verifier evidence, or exact blocker proof. Equivalent reruns after the same inputs and same outputs are not progress. When a verifier passes, move to the next unclosed cleanup target. When a verifier fails, patch the failing cause and rerun the affected check.

No arbitrary top-N, top 3/5/10, representative-only, sample-only, or extension-only cutoff is allowed for root files, stale claims, generated outputs, runtime dirt classes, or references. Ranked summaries are allowed after full ledgers preserve all material rows.

Full same-evidence-class pursuit is required. Literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available from the approved source class has been attempted or proven inapplicable with row-level source proof.

There is no conservative brake inside this cleanup evidence class. Execute deletion/archive/retarget/repair decisions when disk evidence proves stale pollution, duplicate context, generated junk, or old-system drift.

External action boundary: live trading broker operation, paid API/vendor spending, credential changes, remote push, and repository history rewrite are outside this task. Reading broker/account/order/history/deal/position evidence is allowed when needed to classify current truth. Prompt/config/risk/execution/safety/canary/selector files are in scope for cleanup-context repair and stale-reference removal; behavior changes belong to the next-level vNext lanes.

## Commit

Create a scoped commit after verification passes. Suggested message:

`chore: close root context cleanup for vnext lanes`

Include:

- this cleanup closure prompt and starter;
- next-level prompt pack files or committed-status proof;
- root deletions/archives and archive pointers;
- generator retargeting script fixes;
- active context/read-order/current-truth repairs;
- cleanup route state update;
- runtime dirt policy artifacts;
- verification artifacts.

Stage raw runtime/live churn only through a curated summary, policy, manifest, archive pointer, or deletion decision produced by this route.

## Completion Standard

This route is complete when:

- every root file is classified;
- stale root files are deleted or archived with pointers;
- root-writing legacy generators are retargeted;
- active context points to current vNext truth;
- next-level prompt pack is preserved as active launch material;
- raw runtime/live dirt is classified without blanket commit;
- current-HEAD large/LFS status is recorded;
- verification passes;
- cleanup state says the root physical cleanup invariant is closed;
- a scoped commit exists.
