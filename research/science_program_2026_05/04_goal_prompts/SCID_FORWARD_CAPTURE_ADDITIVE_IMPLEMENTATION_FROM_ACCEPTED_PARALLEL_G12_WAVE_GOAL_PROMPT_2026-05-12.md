# Goal: SCID Forward Capture Additive Implementation From Accepted Parallel G12 Wave

You are in `C:\Users\MSI\Documents\ai-trading-agent`. Execute the full goal to completion. Do not stop early because a field is missing if you can build additive capture, parser, fixture, verifier, or fail-closed status support inside this repo. Speed is irrelevant; correctness, completeness, and maximum useful implementation progress matter.

Owner approval/access is granted for everything inside the allowed additive evidence-capture implementation scope in this prompt: repo-local reads, absolute local source searches, scoped code/artifact/test edits, additive live-code capture wiring, forward-data/shadow-log schema or append-only capture changes, read-only broker/order/deal/history/account state reads when needed for source/lifecycle evidence with redaction and no performance scoring, controlled restarts needed to load additive capture/logging changes, local verifier/test execution, local copy/read operations needed for evidence binding, and shell escalations needed to complete those tasks. Do not pause for generic approval inside that scope. If the tooling asks for permission, request it immediately and continue after approval. Ask only if the work would require order/risk/safety/prompt decision behavior changes, paid/vendor access, credentials, destructive filesystem operations, or any change that can place/modify/cancel trades.

## Required Preflight

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/goal_session_research_discipline.md`.
4. Read `.context/00_core/research_operating_doctrine.md`.
5. Read `.context/00_core/research_current_state.md`.
6. Read `.context/00_core/local_heavy_data_inventory.md`.
7. Read `.context/00_core/ai_in_loop_cost_control_research_plan.md`.
8. Skim `.context/00_READING_ORDER.md` and any directly relevant Tier 2-4 files it points to if they affect capture, prompt hardening, or research-state interpretation.
9. Read the integration ledger:
   - `research/science_program_2026_05/06_outcome_testing/g0_scid_parallel_g12_integration_orchestration/G0_SCID_PARALLEL_G12_INTEGRATION_DECISION_LEDGER_2026-05-12.md`
   - `research/science_program_2026_05/06_outcome_testing/g0_scid_parallel_g12_integration_orchestration/G0_SCID_PARALLEL_G12_INTEGRATION_DECISION_LEDGER_2026-05-12.json`
10. Run `git status --short` and record a scoped dirty-state ledger before editing. Treat live/runtime/shadow dirt as expected context, not as a reason to stop, and never stage unrelated dirt.

## Mandatory Context Use And Evidence Class

This is a builder/evidence-capture/additive-implementation lane. It is not a G12 audit, not a G0 route selection, not validation execution, not promotion, and not result/performance scoring.

The context files are active instructions, not background reading. Before building, write a prompt-local context anchor in the route package that records:

- current `HEAD`;
- controlling prompt path;
- preflight files read;
- lane evidence class;
- builder posture applied;
- active question stack;
- searched-root plan;
- accepted input artifacts;
- implementation boundaries;
- exact stop conditions.

Apply `.context/00_core/goal_session_research_discipline.md` and `.context/00_core/research_operating_doctrine.md` directly:

- builder posture is output-maximizing and aggressively constructive inside additive evidence capture;
- examples, current GTOS frameworks, current OB edge, current timeframes, current instruments, current data modality, and accepted artifact lists are starting points, not limits;
- "source-safe" means auditable, no-leak, as-of-bound, redacted, hard-boundary-compliant, and machine-checkable. It does not mean narrow, timid, OB-only, historical-only, worktree-only, or field-minimal;
- creativity and breadth are required, but imaginative ideas must be converted into capture fields, statuses, fixtures, ledgers, or future preregistered hypotheses rather than performance claims;
- do not import G12 skepticism into this builder lane in a way that suppresses useful capture outputs. G12 can later reject weak evidence; this route must build the strongest auditable capture substrate possible.

The final completion audit must include an instruction-coverage checklist answering:

- whether `goal_session_research_discipline.md`, `research_operating_doctrine.md`, `research_current_state.md`, `local_heavy_data_inventory.md`, and `ai_in_loop_cost_control_research_plan.md` were read after preflight;
- how this lane applied builder posture, anti-boxing, creative breadth, and proof-or-impossibility;
- which outside-current-edge mechanisms, source paths, capture groups, and hypothesis-card families were considered;
- which doctrine requirements were deliberately not answered because they cross into validation, promotion, paid/API spend, credentials/remotes, order behavior, risk/safety, or another evidence-class gate.

## Operating Posture

Operate at maximum useful capability. Be curious, creative, analytical, and implementation-hungry. Do not behave conservatively by default. Do not narrow the work to the old OB edge, existing GTOS habits, easiest fields, or familiar mechanisms. Do not suppress a useful capture path, source join, proxy field, hypothesis-card support, or runtime fixture because it might later fail validation; capture it with provenance and let the later G12/result/audit lanes filter it.

The job is to make future research and edge discovery materially stronger. That means opening every auditable additive evidence door available through repo artifacts, live/shadow/runtime sources, local-heavy-data roots, accepted route artifacts, and approved read-only source routes, not producing a minimal pass. A weak completion is one that technically passes while leaving feasible capture, parser, harness, readonly, LTF/orderflow, broker-read, restart, or hypothesis-card work unexplored.

No checkbox completion. Do not merely assert that files exist or that requirements were "covered." For every major claim, read the relevant artifact/code, implement or repair the actual path, run the relevant verifier/test, and record the command/result/evidence file. If a checklist item is marked PASS, it must correspond to real inspected behavior or a reproducible verification result.

No shortcuts for speed. Do not choose a smaller, shallower, summary-only, compact-only, or partial implementation because the full route is slow. If the complete implementation takes hours, take hours. Use checkpoints and progress ledgers if needed, but finish the real work to the highest standard available inside scope.

Do not rely on chat memory, pasted closeouts, or old handoff summaries as evidence. Use committed files and live regenerated context. If conversation compaction, session interruption, or uncertainty occurs, regenerate `LIVE_STATE`, re-read this controlling prompt and the core context docs from disk, re-read the route context anchor/latest artifacts, and continue from the committed or checkpointed state.

## Data And Source Pursuit

Worktree absence is not data absence. Before declaring any source/capture/data blocker, search and record the relevant absolute local roots and prior artifacts, including when applicable:

- repo worktree artifacts under `research/`, `scripts/`, `tests/`, `src/`, `knowledge_base/`, `pipeline_state/`, and `shadow_logs/`;
- local heavy data roots documented in `.context/00_core/local_heavy_data_inventory.md`;
- ignored or external roots such as `data/ticks/`, `data/mt5_research_exports/`, Sierra/MT5 local data roots, `C:\tmp`, and prior worktree/cache roots referenced by accepted artifacts;
- existing source-control ledgers, manifests, row hashes, raw/cached capture folders, and `git log` / `git show` history for accepted lanes.

If source evidence is missing, escalate through the approved acquisition ladder inside this prompt before stopping: local artifact search, absolute-root search, existing logger/shadow/runtime source, read-only extraction/copy route, controlled parser/fixture repair, controlled restart for additive capture activation, and read-only redacted broker/order/deal/history/account state capture when source/lifecycle evidence needs it. A final blocker must name the exact missing field/source/window/parser/logger/approval route and every searched path or extraction route.

Do not use broad paid AI/API/vendor calls for this route. The default is no-API additive capture implementation. If a webpage/vendor document/current public source is needed for source-contract evidence, use local cache first; if unavailable and allowed, request the exact fetch permission, save the raw capture/source index, and continue. Paid/vendor/Databento spend still requires a separate explicit approval gate.

## Accepted Inputs

Read the committed artifacts, not chat summaries:

- Live hardening G12:
  - `research/science_program_2026_05/06_outcome_testing/g12_live_forward_evidence_capture_hardening_audit/`
- SCID implementation design and G12:
  - `research/science_program_2026_05/06_outcome_testing/scid_forward_capture_implementation_design_package_from_offline_schema_synthesis/`
  - `research/science_program_2026_05/06_outcome_testing/g12_scid_forward_capture_implementation_design_package_audit/`
- Synthetic runtime harness and G12:
  - `research/science_program_2026_05/06_outcome_testing/scid_capture_schema_to_runtime_test_harness_synthetic_only/`
  - `research/science_program_2026_05/06_outcome_testing/g12_scid_capture_schema_to_runtime_test_harness_synthetic_only_audit/`
- Readonly monitoring alignment and G12:
  - `research/science_program_2026_05/06_outcome_testing/scid_forward_capture_readonly_monitoring_alignment_expansion/`
  - `research/science_program_2026_05/06_outcome_testing/g12_scid_forward_capture_readonly_monitoring_alignment_expansion_audit/`
- No-API hypothesis factory and G12:
  - `research/science_program_2026_05/06_outcome_testing/scid_no_api_mechanical_hypothesis_factory_from_offline_schema_synthesis/`
  - `research/science_program_2026_05/06_outcome_testing/g12_scid_no_api_mechanical_hypothesis_factory_audit/`
- LTF/orderflow/proxy expansion and G12:
  - `research/science_program_2026_05/06_outcome_testing/scid_ltf_orderflow_proxy_source_expansion_from_offline_schema_synthesis/`
  - `research/science_program_2026_05/06_outcome_testing/g12_scid_ltf_proxy_audit/`

## Objective

Implement the accepted SCID forward-capture design as additive evidence-capture infrastructure. The implementation must support the ten accepted capture groups, preserve the 3,014 candidate boundary, use the synthetic harness, align with readonly monitoring, and keep the 40-card hypothesis factory plus LTF/orderflow/proxy needs as downstream consumers.

This is not a paper-plan route. If the accepted design contains proposed patches or insertion ledgers, use them. If they are incomplete, repair them inline. If a runtime adapter, parser, redaction guard, duplicate policy, as-of binding, fixture, verifier, or monitoring join is missing, build it instead of declaring a blocker.

This route must not collapse back into existing OB-only thinking. The no-API hypothesis factory is a first-class consumer: every one of the 40 accepted hypothesis cards must be mapped to implemented fields, already-existing fields, fail-closed runtime statuses, or concrete future-capture fields. Do not discard non-OB cards because they are unfamiliar, noisy, incomplete, or not immediately tradable.

Actively ask what the additive capture package must preserve for geometry/topology, path shape, stochastic/tail behavior, first-passage/hazard timing, auction/microstructure/orderflow, liquidity/trapped-trader mechanisms, behavioral/session/regime/macro context, execution quality, model disagreement, uncertainty, and non-OB GTOS mechanisms. Convert useful answers into fields, status vocabularies, fixtures, manifests, or future capture requirements.

## Implementation Scope

Allowed and expected when needed:

- Additive changes under `src/research_infra/`.
- Additive live-code capture callsite changes under `src/components/` when needed to write source/forward/shadow evidence and when they do not alter trading decisions, risk, selector, safety, AI prompt, or order behavior.
- Additive research/logging scripts under `scripts/`.
- Additive forward-data/shadow-log schema/capture/backfill changes when append-only or explicitly snapshot-preserving.
- Read-only broker/order/deal/history/account state capture when needed to record source/lifecycle evidence, provided it is redacted, source-status tagged, non-scoring, and cannot place/modify/cancel trades.
- Controlled process restarts when needed to load additive capture/logging changes. Restart only the affected process group, record pre/post health, verify rows land after restart, and do not flatten/cancel/place/modify trades.
- Additive tests under `tests/`.
- Source/control artifacts under `research/science_program_2026_05/06_outcome_testing/scid_forward_capture_additive_implementation_from_parallel_g12_wave/`.
- A next independent G12 audit prompt under `research/science_program_2026_05/04_goal_prompts/`.
- Updates to `.context/00_core/research_current_state.md` and regenerated `.context/LIVE_STATE.md`.

Do not change trading decisions, order behavior, risk settings, safety gates, production AI prompt behavior, canary thresholds, selector behavior, credentials, paid/vendor access, or non-capture live behavior. Controlled read-only broker/order/deal/history/account state capture and controlled restarts for additive evidence capture are allowed and approved when needed.

These boundaries are not excuses for inactivity. Within additive evidence capture, implement everything feasible, including live capture wiring and forward/shadow-log capture changes when they are needed and do not affect decisions or execution. If a requested value cannot be captured directly, emit the strongest source status available: exact captured value, derived/proxy value with provenance, unavailable-source status, redacted/forbidden status, or fail-closed not-computable status. The implementation must make downstream replay/research more informed even when a field remains unavailable.

## Aggressive Completion Standard

You must pursue all implementation doors inside the allowed scope:

- Implement every feasible accepted capture group, not only the easiest one.
- Preserve breadth and novelty. The 40 no-API hypothesis cards and 8 science domains are not decorative; they must actively shape what fields, statuses, fixtures, and downstream compatibility checks are implemented.
- Run and extend the synthetic harness until positive, negative, fail-closed, unavailable-source, duplicate, redaction, manifest-binding, and as-of cases are covered.
- Wire readonly source/status collection where it is additive and has no decision/execution impact.
- Preserve source-safe fail-closed statuses for fields that cannot be emitted at runtime.
- Convert missing recoverable items into concrete implementation tasks and complete them in this goal.
- A blocker is valid only after exhausting repo-local implementation, accepted proposed patches, existing live/shadow/runtime sources, synthetic harness extension, readonly alignment sources, LTF/orderflow/proxy sources, parser repair, fixture repair, and verifier repair inside the allowed scope.
- Stop only for true impossibility or a required change outside the additive evidence-capture scope.
- If a controlled restart is needed to make additive evidence capture active, do it and verify it. If a non-additive live behavior change would be needed, emit the exact owner approval gate and keep the implementation additive.
- If read-only broker/order/deal/history/account state is needed to close a lifecycle/source-state gap, capture it with redaction and provenance instead of leaving the gap open.
- If a route generates new questions inside additive evidence capture, pursue them now rather than emitting a vague future-work line. Split only when the next action crosses into independent G12 acceptance, G0 synthesis, result scoring, validation, promotion, paid/API spend, credentials/remotes, or live trading behavior.

## Saturation And Self-Red-Team Pass

Before completion, run a written saturation pass and pursue any same-evidence-class gap it exposes. The pass must be specific to this implementation and answer at least:

- What exact mistake would let source/control capture be mistaken for validation, result scoring, R/PnL, win-rate, expectancy, or promotion?
- Which fields look harmless but are actually post-outcome, post-cancel, broker-realized, hidden-label, path-label, or future-context fields, and how are they redacted, status-tagged, or blocked?
- Which duplicate-key, canonical-row, projection, or denominator choice could double-count the 3,014 candidates or the downstream 40 hypothesis cards?
- Which accepted capture groups remain only partially wired, and what same-scope implementation, parser, fixture, readonly source, restart, or broker-read action would close them?
- Which local heavy-data roots, prior worktrees, shadow logs, source contracts, code paths, or cached source captures could materially improve this implementation, and were they searched or exactly ruled out?
- Which non-OB, non-current-GTOS, LTF/orderflow/proxy, execution-quality, session/regime, or model-disagreement mechanisms did the implementation preserve for future testing?
- What would a fair G12 audit recompute or reject, and how does this builder preempt concrete source/no-leak/manifest/verifier failures without becoming timid?
- If a future larger cohort enters this lane, what exact source fields, as-of rules, no-leak gates, duplicate policy, sample-floor rule, and capture status vocabulary must exist before it enters the same denominator?
- What did this route deliberately not answer because it crosses into validation, G12 acceptance, G0 synthesis, result scoring, paid/API spend, credentials/remotes, order behavior, risk/safety, or promotion?

## Required Outputs

Create and commit a complete route package containing:

- Context anchor with active question stack and instruction-coverage checklist.
- Context/input inventory.
- Implementation decision ledger.
- Capture-group implementation matrix.
- Runtime adapter/parser/redaction/as-of binding ledger.
- Searched-root/acquisition ladder ledger.
- Synthetic harness compatibility result.
- Readonly monitoring alignment result.
- LTF/orderflow/proxy downstream compatibility result.
- Hypothesis-factory downstream compatibility result covering all 40 cards and all 8 science domains.
- Ambiguity, negative-evidence, and exact-blocker ledger.
- Saturation/self-red-team ledger.
- No-leak/forbidden-surface audit.
- Scoped dirty-state ledger proving unrelated live/runtime/shadow dirt was not staged.
- Restart ledger if any process restart was performed, including pre/post health and row-landing verification.
- Broker-read ledger if read-only broker/order/deal/history/account state was captured, including redaction proof and proof that no order placement/modification/cancellation path was opened.
- Verifier and focused tests.
- Completion audit with `can_mark_goal_complete=true` only if the implementation is complete.
- Next G12 audit prompt and one-line starter message.

## Verification

At minimum run:

- Focused tests for the new implementation.
- Existing forward-capture/shadow integrity tests affected by the implementation.
- The synthetic runtime harness or any extended fixture suite touched by the implementation.
- The new route verifier.
- `python scripts/generate_live_state.py` at closeout.

If Windows cache/temp/path friction blocks normal `py_compile` or pytest cache writes, use explicit `--basetemp`, disabled cache provider, AST parse, or explicit `.pyc` output fallbacks and record the fallback. Do not treat environment friction as a code blocker if syntax/tests can be verified cleanly by an equivalent no-bytecode or controlled-temp method.

Commit all scoped artifacts and code changes. Leave unrelated runtime/shadow dirt unstaged and documented.

Before committing, inspect `git diff --cached --name-only` and `git status --short`; do not use broad `git add .`, autostash, reset, or checkout against unrelated live/runtime/shadow dirt. Stage only scoped files for this implementation route, context refresh, tests, and accepted additive source/capture code changes.

Final safe flags must remain:

- `NO_PROMOTION_VERDICT`
- `validation_safe=false`
- `outcome_review_opened=false`
- `live_effect=false`

Do not call the goal complete unless the implementation, tests, artifacts, prompt pack, context refresh, and dirty-state ledger are all done.
