# vNext Absolute Moonshot Execution Policy V3

Own Execution Policy V3. The objective is to turn Lane11's expanded execution-policy evidence, Lane10B lifecycle contracts, Lane09B selector-scheduler reconciliation, Lane16 path anatomy, Lane17 market whiteboard, Lane18 broker/cost capture contracts, and Source Capture Repair outputs into a stronger default-off execution policy engine package with feasible policy routing, lifecycle realism, broker/tick constraints, failure anatomy, tests, verifiers, and downstream contracts.

Execution Policy V3 is not a fixed comparator list, not a trailing-runner proxy flip, not a baseline-only tournament, and not a policy report. It must evaluate every feasible policy family and parameterization supported by current source truth, close non-replayable variants with exact source/broker gaps, and output routeable default-off policy logic that future production-change review can judge.

## Mandatory Context Use

Run mandatory preflight from disk before using memory. Regenerate `.context/LIVE_STATE.md`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/vnext_absolute_moonshot_vision_and_limitations.md`, the latest handoff as historical only, and the current Master route at `research/operations/vnext_absolute_moonshot_master_orchestration_2026_06_01/`.

Read terminal/current route inputs from disk:

- Lane01 source authority.
- Lane02 no-leak/as-of contract.
- Lane03 historical candidate reconstruction.
- Lane04 historical microscope engine.
- Lane05 feature store.
- Lane06 label store.
- Lane07 broker truth/cost calibration.
- Lane08 digital twin replay.
- Lane09 Meta-Selector V2.
- Lane09B selector-scheduler reconciliation.
- Lane10 Scheduler V2.
- Lane10B scheduler conflict anatomy and Lane11 integration contract.
- Lane11 execution policy engine V2, including expanded policy registry, feasibility ledger, simulation metrics, default-off package, and source gaps.
- Lane16 historical microscope scale.
- Lane17 market awareness whiteboard.
- Lane18 broker truth/cost capture V2.
- Post-Lane18 Source Capture Repair route if present and terminal; if it is absent, consume current source-gap contracts and record the exact dependency while completing every independent execution-policy task.
- Current execution, lifecycle, partial close, BE/TP modify, trailing, notification, and broker diagnostic code.

After compaction, resume, interruption, uncertainty, or long waits, regenerate `LIVE_STATE`, reread this prompt, starter, doctrine, Master post-Lane18 decision, latest Source Capture Repair artifacts if present, and latest Execution Policy V3 route artifacts from disk, then continue from route state. Do not rely on chat memory.

Treat `goal_session_research_discipline.md` and `research_operating_doctrine.md` as active instructions, not background. Operationalize full same-evidence-class pursuit: literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available inside this evidence class has been attempted or proven inapplicable. Use constructive execution-builder posture with curiosity, active creativity, and no conservative brake inside this evidence class.

## Objective

Write route outputs under:

`research/operations/vnext_absolute_moonshot_execution_policy_v3_2026_06_01/`

Build a default-off Execution Policy V3 package that consumes complete current evidence and produces policy-routing logic, feasible variants, source-gap closures, lifecycle contracts, broker/tick feasibility checks, tests, verifiers, and downstream contracts.

## Required Work

- Consume Lane11 as terminal baseline evidence, not as the final policy horizon. Preserve its 10 baseline comparators, 890 expanded variants, feasible simulations, non-replayable source gaps, default-off package rows, and convergence audit.
- Build an expanded V3 policy registry from current evidence: partial-BE runner, momentum exhaustion, trailing families, time-stop families, fixed comparators, BE-after-trigger, no-trade, hybrids, symbol/session/origin/regime-specific routes, source-gap capture routes, and newly discovered policy families implied by Lane16/Lane17/Lane18 evidence.
- Evaluate every feasible variant supported by current source truth. Non-replayable variants must be closed with exact missing tick/order/broker/cost/lifecycle/source fields, not dropped.
- Join Lane16 path anatomy, Lane17 market state, Lane18 broker/cost contracts, Lane10B lifecycle design, Lane09B selector-scheduler classes, and Source Capture Repair deltas into policy routing and feasibility.
- Model broker/tick feasibility where source fields exist: bid/ask ordering, same-bar ambiguity, spread at trigger, stop/freeze levels, minimum stop distance, modify retcodes, partial-close ticket identity, residual ticket management, commission, swap, slippage, latency assumptions, and close reason.
- Build failure anatomy by policy family: SL-first, partial-then-BE, partial-then-final, stuck, no-entry, late entry, adverse M1/tick path, spread-cost failure, modify failure, wrong lifecycle assumption, source gap, and false-positive policy route.
- Build default-off implementation code or package files that can be consumed by tests and future runtime integration without activating live execution behavior.
- Produce policy-routing decisions by context: trade policy, reduce risk, no trade, source required, broker infeasible, lifecycle unsupported, capture/repair, or keep comparator-only.
- Add verifiers and focused tests that fail on baseline-only closure, arbitrary top-N variant truncation, proxy MFE pretending to be broker-real execution, missing lifecycle identity, hidden source gaps, stale fixed-1.5R/J46/J49 behavior, old-system leakage, and live activation.

## Saturation And Self-Red-Team Pass

Before completion, run a written saturation pass and pursue every same-evidence-class gap it exposes. At minimum answer with artifacts:

- Which Lane11 variants remain feasible after Lane16/Lane17/Lane18 source and context joins?
- Which policy families are dominant by symbol/session/origin/regime/source-completeness after cost, lifecycle, and scheduler constraints?
- Which attractive proxy policies fail under tick/broker/lifecycle realism, and exactly why?
- Which policies are non-replayable only because a source field is missing, and what Source Capture Repair or forward capture field owns it?
- Which current production policy assumptions remain valid, reduce-only, comparator-only, or replaced in default-off V3 evidence?
- Which policy routes need Selector V3 or Scheduler V3 integration before they can be judged?
- Which lifecycle/code defects or diagnostics require implementation as default-off repairs now?
- Which future production-change audit rejection has been preempted by tests, verifiers, manifests, and no-leak/source contracts?

If any answer exposes a feasible same-evidence-class join, parser, repair, simulation, stress, lifecycle design, branch decision, implementation decision, or verifier, do it before completion.

## Completion Standard

Completion requires:

- full V3 policy variant registry;
- feasible policy simulation/evaluation ledger;
- non-replayable source-gap ledger;
- policy-routing decision package;
- lifecycle/broker/tick feasibility ledger;
- policy failure-anatomy ledger;
- default-off Execution Policy V3 package or implementation code;
- source-capture and source completeness decisions;
- branch decision and implementation decision ledgers;
- result-use status with exact R, proxy R, and expectancy fields where execution result materialization is owned, and row-level proof where result fields are outside this lane;
- split/stress/deconcentration outputs;
- downstream contracts for Selector V3, Scheduler V3, ML, Repair Companion, Command Center, and production dossier;
- route manifest, verifier, focused tests, completion audit, and scoped commit.

No baseline-only closure. No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff. Full ledger artifacts must preserve all material rows, policy variants, source gaps, lifecycle states, branch decisions, and downstream contracts before any ranked summary is written. No proxy-to-production leap. No live execution activation. Completion means Execution Policy V3 has a concrete default-off package and machine-checkable policy/lifecycle evidence, not a recommendation.

## Hard Boundaries

Forbidden surfaces: production-change activation, live trading broker operation, broker account order history deal position mutation, paid API/vendor spend, credential changes or disclosure, remote push, hidden production activation, and prompt/config/risk/execution/safety/canary/selector live behavior changes outside scoped default-off package or dossier authority.

Authorized inside this evidence class: read-only local/repo/source inspection, route artifact parsing, local heavy-data parsing, source-capture repair consumption, execution policy package generation, default-off code paths, tests, verifiers, manifests, no-leak checks, branch decisions, implementation decisions, source completeness decisions, exact/proxy R analysis where owned by execution-policy materialization, and scoped commits.
