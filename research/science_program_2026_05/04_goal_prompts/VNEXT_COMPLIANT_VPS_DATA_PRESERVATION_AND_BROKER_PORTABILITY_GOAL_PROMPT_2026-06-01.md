# vNext Compliant VPS Data Preservation And Broker Portability Goal Prompt - 2026-06-01

## Objective

Build the complete vNext data-preservation, compliant-VPS migration, and broker-portability package from current disk evidence. Preserve every useful local data surface needed for the next absolute-moonshot research phase, produce exact missing-source/export requirements for data not already local, build the VPS migration contract and verifiers for the current 24-symbol vNext system, and commit the route-owned artifacts when every invariant below is closed.

This route is not a live-trading restart route. It is a data-preservation, source-inventory, broker-portability, compliance-source, VPS-readiness, and migration-verification route. It may write scripts, verifiers, manifests, docs, route ledgers, and default-off runbooks. It must not place, modify, close, cancel, or manage broker orders or positions. It must not start the live trading runtime.

## Mandatory Context Use

Start from disk, not chat memory.

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/current_vnext_system_map.md`.
4. Read `.context/00_core/current_repo_reading_order.md`.
5. Read `.context/00_core/quick_reference_card.md`.
6. Read `.context/00_core/goal_session_research_discipline.md`.
7. Read `.context/00_core/research_operating_doctrine.md`.
8. Read `.context/00_core/repo_cleanup_and_staleness_policy.md`.
9. Read `.context/00_core/vnext_absolute_moonshot_vision_and_limitations.md`.
10. Read the current next-level lane package artifacts under `research/operations/vnext_next_level_master_orchestration_2026_05_31/` if present.
11. Read current live companion state and summaries under `research/operations/vnext_live_activation_active_repair_companion_2026_05_28/`.

After context compaction, interruption, shell crash, long wait, or uncertainty, regenerate `LIVE_STATE`, reread this prompt, reread the starter, reread the doctrine files above, reread the latest route anchor produced by this goal, and continue from route state on disk.

The completion audit must state that the required context files were read after preflight and must record the exact HEAD, prompt path, starter path, route directory, evidence class, forbidden surfaces, and source-use state.

## Current Identity And Compliance Facts To Preserve

Use these as owner-provided current facts unless current official evidence contradicts them:

- The owner is Tunisian and has residency/permanent address in Tunisia.
- The owner is temporarily traveling in Malaysia.
- Do not classify the owner as a Malaysia resident or Malaysia citizen unless current official identity/KYC evidence says that.
- Do not invent United States residency, United States citizenship, or United States broker eligibility.

Fetch and preserve current official redacted_account policy pages as source captures before finalizing compliance claims. Use official pages only for these claims:

- Restricted country list: `https://help.redacted_account.com/en/articles/8020080-does-redacted_account-have-any-country-restrictions`
- VPN/VPS policy: `https://help.redacted_account.com/en/articles/8223809-can-i-use-a-vpn-or-vps`
- Device/network policy: `https://help.redacted_account.com/en/articles/9351758-redacted_account-trading-device-and-network-policy`
- Terms of service: `https://redacted_account.com/terms-of-service`

Record URL, fetch timestamp, fetch status, saved raw path, hash, and exact extracted policy facts in `OFFICIAL_redacted_account_SOURCE_INDEX.json`.

The expected current policy facts to verify from official sources are:

- redacted_account restrictions are described for residents and citizens of listed restricted countries.
- Malaysia is listed as restricted; Tunisia is not expected to be listed in the current source.
- Network access must not use IP addresses originating from restricted countries.
- VPS use is permitted only under redacted_account's VPS rules, including private/dedicated VPS usage, no shared VPS, no broker-sponsored VPS, no manual trading through VPS, no restricted-country VPS IP, and the required VPS/EA account add-on or fee where applicable.
- For MetaQuotes/MT5 access, USA-based IP addresses are not allowed by the official redacted_account guidance. Do not select a United States VPS for MT5.
- Any false residency, false nationality, false identity, restricted-country IP, shared VPS, or concealment claim is a hard incompatibility with this route.

The route must frame VPS migration as compliant infrastructure tied to true Tunisia residency/KYC and a permitted non-restricted, non-US MT5 network origin. It must not frame VPS migration as hiding, bypassing, masking, evading, or concealing anything.

## Evidence Class

This is a local evidence preservation, compliance-source capture, broker-portability, migration-readiness, runbook, default-off script, and verification route.

Allowed:

- Local filesystem inventory, hashes, line counts, byte counts, schema summaries, source coverage maps, manifests, and route ledgers.
- Official redacted_account policy fetches for source capture and policy extraction.
- Default-off scripts for future compliant VPS export, startup verification, source inventory, and broker portability checks.
- Read-only parsing of existing local logs, route artifacts, configs, manifests, research outputs, data folders, local MT5-export files already present, and shadow/runtime evidence already on disk.
- Local tests, `py_compile`, JSON/JSONL parse checks, manifest hash checks, path-bound archive decisions, and scoped commits.

Forbidden:

- Live broker/order/deal/position actions.
- Starting or restarting the live trading runtime.
- Any MT5/redacted_account server connection from a restricted-country network origin.
- Any broker server request from the current local machine unless the route records explicit current network-origin compliance proof first.
- Paid API/vendor calls.
- Credential creation, credential change, credential printing, or secret exfiltration.
- Remote push.
- Any instruction or artifact that tells a future agent to conceal location, residency, identity, IP origin, device ownership, VPS sharing, or account control.

If a needed action crosses a forbidden surface, do not fake it. Produce the exact compliant next action, required proof, expected command, expected artifact, and verification rule.

## Required Route Directory

Create and maintain:

`research/operations/vnext_compliant_vps_data_preservation_broker_portability_2026_06_01/`

At minimum, build these artifacts:

- `ROUTE_CONTEXT_ANCHOR.json`
- `OFFICIAL_redacted_account_SOURCE_INDEX.json`
- `COMPLIANCE_FACT_LEDGER.jsonl`
- `LOCAL_EVIDENCE_INVENTORY.jsonl`
- `LOCAL_EVIDENCE_COVERAGE_SUMMARY.json`
- `DATA_SURFACE_RETENTION_DECISION_LEDGER.jsonl`
- `MISSING_SOURCE_EXPORT_REQUIREMENTS.jsonl`
- `VPS_MIGRATION_CONTRACT.md`
- `VPS_STARTUP_VERIFICATION_RUNBOOK.md`
- `BROKER_PORTABILITY_MAP.json`
- `BROKER_PORTABILITY_GAP_LEDGER.jsonl`
- `MT5_EXPORT_READINESS_CHECKLIST.json`
- `DEFAULT_OFF_EXPORT_SCRIPT_MANIFEST.json`
- `ABSOLUTE_MOONSHOT_DATA_REQUIREMENT_MAP.json`
- `OUTPUT_MANIFEST.json`
- `VERIFICATION_RESULT.json`
- `COMPLETION_AUDIT.json`

Use stronger names if the implementation needs more precise artifacts. Do not replace required artifacts with chat-only statements.

## Local Evidence Inventory Scope

Inventory every relevant local data surface, not a sample and not a top-N list. Include path, type, bytes, modified time, tracked/untracked status, hash or chunk-hash strategy, line count when practical, schema preview, symbol coverage when derivable, time coverage when derivable, retention class, and downstream use.

Cover at least:

- `data/ticks/`
- `data/m1/`
- any M5/M15/H1/H4/D1 local data roots present
- `shadow_logs/`
- `pipeline_state/`
- `knowledge_base/`
- `config/`
- `src/`
- `tests/`
- `.context/00_core/`
- current goal prompt files
- `research/operations/vnext_live_activation_active_repair_companion_2026_05_28/`
- `research/operations/vnext_friday_microscopic_live_forensics_moonshot_repair_2026_05_31/`
- `research/operations/vnext_next_level_master_orchestration_2026_05_31/`
- all Lane01 through Lane09 route outputs present under `research/operations/`
- activation repair-hardening route under `research/science_program_2026_05/06_outcome_testing/`
- current vNext route summaries, manifests, selected shards, replay summaries, broker lifecycle truth artifacts, and Friday microscope artifacts
- local MT5 terminal export/log folders if discoverable from repo config, pipeline state, or known Windows locations without credential disclosure

Do not rely on repo-relative paths only. Search the current repo and obvious local GTOS data roots. If an expected source is not local, record the exact missing source and future compliant export route.

## Data Preservation Target

The route must preserve the data needed to continue the absolute moonshot research program without depending on fragile live access:

- historical tick, M1, M15, higher-timeframe, spread, bid/ask, and symbol-spec evidence already local;
- vNext candidates, skips, rejects, placed orders, fills, partials, BE/TP modifications, closes, lifecycle events, Telegram events, cost/slippage/swap/commission evidence already local;
- Friday microscope and next-level lane outputs;
- current selector, risk, scheduler, packet, broker truth, execution, and microstructure artifacts;
- source gaps that require future compliant broker export or alternate broker/vendor data;
- reproducibility hashes and manifests so another machine can verify the evidence package;
- broker portability map so the system can run against a compliant VPS or future broker without stale redacted_account-specific assumptions leaking into generic code.

Do not compress or delete hot evidence during this route unless the file is proven stale/temp/cache and the cleanup policy allows it. If compression is useful but storage risk exists, write a compression plan and default-off script rather than forcing a risky operation.

## VPS Migration Contract

Build a concrete VPS migration contract for running the current vNext system from a permitted network origin. It must include:

- dedicated/private VPS requirement;
- stable IP requirement;
- country/IP requirement: non-restricted country and not United States for MT5/MetaQuotes access;
- no shared VPS;
- no broker-sponsored VPS;
- no manual trading through VPS;
- trade-taking EA/system requirement if VPS is used with redacted_account;
- account add-on/fee proof requirement if redacted_account requires it;
- owner identity truth: Tunisia residency/KYC, not Malaysia residency;
- support confirmation capture slot if the owner obtains written confirmation;
- local machine role while traveling: development/research/control only unless network-origin compliance is proven;
- VPS machine role: read-only export first, then dry-run verifier, then owner-approved live runtime start in a separate live-ops action.

Build `VPS_STARTUP_VERIFICATION_RUNBOOK.md` so another agent can verify, before any live runtime start:

- current HEAD and clean/scoped worktree status;
- public IP country and provider;
- IP is not restricted and is not United States for MT5;
- VPS is dedicated/private;
- required redacted_account VPS/EA add-on state is documented;
- MT5 terminal/account connection is read-only verified first;
- symbol list and broker aliases match the 24-symbol vNext surface;
- current config points to vNext/moonshot replacement, Lane03 package, Lane05 scheduler, Lane06 broker truth, Lane08 execution policy artifacts, and current package state;
- secrets exist without printing values;
- logs/data directories exist;
- data capture starts before live trading;
- dry-run/check mode passes;
- live broker actions remain blocked until the owner explicitly approves the separate runtime start.

## Default-Off Export And Verification Scripts

Add default-off scripts for the future compliant VPS workflow:

- source inventory script;
- MT5 symbol/spec/export readiness script;
- broker history export verifier;
- tick/M1/M15 capture health verifier;
- evidence package hash verifier;
- startup preflight verifier.

Scripts must not run broker-changing actions. If a script imports MT5, it must default to read-only checks and must require explicit command-line flags for server access. It must refuse to run from restricted-country or United States MT5 network origin if IP-origin proof is available and indicates an incompatible origin.

Add focused tests for every new parser, verifier, manifest writer, and network-origin classifier.

## Broker Portability Map

Build `BROKER_PORTABILITY_MAP.json` and `BROKER_PORTABILITY_GAP_LEDGER.jsonl` for future non-redacted_account or alternate challenge infrastructure. Include:

- canonical GTOS symbol;
- redacted_account broker-native symbol;
- asset class;
- contract size;
- tick size;
- tick value;
- point value;
- min lot;
- max lot;
- lot step;
- stops level;
- freeze level;
- sessions/hours if known;
- commission/swap availability;
- spread evidence availability;
- current data roots;
- missing broker-specific fields;
- migration risk;
- future broker mapping requirement.

Do not assume another broker has the same symbol names, contract geometry, sessions, costs, order fill model, or stop/freeze constraints.

## Subagent Use

Use high-reasoning subagents where available. Split them by artifact family, not by arbitrary small summaries:

- official redacted_account policy source capture and extraction;
- local hot-data inventory and coverage;
- current vNext route/lane artifact preservation map;
- broker portability and VPS runbook;
- missing-source/export requirements and verifier design.

Integrate subagent findings into route-owned artifacts. Close subagents after extracting their findings. Do not leave independent subagent conclusions as unmerged chat memory.

## Anti-Drift Rules

Do not turn this route into:

- a live trading restart;
- a generic legal memo;
- an argument about Malaysia residency after the owner-provided Tunisia fact is recorded;
- a chat-only checklist;
- a broad cleanup/deletion route;
- an infinite ledger loop;
- a repeated verifier loop that does not change artifacts;
- a remote push;
- a paid data pull.

Every ledger must feed a manifest, verifier, completion audit, runbook, data-preservation decision, export requirement, or code/test artifact.

## Verification Requirements

Run the strongest practical local checks:

- official source index exists and raw captures or fetch failure records are hashed;
- JSON/JSONL parse for route artifacts;
- output manifest hash/byte/line verification;
- local evidence inventory covers every required root or records exact absence;
- required route artifacts exist;
- no secret values are printed into artifacts;
- no broker-changing action happened;
- no live runtime start happened;
- no remote push happened;
- no paid API/vendor call happened;
- new scripts pass `py_compile`;
- focused tests pass for any new scripts/parsers/verifiers;
- `git diff --check` on scoped files passes.

If a check cannot run, record the exact command, failure, and whether the failure is environment friction, missing source, or a code defect.

## Completion Standard

Mark the goal complete only when:

- the official redacted_account source facts are captured, hashed, and summarized from official sources;
- the Tunisia residency/Malaysia temporary-travel distinction is preserved without false Malaysia-resident wording;
- the local evidence inventory covers every required local surface or records exact absence/source-export requirement;
- the moonshot data requirement map exists and ties preserved data to the next historical microscope, ML, replay, selector, scheduler, execution, broker truth, and command-center programs;
- the VPS migration contract and startup verification runbook are complete enough for another agent to execute on a compliant VPS without relying on chat memory;
- broker portability map and gap ledger exist;
- default-off future export scripts and verifiers exist and are referenced by `DEFAULT_OFF_EXPORT_SCRIPT_MANIFEST.json`;
- all route artifacts are in the route directory and inventoried in `OUTPUT_MANIFEST.json`;
- `VERIFICATION_RESULT.json` is `ok=true`;
- `COMPLETION_AUDIT.json` states `can_mark_goal_complete=true`;
- scoped files are committed with a commit message that includes `Co-Authored-By: Codex GPT-5 <redacted@example.com>`.

Do not mark complete with unresolved placeholders, vague future work, unsourced compliance claims, incomplete route manifests, missing inventory roots, or unmerged subagent findings.
