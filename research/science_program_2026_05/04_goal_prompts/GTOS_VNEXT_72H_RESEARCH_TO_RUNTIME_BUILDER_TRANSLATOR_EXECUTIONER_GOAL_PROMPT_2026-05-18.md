# GTOS_VNEXT_72H_RESEARCH_TO_RUNTIME_BUILDER_TRANSLATOR_EXECUTIONER

Date: 2026-05-18
Mode: main-worktree long goal session
Primary objective: `GTOS_VNEXT_72H_RESEARCH_TO_RUNTIME_BUILDER_TRANSLATOR_EXECUTIONER`

## Mission

Run on `main` for a full 72-hour wall-clock window. This is not another orchestrator, audit, synthesis, route planner, blocker mapper, or prompt factory. This is the builder, translator, and executioner for GTOS vNext: absorb the full research base already on disk and convert it into executable GTOS architecture, code, config, prompt/schema/parser changes, replay validators, strategy modules, scorer/filter/router modules, AI role changes, market/timeframe expansion machinery, and tests.

The product is a stronger trading system implementation. The product is not a better explanation of why a system might work. Research intelligence has already been harvested across moonshot, main, READY8, v2/v3/v4/cascade, primitive sciences, live/shadow friction, source-repair, no-fill, pending lifecycle, gates, filters, selectors, AI behavior, market expansion, and replay. This session must drink from that ocean and build the machine that uses it.

Primary performance truth is simulated historical replay R, exact replay R where complete replay geometry exists, proxy R where disk evidence defines source-bound geometry, gross/cost/stress expectancy, path ordering, duplicate/effective-N, concentration, pass/control deltas, branch/family/symbol/session/timeframe/horizon splits, and replayable implementation behavior. Do not chase owner R, broker-fill R, or sparse live/account realized R as the objective or blocker. Existing broker/account/order/history/deal/position files may be read only for execution-friction context and join diagnostics. Actual broker operations that place, close, or modify trades are outside this goal unless the owner gives a separate explicit operational command.

This prompt is explicit owner approval for repository production-change work when it is tied to disk evidence, replay/proxy-R results, tests, current code, production friction, or architecture conversion. The session may change any repo code, config, prompt, risk logic, execution-adjacent logic, safety/canary/test infrastructure, selector logic, AI prompt/schema/parser code, replay tooling, logging, validators, scripts, and system-design docs. Do not wait for separate approval for those repo edits. Paid API/vendor spend, credential changes, remote push, and actual broker operation require a separate explicit owner command. This boundary must not reduce implementation, computation, replay, expansion, AI architecture work, or code changes inside the repo.

## North Star

The north star is the strongest executable GTOS vNext that can be built from all accumulated intelligence. The system should become mechanical wherever mechanical/replay evidence beats the AI decision loop. AI/API is not sacred. Remove it from branches it weakens or slows, narrow it to exact ambiguity classes, turn it into explanation/geometry assistance only, harden it where it earns its role, or retain it only where replay/log/test evidence shows incremental value beyond deterministic scorers.

The session must not be boxed into READY8, CP281, OB retest, the current seven-symbol universe, a single timeframe, a single market family, a single entry style, a single exit style, a single branch family, or one recent checkpoint. Moonshot is the current replay/scoring substrate. Prior research is the intelligence library. vNext is the system that combines them.

Every checkpoint must follow this line:

1. reread active context and freeze the current evidence universe from disk;
2. identify the system surface affected by that evidence;
3. implement, replay, expand, remove, narrow, route, or harden the system behavior;
4. verify with tests, parsers, replay checks, or machine-readable ledgers;
5. commit scoped work on main;
6. immediately continue to the next highest-value system conversion plate.

If a task does not move code, config, prompt/schema, replay machinery, scorer/filter/router behavior, AI architecture, market/timeframe coverage, risk/exit/execution-adjacent behavior, or tests toward a stronger GTOS vNext, stop that task and choose a direct implementation task.

## Active Context And Reread Discipline

Mandatory start:

1. Run `py -3 scripts\generate_live_state.py` and read `.context/LIVE_STATE.md`.
2. Read `AGENTS.md`, `CLAUDE.md`, the latest handoff in `.context/02_session_handoffs/`, `.context/00_core/quick_reference_card.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/orchestrator_successor_operating_brief.md`, `.context/00_core/orchestrator_methodology_hardening_controls.md`, `.context/00_core/parallel_goal_merge_playbook.md`, `.context/00_core/research_current_state.md`, `.context/00_core/local_heavy_data_inventory.md`, `.context/00_core/ai_in_loop_cost_control_research_plan.md`, the route registry, cross-route question/ambiguity ledger, this prompt, and the starter.
3. Snapshot `` if present: HEAD, branch, status, latest checkpoint, committed/working-tree state, source paths, sizes, SHA256s, and artifact roles. Consume it read-only. Do not modify that worktree.
4. Read all current main-side moonshot/compiler artifacts, including SESSION_60 handoff and every `MAIN_ORCH48_*` artifact present at launch.
5. Read READY8 final artifacts, old v2/v3/v4/cascade results, phase syntheses, program-control blockers/unblockers, live/shadow/forward logs, strategy/no-fill/pending lifecycle logs, malformed AI output logs, current simulations, current production code, tests, config, prompts, and scripts before making claims.
6. Do not rely on chat memory or pasted closeouts.

After compaction, resume, interruption, uncertainty, long gap, checkpoint transition, or plate transition: regenerate `LIVE_STATE`, reread this prompt, the starter, doctrine, latest handoff, current-state doc, active artifact paths, and latest changed files before continuing. Record instruction-coverage evidence at every checkpoint.

Retired defensive wrapper language in older artifacts has no authority in this session and must not be copied into new active builders, result summaries, tests, verifiers, prompts, or reasoning. Do not rewrite old historical artifacts just to rename them. If old wording controls active behavior, patch the active generator/test/surface and consume that patch into implementation.

No conservative brake. No arbitrary top-N, top 3/5/10, number-limited cutoff, representative-only sample, or summary-only review. Preserve all material rows in full ledgers before summarizing. Proof-or-impossibility and same-evidence-class pursuit are mandatory, but proof rows are not the product. A proof row is acceptable only where computation, implementation, replay, extraction, source repair, or weaker source-bound proxy is genuinely exhausted for that row/source class.

## Evidence Universe To Absorb

Build and maintain a master evidence inventory. Consume every material row family, not only the newest CP checkpoint:

- moonshot CP203 through latest checkpoint, including CP213 performance matrix, CP214/CP215 surfaces/executions, CP216 expanded-market proxy-R, CP217 side-pair robustness, CP218 temporal robustness, CP223/CP224 reduced-surface work, CP280 full frozen universe, CP281 executable ready slice, CP282 handoff, and any later artifacts present at launch;
- moonshot counts and anchors: CP280 `3485` input artifacts, `6442524` JSONL rows scanned, `297622` action closure rows, `28474` market/timeframe/source coverage rows, `461` implementation-ready rows, `243649` repair-needed rows, `25811` kill/preserve rows; CP281 `461` runtime rule rows, `461` self-test pass rows, `107` aggregate scopes, `6428` represented source rows, `173` follow rules, `288` avoid filters; CP282 `27` artifact rows and `5` consumption-order rows;
- main `MAIN_ORCH48_*` artifacts, including CP281 ready runtime mapping, branch decisions, event availability, branch-scope registry, branch-scope replay events, rule replay execution, reduced-surface candidates/registry/contracts/validators, gate/filter/selector artifacts, AI trace/provenance/parser artifacts, and current code changes;
- READY8 final decision: use neutral movement, HAZ001, MAC, UNC004, HAZ005, failure intelligence, and killed claims as tags/features/priors/avoid/control intelligence only, not as standalone trade geometry;
- V2/V2B/V3/V4/cascade, prompt cascade, phase syntheses, J46-J49, S79, K54/K55, FVG/OB, breaker, liquidity sweep, no-fill, pending lifecycle, orderflow/Sierra, market expansion, exits, risk, session/regime/time, side-aware sizing, primitive-science hypotheses, and old blockers/unblockers;
- live/shadow/forward logs as production-friction, source-capture, behavior, and integration evidence, not as a reason to wait for sparse future samples;
- code and config in `src/`, `prompts/`, `config/`, `scripts/`, `tests/`, `shadow_logs/`, `knowledge_base/`, `.context/`, and research tooling.

## Master Build Contract

Emit and update a machine-readable evidence-to-system build matrix. It must not be a backlog. Every material evidence family, result ledger, branch family, implementation candidate, gate/filter/selector, AI component, market/timeframe candidate, primitive-science hypothesis, and production-friction class must end as one of:

- code implemented;
- config/prompt/schema/parser changed;
- risk/exit/execution-adjacent behavior changed;
- scorer/filter/router/selector implemented;
- replay/validator/test implemented;
- market/timeframe extraction or expansion implemented;
- AI removed, narrowed, hardened, or retained with a concrete code/config result;
- branch killed as a trade claim while useful inverse/avoid/filter/context intelligence is preserved in code or a callable surface;
- exact row-level source/access proof with the next executable owner inside the same 72h plan.

Each matrix row must include:

- source artifact paths, hashes, row counts, row ids, and source role;
- simulated exact/proxy replay R where computable, gross/cost/stress expectancy, win/loss/zero counts, average win/loss R, duplicate/effective-N, concentration, pass/control delta, and symbol/session/timeframe/horizon splits;
- current production touchpoint and file paths affected;
- implementation action and exact files changed;
- before/after behavior;
- tests/verifiers/parse checks;
- remaining row-level source proof only when direct implementation or computation is exhausted.

Summary prose alone is not progress. A checkpoint that only updates context, route ledgers, or wrappers is not enough unless it also ties directly to code/config/prompt/schema/replay/test implementation.

## Build Plates

These plates are all in scope. They are not a top-N list. Continue through them for the full 72-hour window, choosing the next plate by highest implementation value and evidence readiness.

### Plate A - vNext Runtime Foundation

Build the vNext runtime substrate inside the repo. Expected surfaces include an evidence store/loader, replay-backed branch-scope registry, scorer/filter/router interface, feature/tag resolver, event contract, decision logger, replay validator, and integration tests. It must consume CP281/reduced-surface/moonshot evidence and expose callable behavior that the current orchestrator/system can use. It must be designed so mechanical decisions can replace AI where geometry and replay evidence are complete.

### Plate B - Moonshot Replay To GTOS Code

Consume latest moonshot/main replay artifacts into GTOS code. Convert follow-rule and avoid-filter results, reduced-surface candidate rows, branch decisions, market/timeframe expansion rows, held replay rows, and kill/preserve/repair-needed rows into callable scorer modules, avoid filters, branch routers, replay validators, source contracts, event adapters, tests, and config surfaces. Preserve all rows and duplicate/conflict cases as router logic or explicit implementation decisions.

### Plate C - AI/API Decision Architecture

Implement the mechanical-first AI architecture. Use malformed logs, AI trace provenance, prompt hashes, parser failures, canary behavior, cost/latency, deterministic bugs, replay comparisons, and current code to decide and encode where AI is removed, narrowed, retained, or hardened. Do not make new paid API calls. Use existing artifacts, prompt fixtures, tests, local replay, and code inspection.

Concrete outcomes include: no-AI mechanical branches, AI-only-for-geometry branches, ambiguity-class routing, prompt/schema/parser hardening, strict response validators, cache/provenance controls, canary changes, and removal/narrowing of branches where AI adds noise or latency without value.

### Plate D - Gate, Filter, Selector, Risk, Exit, And Execution-Adjacent Upgrade

Inventory and modify every live/shadow gate/filter/selector/risk/exit/execution-adjacent surface where evidence demands it: touch count, L2 framework checks, pre-AI POI, confidence handling, SL buffer/tolerance, correlation, KZ/session windows, SPRT/CUSUM, side-aware risk, J46-J49 exits, no-fill/pending lifecycle, source-capture, candidate path ordering, framework dispatch, malformed-response handling, and symbol risk asymmetry. Quantify what each surface blocks, saves, costs, or misses in replay R; then keep, change, remove, narrow, invert, or convert it into a mechanical scorer/avoid/router with tests.

### Plate E - Market, Timeframe, And Primitive-Science Expansion

The current instrument list is not the boundary. Consume the moonshot 301-row/44-symbol and 1,145-row/47-symbol-label expansion artifacts and every later expanded-market checkpoint. Expand further when local, MT5, Sierra, historical, or free/public sources allow. Cover structure, liquidity, volatility, trend/momentum, mean reversion, session/calendar, spread/cost/fill, path ordering, intermarket context, regime, execution mechanics, exits, risk, position management, failure/avoid behavior, and any other source-backed primitive family that can improve GTOS. Output numeric rows, code, config, replay machinery, or exact source proof.

### Plate F - Legacy Research Merger

Merge old winners, losers, blockers, unblocked paths, and shelved paths into vNext. V3/cascade history, V4/LIRA failure, J46-J49, S79, K54/K55, FVG/OB, breaker, liquidity, no-fill, pending lifecycle, orderflow/Sierra, side-aware sizing, session/regime/time, and all LTO/program-control work must become code/config/prompt/schema/replay/test changes, features, avoid filters, routers, or explicit killed claims with preserved useful intelligence. Do not leave old research as archive prose when it can become behavior.

### Plate G - End-To-End System Assembly

Wire the pieces together into a coherent architecture: configuration, runtime interfaces, replay contracts, test fixtures, canary/parser coverage, decision logs, source-capture fields, and rollback clarity. The result should make it obvious how GTOS vNext will select a candidate, score/filter/route it, decide AI involvement, bind geometry, size risk, manage exits, and record evidence.

## Operational Discipline

Work on main. Do not create a branch or worktree. Commit scoped checkpoints on main. Do not stage unrelated runtime/shadow/generated dirt. Do not modify ``; consume it read-only.

Use subagents when they materially accelerate disjoint work: artifact extraction, code surface mapping, test review, AI architecture audit, market expansion inventory, or old-research mapping. Keep the critical implementation path local. Integrate results into code and tests instead of letting subagent summaries become the product.

Use existing local, MT5, Sierra, historical, disk, shadow, and free/public sources when needed. Do not skip extraction because a dataset is not already in the immediate folder. If source access is blocked, emit exact source/access proof and continue to the next implementation-ready path.

Do not restart trading runtime, watchdog, observer, scheduler, or place/close/modify orders unless separately commanded by the owner. Runtime is currently halted for research priority. This does not restrict repo implementation.

## Required Outputs

The session must produce, at minimum:

- `GTOS_VNEXT_EVIDENCE_TO_SYSTEM_BUILD_MATRIX_*.jsonl`
- `GTOS_VNEXT_RUNTIME_ARCHITECTURE_DECISION_LEDGER_*.jsonl`
- `GTOS_VNEXT_SCORER_FILTER_ROUTER_IMPLEMENTATION_LEDGER_*.jsonl`
- `GTOS_VNEXT_AI_DECISION_ARCHITECTURE_LEDGER_*.jsonl`
- `GTOS_VNEXT_GATE_FILTER_SELECTOR_RISK_EXIT_LEDGER_*.jsonl`
- `GTOS_VNEXT_MARKET_TIMEFRAME_EXPANSION_LEDGER_*.jsonl`
- `GTOS_VNEXT_LEGACY_RESEARCH_MERGER_LEDGER_*.jsonl`
- exact/proxy replay-R and expectancy summaries for every implemented branch family where computation is possible;
- code/config/prompt/schema/test changes with file paths and before/after behavior;
- JSON/JSONL parse checks, focused pytest or custom verifiers, and AST/compile checks for changed Python surfaces;
- completion audit and instruction-coverage ledger;
- scoped commits and context refresh.

File names may differ if the session uses established local route naming, but the information content above is mandatory.

## Completion Standard

Run the full 72-hour window. Do not finish early after first code change, first checkpoint, first replay table, first AI decision, first gate update, first market expansion, first source proof, first context refresh, or first commit. When one plate is complete, immediately move to the next highest-value implementation or replay plate.

Final closeout must include:

- total exact replay R rows, proxy R rows, non-computable rows, gross/cost/stress expectancy, and splits by branch/family/symbol/session/timeframe/horizon;
- implemented repo changes with file paths, tests, and before/after behavior;
- AI/API architecture after the session, with exact branches where AI was removed, narrowed, retained, or hardened;
- gates/filters/selectors/risk/exits/execution-adjacent surfaces kept, changed, removed, inverted, or converted;
- market/timeframe expansion performed and remaining exact source/access proof;
- old research merged, redesigned, converted to features/avoid/router logic, or killed as unsupported trade claims while preserving useful intelligence;
- remaining highest-value work that could not fit into 72 hours, with exact source and implementation paths.

Forbidden terminal products: summary-only output, audit-only output, prompt-only output, blocker-only output, source-capture-only output, waiting for live data, owner/broker R chasing, one-family tunnel vision, route laddering, ending because a single branch was handled, or ending before the 72-hour window.

## Launch

```powershell
cd C:\Users\MSI\Documents\ai-trading-agent
codex --enable goals -c model_reasoning_effort=xhigh
```

Paste the starter from:

`research/science_program_2026_05/04_goal_prompts/GTOS_VNEXT_72H_RESEARCH_TO_RUNTIME_BUILDER_TRANSLATOR_EXECUTIONER_STARTER_2026-05-18.txt`
