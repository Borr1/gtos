# MAIN_ORCHESTRATOR_36H_RESEARCH_TO_PRODUCTION_SYSTEM_UPGRADE_FROM_ALL_EVIDENCE

Date: 2026-05-18
Mode: main-worktree long goal session
Primary objective: `MAIN_ORCHESTRATOR_36H_RESEARCH_TO_PRODUCTION_SYSTEM_UPGRADE_FROM_ALL_EVIDENCE`

## Mission

Run on `main` for a full 36-hour wall-clock window. This is not another route ladder, audit chain, source-gap packet, capture-only packet, or research summary. This is the research-to-production system-upgrade session that consumes the evidence already created across GTOS, moonshot, READY8, v2/v3/v4/cascade, live/shadow, primitive-science, replay, and scorer work, then converts it into concrete system changes, scored result ledgers, implementation decisions, and verified code/config/prompt/tooling upgrades.

The primary scoring truth is simulated historical replay R, source-bound proxy R, and replayable expectancy. Do not chase live account realized R, account-owner R, or broker-fill R as the objective or as a blocker. If account/order/history/deal/position logs already exist on disk, use them only for execution-friction calibration, join diagnostics, or production-incident context. They must never replace the replay/backtest scoring objective.

The session must not be boxed into READY8, OB retest, the current seven-symbol universe, a single timeframe, a single market family, a single branch family, or a single prior hypothesis. It must use every material line of research that can alter the system: moonshot replay geometry, main 24h ledgers, READY8 merged intelligence, V2/V2B/V3/V4/cascade findings, J46-J49, S79, K54/K55, FVG/OB, breaker, liquidity sweep, no-fill, pending lifecycle, source repair, Sierra/MT5/history, orderflow/proxy signals, market expansion, session/regime/time/calendar effects, side-aware risk, exits, costs, spreads, path ordering, and AI decision-loop evidence.

Treat this prompt, starter, doctrine, current-state docs, latest handoff, and current artifacts as active instructions, not background. Operationalize them and record instruction-coverage evidence.

No conservative brake. No arbitrary top-N, top 3/5/10, representative-only sample, or number-limited cutoff. Preserve all material rows in full ledgers before summarizing. Proof-or-impossibility and same-evidence-class pursuit apply, but proof rows are not the product; they are only acceptable where computation or implementation is genuinely impossible after searching the relevant disk/source class.

Retired defensive wrapper language from older artifacts has no authority in this session and must not be emitted in new active builders, summaries, starters, result ledgers, verifiers, or reasoning. Do not spend time rewriting old historical artifacts only for wording cleanup. Patch active behavior and new outputs when the old language can steer current work away from computation or implementation.

## Mandatory Disk Preflight

1. Run `py -3 scripts\generate_live_state.py` and read `.context/LIVE_STATE.md`.
2. Read `AGENTS.md`, `CLAUDE.md`, the latest file in `.context/02_session_handoffs/`, `.context/00_core/quick_reference_card.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/orchestrator_successor_operating_brief.md`, `.context/00_core/orchestrator_methodology_hardening_controls.md`, `.context/00_core/parallel_goal_merge_playbook.md`, `.context/00_core/research_current_state.md`, `.context/00_core/local_heavy_data_inventory.md`, `.context/00_core/ai_in_loop_cost_control_research_plan.md`, the route registry, and the cross-route question/ambiguity ledger.
3. Snapshot `` if it exists: HEAD, branch, git status, consumed paths, sizes, SHA256 hashes, committed vs working-tree-only status, and latest checkpoint/result files. Consume at least CP210 if present and every later checkpoint present at launch. Do not modify that worktree from this main session.
4. Read main 24h completion artifacts, READY8 final-decision artifacts, moonshot artifacts, old research outputs, live/shadow/forward logs, strategy/no-fill/pending lifecycle logs, malformed AI output logs, existing simulations, and current production code before making claims.
5. Do not rely on chat memory or pasted closeouts. After compaction, resume, interruption, uncertainty, or long gap, regenerate `LIVE_STATE` and reread this prompt, the starter, doctrine, latest handoff, current-state doc, moonshot snapshot paths, and latest artifacts before continuing.

## Evidence Universe

Build a complete evidence inventory before and during implementation. Include all material artifacts and code surfaces, not only the files that are easiest to parse:

- moonshot CP203+ through latest checkpoint, including runtime replay geometry, executable code surfaces, held replay rows, market gaps, repaired proxy execution registry, M1/M15/tick/OHLC/Sierra path and cost artifacts;
- main 24h result materialization ledgers, source-capture patches, scorer patches, live/shadow gap closure, implementation decision ledgers, and tests;
- READY8 final decision and its neutral/failure/HAZ001/MAC/UNC004/HAZ005 intelligence as tags, priors, avoid/control labels, or context features only;
- V2/V2B/V3/V4/cascade and prompt-cascade research, including why V3 was empirically stronger and why V4/LIRA were shelved;
- J46-J49 portfolio policy, S79 risk policy, K54/K55 ML/filter work, FVG/OB exact-countable replay, no-fill near-miss, pending lifecycle, orderflow, depth/Sierra, spread/cost, source-repair, and expansion blockers;
- live/shadow/forward logs as production-friction and behavior evidence, not as a reason to wait for sparse forward samples;
- code and config in `src/`, `prompts/`, `config/`, `scripts/`, `tests/`, `shadow_logs/`, `knowledge_base/`, and research tooling.

## Master Output Contract

Produce a master evidence-to-action matrix with one row for every material research family, result ledger, branch family, implementation candidate, gate/filter, AI decision component, market/timeframe expansion candidate, and production friction class. Each row must include:

- source artifact path(s), source hashes, row counts, and relevant IDs;
- simulated/replay R or proxy R where computable, gross/cost/stress expectancy, win/loss/zero counts, average win/loss R, duplicate/effective-N, concentration, pass/control delta, symbol/session/timeframe/horizon splits;
- current production touchpoint and files affected;
- action: implement, modify, remove, merge, redesign, replay, expand, convert to avoid-filter, keep as feature, or kill unsupported claim while preserving useful intelligence;
- exact code/config/prompt/tooling/test changes made or the row-level reason none can be made;
- before/after expected behavior and verification command.

The session must emit machine-readable ledgers, concise synthesis files, a completion audit, and an instruction-coverage ledger. Summary prose alone is not progress.

## Implementation Plates

The plates below are all in scope. Do not treat them as a top-N list; work through them as evidence and time allow, and after one plate is checkpointed continue to the next highest-value plate.

### Plate A - Moonshot To System

Consume the latest moonshot artifacts into current GTOS integration code. Use CP210/CP211+ executable code surfaces, replay geometry rows, held replay rows, selected-action tables, scorer/avoid candidates, and market-gap outputs. Turn them into runnable branch-local scorers, avoid filters, comparator outputs, or production-shadow decision modules with tests. Compute and preserve exact/proxy R summaries from the latest available replay evidence. Do not duplicate active moonshot builders; consume their current fixed snapshot.

### Plate B - Gate And Filter Architecture

Inventory every live and shadow gate/filter: touch count, L2 framework checks, POI availability, min RR, confidence handling, SL buffer/tolerance, correlation, kill-zone/session windows, SPRT/CUSUM, risk sizing, heartbeat behavior, framework dispatch, canary/test fixtures, malformed response handling, no-fill handling, pending lifecycle, and selector logic. For each, quantify what it blocks, what it saves, what it costs in missed replay R, and whether it should be kept, changed, narrowed, inverted into avoid intelligence, defaulted differently, or removed. Evidence-backed changes to prompt, config, risk, execution-adjacent code, safety code, canary/test code, selector logic, and tooling are authorized.

### Plate C - AI Decision Loop

Audit the AI/API role using prompt outputs, malformed logs, refusal logs, canary behavior, cost/latency, deterministic precision bugs, live candidate flow, and replay comparisons. Decide and implement the strongest architecture: deterministic scorer first, AI as explanation only, AI only for ambiguous cases, API removed from certain branches, prompt/schema hardened, parser narrowed, or current AI retained only where it beats deterministic alternatives. Do not spend new paid API calls. Use existing artifacts, tests, and local replay. Produce code/config/prompt changes where evidence supports them.

### Plate D - Legacy Research Merger

Map every prior research winner, loser, blocker, and expansion path into the current system architecture. Include V2/V2B/V3/V4/cascade, J46-J49, S79, K54/K55, FVG/OB, breaker, liquidity, no-fill, pending lifecycle, orderflow/Sierra, market expansion, exits, risk, session/regime/time, side-aware, and primitive-science hypotheses. For each, implement now, replay now, merge as feature/avoid/control, redesign, expand, or kill unsupported claim. Do not lose useful failure intelligence.

### Plate E - Market, Timeframe, And Primitive-Science Expansion

The current instrument list is not the research boundary. Use accessible local, MT5, Sierra, historical, and free/public data sources to expand symbol/timeframe/session tests where the repo and tools allow. Do not stop because a file is not already in the repo if MT5/Sierra/local tooling can extract it. Cover primitive sciences broadly: structure, liquidity, volatility, trend/momentum, mean reversion, session/calendar, spread/cost/fill, path ordering, intermarket context, regime, execution mechanics, and exit/risk behavior. Every expansion must produce numeric rows, implementation rows, or explicit source/access proof.

### Plate F - Production Upgrade Implementation

Change the system. Research that only sits in a ledger has not reached the goal. Implement scoped production or shadow-production changes where evidence supports them: scorers, filters, selectors, prompt/schema, config, risk logic, execution-adjacent behavior, observability, replay validators, data extractors, or test/canary coverage. Each code change must have a test or verifier and a clear before/after description.

## Operational Side Effects

Production-change authority covers live trading repo code paths when evidence supports improvement. Repository code, prompts, config, risk logic, execution-adjacent code, safety code, canary/test infrastructure, selector logic, tooling, logging, replay builders, validators, and system-design docs are authorized when evidence supports improvement. Actual broker operation that places, closes, or modifies trades; paid API/vendor spend; credentials; and remote push are not part of this session unless the owner gives a separate explicit operational command. Reading broker/account/order/history/deal/position files already on disk is allowed only as context. This boundary must not reduce computation, implementation, replay, or code changes inside the repo.

## Completion Standard

The session must run the full 36-hour window. No early finish after a checkpoint, verifier, context refresh, first result table, or first implementation. When a plate is complete, immediately continue to the next highest-value plate from disk evidence.

Every checkpoint must include scoped artifacts, JSON/JSONL parse checks for new ledgers, focused tests or verifiers for changed code, scoped commits on main, and a context refresh. The final closeout must include:

- total simulated/replay exact R rows, proxy R rows, and non-computable rows;
- gross, cost-adjusted, and stress-adjusted expectancy by branch/family/symbol/session/timeframe/horizon;
- implemented changes with file paths, tests, and before/after behavior;
- gates/filters kept/changed/removed and the evidence for each;
- AI decision-loop architecture after the session and why;
- old research merged, redesigned, expanded, or killed;
- market/timeframe expansion performed and remaining source/access proof;
- remaining highest-value work that could not be completed inside 36 hours, with exact source and implementation paths.

Forbidden final products: summary-only output, blocker-only output, prompt-only output, audit-only output, capture-only output, waiting for live data, chasing account-owner R, or ending because one branch family was handled. The goal is tangible system improvement from all accumulated evidence.

## Launch

```powershell
cd C:\Users\MSI\Documents\ai-trading-agent
codex --enable goals -c model_reasoning_effort=xhigh
```

Paste the starter from:

`research/science_program_2026_05/04_goal_prompts/MAIN_ORCHESTRATOR_36H_RESEARCH_TO_PRODUCTION_SYSTEM_UPGRADE_FROM_ALL_EVIDENCE_STARTER_2026-05-18.txt`
