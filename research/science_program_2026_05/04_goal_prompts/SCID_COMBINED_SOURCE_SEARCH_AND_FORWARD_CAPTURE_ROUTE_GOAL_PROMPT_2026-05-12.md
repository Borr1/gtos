# SCID Combined Source Search And Forward Capture Route Goal Prompt

Date: 2026-05-12
Owner lane: research/control next-route prompt emitted by G0 strategy-field packet synthesis
Evidence class: `SCID_COMBINED_SOURCE_SEARCH_AND_FORWARD_CAPTURE_ROUTE_ONLY`
Input G0 route: `research/science_program_2026_05/06_outcome_testing/g0_scid_strategy_field_source_expansion_packet_synthesis/`
Promotion posture: `NO_PROMOTION_VERDICT`
Required flags: `validation_safe=false`, `outcome_review_opened=false`, `live_effect=false`

## Objective

Run the rank-1 combined route selected by G0: exhaust the broadest auditable historical source-state search for any explicit SCID strategy-intent/source-state artifacts, then freeze a forward capture contract for every still-missing field. The route must not implement live wiring or score results; it should produce a G12-auditable source/capture contract and proposed offline schemas that move fastest toward future direction-aware edge testing.

This route must be constructive and anti-boxing: examples are starting points, not limits; current GTOS edge/frameworks are not the research horizon; source-safe means auditable/as-of/no-leak/hard-boundary-compliant, not conservative or narrow. The session must find as many useful hard-boundary-compliant source-search, source-recovery, source-join, source-contract, and forward-capture route families as it can inside this evidence class, then build the strongest route bundle rather than only naming missing fields.

## Maximum Search And Non-Boxing Posture

Do not limit the search to the obvious packet artifacts or to a single "missing historical intent" explanation. Search for explicit source-state truth anywhere it could exist without crossing hard boundaries: accepted G12/G0 route artifacts, prior no-API replay artifacts, FPB ledgers, source-field packet ledgers, source inventories, shadow logs, program-control artifacts, pipeline state, knowledge base records, local data roots, prior worktrees, route builders, tests, prompt contracts, and any adjacent auditable artifact that carries candidate id, duplicate key, timestamp, symbol/session, strategy state, or lifecycle state.

If a route can recover explicit source-state, build the recovery ledger. If a route cannot recover it but can prevent recurrence, build the capture contract. If a route exposes a new useful auditable path not listed here, add it to the route bundle. Do not treat "no direct field in the first artifacts" as completion.

## Maximum Pursuit Inside This Evidence Class

Inside the hard boundaries, this route should be treated as effectively unbounded. Do not choose a weaker result because the full pursuit is long, inconvenient, or produces many artifacts. Runtime, number of searched roots, number of route families, number of joins attempted, and output size are not quality limits. If the work is large, checkpoint it, chunk it, stream progress, resume from the context anchor, and keep going.

If a recoverable source path requires filesystem, extraction, or runtime approval that is allowed by this evidence class, request the exact approval and continue after approval. Do not end at "needs approval" when the route can ask, receive approval, and finish. A terminal same-run boundary exists only for hard forbidden boundaries, impossible historical source truth, unavailable external credential/manual export, paid/API spend not authorized by this prompt, or validation/live/broker evidence crossing.

## Independent G12 Handoff Discipline

This builder route may emit a next G12 audit prompt so the independent audit session has the full artifact map, recomputation checklist, and acceptance/repair questions. That is not context overfitting and not self-acceptance. The G12 prompt is a handoff scaffold only: this route must maximize source-search/source-recovery/capture construction, while the next G12 independently verifies hashes, denominators, no-leak posture, evidence-class boundaries, row coverage, and exact repair needs.

## Mandatory Preflight

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/quick_reference_card.md`.
4. Read `.context/00_core/research_operating_doctrine.md`.
5. Read `.context/00_core/goal_session_research_discipline.md`.
6. Read `.context/00_core/research_current_state.md`.
7. Read this prompt from disk.
8. Read the G0 strategy-field synthesis context anchor, G12 reconciliation, source-field readiness synthesis, route ranking ledger, anti-boxing ledger, capture route specification, forbidden-surface audit, decision ledger, completion audit, and verifier result.
9. Read the accepted G12 strategy-field packet audit and builder packet artifacts from disk.

Do not rely on chat memory. If interrupted, regenerate `LIVE_STATE`, re-read this prompt and the route artifacts, and resume from disk evidence.

## Mandatory Context Use

Treat `.context/00_core/goal_session_research_discipline.md` and `.context/00_core/research_operating_doctrine.md` as active instructions. Record in the completion audit:

- whether both were read after preflight;
- the builder/control posture applied;
- anti-boxing questions pursued;
- newly discovered route families considered;
- searched roots and artifacts inspected;
- approval/access requests made and whether work continued after approval;
- checkpoint/chunk/resume status if the run is large;
- terminal proof-or-impossibility boundary;
- what was deliberately not answered because it crosses validation/result scoring/broker/live/AI/API/paid/raw-data boundaries.

## Hard Boundaries

Preserve `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, and `live_effect=false`.

Do not open validation execution, target/result/performance scoring, strategy-edge claims, R/PnL/win-rate/expectancy/performance, broker account/order/history/deal/position evidence, AI/API calls, paid/vendor access, raw `.scid`, `.parquet`, `.csv`, `.dly`, `.bin`, `.depth`, or market-data blob commits, live behavior, or prompt/config/risk/safety/execution/canary/selector changes.

## Required Outputs

Create a dedicated route directory under `research/science_program_2026_05/06_outcome_testing/` and emit:

- context anchor and searched-root ledger
- active question stack and checkpoint/resume ledger
- historical source-state recovery attempt ledger
- source-state join/recovery candidate ledger
- anti-boxing route-discovery ledger
- forward capture contract for side, entry, stop, target, POI, framework, lifecycle, LTF, orderflow/proxy, and baseline-control fields
- schema/redaction/as-of/no-leak specification
- implementation-readiness ledger with no-live-effect boundary
- ranked continuation bundle if multiple constructive paths remain
- next independent G12 audit handoff prompt with recomputation, acceptance, and repair questions, without self-accepting or pre-rejecting this builder route
- standalone verifier, focused tests, output manifest, completion audit, closeout verification

## Required Saturation And Self-Red-Team

Before completion, answer and pursue same-evidence-class gaps:

- Are any historical strategy-intent/source-state fields being inferred from price?
- Are any target/result/performance fields leaking into source/control artifacts?
- Are side, entry, stop, target, POI, framework, lifecycle, LTF, and orderflow/proxy fields all either recovered from explicit evidence, assigned exact capture requirements, or forbidden by hard boundary?
- Are current GTOS frameworks, SCID-only artifacts, or M15 bars being treated as the entire horizon?
- Are any useful auditable route families being ignored because they were not named in this prompt?
- Did the route search every accessible local/committed/prior-worktree source-state root before deciding a field is non-generatable historically?
- Did the route attempt to end because of runtime, output size, or approval friction instead of checkpointing/requesting approval and continuing?
- Are any remaining terminal boundaries exact and actionable rather than generic handoff labels?
- Is the artifact audit-ready for denominator stability, no-leak proof, source hash strength, exact capture requirements, and hard-boundary compliance?

## Completion Standard

1. Mandatory preflight and context use are recorded.
2. Maximum-pursuit posture is executed: large searches are checkpointed/chunked rather than skipped, and allowed approval/access needs are requested and pursued instead of becoming lazy blockers.
3. Every accepted field family is either recovered from explicit source, proven non-generatable after broad auditable source-state pursuit, or assigned exact prospective capture requirements.
4. No validation, scoring, broker evidence, AI/API, paid/vendor access, raw market-data blob commit, live behavior, or trading-surface change is opened.
5. Verifier and focused tests pass.
6. A full next independent G12 audit handoff prompt is emitted without turning the builder route into its own auditor.
7. Scoped commits and closeout verification are complete.

## One-Line Starter

`/goal Follow the full controlling prompt in research/science_program_2026_05/04_goal_prompts/SCID_COMBINED_SOURCE_SEARCH_AND_FORWARD_CAPTURE_ROUTE_GOAL_PROMPT_2026-05-12.md as the complete objective; do mandatory preflight and context refresh first; do not rely on chat memory; stay SCID_COMBINED_SOURCE_SEARCH_AND_FORWARD_CAPTURE_ROUTE_ONLY with no validation/strategy-edge/R/PnL/win-rate/expectancy/performance/promotion/live behavior/AI/API/paid-vendor/broker-account-order-history-deal-position/raw-market-blob/prompt-config-risk-safety-execution-canary-selector changes; search as broadly and creatively as the evidence class allows for explicit historical strategy/source-state truth across accepted artifacts, shadow/program-control/pipeline/knowledge/data/prior-worktree roots; runtime/output size/root count are not limits, so checkpoint/chunk/resume and ask for allowed approval/access then continue; recover every auditable field possible for all 3,014 accepted SCID candidates, freeze exact forward capture contracts for only what remains non-generatable, emit anti-boxing/search/recovery/capture/checkpoint ledgers, verifier, focused tests, full independent G12 audit handoff prompt, scoped commits, closeout verification, NO_PROMOTION_VERDICT, validation_safe=false, outcome_review_opened=false, live_effect=false; mark complete only when the prompt file's completion standard is fully satisfied.`
