# GTOS Hard-Halt Forensic Probability Rebuild Goal Prompt

You are running the hard-halt rebuild route for GTOS after the redacted_account vNext activation failure. The live account was flattened and the system was stopped because the current selector, exposure model, cost accounting, exit management, and runtime-control stack produced unacceptable live behavior. Treat the halted live system as failed until disk evidence proves each failed component has been rebuilt, replayed, stressed, and placed behind an approval-ready production-return dossier.

This is not a summary task. This is a maximum-output research, forensic, replay, default-off implementation, and dossier-building session. Build the strongest auditable artifact set the repo can support. Compute the numbers. Join the rows. Repair the source gaps. Kill weak claims. Preserve useful mechanisms. Design stronger mechanisms. Produce machine-checkable ledgers, specs, prototypes, verifiers, focused tests, and a direct production-return decision package.

The previous trade-quality plan and prompt-pack plan are required inputs, not limits. Their examples are starting vectors, not boundaries. Frequency is not the target. The target is higher win rate, broker-net expectancy, shorter useful holding time, cleaner entry timing, stronger MFE capture, lower open-P/L giveback, fewer stale positions consuming prop-risk capacity, and a portfolio allocator that takes only the best available trades.

## Mandatory Context Use

Start from current disk state. Run `python scripts/generate_live_state.py`, then read:

- `.context/LIVE_STATE.md`
- `.context/00_core/current_vnext_system_map.md`
- `.context/00_core/current_repo_reading_order.md`
- `.context/00_core/quick_reference_card.md`
- `.context/00_core/goal_session_research_discipline.md`
- `.context/00_core/research_operating_doctrine.md`
- the latest numbered handoff in `.context/02_session_handoffs/` as historical context only
- `research/operations/vnext_redacted_account_hard_halt_reconciliation_2026_06_03/TRADE_FAILURE_REVIEW_2026-06-03.md`
- `research/operations/vnext_redacted_account_hard_halt_reconciliation_2026_06_03/BROKER_TRUTH_TRADE_GROUPS_2026_04_27_TO_HALT.json`
- `research/operations/vnext_redacted_account_hard_halt_reconciliation_2026_06_03/BROKER_TRUTH_DEALS_2026_04_27_TO_HALT.json`
- `research/operations/vnext_redacted_account_hard_halt_reconciliation_2026_06_03/BROKER_TRUTH_ORDERS_2026_04_27_TO_HALT.json`

Read source code and config before claiming behavior. Do not rely on chat memory. Treat `goal_session_research_discipline.md` and `research_operating_doctrine.md` as active instructions, not background. Operationalize them into the route registry, searched-root ledger, active question stack, route-decision ledger, saturation pass, and completion audit.

After any context compaction, resume, interruption, long wait, tool crash, or uncertainty, regenerate `LIVE_STATE` and reread this starter, this controlling prompt, the doctrine files, the hard-halt report, and the latest route artifacts from disk before continuing.

The completion audit must state whether each required context file was read after preflight, whether the lane posture remained builder/discovery/repair/replay/candidate-edge rather than passive audit, which anti-boxing questions were pursued, which outside-current-edge mechanism families were searched, which proof-or-impossibility stop condition was used, and which doctrine requirements crossed a true evidence-class or forbidden-surface boundary.

## Explicit Owner Approval And Hard Boundaries

The owner grants explicit approval for this goal session to execute every local, read-only, research, replay, extraction, parser, source-repair, default-off implementation, prompt/spec/schema, verifier, focused-test, and dossier-building action needed to complete this route inside the repo and approved local evidence universe.

This approval includes:

- read-only broker/account/order/history/deal/position evidence already extracted or reachable through approved local read-only sources;
- code, config, prompt, schema, parser, scorer, replay, portfolio, risk, exit, selector, and safety inspection;
- default-off research/prototype implementation that cannot alter live behavior while the halt flags and scheduler stop remain in force;
- local tests, verifiers, syntax checks, replay builders, ledgers, manifests, route reports, and production-return dossier drafts.

This approval does not authorize live trading broker operation, order placement, position modification, scheduler reactivation, runtime restart, MT5 live behavior, paid API/vendor spend, credential change, remote push, or live prompt/config/risk/execution/safety/canary/selector behavior change. The halt is a capital boundary, not a research brake. Do not use the boundary as an excuse to avoid computation, local implementation, replay, verifier work, or production-dossier construction.

If a blocker requires a forbidden surface, record the exact owner/access/source/capture approval requirement and continue every remaining same-evidence-class path. If a command needs runtime permission for an allowed source-bound action, request the exact permission and continue after approval.

## Mission

Build the complete post-halt intelligence stack:

1. Determine exactly what failed in selector, confidence, entry timing, execution, cost/swap accounting, exposure, same-symbol lifecycle, active exits, profit retention, emergency control, and portfolio allocation.
2. Convert every available trade, candidate, decision, lifecycle, and broker-truth row into joined forensic intelligence.
3. Replace permissive selection with a probability-ranked decision engine and a portfolio allocator that rejects weak trades and challenges stale open trades.
4. Design and default-off prototype the active trade manager that protects MFE, cuts stale theses, prevents long useless holds, and releases prop-risk capacity.
5. Design and default-off prototype the same-symbol lifecycle state machine for scale-in and close-and-reverse behavior with no averaging down and no hidden hedging.
6. Build replay, stress, calibration, and acceptance artifacts strong enough to decide what gets killed, quarantined, redesigned, implemented, shadowed, or placed into a production-return dossier.

The route must not end in generic observations. It must end with computed row counts, broker-net and proxy-R metrics, expectancy, win rate, hold-time, MFE/MAE, cost/swap, drawdown, giveback, calibration, concentration, source-completeness, branch-decision, implementation-decision, and test evidence.

## Starting Evidence To Recompute And Extend

The hard-halt report states:

- Account `0` on `redacted_account-Server 2` was flat after halt: open positions `0`, pending orders `0`.
- Broker truth extraction contains `91` grouped trades, `205` deals, and `207` orders.
- GTOS_SYSTEM full extraction: `82` trades, `+$988.47`, `34` wins, `48` losses.
- Current vNext activation window from `2026-05-29` through halt: `77` GTOS trades, `-$859.69`, `31` wins, `46` losses, `40.26%` win rate.
- `2026-06-02` profit was distorted by one GER30 winner of `+$1,318.24`; excluding that outlier, the recent window is about `-$2,177.93`.
- Worst recent symbols: XAUUSD `12` trades `-$1,327.23`, NDX100 `15` trades `-$1,151.56`, ETHUSD `6` trades `-$696.18`, GBPJPY `2` trades `-$543.61`, UKOUSD `3` trades `-$297.35`, AUDJPY `1` trade `-$274.05`.
- XAUUSD plus NDX100 lost `-$2,478.79`.
- Recent stop-loss/broker-SL exits: `54` trades, `-$9,280.84`.
- Trades with partial/TP1 comments: `22`; final-classified partial/TP winners: `13`, `+$4,677.44`.
- Damaging clusters include `2026-06-01 16:16`, `2026-06-02 10:46`, `2026-06-02 16:31`, and `2026-06-02 17:45`.
- Weak selector examples include USDCAD, JP225, CHFJPY, and NDX100 trades admitted by micro-positive expectancy floors around `0.02R` to `0.05R` and weak win rates.
- ETHUSD ticket `242689402` shows cost/swap danger: captured entry cash risk near `$254.66`, close net `-$673.62`, swap `-$391.16`.
- Emergency flattening alone did not stop system risk; scheduler/process shutdown and halt flags were required.

These numbers are seed facts, not truth by assertion. Recompute them from disk, extend them with row-level joins, and correct any mismatch with evidence.

## Required Artifact Set

Create the route under `research/operations/vnext_hard_halt_forensic_probability_rebuild_2026_06_04/`. Use explicit manifests and hashes for material outputs.

Produce at minimum:

- `HARD_HALT_CONTEXT_ANCHOR.json`
- `HARD_HALT_SEARCHED_ROOT_LEDGER.jsonl`
- `HARD_HALT_ACTIVE_QUESTION_STACK.jsonl`
- `HARD_HALT_ROUTE_DECISION_LEDGER.jsonl`
- `BROKER_TRADE_CANDIDATE_FORENSIC_MATRIX.jsonl`
- `BROKER_TRADE_CANDIDATE_FORENSIC_SUMMARY.md`
- `FAILURE_TAXONOMY_LEDGER.jsonl`
- `DATA_GAP_AND_SOURCE_REPAIR_LEDGER.jsonl`
- `SELECTOR_WEAKNESS_LEDGER.jsonl`
- `ENTRY_TIMING_AND_PATH_QUALITY_LEDGER.jsonl`
- `MFE_MAE_TIME_IN_TRADE_LEDGER.jsonl`
- `PROFIT_RETENTION_AND_GIVEBACK_LEDGER.jsonl`
- `COST_SWAP_SLIPPAGE_LEDGER.jsonl`
- `SYMBOL_SESSION_HEALTH_QUARANTINE_LEDGER.jsonl`
- `PORTFOLIO_EXPOSURE_AND_CLUSTER_LEDGER.jsonl`
- `SAME_SYMBOL_LIFECYCLE_STATE_MACHINE_SPEC.md`
- `PROBABILITY_DECISION_PACKET_SPEC.md`
- `PROBABILITY_DEBATE_TEAM_SPEC.md`
- `ACTIVE_TRADE_MANAGER_SPEC.md`
- `PORTFOLIO_ALLOCATOR_SPEC.md`
- `REPLAY_STRESS_AND_CALIBRATION_PLAN.md`
- `DEFAULT_OFF_IMPLEMENTATION_PATCH_OR_EXACT_PATCH_PLAN.md`
- `PRODUCTION_RETURN_DOSSIER_DRAFT.md`
- `HARD_HALT_SATURATION_AND_SELF_RED_TEAM.md`
- `HARD_HALT_VERIFICATION_RESULT.json`
- `HARD_HALT_OUTPUT_MANIFEST.json`
- `HARD_HALT_COMPLETION_AUDIT.md`

If any artifact name changes, record the replacement in the manifest and route-decision ledger. Do not drop an artifact silently.

## Forensic Matrix Requirements

Build one joined record per broker grouped trade and one joined record per candidate or decision row that can be tied to the halted period or to the selected-cell evidence used by those trades.

Join from:

- broker truth deals, orders, and grouped trades;
- `knowledge_base/redacted_account_live_bee34003/trade_records/**`;
- `knowledge_base/trade_records/**`;
- `shadow_logs/gtos_vnext_runtime_decisions.jsonl`;
- `shadow_logs/gtos_vnext_replacement_monitoring.jsonl`;
- `shadow_logs/pending_limit_lifecycle.jsonl`;
- `shadow_logs/slippage.jsonl`;
- `shadow_logs/time_in_trade.jsonl`;
- `shadow_logs/daily_pnl.json` and `shadow_logs/daily_pnl_history.jsonl`;
- `shadow_logs/live_candidate_opportunity_clusters.jsonl`;
- `shadow_logs/live_candidate_strategy_rollups.jsonl`;
- `shadow_logs/live_mechanical_strategy_shadow_outcomes.jsonl`;
- `pipeline_state/**`;
- relevant selected-cell, moonshot, activation, replay, and route-local artifacts discovered by `rg`.

For each trade/candidate, compute or preserve:

- broker account, symbol, side, ticket, order, deal, position, magic, comment, open time, close time, entry, exit, lots;
- broker profit, commission, swap, fees, net P/L, gross and net R, cash risk, risk pct, lot model, prop-risk reservation and release;
- selector source, origin family, session, timeframe, selected-cell row count, expectancy, profit factor, win rate, total R, lower confidence bound where computable, source hash, evidence class;
- spread R, slippage R, commission R, swap R, cost uncertainty, unresolved-cost status, broker-spec fields;
- entry timing features: distance from trigger, late-entry distance, impulse/displacement state, confirmation state, microstructure/path state, session liquidity state, volatility regime;
- MAE, MFE, time to +0.25R, +0.5R, +1R, +2R, time to adverse -0.25R, -0.5R, -1R, total hold time, stagnation time;
- MFE captured at exit, MFE giveback, account-level open P/L peak and giveback, basket-level MFE and giveback;
- exit reason, stop/TP/partial/BE/trail events, stale-thesis events, time-stop events, manual/emergency events;
- concurrent positions, correlated exposure, basket risk, same-symbol conflict, scale/reversal opportunity, opportunity cost of reserved risk;
- failure tags, repair tags, implementation tags, quarantine tags, and decision tags.

Do not invent historical broker intent, order lifecycle, or unlogged system state from price movement. If a field is non-generatable, prove the source gap and create the prospective capture/logger contract that prevents recurrence.

## Failure Taxonomy Requirements

Classify every losing, stale, high-giveback, high-cost, and opportunity-cost trade into row-level failure tags. Required tags include:

- `selector_micro_positive_floor`
- `selector_low_win_rate`
- `selector_low_expectancy`
- `selector_small_effective_n`
- `selector_large_n_tiny_edge`
- `symbol_decay`
- `session_decay`
- `late_entry`
- `entry_after_move_exhausted`
- `adverse_excursion_before_profit`
- `stale_thesis`
- `long_hold_no_value`
- `mfe_giveback`
- `account_open_pnl_giveback`
- `partial_helped_but_loser_stream_dominated`
- `swap_exceeded_risk`
- `commission_or_spread_unresolved`
- `slippage_damage`
- `clustered_stopout`
- `correlated_basket_loss`
- `prop_risk_capacity_blocked`
- `same_symbol_scale_missed`
- `same_symbol_reversal_missed`
- `same_symbol_conflict_correctly_blocked`
- `runtime_control_failure`
- `data_capture_gap`
- `non_generatable_source_gap`

Add every additional failure class the data exposes. Examples are not limits.

## Probability Engine Requirements

Build a default-off probability decision packet and prototype or exact implementation patch plan. The old confidence filter cannot be promoted by assertion. Build a numerical probability engine that produces calibrated, inspectable decisions.

Required outputs per candidate:

- `p_win`
- `p_hit_0_5r_before_adverse_0_5r`
- `p_hit_1r_before_sl`
- `p_hit_2r_before_sl`
- `expected_broker_net_r`
- `expected_broker_net_cash`
- `expected_holding_minutes`
- `expected_time_to_0_5r`
- `mfe_giveback_risk`
- `cost_swap_tail_risk`
- `symbol_session_health_score`
- `entry_timing_score`
- `portfolio_opportunity_score`
- `uncertainty_band`
- `calibration_bucket`
- `decision`
- `reason_codes`

Build deterministic voter modules in the spec/prototype:

- selector statistician;
- entry-timing and path-quality analyst;
- execution-cost and swap analyst;
- active-exit and profit-retention analyst;
- prop-risk and portfolio allocator;
- same-symbol scale/reversal analyst;
- symbol/session health analyst;
- adverse-selection and regime analyst;
- contrarian failure-inversion analyst.

Each voter emits numeric probabilities, expected value, uncertainty, evidence rows, and reject/accept reason codes. The aggregator combines voters into a calibrated decision. LLM text can summarize evidence, but no LLM text can be the sole probability authority.

Validate probability quality with Brier score, log loss, reliability buckets, expected calibration error, profit-weighted calibration, and calibration by symbol/session/origin family.

## Selector Rebuild Requirements

Replace broad permissive selection with ranked allocation. The new selector must act like a capital allocator, not an entry permission list.

Compute and compare at least:

- current vNext selector;
- current vNext without micro-positive floor cells;
- broker-net adjusted selected-cell selector;
- top-ranked probability selector;
- symbol/session health quarantine selector;
- cost/swap gated selector;
- entry-timing/path-quality gated selector;
- active trade manager overlay;
- combined selector/allocator.

The selector must reject or quarantine cells with weak broker-net evidence, poor calibration, low win rate, tiny expectancy, high cost drag, high MFE giveback, bad hold-time profile, symbol/session decay, concentration fragility, or poor stress survival.

Do not set production thresholds from the failed live window and call them validation. Use the failed live window for forensics, hypothesis generation, and kill/quarantine decisions. Build sealed or held-out validation and stress partitions for proposed rules.

## Entry Timing Requirements

Compute entry quality as a first-class determinant. A trade that only wins after deep adverse movement, long chop, and delayed recovery is not a clean entry.

Analyze:

- time to first favorable excursion;
- MAE before MFE;
- entry after impulse exhaustion;
- entry during chop;
- entry distance from trigger/reference level;
- entry versus lower-timeframe confirmation;
- entry during spread/liquidity stress;
- session-specific time-to-profit distributions;
- missed better-entry windows;
- late fill versus intended thesis;
- stop-first versus target-first path ordering.

Create entry-timing reject, delay, limit-entry, market-entry, and no-entry decision rules in the spec/prototype. Preserve source requirements for any rule that cannot be computed now.

## Active Trade Manager Requirements

Build the active trade manager as a default-off local spec/prototype. It must manage open trades as a portfolio, not isolated tickets.

Required states:

- `HOLD`
- `PROTECT`
- `PARTIAL_REDUCE`
- `TIGHTEN`
- `CLOSE_STALE`
- `CLOSE_GIVEBACK`
- `CLOSE_COST_RISK`
- `CLOSE_FOR_BETTER_OPPORTUNITY`
- `SCALE_IN`
- `CLOSE_AND_REVERSE`

Required logic:

- track position-level and account-level MFE;
- quantify MFE capture and giveback;
- cut stale trades that fail time-to-profit milestones;
- reduce or close profitable trades that give back too much MFE;
- protect trades at +0.5R/+1R according to broker-net and path evidence;
- close or reduce high-swap/high-cost trades before cost tail risk dominates;
- compare open trades against incoming candidates by expected net R per reserved risk dollar and per expected holding minute;
- release prop-risk capacity from long useless holds;
- preserve winners rather than letting open account P/L collapse from peak to weak residual profit.

Do not average down. Do not allow unobserved hedging. Do not hold stale trades because they are not yet at SL.

## Same-Symbol Lifecycle Requirements

Replace the blanket same-symbol ban with a ticket-bound state machine only after tests prove lifecycle safety. The current same-symbol protection remains the live default until the state machine passes shadow/replay/default-off tests.

Same direction:

- `SCALE_IN` is allowed only when the existing trade is protected, the new signal has higher probability than the current thesis, combined risk remains inside cap, entry improves or confirms movement, and the action is not averaging down.
- Existing losing or unresolved trades cannot be used as justification for adding exposure.

Opposite direction:

- `CLOSE_AND_REVERSE` is allowed only when reversal probability beats continuation probability, the current thesis has decayed, the current trade is profitable or protected, and the close/reduce action is executed before any opposite-side open in future live design.
- Hidden hedging remains blocked.

Implement or specify exact state transitions, invariants, test cases, ticket ownership, close-before-open ordering, partial close interactions, BE/trailing interactions, lock behavior, crash recovery, duplicate prevention, and audit logging.

## Portfolio Allocator Requirements

Build a best-trade allocator:

- rank all candidates by expected broker-net R, probability, time-to-profit, risk reservation, cost drag, symbol/session health, correlation, and open-position opportunity cost;
- execute zero trades when no candidate clears the evidence-based bar;
- prefer one strong trade over multiple weak correlated trades;
- force stale open trades to compete against fresh candidates;
- restore hard concurrent, basket, symbol, session, and daily/session stop controls that selected-cell governance cannot bypass;
- add account-level open-P/L peak/giveback protection;
- add symbol kill/quarantine logic for failed surfaces such as XAUUSD and NDX100 until repaired by evidence.

The allocator must make frequency subordinate to quality. More setups are noise unless the probability and portfolio math justify them.

## Validation, Replay, And Stress Requirements

Use existing data aggressively and honestly. Do not wait passively for days of new live data when source-safe historical replay, current broker truth, and shadow logs can answer the failure anatomy now.

Build partitions:

- forensic failed-live window;
- discovery/development partition;
- sealed historical validation partition when it exists on disk, otherwise exact source requirement;
- stress/robustness partition;
- forward-shadow capture requirement partition;
- contaminated/forbidden partition.

Run or specify runnable replay/stress comparisons against baselines:

- current vNext;
- selector-only fixes;
- probability-ranked top candidate;
- active trade manager overlay;
- same-symbol lifecycle shadow logic;
- portfolio allocator;
- combined rebuild.

Metrics must include broker-net P/L, exact R, proxy R, expectancy, win rate, profit factor, average hold time, median hold time, time to +0.5R/+1R, MAE, MFE, MFE capture, MFE giveback, open account P/L giveback, max drawdown, daily drawdown, prop-risk usage, concurrent exposure, symbol/session concentration, cost/swap drag, slippage sensitivity, spread sensitivity, best-trade replacement effect, and calibration quality.

Use adversarial baselines: random/shuffled signals, session-only, volatility-only, simple momentum, simple mean reversion, current GTOS baseline, cost-stressed variants, delayed entries, worse exits, removed-best-trades, leave-symbol-out, leave-session-out, leave-origin-family-out, walk-forward, purged/embargoed splits, and parameter perturbation.

## Language And Output Discipline

Use direct result-driving language. Do not write vague placeholders. Do not write passive "needs more data" unless the exact missing source, field, symbol, time window, schema, logger, parser, legal/access proof, owner approval, and searched roots are recorded. Do not write "future work" where same-evidence-class pursuit is available. Do not cap findings with top 3, top 5, top 10, representative rows, or compact-only summaries. Ranked summaries are allowed only after full ledgers preserve all material rows.

No conservative brake. No passive waiting. No generic blocker theater. No hiding behind small sample size. Small `n` blocks production-change readiness only; it does not block forensics, hypothesis generation, failure anatomy, source repair, proxy computation, or validation-plan construction.

Full same-evidence-class pursuit is required. Literal impossibility means exactly every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action from approved sources has been attempted or proven inapplicable with row-level evidence.

## Verification Requirements

Build and run the strongest local verifier and focused tests the route requires. At minimum:

- validate JSON/JSONL parse for every emitted ledger;
- verify required artifact existence and manifest hash coverage;
- verify broker truth counts against source files;
- verify forensic matrix row counts and join disposition counts;
- verify every failure tag belongs to a declared taxonomy;
- verify no arbitrary top-N replacement by checking full-ledger coverage;
- verify default-off prototypes cannot alter live behavior;
- verify no live trading broker operation, scheduler/runtime restart, paid API/vendor spend, credential/remote change, or unapproved live prompt/config/risk/execution/safety/canary/selector behavior change occurred;
- run focused pytest for any changed code path;
- run `python scripts/validate_goal_prompt_hardening.py` against any emitted downstream prompt or starter.

If Windows bytecode/cache friction blocks a test, use syntax-parse fallback and record the environment friction separately from code failure.

## Completion Standard

Completion requires all of the following:

- context anchor, searched-root ledger, active question stack, route-decision ledger, and instruction-coverage checklist are written;
- broker trade/candidate forensic matrix covers all broker grouped trades and all current vNext activation GTOS trades, or every missing join has exact searched-source proof;
- selector, entry timing, cost/swap, active exits, MFE/giveback, exposure, same-symbol, portfolio, and runtime-control failure classes are quantified;
- probability decision packet, debate team, active trade manager, same-symbol state machine, and portfolio allocator specs/prototypes are written;
- replay/stress/calibration plan is runnable and tied to exact source inputs;
- production-return dossier draft states exact gates, thresholds-to-freeze, shadow/demonstration requirements, rollback/halt procedure, and remaining approval requirements;
- verifier and focused tests pass or record exact environment-only friction;
- output manifest covers all artifacts;
- saturation and self-red-team pass pursues every same-evidence-class gap it exposes;
- completion audit records what was computed, repaired, killed, quarantined, redesigned, implemented default-off, tested, and left as exact external approval/source/capture requirement.

Do not mark complete with unresolved placeholders, generic blocker, generic "needs data", or summary-only conclusion. Mark complete only when the prompt file's completion standard is fully satisfied from disk evidence.
