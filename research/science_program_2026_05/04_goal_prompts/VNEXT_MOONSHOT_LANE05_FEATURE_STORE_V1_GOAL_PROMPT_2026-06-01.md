# vNext Moonshot Lane 05 - Feature Store V1

Own the first versioned no-leak feature store for the moonshot program. Convert source authority, as-of contracts, candidate reconstruction, and microscope outputs into reusable feature rows for selector, scheduler, execution, ML, replay, and repair systems.

## Mandatory Context Use

Run mandatory preflight. Read doctrine, moonshot vision, master route, Lane01 source authority, Lane02 as-of contract, Lane03 candidate reconstruction, Lane04 microscope outputs when present with exact absent-state row when not, Friday microscope artifacts, selected-denominator artifacts, and current runtime packet schemas. Do not rely on chat memory. After compaction/resume/interruption/uncertainty, reread prompt/starter/doctrine/latest artifacts from disk.

This is a feature-store builder lane. Treat doctrine as active instructions. Build as much as the approved evidence supports; do not shrink to a small manual feature list. Examples are starting points, not boundaries.

## Objective

Write route outputs under:

`research/operations/vnext_moonshot_lane05_feature_store_v1_2026_06_01/`

Build a versioned as-of feature store with feature families for symbol/session/time, origin/framework/side, HTF/M15/M1/tick state, spread/cost, volatility, liquidity sweep, displacement, failed displacement, source completeness, selected-cell risk proof, portfolio state, broker feasibility, correlation cluster, regime, source-backed news/calendar state with exact missing-source requirements when unavailable, and stale-label flags.

## Required Work

- Define feature schema with as-of timestamp, source path, source-use state, leakage class, nullable policy, and downstream eligibility.
- Build feature extraction code from candidate/event rows and market/source data.
- Include missingness/source-completeness features instead of silently dropping rows.
- Add no-leak validator integration from Lane02.
- Produce feature coverage by symbol/session/timeframe/mechanism/source family.
- Produce feature importance-readiness metadata for ML and selector lanes.
- Preserve every feature family that can be source-bound; create exact capture/source requirements for unavailable fields.

## Completion Standard

No arbitrary top-N or manually narrowed feature list. Full material feature families and rows must be preserved. Full same-evidence-class pursuit is required; every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action must be attempted or proven inapplicable.

Forbidden surfaces: live trading broker operation, order action, credential print/change, paid API/vendor calls, remote push, hidden production activation. Read-only MT5 market/spec/history data is authorized for source-bound feature extraction.

Finish with feature schema, feature builder, feature ledgers/partitions, source completeness, no-leak checks, manifest, verifier, focused tests, and completion audit.

## Operative Hardening Anchor

Read `goal_session_research_discipline.md` and `research_operating_doctrine.md`; do not rely on chat memory. Treat those files as active instructions, not background. This is constructive research/build work with curiosity, active creativity, and no conservative brake inside the evidence class. Full same-evidence-class pursuit is mandatory. The phrase literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action has been attempted or proven inapplicable. No arbitrary top-N, top 3/5/10, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows. Produce artifact, ledger, manifest, verifier or focused test output, completion audit, source-capture or source completeness decisions, branch decision or implementation decision, and exact R, proxy R, or expectancy fields when result materialization is owned; when result fields are absent, produce row-level missing-field proof plus the strongest source-bound proxy or exact capture/export requirement. Scope boundaries are rails around execution, spend, credentials, remotes, and hidden production activation; they are not permission to shrink local/repo/MT5/read-only evidence pursuit. Forbidden surfaces remain production-change activation, live trading broker operation, paid API/vendor calls, broker account order history deal position changes, credential/remotes, and prompt/config/risk/execution/safety/canary/selector changes outside scoped default-off or dossier authority.
