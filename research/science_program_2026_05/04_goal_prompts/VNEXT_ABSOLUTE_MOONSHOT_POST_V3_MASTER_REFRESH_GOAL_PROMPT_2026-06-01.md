# vNext Absolute Moonshot Post-V3 Master Refresh

Own the post-V3 Master integration refresh. The objective is to consume terminal Source Capture Repair, Selector V3, Scheduler V3, and Execution Policy V3 evidence from disk, update the absolute moonshot Master registry and context, close stale launch-state wording, and open the next lanes only from verified artifacts.

This is an integration/control lane, not a replacement for the V3 lanes. It must not relaunch completed lanes, hand-write summaries, or accept stale chat claims. It must inspect disk artifacts, verify terminal status, update Master code/artifacts/tests, refresh current context, and produce exact next-wave launch decisions.

## Mandatory Context Use

Run mandatory preflight from disk before using memory. Regenerate `.context/LIVE_STATE.md`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/orchestrator_successor_operating_brief.md`, `.context/00_core/orchestrator_methodology_hardening_controls.md`, `.context/00_core/parallel_goal_merge_playbook.md`, `.context/00_core/vnext_absolute_moonshot_vision_and_limitations.md`, the latest handoff as historical only, and the current Master route at `research/operations/vnext_absolute_moonshot_master_orchestration_2026_06_01/`.

Read terminal/current route inputs from disk:

- Master post-Lane18 artifacts.
- Lane01-Lane18 terminal artifacts.
- Lane09B selector-scheduler reconciliation.
- Lane10B scheduler conflict anatomy.
- Source Capture Repair route.
- Selector V3 route.
- Scheduler V3 route.
- Execution Policy V3 route.
- Existing ML Lane12/Lane13 prompts as later subsystem context only.
- Repair companion and command-center prompt files as later-lane context only.
- Current research state and launch-order files.

After compaction, resume, interruption, uncertainty, or long waits, regenerate `LIVE_STATE`, reread this prompt, starter, doctrine, Master artifacts, and latest V3/source-capture artifacts from disk, then continue from route state. Do not rely on chat memory.

Treat `goal_session_research_discipline.md`, `research_operating_doctrine.md`, and orchestrator hardening controls as active instructions, not background. Operationalize full same-evidence-class pursuit for integration: literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available inside this integration evidence class has been attempted or proven inapplicable. Use constructive integration posture with curiosity, active creativity, and no conservative brake inside this evidence class.

## Objective

Refresh:

`research/operations/vnext_absolute_moonshot_master_orchestration_2026_06_01/`

and current context files so Master consumes terminal post-Lane18 V3/source-capture evidence and opens the next implementation/research lanes from verified disk state.

## Required Work

- Inspect Source Capture Repair, Selector V3, Scheduler V3, and Execution Policy V3 route artifacts from disk. Do not accept chat outputs as authority.
- Build terminal state tables, artifact inspection ledgers, dependency graph updates, limitation disposition updates, downstream contract matrix, source authority map updates, output schema contract updates, anti-duplication/loop rules, launch-order updates, runtime-effect boundary updates, and next-wave decisions.
- Update Master builder/tests/verifier so terminal V3/source-capture evidence is mechanically checked. The verifier must fail if a required route is absent, nonterminal, missing required artifacts, missing verifier success, missing focused tests, missing downstream contracts, or still marked launch-pending after terminal consumption.
- Neutralize stale text that still treats Lane16-Lane18, Source Capture Repair, Selector V3, Scheduler V3, or Execution Policy V3 as not started after terminal evidence exists.
- Open the next lanes from verified evidence only. Expected next lanes after terminal V3/source-capture are ML dataset/baselines, ML policy intelligence, daily learning/repair companion, command center, production-change dossier, and any exact source/implementation repair lane proved by V3 evidence.
- Keep ML as a subsystem consumer, not the main actor, unless V3/source-capture artifacts themselves prove a different launch dependency.
- Preserve every material limitation and next action in machine-readable ledgers. Do not collapse unresolved gaps into vague future work.
- Update `.context/00_core/research_current_state.md` so future sessions do not relaunch terminal lanes or drift to stale pre-V3 launch order.
- Regenerate `.context/LIVE_STATE.md` after the context update and confirm research context freshness.
- Add or update tests/verifiers that fail on stale lane status, missing route artifacts, stale launch order, stale dependency text, missing prompt validation, or incorrect source-family counts.

## Saturation And Self-Red-Team Pass

Before completion, run a written saturation pass and pursue every same-evidence-class gap it exposes. At minimum answer with artifacts:

- Which V3/source-capture lanes are terminal, and what exact disk evidence proves it?
- Which lanes are absent, nonterminal, or blocked, and what exact file/source/verification gap proves it?
- Which stale Master/context/launch-order text would cause a future session to relaunch completed work?
- Which downstream contracts are now open, which remain blocked, and why?
- Which limitations are solved, reduced, converted to source-capture, or still owned by later lanes?
- Which future goal sessions run next, in what order, and what inputs must they consume?
- Which broad research ambition remains outside ML so the program does not collapse into one model lane?
- Which verifier/test prevents the Master from declaring completion from a summary-only or stale state?

If any answer exposes a feasible same-evidence-class inspection, manifest repair, context repair, test update, stale text neutralization, or launch-order correction, do it before completion.

## Completion Standard

Completion requires:

- updated Master builder;
- updated Master route artifacts;
- terminal V3/source-capture state table;
- artifact inspection ledger;
- dependency graph and lane registry updates;
- limitation disposition map;
- source authority map and output schema updates;
- next-wave launch decision;
- anti-duplication and loop-control updates;
- stale dependency text neutralization;
- prompt/launch-order hardening updates where needed;
- source-capture and source completeness decisions where Master owns integration state;
- branch decision and implementation decision ledgers;
- result-use status with exact R, proxy R, and expectancy fields where Master integration owns result materialization, and row-level proof where result fields belong to terminal lane routes;
- updated `research_current_state.md`;
- regenerated `LIVE_STATE.md` with fresh research context;
- route manifest, verifier, focused tests, completion audit, and scoped commit.

No chat-memory closure. No summary-only closure. No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff. Full ledger artifacts must preserve all material lane states, limitation rows, dependency rows, launch decisions, and downstream contracts. No live production activation. Completion means the Master mechanically records terminal V3/source-capture evidence and opens the next lanes cleanly.

## Hard Boundaries

Forbidden surfaces: production-change activation, live trading broker operation, broker account order history deal position mutation, paid API/vendor spend, credential changes or disclosure, remote push, hidden production activation, and prompt/config/risk/execution/safety/canary/selector live behavior changes outside scoped default-off package or dossier authority.

Authorized inside this evidence class: read-only local/repo/source inspection, route artifact parsing, Master builder/test/verifier updates, context refresh, launch-order prompt updates, manifests, no-leak/status checks, branch decisions, implementation decisions, source completeness decisions, result-use status, and scoped commits.
