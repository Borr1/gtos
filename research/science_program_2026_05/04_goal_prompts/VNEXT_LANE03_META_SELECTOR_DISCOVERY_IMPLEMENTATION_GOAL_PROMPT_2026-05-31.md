# Lane 03 - Meta-Selector Discovery And Implementation Goal Prompt

You own the vNext meta-selector lane. Convert the Friday quality discovery and broad selected-denominator replay into a source-bound selector design that improves candidate quality without narrowing the system to one fragile Friday rule.

## Active Context

Run `python scripts/generate_live_state.py`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, and `.context/00_core/research_operating_doctrine.md`. Read Friday microscope artifacts and consume Lane02 outputs as they land on disk. Do not rely on chat memory. After any context compaction, resume, interruption, long wait, tool crash, or uncertainty, regenerate `LIVE_STATE` and reread this starter, this controlling prompt, the doctrine files, lane-specific context, and latest route artifacts from disk before continuing.

## Route Directory

Write route-owned outputs under:

`research/operations/vnext_lane03_meta_selector_discovery_implementation_2026_05_31/`

## Required Work

Discover and implement a meta-selector candidate package from selected-denominator evidence. Start with the known strong mechanisms:

- London liquidity sweep reclaim;
- London displacement continuation;
- current vNext selected-cell risk proof;
- spread/R, volatility, source mode, path class, session, symbol, side, origin family, and cost features.

Then expand the search to every mechanism family and failure pattern available in current artifacts. Preserve all material branches. The lane must answer:

- which mechanisms are robust across broad selected rows;
- which symbols/sessions/origin families must be accepted, reduced, routed differently, or rejected;
- which apparent positives are concentration artifacts;
- which negative families become avoid filters, inverse filters, or source-capture requirements;
- which features are as-of available in live runtime packets;
- which selector clauses become local code/config/tests now.

Evidence-supported clauses become a local meta-selector package with tests and verifier coverage. Clauses not implementable from current live-as-of fields become exact capture repairs in Lane04-compatible terms.

## Completion Standard

This lane must produce exact/proxy R rows, expectancy, branch decisions, source completeness, and implementation decision ledgers. No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff is allowed; preserve all material rows and branches in full ledgers.

Full same-evidence-class pursuit is required. Literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available from the approved source class has been attempted or proven inapplicable with row-level source proof.

There is no conservative brake inside meta-selector discovery and local implementation preparation. Operationalize `.context/00_core/goal_session_research_discipline.md` and `.context/00_core/research_operating_doctrine.md` as active instructions, not background. Use as many highest-reasoning subagents as needed for feature availability, leakage review, selector clause criticism, and broad-family coverage.

External action boundary: live trading broker operation, paid API/vendor spending, credential changes, and hidden production-change deployment are outside the task. Reading broker/account/order/history/deal/position evidence is required when cost or outcome truth is involved. Prompt/config/risk/execution/safety/canary/selector code is this lane's implementation surface.

Finish with selector design, evidence-supported code/config/test package, verifier, manifest, and completion audit.
