# Lane 09 - Cross-Lane Merge Dossier And Production Package Goal Prompt

You own the cross-lane merge dossier and production package. Consume finished lane artifacts from disk, verify claims independently, merge compatible work, expose conflicts, and build the package that says exactly what the next vNext version is, what changed, what is proven, what is not proven, and what runs locally from current code.

## Active Context

Run `python scripts/generate_live_state.py`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, `.context/00_core/orchestrator_successor_operating_brief.md`, `.context/00_core/orchestrator_methodology_hardening_controls.md`, and `.context/00_core/parallel_goal_merge_playbook.md`. Read all completed lane manifests, verification results, ledgers, and commits directly from disk. Do not rely on chat memory. After any context compaction, resume, interruption, long wait, tool crash, or uncertainty, regenerate `LIVE_STATE` and reread this starter, this controlling prompt, the doctrine files, lane-specific context, and latest route artifacts from disk before continuing.

## Route Directory

Write route-owned outputs under:

`research/operations/vnext_lane09_cross_lane_merge_dossier_production_package_2026_05_31/`

## Required Work

For every lane:

- inspect the closeout as a claim, then inspect artifacts directly;
- run route verifier/audit commands present on disk, and write exact absence proof for missing verifiers;
- verify row counts, result totals, source completeness, tests, manifests, and commits;
- compare lane facts against the master registry;
- classify conflicts, overlaps, missing inputs, and merge order.

Build the combined package:

- fixed-Friday reproducibility result;
- broad selected portfolio replay result;
- meta-selector design and code/config package;
- selected-cell risk bridge and packet contract;
- runtime portfolio scheduler package;
- broker net-R/cost/lifecycle truth package;
- 24-symbol coverage/source repair package;
- execution-policy stress result;
- cross-lane production-change dossier with exact behavior deltas, evidence class, test/verifier matrix, rollback surface, and remaining proof gaps.

Run global focused tests and verifiers that match the files changed by the lanes. Stage and commit scoped route/package files after verifying the diff boundaries.

## Completion Standard

Produce merge dossier, production package, route manifest, verification matrix, result materialization ledger, implementation decision ledger, blocker ledger, and completion audit. Every lane claim must be inspected from disk.

No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff is allowed; preserve all material lanes, blockers, conflicts, and decisions. Full same-evidence-class pursuit is required. Literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available from the approved source class has been attempted or proven inapplicable with row-level source proof.

There is no conservative brake inside merge verification, local packaging, and local tests. Operationalize `.context/00_core/goal_session_research_discipline.md`, `.context/00_core/research_operating_doctrine.md`, and `.context/00_core/parallel_goal_merge_playbook.md` as active instructions, not background. Use as many highest-reasoning subagents as needed for independent artifact audit, code diff review, test matrix criticism, and production-dossier red-team.

External action boundary: live trading broker operation, paid API/vendor spending, credential changes, and hidden production-change deployment are outside the task. Reading broker/account/order/history/deal/position evidence is required when lane outputs rely on it. Prompt/config/risk/execution/safety/canary/selector code must be inspected, merged locally, and tested when it is part of a lane package.
