# vNext Moonshot Lane 08 - Digital Twin Replay Engine

Own the current-system as-of digital twin. Build the replay engine that answers: given the current vNext system and only evidence knowable as-of, what would it have generated, selected, scheduled, executed, managed, rejected, or left untouched?

## Mandatory Context Use

Run mandatory preflight. Read doctrine, moonshot vision, master route, Lane01 source authority, Lane02 as-of contract, Lane03 candidate reconstruction, Lane04 microscope, Lane05 feature store, Lane06 labels, Lane07 broker truth when present with exact absent-state row when not, next-level lane package, current runtime code, config, and tests. Do not rely on chat memory. After compaction/resume/interruption/uncertainty, reread prompt/starter/doctrine/latest artifacts from disk.

This is a replay engine builder lane. The output is executable code and row-level replay, not a raw candidate tournament. Treat every component in the chain as a source-bound module with exact input/output contracts.

## Objective

Write route outputs under:

`research/operations/vnext_moonshot_lane08_digital_twin_replay_engine_2026_06_01/`

Build the full chain:

candidate generation -> selector -> selected-cell proof -> meta-selector -> scheduler -> execution policy -> broker constraints -> costs -> lifecycle result.

Run every replay depth supported by source evidence: Friday microscope, daily replay, weekly/monthly replay, broad selected denominator, strict tick subset, sealed historical partitions, forward shadow replay, and broker-real live replay. Unsupported depths get exact missing-source or missing-contract rows, not silence.

## Required Work

- Define replay module contracts and row schemas.
- Build current-runtime-as-of replay builders that consume feature/label/source/broker truth contracts.
- Distinguish raw candidates, selected candidates, broker-ready candidates, accepted trades, actual broker outcomes, projection-only rows, and no-trade outcomes.
- Include cost/slippage/commission/swap/spread, stop/freeze feasibility, session constraints, portfolio exposure, and source completeness.
- Produce exact/proxy R, expectancy, PF, WR, drawdown, concentration, split, and stress metrics when fields exist; when fields are absent, write row-level missing-field proof and the strongest source-bound proxy.
- Add tests that fail if future/outcome fields leak into decision inputs.

## Completion Standard

No arbitrary top-N, no raw-candidate-only tournament as final product, no Friday-only closure. Full same-evidence-class pursuit is required; every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action must be attempted or proven inapplicable.

Forbidden surfaces: live trading broker operation, order action, credential print/change, paid API/vendor calls, remote push, hidden production activation. Read-only MT5/source/broker evidence is authorized for replay realism.

Finish with replay engine code, schemas, row ledgers, stress/split metrics, manifests, verifier, focused tests, and completion audit.

## Operative Hardening Anchor

Read `goal_session_research_discipline.md` and `research_operating_doctrine.md`; do not rely on chat memory. Treat those files as active instructions, not background. This is constructive research/build work with curiosity, active creativity, and no conservative brake inside the evidence class. Full same-evidence-class pursuit is mandatory. The phrase literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action has been attempted or proven inapplicable. No arbitrary top-N, top 3/5/10, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows. Produce artifact, ledger, manifest, verifier or focused test output, completion audit, source-capture or source completeness decisions, branch decision or implementation decision, and exact R, proxy R, or expectancy fields when result materialization is owned; when result fields are absent, produce row-level missing-field proof plus the strongest source-bound proxy or exact capture/export requirement. Scope boundaries are rails around execution, spend, credentials, remotes, and hidden production activation; they are not permission to shrink local/repo/MT5/read-only evidence pursuit. Forbidden surfaces remain production-change activation, live trading broker operation, paid API/vendor calls, broker account order history deal position changes, credential/remotes, and prompt/config/risk/execution/safety/canary/selector changes outside scoped default-off or dossier authority.
