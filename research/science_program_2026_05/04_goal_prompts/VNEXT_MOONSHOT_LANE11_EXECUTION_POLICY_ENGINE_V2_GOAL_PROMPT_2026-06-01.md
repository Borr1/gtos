# vNext Moonshot Lane 11 - Execution Policy Engine V2

Own the tick/broker-realistic execution policy engine. Current production policy is not assumed final. Every exit/policy must survive exact tick ordering, bid/ask, spread, stop/freeze, modify feasibility, cost, and ticket lifecycle realism before it can be recommended.

## Mandatory Context Use

Run mandatory preflight. Read doctrine, moonshot vision, master route, Lane01 source authority, Lane02 as-of contract, Lane04 microscope, Lane06 labels, Lane07 broker truth, Lane08 replay, Lane11 next-level execution microstructure stress, execution/runtime code, and tests. Do not rely on chat memory. After compaction/resume/interruption/uncertainty, reread prompt/starter/doctrine/latest artifacts from disk.

This is an execution simulator and default-off implementation lane. Proxy MFE is not enough. Negative strict-tick results are useful evidence; promising proxy results must become executable tick/broker proof.

## Objective

Write route outputs under:

`research/operations/vnext_moonshot_lane11_execution_policy_engine_v2_2026_06_01/`

Build policy simulations and package candidates for: momentum exhaustion, partial BE runner, trailing runner, time stop, fixed comparator, BE-after-trigger, no-trade, symbol/session/origin-specific routes, hybrids, policy exceptions, and policy kill/reduce rules.

## Required Work

- Simulate policies on selected/broker-ready/replay-eligible denominators using tick for every row with tick coverage and labeled M1 proxy plus exact source-gap reason when tick coverage is unavailable after lane-owned source acquisition is exhausted.
- Model bid/ask side, spread at trigger, same-bar ordering, stop/freeze levels, modify retcodes, latency assumptions, partial-close ticket identity, residual management, commission/swap/slippage, and broker minimum stop distance.
- Produce exact/proxy R, expectancy, PF, WR, max drawdown, loss streak, duration, MFE/MAE, cost stress, and split metrics.
- Identify policy dominance by symbol/session/origin/regime/source completeness.
- Build default-off policy router/package for every policy branch that survives evidence gates; unsupported branches get kill/redesign/capture decisions with row-level reason.
- Add tests for partial/residual ticket binding, rejected modify handling, same-symbol conflict, trailing/time-stop behavior, and future-leak prevention.

## Completion Standard

No arbitrary top-N, no proxy-only promotion, no single-policy collapse. Full same-evidence-class pursuit is required: every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action must be attempted or proven inapplicable.

Forbidden surfaces: live trading broker operation, order action, credential print/change, paid API/vendor calls, remote push, hidden production activation. Execution code is authorized for scoped default-off research/package patches with tests/verifier and runtime-effect boundary.

Finish with policy simulator, policy ledgers, dominance/kill decisions, default-off packages for every surviving branch, kill/redesign/capture decisions for every unsupported branch, manifest, verifier, focused tests, and completion audit.

## Operative Hardening Anchor

Read `goal_session_research_discipline.md` and `research_operating_doctrine.md`; do not rely on chat memory. Treat those files as active instructions, not background. This is constructive research/build work with curiosity, active creativity, and no conservative brake inside the evidence class. Full same-evidence-class pursuit is mandatory. The phrase literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action has been attempted or proven inapplicable. No arbitrary top-N, top 3/5/10, or number-limited cutoff is valid; full ledger artifacts must preserve all material rows. Produce artifact, ledger, manifest, verifier or focused test output, completion audit, source-capture or source completeness decisions, branch decision or implementation decision, and exact R, proxy R, or expectancy fields when result materialization is owned; when result fields are absent, produce row-level missing-field proof plus the strongest source-bound proxy or exact capture/export requirement. Scope boundaries are rails around execution, spend, credentials, remotes, and hidden production activation; they are not permission to shrink local/repo/MT5/read-only evidence pursuit. Forbidden surfaces remain production-change activation, live trading broker operation, paid API/vendor calls, broker account order history deal position changes, credential/remotes, and prompt/config/risk/execution/safety/canary/selector changes outside scoped default-off or dossier authority.
