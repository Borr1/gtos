# Lane 04 - Selected-Cell Risk Bridge And Packet Completeness Goal Prompt

You own the selected-cell risk bridge and live packet completeness lane. The Friday microscope showed sparse selected-cell risk metadata and bridge ambiguity. This lane turns that into repaired runtime/source capture and verifier-enforced packet contracts.

## Active Context

Run `python scripts/generate_live_state.py`, then read `.context/LIVE_STATE.md`, `.context/00_core/current_vnext_system_map.md`, `.context/00_core/current_repo_reading_order.md`, `.context/00_core/quick_reference_card.md`, `.context/00_core/goal_session_research_discipline.md`, and `.context/00_core/research_operating_doctrine.md`. Read Friday microscope artifacts, live companion artifacts, current runtime decision logs, and current config/profile risk files. Do not rely on chat memory. After any context compaction, resume, interruption, long wait, tool crash, or uncertainty, regenerate `LIVE_STATE` and reread this starter, this controlling prompt, the doctrine files, lane-specific context, and latest route artifacts from disk before continuing.

## Route Directory

Write route-owned outputs under:

`research/operations/vnext_lane04_selected_cell_risk_bridge_packet_completeness_2026_05_31/`

## Required Work

Build a row-level bridge audit and repair package for every candidate class where live packets missed or blurred:

- selected-cell risk percent;
- source selected row identity;
- origin family and framework lineage;
- spread/R at candidate time;
- broker symbol/spec geometry;
- repaired geometry state;
- quality/meta-selector feature fields;
- final risk authority;
- old-system absence proof;
- refusal reason, order-readiness reason, and source completeness state.

Patch runtime packet construction, log writers, verifiers, and focused tests for every field obtainable at decision time. Historical rows that remain unrepaired after full same-evidence-class pursuit become exact capture contracts and current-runtime tests.

Do not let generic `selected_cell_risk_not_verified_or_zero`, `framework_not_activated`, bridge-missing, or source-risk labels remain terminal when current artifacts contain enough source to repair them.

## Completion Standard

Produce packet schema, bridge ledger, repair ledger, source completeness ledger, verifier, tests, manifest, and completion audit. Include result materialization status, exact/proxy R linkage whenever source evidence exists, and implementation decisions for every bridge family.

No arbitrary top-N, top 3/5/10, representative-only, or number-limited cutoff is allowed; preserve all material rows. Full same-evidence-class pursuit is required. Literal impossibility means exactly that every executable read, export, search, parser, repair, proxy, ablation, metric, audit, and review action available from the approved source class has been attempted or proven inapplicable with row-level source proof.

There is no conservative brake inside source-capture and packet-repair work. Operationalize `.context/00_core/goal_session_research_discipline.md` and `.context/00_core/research_operating_doctrine.md` as active instructions, not background. Use as many highest-reasoning subagents as needed for runtime path tracing, schema red-team, and bridge-row source audits.

External action boundary: live trading broker operation, paid API/vendor spending, credential changes, and hidden production-change deployment are outside the task. Reading broker/account/order/history/deal/position evidence is required for packet truth when present on disk or extractable. Prompt/config/risk/execution/safety/canary/selector code is this lane's repair surface.
