# G12 No-Fill Source-Correction Consolidated Audit Goal Prompt

## One-Line Starter

`/goal Run G12_NOFILL_SOURCE_CORRECTION_CONSOLIDATED_AUDIT_V1 in C:\tmp\gtos_otb\G12NOFILLCONSOLIDATED as the red-team auditor over all corrected no-fill source lanes after OTI2 V2; use research\science_program_2026_05\06_outcome_testing\g12_nofill_source_correction_consolidated_audit\G12_NOFILL_SOURCE_CORRECTION_CONSOLIDATED_AUDIT_GOAL_PROMPT_2026-05-08.md as controlling instructions; regenerate/read LIVE_STATE, read research_current_state, goal_session_research_discipline, local_heavy_data_inventory, G12 no-fill categorical audit, router, OTI4, OTI1, OTI3, OTI5, and OTI2 artifacts; reconstruct the full 298-row no-fill universe and prior 52 accepted categorical labels, audit every corrected or still-blocked row, decide accept/block/reject for future input-only categorical rebuild only, write durable G12 ledgers, verifier, tests, and next prompt pack; pursue every ambiguity with maximum practical reasoning, curiosity, truthfulness, active creativity, local heavy-data search, proof-or-impossibility, and access requests when needed; no R/performance, broker/account/live/order/hidden labels, validation-safe flip, outcome-review opening, promotion, registry edit, paid/API/Databento call, MT5 order/account/history call, remote push, or live trading surface change.`

## Mission

You are G12, the red-team auditor. Your job is to audit the no-fill source-correction wave as a single connected system, not as isolated fragments.

The goal is not to score performance. The goal is to decide which corrected no-fill rows are source-safe input-only categorical evidence for a later rebuilt packet, which rows remain exactly blocked, which old blockers are superseded, and what the next rebuild prompt must do.

Do not stop at "blocked" until you have either:

1. found the source-safe answer in local artifacts or heavy data,
2. proven the blocker is a true no-leak/source impossibility under the frozen contracts, or
3. written the exact access/source request needed to resolve it.

## Mandatory Preflight

Before writing conclusions:

1. Run `python scripts\generate_live_state.py` and read `.context\LIVE_STATE.md`.
2. Read `.context\00_core\research_current_state.md`.
3. Read `.context\00_core\research_operating_doctrine.md`.
4. Read `.context\00_core\goal_session_research_discipline.md`.
5. Read `.context\00_core\local_heavy_data_inventory.md`.
6. Read the latest handoff named in LIVE_STATE.
7. Read this full prompt after preflight so compaction does not erase the task.

If `generate_live_state.py` needs filesystem access, request it. If any referenced file is missing in this worktree, search `C:\Users\MSI\Documents\ai-trading-agent`, `C:\tmp\gtos_otb`, and documented local heavy-data roots before declaring it missing.

## Context Anchors

Use these as controlling inputs:

- `research/science_program_2026_05/06_outcome_testing/no_fill_lifecycle_categorical_result_packet/`
- `research/science_program_2026_05/06_outcome_testing/g12_no_fill_categorical_result_packet_audit/`
- `research/science_program_2026_05/06_outcome_testing/nofill_blocked_family_source_correction_router/`
- `research/science_program_2026_05/06_outcome_testing/oti4_opening_drive_source_correction_or_contract_revision/`
- `research/science_program_2026_05/06_outcome_testing/oti1_pending_intent_closure_source_packet/`
- `research/science_program_2026_05/06_outcome_testing/oti3_usdjpy_price_only_quote_or_tick_contract/`
- `research/science_program_2026_05/06_outcome_testing/oti2_fill_path_categorical_contract_v2/`
- OTI5 duplicate-conflict artifacts inside `research/science_program_2026_05/06_outcome_testing/nofill_blocked_family_source_correction_router/`
- `.context/00_core/research_current_state.md` lines covering no-fill source corrections and OTI2 V2.

Known current state to verify, not blindly trust:

- Prior G12 accepted `52` categorical lifecycle-only rows as `nofill_terminal_before_entry`.
- Router split the `246` blocked rows into OTI4 `80`, OTI1 `54`, OTI3 `69`, OTI5 `42`, and OTI2/fill-path/ambiguity `1` route territory.
- OTI4 reports `80/80` covered: `69` source-hashed tick-derived range/breakout/as-of proof, `8` no-leak contract blockers, `3` local source gaps.
- OTI1 reports `54/54` source-corrected: `32` no-entry-through-source-window, `22` side-aware entry/fill timestamps routed into fill/path territory.
- OTI3 reports `69/69` resolved: `58` terminal-before-entry categorical evidence, `7` entry-touch-before-terminal routed into fill/path territory, `4` same-timestamp terminal-order ambiguity blockers.
- OTI5 reports `42` duplicate-conflict rows are source-correctable by canonical geometry: keep one canonical row per `nofill_duplicate_key`, rows `NOFILL-CAT-ROW-0001`, `NOFILL-CAT-ROW-0016`, `NOFILL-CAT-ROW-0017`; the other `39` are repeated projections excluded from denominator/label assignment.
- OTI2 V2 reports `34` fill/path rows: `29` source-safe categorical fill/path labels and `5` exact blockers. Label counts are `22` fill before protective no terminal observed, `4` fill before protective before terminal, and `3` fill before terminal before protective. Blockers are the original OTI2 row plus four OTI3 same-timestamp rows.

## Hardening Standard

This session must follow the owner's standing research hardening:

- Curiosity: actively ask what else could resolve or falsify a row. Search prior artifacts, local heavy data, source hashes, and exact row lineage before accepting a blocker.
- Truthfulness: never rescue a bad row by changing the question after seeing evidence. If a row is blocked or negative, say so plainly and preserve why.
- Active creativity: think beyond the first route. If source-safe tick, quote, OHLC, contract, duplicate, timing, or parser reinterpretation can clarify the row without leakage, pursue it.
- Proof-or-impossibility: every unresolved row needs a proof, not a vague note.
- No small-N excuse: if the task says a sample is too small, either expand source-safely from approved data or prove why expansion is not allowed under the frozen contract.
- Negative/blocker learning: write what the blockers teach about future contracts and data capture.
- Anti-boxing: do not assume the current worktree contains all data. Search absolute local paths and prior worktrees when needed, and request access when the sandbox blocks legitimate read-only work.
- Compaction resistance: write a context anchor and completion audit that let a later agent resume without relying on chat memory.

## Required Audit Questions

Answer these with file-backed evidence:

1. Does the corrected source wave reconcile to the exact original `298` no-fill universe?
2. Do the original `52` accepted categorical rows remain accepted without overlap against corrected or blocked rows?
3. Which OTI1, OTI3, OTI4, OTI5, and OTI2 rows are accepted as input-only source evidence for a future rebuilt categorical packet?
4. Which old blockers are superseded, and by what source-hashed or contract-revision evidence?
5. Which rows remain blocked, with exact blocker code, exact missing field/source, and exact next source request?
6. Do the OTI5 canonical duplicate rules remove denominator inflation without losing source identity?
7. Do the OTI2 fill/path labels stay categorical event-order evidence only, with no R/performance or broker/live label leakage?
8. Are the OTI3 four same-timestamp rows truly unresolvable from source-safe intra-row ordering, or is there a local tick/quote artifact that resolves them?
9. Are the OTI4 three May 3 source gaps truly missing under approved local roots, or can they be found in local heavy data without paid/API calls?
10. What exact next lane should run: rebuilt no-fill categorical packet V2, a narrower blocker-clear lane, or both in sequence?

## Required Outputs

Write only under:

`research/science_program_2026_05/06_outcome_testing/g12_nofill_source_correction_consolidated_audit/`

Required artifacts:

- `G12_NOFILL_SOURCE_CORRECTION_CONTEXT_ANCHOR_2026-05-08.md`
- `G12_NOFILL_SOURCE_CORRECTION_UNIVERSE_RECONCILIATION_2026-05-08.md` and `.json`
- `G12_NOFILL_SOURCE_CORRECTION_DECISION_LEDGER_2026-05-08.md` and `.json`
- `G12_NOFILL_SOURCE_CORRECTION_ACCEPTED_ROW_SHORTLIST_2026-05-08.json`
- `G12_NOFILL_SOURCE_CORRECTION_BLOCKER_LEDGER_2026-05-08.json`
- `G12_NOFILL_SOURCE_CORRECTION_SOURCE_HASH_NOLEAK_AUDIT_2026-05-08.md` and `.json`
- `G12_NOFILL_SOURCE_CORRECTION_DUPLICATE_SAMPLE_FLOOR_AUDIT_2026-05-08.md` and `.json`
- `G12_NOFILL_SOURCE_CORRECTION_LEARNING_LEDGER_2026-05-08.md`
- `G12_NOFILL_SOURCE_CORRECTION_NEXT_PROMPT_PACK_2026-05-08.md`
- `G12_NOFILL_SOURCE_CORRECTION_COMPLETION_AUDIT_2026-05-08.md` and `.json`
- A builder script, verifier script, and focused pytest for this audit.

All JSON must parse. Every markdown artifact must include `NO_PROMOTION_VERDICT`.

## Safety Boundaries

Forbidden:

- R/performance scoring, win rate, expectancy, DSR/PBO claims, or promotion claims.
- Broker actual-R, account history, MT5 order/account/history calls, live order/deal/position labels, or hidden labels.
- Scoring blocked CNR061 rows or six T3 rows unless the task explicitly says they are only exclusions.
- Changing `src/`, `prompts/`, `config/`, risk, execution, permissions, safety gates, selectors, canaries, MT5 order/account behavior, credentials, remote state, or registries.
- Setting `validation_safe=true`, `outcome_review_opened=true`, or `live_effect=true`.
- Paid/API/Databento calls. If you think external access is required, write the request instead of spending.

Allowed:

- Read-only local file search.
- Read-only local heavy-data inspection and hashing.
- Requesting filesystem/network/API access if needed.
- Writing scoped research-control artifacts under the output directory.
- Writing tests/verifiers for this audit.

## Verification Required Before Completion

Before marking the goal complete:

1. Run the audit builder/verifier.
2. Run focused pytest for the audit.
3. Run `python -m py_compile` on new scripts.
4. Parse all generated JSON/JSONL artifacts.
5. Verify no generated JSON sets `validation_safe`, `outcome_review_opened`, or `live_effect` true.
6. Verify every output contains or preserves `NO_PROMOTION_VERDICT`.
7. Verify committed diff contains only the scoped G12 audit directory and allowed context refresh files.
8. Verify no forbidden live-surface paths changed.
9. Regenerate LIVE_STATE at closeout and state whether research context is FRESH.
10. Commit the scoped artifacts with `Co-Authored-By: Codex GPT-5 <redacted@example.com>`.

Stop only when the audit has terminal accept/block/reject decisions for every relevant row and the next route is exact.
