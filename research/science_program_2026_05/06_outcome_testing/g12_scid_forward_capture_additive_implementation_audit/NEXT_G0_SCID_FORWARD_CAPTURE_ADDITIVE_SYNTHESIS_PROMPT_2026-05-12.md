# Next G0 Prompt - SCID Forward Capture Additive Synthesis

Follow this prompt only after the G12 audit directory `research/science_program_2026_05/06_outcome_testing/g12_scid_forward_capture_additive_implementation_audit/` is committed.

Objective: synthesize the accepted G12 source/control audit of the SCID forward-capture additive implementation into the science-program control state, then rank the strongest next routes without boxing the program into a passive activation-only loop. Keep this as G0 control synthesis only: no validation/result scoring, no R/PnL/win-rate/expectancy/performance claims, no promotion, no paid/API/vendor access, no credentials/remotes, no order behavior, no risk/safety, no production prompt-decision, no canary/selector change, and no live restart unless separately approved by the owner.

Operate as G0 synthesis, not as a timid summary. Be broad, creative, and route-generating while preserving evidence-class boundaries. Do not collapse the accepted implementation into "wait for live rows" only. The nonblocking activation follow-up must be tracked, but the science program should also preserve and rank downstream routes unlocked by accepted source capture: activation/readiness verification, no-API hypothesis-card preregistration/replay design, LTF/orderflow/proxy validity expansion, source-status monitoring, and later sealed result-packet design only after input/source gates are frozen.

This prompt is intentionally not a negative-warning prompt. Your job is to maximize useful next-route discovery, sequencing, and control-state clarity, not to manufacture caution. If a route is valid inside its evidence class, keep it alive, rank it, and emit a runnable prompt/starter for it. If several routes are independent and non-conflicting, treat them as parallel-ready rather than forcing a single serialized path for convenience.

Mandatory preflight:

1. Run `python scripts/generate_live_state.py`.
2. Read `.context/LIVE_STATE.md`.
3. Read `.context/00_core/goal_session_research_discipline.md`.
4. Read `.context/00_core/research_operating_doctrine.md`.
5. Read `.context/00_core/research_current_state.md`.
6. Read the G12 audit completion audit and decision ledger:
   - `research/science_program_2026_05/06_outcome_testing/g12_scid_forward_capture_additive_implementation_audit/G12_SCID_FORWARD_CAPTURE_COMPLETION_AUDIT_2026-05-12.json`
   - `research/science_program_2026_05/06_outcome_testing/g12_scid_forward_capture_additive_implementation_audit/G12_SCID_FORWARD_CAPTURE_ADDITIVE_IMPLEMENTATION_DECISION_LEDGER_2026-05-12.json`
7. Read the G12 test/verifier reproduction result and finding ledger.
8. Read the accepted target implementation ledgers that affect route selection:
   - `research/science_program_2026_05/06_outcome_testing/scid_forward_capture_additive_implementation_from_parallel_g12_wave/SCID_FC_ADDITIVE_IMPL_HYPOTHESIS_FACTORY_COMPATIBILITY_2026-05-12.json`
   - `research/science_program_2026_05/06_outcome_testing/scid_forward_capture_additive_implementation_from_parallel_g12_wave/SCID_FC_ADDITIVE_IMPL_LTF_ORDERFLOW_PROXY_COMPATIBILITY_2026-05-12.json`
   - `research/science_program_2026_05/06_outcome_testing/scid_forward_capture_additive_implementation_from_parallel_g12_wave/SCID_FC_ADDITIVE_IMPL_RESTART_LEDGER_2026-05-12.json`
   - `research/science_program_2026_05/06_outcome_testing/scid_forward_capture_additive_implementation_from_parallel_g12_wave/SCID_FC_ADDITIVE_IMPL_VERIFIER_RESULT_DEFAULT_2026-05-12.json`

Hardening:

- Do not rely on chat memory or closeout messages.
- Do not invent limitations for fake productivity.
- Do not turn the nonblocking activation follow-up into a repair blocker.
- Do not treat `row_count=0` from the default verifier as a reason to wait passively; it means active processes have not yet been reloaded or no eligible row has arrived.
- Do not open result scoring or validation in this G0 lane.
- Do rank every useful next route made possible by accepted source capture, including routes outside current OB-only framing.
- Do not let "source/control only" become a synonym for narrow. In this prompt it means auditable, redacted, as-of-bound, no-leak, and non-promotional while still being maximally curious.
- Do not make the current GTOS fields, OB logic, or existing live candidate stream the outer boundary of the research program. The accepted SCID capture substrate exists to support broader hypothesis families once their source fields are defined.
- Do not choose a rank-1 route only because it is safest to explain. Choose it because it gives the highest evidence gain per dependency while keeping other high-value independent routes available.
- If an activation/monitoring route is selected, make it source/control only: controlled reload timing, post-reload row-landing verification, no decision behavior change, no result scoring.
- If a hypothesis/replay route is selected, make it no-API and preregistered/source-bound first: map 40 cards/8 domains to source fields, freeze eligibility and denominators, then only later open a result packet after G12/G0 gates.

Expected G0 output:

- Record the terminal G12 decision exactly: `ACCEPT_WITH_EXACT_NONBLOCKING_ACTIVATION_FOLLOWUPS`.
- Record that G12 accepted the additive implementation as source/control implementation evidence only.
- Build a route-ranking matrix with at least:
  - activation/readiness verification after controlled reload or normal orchestrator reload;
  - no-API hypothesis-card preregistration and replay/input design;
  - LTF/orderflow/proxy source-status and validity expansion;
  - forward source-capture monitoring/health guard;
  - sealed result-packet design gate, explicitly blocked until source/input activation and preregistration gates are satisfied.
- Emit the rank-1 next prompt and one-line starter. Also emit prompt packs/starters for any parallel-ready route that is high-value, non-conflicting, and not genuinely dependency-blocked. Make each prompt maximally clear, non-lazy, non-conservative, and bounded to its evidence class.
- Emit a blocker/follow-up ledger that distinguishes nonblocking activation follow-up from actual repair blockers.
- Emit a saturation/self-red-team ledger answering whether G0 accidentally boxed the program into only activation waiting, only OB edge, only current GTOS fields, or only live-forward data.
- Emit a sequencing ledger that says which routes should run now, which can run in parallel, which require activation/live row landing first, and which require later G12/G0 gates before result scoring.
- Emit a "not in a loop" ledger explaining why the recommended sequence advances from source capture toward edge discovery instead of rebuilding infrastructure for its own sake.
- Update `.context/00_core/research_current_state.md` only if it is stale versus the committed G12 audit.
- If preparing an operational follow-up, keep it monitoring/activation-honesty only: reload/restart affected orchestrators only under owner-approved operational timing, then run `python scripts/verify_scid_forward_capture_schema.py --json` without `--allow-empty` only when live rows are expected.
- Preserve the nonblocking nature of no-restart/no-live-row landing. Do not convert it into a repair blocker or promotion claim.
- Commit only scoped G0 synthesis/context files.

Safe flags must remain `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, and `live_effect=false`.
