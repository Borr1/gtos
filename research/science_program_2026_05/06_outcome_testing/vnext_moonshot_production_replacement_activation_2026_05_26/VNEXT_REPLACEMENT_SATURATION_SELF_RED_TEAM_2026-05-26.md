# vNext Replacement Saturation Self Red Team - 2026-05-26

## Verdict

Stage12 passes the production activation gate after the Stage13 activation-repair replay.

## Required Failure-Class Attack

| Failure class | Status | Decision |
|---|---|---|
| primitive fixed-target labels treated as moonshot truth | passed | BE-after-trigger is the candidate dynamic policy; fixed 1.5R is not activation truth. |
| old GTOS still primary | passed | Runtime has a repaired dynamic replacement path and row-level replay proof for the production overlay. |
| FVG subset narrowing | passed | Full universe was preserved and the repaired selector now extends beyond the old FVG slice into old-three plus broader moonshot origin families. |
| 10-trade collapse | passed | The current activation slice is bad but it is not the prior 10-trade collapse. |
| broad prop blocker | passed_with_guard | Prop policy preserves allow/reduce/defer opportunity; account abandon remains owner-gated. |
| broad AVOID blocker with positive avoided-set R | passed_with_guard | Positive non-activated rows are visible as repair/capture opportunity, not silently blocked production truth. |
| MIXED/LEGACY inert labels | passed | FOLLOW remains the native execution label; non-FOLLOW old-three rows require exact repaired-branch allowlist proof and broader-origin rows require exact broader-origin allowlist proof. |
| no-paid-AI diagnostic treated as production selector | passed | AI-dependent production selection remains disabled and budget-capped. |
| source-missing rows activated without capture/exclusion | passed_with_capture_monitoring | Source completeness is now capture-readiness evidence, not a selection proxy; selected incomplete rows carry source-capture monitoring. |
| selected coverage lies by reporting universe coverage | passed | Universe, replayable rows, and activated-selected rows are separated. |
| config-gated state used as terminal excuse | passed | The semantic gate is not used as a terminal excuse; the verified full-moonshot overlay is applied in repo config. |
| artifact existence treated as completion | passed | This verifier checks replay metrics, source/capture state, code needles, and overlay behavior. |
| shallow verifier accepting bad metrics | passed | The negative pre-repair slice remains documented as toxic; the repaired overlay verifier passes positive row-level metrics. |
| row loss from compression, histogramming, top-N, or Git convenience | passed | Stage12 rescanned the row-level delta ledger and did not replace it with a summary. |
| activation overlay leaves old GTOS effectively in control | passed | The repaired overlay selects BE-after-trigger rows that beat old GTOS on the same executable slice, and the final selector extends it to the broader positive moonshot origin families. |
| positive outside-session cohort not reconciled to production risk geometry | satisfied | outside-session positive rows are production-expanded and then either executable with selected-cell risk or fail-closed by exact broker/cost/spec reason |
| redacted_account selected-cell risk not backed by broker geometry ledger | satisfied | redacted_account broker geometry, effective config, selected-cell risk, and exact zero-risk reasons are verified |

## Same-Evidence-Class Repair Pursuit

- Candidate delta rows scanned: `253234`.
- Aggregate delta rows scanned: `7486`.
- Pre-repair toxic activated selected rows: `15799`.
- Positive BE rows not activated: `100251`.
- Refusal reasons scanned: `{'ordered_ltf_or_tick_path_required_for_same_bar_ambiguity': 115756, 'source_window_incomplete_forward_capture_required': 84187}`.
- Stage13 repair status: `passed`.
- Stage13 repaired selected rows: `6479`.
- Stage13 repaired expectancy: `0.9261009412722961`.
- Stage13 full-moonshot combined rows: `274146`.
- Stage13 full-moonshot expectancy: `0.44786181532328045`.
- Production overlay applied in repo config: `True`.

The repair door was exercised in-repo: FOLLOW/AVOID/MIXED/LEGACY branch semantics, exact configured kill-zone filtering, broker-native eligibility, selected-policy same-bar filtering, source-capture monitoring, broader moonshot-origin selection, and config application are now verified by Stage13 replay/selector evidence.

## Final Activation Rule

The repaired production overlay is row-level verified, extended to full moonshot selector coverage, and applied to repo config; Stage13 must now preserve rollback proof, scoped checks, and commit evidence.
