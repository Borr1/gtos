# NOFILL CAT V3 Result-Contract Update Goal Prompt - 2026-05-09

Promotion posture: `NO_PROMOTION_VERDICT`.

## One-Line Starter

`/goal Build NOFILL_CAT_V3_RESULT_CONTRACT_UPDATE as a hardened source-safe result-contract/control lane using this prompt file as controlling instructions; read fresh GTOS state and all named V3/G12/context artifacts, pursue every same-evidence-class ambiguity to proof-or-impossibility with maximum curiosity/truthfulness/active creativity, freeze exactly how future quarantined categorical scoring may consume the 225 accepted V3 input rows while excluding 4 source-control rows, 4 source-impossible rows, and 65 rejects, produce committed files plus verifier/tests/next prompt packs, request access when needed, and do not score outcomes or touch live trading surfaces.`

## Objective

Build a frozen result-contract/control package for a future no-fill V3 quarantined categorical scoring lane. This is not the scoring lane. The job is to convert the accepted G12 V3 input evidence into a precise, testable, source-safe contract so the next lane cannot accidentally launder source-control rows, source-impossible rows, rejects, duplicate projections, hidden labels, or post-outcome fields into a result.

The goal is complete only when the lane has produced durable files, verifier code, focused tests, and next prompt packs that make the future scoring lane unambiguous.

## Mandatory Preflight And Context Anchors

Regenerate and read current state before reasoning:

1. Run `python scripts/generate_live_state.py` and read `.context/LIVE_STATE.md`.
2. Read the latest numbered handoff in `.context/02_session_handoffs/`.
3. Read `.context/00_core/quick_reference_card.md`.
4. Read `.context/00_core/research_operating_doctrine.md`.
5. Read `.context/00_core/research_current_state.md`.
6. Read `.context/00_core/goal_session_research_discipline.md`.
7. Read `.context/00_core/local_heavy_data_inventory.md`.
8. Skim `.context/00_READING_ORDER.md`.

Then read these controlling artifacts directly from disk, not from chat memory:

- `research/science_program_2026_05/06_outcome_testing/nofill_cat_v3_source_control_rebuild/`
- `research/science_program_2026_05/06_outcome_testing/g12_nofill_cat_v3_source_control_audit/`
- `research/science_program_2026_05/06_outcome_testing/g12_nofill_cat_v3_source_control_audit/G12_NOFILL_CAT_V3_NEXT_PROMPT_PACK_2026-05-09.md`
- `research/science_program_2026_05/06_outcome_testing/no_fill_lifecycle_result_contract_design/`
- `research/science_program_2026_05/06_outcome_testing/g12_no_fill_result_contract_audit/`
- `research/science_program_2026_05/06_outcome_testing/nofill_cat_v2_quarantined_categorical_synthesis_forensics/`
- `research/science_program_2026_05/06_outcome_testing/g12_nofill_cat_v2_quarantined_categorical_synthesis_forensics_audit/`
- Any V3 source/hash/duplicate/no-leak artifacts they reference.

Record exact commit SHA, source artifact paths, and source hashes where available.

## Hardening Doctrine

Use the owner's standing research doctrine as binding:

- Curiosity: actively look for what would make the system better, not only what is already obvious in the prompt.
- Truthfulness: do not rescue a failed or blocked idea by inventing evidence; negative and impossible findings are valuable.
- Activity and creativity: if the current route exposes a source-safe ambiguity, pursue it within this goal until cleared, proven impossible, or exactly externally blocked. Do not stop at taxonomy if the same evidence class can still be resolved.
- Proof-or-impossibility: every blocker must become cleared, proven impossible from approved routes, or left with the exact next source/access requirement.
- Anti-boxing: do not restrict yourself to one timeframe, one folder, one worktree, one prior agent's summary, or one obvious source if the contract question requires more. Worktree absence is not data absence.
- Context preservation: write the reasoning, searched paths, unresolved questions, and next routes into artifacts so context compaction cannot erase them.
- Small-N is not a stopping excuse for contract design. If sample size matters, freeze how larger future cohorts would be added without leakage rather than using current N as a reason to avoid the contract.
- Negative result learning matters. The contract must preserve enough diagnostic fields for future forensics to understand why no-fill categories fail or succeed.

## Saturation And Self-Red-Team Requirement

Before marking complete, run a deliberate saturation pass and write it into the artifacts. This pass must not be a generic checklist. It must answer the hard questions that could break the future scoring lane:

- What exact mistake would make source-control rows look like accepted result rows?
- What exact mistake would let source-impossible USDJPY rows leak back into denominators?
- What exact mistake would let the 65 rejects influence sample size, label counts, or future interpretation?
- Which fields look harmless but are actually post-outcome, post-cancel, broker-realized, hidden-label, path-label, or future-context fields?
- Which duplicate-key or canonical-projection choice could double-count opportunity?
- Which rows are eligible only under row-level counting, and which remain eligible under duplicate-key counting?
- Which label family is most likely to be over-interpreted as performance, and how does the contract prevent that?
- Which local heavy-data roots or prior worktree artifacts could change the contract if discovered, and were they searched or explicitly out of scope?
- What would a skeptical G12 audit reject, and how does this contract preempt that rejection?
- If the next scorer finds an ambiguity, should it score, exclude, split, or route back to source-control? Freeze the rule.
- If a future larger cohort is added, what exact fields and as-of rules must be present before it enters the same denominator?
- What did this contract deliberately not answer, and what exact future lane owns it?

If any answer exposes a same-evidence-class gap, pursue it inside this goal until it is resolved, proven impossible, or converted into an exact external requirement. Do not use "outside scope" as a shortcut unless crossing into scoring, G12 acceptance, registry edit, validation, promotion, live trading behavior, paid spend, or forbidden broker/account/order evidence.

## Evidence Boundary

Allowed:

- Reading repo artifacts and local heavy data roots as needed for source-field and no-leak contract verification.
- Read-only local data/source inspection.
- Read-only web/curl/webfetch for official documentation, source-contract rules, or timestamp/as-of semantics if needed.
- Requesting access when the sandbox, file permissions, local data roots, or source reads block the objective.

Not allowed in this lane:

- Outcome scoring, R scoring, win rate, expectancy, DSR/PBO result computation, or promotion claims.
- Broker actual-R, account history, live order/deal/position labels, hidden result labels, MT5 order/account/history calls, or live execution behavior.
- Registry edits, `validation_safe=true`, `outcome_review_opened=true`, `live_effect=true`, selector changes, live prompt changes, risk/execution/permissions/safety/canary/MT5-order behavior changes.
- Paid/API/Databento spend unless a separate written owner cap and source contract are present. Existing local caches may be read and hashed.

## Required Contract Decisions

Freeze these decisions in machine-checkable JSON plus human-readable markdown:

1. Universe: `298` V3 rows exactly once.
2. Scoring-eligible future universe: only the `225` accepted V3 input-only categorical rows unless your artifact proves a narrower source-safe denominator is required.
3. Non-denominator source-control rows: `NOFILL-CAT-ROW-0049`, `0050`, `0051`, `0241` must be excluded from future scoring denominators unless a separate future G12 gate changes evidence class.
4. Source-impossible rows: `NOFILL-CAT-ROW-0130`, `0143`, `0165`, `0178` must remain excluded unless a broker-native USDJPY quote-event sequence source with sequence ID or sub-row/sub-millisecond timestamp appears without account/order/history labels.
5. Reject rows: all `65` rejects remain outside labels, denominators, result use, validation, promotion, and live effect.
6. Label-family separation: categorical lifecycle/source labels must stay separate from synthetic path-R, broker actual-R, live order outcomes, lifecycle/no-fill labels, source-control-only rows, source-impossibility rows, and observation-only rows.
7. Duplicate policy: freeze row-level and duplicate-key denominators, canonical/noncanonical projection handling, and how duplicate groups affect counts.
8. Source/no-leak schema: enumerate all allowed decision-time fields, prohibited post-outcome fields, required as-of timestamps, source hashes, and stale-source rules.
9. Sample-floor and methodology: state which future metrics are computable, not computable, or categorical-only; include DSR/PBO/effective-N handling or explicit `not_computable_until_scoring_lane` reasons.
10. Future lane requirements: specify exactly what the future scoring lane must read, what it must exclude, what tests it must pass, and what G12 audit must review afterward.

## Required Artifacts

Write only under:

`research/science_program_2026_05/06_outcome_testing/nofill_cat_v3_result_contract_update/`

Minimum artifact set:

- `NOFILL_CAT_V3_RESULT_CONTRACT_CONTEXT_ANCHOR_2026-05-09.md`
- `NOFILL_CAT_V3_RESULT_CONTRACT_FROZEN_RULEBOOK_2026-05-09.md`
- `NOFILL_CAT_V3_RESULT_CONTRACT_FROZEN_RULEBOOK_2026-05-09.json`
- `NOFILL_CAT_V3_RESULT_CONTRACT_ELIGIBILITY_LEDGER_2026-05-09.jsonl`
- `NOFILL_CAT_V3_RESULT_CONTRACT_EXCLUSION_LEDGER_2026-05-09.jsonl`
- `NOFILL_CAT_V3_RESULT_CONTRACT_SOURCE_NOLEAK_SCHEMA_2026-05-09.json`
- `NOFILL_CAT_V3_RESULT_CONTRACT_DUPLICATE_SAMPLE_POLICY_2026-05-09.md`
- `NOFILL_CAT_V3_RESULT_CONTRACT_SATURATION_REVIEW_2026-05-09.md`
- `NOFILL_CAT_V3_RESULT_CONTRACT_LEARNING_AND_RISKS_2026-05-09.md`
- `NOFILL_CAT_V3_RESULT_CONTRACT_NEXT_PROMPT_PACK_2026-05-09.md`
- `NOFILL_CAT_V3_RESULT_CONTRACT_COMPLETION_AUDIT_2026-05-09.md`
- `NOFILL_CAT_V3_RESULT_CONTRACT_COMPLETION_AUDIT_2026-05-09.json`
- Builder, verifier, and focused pytest file.

## Verification Requirements

Before completion:

- JSON/JSONL parse must pass.
- Verifier must assert exact row counts: `298 = 225 accepted + 4 source_control + 4 source_impossible + 65 reject`.
- Verifier must assert `0` scoring/result records are produced.
- Verifier must assert no `validation_safe=true`, `outcome_review_opened=true`, or `live_effect=true`.
- Verifier must assert all artifacts preserve `NO_PROMOTION_VERDICT`.
- Verifier must assert forbidden live-surface committed diff is empty.
- Focused pytest must pass.
- Completion audit must say `can_mark_goal_complete=true` only if all required artifacts and verification checks pass.

## Stop Condition

Do not mark complete until:

- The future scoring contract is frozen and unambiguous.
- Every same-evidence-class ambiguity found during contract design has been pursued to clearance, proof of impossibility, or exact external requirement.
- All artifacts are committed.
- The worktree is clean except allowed generated `.context/LIVE_STATE.md` if closeout regeneration dirties it.
