# DECIDE_READY8_SCID_AND_RELATED_GEOMETRY_RESEARCH_DIRECTION - Decision Memo (2026-05-16)

## Terminal Decision

**MERGE_READY8_SCID_INTELLIGENCE_INTO_A_BROADER_SYSTEM_BUT_DO_NOT_CONTINUE_IT_AS_STANDALONE**

This closes the READY8 SCID standalone ladder. READY8 SCID produced useful source-bound intelligence, but it did not produce a standalone scoreable trade strategy. The right move is to merge its useful intelligence into broader replayable geometry/moonshot/GTOS research and stop continuing READY8 SCID as its own G0/G12/repair/capture-requirement chain.

Safe flags remain: `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, `live_effect=false`.

## What READY8 SCID Is Useful For

READY8 SCID is useful as a neutral movement and failure-intelligence source. Disk evidence confirms 5,320 repaired target rows with source-bound neutral movement: 3,688 positive, 1,512 negative, and 120 zero, with mean neutral movement 0.0028383067873157895. It also preserved 2,641 failure-intelligence rows, 143 HAZ001 forward-retest rows, 32 MAC inverse/avoid-filter rows, 5 HAZ005 source-control rows, 1,154 R10 UNC004 packet-family rows, and residual/control/concentration/source-limited diagnostics.

That is real research value. It should become design priors, filters, avoid rules, source-confidence diagnostics, source-capture fields, and control rules inside the broader system.

## What READY8 SCID Is Not Useful For

READY8 SCID is not useful as a standalone trade strategy from current accepted evidence. G12 R11 audits 5,502 geometry rows, 5,502 capture requirements, 51 source roots, and 80 weak symbol-time overlaps, but leaves 0 exact R rows and 0 target/stop hit/miss rows. R10/G12 R10 already attempted 5,502 row-level geometry repairs and still left exact R/expectancy and target/stop scoring at 0 computed rows.

The missing side/entry/stop/target/fillability/path/cost geometry is not another blocker to hand off. It is the decision evidence: READY8 SCID cannot continue as a standalone scoreable path.

## Evidence Against The Other Decisions

`TRADEABLE_NOW_WITH_RECONSTRUCTED_OR_PROXY_GEOMETRY` is rejected because accepted disk evidence gives 0 exact R rows and 0 target/stop rows. The neutral movement table is useful, but it is not trade geometry.

`TRADEABLE_AFTER_FORWARD_GEOMETRY_CAPTURE_IMPLEMENTED` is rejected as the standalone direction because it would continue the READY8 SCID ladder. Forward capture may be useful, but it belongs in general executable geometry infrastructure, not another READY8-only route.

`READY8_SCID_NOT_THE_RIGHT_PATH_REDIRECT_TO_MOONSHOT_OR_OTHER_REPLAYABLE_GEOMETRY` is partly right but too destructive. Moonshot/replayable geometry is stronger, but READY8 SCID has useful intelligence that should not be discarded.

`READY8_SCID_USEFUL_ONLY_AS_NEUTRAL_MOVEMENT_INTELLIGENCE_NOT_TRADE_STRATEGY` is partly right but too narrow. Neutral movement is the strongest numeric contribution, but the failure-intelligence, source-bias, avoid-filter, and branch diagnostics also matter.

## Broader Geometry Evidence

The moonshot/replayable geometry artifacts dominate READY8 SCID for executable strategy research. Disk evidence includes 39,003,954 tick rows, 8,977 primitive events, 22,229 target events, 3,128 path-geometry rows, 386 branch-router application rows, 1,930 family spec rows, 243 positive specs, and 141 M1 spread-adjusted threshold-fill rows. These artifacts are still research/no-promotion material, but they are the stronger substrate for actual scoreable geometry because they contain replay/path/fill/target-stop proxy machinery READY8 SCID lacks.

## System Action

Stop READY8 SCID as a standalone route now. Do not launch another READY8 SCID G0, G12, audit, capture-requirement-only packet, or proof-of-impossibility packet. Merge its neutral movement and failure intelligence into broader replayable geometry/moonshot/GTOS design work. Prioritize executable geometry routes that can bind or replay side, entry, stop, target, fillability, path ordering, and cost/slippage from source-bound historical evidence.

## Completion Audit

- Preflight was rerun with `py scripts/generate_live_state.py`; `python` was unavailable on this host, so the functional launcher was used.
- AGENTS.md, CLAUDE.md, `.context/LIVE_STATE.md`, the latest handoff, quick reference, successor brief, doctrine files, methodology controls, merge playbook, route registry, question ledger, controlling prompt, starter, READY8 R7-R11/G12 artifacts, and moonshot artifacts were read from disk and hashed in the decision ledger.
- Accepted source artifacts verify 5,320 repaired target rows, 3,688 positive / 1,512 negative / 120 zero neutral rows, 5,502 geometry rows, 80 weak overlaps, 51 searched roots, 0 exact R rows, 0 target/stop hit/miss rows, and 2,641 failure-intelligence rows.
- All five allowed terminal decisions were evaluated; exactly one was selected.
- The selected decision is not a missing-geometry blocker, proof-of-impossibility packet, capture-only packet, audit route, G0, G12, prompt-hardening route, next prompt, or next starter.
- JSON/JSONL parse and one-of-five invariant checks are recorded in `DECIDE_READY8_SCID_RESEARCH_DIRECTION_DECISION_CHECK_RESULT_2026-05-16.json`.
- Safe flags remain `NO_PROMOTION_VERDICT`, `validation_safe=false`, `outcome_review_opened=false`, and `live_effect=false`.
- The scoped artifacts do not change live, promotion, broker, order, account, history, deal, position, prompt, config, risk, safety, execution, canary, selector, AI/API, paid/vendor, raw-blob, credential, remote-push, or live-trading surfaces.
