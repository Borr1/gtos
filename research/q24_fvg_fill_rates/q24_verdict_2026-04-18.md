# Q-2.4 / B7 — Verdict: FVG gap fill rates

**Date:** 2026-04-18
**Author:** GTOS Claude Code (session 26)
**Report:** `research/q24_fvg_fill_rates/q24_report_2026-04-18.md`

---

```
VERDICT: DEFER (shadow-log gap/ATR only — no live trade-gate integration pre-challenge)
CONFIDENCE: HIGH
ONE-LINE: Cross-symbol fill rates are homogeneous (no per-symbol filter justified) and per-FVG fill probability is NOT shown to be orthogonal to the existing OB retest edge; the only defensible action is to enrich the R2 candidate features logger with gap/ATR magnitude and re-evaluate post-challenge.

EVIDENCE FROM REPORT:
- Full-fill @ H=20 M15 bars (~5h) pooled rate: 70.0% (n=51,593).
- Per-symbol full-fill @ H=20: XAUUSD 66.6% / GBPUSD 70.4% / USDJPY 71.4% / GBPJPY 71.5% / US30 69.1%. Max absolute deviation from pooled = 3.4pp (XAUUSD). Pre-registered ≥10pp economic threshold NOT met.
- Size-stratified full-fill @ H=20 (gap/ATR14 buckets, pooled):
    [0, 0.5) → 76.0% (n=29,655)
    [0.5, 1.0) → 66.4% (n=15,363)
    [1.0, 2.0) → 54.9% (n=5,408)
    [2.0, ∞) → 36.4% (n=1,158)
  Monotone within every symbol. Chi-square across buckets: chi2=1,814, df=3, p≈0.
- WR conditional on fill / no-fill: NOT REPORTED. Report never ties FVG-fill to trade outcomes — it measures fill probability of detected FVGs, not downstream PnL.
- Methodology: pre-registered chi-square; FVG detection copied verbatim from `src/components/market_state.py::identify_fvgs` using production `fvg_min_gap` thresholds; fill definition matches production `filled` flag; horizons [5, 10, 20, 50, 100] M15 bars; censoring handled (<0.2% at H=100).
- Cross-symbol chi-square @ H=20: chi2=62.289, df=4, p=9.575e-13 (statistically detectable, economically minor).

STATISTICAL RIGOR:
- n: overall 51,593 full-fill trials @ H=20; smallest per-symbol cohort = XAUUSD 5,776. Size buckets: smallest [2.0, ∞) = 1,158 pooled.
- Multi-comparison correction: Bonferroni alpha/10 = 0.005 applied to 10 pairwise 2x2 tests. Pre-registered before looking at data.
- Pre-registered or post-hoc: PRE-REGISTERED. Hypothesis, tests, sample thresholds, and buckets committed before touching fill numbers (per report header).
- Weakness: the stratification by gap/ATR is a discovery within the pre-registered scope (not a separate post-hoc fishing expedition), but it was NOT pre-specified as the primary test — treat the gap/ATR finding as exploratory and require live replication before weighting it as a trade gate.

INDEPENDENCE FROM EXISTING OB EDGE:
- Is FVG-fill signal shown to be orthogonal to OB retest? NOT TESTED. The report never joins FVG events to OB retest events, never measures trade WR conditional on FVG-fill prediction, and never asks whether large (low-fill) FVGs coincide with OB retest setups. This is the primary gap.
- Lift beyond the already-known FVG-in-impulse signal (+7–20pp)? NOT ESTABLISHED. The existing FVG-in-impulse edge is binary (present/absent inside an impulse leg). This report measures a different quantity (fill probability of any detected FVG). They are conceptually related but neither subsumes nor complements the other in the evidence presented.
- Redundancy assessment: PARTIAL OVERLAP (suspected, unquantified). Large FVGs (≥ 2×ATR) with low fill rates are plausibly the same displacement events that create clean OB retest setups — i.e., a large FVG and the originating OB are generated by the same impulse. Without a joint analysis the signal's marginal value is unknown.

IF SHIP (path NOT taken, but scoped for completeness):
- Integration surface: SHADOW-ONLY enrichment of R2 candidate features logger.
- Files affected: `src/components/candidate_features_logger.py` (primary — add `gap/ATR` magnitude features); no changes to `src/components/market_state.py`, no changes to `prompts/`, no changes to `config/agent_config.yaml`.
- Diff estimate: ~30–50 lines in candidate_features_logger.py — iterate `fair_value_gaps` on H1/M15 TF states, compute `(top - bottom) / atr_14` per FVG, aggregate (max, mean, count of ≥2×ATR). `FairValueGap.top`/`bottom` already exist (`market_state.py:606-612`); ATR14 already available (`candidate_features_logger.py:400`).
- Pre-challenge safe (before Tuesday 2026-04-21)? YES for shadow-only enrichment (no trading-logic change, no prompt change, observation-only — matches the R2 logger's stated contract at `candidate_features_logger.py:1-17`).
- If NO: N/A. But any filter / entry trigger / prompt context addition is NOT pre-challenge safe and must wait for post-challenge review.
- CEO approvals needed:
    1. Approval to enrich the R2 shadow logger (low-risk: observation-only, failure-isolated per file header contract).
    2. Explicit rejection of any live trade-gate change pre-challenge.

IF KILL (path PARTIALLY applies):
- KILL the cross-symbol heterogeneity hypothesis: no symbol deviates ≥10pp from pooled; per-symbol fill-rate filters are NOT justified. The pre-registered H1 is not supported. Do not build symbol-specific FVG logic.
- Salvage: gap/ATR size stratification is a real, monotone, large-magnitude finding (76.0% → 36.4%, p≈0) and merits shadow collection.

IF DEFER (chosen path):
- Revisit trigger:
    1. After the redacted_account Stellar 2-Step $100K challenge completes (post 2026-04-21 start, horizon TBD), review accumulated R2 shadow log for live correlation between gap/ATR feature and trade outcome (WR, R-multiple).
    2. Minimum sample: n ≥ 30 CANDIDATE evaluations where the MSO contains an FVG with measurable gap/ATR, stratified across the four ATR buckets.
    3. Explicit independence test against OB retest signal required before any promotion: measure WR of trades where (a) OB retest alone fires, (b) FVG-fill prediction alone fires, (c) both fire. If (c) ≠ (a), FVG-fill adds lift.
- What's missing from current data:
    1. Trade-outcome conditioning: the report measures fill probability of detected FVGs, NOT WR of trades taken near those FVGs. Policy decisions require the latter.
    2. OB-independence joint analysis: per-FVG co-occurrence with OB retest setups is unquantified.
    3. H1 / H4 fill dynamics (report explicitly scopes to M15 only per caveats section).
    4. Live pre-registration of the gap/ATR effect — currently exploratory within the pre-registered study.

RISKS / UNKNOWNS:
- Overfitting risk: LOW for the cross-symbol test (pre-registered, Bonferroni-corrected). MEDIUM for the gap/ATR finding (discovered inside the study rather than pre-specified as primary). Very large n keeps sampling variance low, but the economic question (does this change trade selection?) is untested.
- Redundancy risk: HIGH. Large FVGs and OB retest setups likely share underlying impulse events. Without joint analysis the gap/ATR signal may be measuring the same thing the existing OB edge already captures. Shipping a filter or prompt signal based on this data risks adding complexity without lift — the exact failure mode the edge-mechanism doc warns against.
- Implementation complexity vs edge magnitude: a live filter based on gap/ATR would touch `primary_analyzer.py` and/or the T7 prompt (frozen). Expected lift is unquantified. Shadow-logging first is the only proportionate response.
- Prompt-frozen constraint: the T7 C-gate prompt is deliberately minimalist (H1 bias / M15 not opposing / direction match). Adding FVG gap-size context to the prompt would contradict the T5→T8 finding that Q-scores are uncorrelated with wins (r=-0.06). Evidence for prompt enrichment is currently absent.
- Challenge-start timing: redacted_account Stellar 2-Step starts in 3 days (2026-04-21). Any non-shadow change is gratuitous risk.

CEO DECISION POINTS:
1. Approve adding `gap/ATR` (max, mean, count-over-2x-ATR) to the R2 candidate features logger — shadow-only, observation-only, pre-challenge safe. ~30–50 LOC diff in `src/components/candidate_features_logger.py` only.
2. Explicitly REJECT any live trade-gate, filter, entry-trigger, or T7 prompt change based on Q-2.4 before the redacted_account challenge starts.
3. Approve post-challenge re-evaluation on accumulated live R2 data, with an independence test vs OB retest as a precondition for any promotion.
4. Endorse KILL of the per-symbol fill-rate-filter hypothesis (no symbol deviates ≥10pp; pre-registered threshold not met).
```

---

## Notes on scope and defaults

Per CLAUDE.md default skepticism: the system's validated edge is OB zone precision, and prior prompt work (T5→T8) shows that adding evaluation signals without discriminative power hurts more than helps. Q-2.4 produces a real (p≈0) size effect but does not measure trade outcomes or demonstrate independence from the existing OB edge. The only action that clears the "novel signal earns its keep" bar at this point is shadow-logging for future independence testing. All other integrations (filter, trigger, prompt) require evidence that does not exist yet.
