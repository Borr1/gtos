# Session CM result — one armed-money exit repair is ready to execute

Session CM · Wave 17 · B2700–B2749 · branch `phase17/armed-fidelity` · 2026-08-01.

**Result: package and select `crypto@stop_1p5x_target_scale` on FTMO only.** The current
per-account RECORDED re-read improves at low/mid/high, improves the latest fold at all three
bands, and improves 4/5 paired folds. The default-off carrier, host-derived payload, exact
supervisor diff, pure verifier, proving lines, watch, and rollback are committed. **The
ceremony has not been executed:** CM did not contact the VPS or a broker.

## Findings first

### 1. FTMO crypto is the only current fidelity pass

On 92 FTMO RECORDED trades, the 1.5x stop-width cell with the target scaled at the existing
4R contract moves mid-cost pooled performance from **+0.29843 to +0.55109 R/day**:
**+0.25267 R/day**. Low/mid/high deltas are **+0.25221 / +0.25267 / +0.27470**. The latest
fold deltas remain **+0.03522 / +0.03552 / +0.03611**, and 4/5 paired folds improve at every
band.

This is a **fidelity** decision, not a back-door admission. The candidate itself still
REJECTS family significance (`p_raw=0.03770`, `q=1.0` at mid). The claim is narrower: an
already-armed FTMO sleeve is better expressed by the measured wider-stop contract than by
its current geometry. The planning basis for the live watch is the latest-fold increment,
**+0.03552 R/day at mid**, not the pooled +0.25267.

### 2. The same crypto cell is not selected on redacted_account

redacted_account's 144 RECORDED trades improve pooled mid performance from **−0.14958 to
−0.03387 R/day**, but the latest fold delta is **−0.18613** at mid and negative at every
band. Only 3/5 paired folds improve. CM therefore leaves redacted_account with no frontier-exit
argument. A pooled improvement does not overrule a recent reversal on armed money.

### 3. The energy scale-out question is answered as far as today's inputs allow: no arm

The historical sign is real and repeated. AD measured plain exit ahead of the live
scale-out by about **+0.308 R/day** on n=67; BD independently re-gated a **+0.2302 R/day**
plain-minus-live delta at every band on 64 RECORDED trades. But both BD arms had a negative
most-recent fold, and the current lane does not extend the sample.

CM called the production energy generator directly on the true-UTC TRAIN/VAL lane and made
54 causally safe intents (28 UKOIL, 26 USOIL). The FTMO RECORDED gate retains 50 and only two
folds are evaluable. The lane begins late in 2020 for both oils and contains neither CORN nor
COTTON. It therefore does **not** exceed AD's n=67 and cannot honestly turn the historical
question into an arm.

The exact unblock is longer true-UTC H4 oil history or true-UTC CORN/COTTON history, with
current broker-cost coverage, sufficient to exceed n=67 and produce at least three
evaluable RECORDED folds. CM did not inflate n with old off-surface rows that lack the
production candidate path and stop geometry.

### 4. The other measured-better cells stay off for current, named reasons

| account / sleeve | current re-read at mid | recent/fold result | disposition |
|---|---|---|---|
| FTMO `sub_mid_dn_revert@time_stop40` | 209 RECORDED; +0.01309 → +0.02497, Δ **+0.01188 R/day** | latest Δ +0.01629, but only 2/5 paired folds improve | off: fails 60% paired-fold stability |
| redacted_account `sub_mid_dn_revert@time_stop40` | 57 RECORDED; +0.44168 → +0.52307, Δ **+0.08139** | latest Δ 0.0; only 2/4 improve | off: no positive latest delta and unstable |
| FTMO `sub_xvol_pullback@target_4R` | 44 safe generated; 42 RECORDED | fewer than three evaluable folds | off: not evaluable |
| redacted_account `sub_xvol_pullback@target_4R` | no RECORDED population | absent | off |
| FTMO `mx_btcusd@target_5R` | 228 RECORDED, +1.00066 R/day mid | current and candidate identical; latest +0.08818 | keep the already-live FTMO selection |

The inventory also separates distinct management work. BA's weekend cells do not earn an
arm: flattening worsens energy, xvol, and submid, while crypto's pooled change is only
+0.00054 R/day. CE's spread-geometry floor is already armed on submid/xvol and is a
generation-fidelity repair; it does not authorize their rejected exit cells. AL's threshold
variants are entry/admission-class and stay on the sealed gate side of the boundary.

## What was measured

`CM_REVERIFY_V1.json` is generated by `cm_armed_fidelity.py` from CJ's provenance-bound
true-UTC lane catalog. It calls the current production generators directly for crypto,
energy, xvol, submid, and mx; it does not synthesize candidates from an old trade ledger.
Generated candidate counts are 144 crypto, 54 energy, 44 xvol, 285 submid, and 291 mx.

The causal guard requires the complete 259-closed-bar generator lookback plus the complete
80-bar outcome path to lie in TRAIN/VAL. The semantic loader classifies the timestamp before
converting OHLC, so March and uncovered June+ rows are discarded before an economic field
can be parsed. The receipt records:

- `march_outcomes_read=false`;
- `test_surface_consumed=false`;
- no MT5 client constructed and no broker/VPS contact;
- no TrialLedger or graduation run;
- no config bytes written.

The population is RECORDED, the cost option is `B_balanced`, the bands are low/mid/high, and
the family is the existing `CANDIDATE_BOOK_V1` / V25 family. These are ten **unbilled carry
re-reads** of declared cells. `CM_ITERATION_LEDGER_V1.jsonl` records them with `billed=false`;
no new grid, threshold, DSR ratchet, or post-result sensitivity family was introduced.

The VAL period is already consumed and is used once here as selection evidence. Nothing in
this result is represented as TEST confirmation.

## The inventory corrected two inherited readings

The complete inventory is `CM_ARMED_EXIT_INVENTORY_V1.json`.

First, AK's often-repeated **+1.157 R/day** for xvol target-4R is the candidate's level, not
its increment. Its mid delta versus as-walked is **+0.130065 R/day**. The cell still rejected
family significance and the current lane cannot re-evaluate it across three folds.

Second, “more history exists” did not mean “more usable energy history exists.” CJ's lane
has deeper H4 data generally, but the energy surface is two oils with the same late-2020
start and omits the two agricultural members. The n-extension premise failed on source
coverage, not because the production generator was unavailable.

## The live carrier

`src/components/ultimate_book/execution_packets.py` now wires `crypto` into the existing
per-sleeve `--frontier-exits` mechanism:

- `stop_distance_multiplier=1.5`;
- frontier cell `stop_1p5x_target_scale`;
- fixed 4R target measured from the widened risk unit;
- placement provenance names the cell;
- no selection means multiplier 1.0 and preserves the prior stop, target, hash surface, and
  behavior.

The implementation rebuilds both broker SL and TP from the scaled risk distance. A
packet-only test proves a 3000 entry / 10 native stop becomes SL 2985 and TP 3060 when
selected, while staying 2990/3040 when off. A second behavioral test intercepts the real
`ExecutionEngine.open_trade` request before `safe_place_order`: the wider stop reaches the
request, target remains 4R, risk percentage is unchanged, and volume falls to the rounded
two-thirds neighborhood. No order-send call runs.

The carrier is default-off. No local launcher or config selects it, and no config byte moved.
R2 membership was checked before the edit: `execution_packets.py` is unbound. The hydrated
R2 check reports the two known denominator-ledger absolute-path drifts and no new CM-bound
source drift.

## The host-specific package

The executed host is a fork, so copying mainline's 44 KB `execution_packets.py` would be an
unreviewed multi-wave carry. `activation_carry_armed_fidelity/build_carry.py` instead starts
from Session AZ's executed 38,376-byte host payload, whose SHA-256 is
`06a301bff0db7b2b902cf70384f033d46ee8ba2ba2b9b5215bf580281ebdeb10`, and applies five
single-match anchored edits. It emits a 39,934-byte payload at
`927bed1f4ee2…`, its unified diff, and `MANIFEST.json`.

The reconstructed-host dry run passes before-hash preflight, after-hash postflight, combined
MX+crypto parsing, default/selected geometry, provenance, and both launcher states. The
verifier imports only the pure packet builder; it has no MetaTrader5 import, broker client,
order path, or runtime write.

## Ceremony handoff

The orchestrator should execute
`activation_carry_armed_fidelity/ARMED_FIDELITY_CEREMONY.md` exactly. Its key handoff is:

1. Re-read branch/HEAD/status, `CARRIED_STATE.json`, the full host bytes, supervisor bytes,
   both config hashes, and both token digests. The latest verified host receipt is commit
   `267cccc94`; this session does not claim that state is still current.
2. Transfer the package only, run `verify_carry.py --check preflight`, and stop on any
   unrecognized byte or semantic launcher state.
3. Confirm both accounts flat, hold both stand-down flags, and back up the shared execution
   file plus supervisor with a SHA-bound manifest.
4. Carry one inert file and pass postflight/behavior checks.
5. Change only FTMO frontier from `mx_btcusd...` to `mx_btcusd...,crypto`. Tags,
   redacted_account, both spread floors, configs, tokens, and weights stay unchanged.
6. **Preserve both `firing_sleeves.json` files.** The tag sets and generation union do not
   change; deleting them would create a sizing discontinuity rather than repair one. Prefer a
   decision-day boundary and require flatness for the exit-contract flip.
7. Restart by proving new supervisor/book creation times, not by trusting a task restart or
   process count. Prove the FTMO crypto FRONTIER banner, retain the MX and spread-floor
   banners, prove redacted_account has no frontier argument, then release flags.
8. Watch geometry/provenance/risk/volume and the latest-fold basis for the first week. Behavior
   rollback is one FTMO launcher-field reversal; full byte rollback restores the supervisor
   first, then the execution file.

This page and package do not authorize or execute a VPS write. They are the exact input for
the orchestrator's separately evidenced ceremony.

## Verification

Focused behavior after the final carrier/package bytes:

```text
pytest -q tests/ultimate_book/test_frontier_exit_contracts.py \
          tests/research_infra/test_cm_armed_fidelity.py
55 passed, 1 warning
```

The warning is the repository's existing unknown pytest option `asyncio_mode`; there are no
bad nodes in the focused run. The final tool-emitted A/B receipt is
`phase17/receipts/SESSION_CM_AB_RECEIPT.md`; it compares the tool-derived Session CM scope
against the committed 12,600-pass ZERO baseline. The final 17-file scope is **503 passed,
1 expected xfail, 0 failed, 0 errored**: **0 → 0 bad, 0 regressed**. The receipt embeds the
complete `gtos-ab-receipt-v1` fence.

## What I got wrong

1. I initially carried the commission's shorthand “AK target-4R +1.157” as though it were the
   improvement. Reading AK's two arms showed it is the candidate level; **+0.130065** is the
   delta. The inventory and result use the corrected quantity.
2. I expected CJ's deeper lane to extend energy beyond n=67. It extends the estate generally,
   not this member surface: only two oils exist and neither starts early enough; CORN/COTTON
   are absent. I replaced the expected extension with an exact source/capture requirement and
   did not treat an undersized positive gross difference as a result.
3. The first packaging instinct was to carry the locally tested mainline file. The host is
   not mainline. The final package is rebuilt from the last executed host-specific payload,
   has an exact before-hash, and refuses drift.
4. `scripts/gtos_hydrate_test_data.py --help` does not implement a help-only path; it hydrated
   the prescribed sparse test data instead. That made the cost/source reads available and
   changed no tracked economic artifact, but it was still an avoidable assumption about a
   script interface. The R2 check after hydration reports the known two external-ledger path
   drifts explicitly.
5. The focused carrier tests did not include AZ's older “every frontier kind names itself”
   compatibility test. The tool-derived scope did, and its first capture correctly failed on
   the new `stop distance` banner vocabulary. I extended that behavioral contract and threw
   away the red capture; the final recapture is zero-bad. This is why the A/B scope is derived
   from the diff rather than chosen by the implementing session.

## Commits

- `af6a8d7a0` — declare the unbilled CM re-reads before outcomes;
- `4b6de13c8` — generate/re-gate current candidates and wire the default-off FTMO crypto
  fidelity carrier;
- `20d3014e1` — inventory the historical exit surface and package the exact host ceremony.

The final result/block/A-B retirement commit follows these scoped implementation checkpoints.
