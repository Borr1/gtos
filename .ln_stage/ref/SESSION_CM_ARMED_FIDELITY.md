# Session CM — every measured exit improvement on armed money, packaged to arm

Session: CM · Wave 17 · Blocks **B2700–B2749** · Branch `phase17/armed-fidelity` ·
Worktree `/Users/borr/GTOSActive/worktrees/wave17-armed-fidelity-20260801`

**Authority: OD-ALL-IN (`phase17/OD_ALL_IN_20260801.md`) — read whole.** Built-but-unarmed
measured improvements are a defect state with no waiting room. The armed sleeves are trading
real money on exit contracts the estate has already measured as inferior in specific cells.
Close that gap: find every measured-better exit/management geometry for the ARMED sleeves,
re-verify it at today's evidence, and package the arming. The orchestrator executes the
ceremony; the `--frontier-exits` mechanism (used live for `mx_btcusd@target_5R` since
2026-07-31) is the precedent and the carrier.

Standing owner directives: judgment over script; build/improve/fix, never refute-and-stop;
Workflow tool opt-in approved.

## Work orders

**CM-1 — the inventory.** Sweep AD's 1,631-cell gated exit receipts (wave 7), AK's frontier
work (`sub_xvol_pullback` had NO exit frontier until `target_4R` measured **+1.157 R/day**),
AL's wave-9 closers, and any wave 10–16 receipt that touches an armed sleeve's exit or
management. Armed sleeves: FTMO `crypto, energy_agri, sub_xvol_pullback, sub_mid_dn_revert,
mx_btcusd` (mx already carries `target_5R`); FN the first four. For each: the measured best
cell, its gate state at measurement time, the binding constraint it failed (usually
`significance` — say so), and whether it is admission-class (new edge) or fidelity-class
(better contract for a sleeve already armed). Fidelity-class arms under OD-ALL-IN;
admission-class routes to the sealed gate, not around it.

**CM-2 — `energy_agri`'s scale-out question, answered instead of filed.** AD §6.2 measured
the live `partial_be_runner` scale-out at **−0.308 R/day against the plain exit** on n=67 —
"a question, not a recommendation". Extend the n: the lane, the caches, and the JPY-class
generator re-runs give you history the original measurement did not have. If the sign holds
at usable n, package the removal (or the measured-better variant) as a fidelity arm. If it
flips, file the refutation with the same receipts.

**CM-3 — re-verify before packaging.** Whatever CM-1 selects, re-run its cells against the
current caches/pools at the ratified populations (RECORDED eras where that is the sleeve's
basis) so the package arms today's number, not a wave-7 memory. State pooled vs recent-fold
separately (the `sub_mid` +0.426→+0.041 lesson). Bill nothing new without declaring it:
exit-cell re-reads on already-declared grids are unbilled carries; a genuinely NEW grid is a
declared look with a family ratchet — decide which you are doing and write it down before
reading outcomes.

**CM-4 — the ceremony package.** Template: CE's package + the `--frontier-exits` precedent.
Exact supervisor-args diffs, per-account; proving log lines (the FRONTIER banner form);
preflight against host bytes; verify script; stop conditions incl. a week-one telemetry watch
per changed sleeve; rollback = the previous args line. Same absolute constraints as every
ceremony: no config byte moves (token digests bind them), `firing_sleeves.json` /
decision-day-boundary handling (B365), never-execute list untouched.

## Boundaries

Same as CL verbatim: never-execute list; token-bound configs untouchable; R2 membership check
before any `src/` edit (H1); B905; VPS is the orchestrator's; March 2026 never read; TEST
surfaces inviolable. Conventions: `WAVE_11_WORKING_AGREEMENT.md` §5–§6 + CG's
"Conventions that bind you"; scoped A/B with embedded `gtos-ab-receipt-v1` fence against the
committed ZERO baseline; blocks B2700+ with `file:line` citations; commit as you go. Result
doc `phase17/SESSION_CM_ARMED_FIDELITY_RESULT.md`, findings-first, "What I got wrong",
ceremony-steps handoff.
