# Jev Alive Organism — 2026-09-17

**Seat:** architecture / docs. **No broker place. No TypeSafe call. No secrets.**  
**Owner word:** 2026-09-17 ~15:25 ICT (verbatim in §0). Full research approval. Cut passive conservatism.  
**Calibration account:** Challenge **0** / **$110k** / magic **0**. Verification **0** quarantined.  
**Companions:** [`JEV_WHAT_IT_IS_RESEARCH.md`](JEV_WHAT_IT_IS_RESEARCH.md) (what Jev *is*), [`JEV_GOLD_STATE_SCHEMA_DRAFT.md`](JEV_GOLD_STATE_SCHEMA_DRAFT.md) (XAUUSD state).  
**Still correct, incomplete vs this vision:** [`JEV_INTEGRATION_V2_20260917.md`](JEV_INTEGRATION_V2_20260917.md) — house laws in code, no invent NEWS, no Challenge-45 KEEP grid, no place/remint/flatten.

This file is the map Fable / redacted_account implement from. Success = a gate can be rewritten as **an outcome question + a Jev primitive + a code compose**, without shrinking the owner’s vision back into a refuse sidecar.

---

## 0. Owner verbatim (do not paraphrase away)

> No, no, it's not it's not a brain or a remit because now we have all the architecture of the system, like the foundation, the whole core of everything, like from start to finish. The script is really fast, like it works really fast, and it really detects whatever we throw at it, like it's really well made, I think, I believe so. So if we made a system that is capable of reading the market and, and have a very organized flow from start to finish, If you think about it, the system was really working well, you know, fundamentally and the way it places trades, I think, maybe just the sleeves need to be accurate. And I know the whole thing is, is about the strategy itself or the edge or the sleeve. But what I'm trying to say is, we have the selector and the scheduler and everything else, and we're connected to all the markets. And Jeff is really cheap. Like, see now it made like one hundred and thirty-seven requests, all of it was less than zero point zero one cent. Like, it was one cent literally, zero point zero one dollar. Even less, like it's almost free, you know. And I believe we can use that with really knowing how to set the configuration really, really early, you know? Not to make, not to make sure the whole edge is not, I didn't say that. I said implement it in a way that everything goes through the system, like and with the news and everything, like, it's all there in the logs, and I basically won't really have to use Grokbot as the one placing or whatnot. And it can be not the one reminting or placing or whatever, but the one maybe having the final decision. Like with all the elements we have, with all the data, structural data that we have, there must be a decision that needs to be made. Like, do I take, take this rate or not? With all the information. And it can be thousands and thousands of candidates. And it can pick whatever it sees with all the information and context and all the intelligence that we have. […] If we are calculating, and I, as I said this before so many, many times, if a trade was wrong, it means that a state was missing or a calculation was wrong. Now, if we have all the states, like we understand everything that's going on, in news, in levels, in all the contexts, from macro to micro to all the timeframes, we can have all the calculations be through JEV, like through the code. Like, it won't be like you checking things and seeing if it's okay or no. No, it's actually JEV doing everything through the code, through the script, through the system. And it won't, it doesn't really need to be conversationalist, because it doesn't do conversation, it's with structured data. […] And we can have it on replace on, for example, like we can have it for gold, you know. How does gold go up and go down if we study all the market of gold from start to finish, and seeing all the context of all the events that happen, and how does market work, and in Europe session, in London session, in this minute, in this five minutes, you know, we can try everything and seeing what works exactly, and what is the pure state of the market, in order to find The edge, because we will know exactly when does gold go up and when does it go down. If there is a way of going, are we with it or against it, you know? And all these calculations can happen through the code. It won't be a dead code anymore. Finally, this is the final solution. This is the element alive in the system […] implementing JEV in the code would be so fucking huge. And having the code being alive, literally alive. Like, and it can be through the gates too, like, since we have so many gates, and since we have so many if conditions, they can all be through JEV. Imagine all the if conditions that we have are smart, like, they're actually smart, they are conscious, they make sense. Because it's not always yes or no, or false or true, it's sometimes a range. Fluid. And with the combination of everything, the final trade will happen, not when we limit it. Don't put rules on it. Don't put rules. […] I need the thing to be more outcome-oriented, like, what do I need this gate for? Like, what's the outcome? Okay, then give the outcome to that Jev in that piece of code, in the selector or in the scheduler or all these if conditions that we have, because we studied the market so much and we've done so much research on it, that we know all the conditions, but sometimes the conditions are too static. These solutions make everything dynamic, so dynamic. […] we don't have to do all the data from the first year to the, say, last year and, like, spend days. We can just have it, for example, we can do gold for the past 40 years, or past two years, or past 10 months, or past two months. There is so much data we can work on. Instead of waiting for tomorrow and the next day and the next day, we can do last week, we can do last two weeks, last month.

Full paste: session upload `OWNER_JEV_ALIVE_VISION_20260917_32b9.md`.

---

## 1. Punchline (so nobody shrinks this)

| The foundation | The living tissue |
|---|---|
| Printer, selector, scheduler, market connects, logs — **already fast and organized** (owner). | **Jev runs through those gates** over **complete structured state**. |
| Wrong trade = **missing state** or **wrong calculation**. | Assemble the state; give each gate the **outcome question** it exists for; Jev returns Choice / Score / Noul; **code composes**. |
| Static `if`s are the studied conditions we already know. | Those conditions become **fluid ranges**. The final fire is the **combination**, not a limit list. |
| Chat seats (Grokbot, Project coordinator) | **Not** the decision engine. Jev is non-conversational structured decisions. |
| Cheap: 137 calls < $0.01; ~140–230 ms; fan-out ≈ one call. | Historical gold studies (2 weeks / 2 months / 10 months) **now**, not wait-for-tomorrow. |
| V1/V2 refuse-sidecar | **Incomplete vs this vision.** Keep V2’s *correctness* (no place, no invent NEWS, no 45-row KEEP). Drop V2’s *ambition cap* (“intelligence sidecar only”). |

**Shadow-first until a named live wire is proven.** Design *for* Jev-in-gates. Do not design another refuse-only envelope and call it done.

**“Don’t put rules on it”** does **not** mean delete house integers (token digest, 2-stop counter, US30 off, flatten-first / H8). Those are **writer physics**. It means: stop encoding *judgment* as dead booleans. The 2-stop *count* stays code. The question “is this fire *with* the named gold flow in London on this M15?” becomes Jev.

---

## 2. Authority — what supersedes what

| Layer | File | Wins on |
|---|---|---|
| Owner direction (this vision) | This file + verbatim §0 | Ambition, gold-first lab, Jev-in-gates, cut conservatism |
| V2 correctness | `JEV_INTEGRATION_V2_20260917.md` | No place/remint/flatten; no invent NEWS; Challenge 45 is a mechanism pack not a KEEP oracle; LIVE vs STUDY clocks; chair writes inbox |
| V1 evidence | `JEV_V1_CRITIQUE.md` + `lab/challenge_replay_20260917/` | Primitives work; house toxic is code; time_stop hole; echo-the-digest |
| TypeSafe | live docs + skill | Primitives, fan-out, confidence ≠ permission, schema-hard |
| Doctrine | `.context/00_core/research_operating_doctrine.md` | No tune-after-see; AI API is not the historical backtest *engine*; small-n ≠ validation |
| W7 live physics | `CLAUDE.md`, `book_owner.py`, `bridge.py`, `admission.py` | Triple-gate, H8 flatten-suppressed, armed-set, metals A8 |
| F5 Challenge physics | leftover-ship / live `f5-live` (not this `main` printer) | Isolated 15m re-entry, HIGH T−15..T+60, 2-stop, spring+vss |

Two fire-paths. Do not splice W7 `already_placed_today` onto F5 isolated re-entry (kills the wanted spring reprint). Do not silent-edit `open_trade`.

---

## 3. Outcome-oriented method (the implementer’s loop)

For **every** gate in §5–§8, Fable fills this row before writing code:

```
1. What OUTCOME does this gate exist for?     (one sentence, no boolean)
2. What STATE does that outcome need?         (named paths on gold_state / slate)
3. Is that state assembled here today?        (file:line or GAP)
4. Which Jev primitive returns that outcome?  (Choice / Score / Noul / fan-out)
5. How does CODE compose it with integers?    (weights, ignore-if-unused, house_block)
6. What happens if Jev is down / low-conf?    (never extra PASS)
7. Wire class                                 (A0 house-only | A1 log | A2 chair-sees | W_named live)
```

That is the owner’s “what do I need this gate for? Okay, give the outcome to that Jev in that piece of code.”

**Integer vs judgment (so “no rules” is not chaos):**

| Stays integer in code | Becomes Jev outcome |
|---|---|
| Token digest match | “Is this enrolled fire still the one we want *given named tape*?” |
| 2-stop remaining count | Label a would-be third fire (log); do not be the counter |
| `--tags` / hard-off / US30 | Consistency Noul only |
| Occupancy keep-one / 15m yield | Correlation / event HOLD drafts — never “this looks like a remint” |
| Governor −3% / −10% / gross cap | Size-vs-headroom Score is optional study; the wall stays code |
| Calendar window math (15/2 or 15/60) | `event_proximity` Noul on **named** events only |
| `plan_r` arithmetic | `geometry_vs_tape` Score |
| `htf_trend` / `ac60` / A8 K=3-of-4 | `flow_stance` Choice + `flow_alignment` Score; A8 integer may later *read* the Score on a named wire |

---

## 4. Where state must be assembled

Priority = what the owner named: **news, levels, macro→micro, sessions, timeframes.**

| State block | Assemble here (today) | Gap |
|---|---|---|
| Multi-TF snaps (M15/H4/D1) | Historical CSVs in `data/historical*`, `exports/multi_instrument/`; metals generator already holds H4 bars | No single `assemble_gold_state_v0` yet |
| Sessions | `metals.py` server-local hour; `broker_clock.py`; wave21 `utc_session` | `data/sessions/` missing; three session dialects exist — pin `sessions.source` |
| Levels | wave21 `dist_to_prior_*`, metals FVG, `run_raw_ohlc_path_scaling_v2_levels.py` | No gold-levels pack under `judgment/` |
| News | W7 `news_calendar.py` + `data/news_calendar.json` (**2026-06-01..05 only**) | F5 `official_high_spine` / `NEWS_PROTOCOL` **not in git**. Empty spine ≠ no HIGH. **Never invent.** |
| Sleeve features | `metals.py` A8 + `ac60` + `_runner_R`; F5 tags on leftover-ship | DSP features not on this W7 tree |
| Cost / geometry | `TradeIntent.stop_dist`; wave21 `cost_r` / ATR fields; `_spread_cost_screen` | 1.5-vs-2.0 ATR basis defect — pin `geometry_atr_basis` |
| Occupancy / 2-stop / 15m | W7 `book_owner`; F5 writer integers | Live slates not in git |
| Slate bind | leftover-ship `gtos.judgment.slate.v2` | Not on this `main` |

Assembler target: default-off research function, schema `gtos.judgment.gold_state.v0`. Spec in the gold-state draft. **Not** imported by `ExecutionEngine.open_trade`.

---

## 5. W7 `ultimate_book` gate map

Live chain (this `main`):

```
run_book.py → BookLauncher.tick
  → UltimateBookOwner.manage_open_positions          (every tick)
  → UltimateBookOwner.run_cycle
      → UltimateBookLiveEngine.evaluate
          → _generate_intents
          → evaluate_vnext_ultimate_book_admission   (bridge)
              → admit_and_size
      → UltimateBookOrderRouter.place
          → ExecutionEngine.open_trade               # Jev NEVER sits here
              → RealMT5.order_send                   # token choke; Jev NEVER sits here
```

`W` = wire class. **A1 is the default next step.** `W_named` requires owner word naming that site.

### 5.1 Authority / bridge (`UB-AUTH`)

| ID | Site | Static today | OUTCOME the gate wants | Jev primitive | State to assemble | W |
|---|---|---|---|---|---|---|
| UB-AUTH-001 | `bridge.py` missing `REQUIRED_DIAL_KEYS` | fail-closed | Is the dial complete enough to size? | Noul `dial_complete` (consistency) | cfg key list | A1 |
| UB-AUTH-002–004 | unknown profile / sleeves | fail-closed | Is this allowlist a real book? | — integer | registry | A0 |
| UB-AUTH-005 | `disable_account` vs broad apply | fail-closed | Must broad stay off while book is live? | — integer | selector flags | A0 |
| UB-AUTH-006–009 | enabled / apply / live_allowed / broker_authority | four booleans | Shadow vs realized risk | — integer (H8) | runtime flags | A0 |
| UB-AUTH-010 | `bridge.py:592-602` after `admit_and_size` | status string | **Would we still want these sized units given named tape?** | Choice `admit` + Scores `flow_alignment`, `geometry_vs_tape` | gold_state per unit + governor snapshot **without** using PnL as a feature | **A1 first hook** |

### 5.2 Generate (`UB-GEN`)

| ID | Site | Static today | OUTCOME | Jev | State | W |
|---|---|---|---|---|---|---|
| UB-GEN-001 | `registry.py` tags ∩ BUILT | filter | Which sleeves may even exist? | — integer | tags | A0 |
| UB-GEN-002 | `book_engine.py` active sleeves / `include_clean3` | drop | Is this sleeve on the effective registry? | — integer | include flags | A0 |
| UB-GEN-003 | broker `supports(symbol)` | drop | Is the instrument configured? | — integer | profile | A0 |
| UB-GEN-004–006 | warmup / future bar / stale bar | drop | Is the market clock alive? | Noul `tape_fresh` | bar age, interval | A1 |
| UB-GEN-007 | per-sleeve `generator(...) is None` | drop | **Does this bar deserve a candidate *as a range*, not a cliff?** | Score `signal_presence` 0–2 + existing integer still emits/doesn’t | bars + gold_state | A1 then W_named |
| UB-GEN-008 | `_spread_geometry_refusal` | floor | Is spread acceptable vs this stop *today*? | Noul `cost_hurtful` + Score | tick, stop | A1 |
| UB-GEN-009 | `_entry_hour_deferral` | hour < target | Is this the sleeve’s clean hour, or merely “not yet”? | Score `session_fitness` | broker hour | A1 |
| GOLD-002–006 | `metals.py` FVG + `ac60` cliffs (0.10 / 0.04) | hard bands | **Is persistence / FVG quality a range?** | Score `persistence` on `ac60` + `flow_stance` | A8 fields + H4/D1 | A1 / gold lab |

`ac60 >= 0.10` is the owner’s “static condition” example. Alive design: keep the integer as a **default-on house band**, log a Score across 0.02–0.20, and only on a **named wire** let the Score *move* the band. Historical gold lab is how we learn whether the cliff is costing fires.

### 5.3 Admit / size / governor (`UB-ADM`)

| ID | Site | Static today | OUTCOME | Jev | State | W |
|---|---|---|---|---|---|---|
| UB-ADM-001 | 2% ceiling × `derisk_mode` | fail-closed | Dial safe? | — integer | profile | A0 |
| UB-ADM-002–003 | circuit / NaN equity | fail-closed | State valid? | — integer | GovernorState | A0 |
| UB-ADM-004–006 | soft daily −3%, max-DD −10%, entry buffer 9% | walls | Are we near a prop wall? | — integer; optional Score `wall_pressure` for logging | equity, ref | A1 log only |
| UB-ADM-007 / 014 | gross cap / FFD shed | shed | Which unit deserves remaining headroom? | Score `conviction_vs_tape` **to order the shed**, not to invent headroom | unit gold_state | A1 |
| UB-ADM-008 | derisk / profit-target protect | cap_mult | How hard to shrink? | — integer | dd, gain | A0 |
| UB-ADM-009 | VP `above_va` | drop | Is this VP noise? | Noul (study) | `vp_loc` | A1 |
| UB-ADM-010 | learning `mult==0` | drop | Learning GATE? | — integer | rerate map | A0 |
| UB-ADM-011 | metals A8 K=3-of-4 | drop if features present & fail | **Is confluence a range?** | Score `a8_quality` 0–2 over the five named fields; integer K=3-of-4 stays until named wire | A8 + news + levels | A1 / gold lab |
| UB-ADM-012 | symbol QUARANTINE | drop | Is this symbol damaged? | Noul consistency | damage metrics | A1 |
| UB-ADM-013 | unknown sleeve / bad stop | fail-closed | Unit legal? | Noul `state_sufficient` | intent | A1 |
| UB-ADM-015 | joint daily −5% | tighten | Prop daily guard | — integer | realized today | A0 |

### 5.4 Placement (`UB-PLC`) — last intelligence seat before send

| ID | Site | Static today | OUTCOME | Jev | State | W |
|---|---|---|---|---|---|---|
| UB-PLC-001–002 | breach latch / kill flag | block | Operator brake? | — integer | flags | A0 |
| UB-PLC-003 | `not runtime_effect_now` | shadow | Shadow cycle | log Jev anyway | bridge | A1 |
| UB-PLC-004–005 / 008 / 012 | AI companion pause / cooldown / zero-risk | block | Companion veto | Jev may *replace* companion prose later; not this PR | AI snapshot | A1 |
| UB-PLC-006 | missing instrument | skip | Tradable? | — integer | profile | A0 |
| UB-PLC-007 | weekend embargo | block | Weekend legal? | Score `weekend_carry` only if policy is on | broker clock | A1 |
| UB-PLC-009–010 | already placed this bar / today | skip | Idempotency / one-unit-day | — integer on W7 | ledger | A0 |
| UB-PLC-011 | cluster cap | skip | Same-cluster second fire? | Choice `action_scope` + Noul `speak_hold` (`jev_corr_hold_v1`) | cluster slate | A1 |
| UB-PLC-013 | tick age > 900s | skip | Market open? | Noul `tape_fresh` | tick | A1 |
| UB-PLC-014–015 | already holds / symbol lifecycle | skip | Duplicate / conflict | — integer | positions | A0 |
| UB-PLC-016 | `_entry_too_late` | skip | Restart chase? | Score `chase_toxicity` | bar frac | A1 |
| UB-PLC-017 | `_spread_cost_screen` | skip | **Pre-send: is cost the whole trade?** | Noul `cost_hurtful` + Score `geometry_vs_tape` | tick + gold_state | **A1 second hook** |
| UB-PLC-018–022 | geometry / halt / policy / tick side | fail-closed | Send-contract complete? | — integer | trade_params | A0 |
| UB-PLC-023 | `mt5_real.py` token | refuse increase | Activation valid? | — integer. **Never remint from Jev.** | digest match bit only | A0 |

**Do not hook:** `ExecutionEngine.open_trade` send, `order_send`, token mint.

### 5.5 Manage / flatten (`UB-MGT`)

| ID | Site | Static today | OUTCOME | Jev | W |
|---|---|---|---|---|---|
| UB-MGT-001 | flatten requested + authority false | observe only (H8) | Positions stay open unmanaged | **Jev must not be the flatten.** Chair ENFORCE. | A0 |
| UB-MGT-002–004 | flatten flag / breach −4% / hysteresis | flatten | Prop breach? | — integer | A0 |
| UB-MGT-005 | authority false in manage | observe-only TP/SL | Same H8 | — | A0 |
| UB-MGT-006 | weekend flat | close | Compliance | — integer | A0 |
| UB-MGT-007 | `time_stop_bars` vs M15 count | close | Horizon reached? | — integer (M15 printed bars). Jev **labels** the exit (`time_stop`). | A1 on close |
| CLOSE | manage close path | record reason | **What noun is this exit?** | Choice `exit_class` with **time_stop first-class** | A1 |

H8 remains operating law: flatten first, confirm flat, then shut the gate. Jev is not an emergency brake.

---

## 6. Selector V4 / Scheduler V4 — still in the organism

Config: selector enabled, `apply_to_execution: false`, `live_activation_allowed: false`. Scheduler apply-on, live-off. They still **compute**. Owner named them.

| ID | Site | OUTCOME | Jev | W |
|---|---|---|---|---|
| SEL-V4-001 | `selector_v4.py` not enabled | Off? | — integer | A0 |
| SEL-V4-002 | sub-evaluators merge (~3598) | **Given this packet, how good is this candidate on named dimensions?** | Fan-out: Scores confluence / cost / lifecycle / geometry_vs_tape; Noul fillability | A1 |
| SEL-V4-003 | `live_allowed && apply && would_action` | Runtime effect? | Choice `admit` **after** merge, **before** `runtime_effect` | A1; W_named only with owner word |
| SCH-V4-001 | `permissions.py` `_reject_if_scheduler_v4_not_selected_authority` | Is this the window winner? | Choice among **prefiltered** candidate_ids (≤255) or Score-then-Choice | A1 |
| SCH-V4-002 | `moonshot_scheduler_v4_best_trade_allocator.py` | Best trade in window? | Same, on the allocator path — not on ultimate_book send | A1 |

V4 holds **no live authority** today. That is why it is the **safest** place to make gates alive first: thousands of candidates, complete packets, zero broker mutation.

---

## 7. Permissions / news / correlation (broad / orchestrator)

Not wired to `ultimate_book` generate. Still organism tissue.

| ID | Site | OUTCOME | Jev | Note |
|---|---|---|---|---|
| PERM-001–004 | halt / phase / trading_enabled / killed symbol | May we evaluate at all? | — integer | A0 |
| PERM-005 | daily PnL ≤ −max | Daily circuit | — integer | A0 |
| PERM-009 | correlation excess | **HOLD draft or hard reject?** | `jev_corr_hold_v1` + mechanism Choice | A1 |
| PERM-011 | broker-net pretrade refuse | Cost complete? | Noul `cost_hurtful` | A1 |
| NEWS-W7 | `news_calendar.py` `should_skip` | Is a **named** HIGH in the **code** window (T−15..T+2)? | Noul `event_proximity` | Empty calendar → all trades proceed **unaware** today (`news_calendar.py:84-87`). That is a **state defect**, not “no news.” |
| NEWS-F5 | leftover-ship inbox + `F5_HIGH_PRE_MIN` / POST | Named HIGH in T−15..T+60? | same Noul, different window facts in state | Do not copy W7’s 2-minute post onto F5 |

---

## 8. F5 / Challenge house law (`F5-JEV`)

Printer of record is **not this `main`**. Closest git: `origin/f5-leftover-ship`. Live: `f5-live` (not fetched). V2 §1.1 table still names the integers. Alive-organism adds the **outcome** column V2 left as “Jev silent.”

| ID | Law (integer stays) | OUTCOME Jev may answer | Primitive | Forbidden |
|---|---|---|---|---|
| F5-JEV-001 | `--tags` ∩ hard-off / US30 | Consistency: does `family_class` agree with surface digest? | Noul | Discovering bleed as a Noul |
| F5-JEV-002 | 2-stop same-sleeve orig_stop / UTC day (live const, **absent** leftover-ship) | Label a would-be third fire | Noul log-only | Being the counter |
| F5-JEV-003 | HIGH T−15..T+60 | Draft `event_proximity` **before send** | Noul | Invent HIGH |
| F5-JEV-004 | `cost_screen` | Score cost-vs-stop as study | Score / Noul | Removing the screen |
| F5-JEV-005 | Chair VETO pre-fill only | Draft `{hold, mechanism}` while `status` is intent/standing | corr-HOLD + mechanism Choice | Occupancy HOLD; HOLD a legal 15m reprint *as remint* |
| F5-JEV-006 | Close LABEL nouns | Name `time_stop` | Choice with time_stop **in criteria** | Accepting `manual_other` @ 0.8 on winners |
| F5-JEV-007 | Isolated re-entry ≥15m on a **flat** symbol | Confirm writer integers; draft only correlation/event | Noul `isolated_reentry_is_new` | “Looks like a remint” |
| F5-JEV-008 | Dead window 21–00Z / Friday 16:00Z | `session_fitness` | Score | Replacing the clock |
| F5-JEV-009 | Token digest | Log “would have scored” if mismatch | — | Remint |
| F5-JEV-010 | Nightly KEEP/OFF `n≥40` / `2·SE` | Label rows for the **body** | Score `lesson` | Emitting KEEP/OFF |

Challenge 45 (`lab/challenge_replay_20260917/`): 45 tickets, 6W/39L, **−$4,295.92**. House toxic 24 tickets / **−$3,897**. Remainder 21 / **−$399**. V1 +$406 is house-cut + in-sample grid. **Use as mechanism pack** (fast-stop DSP, remint chains, time_stop nouns, bleed cluster HOLD-too-late). Do not re-grid KEEP.

XAUUSD is already the Challenge’s paying surface (spring / three_fresh / expanding time_stops). The gold lab and F5 admit share one state shape.

---

## 9. Gold-first historical lab (2 weeks / 2 months / 10 months)

Owner: do not wait for tomorrow; do not boil 40 years first. Slices:

| Slice | Calendar window (use **bar dates we actually have**) | Bars (order-of-magnitude) | Purpose |
|---|---|---|---|
| **G-2W** | Last **2 weeks** present in `data/historical/XAUUSD_M15.csv` (file ends **2026-04-17**) → **2026-04-06 .. 2026-04-17** | ~10 sessions × ~96 M15 ≈ **1,000 M15** + corresponding H4/D1 | Assembler proof, question fan-out, `state_sufficient` rates |
| **G-2M** | **2026-02-17 .. 2026-04-17** (2 months of that M15 file) | ~40 sessions × 96 ≈ **3,800 M15** | Session / flow / geometry distributions; first reliability sketch |
| **G-10M** | **2025-06-17 .. 2026-04-17** if the file covers it (M15 starts **2024-04-01** — 10 months **is** on disk) | ~200 sessions × 96 ≈ **19,000 M15** | With-flow vs against-flow; sleeve-band (`ac60`) fluidity; Nightly-scale n |

Longer D1 (`exports/multi_instrument/XAUUSD_D1.csv` **2014-08-20 → 2026-04-02**) is a **macro context** layer for G-10M, not a substitute for M15.

**News on these slices:** the only committed calendar is **2026-06-01..05** — **after** the M15 file ends. Therefore G-2W / G-2M / G-10M on *this clone* run with `news.spine_empty = true` unless an operator opens a **real** calendar that covers those as-ofs. Event questions **abstain**. That is honest. **Do not invent NFP rows for 2025–2026 to make the lab look complete.**

If leftover-ship `official_high_spine` is later opened and its `extracted_utc` covers a slice, fold it under `news.source = f5_official_high_spine` and re-run **event** questions only. Do not retune flow/geometry on the same look.

### 9.1 Lab loop (result-first, doctrine-legal)

```
for each slice in (G-2W, G-2M, G-10M):
  1. assemble_gold_state_v0 for every metals_core / metals_softband fire
     AND every H4 bar (negative set — owner: when does gold go up AND down)
  2. write JSONL  (no API key required)
  3. pre-register metrics BEFORE any Jev call:
       - state_sufficient rate
       - flow_stance vs next-H4 / next-D1 sign (as-of, no EXPOST in state)
       - session_fitness histogram vs realized range
       - geometry_vs_tape vs stop-hit within 2 / 8 / 32 M15 bars
       - event_proximity evaluated ONLY on rows with spine_empty=false
  4. if TYPESAFE_API_KEY in secrets: fan-out §4 of the gold-state draft
  5. reliability diagram per question; Nightly word on any KEEP is still n≥40 / 2·SE
```

**Forbidden:** maximize kept PnL on Challenge 45; put later `broker_net` into LIVE state; call empty spine “no HIGH”; skip G-2W because “we need 40 years.”

**Cost ceiling (published $0.042/MTok, output free):** G-2W ~1k calls × 2k tok ≈ **$0.08**; G-2M ≈ **$0.32**; G-10M ≈ **$1.60**. Owner’s “almost free” holds. RPM (1,200) is the wait, not dollars.

### 9.2 What “pure state of the market” means here

Not a mystical gold seasonality. A **repeatable object**: sessions + levels + (real) events + multi-TF flow + sleeve features + cost + geometry. Jev’s `flow_stance` / `session_fitness` / `geometry_vs_tape` / `level_respect` are how we *measure* with-vs-against. WAVE6’s “session buckets added nothing once vol is gated” is the **mechanical baseline**. The lab either confirms it on complete state or shows the static buckets were the dead `if`.

---

## 10. Compose — the organism is the combination

TypeSafe: questions in one request cannot see each other. The **alive** part is the compose:

```
house_block = token_mismatch | hard_off | us30 | two_stop_exhausted
            | cost_screen_refuse | dead_window | (occupied and not isolated_reentry)
            | governor_wall | halt

if house_block:
    disposition = writer_blocked          # Jev log-only
elif not state_sufficient:
    disposition = assemble_or_escalate    # missing state — owner's first failure mode
else:
    # fluid — not a single if
    flow   = answers.flow_alignment.score          # 0..2
    sess   = answers.session_fitness.score
    geo    = answers.geometry_vs_tape.score
    lvl    = answers.level_respect.score
    event  = answers.event_proximity.noul          # ignored if spine_empty
    costn  = answers.cost_hurtful.noul

    composite = w_flow*flow + w_sess*sess + w_geo*geo + w_lvl*lvl
                - w_cost*costn - w_event*event

    # weights live in code; retune without a new call if atomics are stored
    disposition = log_only                 # A1
    # W_named later: selector merge / metals band / pre-send steer
    # NEVER: place | remint | flatten | write_verdict | splice_tags
```

You will **not** know the outcome in advance (owner). That is the point. The combination is dynamic. The integers keep the book from killing itself while we learn the weights on G-2W → G-2M → G-10M.

Missing Jev / exception / schema-break: **no extra PASS.** Alive ≠ fail-open.

---

## 11. Sequence for Fable (so this is not a poster)

1. **Assembler** `gold_state.v0` + tests in §8 of the gold-state draft. JSONL for G-2W. No API.
2. **A1 hooks (log only)** when key is in secrets:  
   - `bridge.py` after `admit_and_size` (UB-AUTH-010)  
   - `book_owner` after cost screen (UB-PLC-017)  
   - selector V4 merge (SEL-V4-002)  
   - close-label with `time_stop` in criteria  
3. **G-2W Jev fan-out** → reliability sketch. Then G-2M. Then G-10M. Pre-register metrics each time.
4. **Chair A2** only if `f5-chair-sit` can consume a draft **pre-fill**.
5. **Named live wire** — owner names the site (example: “metals `ac60` band may read `persistence` Score”). Until that sentence exists, no send-path AND.
6. Ingest V2 §8 Project files (`MAC_INGEST`, `OWNER_LAW`, `ASTRA_ARCH`, `NEWS_PROTOCOL`) **when they exist on disk**. Do not invent their contents.

V2 A0/A1/A2/A3/B-forbidden table still applies. This file adds **gold lab** and **fluid compose** as first-class work, not as an afterthought.

---

## 12. Explicit non-goals

- No place, remint, flatten, take-at-R, token mint, MT5 login switch, broker mutation from this PR or from Jev.
- No Jev import on `open_trade` / `order_send` / leftover-ship send path.
- No `verdict.json` writer except `write_inbox_verdict.py`.
- No invented HIGH / FOMC / NFP / BOJ rows. No chat autopsy as calendar.
- No Challenge-45 KEEP grid. No `admit_now` as a historical LIVE gate.
- No secrets in repo or chat.
- No claim this PR live-wires Jev.
- No silent W7↔F5 splice.

---

## 13. What “do not underestimate this” means for the next agent

If you implement only `jev_admit_v2` shadow refuse on close-reconstructs, you implemented **V2 again** and missed the owner.

If you implement:

- a **closed gold state**,
- **outcome questions inside named gates**,
- **fluid Scores composed in code**,
- a **2-week → 2-month → 10-month** as-of lab,
- shadow logs that can become a **named live wire**,

you implemented the vision.

The foundation is done. The code is dead until Jev runs through it. This file is the map. The gold-state draft is the object. The what-it-is note is the primitive literacy. Go assemble state and ask outcomes.
