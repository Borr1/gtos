# Admission KEEP integers — env flags

Challenge `0` / `operator` only. These flags cannot open broker send, remint, flatten, or envelope walls.

`ADMISSION_APPLY` is hardcoded `0`. `pack1b_beaten` is `False`.

| flag | default | effect | opens APPLY? |
|---|---|---|---|
| `GTOS_JEV_ADMISSION_APPLY` | unset / ignored | reserved; ADMISSION_APPLY stays 0 | False |
| `GTOS_JEV_ADMISSION_CHOICE` | 1 (missing counts as on) | wires LABEL Choice on hist KEEP rows only | False |
| `GTOS_JEV_ADMISSION_KEEP` | 1 (missing counts as on) | stamps KEEP integers on Challenge host_occupancy_governor | False |
| `GTOS_JEV_APPLY_LIVE` | off | physical Challenge size haircut only; does not open admission APPLY | False |
| `GTOS_JEV_FLUID_GATES_APPLY` | off | fluid LABEL drafts only; does not open admission APPLY | False |

## Contract

- KEEP (S28 / S29 / S30 / S32): house integer stays fail-closed; Choice LABEL only.
- KILL (S31 / S33): dead soft path; no Choice; no SHADOW rest.
- Soft daily stop KEEP (Dig B STATIC, Challenge soft_2pct n=1) does not lift `ENV-DD`.
- `leader_impulse_veto` KILL: `overlay_sizeup_allowed=false` on Challenge.
- Dig / Jev never `order_send`.
- Do not invent `NEWS_PROTOCOL`.
