# Paste-ready writer/builder execution contract — Astra V2

Implement the NEWS_PROTOCOL_V2 transport and enforcement contracts for F5 only, with independent tests against the exact deployed source. This is staged work: no live file mutation, process restart, remint, order send or flatten is authorized by this artifact. Nightly Decide currently holds live laws/no splice. Cursor builds locally; redacted_account verifies operational receipts; the owner controls the live load. Existing F5 identity is login 0, namespace operator, magic 0, portable C:\MT5\FTMO. Preserve the actual active $150/tag/exit contract until explicitly changed.

Writer owns the complete action from candidate generation through broker request, fill, adoption, management and close. Give every fire an exact immutable identity containing symbol, sleeve, direction, decision bar and geometry; make all refusal and opportunity records joinable to it. Publish genuine predecision context before dispatch. An event from a broker ticket is order/fill acknowledgement, not a predecision candidate.

At the final send boundary locally revalidate account, active policy/event version, candidate expiry/frozen level, geometry, costs, exact current judgment and risk conditions. Event facts survive slate turnover; candidate decisions do not survive object or material book-state changes without revalidation. No LLM response is an indefinite printing prerequisite. Missing optional judgment remains the current PASS behavior; known scoped deterministic restrictions keep their own independent validity. Failed/unknown state is explicitly recorded rather than renamed success.

Use immutable input packets and durable idempotent receipts. Preserve source/analyzed times; reject stale reasoning instead of copying on a current fingerprint. Honor absolute expiry; repeated apply cannot renew it. Broad sleeve-day aliases are lookup aids only. Check actual disk write results, then consumer acknowledgement. A partial flow/manage write is not APPLIED. Keep close labels and audits out of the risk-action path and accept them when the book is empty.

Read canonical UTF-8 JSON and handle a BOM explicitly. Parse/validation failure emits health evidence with path/source/version and never becomes a silent empty calendar. Produce one validated event snapshot from official schedule/revisions using stable occurrence IDs. Do not mistake separately delivered NFP headline updates for multiple scheduled events.

Honor current scheduled no-new-risk policy on submissions and resting native pendings. At event start reconcile affected orders; request cancellation through the writer's existing execution interface, confirm broker state, and record RACE_FILL if fill wins. A race-filled position retains its original contract; no retrospective flatten. Unaffected symbols keep printing. On event expiry immediately reevaluate valid unexpired opportunities under existing strategy rules, preserving frozen limit prices and costs. No market chase, no remint of a spent ticket, no occupancy-shaped universal HOLD.

Original contract is entry, original stop/risk denominator, broker TP, sleeve/time-stop identity and expiry policy, plus actual lot/risk. Persist it at order/fill before any restart-sensitive path. Startup reconciles broker positions against that record and preserves it. Prove that hydration never synthesizes a different TP/time stop from a live locked stop and that the first management tick cannot generate an unintended TP1_full. A backup JSON without a reader on the startup path is not protection. Leave spent gold 180717112 spent.

Management follows the verified original contract and exact authorized ticket-bound instructions. Auto-BE remains off. No R-number take, software TP1 or opportunistic stop change is reintroduced by adoption, rescue, a leftover flag or a seat label. Existing real account-loss compliance and internal buffers are separately named and tested against the correct Prague accounting day; neither is inferred from notional ledger money.

Acceptance includes both prevented defects and preserved supply: stale decisions rejected; concurrent WATCH/CHAIR messages both accounted for; event expiry cannot drift; new 15m re-entry remains eligible; other fires cannot inherit an old correlation veto; original contracts survive restart; disk/BOM/cancel failures surface truthfully; and unaffected opportunities still reach the broker path. Replay recorded NFP and burst traffic with source publication, ingress, commit, consume, request, broker acknowledgement and fills timestamped separately. Engineering latency passing is reliability evidence, not profitability evidence.

Ship outputs are an exact diff, source and launch manifest, targeted test results, pre/post contract comparison, activation scope, restart/remint classification and rollback using the actual previous deployed bytes. Do not copy HEAD as rollback when current printing bytes are uncommitted. No existing tag changes need a remint unless the supplied plan changes the token-bound tag/size contract. Each writer change still carries startup risk and requires the prescribed live-original-contract ceremony.
