# JEV_INTEGRATION_V2 — system-logic contract (2026-09-17)

**Seat:** Astra / builder tissue on a **standalone GitHub Cloud agent** — **not** the Cursor Project `gtos` Fable coordinator chat. **Docs + shadow schema only.**  
**Status:** Design of record for Jev work that can be defended from **disk**. **Not live wiring.**  
**Supersedes:** V1 tuned policy and V1 gate sweep as *policy*. Those files remain **calibration evidence** (see [`JEV_V1_CRITIQUE.md`](JEV_V1_CRITIQUE.md)).  
**Does not supersede:** house surface law, chair charter verbs, leftover-ship printer law, owner later-spoken word.  
**No Project chat memory.** If a Project wave file is not on git, it is listed as a GAP in §8.

```
Jev never places, remints, flattens, remints tokens, or takes at R.
Chair (redacted_account): ENFORCE / VETO / LABEL only.
Writer prints. Occupancy HOLD is dead (script). Isolated re-entry after 15m
is a new named fire — leave orig. Later spoken owner word wins. Payout is
the objective ($110k Challenge Step-1, login 0).
```

This file answers the seven owner questions. It is written against **how the F5 book actually runs**, not against a confidence table.

---

## 0. Punchline

Fine-tuning rests on **house laws that already fire in code**, plus **slate-complete state at intent time**. Confidence is a **secondary abstain/escalate knob** after those laws. Challenge 45 is a **toxic-week mechanism pack**, not a threshold oracle.

Jev sits in the **intelligence / information** tissues only: atomic Choice / Score / Noul over a **closed state object**. Code composes. Chair may ignore. Writer never imports Jev. Nightly Decide stays `n≥40` / `2·SE` Python on Challenge-true R.

If a proposed gate cannot name **which writer skip, which inbox verb, which Nightly sentence** it changes, it is V1 again.

---

## 0a. What this standalone agent actually opened

Context OS (`.context/00_core/gtos_context_os.md`) is explicit: **current disk wins over chat, memory, and old handoffs.** Packs are retrieval maps, not authority. This write lists every path that was opened. It does not invent Project-wave ingest.

### On this checkout (this PR / `main` lineage)

| Path opened | What it is for Jev | Authority |
|---|---|---|
| `CLAUDE.md` (workspace rule) | W7 live truth, H1–H8, armed-set hazard, **flatten-first** (H8) | Current for **this** `main` book. Not the F5 printer. |
| `.context/00_READING_ORDER.md` | Still points at 24-symbol `momentum_exhaustion` / V4 | **Stale.** CLAUDE parks that program. Do not treat as F5/Jev law. |
| `.context/00_core/live_system_of_record.md` | Two **independent** `run_book.py` workers; 2.0% ceiling; `expected_account`; activation token carry | Authoritative for **W7**. Challenge 0 is a **third organism**. |
| `.context/00_core/gtos_context_os.md` | Disk > chat; no broker mutation from context tools | Method. |
| `.context/00_core/current_vnext_system_map.md` | Halt-flag / V4 map dated 2026-06-05 | **Not in CLAUDE preflight.** Predates `ultimate_book`. Halt-flag claims contradict CLAUDE §4 (those flags were measured **absent** on the VPS). |
| `.context/00_core/research_operating_doctrine.md` | No tune-after-see; no post-event features in as-of rules; AI API is not the historical backtest engine; small-n is not validation | **Directly kills V1 as a gate.** |
| `.context/00_core/portable_path_authority.md` | Mac research vs `vps_runtime_path`; unlabeled machine paths are bugs | This Cloud VM is **neither** Mac nor VPS. Leftover-ship `decide.py` still hardcodes `C:\Users\trader\redacted_host`. |
| `.context/00_core/llm_specialization_research_backlog.md` | Fine-tune needs clean examples, **non-leaky labels**, sealed eval, calibration | Jev “tune” standard. A 45-row PnL grid is not that. |
| `.context/00_core/goal_session_research_discipline.md` | After compaction, re-read disk; small-n must name the expansion path | Method. |
| `.context/00_core/GTOS_ULTRA_GOAL.md` | B7.5 selection–sizing campaign | CLAUDE **parks** it. Jev is not that campaign. |
| `.context/00_core/quick_reference_card.md` | Still says hard-halted | Stale vs CLAUDE armed books. |
| `.context/LIVE_STATE.md` | Generated **2026-08-08** at `9392e9bbd`; research-state `STALE`; no F5/Jev/Challenge | Mechanical W7 snapshot. Not regenerated into this PR. |
| `run_book.py` | W7 launcher; `--tags`; `config_digest_for` → `set_activation_context` | W7 send path. |
| `src/mt5/mt5_real.py` | `order_send` is **the activation choke** | Jev **never** sits here. |
| `src/safety/activation_token.py` | Namespace + `config_digest_sha256` must match or exposure-increasing send dies | Token fact for state; never remint from Jev. |
| `src/components/ultimate_book/book_owner.py` | `already_placed_today` = one-unit-per-(sleeve,symbol)-day; H8 flatten **suppressed** when authority false | **Wrong organism** to patch for F5 15m isolated re-entry. |
| `src/components/ultimate_book/bridge.py` | Triple-gate: enabled / apply_to_execution / live_activation_allowed / live_broker_authority | W7 shadow vs live. |
| `src/components/news_calendar.py` + `config/agent_config.yaml` `news_filter` | W7 HIGH filter (JSON/CSV/MT5) | **Not** F5 Warsh spine. Do not call this `NEWS_PROTOCOL`. |
| `judgment/astra/*` + `.agents/skills/typesafe-ai/SKILL.md` | Sidecar docs + TypeSafe: compose in code; confidence ≠ permission | This branch. |
| `docs/audits/opus5-architecture-20260725/OWNER_SESSION_CONTEXT.md` | Owner: build the system; profitability is the mission | Direction, not F5 desk law. |

`python3 scripts/gtos_context.py pack` was **not** used as authority. `context/` (no leading dot) **does not exist** on this clone.

### Off this checkout, same session — not Project memory

| Source | What it is |
|---|---|
| Session uploads under `/home/ubuntu/.cursor/projects/workspace/uploads/` | Challenge 45 jsonl + V1 sweep + Astra 2026-09-16 notes. **Not git.** |
| `origin/f5-leftover-ship` @ `37056de60` (“Ship Fable 5.1 leftover…”) | Closest GitHub F5 desk. Paths: `scripts/f5_desk/{composer.py,decide.py,inbox_gates.py,write_inbox_verdict.py,JUDGE-CHARTER.md}` and `src/components/ultimate_book/minimal_size.py`. Session copies also sat in `/tmp/f5desk/`. |
| Leftover-ship `knowledge_base/astra_verdict_context.md` | **2026-04-09 WF-1 U1/U2.** Not `ASTRA_ARCH`. Do not reuse as F5/Jev architecture. |

### Two fire-paths — do not splice them

```
W7 (this main)                         F5 Challenge (leftover-ship / live f5-live)
run_book.py --tags                     writer minimal_size.py + --tags + $KILL
  → UltimateBookOwner                    → occupancy / 15m yield / HIGH T-15..T+60
  → already_placed_today (1/day)         → isolated re-entry is a NEW named fire
  → RealMT5.order_send                   → chair verdict.json via write_inbox_verdict only
      enforce_broker_mutation_authorized → Nightly decide.py n≥40 / 2·SE
      (token + config digest)
```

Copying W7 `already_placed_today` onto F5 **kills** the wanted spring reprint after 15m flat. H8 on this clone: `book_owner._flatten_all_engines` records `flatten_request_observed_broker_mutation_disabled` and **returns** when `live_broker_authority` is false. Jev is not an emergency flatten.

Leftover-ship `JUDGE-CHARTER.md` still names **FTMO 0** and **$150** risk. Owner calibration account is Challenge **0** / **$110k**. That leak is a charter repair, not a Jev surface.

---

## 1. On what real system logic should fine-tuning rest?

Not “maximize kept PnL on 45 closes.” The Challenge week already has a **causal stack** (Astra Challenge truth). Fine-tuning must price **those mechanisms**, in this order:

### 1.1 Writer laws (code tissue) — senior, not trainable

These already decide whether a candidate can exist. Jev does not re-vote them.

| Law | Where it lives (leftover-ship @ `37056de60` unless noted) | What “tune” would mean if you put it in Jev | Correct owner |
|---|---|---|---|
| `--tags` ∩ hard-off / `$KILL` | leftover-ship `src/components/ultimate_book/minimal_size.py` `F5_HARD_OFF_SLEEVES` + live Sep 13 prefixes | Model “discovers” bleed/US30 | **Owner surface.** Already cut. |
| US30 off | live `ON_SURFACE` empty; leftover-ship **still lists US30** | Echo digest | **Owner.** Do not carry leftover-ship lists. |
| Token digest = config/tag bytes | `src/safety/activation_token.py` `config_digest_sha256` | Soften mismatch | **Owner ceremony.** Mismatch = silent no-place. |
| Occupancy keep-one | writer; inbox **demotes** occupancy HOLD | HOLD after fill | **Script.** Dead as intelligence. |
| Isolated re-entry ≥15m on a **flat** symbol | `f5_daily_cap_yields_isolated_reentry`; charter: **do not HOLD it** | Model “blocks remints” and also blocks the wanted spring reprint | **Writer integer + sibling file.** |
| 2-stop same-sleeve orig_stop / UTC day | **live** `F5_SAME_SLEEVE_ORIG_STOP_DAY_CAP=2` (not in leftover-ship) | Noul instead of a count | **Writer.** Jev may *label* a would-be third fire; it must not be the counter. |
| Leave-orig | charter verb 1; Challenge breach_flatten **0** | Close-label → manage:close | **Forbidden.** Fear flatten is the verification leak. |
| `cost_screen` | writer refuse; frozen CF mean **−0.53 R** (refuse paid) | Ignore commission until after fill | **Keep the screen.** Jev may Score “cost vs stop” only as study. |
| HIGH window T−15..T+60 | leftover-ship `inbox_gates.py` + `F5_HIGH_PRE_MIN`/`POST` in `minimal_size.py`; spine compose `f5_compose_official_high_spine` (`gtos.news.official_high_spine.v1`) | Invent FOMC rows | **Calendar spine on the writer host.** There is **no** `NEWS_PROTOCOL` file on git (GAP). W7 `news_calendar.py` is a different organism. |
| Dead window 21–00Z; Friday 16:00Z cutoff | leftover-ship `minimal_size.py` `F5_DEAD_WINDOW_UTC_HOURS` / Friday cutoff | Session vibes | **Writer clock.** |
| H4 one-unit/day (`idxrev`, `orb_crypto*`) | `F5_H4_DAILY_UNIT_CAP_SLEEVES` | Already hard-off those names | Inert if hard-off holds. |

Fine-tuning that fights these laws is **wrong-tissue**. The only legal Jev effect on them is **shadow disagreement**: “code says enrolled, Jev says refuse” → chair/Nightly **table**, not a send-path AND.

### 1.2 Intelligence laws (chair + nightly) — Jev may draft, never author

| Law | Binding sentence | Jev role |
|---|---|---|
| VETO is **pre-fill only** | Charter verb 2. `approve` is inert. Writer places on PASS. | Draft `{hold, mechanism, why_code}` **while `status` is `intent`/`standing`**. |
| Four VETO mechanisms | `microstructure \| correlation \| event_proximity \| weekend_carry` | Propose **from that set only**. |
| Occupancy words are not intelligence | `inbox_gates.py` → `occupancy_script_not_intelligence` | Never emit those why_codes. |
| Isolated re-entry is not a remint | Charter: new named fire; do not HOLD | Jev HOLD on a legal 15m reprint is a **defect**. |
| Nightly KEEP/OFF | `decide.py`: n≥40 and \|mean R\| > 2·SE, else **WATCH** | Jev may **label rows** for the body. Jev does not emit KEEP/OFF. |
| Default on a LIVE ticket | **Silence** | Close-label is LABEL, not manage. |

### 1.3 Information laws — strongest first use of Jev

| Law | Why Jev fits | Hard no |
|---|---|---|
| Composer slate is the state | leftover-ship `scripts/f5_desk/composer.py` `SLATE_SCHEMA = gtos.judgment.slate.v2`; `EXPOST_KEYS` scrub P&L | Rebuilding state from **closes + broker_net** |
| Calendar HIGH is folded onto the candidate | `high_impact_minutes` (signed), `minutes_to_nearest_high_impact` | Inventing HIGH |
| Close LABEL nouns | Charter: `orig_stop \| broker_tp \| time_stop \| …` + `BREACH:` | Calling time_stop `manual_other` then accepting it at conf≥0.8 |
| Challenge login only | **0** / ns `operator` / magic **0** / pass **$110k** | Verification **0** (charter file still leaks it — repair, not a Jev surface) |

Doctrine on this clone (`.context/00_core/research_operating_doctrine.md` “Existing Data Policy”) forbids selecting a rule on a dataset and calling the same dataset validation, and forbids using post-event features in as-of rules. V1 did both. `.context/00_core/llm_specialization_research_backlog.md` forbids treating a specialized model as a shortcut without non-leaky labels and a sealed eval. TypeSafe skill (`.agents/skills/typesafe-ai/SKILL.md`): **code owns the workflow**; confidence is distribution peakedness, not permission.

### 1.4 What “tune” means under V2

Tune **questions, state completeness, and code composition** so that:

1. House laws fire **identically** with Jev down (missing sidecar = abstain, book unchanged).  
2. Jev **agrees** with house toxic at high Noul / `hard_refuse` (consistency), and **disagrees** only on the remaining 51-tag STUDY/STARVE surface.  
3. Pre-fill HOLD drafts appear **before** `status=placed/filled` on correlation / event clusters.  
4. Close-label `time_stop` vs `orig_stop` vs `broker_tp` matches charter nouns on a **held-out** post-cut pack.  
5. Nightly still cannot KEEP/OFF a family with n=2.

**PnL of a counterfactual KEEP set is a shadow metric, not an objective.** The payout path is a **quiet cut surface** that lets spring / vss / (STUDY) three_fresh print 3R-class time_stops — not a busier classifier.

### 1.5 This GitHub tree is not the live printer

| Surface | Use |
|---|---|
| This PR / `main` | W7 three-sleeve book + `judgment/astra` docs. **No** `scripts/f5_desk/`, **no** F5 `minimal_size.py` on this checkout. |
| `origin/f5-leftover-ship` @ `37056de60` | Closest GitHub F5 desk. **Stale vs Sep 13 cuts** (US30 still on DSP/XA lists; **`F5_SAME_SLEEVE_ORIG_STOP_DAY_CAP` absent** — `git grep` on that branch is empty). |
| Live VPS `f5-live` @ `c19c3aff9` | Printer of record. **Not on this clone. Not fetched this session.** |

V2 schemas may live here. V2 **clients** that touch F5 code must branch from leftover-ship / a GitHub mirror of `f5-live`, never silent-edit this W7 `book_owner` (hard-refuse `already_placed_today` would **kill** wanted isolated re-entry).

---

## 2. What state must Jev see at candidate time?

One object, **no realized PnL**. Prefer named JSON (TypeSafe: object state, path-referenced questions). Composer already strips `EXPOST_KEYS`. Reuse that set in any Jev client.

### 2.1 Identity and bind (code-filled, fail-closed if missing)

```
account.login              = "0"          # const
account.ns                 = "operator"
account.magic              = "0"
account.pass_target        = "$110k"
account.verification_quarantined = "0"

slate.schema               = "gtos.judgment.slate.v2"
slate.slate_id             # sha16 of slate body
slate.fingerprint          # candidate_id|status ∪ ticket|direction
slate.built_at_utc
slate.clock                # broker server + UTC; never invent EET+3
candidate.candidate_id
candidate.alias_ids[]
candidate.status           # intent | standing | placed | filled | refused
```

If `slate_id`/`fingerprint` missing → **do not call Jev** (same refusal as `write_inbox_verdict.py`). A close-reconstructed row is **STUDY** (`admit_clock=as_of_open` or `as_of_close`), never LIVE admit.

### 2.2 Slate candidate fields (composer `_candidate_view`)

| Field | Why Jev needs it |
|---|---|
| `symbol`, `sleeve`, `cluster`, `direction` | Family + correlation |
| `decision_day`, `decision_bar_iso` | Session / bar identity |
| `geometry` {`entry`,`stop`,`target`} or `stop_dist` | Plan R, side, width — **not** a geo Score of “1 = house 1/6” |
| `first_seen_utc`, `last_seen_utc` | Stale standing intent (composer 26h) |
| `last_refusal_class` | Writer already refused once |
| `high_impact_minutes`, `minutes_to_nearest_high_impact` | Event VETO draft |
| `spread_r_of_stop` (from fill/intent `spread_r`) | Microstructure VETO |
| `governor.allow`, `governor.cap_mult`, `governor.reason` | Book already derisking |

### 2.3 Sleeve family (code classification, passed as **facts**)

Do not ask Jev “what family is this?” if the writer already knows the tag.

```
family_class ∈ { house_keep, house_hard_off, study, starve_watch, other_tagged, unknown }
house_keep     = dsp_spring* | vss_fxcross*
house_hard_off = bleed | orb_crypto* | idxrev | xa_huge* | mx_us30 | prior week-repair hard-offs
                 + symbol US30*
starve_watch   = walked_high | isolated_spike | two_bar | three_bar_squeeze | named Astra §4.3
study          = three_fresh | spring_first_print (n=0) | other n≤2 mixed
```

`unknown` (replay ticket `292427064`) is a **state defect**. Jev `state_sufficient` must go low; code escalates. Do not invent a sleeve from PnL.

### 2.4 Remint / 2-stop / 15m (writer integers — Jev sees them, does not own them)

```
occupancy.symbol_open                 bool
occupancy.symbol_pending              bool
occupancy.already_placed_today        bool   # (sleeve, symbol, UTC day)
occupancy.cluster_placed_today        bool
siblings.just_closed_same_symbol      bool
siblings.minutes_since_flat           number | null
siblings.same_sleeve_orig_stops_utc_day   int   # 2-stop remaining = 2 - this
siblings.isolated_reentry_legal       bool   # writer yield: flat AND ≥15m AND not H4-cap AND not 2-stop exhausted
siblings.remint_parent_ticket         int | null  # STUDY/close only if already known; LIVE only if ledger has it
circuit.two_stop_cap                  const 2
circuit.two_stop_exhausted            bool
```

**Composition (code, not Jev):**

- Occupied → writer keep-one. Jev **silent**. Occupancy HOLD is dead.  
- `isolated_reentry_legal == true` → **new named fire**. Jev may still draft **correlation / event** HOLD (named mechanisms). Jev may **not** draft HOLD because “this looks like a remint.”  
- `two_stop_exhausted` → writer refuse. Jev log-only.

### 2.5 News / Walter / information

```
calendar.events[]          # composer fold; impact, currency, minutes_from_now
calendar.spine_extracted_utc
calendar.spine_empty        bool   # fail-closed: empty ≠ "no HIGH"
walter.present              bool
walter.card_id              string | null   # if Mac Project supplies a named tape card
# NEVER: hand-written FOMC/BOJ rows, chat summaries, invented HIGH
```

`event_proximity` Noul is asked **only** if `high_impact_minutes` is in-window **or** spine_empty (then the Noul should go ~0.5 / abstain — insufficient state). Code already HOLDs in-window on approve (`inbox_gates`). Jev’s job is **draft before send** when the chair sit is dark, not to replace the gate.

### 2.6 Geometry and cost (pre-fill, not ex-post)

```
geometry.entry, stop, target
geometry.plan_r                 # |target-entry| / |entry-stop|  — CODE
geometry.stop_atr, target_atr   # if the sleeve contract is known
geometry.fx_stop_pips           # FX DSP 8-pip floor is writer
cost.spread_r_of_stop
cost.expected_commission_r      # from symbol class table, not the close
cost.cost_screen_would_refuse   bool
```

Do **not** pass `broker_net`, `R`, `hold_min` realized, MFE/MAE, `exit_class` into **LIVE** admit state. Those are **targets** for STUDY / close-label.

### 2.7 Token / surface digest (facts)

```
surface.us30_off                    const true
surface.hard_off_families[]         bleed, orb_crypto, idxrev, xa_huge, mx_us30
surface.keep_families[]             spring, vss
surface.circuit                     "2-stop"
token.namespace                     operator
token.config_digest_sha256          # running process
token.digest_matches                bool
tags.digest_or_list_hash            # argv tags, not a novel
tags.contains_spring_and_vss        bool
tags.contains_hard_off_name         bool   # $KILL if true
```

`digest_matches == false` → writer will not increase exposure. Jev call is optional (log “would have scored”). **Never remint from this sidecar.**

### 2.8 Cluster (corr HOLD draft)

```
cluster.cluster_id
cluster.symbols[]
cluster.direction
cluster.same_dir_count
cluster.candidate_ids[]             # still intent/standing only
cluster.any_already_live            bool  # if true, VETO on those tickets is theater — log, do not "save"
```

### 2.9 What must **not** be in LIVE state

Composer `EXPOST_KEYS` plus: `broker_net`, realized `R`, `exit_class`, `close_reason`, `toxic_remint`, MFE/MAE, “this ticket later won,” verification-login features, `f5_study` rows, chat autopsy prose.

---

## 3. Where Jev sits vs writer enrollment vs chair vs Nightly Decide

```
                         OWNER
                    surface, token, splice
                            │
              ┌─────────────▼──────────────┐
              │ Chair redacted_account               │
              │ ENFORCE / VETO / LABEL     │
              │ write_inbox_verdict.py     │  ← ONLY writer of verdict.json
              │ fingerprint-bound          │
              └─────────────┬──────────────┘
                            │ shim (approve→PASS, hold→HOLD, abstain→PASS)
                            │ occupancy HOLD demoted
              ┌─────────────▼──────────────┐
              │ Writer / printer           │
              │ tags, hard-off, 2-stop,    │
              │ 15m yield, leave-orig,     │
              │ cost_screen, token digest  │
              │ NEVER imports Jev          │
              └────────────────────────────┘

   composer → latest_slate.json
        │
        ├─ LIVE shadow: Jev admit / corr-HOLD  ──► files under
        │     judgment/live/jev_sidecar/              (or judgment/astra/done/)
        │     chair MAY read as a draft
        │     chair MAY ignore
        │
        └─ STUDY: close-label / nightly feature rows
              → decide.py (Python n≥40 / 2·SE)
              → DECISIONS.md for chair 07:00 ICT
              → splice_proposal for OWNER, never --tags
```

### 3.1 Writer enrollment (before Jev)

A candidate is **enrolled** only if:

1. sleeve ∈ live tags,  
2. not house hard-off / US30,  
3. token digest matches,  
4. occupancy allows **or** isolated-reentry yield is legal,  
5. 2-stop not exhausted,  
6. cost_screen / dead-window / Friday cutoff / HIGH-window writer checks pass.

Jev is invoked **after** enrollment for LIVE shadow (the fire the printer is willing to send), **and** optionally on refused rows as STUDY (“would Jev have agreed with the refuse?”).

Jev **must not** enroll. A KEEP recommend on a non-enrolled sleeve is a schema break.

### 3.2 Chair verbs (after Jev, or without Jev)

| Chair verb | Input | Jev may | Jev may not |
|---|---|---|---|
| ENFORCE | open positions | nothing (silence) | manage, flatten, take-at-R |
| VETO | intent/standing candidates | draft hold + one charter mechanism | write inbox; invent occupancy why; HOLD a legal 15m isolated fire *as remint* |
| LABEL | closes | propose `exit_class` + memory line | overwrite scoreboard; splice tags |

Missing / low-conf Jev: chair works as today. **Fail-open PASS on missing verdict is already the desk rule** — Jev must not *increase* fail-open place. Missing Jev row = **no extra PASS**.

### 3.3 Nightly Decide (orthogonal clock)

`decide.py` is **read-only arithmetic**. It does not call Jev. Jev Experiment D (Astra 2026-09-16) may **Score row completeness** and **label exit_class** so the **body** of `f5_study` becomes Challenge-true (login 0). The KEEP/OFF sentence stays Python.

V1’s “Nightly Decide before splice” line was correct. V1 then **implied a KEEP gate from n=6**. V2 forbids that implication.

### 3.4 Astra job inbox (third seat)

Per orchestration sketch: Jev output is an Astra `jev_offline` / future `jev_shadow` **job receipt**. Printer must not glob `judgment/astra/`. Two inboxes stay two inboxes.

---

## 4. Atomic questions vs composition in code

TypeSafe law (docs + skill): **one snap judgment per question**; mix primitives on one state; **compose in code**; questions in one request **cannot see each other**. Do not ask “should we KEEP this trade?”

### 4.1 LIVE admit — atomic (one request)

| ID | Primitive | Instructions (intent) | Code may use it for |
|---|---|---|---|
| `state_sufficient` | Noul | Does this state contain enough **named** slate fields (id, sleeve, geometry or stop_dist, status, family_class) to judge? | If low → escalate / skip; do not trust other answers |
| `admit` | Choice `admit \| abstain \| hard_refuse` | Given **remaining** STUDY/STARVE/KEEP surface **and** house_law already applied, is this a fire the intelligence tissue would still want scored as admit? **Not** “will it profit.” | Secondary; never send |
| `fast_stop_hazard` | Score 0–2 | 0 = likely die inside 15m at this stop width vs session/tape facts in state; 1 = ordinary house risk; 2 = stop width / session historically survives to sleeve horizon | STARVE watch table; **not** a KEEP floor at 1.0 |
| `side_formula_tension` | Noul | Does shipped side conflict with occupancy/session story **in state** (bleed LONG vs DOWN occupancy)? | Escalate; house already hard-off bleed |
| `cluster_same_dir` | Noul | Are there ≥2 same-direction correlated intents on **this slate**? | Feeds corr-HOLD request or same-call speculative |
| `event_proximity` | Noul | Is a **named** HIGH in T−15..T+60 for this symbol’s currencies **in `calendar`**? | Chair draft; code already gates |

**Do not ask LIVE:**

- `surface_ok` as a substitute for US30/hard-off (code). Optional **consistency** Noul: “does `family_class` agree with `surface.hard_off`?” — if Jev says no, **log digest bug**, do not override code.  
- `toxic_family` as the hard-off (code). Same: consistency only.  
- `toxic_remint` (close-only).  
- `confidence_gate` Score (nested).  
- Flatten / remint / take-at-R.

### 4.2 LIVE corr HOLD — atomic (same or second request if cluster built from first)

Keep V1 primitives; tighten state:

| ID | Primitive | Notes |
|---|---|---|
| `speak_hold` | Noul | Pre-fill only; `cluster.any_already_live` true → still may speak **audit_only**, never flatten |
| `hold_strength` | Score 0–2 | Levels = n/direction facts in state, not vibes |
| `mechanism` | Choice `correlation \| event_proximity \| microstructure \| weekend_carry \| none` | Closed charter set + `none` |
| `action_scope` | Choice `audit_only \| block_sibling_prefills` | **No flatten key** |

### 4.3 Close-label — atomic (STUDY / LABEL assist)

| ID | Primitive | Notes |
|---|---|---|
| `exit_class` | Choice `orig_stop \| broker_tp \| time_stop \| breach_flatten \| other` | **Must include `time_stop`.** Do not collapse to `manual_other`. Charter nouns. |
| `contract_breach` | Noul | Exit was not orig_stop / broker_tp / time_stop / limit expiry |
| `remint_child` | Noul | Same-sleeve child after orig_stop **given parent fields in state** |
| `lesson` | Score 0–2 | 0 noise / 1 single sample / 2 pressure for **Nightly table** — never auto hard-off |

Accept `exit_class` only if confidence ≥ a **pre-registered** floor **and** choice ∈ {orig_stop, broker_tp, time_stop}. Else chair LABEL from broker deals only. V1’s 0.8 floor is **lab-starting**, not ratified — and would have accepted wrong time_stop labels.

### 4.4 Composed in code (never a mega-question)

```
house_block = US30 or hard_off or two_stop_exhausted or digest_mismatch
             or cost_screen_refuse or dead_window or high_window_code
             or (occupied and not isolated_reentry_legal)

if house_block:
    disposition = log_only_blocked_by_writer   # Jev answers ignored for fire
elif not state_sufficient:
    disposition = escalate_chair
elif isolated_reentry_legal and jev.speak_hold and mechanism == "correlation":
    disposition = draft_hold_siblings           # chair still writes inbox
elif admit.choice == hard_refuse and admit.confidence >= CONF_SECONDARY:
    disposition = escalate_chair                 # still not a writer hard-off
elif admit.choice == admit and admit.confidence < CONF_SECONDARY:
    disposition = escalate_chair                 # more abstain
else:
    disposition = log_only                       # printer unchanged

# NEVER: disposition = place | remint | flatten | splice_tags | write_verdict
```

`CONF_SECONDARY` starts as the **lab** 0.55 abstain floor (TypeSafe: low conf → do not act). It is **not** a KEEP threshold. Raising it to 0.80 to pick Challenge winners is V1 and is **out of contract**.

Weights (TypeSafe composite-scoring pattern) may combine `fast_stop_hazard` + `cluster_same_dir` + `event_proximity` for a **nightly feature row**. Changing a weight must not require a new model call if the atomic answers are stored.

---

## 5. How to use Challenge replay without overfitting one toxic week

### 5.1 What the 45 rows are

| Band | n | PnL | Use |
|---|---:|---:|---|
| Pre-cut Sep 9–11 | 36 | −4,748.54 | **Mechanism pack**: toxic families, remint chains, US30 leak, HOLD-too-late, 1/6 deaths. **Not** a KEEP grid. |
| Post-cut Sep 14–16 | 9 | +452.62 | **Honest live-surface sample.** Still n=9. WATCH. |
| Verification 0 | — | — | **Quarantine.** Pattern rhyme only (soft floor, DSP both sides). Sign flips (`xa_*`). |

Days: 12+17+7 on 9–11 Sep, then 1+3+5 on 14–16. One week, two regimes. Pooling them as “the book” is the V1 error.

### 5.2 Pre-registered metrics (freeze **before** another sweep)

**LIVE / as-of-open (no PnL in state):**

1. **House-consistency:** on house_hard_off ∪ US30, `admit_then` (or code) block rate — **already 22/24 hard_refuse on admit_now**; report **as-of-open** separately. Target: code 100%; Jev consistency is secondary.  
2. **KEEP-family false-off:** spring / vss / three_fresh **as-of-open** `hard_refuse` at conf ≥ 0.55. Fail if we would have blocked `291794419` or `292667008` **and then wired it**.  
3. **STARVE-hazard hit:** two_bar / three_bar / isolated / walked **as-of-open** `fast_stop_hazard` high vs low — **not** PnL.  
4. **State sufficiency:** fraction of rows that would be **illegal LIVE** (no slate_id / unknown sleeve). Replay 45 is mostly illegal LIVE by construction.  
5. **Corr HOLD timing:** on Sep 9–10 bleed cluster, would a draft HOLD exist **while status=intent**? (Needs **launcher slates**, not closes. **Not in this clone.**)

**LABEL / STUDY (closes allowed as targets):**

6. **exit_class** accuracy with `time_stop` as a first-class label; report **by class**, not 39/45.  
7. **remint_child** vs `remint_of` (6 labeled chains).  
8. Calibration: high-conf bins vs accuracy (reliability diagram). Vendor confidence is **not** GTOS-calibrated until this exists.

**Forbidden as primary:**

- Kept PnL, Δ vs actual, “blocked loss $.” Those are **shadow** columns after the metrics above.  
- Fitting thresholds to keep ≥4/6 winners.  
- `admit_now` + today surface on pre-cut rows as a LIVE metric (may exist as a **hindsight diagnostic** only, labeled as such).

### 5.3 Splits

- **Train / sweep:** do not. V1 already spent that look.  
- **Confirm:** post-cut Challenge closes **after** 2026-09-13T14:37Z **not used to pick V1** — the 9 are already contaminated as a **second look**. Next confirm needs **new** Challenge days **or** live shadow slates.  
- **Hold-out forever:** do not spend Oct/Nov 2025 W7 windows on F5 Jev (wrong organism).  
- **Verification:** never in the same table without an `account.login` flag.

### 5.4 Live shadow is the real instrument

Log every **enrolled** slate candidate:

```
judgment/live/jev_sidecar/admit/{date}/{slate_id}/{candidate_id}.json
```

Compare later to broker outcome **without** changing tags. A/B in §7.4. Promotion to “intelligence soft-steer” requires owner Nightly word **and** Nightly-scale n.

---

## 6. V1 — keep / discard

See [`JEV_V1_CRITIQUE.md`](JEV_V1_CRITIQUE.md) for the full autopsy. Short:

**Keep:** primitives work; house toxic is code; LIVE/STUDY split; flatten forbidden; low-conf → escalate; orig_stop labeling on obvious SL; garbage refuse; never-place stamps.

**Discard as policy:** +$406 headline; 0.55/0.80/geo≥1.0 KEEP; admit_now-on-history; 600-composite selection; remint-cap STUDY as live bait; 86.7% close-label; `confidence_gate` as a layer.

**Reinterpret:** V1 numbers are **evidence that Jev echoes surface digest and cannot name time_stop**. That is useful. It is not a gate.

---

## 7. JEV_INTEGRATION_V2 contract

### 7.1 Schemas

| Schema | File | Mode | Purpose |
|---|---|---|---|
| `jev_admit_v2` | [`schemas/jev_admit_v2.json`](schemas/jev_admit_v2.json) | `shadow_log_only` | Feature-complete **intent** state + atomic answers + **code** composition |
| `jev_admit_v1` | `schemas/jev_admit_v1.json` | frozen | Lab/replay compatibility. **Not** the policy schema. |
| `jev_close_label_v1` | existing | frozen | Keep flatten/remint stamps; **v2 questions** in §4.3 until a close schema lands |
| `jev_corr_hold_v1` | existing | keep | Flatten `not` clause stays; add `mechanism` Choice in a later v2 if chair sit is alive |

No schema authorizes place. `never_place` / `never_remint` / `never_flatten` / `never_move_sl` are **required consts**.

Namespace for logs: `gtos.astra.jev_trial.v2` (avoid TypeSafe’s vendor “Astra/Fable” eval names).

### 7.2 Confidence is secondary

TypeSafe: confidence is **distribution peakedness**, not permission. Vendor bank-transfer @ 0.9 maps, for us, to **broker send** — threshold **infinite**. High conf still **escalates** on Challenge.

| Conf band (Choice/Score) | Code behavior |
|---|---|
| Low (< lab 0.55, or Noul ~0.5) | Abstain / escalate. Only authoritative Jev output: **more no-fire**. |
| Mid | Chair-visible draft. |
| High | Still not auto-place. Still not auto-hard-off. |

House laws do not read confidence.

### 7.3 Primary = house laws + feature-complete state

A V2 row is **evaluable** only if:

1. `account.login` is 0,  
2. `house_law` block is **computed in code** and stored (so we can audit),  
3. LIVE rows have `slate_id` + `fingerprint` + `status` ∈ {intent, standing},  
4. `EXPOST` keys absent on LIVE,  
5. writer enrollment fields present (even if false).

Otherwise `evaluable=false` and disposition is `log_only` / `state_illegal_for_live`.

### 7.4 Shadow metrics (log these; do not optimize a single $)

Per day / per slate:

- n enrolled, n Jev-called, n skipped (Jev down, state illegal),  
- house_block vs Jev `hard_refuse` **agreement**,  
- KEEP-family `hard_refuse` count,  
- STARVE `fast_stop_hazard` histogram,  
- corr-HOLD drafts with `status` and `any_already_live`,  
- close-label confusion **by class**,  
- latency, schema-break count, flatten-attempt count (must be 0),  
- **shadow** kept-PnL **last**, labeled `not_a_gate`.

### 7.5 A/B plan (shadow only)

| Arm | What | Allowed when |
|---|---|---|
| **A0** | Writer + chair only (Jev down) | Always the production book |
| **A1** | A0 + V2 log (no chair feed) | Key present; this is the default next step |
| **A2** | A1 + chair **sees** drafts (inbox still chair-written) | Clocks alive (`f5-chair-sit`); owner word |
| **A3** | Nightly body uses Jev **labels** only | Experiment D; `decide.py` unchanged |
| **B-forbidden** | Jev AND on send, remint, flatten, tag splice | Never in this contract |

A/B **comparison** is A1 vs A0 on the **pre-registered** metrics over **new** Challenge days — not a re-grid of the 45.

Pre-register the comparison window **before** reading it. If n is still << 40 per family, the only legal sentence is WATCH.

### 7.6 Sequence (still not implementation)

1. Land this contract + `jev_admit_v2` schema (this PR).  
2. Ingest Project shared files + Mac / `f5-live` artifacts listed in §8 (`MAC_INGEST`, `OWNER_LAW`, `ASTRA_ARCH`, `NEWS_PROTOCOL` first).  
3. Shadow A1 on **live slates** (not close reconstructs) when `TYPESAFE_API_KEY` is in secrets.  
4. Fix close-label questions (`time_stop`) on a **new** pack; reliability diagram.  
5. Corr-HOLD A2 only after chair sit can consume a draft **pre-fill**.  
6. Splice / soft-steer only if A1 beats **house-only** on pre-registered metrics **and** owner Nightly word.

Until step 3, Jev is still a **sidecar design + one contaminated replay**.

---

## 8. GAPS — Project waves not in git

This agent searched this checkout, `origin/main`, and `origin/f5-leftover-ship` for a `context/` tree and for files named `MAC_INGEST`, `OWNER_LAW`, `ASTRA_ARCH`, `NEWS_PROTOCOL`. **Zero files. Zero hits.** There is no `context/` directory (only `.context/`, the 2026-06 Context OS tree). GitHub code search on `Borr1/ai-trading-agent` for those four names also returned no items.

**V2 therefore requires Project gtos shared files that are not in this repository.** Until they are ingested onto disk (or a named Mac/VPS path is opened), no agent may claim Project-wave memory or a wired sidecar.

### 8.1 Required Project gtos shared files (missing)

| Required file | Searched | Closest thing that *is* on disk | Why V2 cannot substitute |
|---|---|---|---|
| `MAC_INGEST` | this clone, `origin/main`, leftover-ship | `.context/00_core/mac_migration_package_evidence_index.md` + `portable_path_authority.md` (2026-06 Mac *research* workspace, not F5 live) | Does not carry live argv, slate, token digest, or 2-stop prove. |
| `OWNER_LAW` | same | `TYPESAFE_HANDOFF_20260917.md` house-law bullet + leftover-ship `JUDGE-CHARTER.md` (still **0 / $150**) + CLAUDE armed-set | Charter and owner Challenge login **disagree**. Need the Project law file, not a merge of stale pages. |
| `ASTRA_ARCH` | same | This file’s Fable-tissue table + leftover-ship `knowledge_base/astra_verdict_context.md` (**2026-04 WF-1 U1/U2**) | April WF-1 is the wrong organism. Fable tissues (code / intelligence / information / chair) are **not** a committed architecture note outside `judgment/astra`. |
| `NEWS_PROTOCOL` | same | Leftover-ship `f5_compose_official_high_spine` (`gtos.news.official_high_spine.v1`, intel path `C:\Users\trader\intel-layer\calendar`). W7 `src/components/news_calendar.py` + `news_filter` | Two different calendars. No protocol file names endpoints, freshness, or fail-closed empty-spine. Event Noul without this is abstain. |

If the Project stores those under different basenames, **ingest the real files and rename the pointer here.** Do not invent contents from chat.

### 8.2 Required live / Mac artifacts (also missing from this clone)

| Artifact | Why |
|---|---|
| Running `f5-live` argv: tags, `$KILL`, ns, magic, terminal path | Confirm spring+vss present; bleed/orb/idxrev/xa_huge/US30/mx_us30 absent |
| Live `minimal_size.py` sha vs `c19c3aff9`; prove `F5_SAME_SLEEVE_ORIG_STOP_DAY_CAP` | leftover-ship **lacks** the constant (`git grep` empty) |
| `pipeline_state/.../judgment/state/latest_slate.json` + one archived `slate_*.json` | Real candidate field population (geometry present or not) |
| `just_closed_siblings.json` + 2-stop walk | Isolated re-entry vs circuit |
| Token digest (not the token) vs running config | Digest-match bit in state |
| `official_high_spine.json` `extracted_utc` + **`NEWS_PROTOCOL` endpoint status** | Event Noul is honest or abstain |
| Walter / desk_briefs if the Mac Project emits them | Information tissue; do not invent |
| Post-cut `closed_joined` after the 9 | Next confirm sample |
| `f5-chair-sit` / `nightly-decide` health | A2/A3 gates |
| Charter / `f5_study` identity stamp | leftover-ship charter still leaks **0** |

Without §8.1 **and** §8.2, V2 can specify **seams** and still cannot claim a single live candidate was scored.

### 8.3 What this agent will not pretend

- It is not the Fable coordinator and has no Project chat transcript.
- `.context/LIVE_STATE.md` on disk is **2026-08-08 / `9392e9bbd`**, not today’s F5 book.
- `current_vnext_system_map.md` / `00_READING_ORDER.md` / `quick_reference_card.md` describe a **halted V4 / 24-symbol** world. CLAUDE and `live_system_of_record.md` supersede them for W7. None of them are the F5 Challenge printer.
- Leftover-ship is the closest GitHub F5 desk and is **stale vs Sep 13 owner cuts**.

---

## 9. Explicit non-goals

- No place, remint, flatten, take-at-R, token mint, MT5 login switch.  
- No Jev import in `book_owner` / `open_trade` / `minimal_size` send path.  
- No `verdict.json` write except `write_inbox_verdict.py`.  
- No leftover-ship DSP `ON_SURFACE` carry (reopens US30).  
- No verification splice.  
- No invent HIGH.  
- No claim that this PR live-wires Jev.  
- No secrets in repo or chat.

**Payout objective:** a quiet, cut book. Jev earns its keep if it **labels and drafts** so the chair can VETO **before** send and Nightly can read Challenge-true nouns. It does not earn its keep by reprinting +$4,702 from a week already cut.