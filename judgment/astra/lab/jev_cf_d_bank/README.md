# Chair CF D bank — Challenge 0

Historical question bank (n=10) + CF D report. No live bars. Never place.

`CF_D` (+11.1678 R on Challenge-60): stand_down `false_structure` when
not KEEP; KEEP exempt. Ablation `miss › size › family › session › conf`.
Not INDEX/CRYPTO/xa size0 religion.

Train hints in the bank are F+G-era. Chair CF D overrides FS non-KEEP
from `B_SIZE_HALF` to `A_STAND_DOWN`. KEEP stays `E_KEEP_CAP`.
