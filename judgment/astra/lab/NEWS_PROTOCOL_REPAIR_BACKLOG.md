# NEWS_PROTOCOL ranked backlog

Ranked by **leverage for feeding typed state into Jev** on Challenge
`0` / `operator`. Full evidence:
`NEWS_PROTOCOL_REPAIR_MAP.md`.

Lock: missing protocol = repair item. **Zero invented endpoints.**
`calendar_honest` is already APPLIED as writer-not-READ. Do not invert
`spine_empty`.

| Rank | ID | Class | Repair | Owner | Dependency | Jev leverage |
|---:|---|---|---|---|---|---|
| 1 | G1 | MISSING wire | Pass `news_inventory_at` into live A1 / `compose_shadow(..., extra=)` the same way `challenge_shadow` already does | Judgment | Existing `host_events.py` + landed events jsonl | Highest. Live observe currently cannot see unread |
| 2 | G2 | LIVE emit / MISSING read | Host writer actually parses existing `f5_high_calendar.json` and stamps `inventory_status=READ` | Chair / host f5-live | The 13-event spine already on the VPS. No new URL | Unlocks `calendar_honest=true` on Sep 15+ without flipping spine |
| 3 | G3 | STALE prompt | Rewrite `jev_questions.calendar_honest` to Wave M meaning (writer-READ or no writer row; unread is false; empty ≠ no HIGH) | Judgment | Wave M `_calendar_honest` | System One matches APPLIED label |
| 4 | G4 | MISSING file | Ingest real `NEWS_PROTOCOL` (or the Project’s real basename) onto disk. Record endpoints **from that file** | Owner / Mac Project | File must exist. Do not draft it | Freshness / endpoint status become typed. Until then abstain on freshness |
| 5 | G5 | STUB | Host `official_high_spine` + `news_brief` are 0-kept. Compose **writes** the spine; do not recycle it as input. Ingest a dated copy only when events exist | Host composer | Live brief or live (not stale) calendar dump | Second axis. Today Challenge axis is the 13-event host calendar only |
| 6 | G6 | MISSING on this branch | Copy leftover-ship calendar-fold + `inbox_gates` T−15..T+60 **named files only** onto Challenge. Do not wholesale `book_owner.py` | Chair | Host already folds `f5_high_calendar`. This tree already has 15/60 in `news_spine.py` | Reproducible slate `calendar_fold` here |
| 7 | G7 | MISSING / STALE | Intel-layer + Walter + `news_tape`. Name the host. Leftover-ship Mac path is not the Challenge VPS. `news_tape` stays excluded (stale 2026-08-27) | Mac Project vs VPS | Real files on that host. Path from `NEWS_PROTOCOL` or a measured listing — not invented | `walter.present` stays false until then |
| 8 | G8 | Optional W7 tool | FF this-week via **already-cited** `https://nfs.faireconomy.media/ff_calendar_thisweek.json`. Dated snapshot only. Never overwrite June. Never replace Warsh-class | Operator | Fetch fail → exit 2, no hand-written HIGH | Not Challenge law. Do not retune `warsh_class` from FF |
| 9 | G9 | W7 only | `news_calendar.py` fail-open if JSON empty. Keep Challenge off this component | W7 | Operator weekly JSON | None for Challenge Jev |
| 10 | G10 | SHADOW wait | `FLUID-NWS-005` needs a real as-of outside 10d of a real spine event | Time | G2/G5 calendar repair | Do not invent empty. Do not invert unread into `spine_empty` |

## DeItaone / headline mill (not a ranked invent)

External transmission. May be down. Two historical `DeItaone/<id>` stamps on
past unscheduled NFP only. No mill client in git.

Degrade: keep stamps historical; `calendar_honest=false` when writer present
and not `READ`; never set `spine_empty` because the mill is down; do not
synthesize prints.

## Done when

- G1 lands on Challenge observe (typed unread visible to Jev).
- G2 produces at least one host row with `inventory_status=READ` **or** a
  named host reason it cannot (still `NOT_READ` / `UNKNOWN`, never invented HIGH).
- G4 is either ingested or still explicitly missing — not drafted.
- `spine_empty` on covered Challenge as-ofs stays false.
