# F5-live commit ceremony prep

APPLY era. Ticket **293332188 leave orig**.
BOE print ~11:01Z sit: still open ~+0.6R — **leave orig**.

Owner NAME 2026-09-17 ~18:18 ICT: **"apply everything yes"** (Alive organism).
Both named wires are `APPLIED_NAMED`. Full 48-fluid shadow observers land with them.

## Do not

- Wholesale-copy GitHub `book_owner.py` onto the dirty 10069-line host.
- Edit `selector_v4.py` (R2 / H1).
- Place, remint, flatten, or write inbox.
- Convert envelope walls into Jev toys.
- Print the TypeSafe key.

## Land order

See [`HOST_APPLY_ALIVE.md`](HOST_APPLY_ALIVE.md).

1. `bridge.py` `admit_and_size(` (pulled :572) — AFTER `_decide`. Env-gate first.
2. `book_owner.py` `_spread_cost_screen` (pulled :9531) — AFTER `cost_skip`. Do not mutate skip.
3. APPLY the two named tilts after skip is None. Never refuse. Never touch 293332188.
4. `GTOS_JEV_ALIVE_SHADOW=1` + `GTOS_JEV_APPLY_LIVE=1` on f5-live only.
5. `SEL-V4-002` stays in `src/judgment/a1_log.py`.
6. Confirm host key fingerprint `00000000`.
