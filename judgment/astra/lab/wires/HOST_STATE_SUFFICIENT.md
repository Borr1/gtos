# HOST STATE SUFFICIENT — Chair land after Wave M

Tip on this branch after prove: land **`src/judgment/`** onto Challenge f5-live
(`0` / `operator` only). Do **not** wholesale-copy GitHub
`book_owner.py` onto the ~10069-line dirty host.
Harvest extras stay on `gold_state.v0` (Chair NAME 2026-09-18); do not cut `v0.1` on host — research-only, live multiplier 1.0. P0-2 `route_class` table is Chair-named as SHADOW labels only (`metal`/`fx_major`/`fx_cross`/`index`; UK100/BTC/ETH unknown). Do not APPLY on host. P0-3 / P0-4 idle.

Prove C still waits the first live place with `combined_live_tilt ≠ 1.0`
and intended ≠ `$150` (size-apply already on host at `c946b019e`).

## Why live flow stayed 1.0

A1 rows were `state_sufficient=false` almost always because
`intent_gold_state` assembled geometry+cost only — **no books** — so
`tfs_ok` was false and compose locked `shadow_flow = 1.0` even when
TypeSafe fired. Cost tilt never needed sufficient. The Challenge shadow
pack was already sufficient when books were passed.

Do **not** relax the sufficient predicate. Load Challenge tape.

## What Chair copies

1. Copy `src/judgment/` (especially `a1_log.py`, `apply_size.py`,
   `sleeve_from_tape.py`, `host_events.py`, `fluid_local.py`,
   `challenge_shadow.py`, `gold_state.py`, `news_spine.py`).
2. Keep the existing splice:

```python
from src.judgment.apply_size import haircut_challenge_unit
adjusted_unit = haircut_challenge_unit(
    adjusted_unit,
    intent=intent,
    tick=tick,
    login=<mt5 login>,
    ns=self._namespace,   # must be operator
    already_admitted=True,
    evaluate_jev=True,
)
```

`haircut_challenge_unit` now loads Challenge books itself when M15 is
landed. Optional later: pass live `books=` / `as_of_utc=` from the engine.

3. Env unchanged: `GTOS_JEV_ALIVE_SHADOW=1` `GTOS_JEV_APPLY_LIVE=1`
   `GTOS_JEV_A1_LOG=1`. TypeSafe fp `00000000`.
4. Restart **only** `GTOS_F5_FTMO`. No remint. No flatten. No place from Jev.

## How flow leaves 1.0 on the next place

On the next `cost_skip is None` Challenge place that is not leave-orig:

1. Landed XAU M15+H4 must be fresh (`tf_snap` stale if M15 lag > 12h or
   H4 lag > 36h). Refresh the Challenge CSVs or pass live `books=`.
2. Geometry must name entry+stop (or `stop_dist`). Family must not be
   `unknown` (empty sleeve).
3. Then `state_sufficient_for_live=true`. If H4 trend is `±1` and side is
   long/short, `shadow_flow` is `1.15` (with) or `0.70` (against).
4. APPLY_LIVE writes `live_size_tilt`. Combined = flow × cost scales
   `risk_pct_per_trade` / `unit_risk_pct`. Prove C: before≠after risk
   and intended ≠ `$150`.

## Remaining 3 SHADOW — do not invent

| Gate | Why still SHADOW | Chair |
|---|---|---|
| `FLUID-HLD-005` trail | 10 tickets have `f5_stop_move`; none differ from `orig_sl` by 0.05. Monitor jitter ~0.005 is not a trail. | Keep pulling events. Prove when ≥20 tickets have a named final vs orig, both True/False. |
| `FLUID-HLD-008` friday | Friday 06 asia exists; no Friday `utc>=16` (`friday_cutoff`). | Wait for a Friday 16:00Z+ Challenge as-of. Do not rename the question. |
| `FLUID-NWS-005` spine_empty | JSON `f5_high_calendar` covers every Challenge as-of. Host unread ≠ empty spine. | Need an as-of outside 10d of any spine event after calendar repair — not an invented empty. |

## Calendar repair (already noted)

`NEWS_CALENDAR_REPAIR.md`: do **not** overwrite June
`data/news_calendar.json`. Do **not** invent NEWS_PROTOCOL endpoints.
Host writer `inventory_status` on this tape is never `READ`
(`NOT_READ` / `UNKNOWN` / None). Wave M APPLIED `calendar_honest` as
the label of that fact. Repair = host writer actually READS the spine.

## Applied this wave (labels only)

- `FLUID-ADM-008` `a8_agrees` — named from Challenge M15 metals primitives (34/3/31).
- `FLUID-ADM-009` `calendar_honest` — writer-not-READ is false (10) vs JSON-only true (87).

Physical lots stay **flow × cost**. These two are writer labels, not stacked size.
Envelope walls stay integers. SEL-V4-002 stays research-only.
