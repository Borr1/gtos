# SHADOW UNLOCK BACKLOG — remaining fluid axes + selector multi-instrument

Chair ultragoal. Measured 2026-09-18 against the landed Challenge pack
(`judgment/astra/lab/challenge_shadow_20260917/shadow.jsonl`, n=97), host
events, deals, and `JEV_GATE_INVENTORY.json`. Receipt:
`SHADOW_UNLOCK_RECEIPT.json`. Regenerator: `scripts/jev_shadow_unlock_backlog.py`.

**No SHADOW field can be honestly filled from existing Challenge state.**
Chair landed Challenge FX tape on 2026-09-18. This pass rematerialized the
pack and re-measured `n_non_xau_sufficient`. It does not APPLY a SHADOW
gate. It does not invent HIGH, a trail, a Friday cutoff, a NEWS_PROTOCOL
endpoint, or an empty spine. Jev never places.

Process lock stays `shadow_score_then_wire`. Envelope walls stay integers.
Physical lots stay flow × cost. Ticket 293332188 stays leave-orig.

---

## 0. Inventory (48 fluid / 8 envelope)

| Status | n | IDs |
|---|---:|---|
| `APPLIED_NAMED` | 44 | see §0.1 |
| `PROVED_SHADOW` research-only | 1 | `SEL-V4-002` |
| `SHADOW` / `NOT_PROVED` | 3 | `FLUID-HLD-005` trail, `FLUID-HLD-008` friday, `FLUID-NWS-005` spine_empty |

Evidence: `JEV_GATE_INVENTORY.json` + `FLUID_PROVE_LEDGER.json` (`n_not_proved=3`)
+ `WAVE_M_RECEIPT.json` + this re-measure (same three, same bars).

Not in the 48, still organism tissue, **not** fluid APPLY targets:

| ID | Why it is not a fluid axis |
|---|---|
| `SEL-V4-001` | Selector enabled-integer. Off for live authority. |
| `SEL-V4-003` | Runtime effect after merge. V4 has no live authority. |
| `SCH-V4-001` | Scheduler window-winner (`permissions.py`). Live-off. |
| `SCH-V4-002` | Allocator path. Not on `ultimate_book` send. |

Envelope walls (stay integers, never Jev toys): `ENV-KILL`, `ENV-TWO-STOP`,
`ENV-TOKEN`, `ENV-H8`, `ENV-DD`, `ENV-US30`, `ENV-OCC`, `ENV-DEAD`.

### 0.1 Every APPLIED_NAMED fluid axis

| ID | question | family | effect | chair |
|---|---|---|---|---|
| `FLUID-ADM-001` | state_sufficient | admit | label | LABEL |
| `FLUID-ADM-002` | flow_stance | admit | label | LABEL |
| `FLUID-ADM-003` | flow_alignment | admit | size_tilt | ENFORCE |
| `FLUID-ADM-004` | session_fitness | admit | label | LABEL |
| `FLUID-ADM-005` | geometry_vs_tape | admit | label | LABEL |
| `FLUID-ADM-006` | level_respect | admit | label | LABEL |
| `FLUID-ADM-007` | admit | admit | label | VETO |
| `FLUID-ADM-008` | a8_agrees | admit | label | LABEL |
| `FLUID-ADM-009` | calendar_honest | admit | label | LABEL |
| `UB-AUTH-010` | admit | admit | label | LABEL |
| `FLUID-ADM-012` | family_study_vs_keep | admit | label | LABEL |
| `f5_xau_flow_alignment_size_tilt` | flow_alignment | size | size_tilt | ENFORCE |
| `F5-JEV-004` | cost_hurtful | size | size_tilt | VETO |
| `FLUID-SIZ-003` | session_size | size | size_tilt | ENFORCE |
| `FLUID-SIZ-004` | geo_size | size | size_tilt | ENFORCE |
| `FLUID-SIZ-005` | level_size | size | size_tilt | ENFORCE |
| `FLUID-SIZ-006` | event_size | size | size_tilt | VETO |
| `FLUID-SIZ-007` | ac60_size | size | size_tilt | ENFORCE |
| `FLUID-SIZ-008` | combined_size | size | size_tilt | ENFORCE |
| `FLUID-PLC-001` | veto_corr | place_fluid | label | VETO (draft_only) |
| `FLUID-PLC-002` | veto_event | place_fluid | label | VETO (draft_only) |
| `FLUID-PLC-003` | veto_cost | place_fluid | label | VETO (draft_only) |
| `FLUID-PLC-004` | veto_occupancy_label | place_fluid | label | VETO (draft_only) |
| `FLUID-PLC-005` | cost_vs_tape | place_fluid | label | LABEL |
| `FLUID-PLC-006` | stale_standing | place_fluid | label | LABEL |
| `FLUID-PLC-007` | last_refusal_class | place_fluid | label | LABEL |
| `UB-PLC-017` | cost_hurtful | place_fluid | size_tilt | VETO |
| `FLUID-HLD-001` | close_label | hold_exit | label | LABEL |
| `FLUID-HLD-002` | time_stop_vs_orig | hold_exit | label | LABEL |
| `FLUID-HLD-003` | hold_too_late | hold_exit | label | LABEL |
| `FLUID-HLD-004` | mfe_shape | hold_exit | label | LABEL |
| `FLUID-HLD-006` | leave_orig_293332188 | hold_exit | label | ENFORCE |
| `FLUID-HLD-007` | runner_r | hold_exit | label | LABEL |
| `FLUID-REN-001` | isolated_reentry | reentry | label | LABEL |
| `FLUID-REN-002` | sibling_cooldown | reentry | label | LABEL |
| `FLUID-REN-003` | spring_reprint | reentry | label | LABEL |
| `FLUID-REN-004` | minutes_since_flat | reentry | label | LABEL |
| `FLUID-REN-005` | two_stop_would_be_third | reentry | label | LABEL |
| `FLUID-REN-006` | cluster_same_day | reentry | label | LABEL |
| `FLUID-NWS-001` | event_proximity | news_window | label | VETO |
| `FLUID-NWS-002` | high_in_f5_window | news_window | label | LABEL |
| `FLUID-NWS-003` | minutes_to_nearest | news_window | label | LABEL |
| `FLUID-NWS-004` | warsh_class | news_window | label | LABEL |
| `FLUID-NWS-006` | past_flag | news_window | label | LABEL |

Wave M applied `FLUID-ADM-008` / `FLUID-ADM-009` as **labels**. Extra size axes
stay writer-named. They do not stack lots.

### 0.2 Research-only

`SEL-V4-002` — `PROVED_SHADOW` / research-only. After FX land: 73
decidable, 2 distinct (1.0×68 / 2.0×5), now including paying FX + US30.
`may_auto_apply=false`. Observer is
`src/judgment/a1_log.py:observe_sel_v4_002`. **Do not import from
`selector_v4.py` (R2 / H1).** Do not APPLY.

---

## 1. Each remaining SHADOW — missing field, source, invent forbidden

Frozen prove bars (label): `min_decidable=20`, `min_non_default=5`,
`min_distinct=2`, `invented_high_forbidden=true`.

### 1.1 `FLUID-HLD-005` `trail_vs_orig` — SHADOW

| | |
|---|---|
| Exact missing field | `extra.final_sl` vs `geometry.stop` differing by `TRAIL_EPS=0.05` |
| Instrument | `fluid_local.trail_vs_orig` ← `host_events.sl_differs` / `named_final_sl` |
| Where it would come from | Host `events.jsonl` `event=f5_stop_move.stop_now` (last per ticket), else `deals.final_sl` |
| Named Challenge state needed | ≥20 tickets with both orig and a named final, **and both True and False poles** |
| Invent forbidden | Yes. Monitor jitter ~0.005 is not a trail. Do not mint a `stop_now`. Do not remint. |

Measured on this tape:

- Pack: decidable 10 / moved 10 / distinct 1 / `{false: 10}`
- Events: 943 `f5_stop_move` on **10 tickets**. Vs deal `orig_sl`: 0 true / 10 false.
  Ticket `292885676` is 934 reprints — still false vs orig.
- Deals: `final_sl` missing on **46/47**. One row has both columns and they match
  within 0.05. Named trails from the deal column: **0**.

Honest fill this pass: **no**.

### 1.2 `FLUID-HLD-008` `friday_cutoff_label` — SHADOW

| | |
|---|---|
| Exact missing field | `clock.is_friday` **and** `sessions.named == friday_cutoff` |
| How `friday_cutoff` is named | `gold_state._session_named`: Friday **and** `utc_hour >= 16` |
| Where it would come from | A Challenge as-of at Friday 16:00Z+ (writer clock `ENV-DEAD` stays the integer) |
| Named Challenge state needed | Enough Friday-cutoff as-ofs to move distinct off 1 (need the True pole) |
| Invent forbidden | Yes. Do not rename the question to `close_session`. Do not use Friday 01–06Z asia. Do not back-date an as-of. |

Measured on this tape:

- Pack: decidable 97 / distinct 1 / `{false: 97}`
- Friday rows exist: **7**, all `2026-09-11T01..06Z`, all `sessions.named=asia`
  (XAU×3, EURUSD, EURGBP, BTCUSD, GBPUSD). `utc_hour` max = 6.
- Pack sessions: london 13 / ny 63 / asia 20 / dead_21_00z 1. **Zero**
  `friday_cutoff`.
- Pack ends `2026-09-17T10:41Z` (Thursday). Calendar Friday 2026-09-18 16:00Z+
  has **not** been pulled. A later sit/deals pull after that clock is the
  state. Waiting is not inventing.

Honest fill this pass: **no**.

### 1.3 `FLUID-NWS-005` `spine_empty_honesty` — SHADOW

| | |
|---|---|
| Exact missing field | `news.spine_empty == True` on a Challenge as-of |
| How empty is named | `news_spine.attach_news`: no spine event within 10d of as-of |
| Where it would come from | An as-of outside the host `f5_high_calendar` + JSON spines' 10-day cover |
| Named Challenge state needed | The True pole (honest empty). Distinct is 1 because every as-of is covered |
| Invent forbidden | Yes. Host unread ≠ empty spine. Do not invent NEWS_PROTOCOL. Do not overwrite June `data/news_calendar.json`. Do not invert `calendar_honest` |

Measured on this tape:

- Pack: decidable 97 / distinct 1 / `{false: 97}`. `n_news_empty=0`.
- Every row: `(spine_empty=False, challenge_axis_covering=True, events nonempty)`.
- Deal as-ofs vs live spines: **0** empty.
- Host news writer on events tape: `NOT_READ` 3244 / `None` 3337 / `UNKNOWN` 93 /
  `READ` **0**. That fact is already `FLUID-ADM-009` `calendar_honest`
  (APPLIED_NAMED). It is not `spine_empty`.

Honest fill this pass: **no**.

---

## 2. Selector / sleeve multi-instrument unlock backlog

Chair 2026-09-18 landed Challenge M15+H4 under
`challenge_shadow_20260917/multi/` (`_peer_multi_20260918` +
`_gbpjpy_eurgbp_20260918`). The organism now **thinks** on paying FX and
US30 tape. It still does not think on BTC/ETH/UK100 (no tape) or GBPJPY
(tape landed, zero pack rows).

### 2.1 What the rematerialized pack names (2026-09-18)

| symbol | n on pack | family mix | M15+H4 | sufficient | note |
|---|---:|---|---|---:|---|
| `XAUUSD` | 34 | study / other / keep | landed | **28** | parent Wave E tape; 6 slate/skip lack stop+cost |
| `USDJPY` | 16 | study / other | landed | **11** | 5 lack geometry.stop_dist |
| `US30.cash` | 14 | house_hard_off `mx_us30` | landed | **14** | tape landed; `ENV-US30` stays; log_only |
| `EURUSD` | 13 | study / other / xa_huge | landed | **11** | 2 lack geometry |
| `GBPUSD` | 12 | study / other / xa_huge | landed | **7** | 5 lack geometry |
| `BTCUSD` | 3 | house_hard_off `orb_crypto` | **absent** | 0 | not in Chair zip |
| `EURGBP` | 2 | house_keep `vss` | landed | **2** | |
| `UK100.cash` | 2 | house_hard_off `idxrev` | **absent** | 0 | not in Chair zip |
| `ETHUSD` | 1 | house_hard_off `orb_crypto` | **absent** | 0 | not in Chair zip |
| `GBPJPY` | 0 | — | landed | 0 | Chair zip; unused on this pack |

`n_non_xau_sufficient=45`. `n_xau_sufficient=28`. `n_missing_m15_h4=6`
(BTC+UK100+ETH). Landed bars: `XAUUSD`, `EURGBP`, `EURUSD`, `GBPJPY`,
`GBPUSD`, `US30`, `USDJPY`. April `data/GBPUSD_M15.csv` is **not**
Challenge-true. `books_for_symbol` never wears XAU onto another pair.

Six XAU insufficients are slate/skip rows missing `geometry.stop_dist` +
`cost` — a packet hole, not a tape hole. Do not invent stops.

Rematerialize clock: `_parse_mt5_time` must not smash ISO microseconds on
`last_seen_utc`. Doing so used `now()` and stale-killed parent XAU M15
(lag 17h > 12h). Slate rows with no clock fall back to `built_at_utc`.

### 2.2 What blocks equal intelligence

1. **Challenge M15+H4 is now landed** for EURUSD / GBPUSD / EURGBP /
   USDJPY / GBPJPY / US30. Remaining tape hole is BTC / ETH / UK100 (do
   not land those next). D1 stays optional for non-XAU. GBPJPY has tape
   and no pack rows.
2. **`SEL-V4-002` is research-only and R2-bound.** It already scores
   `geometry_vs_tape` (stop/ATR) when M15 exists. It cannot APPLY. Editing
   `selector_v4.py` reseals (~16.5 h/window) and is forbidden here.
   Scheduler V4 (`SCH-V4-001/002`) computes with `live_activation_allowed=false`
   and is not in the 48.
3. **A8 / metals confluence is XAU-surface.** `metals.ON_SURFACE` is
   XAU/XAG/*. `sleeve_from_tape.features_from_books` will run metals
   `_a8_features` + `metals_confluence` on **any** landed M15. After FX tape
   lands, keep `FLUID-ADM-008` a **label**, and only on metals sleeves
   (`jev_questions` already says ignore when not metals). Do not let A8
   become a FX veto — that chokes gates.
4. **The live size wire is XAU-named** (`f5_xau_flow_alignment_size_tilt`).
   After rematerialize, shadow flow tilt also moves on paying FX (pack
   `n_shadow_flow_tilt_moved=53`). Chair has not NAMED that wire on FX.
   Do not invent `f5_fx_*`. Do not treat compose score as an APPLY
   ceremony. US30 stays log_only.
5. **House integers already decide several symbols.** `ENV-US30`,
   `orb_crypto` (BTC/ETH), `idxrev` (UK100). Landing tape there labels host
   drift. It does not create a live edge. Do not pull BTCUSD as a trade
   surface (`CHAIR_PULL_LIST.md` item 7).
6. **Family / sleeve generators are not symbol-portable.** DSP study sleeves
   fire across FX in the slate; A8/metals primitives and XAU D1 levels do
   not follow them. Equal intelligence means **same gold_state shape + same
   fluid questions on that symbol's own M15+H4**, not copying XAU features.

---

## 3. Prove order (max multi-instrument edge, no choke)

Do **not** lower prove bars. Do **not** APPLY a new size stack. Do **not**
place.

| Step | Action | Why it unlocks edge | What it must not do |
|---|---|---|---|
| **1** | **DONE 2026-09-18.** Chair landed Challenge M15+H4 for EURUSD / GBPUSD / EURGBP / USDJPY (+ GBPJPY unused, + US30 hard-off). Pack rematerialized. `n_non_xau_sufficient=45`. | Paying FX + vss keep now have gold_state. | Did not wear April historical. Did not substitute XAU. Did not pull BTCUSD. |
| **2** | Re-run `scripts/jev_fluid_prove.py --no-apply` on this rematerialized pack (next pass). | Re-score SEL-V4-002 / flow poles on FX before any APPLY. | Do not APPLY from that re-score in the same pass. Do not promote SHADOW. |
| **3** | If FX sufficient ≥20 and flow/geo move with 2 poles: Chair may NAME the existing flow wire on those symbols (same `[0.70, 1.15]`, already-admitted only). | Same instrument, more symbols. No new choke. | Do not invent `f5_fx_*`. Do not stack SIZ-003..007. Do not refuse. |
| **4** | Keep A8 / `FLUID-ADM-008` label-only and metals-sleeve-only after FX tape lands. | Prevents metals confluence from haircutting vss/DSP FX. | Do not APPLY A8 as size or veto on FX. |
| **5** | Skip US30 / UK100 / BTC / ETH as the next tape drop. | 19 rows are already `house_hard_off` + envelope. | Do not lift `ENV-US30` / hard-off from a shadow score. |
| **6** | Friday: pull sit/deals/slate **after** the next Friday 16:00Z+ Challenge as-of (first unpulled window: 2026-09-18T16:00Z if the book fires). Then re-prove `FLUID-HLD-008`. | Supplies the True pole without renaming the question. | Do not use asia Friday. Do not use `close_session`. |
| **7** | Trail: keep pulling `events.jsonl`. Prove `FLUID-HLD-005` only when ≥20 tickets have named final vs orig **and** both poles exist. | Label only. No remint. | Do not treat `292885676` reprints as a trail. Do not invent `final_sl`. |
| **8** | spine_empty: wait for an as-of outside 10d of any spine event **after** the host writer actually `READ`s. Repair = host READ, not a new endpoint. | Honest empty is the missing pole. | No NEWS_PROTOCOL invent. No June overwrite. No unread→empty invert. |
| **9** | `SEL-V4-002`: after FX tape, re-prove the **observer** on multi-symbol geometry. Stay research-only. | Selector treats FX with the same question it already proved on XAU. | Do not edit `selector_v4.py`. Do not APPLY. `SCH-V4-*` stay observe-only. |

Step 1 landed. Next is `--no-apply` prove on the rematerialized pack, then
Chair NAME of the existing flow wire on paying FX if poles exist. Steps 6–8
stay SHADOW until Challenge names the field. Do not treat US30's 14
sufficient rows as a live edge.

---

## 4. What this pass did not do

- Did not set any SHADOW gate to `PROVED_SHADOW` or `APPLIED_NAMED`.
- Did not invent `final_sl`, Friday 16Z+, or an empty spine.
- Did not add a NEWS_PROTOCOL endpoint.
- Did not edit `selector_v4.py`.
- Did not place, remint, flatten, or remint a token.
- Did not copy April historical FX bars into the Challenge landing dir.
- Did not run `jev_fluid_prove.py` APPLY. Compose may score the
  already-named flow wire on newly-sufficient FX; that is not a SHADOW
  promote and not a place.

Chair land after a later FX-bar pull remains: copy `src/judgment/` onto
Challenge f5-live only. Do not wholesale-copy GitHub `book_owner.py`.
Env stays `GTOS_JEV_ALIVE_SHADOW=1` `GTOS_JEV_APPLY_LIVE=1` `GTOS_JEV_A1_LOG=1`.
Restart only `GTOS_F5_FTMO`.
