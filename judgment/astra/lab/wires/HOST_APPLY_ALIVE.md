# Host APPLY + Alive fluid observers (dirty f5-live)

Owner NAME 2026-09-17 ~18:18 ICT (Borhen): **"apply everything yes"**.
Clarification: Alive organism, not two hooks.

Ticket **293332188 leave orig** (closed broker_closed 2026-09-17T11:10:58Z on host). Lock stays. No remint.
Writer still prints. Chair ENFORCE / VETO / LABEL. Jev never owns MT5 send.

## Do not

- Wholesale-copy GitHub `book_owner.py` onto the dirty 10069-line host.
- Edit `selector_v4.py` (R2 / H1). SEL-V4-002 stays in `src/judgment/a1_log.py`.
- Place, remint, flatten, or write inbox from this landing.
- Convert `$KILL`, 2-stop **COUNT**, token digest, H8 flatten, hard DD floors, `us30_off` into Jev toys.
- Resize W7 armed books (`crypto` / `energy_agri` / `sub_xvol_pullback`).
- Print the TypeSafe key.

## Env on f5-live only

```
GTOS_JEV_ALIVE_SHADOW=1
GTOS_JEV_APPLY_LIVE=1
```

Keep `GTOS_JEV_A1_LOG=1` if already set. Confirm key fingerprint `00000000` before the first POST.
Do **not** set `GTOS_JEV_APPLY_LIVE` on W7 / `run_book.py` workers.

## Land order (mapped host sites)

1. `bridge.py` `admit_and_size(` (pulled **:572**) — AFTER final `_decide`. Env-gate **BEFORE** import (`GTOS_JEV_A1_LOG` or `GTOS_JEV_ALIVE_SHADOW`). Call `maybe_observe_ub_auth_010` (stamps UB-AUTH-010 + full 48-fluid inventory). Never change the decision.

2. `book_owner.py` `_spread_cost_screen` (pulled **:9531**) then its **CALL**. AFTER `cost_skip = …`. **Do not mutate `cost_skip`.** Observe `UB-PLC-017` + `F5-JEV-004` + `f5_xau_flow_alignment_size_tilt` + `maybe_observe_fluid_at_place`.

3. **APPLY the two** only when `cost_skip is None` (already admitted):
   - `live_flow` from `f5_xau_flow_alignment_size_tilt` clamp `[0.70, 1.15]`
   - `live_cost` from `F5-JEV-004` clamp `[0.70, 1.00]` — never a new refuse
   - combined = host product `flow × cost`
   - skip ticket **293332188** and any already-open row (`leave_orig`)
   - physical apply only if `GTOS_JEV_APPLY_LIVE=1` **and** login `0` / ns `operator`
   - **scale `risk_pct_per_trade` + `unit_risk_pct`** (F5 `_UnitView` — lots keys alone are a silent no-op)
   - splice `haircut_challenge_unit(...)` (or copy `src/judgment/apply_size.py`); do not leave `compose_shadow(state, None)` without answers if TypeSafe is up
   - one-line `JEV_APPLY receipt` + `judgment/astra/lab/a1/apply_receipt.jsonl`

4. Copy `src/judgment/` (unbound) onto the host tree. Do not copy GitHub `book_owner.py` wholesale — splice the three hooks at the mapped sites.

5. Envelope walls stay the host integers.

## Files flipped on GitHub this wave

- `src/judgment/process_lock.py` — `wire_apply` open for the two named wires; leave-orig 293332188 live 1.0
- `src/judgment/compose.py` — live tilts move except leave-orig / house_hard_off
- `src/judgment/apply_size.py` — Challenge-gated physical haircut
- `src/judgment/fluid_gates.py` + `fluid_pipeline.py` + `judgment/astra/JEV_GATE_INVENTORY.json` (48 fluid)
- `src/judgment/jev_questions.py` — full fluid fan-out
- `src/judgment/a1_log.py` — `observe_fluid_inventory`
- `src/components/ultimate_book/bridge.py` + `book_owner.py` — env-gated observers; dead APPLY hook (no-op without Challenge ns + compose)

## Subsequent fluid

Shadow score → prove on Challenge-true tape → APPLY when `PROVED_SHADOW` if effect is `size_tilt` or `label`. Pre-authorized. New chat ritual only if the gate would place / remint / flatten or break an envelope wall.

Wave J on this Challenge pack (n=87): **22 PROVED_SHADOW + 2 inherited aliases APPLIED** (24 this wave). **21 remain SHADOW** — missing named state, constant-on-this-tape, or 4/5 moved. SEL-V4-002 stays research-only. Physical lots stay flow × cost. Receipt: `WAVE_FLUID_PROVE.json`.

Host already observing the 48. Copy updated `src/judgment/` + `JEV_GATE_INVENTORY.json` + `FLUID_PROVE_LEDGER.json`. Do not stack extra size axes into the Challenge haircut unless you choose to. Do not remint 293332188.
