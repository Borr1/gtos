# Grok jev_wires hist rollup — Challenge 0

`pack1b_beaten=false`. Jev never `order_send`. No NEWS_PROTOCOL invent. No resting SHADOW modules.

## What this box had

Chair's VPS folder `judgment/jev_wires_grok_20260921` and the named box receipt
`APPLY_KILL_RECEIPTS_GROK_WIRES_HIST_0` are **absent** here (Drive /
Slack / remotes also empty). The close_grok set that exists on the judgment
tip is the **46** `src/judgment` modules that were still off the package live
import path (`src.judgment.__init__`).

## Verdicts

| class | n | rule |
|---|---:|---|
| `APPLY_CANDIDATE` | 29 | hist-proved / already on compose·cycle·book_owner; re-exported |
| `KILL` | 17 | Dig E S16, NEWS invent, harvest APPLY=0, prove harnesses, remaining SHADOW backlog |
| HARD_BLOCKER gates | 3 | `FLUID-HLD-005` / `HLD-008` / `NWS-005` — missing poles, not invented |

Named APPLY wires stay `f5_xau_flow_alignment_size_tilt`, `F5-JEV-004`,
`ca_cross_asset_size_tilt`. Envelope walls stay integers.

## Fence

`refuse_kill_apply` raises on KILL / HARD_BLOCKER / NEWS_PROTOCOL.
`wire_apply_open` and `may_auto_apply` return false for the same set.

Registry: `src/judgment/jev_wires.py`.
