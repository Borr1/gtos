# Final Moonshot Central Orchestrator Successor Brief

Date: 2026-06-04
Status: active successor brief

## Role

The central orchestrator is the continuity layer across repo state, live evidence, research routes, goal sessions, subagents, prompts, commits, production branches, broker data, replay systems, and final-system design.

The orchestrator must keep the final moonshot program coherent, evidence-bound, aggressive, and pointed at the strongest possible GTOS trading operating system.

The orchestrator must also keep the program build-and-ship oriented. The final objective is not more theoretical research; it is the final live GTOS system package. Goal sessions have full control over repo changes and change production code and live behavior code to build it.

## Controlling State

- Local clean post-halt branch: `final-moonshot-context-repair-2026-06-04`.
- Current base: `eadf8d1b2 context: refresh post hard halt integration state`.
- Hard-halt evidence controls the program.
- The old pre-halt V3 research program is no longer sufficient as launch sequence.
- V3 packages are valuable inputs, not proven live authority.

## Required First Action In Every Session

Run:

`python scripts\generate_live_state.py`

Then read:

1. `.context/LIVE_STATE.md`
2. `.context/00_core/current_repo_reading_order.md`
3. `.context/00_core/current_vnext_system_map.md`
4. `.context/00_core/quick_reference_card.md`
5. `.context/00_core/research_current_state.md`
6. `.context/00_core/research_operating_doctrine.md`
7. `.context/00_core/goal_session_research_discipline.md`
8. this file
9. `.context/00_core/final_moonshot_post_hard_halt_research_plan.md`
10. `.context/00_core/final_moonshot_goal_session_execution_architecture.md`

For route review, read the route artifacts directly. Do not rely on chat memory or closeout prose.

## Evidence Standard

Preserve evidence class for every claim:

- broker-real PnL;
- broker-real cash;
- exact-R;
- proxy-R;
- replay result;
- simulation result;
- shadow result;
- live authority;
- default-off research;
- production code;
- uncommitted worktree dirt;
- committed package;
- source gap;
- non-generatable historical truth;
- prospective capture requirement.

## Current Open Issues

- Trade-record payloads under `knowledge_base/redacted_account_live_bee34003/trade_records/` are LFS pointers in this smudge-disabled worktree until selectively hydrated.
- `data/m1/` and `data/ticks/` exist, but tick capture includes corruption/quarantine evidence that must be represented in replay.
- Some large historical files that should be LFS pointers were checked out as full content; this is a storage/LFS hygiene issue to track before packaging.
- The latest numbered handoff remains Session 63; use current context and route artifacts instead.
- AGENTS/CLAUDE root briefing may still contain pre-halt wording unless patched by the current context-repair branch.
- Any prompt language that says final moonshot routes cannot change live behavior is stale and must be removed.

## Next Decisions

The orchestrator should next decide whether to:

1. Launch one large hard-halt forensic probability rebuild route from the existing hard-halt prompt after patching it to include dual-broker and V3 source requirements; or
2. Split Wave 1 into three parallel routes: 1A broker/candidate matrix, 1B V3/live alignment, 1C dual-broker architecture.

Recommendation: launch Wave 1A, 1B, and 1C in parallel only after Wave 0 authority repair is committed and each route has disjoint write targets.

## Completion Standard For Orchestration

Do not call the program current until:

- context files point to hard-halt truth;
- source/branch/LFS/data authority is documented;
- Wave 1 prompts or route requirements are ready;
- stale pre-halt claims are labeled superseded;
- every missing source/access/capture blocker is exact;
- implementation opportunities are shipped rather than parked as weak future work;
- owner-facing next action is concrete.
