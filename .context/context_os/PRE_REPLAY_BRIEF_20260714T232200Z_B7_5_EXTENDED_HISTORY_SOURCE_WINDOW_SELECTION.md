# B7.5 Extended-History Source-Window Selection Brief

Generated UTC: 2026-07-14T23:22:00Z. Current status refreshed
2026-07-15T06:26:36Z.

## Current Authority

January B7.5 completed all 31 chunks and passed truth-integrity acceptance. No
replay is currently active. The next authorized action is to commit the compact
January checkpoint and launch the exact bound April command once under the
unchanged shared execution digest. The older source-selection and pre-January
sections below are historical checkpoints; the final January completion addendum
and this section control the current action.

## Decision

Launch April only after the compact January checkpoint is committed. January
and April are already bound as non-adjacent months under one semantic execution
contract with exact same-window denominators, tick/M1/static-source
dispositions, and capacity-safe one-day chunking. Do not tune policy between
the two runs.

## Current Replay And Process State

- No replay, builder, verifier, pytest, repair, or Git-maintenance process is
  active.
- Latest completed prefix:
  BROAD_LIVE_AS_IF_REPLAY_V258_B7_4_BROAD_JUNE_CACHE_SAFE_SOURCE_IDENTITY_20260601_20260619_REPAIRED_ONLY_RELATIONAL_COMPACT_FULLGRID.
- Scope: 2026-06-01 through 2026-06-19, all configured 24 symbols, repaired
  profile, full 82-sleeve / 1,101-axis authority, one-day capacity-safe chunks.
- Broker mutation, live broker authority, final selection, and live activation
  remain false.

## Latest Completed Numbers

- Candidate -> scorecard -> order-event -> terminal-order -> physical-fill:
  104,434 -> 1,440 -> 108 -> 43 -> 42.
- Physical behavior: 42 trades, W/L/F 23/19/0, gross/final/net
  +17.521417677 / +17.521417677 / +15.08858577R, cash +$188.99882038.
- Ordered-tick headline: 4 trades, W/L/F 3/1/0, gross/final/net
  +1.92353135 / +1.92353135 / +1.59506223R, cash -$67.96685163.
- Full-risk: 2 fills, -0.56242814R. Reduced-risk: 40 fills,
  +15.65101391R.
- Missed: 104,391 rows; 19,754 diagnostic-scoreable at
  -14,229.43645764R; 84,637 unscoreable; executable missed R is zero.
- Executed broker-cost REFUSED/source-gap: 0/0. Expired unfilled: 1.
- Exact June-window parity: 1,101 axes -> 961 candidate axes -> 484
  scorecard/order axes -> 25 filled axes. The exact window source-bound
  denominator is 80,388.20018256R; the global reservoir is not this replay's
  denominator.

## Baseline Anchors

- V89D/V90/V92 remain hostile May 13-17 comparators at 56/51/51 trades and
  +34.84520454/+28.84201157/+29.35570236R; they are not June or B7.5
  denominators.
- V249 is the route-certified hostile current-truth proof: 20 physical fills at
  +1.16150430R, 18 headline fills at +0.28044419R.
- V250/V257 are the exact June 1-5 current-truth nested anchor: 13 physical
  fills at +7.03578717R, 4 headline fills at +1.59506223R.
- Same-window V110B contains 95 fills at +22.80442652R under weaker
  historical fill truth. V258 has 14 exact common identities; every one of the
  43 removed V110B winners has a named current predecision blocker.
- V258 is the newest completed and accepted run. It is a 19-day proof, not an
  extended-history or live-readiness claim.

## Current Dirty State And Coordination

- Active changes are scoped to the V258 runtime/proof-consumer code, focused
  tests, V258 route artifacts, canonical manifest/verifier outputs, and Context
  OS controls.
- Unrelated AGENTS.md, .context/LIVE_STATE.md, tick data, knowledge-base
  records, shadow logs, and historical truth-smoke ledgers remain excluded from
  the checkpoint.
- No subagent is active or unreconciled. All prior findings retain their
  incorporated/rejected/deferred dispositions in the Fable matrix and root map.

## Root-Cause Chain

| Stage | Current state |
| --- | --- |
| source-bound -> source authority | B7.4 GREEN. V258 has one invariant 24-symbol source plan across 19 chunks. B7.5 month coverage is not selected yet. |
| source authority -> candidate | B7.4 GREEN. 104,434 exact relational candidates and 961/1,101 generated axes. B7.5 generalization is open. |
| candidate -> selector | TRUTH GREEN / EXTENDED VALUE OPEN. Exact identities and causal blockers are present. |
| selector -> scheduler | TRUTH GREEN / EXTENDED VALUE OPEN. 1,440 scorecards and 484 scorecard/order axes pass B7.4. |
| scheduler -> risk | TRUTH GREEN / CALIBRATION OPEN. Signed full/reduced provenance is complete; extended stability is unproven. |
| risk -> order | GREEN. All 43 terminal orders are PASSED/source-bound; no REFUSED/source-gap execution. |
| order -> lifecycle -> fill | TRUTH GREEN / HEADLINE COVERAGE OPEN. 42 physical fills, one expiry, only four ordered-tick headline fills. |
| fill -> exit | PROXY POSITIVE / HEADLINE THIN. Extended non-adjacent windows must test economic stability. |
| ledger/proof | GREEN. Flow, parity, missed-R, same-window comparison, manifest, verifier, audits, and parent checks reconcile. |

## Closed, Partial, And Open Issues

- CLOSED: missed-opportunity R authority separation.
- CLOSED: sparse parity memory growth and compact relational candidate proof.
- CLOSED: V110B exact trade identity authority and named removed-winner blockers.
- CLOSED: verifier merge of relational candidate counts with available physical
  ledger counts.
- PARTIAL: only four V258 fills carry ordered-tick headline authority.
- OPEN: select two or more non-adjacent months with adequate source authority.
- OPEN: prove no code/config delta between B7.5 windows.
- OPEN: materialize each B7.5 window's exact source-bound denominator, transfer,
  behavior, stress/MC, and missed-opportunity package.

## Highest-Leverage Same-Root Batch

Batch:
B7_5_EXTENDED_HISTORY_SOURCE_WINDOW_AUTHORITY_AND_NO_CONFIG_DELTA_PROOF.

Components to inspect or change only where current evidence requires:

- run_broad_live_as_if_replay_harness.py: source-plan preflight, bounded chunk
  selection, immutable config/runtime-input hashes.
- source resolver/export manifests under the current route and approved local
  evidence roots.
- build_source_bound_execution_parity.py: per-window denominator and transfer.
- compare_broad_live_as_if_replay_runs.py: same-window comparison only.
- route builder/verifier and focused tests for B7.5 window/config invariants.
- Context OS root map, matrix, retention manifest, and the exact replay command
  after windows are selected.

Patch classification before replay:

- source-window inventory and manifests: diagnostic/proof, behavior-neutral;
- missing approved source resolution: correctness, behavior-neutral until run;
- no-config-delta and capacity contracts: correctness, behavior-neutral;
- no selector/scheduler/risk/order/lifecycle/exit tuning is authorized merely
  by choosing B7.5 windows.

## Expected Measurable Effect Before Replay

- Candidate -> scorecard transfer: unchanged until replay; every selected month
  must expose a complete count and exact axis denominator.
- Scorecard -> order and order -> fill: unchanged until replay; no gate is
  opened during source selection.
- Missed positive/negative R: unchanged until replay; future windows must report
  both executable and diagnostic authority separately.
- Trade count, net/gross/final R, cash PnL, W/L/F: unchanged until replay.
- Cost-refused/source-gap execution: must remain zero.
- Full-risk/reduced-risk distribution: unchanged until replay and then reported
  separately with tier causes.

## B7.5 Success, Failure, And Deeper-Flaw Criteria

Source-selection success requires at least two non-adjacent reservoir-covered
months with:

- complete declared source plans for all configured symbols or exact
  symbol-day no-session/source-gap dispositions;
- immutable code/config/runtime-input hashes shared across runs;
- enough free storage for capacity-safe chunks and deterministic retention;
- exact per-window package-axis and source-bound denominator authority;
- no window-specific policy or outcome-bucket configuration.

Replay success requires each accepted window to materialize the full metric set,
zero executed REFUSED/source-gap rows, no opportunity collapse, and no
window-specific config delta. Failure is any source-plan ambiguity, hidden
candidate suppression, cross-window config drift, incomplete missed accounting,
or use of an interrupted run as evidence. A truth-correct but economically weak
result exposes the next causal system defect; it does not authorize suppressing
trades or weakening broker-cost, source, fillability, lifecycle, or signed
authority.

This brief authorizes source inventory and exact window binding. It does not
authorize broker/live/final activation.

## Source-Window Contract Completion - 2026-07-15T00:58:51Z

Source selection and exact window binding are now complete. No B7.5 replay has
started yet.

Contract:
`research/operations/final_moonshot_ultimate_system_denominator_to_deployment_execution_2026_06_20/B7_5_EXTENDED_HISTORY_SOURCE_WINDOW_CONTRACT.json`.

- Contract digest:
  `a3df5e1fe37b3b02b49217c434253193d8cb29f3679f5489b1e9cffb23379788`.
- Pair binding:
  `7bcf2a881cc8b9b5634dbab3c84180d44aa76ffdf19f287da4250c0dd6ec89f6`.
- Shared execution contract:
  `964e2671394fecd716c53ba1570201ea5aee66db0aad27700d4cb9d7a37f125c`.
- Unexpected config deltas: `0`.
- Full package surface: `82` sleeves and `1,101` member axes.

January 2026:

- selector rows `17,373`;
- present/absent package axes `711/390`;
- same-window source-bound R `1,239,092.20751002`;
- source-plan digest
  `b41714da59188be2ca1c4d72797521589a594ceb000a5e7f58eb3ac2ea5147d2`;
- static rows `96`, M1 symbol-days `744`, unresolved symbol-days `0`;
- M1 statuses: `511` absolute-floor, `13` session-scaled-floor, `220`
  verified no-session;
- tick scope: one covered symbol and 23 none, with 31 covered symbol-days.

April 2026:

- selector rows `18,851`;
- present/absent package axes `707/394`;
- same-window source-bound R `1,242,815.65886908`;
- source-plan digest
  `4987d98cf023963305eb4d34f5940e59491fbac56a2683fb55816e8b5d8e46d2`;
- static rows `96`, M1 symbol-days `720`, unresolved symbol-days `0`;
- M1 statuses: `525` absolute-floor, `11` session-scaled-floor, `184`
  verified no-session;
- tick scope: ten partial symbols and 14 none, with 66 covered symbol-days.

Correctness repairs completed before replay:

- source authority is exact per symbol-day and uses an M15-session-scaled M1
  floor instead of a global 1,000-row veto;
- a missing or short M1 day cannot demote a valid sibling day;
- full-window static, M1, and tick plans are canonical before one-day chunk
  hydration and every chunk must match their exact subset;
- terminal lifecycle close R requires an explicit same-day source authority;
- B7.5 output prefixes fail before replay unless both expected contract hashes
  match.

Verification is green: compile passed, `21` focused tests passed, the real
contract and mutation rejection passed, route verifier reports `ok=true` with
`issue_count=0`, prompt hardening passed, the full-JSONL route artifact audit
passed, and scoped `git diff --check` passed.

Capacity is green without deleting evidence: observed free bytes
`176171900928`, projected retained bytes for both windows `34461339634`,
projected free bytes after both `141710561294`, and mandatory reserve
`34359738368`. The active retention contract is
`.context/context_os/B7_5_EXTENDED_HISTORY_ACTIVE_ARTIFACT_REQUIREMENTS_AND_STORAGE_CHECKPOINT_20260715T005851Z.md`.

The next action is to commit this contract checkpoint, run the exact January
command embedded in the contract once, parse its final and partial artifacts,
and run April only after January source/config/truth integrity is confirmed.
This remains a B7.5 behavioral proof, not live authority.

## Semantic Execution-Identity Repair - 2026-07-15T01:40:58Z

The first bound January invocation did not enter `run_campaign` and is not
behavioral evidence. It failed at runtime preflight because the contract
expected shared digest `964e2671394fecd716c53ba1570201ea5aee66db0aad27700d4cb9d7a37f125c`
while current code produced
`6fc148262360882baf70f76ee80cec19cbdbcf8f08dab09e23beee2fe83fbfe2`.

Exact root cause: `build_config()` hashed the raw bytes of
`RECONSTRUCTED_PROXY_PACKAGE_SELECTION_SUMMARY.json`. That generated summary
contains `generated_utc` and a derived packet hash, so a route rebuild changed
the effective profile hash and each candidate's selected-policy authority hash
without changing economic calibration, package scope, code, config, symbols,
execution options, member axes, or registry inputs.

The source contract now separates two identities:

- execution authority uses canonical semantic SHA
  `abb397ebf5172d57d0e4a5e88dd529040f6224bcc44d630b65fd1e501efd4e7d`,
  recursively excluding only generated timestamps and derived packet/row
  hashes;
- audit provenance retains the raw artifact SHA in runtime and ledger fields,
  but that raw SHA is excluded from effective replay config hashing.

Real rebuild proof is exact. The raw artifact SHA changed from
`88cda41dc3a40b4b511ca91b3d92007262a81bc477602916e1633941fa013b1c`
to
`8609b299171d777f114eb0c4749278374adeb43fe8397c9c951346827b3c03ad`,
while the semantic SHA remained unchanged. The repaired profile config hash
remained
`d2f887fad661e92666e649e0fd2b364e7eb6b057dced45429591be2a6529127a`
and shared execution digest remained
`0eb44cbce93e2be6477e69e0b21c60d6631695ead34de1d4422ecbbbceab31ad`.

The rebuilt B7.5 contract is valid:

- contract digest
  `276a9ef623d40a6af12be9f6eb56022ce0d0b6ebb986d546cf66f973ba8980e4`;
- pair binding
  `3c6756651c52954660db54ff58df9220550042805848772eb5ab6ab715c4181f`;
- January and April source-plan digests remain unchanged;
- all January/April row counts, axis dispositions, and same-window source R
  remain unchanged.

Verification after the repair: compile passed; the complete focused
harness/runtime/B7.5/verifier barrier passed `1,342` tests; timestamp-only and
economic-content mutations behave distinctly; a self-hashed contract with bad
diagnostic/semantic separation is rejected; route verifier is `ok=true` with
zero issues; active prompt and tracked sparse starter hardening pass; and the
standard route artifact audit has no required or warning gaps.

This is a correctness repair with neutral expected economics. The next replay
must use the new command embedded in the rebuilt contract. Success means the
January run passes source/config/hash invariants and materializes its complete
behavior package. Failure before campaign entry is preflight evidence only;
weak or negative completed behavior is retained as truth and selects the next
causal repair rather than weakening cost, source, fillability, lifecycle, or
signed-authority controls. Broker/live/final remain false.

## January Completion And April Launch Gate - 2026-07-15T06:16:43Z

The exact bound January command completed all `31/31` one-day chunks with final
status `broad_live_as_if_replay_materialized_broker_live_closed`. Shared
execution digest `0eb44c...31ad`, January source-plan digest
`b41714...14d2`, the 24-symbol source plan, all per-chunk source subsets, the
82-sleeve registry, and all 1,101 member axes remained exact. Candidate
relational materialization is exact across `154390` candidates:
`154329` missed + `61` trade identities, with no duplicate or missing
candidate-instance keys.

January materialized `69888` decisions, `2016` scorecards, `147` order events,
`61` terminal orders/fills, and `154329` missed rows. All `147` order events are
broker-cost `PASSED`, source-bound, and broker-pretrade executable. Executed
REFUSED/source-gap counts are `0/0`; guarded-market fallback use is `0`.

Economic truth must remain split by authority:

- all physical replay fills: `61`, W/L/F `24/37/0`, net/gross/final
  `-2.76396743 / +0.94088713 / +0.94088713R`, execution cost
  `3.70485455R`, cash PnL `-$1595.02061428`, risk cash/risk percent sum
  `$7860.10366135 / 7.925%`;
- ordered-tick headline fills: `4`, W/L/F `3/1/0`, net/gross/final
  `+0.98832036 / +1.30562064 / +1.30562064R`, execution cost
  `0.31730028R`, cash PnL `+$98.24769712`, risk cash/risk percent sum
  `$395.31136071 / 0.4%`;
- M1-proxy diagnostic fills: `57`, W/L/F `21/36/0`, net
  `-3.75228779R`; they are replay scoreable but not headline authority;
- full-risk fills: `2`, W/L/F `0/2/0`, net `-1.44564903R`; reduced-risk
  fills: `59`, W/L/F `24/35/0`, net `-1.31831840R`;
- headline stress remains positive at `+0.05/+0.10/+0.20R` extra cost per
  trade: `+0.78832036/+0.58832036/+0.18832036R`; deterministic 200-iteration
  Monte Carlo reports total `+0.98832036R` and worst drawdown `-1.09900677R`.

Window-normalized transfer is explicit. Of 1,101 package axes, `990`
generated replay candidates (`89.918256%`), `497` reached scorecard/order
presence (`50.202020%` of generated axes), `29` reached scheduler/order
presence (`5.835010%` of scorecard/order axes), and `28` received filled-axis
attribution (`96.551724%` of order axes). There are `61` unique physical trade
identities but only `4` headline-authority trades (`6.557377%`).

Two R surfaces are intentionally separate. The source-window contract reports
`1239092.20751002R` of combined-signal diagnostic reservoir across the 711
historical selector-present axes. Executable/additive parity admits
`46627.324978458R` across 14 nonzero axes. The former is not an additive replay
denominator; the latter is only `3.763023%` of it. Unique all-physical replay R
against the additive surface is `-2.76396743R`; headline R is
`+0.98832036R`. Neither converts the global reservoir claim.

Missed accounting is complete but predominantly diagnostic: `26812` rows are
counterfactually scoreable with `+6616.22471941R` positive and
`-30828.76993654R` negative opportunity, net `-24212.54521713R`. No missed row
has ordered-tick executable authority in this window, so executable missed R
remains zero rather than promoting M1 outcomes.

January therefore passes the B7.5 truth-integrity and non-collapse gate but is
economically mixed. No policy tuning is authorized from January alone. The
next action is the exact April command already embedded in the contract, under
the same shared digest and no config/code change. April helps if it preserves
all truth contracts, zero executed REFUSED/source-gap rows, non-collapsed
transfer, complete missed accounting, and supplies independent economic
behavior for cross-window comparison. A weak April result exposes the next
causal repair; it does not justify hidden suppression or weakened authority.

## January Verifier-Truth Repair Pre-Replay Barrier - 2026-07-15T07:14:04Z

The full route verifier subsequently scanned the completed January relational
surface and exposed a coherent proof-chain batch before April. April is paused;
no replay, verifier, builder, pytest, or repair process is active.

Current completed authority remains the January prefix above: 154,390
candidates, 2,016 scorecards, 147 order events, 61 physical fills, 24/37/0,
-2.76396743R all-physical and four ordered-tick headline fills at
+0.98832036R. V89D/V90/V92 remain May hostile comparators only; V249 remains
the current hostile proof and V258 the June broad proof. None is a denominator
for this bounded January repair.

The verifier found three same-root truth defects:

1. `risk_authority_status` was absent on 146,975 of 147,036 package rows because
   `ledger_namespace_alias_fields()` overwrote the explicit candidate status
   with `None` when `risk_authority` was a scalar schema name.
2. Three valid signed candidates below the configured 0.70 expected-net-R floor
   were correctly rejected, but the exact `expected_net_r` unresolved failure
   disappeared between order preflight, risk finalizer, and missed serialization.
3. One XAUUSD raw `trade` admission was causally risk-capped and finally demoted
   by missing ordered-tick sequence truth. The verifier incorrectly required a
   signed reduced-action payload even though package new-entry authority was
   explicitly non-required for the raw-trade admission.

The patch changes four files:

- `src/research_infra/v4_timewarp_simulated_live_research_loop.py`;
- `verify_denominator_to_deployment_execution.py`;
- `tests/test_v4_timewarp_simulated_live_research_loop.py`;
- `tests/test_denominator_to_deployment_verifier.py`.

Risk status is now produced inside the pre-scheduler risk packet and consumed
from that packet by every ledger namespace. Fill-floor
`failures/raw/resolved/unresolved` fields now move atomically from the selected
scheduler option through order preflight and risk-finalizer missed attribution.
Verifier authority-stage selection now uses final fields for current executable
claims and explicit prefinal fields only for a preserved proposal. The 0.70
floor, signed reduced-action contract, fill-realism blocker, broker-cost
authority, and broker/live/final closures are unchanged.

The frozen Pasteur/Dalton/Gauss/Aquinas wave survived the app restart as running
but returned no deliverables after bounded collection. All four were closed;
no agent claim was integrated. Current-code reproducers and tests are the proof
authority.

Compile and scoped diff checks pass. Seven direct regressions pass, and the
complete runtime plus route-verifier files pass 1,159 tests with only the
pre-existing unknown `asyncio_mode` warning. The exact January XAUUSD row now
fails the final executable contract, passes the prefinal raw-trade contract,
passes exact member-axis identity, and has no preserved-proposal verifier error.

The patch is expected to be behavior-neutral. Before full January regeneration,
two all-symbol targeted proofs preserve same-day scheduler and portfolio
competition while covering all four exact defect dates:

- 2026-01-22..23 baseline: 15,304 relational candidates, 4,608 decisions,
  192 scorecards, 11 order events, six terminal orders, five trades, W/L/F
  4/1/0, net/gross/final +2.28088461/+2.71839423/+2.71839423R, cash
  +$224.31778307, risk cash $492.38584413, risk 0.5%, cost 0.43750962R.
- 2026-01-29..30 baseline: 10,751 relational candidates, 4,608 decisions,
  192 scorecards, seven order events, four terminal orders, three trades,
  W/L/F 0/3/0, net/gross/final -1.64391447/-1.45242575/-1.45242575R, cash
  -$161.96797256, risk cash $295.45763488, risk 0.3%, cost 0.19148872R.

Success requires exact same-day candidate/scorecard/order/trade/missed counts
and economics, zero executed REFUSED/source-gap rows, nonempty risk status on
every package row, exact `expected_net_r` unresolved provenance on the January
22, 29, and 30 misses, and no false signature/member-axis failure on the January
23 XAUUSD proposal. A count or economic change means the supposedly diagnostic
patch changed behavior and must be explained before proceeding. Missing
provenance means the producer-consumer chain remains incomplete. New concrete
verifier failures expose the next deeper truth defect; they do not authorize
gate weakening.

The all-symbol shared execution contract for both targeted proofs is
`74c8c503497c20ba502f83d2858c2cd80d5bf61db2c8968b6aaacb97d2dfcb43`.
The full January and April source-plan digests remain unchanged. After targeted
success, rebuild the route B7.5 contract with the corrected full-scope shared
digest, rerun January, rebuild flow/parity/manifest/verifier consumers, and only
then run April unchanged. This is a bounded truth repair, not total reservoir
conversion or live authority.

## Risk-Status Stage Alias R2 Barrier - 2026-07-15T08:04:53Z

The first January 22-23 targeted run completed under prefix
`BROAD_LIVE_AS_IF_REPLAY_2026_01_22_23_RISK_FILL_FLOOR_TRUTH_REPAIR_TARGETED`.
It materialized 15,304 exact relational candidates, 4,608 decisions, 192
scorecards, 18 order events, seven terminal orders/trades, and 15,297 missed
rows. The ordered-tick headline contains one winner at net/gross/final
`+0.60931716/+0.70651595/+0.70651595R`, cash `+$60.38580597`, risk cash
`$99.10406261`, risk `0.1%`, and cost `0.09719879R`. All seven physical fills
sum to `+3.15530942R` net and `+3.71839423R` gross/final after `0.56308481R`
cost. Executed REFUSED/source-gap rows remain `0/0`.

This targeted run is not a behavior-neutral comparator to the full-January
baseline. Its two added January 22 XAUUSD fills were both blocked in the
full-month run by `adaptive_replay_memory_guard_blocked:symbol_origin_side`
after state accumulated from January 1-21. Starting directly on January 22
starts that causal memory cold. Targeted slices therefore prove local truth
contracts; warmed economic parity is deferred to the complete January rerun.

The targeted truth scan found 14,128 package rows and zero missing
`risk_authority_status` values. The exact January 23 XAUUSD raw-trade proposal
has no prefinal trade-admission reasons, no prefinal member-axis reasons, no
preserved-proposal reasons, and remains correctly non-executable at the final
fill-realism stage. The scan also exposed seven terminal alias mismatches: the
top-level status remained `pre_scheduler_risk_authority_materialized` while the
nested final authority correctly reported
`risk_finalizer_approved_sizing_bound_to_runtime_risk_authority`.

The same-root repair changes canonical status precedence in
`src/research_infra/v4_timewarp_simulated_live_research_loop.py`: nested final
risk authority owns the top-level alias when present; the existing
`risk_authority_pre_scheduler` packet remains the prefinal stage surface and
fallback. No selector, scheduler, risk threshold, order, lifecycle, fill, exit,
cost, or broker policy changed. Focused plus complete runtime/verifier tests
pass `1,160` tests with only the pre-existing unknown `asyncio_mode` warning.

The refreshed replay-free B7.5 contract is valid. Its shared execution digest
is `b5f44606c20a7984a24158c1bd63efc5d6eeab2efebd1467447edb098d1acfce`;
the January source plan remains `b41714da...14d2` and April remains
`4987d98c...46d2`. No replay is active.

The R2 proof must use all 24 configured symbols, uncapped fullgrid candidate
authority, repaired profile, compact relational ledgers, and the new shared
digest. It helps only if candidate materialization remains exact, all package
rows have a status, terminal top-level and nested final statuses have zero
alias mismatch, the XAUUSD preserved proposal remains final-blocked without a
false signature/member-axis failure, expected-net-R floor failures retain raw,
resolved, and unresolved provenance, and executed REFUSED/source-gap rows stay
zero. A trade-count or R difference from warmed January is not by itself a
failure in this cold-start slice. A truth mismatch, authority promotion, or
cost/source-gap execution is a failure. After both exact-row R2 slices pass,
the complete January rerun is the first valid warmed behavioral comparison.

## Compact Fill-Floor Provenance R3 Barrier - 2026-07-15T08:50:24Z

The January 22-23 R2 proof completed with the same 15,304 candidates, 4,608
decisions, 192 scorecards, 18 order events, seven terminal rows/trades, and
15,297 missed rows as R1. Its ordered-tick headline remains one winner at
`+0.60931716R`; all physical rows remain `+3.15530942R`. Executed
REFUSED/source-gap rows are `0/0`. The candidate scan is complete, all 14,128
package rows have a risk status, terminal top-level and nested final statuses
have zero alias mismatches, and the exact XAUUSD proposal remains correctly
blocked only by final fill realism.

R2 exposed one remaining producer-consumer defect rather than a policy defect.
The exact January 22 EURUSD row `broadorigin_20287b4bddf5417d030448df`
was correctly rejected at expected net R `0.697431285`, and its runtime and
scorecard probe retained `expected_net_r` as an unresolved fill-floor cause.
The compact missed serializer omitted all fill-floor provenance fields, so the
terminal missed artifact could not prove the reason. The serializer now keeps
allowed, failures, raw, resolved, and unresolved lists for the scheduler,
pre-risk-finalizer, risk-finalizer, and finalizer-primary stages. The runtime
producer and compact consumer have direct tests; the full harness/verifier/
runtime barrier passes 1,342 tests with only the existing `asyncio_mode`
warning. No selector, scheduler, risk, order, lifecycle, fill, exit, cost, or
broker threshold changed.

The replay-free R3 contract is valid. Shared execution digest is
`d2edfc058ea58341e7da521431966fc713ec1bad31b3a6399fc65f532972e65f`;
contract digest is
`34e76c77be0ac706dc136fffe62e942943c176850c6843fed72828c295fe60f4`.
January and April source-plan digests remain `b41714da...14d2` and
`4987d98c...46d2`. R3 helps only if the exact compact EURUSD missed row carries
`allowed=false`, failures/raw/unresolved `['expected_net_r']`, and resolved
`[]`, while all R2 truth and safety checks remain green. R3 is a bounded truth
proof, not a warmed January behavioral comparison or total-reservoir claim.

## Terminal Blocker Stage R4 Barrier - 2026-07-15T09:43:41Z

R3 is the latest completed run and no replay, builder, verifier, pytest, or
repair process is active. It preserves the R2 shape and economics exactly:
15,304 candidates -> 192 scorecards -> 18 order events -> seven terminal
orders/trades; one ordered-tick headline winner at `+0.60931716R` and seven
physical rows at `+3.15530942R`; 15,297 missed rows; executed
REFUSED/source-gap `0/0`. V89D/V90/V92 remain May stress comparators only;
V258 remains the June route-certified comparator; warmed January remains the
only valid full-January behavioral baseline.

R3 closed compact fill-floor provenance but exposed one same-root terminal
truth batch. Across 478 explicit prefinal proposals, the final missed row
selected a postdecision fill-realism, scheduler, session, or execution
co-blocker over unresolved predecision package fill-floor authority. Missed
attribution also preferred stale candidate blocker aliases over the richer
contract it had just recomputed. One XAUUSD scorecard preserved true authority
only as `*_before_terminal_execution_truth_reconciliation`, while the signed
runtime-admission consumer requires the equivalent `*_pre_finalization` stage
alias. The corresponding terminal order row was already correct.

The repair spans the complete affected producer-consumer chain:

- unresolved package fill-floor authority remains the canonical primary
  blocker; displaced reasons remain explicit co-blockers;
- scheduler missed attribution consumes recomputed transfer status/class/
  reason/source/binding before stale candidate aliases;
- exact terminal reconciliation preserves true executable claims and their
  provenance as prefinal stage fields before applying terminal truth;
- focused runtime and verifier consumers cover all three contracts.

This is a correctness and ledger repair, not policy tuning. Expected candidate
-> scorecard -> order -> fill counts, missed positive/negative R, trade count,
net/gross/final R, cash PnL, W/L/F, cost authority, and full/reduced-risk
distribution are unchanged. The in-memory current-code projection checks all
15,297 R3 missed rows and reduces preserved-proposal defects from 478 to zero;
the reconstructed XAU stage flow has both prefinal executable aliases true and
zero verifier reasons. Compile, scoped diff check, and the complete focused
harness/verifier/runtime barrier pass 1,345 tests; the sole warning remains the
pre-existing unknown `asyncio_mode` option.

The two frozen-snapshot explorers were resumed and explicitly asked to return,
but remained running without a result and were closed. Their unreturned claims
were not used. Current-code reproducers, the full artifact projection, and
focused tests are the authority for this batch.

The replay-free R4 contract is valid under shared digest
`44a8c0464e1b479abb76b97a72b3365aaefc2fcc8a0b1b31ca55b691c28c8c27`
and contract digest
`dcdefb18c495e2be6b41985e29c2b2ae6134e8a48a47c37c01c3761ee566991b`.
January and April source-plan digests remain unchanged. The next proof is the
same all-symbol January 22-23 cold-start slice under R4, not a broad replay.

R4 succeeds only if behavior and relational counts remain exactly R3, compact
fill-floor provenance remains complete, preserved-proposal scanner bad counts
are empty for missed and scorecard ledgers, the exact XAU scorecard and order
both pass prefinal runtime admission/member-axis contracts, all risk-status
aliases remain exact, and executed REFUSED/source-gap counts remain zero. A
count or economic change means this truth-only batch changed behavior. A
remaining contract defect blocks January 29-30. A green R4 authorizes the
January 29-30 targeted proof; neither slice proves warmed January, total
reservoir conversion, final selection, or live authority.

## Terminal Blocker Stage R4 January 22-23 Result - 2026-07-15T10:21:52Z

The R4 all-symbol cold-start proof completed successfully under shared digest
`44a8c0464e1b479abb76b97a72b3365aaefc2fcc8a0b1b31ca55b691c28c8c27`.
Its source-plan digest is
`1d3c6a2e2c62b25cc0c0c5b8ea28e06f22222581f266844f62cd99738ccdeda9`,
both one-day capacity chunks are valid, and broker/live/final remain false.

Behavior is exactly neutral to R3: 15,304 relational candidates, 4,608
decisions, 192 scorecards, 18 order events, seven terminal orders/trades, and
15,297 missed rows. The ordered-tick headline remains one winner at
`+0.60931716R` net / `+0.70651595R` gross and final, `+60.38580597` cash,
`99.10406261` risk cash, and `0.1%` risk. All seven physical rows remain
`+3.15530942R`; the six M1-only diagnostic rows remain `+2.54599226R`.
Normalized R3/R4 comparison finds zero added, removed, or economically changed
order events and trades. Stress remains `+0.55931716/+0.50931716/+0.40931716R`;
the 200-iteration deterministic Monte Carlo remains `+0.60931716R` with zero
drawdown on the one headline trade.

The complete relational candidate scan covers 15,304 rows and 14,128 package
rows with empty candidate-quality, scheduler-quality, and identity error maps.
Order-executable transfer, broker-cost authority, and terminal path-provenance
scanners all have empty `bad_counts`. Executed REFUSED and source-gap rows are
zero. Missed opportunity remains diagnostic only: 3,294 scoreable rows contain
`+726.19717356R` positive and `-3350.23999089R` negative opportunity for
`-2624.04281733R` net; executable missed rows remain zero.

The named rows pass exactly. EURUSD
`broadorigin_20287b4bddf5417d030448df` retains expected-net-R floor lists
`raw=[expected_net_r]`, `resolved=[]`, and `unresolved=[expected_net_r]` with
package authority as the terminal blocker. The January 23 07:45 XAUUSD
proposal preserves both candidate and order prefinal aliases as true on its
scorecard and order, then is correctly final-blocked by `fill_realism` because
`ordered_tick_required_for_adverse_before_profit_sequence_not_satisfied`.

Disposition: January 22-23 R4 is green as a bounded truth-contract proof. It
does not prove warmed-January behavior or total reservoir transfer. The next
dependency proof is January 29-30 under the same code/config/shared digest;
only after that slice is green may the warmed full-January artifact be
regenerated.

## Terminal Blocker Stage R4 January 29-30 Result - 2026-07-15T10:57:40Z

The second all-symbol R4 proof completed under the same shared digest with a
valid two-chunk capacity contract, exact source-plan binding
`1e433f34afea0241ba03acf20af17d8700100bae71288e78303294c3cb382b44`,
monotonic order sequence, and complete source-cache release. It materializes
10,751 relational candidates, 4,608 decisions, 192 scorecards, eight order
events, three terminal filled orders/trades, and 10,748 missed rows.

There is no ordered-tick headline trade. The three M1-only diagnostic fills are
the same three losses as the prior January subset and remain `-1.64391447R` net
(`-1.45242575R` gross/final). Missed diagnostic opportunity contains 2,625
scoreable rows, `+701.59893136R` positive, `-2605.8092148R` negative, and
`-1904.21028344R` net; executable missed rows remain zero. Executed REFUSED and
source-gap rows remain zero.

All truth consumers pass. Candidate-quality parity covers 10,751 rows and
10,113 package rows with empty missing, scheduler-parity, and identity error
maps. Order-transfer, broker-cost, and path-provenance scanners all have empty
`bad_counts`. The exact USDJPY
`broadorigin_39e12ee61b9bb58b3da25f1a@@2026-01-29T14:15:00+00:00`
and GBPUSD
`broadorigin_bf3d8d72fc98e5bee9c47148@@2026-01-30T07:45:00+00:00`
rows retain `raw=[expected_net_r]`, `resolved=[]`, and
`unresolved=[expected_net_r]` with canonical package-authority blockers.

The apparent order-count difference is proof-only. The prior warmed-January
subset has seven order rows and four unique order IDs. R4 preserves all seven
and adds one non-terminal diagnostic XAUUSD row for
`broadorigin_7e3a7864ad8253fbe7baefe5@@2026-01-30T09:45:00+00:00`.
The old missed row already ranked that exact instance first and final-blocked it
for the same ordered-tick source gap. R4 makes that blocked proposal visible in
the order ledger; it does not create an order, fill, or economic change.

Disposition: both bounded R4 truth slices are green. They prove the corrected
producer-consumer contract locally, not warmed monthly behavior. The next step
is a new January 1-31 run with all 31 one-day chunks and accumulated adaptive
memory, followed by rebuilt flow/parity/manifest/verifier consumers and exact
comparison against the prior full-January artifact.

The required storage checkpoint is complete. Free space is 118 GiB and the
prior full-January artifact set is 14 GiB. The prior full-January evidence and
both targeted R4 proofs remain active comparators, and the new run will use a
new prefix. No deletion is required before regeneration.

## Warmed Full-January R4 Completion - 2026-07-15T15:38:36Z

The complete January 1-31 R4 run finished all `31/31` one-day chunks under
shared execution digest `44a8c046...c8c27` and source-plan digest
`b41714da...14d2`. No replay, analyzer, builder, verifier, pytest, or repair
process is active. Broker mutation, live authority, final selection, and live
activation remain false.

The run is exactly behavior-neutral to the prior warmed January artifact. It
materializes `154390` relational candidates, `69888` decisions, `2016`
scorecards, `147` order events, `61` terminal orders/trades, and `154329`
missed rows. Exact identity comparison has `61` common trades, zero added,
zero removed, zero duplicate identities, and zero delta in every summary,
denominator, transfer, and economic field. The extra XAUUSD order event seen in
the cold January 29-30 slice was cold-state-only proof materialization: in the
warmed run that exact rank-one candidate remains in missed, final-blocked by
ordered-tick fill realism. The correct warmed count is therefore `147`, not the
provisional `148`.

Economic truth remains mixed and unchanged:

- all physical fills: `61`, W/L/F `24/37/0`, net/gross/final
  `-2.76396743/+0.94088713/+0.94088713R`, cash `-$1595.02061428`, risk cash
  `$7860.10366135`, risk-percent sum `7.925%`, cost `3.70485455R`;
- ordered-tick headline: `4`, W/L/F `3/1/0`, net/gross/final
  `+0.98832036/+1.30562064/+1.30562064R`, cash `+$98.24769712`, risk cash
  `$395.31136071`, risk-percent sum `0.4%`, cost `0.31730028R`;
- full-risk/reduced-risk fills: `2/59`, net `-1.44564903/-1.31831840R`;
- missed diagnostic scoreable/unscoreable: `26812/127517`, positive
  `+6616.22471941R`, negative `-30828.76993654R`, net `-24212.54521713R`;
  executable missed R remains zero.

Window-normalized parity is `1101` axes -> `990` candidate-generated axes ->
`497` scorecard/order axes -> `29` order axes -> `28` filled axes. The additive
executable-gated denominator is `46627.324978458R`. The
`1239092.20751002R` combined-signal surface is diagnostic and is not an
additive replay denominator.

All four complete scanners are green: candidate quality and scheduler parity,
order-executable transfer, broker-cost authority, and terminal path provenance
have empty bad-count maps. Canonical quality passes `154390/154390` rows.
Executed REFUSED/source-gap rows are `0/0`, and guarded-market fallback use is
zero. Flow, source-bound parity, leakage, repair-plan, and exact comparison
consumers are materialized deterministically.

The canonical B7.5 contract is rebound to digest
`dcdefb18...991b`, pair binding `154fddf2...d940`, and the unchanged R4 shared
digest. Current free space is `89111756800` bytes. Projected April retention is
`16948199820` bytes, leaving `72163556980` bytes and
`37803818612` bytes above the mandatory reserve, so no evidence deletion is
required.

Disposition: warmed January R4 truth and deterministic consumers are GREEN;
canonical route certification is the immediate dependency. Once its builder,
verifier, prompt hardening, artifact audit, and scoped diff check pass, commit
the January checkpoint and run April once under the same shared digest and
April source-plan digest `4987d98c...46d2`. This January proof is not B7.5
generalization, final selection, or live authority.

## Warmed Full-January R4 Route Certification - 2026-07-15T16:10:15Z

The January R4 prefix is now canonical route-certified. The builder completed
at `2026-07-15T15:47:01Z` and bound 23 January-prefix artifacts in a 202-file
manifest while retaining compact replacements for four intentionally omitted
raw artifacts. The physical verifier completed at `2026-07-15T16:05:48Z`
with `ok=true`, `issue_count=0`, exact January prefix selection, 154,390
candidate-quality rows, and empty transfer, broker-cost, and path-provenance
bad-count maps.

Fresh compile, prompt hardening, the standard route artifact audit, and scoped
diff validation all pass. The route audit has no required or warning gaps, no
zero-byte JSON, no JSON parse failures, no JSONL parse/read failures, and all
retired-label checks pass. Certification changes no replay behavior and keeps
`final_package_selected=false`, `live_trading_enabled=false`, and broker/live
authority closed.

Disposition: January route certification is DONE. Commit this scoped proof
checkpoint, then run April 1-30 exactly once under shared digest
`44a8c046...c8c27` and source-plan digest `4987d98c...46d2`. No selector,
scheduler, risk, order, lifecycle, fillability, cost, or exit policy may change
between the paired windows. April and the subsequent January/April/May/June
comparison remain the active B7.5 proof; January alone does not establish
generalization, final selection, or live authority.
