# B7.3 V250 Post-Replay Truth And Scalability Brief

Generated UTC: 2026-07-13T20:24:31Z.

## Decision

Do not run another replay. V250 completed on unchanged V249 behavior code and
is the current B7.3 independent-regime evidence. Close one behavior-neutral
same-root batch across parity authority, same-window comparison identity,
headline/strict result truth, and harness GC lifetime. Then rebuild only the
deterministic V250 analyzers and compare every removed V104 trade against the
current candidate-instance projection before selecting a behavior repair.

Broker mutation, live authority, and final selection remain false. Local
replay/package authority remains full across all 82 sleeves.

## Current Disk And Process State

- HEAD: `f0fc2b425` (`certify V249 hostile replay authority`).
- No replay, parity builder, flow analyzer, comparator, pytest, compile, route
  verifier, or audit process is active.
- Task-owned dirty code before this batch:
  `compare_broad_live_as_if_replay_runs.py` and
  `tests/test_compare_broad_live_as_if_replay_runs.py` for explicit cross-root
  comparison support.
- Task-owned control dirt: `CURRENT_ROOT_CAUSE_MAP.json`,
  `CONTINUATION_CURSOR.json`, the Fable matrix, V250 retention manifest, and
  this brief. `.context/LIVE_STATE.md` and `AGENTS.md` are excluded from the
  eventual scoped commit.
- Current V250 raw, summary, parity, flow, and V104 comparison artifacts are
  complete and retained. They must not be deleted or regenerated by simulation.

## Latest Completed Replay

Prefix:
`BROAD_LIVE_AS_IF_REPLAY_V250_B7_3_NON_HOSTILE_5D_UNCHANGED_V249_20260601_20260605_REPAIRED_ONLY_COMPACT_FULLGRID`

Scope: configured 24-symbol `2026-06-01..05` independent regime, repaired
profile only, compact candidate index and missed ledger, ordered-tick headline
truth enabled.

- source / decision / candidate / scorecard / order-event / accepted-pending /
  terminal-order / physical-trade / headline-trade / missed:
  `310 / 11520 / 35191 / 480 / 30 / 14 / 14 / 13 / 4 / 35177`
- physical W/L/F and gross/final/net:
  `8/5/0`, `+7.82542991/+7.82542991/+7.03578717R`
- headline W/L/F and gross/final/net:
  `3/1/0`, `+1.92353135/+1.92353135/+1.59506223R`
- headline cash PnL / risk cash / risk pct:
  `-$67.96685163 / $1199.80760351 / 1.20%`
- headline full-risk / reduced-risk fills: `2/2`; all physical rows: `2/11`
- diagnostic-only M1 rows / net R: `9 / +5.44072494R`
- cost REFUSED / source-gap executions: `0/0`
- headline stress at extra `0.05/0.10/0.20R`:
  `+1.39506223/+1.19506223/+0.79506223R`
- Monte Carlo worst drawdown: `-1.12773511R`
- package / candidate-generated / scorecard-or-order / order / filled axes:
  `1101 / 894 / 319 / 14 / 12`

V250 is locally positive and stress-positive, but headline cash is negative
because the two full-risk trades net negative cash while the two reduced-risk
trades net positive cash. That is a later causal value-calibration question,
not permission to change risk policy inside this truth batch.

## Baseline Discipline

- V89D, V90, and V92 are hostile-window historical comparators only. Their
  respective headline-era trade/net-R anchors are `56/+34.84520454R`,
  `51/+28.84201157R`, and `51/+29.35570236R`; they are not V250 denominators.
- V249 is the current hostile ordered-tick checkpoint: `18` headline trades,
  `10/8/0`, `+0.28044419R`, `+$113.42435391`.
- V104 is the exact V250 window but weaker M1-proxy truth: `46` trades,
  `26/20/0`, `+14.73101491R`, `+$2850.02330565`.
- Exact cross-root V104 versus V250 comparison currently finds `4` common,
  `9` V250-added, and `42` V104-removed trade keys. Added rows net
  `+2.19996363R`; removed rows net `+9.89519137R`. The removed rows are not
  accepted as executable winners until joined to current signed authority,
  cost, fillability, lifecycle, and ordered-tick truth.
- V110B covers `2026-06-01..19` and remains structural context only. It cannot
  be compared directly to this five-day denominator.

## Subagent Reconciliation

- Active agents: `0`; unreconciled returns: `0`.
- All pre-V249 findings were either incorporated in commit `f0fc2b425` or
  superseded by the certified V249 route.
- No new audit wave starts before this same-root batch is green and the V250
  deterministic rebuild is parsed.

## Root-Cause Chain

| Stage | State | Current evidence |
| --- | --- | --- |
| source-bound -> candidate | PARTIAL TRUTH DEFECT | 894/1101 axes generate, but axis parity selects the first stale candidate proof instead of the current order/trade authority envelope, falsely reducing V250's local source-bound denominator to zero. |
| candidate -> selector | CONTRACT GREEN / VALUE OPEN | 35,191 candidates and 480 scorecards match V104. Current authority is valid for 3,119 candidates; 32,072 remain absent. Do not relax policy until exact removed-key blocker attribution exists. |
| selector -> scheduler | CONTRACT GREEN / VALUE OPEN | 480 scorecards produce 14 current orders; exact V104 removed keys must be joined to final blocker and displacement evidence. |
| scheduler -> risk | CONTRACT GREEN / VALUE OPEN | Full/reduced authority is present. Headline cash sign differs from R because full-risk rows lose cash; causal calibration is later work. |
| risk -> order | GREEN | Zero REFUSED/source-gap execution. Preserve this invariant. |
| order -> lifecycle -> fill | TRUTH GREEN / TRANSFER OPEN | 14 terminal orders, 13 physical rows, 4 ordered-tick headline rows. Nine M1 rows remain diagnostic; no promotion without tick truth. |
| fill -> exit | VALUE OPEN | Headline R is positive and stress-positive, but only four authoritative rows and negative cash. Broader proof remains required. |
| ledger/verifier | OPEN SAME-ROOT BATCH | Comparator excludes one midnight scorecard, outputs only sampled identities, strict parity can contradict headline truth, and chunk-local GC restoration causes pathological final aggregation. |

## Same-Root Batch

Batch: `B7_3_V250_CURRENT_AUTHORITY_WINDOW_IDENTITY_AND_GC_TRUTH`.

Affected files:

- `build_source_bound_execution_parity.py` and its focused tests;
- `compare_broad_live_as_if_replay_runs.py` and its focused tests;
- `run_broad_live_as_if_replay_harness.py` and its focused tests;
- current root map, cursor, Fable matrix, and rebuilt V250 compact artifacts.

Patch classifications:

- current-stage authority envelope selection: correctness and diagnostic-ledger;
- trading-day-first windowing and complete key sets: correctness and diagnostic-ledger;
- strict result eligibility as a headline subset: correctness and ledger truth;
- outer-frame-only automatic-GC restoration: performance/scalability.

## Expected Effects Before Proof

- candidate -> scorecard, scorecard -> order, order -> fill, trade count, R,
  cash, W/L/F, and full/reduced distribution: unchanged because no simulation
  or policy code changes;
- V250 comparator scorecard rows inside the window: `480`, not `479`;
- exact added/common/removed identity arrays: `9/4/42` complete keys;
- V250 exact-window effective source-bound denominator: nonzero wherever a
  current signed executable order/trade envelope exists, while global reservoir
  fields remain diagnostic;
- cost REFUSED/source-gap execution: remains `0/0`;
- strict eligibility: every future strict-eligible row is headline-eligible;
- harness automatic GC: remains disabled through all chunks and final
  aggregation, then restores exactly on return or exception.

## Acceptance And Failure Rules

Helped:

- focused compile/tests pass;
- existing V250 parity and comparison rebuild without simulation;
- local denominator no longer becomes zero from stale first-proof selection;
- 480 scorecards are windowed correctly and all exact trade identities emit;
- no safety or authority invariant changes;
- GC state is disabled inside multiple chunks and restored after the outer
  harness frame exits.

Failed:

- any changed replay behavior, R, trade identity, or executable authority;
- multiple distinct signed authority envelopes are silently merged;
- REFUSED/source-gap rows become executable;
- strict eligibility remains broader than headline eligibility;
- the V250 local denominator remains zero despite current signed order/trade
  authority.

Deeper flaw exposed:

- after the truth rebuild, exact V104 removed-key joins show current signed,
  broker-cost-passed, ordered-tick-fillable positive opportunities blocked by a
  common causal selector/scheduler/risk/lifecycle defect. That coherent blocker
  family becomes the next behavior-changing B7 batch; do not broad replay first.

This batch repairs current proof and scalability. It does not itself improve
V250 economics, prove broad-history robustness, or open broker/live/final.

## Completion Evidence

Completed UTC: 2026-07-13T21:31:31Z.

The behavior-neutral truth batch is closed:

- parity now carries the complete current terminal execution contract and all
  `13/13` physical trade candidate identities have an executable trace;
- `execution_fillability_missing_source_bound_input` fell from `8` to `0`;
- the exact five-day executable-gated source-bound denominator is
  `356027.9026815028R`, separate from the global diagnostic reservoir;
- deterministic V250 artifacts contain `22,971` parity rows, `35,191`
  candidate-instance projections, and `1,101` leakage buckets;
- the exact V104 removal set is fully classified: `9` current candidate-
  generation absences, `16` selector blocks, `16` scheduler blocks, and `1`
  order/fillability/lifecycle block;
- all `42` removed V104 outcomes are M1-proxy diagnostics, not current
  executable proof. Their aggregate `+9.89519137R` does not authorize weaker
  broker-cost, session, selector, scheduler, fillability, or lifecycle rules;
- `273` focused tests pass, the canonical builder binds V250, the physical
  verifier reports zero issues, prompt hardening passes, and the standard route
  artifact audit is green with no missing required or warning artifacts;
- replay behavior is unchanged and broker/live/final remain false.

B7.3 is closed as bounded independent-regime evidence: headline behavior is
`4` trades, `3/1/0`, `+1.59506223R`, stress-positive through `+0.20R` per
trade, but cash is `-$67.96685163` and the sample is too small for a final
economic claim. The next Fable dependency is B7.4: one unchanged current-truth
run over `2026-06-01..19`, after this checkpoint is committed and its own
pre-replay brief fixes the acceptance criteria.
