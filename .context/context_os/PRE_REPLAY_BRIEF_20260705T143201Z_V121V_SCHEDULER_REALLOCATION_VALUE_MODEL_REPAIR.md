# V121V Scheduler Reallocation Value-Model Repair Pre-Replay Brief

Generated: `2026-07-05T14:32:01Z`

Broker/live/final remain closed. Local replay/package authority remains full 82-sleeve authority.

## 1. Latest Completed Replay

Latest completed replay: `BROAD_LIVE_AS_IF_REPLAY_V121U_STOP_HAZARD_PUBLIC_RISK_SURFACE_REPAIR_20260514`

Window: `2026-05-14..2026-05-14`

Interpretation: bounded one-day hostile truth repair proof only. It does not prove total million-R reservoir conversion.

Rows: candidate `7968`, scorecard `96`, order `51`, trade `24`, missed `7942`.

Behavior: `+0.29253564 net R`, `+2.11053495 gross/final R`, `+24.60851082 cash PnL`, `2406.04506533 risk cash`, W/L/F `9/15/0`, expected cost `1.81799931R`.

Same-window transfer summary: `1101` source axes, `848` candidate-generated axes, `299` scheduler-option-present axes, `25` scorecard-selected axes, `23` axis-attributed trades, `239087.650287501` same-window source-bound R denominator, `0.0348003` axis-attributed actual R.

## 2. Active Process State

No broad replay, pytest, or broad git-add process is running. Idle Context OS MCP helpers remain present but are not active replay/build work.

## 3. Baseline Comparison

These are hostile five-day comparators, not same-denominator proof against the one-day V121V smoke.

| Run | Window | Trades | Orders | Scorecards | Missed | Net R | Gross/Final R | Cash PnL | W/L/F |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| V89D | 2026-05-13..17 | 56 | 243 | 288 | 24885 | +34.84520454 | +39.93441037 | +8178.90660707 | 41/15/0 |
| V90 | 2026-05-13..17 | 51 | 233 | 288 | 24890 | +28.84201157 | +33.36349114 | +6371.80465431 | 37/14/0 |
| V92 | 2026-05-13..17 | 51 | 239 | 288 | 24887 | +29.35570236 | +33.93212860 | +6228.63096022 | 37/14/0 |
| V121U | 2026-05-14 | 24 | 51 | 96 | 7942 | +0.29253564 | +2.11053495 | +24.60851082 | 9/15/0 |

## 4. Dirty Files And Active Code Changes

Active V121V files:

- `src/research/moonshot_scheduler_v4_best_trade_allocator.py`
- `tests/test_moonshot_scheduler_v4_best_trade_allocator.py`

Known unrelated dirty surface remains from prior route/context work and is not part of this checkpoint: Context OS docs/state files, route builders/analyzers, selector/timewarp tests/code, old deleted science ledgers, and route summaries.

## 5. Subagent Findings

Schrodinger 2nd, B1 raw/effective provenance: partially incorporated, deferred for the next same-root batch. Open items are explicit `effective_selector_action` on scheduler rows, namespace alias original/raw selector search, early `risk_request_selector_action` source order, and terminal lifecycle R identity.

Cicero 2nd, B2 scheduler/reallocation: incorporated in V121V. Release bonus no longer contributes to transfer score, and authority tiers are diagnostic rather than primary sort value. Follow-on: general best same-window executable comparator before final max selection.

Einstein 2nd, verifier/proof: incorporated as proof context. V121U verifier scans are fatal-clean for current proof surfaces; risk ladder is guarded but V121U remains reduced-risk-only. Open precision gap: dedicated capped candidate-generation/symbol-subset fatal tests.

## 6. Known Mismatch Classes

Source-bound -> candidate: partially fixed. V121U same-window has `848/1101` generated axes and `253/1101` not generated.

Candidate -> selector: partially fixed. Raw/effective action provenance is mostly preserved, but one early risk-request chain and public scheduler row semantics remain ambiguous.

Selector -> scheduler: active V121V patch. Release/reallocation diagnostics were acting like transfer value and could displace better executable candidates.

Scheduler -> risk: partially fixed. Stop-hazard cap and finalizer proof are clean, but behavior is reduced-risk-only.

Risk -> order: partially fixed. Cost-refused/source-gap execution is blocked; order transfer remains small.

Order -> lifecycle -> fill: open. Expiry/fill/lifecycle buckets need review after scheduler value truth is restored.

Fill -> exit: open. V121U has 15 losses; exit/stop geometry remains a likely next bucket.

Ledger: partially fixed. Normal close R identity is fixed; terminal lifecycle close R identity remains open.

## 7. Fixed / Partial / Open / Newly Exposed

Fixed in focused code/tests: release quality is eligibility/tie-break metadata, not transfer alpha; package/scheduler authority priority tiers are diagnostic-only; exact-vs-inferred quality and primary-vs-reduced-fallback contracts are now represented as predecision transfer-quality calibration; same-window diagnostics now report the best executable comparator; active package-authority rows with `final_blocked` or `not_order_executable` order-transfer status are not final-selected; explicit package replay executable authority is accepted as admission authority when source-bound/broker-cost authority is present; compile passed; focused scheduler tests passed `6 passed`; full scheduler tests passed `239 passed`.

Partial: same-window selected-transfer quality must be proven by replay; B1 public provenance remains open.

Open: terminal lifecycle R identity, reduced-risk-only behavior, lifecycle/exit losing buckets, and broad proof that same-window comparator truth improves executable behavior.

Newly exposed expectation: if V121V loses R, first check whether it removed artificial release/authority value and exposed downstream order/lifecycle/exit leaks.

## 8. Highest-Leverage Same-Root Batch

Batch: scheduler replacement/reallocation value-model truth.

Root cause: non-alpha release/cap-cleanup diagnostics and authority tiers could influence winner choice as if they were expected transfer value; active package-authority rows with non-executable order-transfer status could still survive selection if only runtime eligibility was checked; package replay executable aliasing was not consistently admitted into order authority; exact/primary contracts were previously enforced by priority tiers instead of transfer calibration.

## 9. Files / Components

- `src/research/moonshot_scheduler_v4_best_trade_allocator.py`: scheduler sort, release/reallocation diagnostic, same-window executable comparator diagnostics, package order-transfer selection filter, package admission alias parity, expected-transfer quality calibration.
- `tests/test_moonshot_scheduler_v4_best_trade_allocator.py`: regression coverage for sort contract, release semantics, same-window comparator, and active package-authority non-executable selection blocking.

## 10. Patch Type

Scheduler patch: correctness repair.

Test patch: regression/diagnostic repair.

No performance-only rule, no loss-bucket overfit, no date/symbol/session suppression rule.

## 11. Expected Measurable Effect

Candidate -> scorecard transfer: unchanged or small mix shift.

Scorecard -> order transfer: selected primary expected-transfer quality should improve or remain honest; lower-transfer release-driven winners should decline; active package-authority `not_order_executable` rows should not become selected order paths; exact package and primary trade rows should remain protected without direct authority-tier sort keys.

Order -> fill transfer: may shift with order mix; report filled/expired.

Missed positive R: may increase if better candidates still fail downstream, exposing next leak.

Missed negative R: may change; separate positive and negative.

Trade count: may rise or fall; not accepted as better if it only suppresses trades.

Net/gross/final R: same-window compare against V121U `+0.29253564 / +2.11053495 / +2.11053495`.

W/L/F: same-window compare against V121U `9/15/0`.

Cost-refused/source-gap execution: must remain zero executed refused/source-gap rows.

Risk distribution: report full-risk vs reduced-risk; V121U baseline is reduced-risk-only.

## 12. Replay Success / Failure Criteria

Helped: verifier remains green, release value contribution stays zero, selected-transfer quality improves, active package non-executable rows stay missed/diagnostic, and behavior improves through better conversion/selection rather than pure blocking.

Failed: lower-transfer release rows still displace higher-transfer executable candidates, or authority tiers re-enter sort value.

Exposed next flaw: R worsens while selected-transfer truth is cleaner; then inspect scheduler comparator, lifecycle/expiry, risk-expression, and exit/stop buckets before policy tuning.
