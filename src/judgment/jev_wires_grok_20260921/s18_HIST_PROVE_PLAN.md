# HIST-PROVE PLAN — paired train-row harvest (Challenge tape)

**as_of_ict:** `2026-09-21T06:04:19+07:00`  
**session:** `18_jev_train_row_harvest`  
**login:** `0` · ns `operator` · magic `0`  
**never_broker_place:** true · **harvest APPLY:** 0 · **sleeve-select global APPLY:** do not flip  
**Module_ATR honesty:** no invented ATR/regime; never merge R lenses.

This plan proves the **harvest sidecar** (emit + TypeSafe evaluate + pairing). It does **not** authorize live fire-rate change. Fire-rate APPLY (Policy C priors / admit / place) is a **later Chair verb** after this sidecar proves honest.

---

## 1. Tape paths (FOUND on box — cite these)

### 1.1 Challenge closes (primary)

| path | what | n / metrics |
|---|---|---|
| `/workspace/gtos/close_loop/war_room_20260920/learning_scoreboard_60.jsonl` | per-close Challenge-true rows | **n=60** · sumR=**-38.5948** · wins=**7** · losses=**53** · WR=0.1167 |
| `/workspace/gtos/close_loop/war_room_20260920/learning_scoreboard_60_summary.json` | asset/session folds | INDEX 16 / -16.64; XAU 33 / -16.41; CRYPTO 3 / -3.28; FX 8 / -2.26 |
| `/workspace/gtos/close_loop/war_room_20260920/jev_everywhere_lessons_0.jsonl` | lesson overlay n=60 | miss FS=50, event_gap=3, ok_win=7 |
| `/workspace/gtos/close_loop/war_room_20260920/_pr41_land/ai-trading-agent/judgment/astra/lab/challenge_shadow_20260917/deals_since_20260909.jsonl` | broker deals | FOUND |
| `.../sit_20260917.json` | sit snapshot | FOUND |
| `.../learn_loop_v0/CLOSE_LOOP_BATCH_V1.json` | sealed aggregates n=48 | `tickets_invented=false`; **do not invent the other 46 tickets** |
| `.../learn_loop_v0/fixtures/challenge_closes.jsonl` | n=6 sample | not the batch |

Exit mix (scoreboard_60): orig_stop=53, time_stop=5, orig_tp=2.

### 1.2 Challenge bars (for entry-legal COMPLETE_STATE / ATR-from-bars)

`CHALLENGE_BAR_DIR` =  
`/workspace/gtos/close_loop/war_room_20260920/_pr41_land/ai-trading-agent/judgment/astra/lab/challenge_shadow_20260917/`

Landed M15+H4: XAUUSD (also D1), EURUSD, EURGBP, GBPUSD, GBPJPY, USDJPY, BTCUSD, ETHUSD, US30_cash, UK100_cash.

Also: `/workspace/gtos/fable_joint_pull_20260917/challenge_shadow_bars/multi/`.

**MISSING (honest):** `XAGUSD_M15.csv` — matches `JEV_WIN_LOSE_LEARN_XAGUSD_HONEST_EMPTY` n_pairs=0. Do not invent year twins.

**FORBIDDEN:** April `data/historical` (learn_loop `tape_uses_april_historical`).

ATR: use `bars` / `atr14` on **landed** M15/H4. If tape dark → `geometry.atr14_from_bars=null` / STATE_MISSING. **Do not invent Module_ATR from Challenge stop_dist.**

### 1.3 Module_ATR blotters (third lens — separate R universe)

| blotter | use |
|---|---|
| `.../war_room_20260920/blotter_Module_ATR_XAUUSD_dsp_three_fresh_lower_lows.jsonl` | B-set year twins |
| `.../blotter_Module_ATR_XAUUSD_dsp_spring_close_on_20low_through_the_box.jsonl` | C-set year twins |
| `.../blotter_Module_ATR_GBPJPY_*` | GBPJPY proxy — **not** live sleeve proof |
| `.../blotter_Module_ATR_XAGUSD_*` | PR40 approx — **not** Module_ATR yearfold deepen |

Yearfold receipts:

- `_intel_capture_activate_20260920/overlays/THREE_FRESH_MODULE_ATR_YEARFOLD_20260920.json`  
  HOLD sumR_Module_ATR **+315.14** · TRAIN **+176.82** · **never merge** with Dig/Edge
- `SPRING_MODULE_ATR_YEARFOLD_20260920.json`  
  HOLD **+261.00** · TRAIN **+225.31**

Cited B/C cards (Module_ATR only):

| pair | win year / sumR | lose year / sumR |
|---|---|---|
| B1 three_fresh | 2025 / +186.00 n=985 | 2026 / -53.86 n=454 le0 |
| B2 three_fresh | 2024 / +147.75 n=910 | 2021 / -58.16 n=723 le0 |
| C1 spring | 2025 / +98.25 n=463 | 2015 / -38.25 n=465 le0 |
| C2 spring | 2017 / +82.50 n=403 | 2023 / +0.00 n=531 le0 |

who_decided=`geometry_proxy+Module_ATR`. Honesty: **APPROXIMATION** ≠ live module TradeIntent (no stay_timing / peer_panel / admission).

### 1.4 Existing pair packs (regression, not the only source)

`JEV_WIN_LOSE_LEARN_20260920` n_pairs=7 n_cards=13 (Challenge KEEP twins A1–A3 + Module year B/C). Determinism PASS (`JEV_WIN_LOSE_LEARN_determinism.json`).

EURUSD Module_ATR SHORT deepen: n=5818 sumR=+1114.95 — **does not prove** live `sub_mid_dn_revert` LONG. Soft twins are RELATIVE_WEAK not invented losses. Challenge STAND cite ticket **291789105** is a **separate lens**.

BTCUSD pack: research-only / `banned_for_aplus`.

### 1.5 VPS live tree

**MISSING this seat:** machineId `7cfa9657-805b-4e9c-9fbb-886c500f997b` UNREACHABLE (no host-mesh). Chair re-runs same rg on Admin when Shell is up. Do not SSH.

---

## 2. Module_ATR honesty checklist (gate — any fail = NOT_PROVED)

1. Every harvest row has exactly **one** filled R column matching `lens`.
2. Challenge closes never carry `R_Module_ATR` unless a blotter join key matched **and** Challenge R stays in `R_Challenge_book`.
3. Year-twin pairs use **Module_ATR blotter years only** — not Challenge ticket R.
4. `module_keep_vs_challenge_stand` pairs cite both sides and **do not sum R**.
5. No `atr14` invented: bars present or null.
6. `regime_tag` stays null/PENDING unless S14 hydrate exists (it does **not** on the 60). Partial `session_h4_context_only` n=54 is allowed; do not rename it `regime_tag`.
7. EURUSD Module_ATR SHORT proxy rows stamp `does_not_prove_live_sub_mid_dn_revert=true`.
8. XAGUSD emit n_pairs=0 (honest empty).
9. Dig_3R 2022/2026 false-spring WHY may be **cited as idea shape** but R on C cards stays Module_ATR.
10. Geometry-proxy year win (`sumR>0`) with `full_state_dark=true` is **not** a live strike (Noul `harvest_geometry_alone_neq_strike`).

---

## 3. Replay protocol

### Phase 0 — inventory (no TypeSafe required)

- Load `learning_scoreboard_60.jsonl` (n=60).
- Identity filter login=0; drop still_open.
- Join deals/sit for geometry; join bars via `books_for_symbol` (no XAU default).
- Count `tape_present` per symbol. Report STATE_MISSING occupancy/corr/regime.
- **Do not POST** if `GTOS_JEV_TRAIN_HARVEST_CALL` off.

### Phase 1 — unpaired harvest emit (SHADOW)

- `GTOS_JEV_TRAIN_HARVEST_SHADOW=1`
- `GTOS_JEV_TRAIN_HARVEST_CALL=1` (if key present)
- `GTOS_JEV_TRAIN_HARVEST_APPLY=0`
- `GTOS_JEV_MAX_CALLS=500000`
- `GTOS_JEV_A1_CALL` not 0
- Emit 60 rows schema `gtos.jev.train_row_harvest.row.v1` to a **new** JSONL (not the mixed 241-row dump).
- Each row POSTs `jev_client.evaluate(complete_state)` with harvest questions + existing `close_label` / `admit` / `event_proximity` (entry-legal state).
- Fail-closed: if Jev dark, row still emits with `jev_dark=true`, `answers={}`, unpaired.

### Phase 2 — pair matcher

- Enumerate ticket twins on Challenge_book (same symbol×sleeve_family×session×side, opposite R sign).
- Stamp loser reuse.
- Add Module_ATR year twins from blotters (B1/B2/C1/C2 already named — regression).
- Jev Choice among enumerated candidates; dark → unpaired.
- Expected Challenge KEEP ticket pairs (regression):
  - A1 EURGBP vss London: **291816474** (+1.87 orig_tp) vs **291087142** (-1.20 orig_stop)
  - A2 sleeve sub_mid_dn_re: **293540988** GBPJPY +2.96 vs **293128383** XAU -0.94 (asset-split law)
  - A3 XAU spring vs sub_mid: **291794419** +3.02 vs **293128383** reused

### Phase 3 — determinism

- Same 60 entry-legal state payloads ×2 → identical Choice/Score/Noul (allow HTTP jitter only if TypeSafe offline; then stub-compare like `JEV_WIN_LOSE_LEARN_determinism.json`).
- Anti-oracle: serialize the state sent to `harvest_strike` / `admit`; **assert `miss_type` absent at top-level**.

### Phase 4 — CF replay metrics (harvest labels as **counterfactual size**, not live)

Replay the 60 closes with **entry-legal** harvest Choice mapped to size_mult:

| Choice | size_mult |
|---|---|
| A_STAND_DOWN | 0.0 |
| B_SIZE_HALF | 0.5 |
| C_SIZE_TRIM | 0.75 |
| D_FULL | 1.0 |
| E_KEEP_CAP | 1.0 (cap concurrent=1; no boost) |
| jev_dark / ABSTAIN | 1.0 fail-closed (do not stand down from dark) |

Report vs frozen baselines (cite, do not overwrite):

| policy | n | sumR | note |
|---|---:|---:|---|
| A raw tape | 60 | **-38.5948** | `JEV_TRAIN_PIVOT` |
| B learn-soft (miss leak) | 60 | -4.1962 | **do not copy — leaks miss_type** |
| C oracle labels | 60 | +8.9314 | upper bound; **not anti-oracle** |
| C entry-fit hold 70/30 | hold n=18 | **+3.3063** | anti-oracle |
| CF_D hold 70/30 | hold n=18 | **+4.713** | cite this, not full +11.17 |
| CF_E hold 70/30 | hold | **+5.012** | frozen — do not spawn E variants |

**Harvest-label CF** (this prove) must use ticket 70/30 split (train 42 / hold 18) matching anti-oracle artifacts:

- `/workspace/gtos/close_loop/war_room_20260920/jev_cf_D_anti_oracle_70_30.json`
- `/workspace/gtos/close_loop/war_room_20260920/JEV_TRAIN_ANTIORACLE_20260920.json`

### Phase 5 — Module_ATR yearfold replay (separate scoreboard)

Replay B/C year cards with **R_Module_ATR only**. Metrics: n, sumR_Module_ATR, WR, le0 years, fire rate = years with Choice != A_STAND_DOWN.

Do **not** add Module_ATR sumR to Challenge sumR.

---

## 4. Metrics to publish (receipt fields)

| metric | definition |
|---|---|
| `n` | harvested closes (expect 60 from scoreboard; report skip reasons) |
| `n_pairs` | enumerated + Jev-accepted pairs |
| `n_unpaired` | remainder |
| `n_jev_dark` | evaluate skip/error |
| `n_ok_wins_preserved` | of 7 ok_wins, how many got A_STAND_DOWN (false_abstain) |
| `sumR_challenge` | Σ R_Challenge_book × size_mult (hold split) |
| `maxDD_challenge` | running sum min on hold, Challenge R only |
| `fire_rate` | fraction size_mult>0 on hold |
| `sumR_Module_ATR` | yearfold only; separate |
| `n_lens_dishonest` | must be 0 |
| `n_news_invented` | must be 0 |
| `n_xau_substituted` | non-XAU wearing XAU tape; must be 0 |
| `determinism_pass` | bool |

Asset split always published separately (INDEX/XAU/FX/CRYPTO). CRYPTO n=3 UNDERPOWERED — do not invent mean_R beyond tape.

---

## 5. Gates

### 5.1 Sidecar PROVED_SHADOW (harvest emit)

All must hold:

- `n_lens_dishonest=0` · `n_news_invented=0` · `n_xau_substituted=0`
- `tickets_invented=false`
- XAGUSD n_pairs=0
- EXPOST absent from strike/admit state
- determinism PASS on stub or TypeSafe×2
- `never_broker_place=true` in every row
- `GTOS_JEV_TRAIN_HARVEST_APPLY` still 0
- ok_wins: false_abstain on hold **= 0** preferred; if >0, report and do not APPLY priors

### 5.2 Harvest labels as Policy C priors (live fire rate) — NOT this session

Only after 5.1 **and**:

- Hold 70/30 `sumR_challenge` ≥ CF_D hold **+4.713** **or** ≥ C entry-fit hold **+3.306**, with fire_rate not higher than CF_D unless sumR **and** maxDD both improve
- n_hold ≥ 18 (current split) — if tape grows, freeze a new split in a receipt
- Chair ENFORCE/LABEL verb
- Still no place/remint/flatten from harvest
- Do not flip `GTOS_JEV_SLEEVE_SELECT_APPLY`

### 5.3 Place Choice APPLY

Out of scope for harvest APPLY. `harvest_place_would_have` remains LABEL. Sessions 01/14 + Challenge hist vs writer actuals.

---

## 6. Fire-rate warning

Harvest Choice A_STAND_DOWN on hold reduces fire rate. That is the point of CF_D — but **jev_dark must not stand down** (fail-closed admits-as-today). Policy C live already refuses on `full_state_dark` when `GTOS_JEV_POLICY_C_APPLY=1`; harvest must not double-refuse from EXPOST miss.

---

## 7. Blockers (honest)

| blocker | impact |
|---|---|
| occupancy concurrent missing on 60 | `occupancy_ok` Noul untrainable — STATE_MISSING |
| corr_cluster missing on 60 | `corr_hold` untrainable |
| regime_tag null on 60 | null_ok; no S14 invent; VPS hydrate skipped |
| CLOSE_LOOP_BATCH_V1 per_ticket_labels=false | cannot expand n=48 aggregates into invented tickets |
| TypeSafe key / budget on harvest process | if absent, Phase 1 emits jev_dark; 5.1 can still pass lens/news/identity; 5.2 blocked |
| VPS Admin unreachable | cannot confirm live env flags this seat |
| Module_ATR = geometry proxy | year twins remain SCORE LANE research; not live TradeIntent |

---

## 8. Run sketch (box — no broker)

```bash
# OUT-only; do not land. Example once Chair copies sketch into src/.
export GTOS_JEV_TRAIN_HARVEST_SHADOW=1
export GTOS_JEV_TRAIN_HARVEST_CALL=1
export GTOS_JEV_TRAIN_HARVEST_APPLY=0
export GTOS_JEV_MAX_CALLS=500000
# NEVER: GTOS_JEV_SLEEVE_SELECT_APPLY=1
# python -m ...  # not executed this session against broker
```

This session does **not** run the replay (design + patch drafts only). Chair executes after land.

---

## 9. Success receipt shape (when Chair runs)

```
schema: gtos.jev.train_row_harvest.hist_prove.v1
login: 0
n, n_pairs, n_unpaired, n_jev_dark
sumR_challenge_hold, maxDD_challenge_hold, fire_rate_hold, n_hold
sumR_Module_ATR_yearfold (separate)
false_abstain_ok_wins
n_lens_dishonest, n_news_invented, n_xau_substituted
determinism_pass
verdict: PROVED_SHADOW | NOT_PROVED
ready_to_apply: false
place: false
```
