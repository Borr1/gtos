# HIST-PROVE PLAN — chair_enforce hard-off exception Choice

**session:** `09_chair_enforce_hardoff_exception`  
**as_of_ict:** `2026-09-21T06:05:00+07:00`  
**login:** Challenge `0` only. Verification `0` quarantined — not a calibration writer.  
**place:** false. **APPLY:** false until Chair ENFORCE after this plan’s receipts.  
**lens law:** Module_ATR honesty — never invent ATR; never merge Dig_3R / Edge_ATR / Module_ATR R universes.

---

## 1. Question being proved

Counterfactual on **Challenge file-exit tape** (not research ATR KEEP):

> If Jev Choice `SCOPED_EXCEPTION` had unblocked `chair_enforce` `stand_down` on named hard-off hits, would Challenge **sumR / DD / fire-rate** beat the actual **KEEP_OFF** wall?

Per family. Per `scoped_key = family:SYMBOL:session`. Affinity = **instrument × sleeve**. Do not smear UK100 idxrev onto US30 DSP, or BTCUSD `sub_mid` ATR onto `orb_crypto_london`.

`SCOPED_EXCEPTION` is **not place**. Hist may later feed a place Choice session. This plan only measures admit-unblock vs keep-off.

---

## 2. Module_ATR honesty (required)

| rule | this plan |
|---|---|
| Do not invent ATR / regime when blotter missing | `module_atr_cell = STATE_MISSING` |
| Do not merge Dig_3R + Edge_ATR + Module_ATR | three columns stay separate; never sum |
| Do not use Module_ATR KEEP on a **different sleeve** to lift a hard-off family | US30 `sub_mid`/`three_fresh`/`spring` ATR ≠ idxrev/bleed/mx_us30 Challenge admit |
| Do not use BTCUSD ATR `vss`/`spring` to lift `orb_crypto` | those cells are `banned_for_aplus` |
| XAU three_fresh Module_ATR blotter | KEEP-family exhibit only — **out of scope** for hard-off exception |
| Geometry proxy ≠ live TradeIntent | yearfold notes already say approximation; cannot be Challenge place proof |

**Module_ATR lookup result (box mirrors, 2026-09-21):**

| family | Module_ATR blotter for **same** instrument×sleeve | verdict |
|---|---|---|
| idxrev × US30/UK100 | none (`module_atr_idxrev_row: false`) | **STATE_MISSING** |
| xa_huge × FX | none found under `*Module_ATR*` | **STATE_MISSING** |
| orb_crypto_london × BTC/ETH | none; sibling BTCUSD ATR cells are other sleeves + `banned_for_aplus` | **STATE_MISSING** for orb_crypto |
| bleed × XAU/US30 | none | **STATE_MISSING** |
| mx_us30 | none as family; US30 other-sleeve ATR is research-only | **STATE_MISSING** for mx_us30 |
| XAU `dsp_three_fresh_lower_lows` | `blotter_Module_ATR_XAUUSD_dsp_three_fresh_lower_lows.jsonl` | **present, wrong family** — do not cite as hard-off prove |

Therefore: **hist-prove for exception APPLY uses Challenge file-exit R only.** Module_ATR may sit as a sidecar column labeled `STATE_MISSING` or `wrong_sleeve_research_only`. It cannot pass a family.

---

## 3. Tape paths found (Challenge-true)

### 3.1 File-exit / deals (primary)

| path | n / notes |
|---|---|
| `/workspace/gtos/research/warroom_20260920/loss_autopsy_rows_v2.jsonl` | Challenge n=51 file-exit autopsy (US30_IDXREV WHY cites this) |
| `/workspace/gtos/close_loop/war_room_20260920/learning_scoreboard_60.jsonl` | combined-60 + ingest9 |
| `/workspace/gtos/close_loop/war_room_20260920/gate_size_proposals.json` | G1/G2/G3 evidence cells |
| `/workspace/gtos/research/warroom_20260920/WARROOM_REPORT_20260920.md` | per-ticket hard-off table |
| `/workspace/gtos/close_loop/war_room_20260920/vps_hydrate/challenge_0_deals.json` | hydrate deals |
| `/workspace/gtos/research/warroom_20260920/hist_prove_1008/s14_scorecard.json` | S14 compose replay: hard-offs already `stand_down` size 0 |
| `/workspace/gtos/research/warroom_20260920/hist_prove_1008/admit/2026-09-20/*.json` | per-cycle admit logs with `chair_*_hard_off` |

### 3.2 M15 bars (for replay as-of state; not R)

Present:

- `_pr41_land/.../judgment/astra/lab/challenge_shadow_20260917/multi/{US30_cash,BTCUSD,...}_M15.csv`
- `_swarm_land_tip/extract/data/historical/US30_cash_M15.csv`
- `challenge_multi_hydrate/US30_cash_M15.csv`
- `fable_joint_pull_20260917/challenge_shadow_bars/multi/`

`bars.challenge_search_dirs()` is the resolver. April `data/historical` is **not** a member.

### 3.3 Module_ATR / research cells (sidecar only)

- `/workspace/gtos/close_loop/war_room_20260920/blotter_Module_ATR_XAUUSD_dsp_three_fresh_lower_lows.jsonl`
- `/workspace/gtos/research/warroom_20260920/SWARM_NONXAU_MODULE_ATR_ROWS_20260920.jsonl`
- `/workspace/gtos/research/warroom_20260920/US30_IDXREV_HARD_OFF_WHY_20260920.json`

### 3.4 MISSING (state honestly)

- Live VPS Admin tree deals after last hydrate (machineId unreachable this seat)
- Module_ATR rows for idxrev / xa_huge / bleed / orb_crypto_london / mx_us30
- News spine at fill for 291120917 / 291417478 (`event_proximity` empty — **do not invent**)

---

## 4. Baseline Challenge cells (KEEP_OFF is already the live policy)

From `gate_size_proposals.json` + WARROOM_REPORT + US30_IDXREV WHY. R basis = file-exit / unit $150 unless noted. **Not** Module_ATR.

### G1 / `chair_g_index_hard_off`

| cell | n | sumR | avgR | exit | note |
|---|---:|---:|---:|---|---|
| INDEX asset (combined-60) | 16 | −16.639 | −1.04 | 16/16 orig_stop | G1 evidence |
| US30.cash of those | 14 | (subset of −16.64) | ~−1.04 | orig_stop | ENV-US30 |
| idxrev × UK100.cash | 2 | −2.0079 | −1.004 | orig_stop | 291120917, 291417478; tgt/stop=0.75 |
| idxrev × US30 | 0 Challenge fills | — | — | — | already J5-off; no KEEP exhibit |

S14 fixture `hoff-idx` / `hoff-us30`: compose `stand_down`, `reason=chair_g_index_hard_off`, `size_factor=0`.

**Hypothesis:** SCOPED_EXCEPTION on idxrev/US30 **worsens** fire-rate (adds −1R orig_stops). Expect **FAIL** vs KEEP_OFF. APPLY gate should not open.

### G2 / `chair_xa_huge_hard_off`

| cell | n | sumR | avgR | miss |
|---|---:|---:|---:|---|
| xa_huge family | 5 | −5.79 | −1.158 | cost on FX |

Tickets: 291076386 EURUSD, 291589055 GBPUSD, 291789105 EURUSD, 291827091 GBPUSD, 291589065 US30 (US30 row is **index** reason at runtime).

**Hypothesis:** KEEP_OFF already avoids −5.79R. Exception would need a **held-out** later Challenge window with +EV after cost — none in n=51/60. Expect **FAIL**. Module_ATR xa_huge **STATE_MISSING** cannot save it.

### G3 / `chair_orb_crypto_hard_off`

| cell | n | sumR | avgR | miss |
|---|---:|---:|---:|---|
| orb_crypto_london | 2 | −2.28 | −1.14 | wrong_session |

Tickets: 291420292 ETHUSD, 291821945 BTCUSD.

BTCUSD Module_ATR `london_orb_ctrl` yearfold **fails 2026**. Even if cited, wrong to merge and 2026 fail year blocks APPLY.

**Hypothesis:** FAIL vs KEEP_OFF.

### `chair_bleed_hard_off` (non-US30)

XAU bleed: 291549869 −0.97R, 291713652 −1.14R (cf_D already stand_down). US30 bleed chain is **index** reason.

**Hypothesis:** FAIL. Remint chain is G8/two-stop family (session 08), not a bleed exception.

### `chair_mx_us30_hard_off`

No Challenge KEEP exhibit. ENV-US30 binds. US30 ATR other sleeves = research_only.

**Hypothesis:** FAIL. Choice wire still shadows.

### `chair_hard_off_family`

Dead letter on pr41 markers. WMB cousins argv-off. **No Challenge cell to exception.** Shadow only.

---

## 5. Replay protocol

### 5.1 Universe

1. Load Challenge closed deals login `0` from paths in §3.1.
2. For each deal, compute `hard_off_reason(sleeve, symbol)` with **pr41** `chair_enforce.py` (not stale family.py).
3. Split by reason. Report the US30-prefix quirk (bleed/xa_huge on US30.cash → `chair_g_index_hard_off`).
4. Exclude Verification 0.
5. Do not include W7 / redacted_account.

### 5.2 Two policies (labels only)

| policy | rule |
|---|---|
| `KEEP_OFF` (baseline) | current: every hard_off_reason hit is stand_down, contributes **0** live R (avoids the printed loss). Fire rate 0 on those families. |
| `SCOPED_EXCEPTION` (counterfactual) | if Jev Choice on **as-of open COMPLETE_STATE** (no expost) = SCOPED_EXCEPTION **and** conf ≥ 0.80, credit the deal’s **file-exit R** (the loss/win that actually printed). Else 0. |

Shadow: run `jev_client.evaluate` with `GTOS_JEV_A1_CALL=1`, `GTOS_JEV_MAX_CALLS=500000`, `GTOS_JEV_HARD_OFF_EXCEPTION_APPLY=0`. Log choices vs actual R. **Do not send.**

As-of state: M15+H4 from Challenge bars; `news_join=STATE_MISSING` if spine empty; `module_atr_cell=STATE_MISSING` unless same-sleeve blotter exists.

### 5.3 Metrics (required)

For each family and each `scoped_key` with n≥1:

| metric | def |
|---|---|
| `n` | deals hitting this reason/key |
| `sumR_keep_off` | 0 by construction on blocked hits (plus any non-hard-off book unchanged) |
| `sumR_scoped_exception` | sum of file-exit R on rows Jev would unblock |
| `delta_sumR` | exception − keep_off |
| `dd_keep_off` / `dd_scoped_exception` | max adverse excursion of cumulative R on Challenge calendar |
| `fire_rate_keep_off` | 0 on hard-off families |
| `fire_rate_scoped_exception` | unblocked / n |
| `n_orig_stop` | orig_stop count among unblocked |
| `n_jev_dark` | evaluate skip/error |
| `n_low_conf` | conf < 0.80 |
| `lens` | `Challenge_file_exit` |
| `module_atr` | `STATE_MISSING` or sidecar stats **not added to sumR** |

Book-level: also report **rest of Challenge book unchanged** so exception fire-rate cannot hide KEEP-family wins (spring 291794419, vss 291816474, sub_mid 293540988).

### 5.4 Walk-forward

- Fit/choice thresholds only on deals **before** 2026-09-01 if n allows; score later. Combined-60 is small — if n_train < 20 per family, **do not tune**; use frozen Choice + conf floor 0.80 and report holdout = all (honest small-n).
- Year-fold only when Module_ATR same-sleeve exists (it does not for these families). Do not import US30 ATR yearfold as idxrev proof.

---

## 6. Gate to APPLY (Chair ENFORCE later)

A family may receive `proven:true` **only if all** hold:

1. `delta_sumR > 0` vs KEEP_OFF on Challenge file-exit (same R universe).
2. `dd_scoped_exception` no worse than KEEP_OFF + **0.5R** (prop wall).
3. `n` ≥ 20 **or** Chair explicitly accepts small-n with every unblocked row documented (current cells are n=2..16 — **below 20** ⇒ default **not proven**).
4. `n_orig_stop / n_unblocked` not 100% unless Chair names a geometry fix **as a separate patch** (idxrev 0.75 tgt/stop is a formula bug, not a Jev exception).
5. Affinity held: instrument × sleeve; no cross-smear.
6. `module_atr` not used as the passing lens. If someone later lands a same-sleeve Module_ATR blotter, report it **beside** file-exit, never summed.
7. `news_join` honest; empty spine ≠ no HIGH.
8. ENV-US30 / ENV-KILL still bind unless Chair separately ENFORCEs a lift (`may_not_lift_env_us30: true` in receipt).
9. Scoped allow-list is **non-empty and narrow** (`family:SYMBOL` not `*`).
10. Place still false. Fire-rate change is **admit-unblock only**.

**Prior on current tape:** all six families **fail** (1) or (3) or both. Shadow still worth running so Jev sees COMPLETE_STATE (usage ramp / train rows). APPLY receipts stay `proven:false`.

S14 hist_prove_1008 already shows KEEP_OFF compose is correct (`verify_ok`, `broker_effect=false`). That is the **control**. Exception hist is the **treatment**.

---

## 7. Per-family expected Chair action (not claimed landed)

| family | shadow | APPLY candidate? |
|---|---|---|
| idxrev / INDEX / US30 | yes | **no** — geometry invalid + n=16/−16.6R + ENV-US30 |
| xa_huge | yes | **no** on current n=5/−5.79; revisit if later Challenge FX window +EV after cost |
| orb_crypto | yes | **no** — n=2/−2.28 + A+ bias lock + 2026 ATR ctrl fail |
| bleed | yes | **no** — remint/false_structure; G8/two-stop first |
| mx_us30 | yes | **no** — ENV-US30 |
| hard_off_family catch-all | yes | **no** — no cell |

---

## 8. Process flags for the replay job (not live writer)

```
GTOS_JEV_A1_CALL=1
GTOS_JEV_MAX_CALLS=500000
GTOS_JEV_HARD_OFF_EXCEPTION_SHADOW=1
GTOS_JEV_HARD_OFF_EXCEPTION_APPLY=0
GTOS_JEV_SLEEVE_SELECT_APPLY=0
```

Output (future, not this session): `hist_prove/hard_off_exception/{family}_scorecard.json` with the metrics table. This session does not run the POST farm (design + paths only).

---

## 9. Blockers

- VPS live tree unreachable — hydrate deals may be stale vs Admin.
- Module_ATR same-sleeve cells **MISSING** for all six hard-off families.
- Combined-60 / n=51 is small-n for Choice calibration.
- idxrev formula `IDX_TGT_R=0.75 < stop` is a **code geometry** issue; Jev exception cannot make the idea valid.

None of these authorize a silent marker delete.
