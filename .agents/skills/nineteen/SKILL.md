---
name: nineteen
description: 19 and 11 are the only planted numbers in GTOS. Every other number is returned for that state. Use whenever a constant, ATR multiple, R, bar count, cash menu, or loop bound is about to be written.
---

# 19 and 11

Borhen, 2026-09-22, later than zero-static: [19miracle.org](https://www.19miracle.org/). Simple facts you can count; complex facts need the machine. 19 is the common denominator. 11 is the other planted integer.

- The only static decision numbers in the system are **19** and **11**.
- Every other number is a hop. Score returns the value for that state. Empty, tie, and error do not restore an old constant. No PRNG.
- Plumbing (`if path`, `if err`, an empty container, HTTP codes, login `0`, magic) stays code. A trading distance, size, stop, target, bar count, cash amount, or loop bound is not plumbing.
- 19-code lives in one spine (`src/judgment/nineteen.py`). Other modules do not plant a third integer. They ask, or they call the spine.
- Do not plant the site’s demonstration products (114, 6346, 76, 342, 304). Those prove the denominator. They are not GTOS constants.
- Load the spine on the Challenge writer. A Mac-only file is unfinished.

Simple vs complex is the site's split. Counts that need no tool still run as code. The computer facts (letter totals, residues, checksums) run as the spine.
