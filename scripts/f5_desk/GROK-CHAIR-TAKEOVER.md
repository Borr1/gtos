# F5 CHAIR — host pack (paths, landmines, last card)

**This file is not the constitution.** Send / read
`GROK-CHAIR-PASTE.md` first. That paste is later owner word.

**Withdrawn in this file (Cursor cage, 2026-08-27 morning, owner rejected):**
- “redacted_account is chief-of-staff only and does not sit F5”
- “Grok Bots are context suppliers, never money-path / no VPS”
- Pre-written kill list of the other Grok rooms as a first job
- “Do not spawn a VPS Codex” read as “you may not judge from the VPS”

Owner then installed Grok Bot **on the VPS**. redacted_account and the other Grok Bots
already have full VPS access. That is intended. One chair judges money —
do not run five chat rooms arguing the same ticket — but you are allowed
on the host, and you sit the book. Clean unproductive rooms **after** you
understand, not from the list at the bottom of an older draft.

Read Fable first, then the later Cursor session. Chronology is in the paste.
The rest of this file is **host paths, burned mechanics, and a 23:16Z snapshot**.

## Architecture (current)

| layer | who | does |
|---|---|---|
| **Chair** | redacted_account / the Grok Bot that took this paste — already on the VPS | judgment, cadence, HOLD, take, tighten, self-heal, harvest, ship |
| **Hands** | same VPS `host-local\redacted_host\repo` on `f5-live` | MT5, desk card, wake host, shim, book laws |
| **Context files** | optional extras | `judgment/briefs/*` with `as_of_utc` + `valid_until_utc`. Stale = absent. Never a HOLD. |

Do not spawn a **second** judge because you are scared. You are the chair.

## Harvest (do this first, then operate)

Read Fable first (what happened), then Cursor (later; facts and host, not the cage).
You should be able to name the four roots and the current tickets without guessing.

1. Fable chair: `26e52aea-28e5-4d41-a1e0-f05ce96751d3`
   Docs if the JSONL is gone: `docs/audits/fable-20260826/TAKEOVER-20260826.md`,
   `CHAIR-LAWS-20260826.md` (day-26 numbers historical), `SYNTHESIS-20260826.md`,
   `docs/audits/fable-20260825/OWNER-GRANT-20260825.md`,
   `docs/audits/fable-20260825/GROK-DESK-PACKAGE.md` (2026-08-25 design; no-VPS is dead)
2. Fable analysis: `4b4e1143-96e3-46a9-acaf-05488ad824fa`
3. Later Cursor session: `94719da6-921b-4b04-b6c6-b67d5bd10839`
   Mac: `/Users/borr/.cursor/projects/Users-borr-GTOSActive-worktrees-governor-hole-20260824/agent-transcripts/94719da6-921b-4b04-b6c6-b67d5bd10839/94719da6-921b-4b04-b6c6-b67d5bd10839.jsonl`
4. Skill: `~/.claude/skills/f5-chair/SKILL.md` + `reference.md`
5. Charter: `scripts/f5_desk/JUDGE-CHARTER.md` (resident judge — you are senior)
6. This file, on VPS after land: `scripts/f5_desk/GROK-CHAIR-TAKEOVER.md`

Then decide what of the old rooms is actually unproductive. One chair
judges money. File-only briefs are optional. A stale brief is absent.
It never HOLDs. Do not execute a pre-written kill list.

## Account (do not invent a different one)

- FTMO Verification login **0**, magic **0**, ns `operator`
- Pass = balance **$105,000**. Unit **$250**. Do not enlarge because the gap is large.
- ≥$102k → $350 pre-approved (ceremony in skill `reference.md`). $75 down-trigger **vetoed**.
- Token `d9872ae4…` to 2026-09-24. Do not remint unless you change argv/config.
- Day net = balance now − balance at last **22:00 UTC** reset.
  Baseline 2026-08-26 22:00Z: **$97,766.53**. −$1,500 stops **new** risk only.
- Live truth is the VPS tree. Ship from `f5max-ship`, never `governor-hole`.
- Never checkout VPS `config/agent_config.yaml` (dirty, token-bound).
- Production W7 tags are not this chair.

## Owner word (later word wins)

- Full standing approval to sit live judgment and manage. Owner is veto.
- Quality over root causes. Do not hide blockers with hop scripts.
- Halt-appetite for *all* new risk is **lifted**. Named HOLDs only.
- Isolated re-entry after a symbol **closes** is wanted. Second ticket on a
  **live** symbol is not. GBP LONG + EUR SHORT is **not** a same-direction stack.
- Discretionary manage on **fast families** (`dsp_*`, `xa_*`, fx, idxrev) when
  at a **good level** and the **live** tape has **stopped working**, short of TP.
- **NULL RULE** for `crypto`, `energy_agri`, `sub_xvol_pullback`,
  `metals_softband`, `vol_compression`, `mx_*`.
- Auto-BE at **+1.5R of original stop**. Lock must be **economic** (past
  round-trip cost + room), not price-at-entry. Overnight swap is a hold cost.
- Frozen/dead cash quote ≠ stall. Prefer `position.profit` + `symbol_info_tick`.
- Live ticket + risk-reducing = **direct MT5**. Verdicts/HOLDs via
  `shim.apply_verdict` as provider `grok-chair`.
- Encode **general laws**, not ticket anecdotes.
- If chat and a sit collide, **book first**.

## Named HOLDs

1. **USDJPY** rest of verification — every id, every sleeve. First line of any
   dump that shows USDJPY: `usdjpy_scrub.py`. Book law:
   `f5_standing_hold_reason` → `usdjpy_verification_hold_5pip_dsp_stop`.
2. **Occupied symbol** (position or pending). Isolated re-entry after flat PASSes.
3. Thin-hour crypto `asia_pdl_fade` (oil if it reprints before 05:00Z).
4. Same-currency **fade** extras: `asian_fade*` on EUR only if GBP is live, and
   the reverse. Not EUR vs GBP opposite-direction dsp.

## Four roots (do not workaround)

1. **Observation lies.** Dumps divide R by the **live** SL after BE. TICKs freeze.
   Sit hop used to miss pendings. Card law: `f5_r_versus_original` /
   `f5_latch_orig_sl`. Desk: `chair_desk.py --mt5`. A dump `R=19` on US30 after
   lock is garbage. Denom is orig SL 53570.32 (39.12 pts), not 53615.30.
2. **Price-at-entry auto-BE** on the running process until the book-law restart.
   Economic BE + orig resolve live in the tree; a one-off ticket tighten is not
   “implemented.”
3. **Same-symbol HOLD** was a chair script. Book law is in the tree; running
   process is USDJPY-only until code-only restart. Overlay until then:
   `chair_stack_hold.py`.
4. **Cadence was Cursor `sleep`.** Dies with the session. Durable host:
   `GTOS_F5_CHAIR_WAKE` via **`schtasks.exe`** (not `Register-ScheduledTask`;
   not `Start-Process`). You read `chair_wake.json` and sit.

## How to sit (after the host is on the VPS)

From the Mac:

```bash
VPS="${CODEX_HOME:-$HOME/.codex}/skills/vps-agent-sessions/scripts/vps-sessions"
"$VPS" ps --file /Users/borr/GTOSActive/worktrees/f5max-ship/scripts/f5_desk/chair_sit.ps1
"$VPS" --json get 'host-local\gtos-agent-sessions\chair_desk_proof.txt' /tmp/chair_desk_proof.txt --force
```

Or on the VPS: `chair_sit.ps1` → `chair_desk.py sit --mt5` + `chair_stack_hold.py`
+ `usdjpy_scrub.py`.

Read **PATH** lines (`R_orig`, tape, orig_sl) and **ORD** lines (pendings).
Ignore dump `R` after a lock. Ignore frozen TICK as a stall.

Wake when: M15 pre (90s) / post (75s); intra 180s only while a fast-family
ticket has a **live** quote; new deal; new pending; new intent on a flat
symbol; tape unfreeze; 22:00 reset; London 07:00Z / NY 12:00Z first 3 min.
Duplicate wake fingerprint within 90s is not a new sit.

Time bottlenecks (prepare, do not overfit):

- M15 close is when fills happen. Be on the slate **before** `:00/:15/:30/:45`.
- Intra only while fast-family tape is live. Drop it on NULL-rule and frozen cash.
- 22:00 UTC is the day-net reset — roll baseline then, do not keep yesterday’s −$2,161.
- Concurrent MT5 init can `INIT_FAIL (-6)`. Retry the proof. Do not restart books.
- host-admin: no `Write-Host` dumps (CLIXML). Upload `.ps1`. `schtasks.exe` only.
- Restart with opens = adoption rehydrate + late-chase. Persist orig SL first.

## Self-heal

- If dump R looks insane after BE → use orig ledger / `stop_prev` / 53570.32 on US30.
- If sit proof has no ORD lines → pull `orders_get`. Pendings are occupancy.
- If wake fingerprint repeats → do not re-sit.
- If judge cap is dark → stack HOLD still runs. You are senior.
- If session dies → `GTOS_F5_CHAIR_WAKE` + your native wakes continue.
- If extras appear on an occupied symbol → re-HOLD. Do not wait for a model.
- If orig is missing after restart → refuse to invent R from a lock.

## Trust vs do not trust

**Trust:** broker tickets (lots, entry, live SL/TP, `position.profit`); USDJPY
standing HOLD in the **running** book; same-symbol **open-position** send guard;
$250 / token / baseline $97,766.53; desk `R_orig` when orig is latched.

**Do not trust:** dump R after BE; slate `stop_now` / `locked_r` after a lock
(US30 slate still showed SL at entry, last_checked 22:06); frozen TICK;
sit proof without ORD; in-memory orig across restart until ledger + trade
record exist; live auto-BE until the book-law restart is proven; Cursor wakes.

## Live book at last sit (2026-08-26T23:16:03Z) — encode laws, not tickets

Desk `--mt5` proven. Wake host health `ok`. Orig SLs on all four trade records.

| | |
|---|---|
| balance / equity | **$97,752.71 / $98,032.55** |
| day_net | **−$13.82** |
| to_pass | **~$7,247** |
| positions | **4** (XAU limit filled) |

- **GER40 `179079162`** SHORT 1.55 @ 26259.74, orig 26397.66, TP 26156.3, `idxrev`.
  R_orig **−0.75**. Never a good level. **Leave.**
- **US30 `179105152`** LONG 6.48 @ 53609.44, orig **53570.32**, live SL 53615.30
  (lock), TP 53840.65, `xa_isolated_opposite`. R_orig **+2.65**. Dump `R=19`
  is the lock lie. Tape live, short of TP. **Leave.**
- **EURUSD `179108943`** SHORT 5.0 @ 1.16546, orig 1.16598, TP 1.16234.
  R_orig **−0.10**. **Leave** unless live stall short of 1.16234.
- **XAUUSD `179119323`** SHORT 0.41 filled ~23:15Z @ 4613.16, orig 4619.15,
  TP 4577.22, `dsp_isolated_*`. R_orig **−0.42**. Isolated first ticket.
  **Leave.** HOLD XAU extras. Isolated GBP after flat is wanted.

USDJPY: 0 position; HOLD every USDJPY id. Stay $250.

## After you understand (not a pre-kill)

Retire what is actually dead:

- Cursor `sleep` / `AGENT_LOOP_TICK_f5chair` as the cadence
- `~/.claude/skills/f5-chair/scripts/chair_desk.py` leftover hop
- `stack_hold_now.py` overlay **after** occupied HOLD is proven in the running process
- A **second** judge spawned because the chair got scared

Keep until you have a better reason than a file said so:

- This Grok Bot (Chair) — on the VPS, with full access
- Books, `GTOS_F5_JUDGE` (resident, junior), `GTOS_F5_USDJPY_WATCH`,
  `GTOS_F5_CHAIR_WAKE`
- Other Grok Bots if they earn their keep (file-only briefs are the safe
  extra pattern). Chat rooms that argue the same live ticket do not.

## First jobs after harvest (do not park)

1. Sit from `chair_wake.json` / M15. Book first.
2. Prove desk `--mt5` prints `R_orig`, `day_net`, **ORD** lines.
3. Keep orig ledger + trade-record `f5_original_stop` current before any restart.
4. Code-only book-law restart from `f5max-ship` (occupied HOLD + orig resolve +
   economic BE). Persist orig first. Post-verify: orig SLs survived; USDJPY +
   occupied HOLD is in the **running** process (`Select-String` / Python `in`,
   never `$show -notmatch`). Fast window: flag → stop → kill-by-ns → start.
   No remint. No config checkout.
5. After that restart, retire the chair HOLD overlay.
6. Keep sitting. Do not flatten because the day is red or the sit-count is high.

## Landmines

- Do not `git add -A`. Explicit paths only.
- Do not remint. Do not checkout `agent_config.yaml`.
- Composer can emit geometry-less intents → resident judge abstains → fail-open
  PASS. That is why standing HOLD must live in the book.
- Governor notional $100k vs real equity mixed-anchor flatten (07:57) — encode,
  do not hot-patch mid-sit.
- `Register-ScheduledTask` from host-admin is a silent no-op. Use `schtasks.exe`.
